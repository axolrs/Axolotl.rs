// Owner: PascalElixir / axolrs (GitHub org)
// File: Auto-generated tests for variable bindings.

use axolc_core::interpret;

#[test]
fn let_0() {
    let (out, diags) = interpret("fn main() let x = 0 print(x) end", 0);
    assert!(!diags.has_errors(), "let_0: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"0
");
}

#[test]
fn let_1() {
    let (out, diags) = interpret("fn main() let x = 1 print(x) end", 0);
    assert!(!diags.has_errors(), "let_1: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"1
");
}

#[test]
fn let_2() {
    let (out, diags) = interpret("fn main() let x = 2 print(x) end", 0);
    assert!(!diags.has_errors(), "let_2: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"2
");
}

#[test]
fn let_3() {
    let (out, diags) = interpret("fn main() let x = 3 print(x) end", 0);
    assert!(!diags.has_errors(), "let_3: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"3
");
}

#[test]
fn let_4() {
    let (out, diags) = interpret("fn main() let x = 4 print(x) end", 0);
    assert!(!diags.has_errors(), "let_4: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"4
");
}

#[test]
fn let_5() {
    let (out, diags) = interpret("fn main() let x = 5 print(x) end", 0);
    assert!(!diags.has_errors(), "let_5: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"5
");
}

#[test]
fn let_6() {
    let (out, diags) = interpret("fn main() let x = 6 print(x) end", 0);
    assert!(!diags.has_errors(), "let_6: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn let_7() {
    let (out, diags) = interpret("fn main() let x = 7 print(x) end", 0);
    assert!(!diags.has_errors(), "let_7: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn let_8() {
    let (out, diags) = interpret("fn main() let x = 8 print(x) end", 0);
    assert!(!diags.has_errors(), "let_8: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"8
");
}

#[test]
fn let_9() {
    let (out, diags) = interpret("fn main() let x = 9 print(x) end", 0);
    assert!(!diags.has_errors(), "let_9: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"9
");
}

#[test]
fn let_10() {
    let (out, diags) = interpret("fn main() let x = 10 print(x) end", 0);
    assert!(!diags.has_errors(), "let_10: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"10
");
}

#[test]
fn let_11() {
    let (out, diags) = interpret("fn main() let x = 11 print(x) end", 0);
    assert!(!diags.has_errors(), "let_11: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"11
");
}

#[test]
fn let_12() {
    let (out, diags) = interpret("fn main() let x = 12 print(x) end", 0);
    assert!(!diags.has_errors(), "let_12: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"12
");
}

#[test]
fn let_13() {
    let (out, diags) = interpret("fn main() let x = 13 print(x) end", 0);
    assert!(!diags.has_errors(), "let_13: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"13
");
}

#[test]
fn let_14() {
    let (out, diags) = interpret("fn main() let x = 14 print(x) end", 0);
    assert!(!diags.has_errors(), "let_14: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"14
");
}

#[test]
fn let_15() {
    let (out, diags) = interpret("fn main() let x = 15 print(x) end", 0);
    assert!(!diags.has_errors(), "let_15: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"15
");
}

#[test]
fn let_16() {
    let (out, diags) = interpret("fn main() let x = 16 print(x) end", 0);
    assert!(!diags.has_errors(), "let_16: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"16
");
}

#[test]
fn let_17() {
    let (out, diags) = interpret("fn main() let x = 17 print(x) end", 0);
    assert!(!diags.has_errors(), "let_17: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"17
");
}

#[test]
fn let_18() {
    let (out, diags) = interpret("fn main() let x = 18 print(x) end", 0);
    assert!(!diags.has_errors(), "let_18: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"18
");
}

#[test]
fn let_19() {
    let (out, diags) = interpret("fn main() let x = 19 print(x) end", 0);
    assert!(!diags.has_errors(), "let_19: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"19
");
}

#[test]
fn let_20() {
    let (out, diags) = interpret("fn main() let x = 20 print(x) end", 0);
    assert!(!diags.has_errors(), "let_20: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"20
");
}

#[test]
fn let_21() {
    let (out, diags) = interpret("fn main() let x = 21 print(x) end", 0);
    assert!(!diags.has_errors(), "let_21: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"21
");
}

#[test]
fn let_22() {
    let (out, diags) = interpret("fn main() let x = 22 print(x) end", 0);
    assert!(!diags.has_errors(), "let_22: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"22
");
}

#[test]
fn let_23() {
    let (out, diags) = interpret("fn main() let x = 23 print(x) end", 0);
    assert!(!diags.has_errors(), "let_23: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"23
");
}

#[test]
fn let_24() {
    let (out, diags) = interpret("fn main() let x = 24 print(x) end", 0);
    assert!(!diags.has_errors(), "let_24: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"24
");
}

#[test]
fn let_25() {
    let (out, diags) = interpret("fn main() let x = 25 print(x) end", 0);
    assert!(!diags.has_errors(), "let_25: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"25
");
}

#[test]
fn let_26() {
    let (out, diags) = interpret("fn main() let x = 26 print(x) end", 0);
    assert!(!diags.has_errors(), "let_26: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"26
");
}

#[test]
fn let_27() {
    let (out, diags) = interpret("fn main() let x = 27 print(x) end", 0);
    assert!(!diags.has_errors(), "let_27: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"27
");
}

#[test]
fn let_28() {
    let (out, diags) = interpret("fn main() let x = 28 print(x) end", 0);
    assert!(!diags.has_errors(), "let_28: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"28
");
}

#[test]
fn let_29() {
    let (out, diags) = interpret("fn main() let x = 29 print(x) end", 0);
    assert!(!diags.has_errors(), "let_29: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"29
");
}

#[test]
fn let_30() {
    let (out, diags) = interpret("fn main() let x = 30 print(x) end", 0);
    assert!(!diags.has_errors(), "let_30: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"30
");
}

#[test]
fn let_31() {
    let (out, diags) = interpret("fn main() let x = 31 print(x) end", 0);
    assert!(!diags.has_errors(), "let_31: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"31
");
}

#[test]
fn let_32() {
    let (out, diags) = interpret("fn main() let x = 32 print(x) end", 0);
    assert!(!diags.has_errors(), "let_32: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"32
");
}

#[test]
fn let_33() {
    let (out, diags) = interpret("fn main() let x = 33 print(x) end", 0);
    assert!(!diags.has_errors(), "let_33: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"33
");
}

#[test]
fn let_34() {
    let (out, diags) = interpret("fn main() let x = 34 print(x) end", 0);
    assert!(!diags.has_errors(), "let_34: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"34
");
}

#[test]
fn let_35() {
    let (out, diags) = interpret("fn main() let x = 35 print(x) end", 0);
    assert!(!diags.has_errors(), "let_35: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"35
");
}

#[test]
fn let_36() {
    let (out, diags) = interpret("fn main() let x = 36 print(x) end", 0);
    assert!(!diags.has_errors(), "let_36: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"36
");
}

#[test]
fn let_37() {
    let (out, diags) = interpret("fn main() let x = 37 print(x) end", 0);
    assert!(!diags.has_errors(), "let_37: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"37
");
}

#[test]
fn let_38() {
    let (out, diags) = interpret("fn main() let x = 38 print(x) end", 0);
    assert!(!diags.has_errors(), "let_38: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"38
");
}

#[test]
fn let_39() {
    let (out, diags) = interpret("fn main() let x = 39 print(x) end", 0);
    assert!(!diags.has_errors(), "let_39: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"39
");
}

#[test]
fn let_40() {
    let (out, diags) = interpret("fn main() let x = 40 print(x) end", 0);
    assert!(!diags.has_errors(), "let_40: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"40
");
}

#[test]
fn let_41() {
    let (out, diags) = interpret("fn main() let x = 41 print(x) end", 0);
    assert!(!diags.has_errors(), "let_41: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"41
");
}

#[test]
fn let_42() {
    let (out, diags) = interpret("fn main() let x = 42 print(x) end", 0);
    assert!(!diags.has_errors(), "let_42: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"42
");
}

#[test]
fn let_43() {
    let (out, diags) = interpret("fn main() let x = 43 print(x) end", 0);
    assert!(!diags.has_errors(), "let_43: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"43
");
}

#[test]
fn let_44() {
    let (out, diags) = interpret("fn main() let x = 44 print(x) end", 0);
    assert!(!diags.has_errors(), "let_44: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"44
");
}

#[test]
fn let_45() {
    let (out, diags) = interpret("fn main() let x = 45 print(x) end", 0);
    assert!(!diags.has_errors(), "let_45: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"45
");
}

#[test]
fn let_46() {
    let (out, diags) = interpret("fn main() let x = 46 print(x) end", 0);
    assert!(!diags.has_errors(), "let_46: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"46
");
}

#[test]
fn let_47() {
    let (out, diags) = interpret("fn main() let x = 47 print(x) end", 0);
    assert!(!diags.has_errors(), "let_47: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"47
");
}

#[test]
fn let_48() {
    let (out, diags) = interpret("fn main() let x = 48 print(x) end", 0);
    assert!(!diags.has_errors(), "let_48: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"48
");
}

#[test]
fn let_49() {
    let (out, diags) = interpret("fn main() let x = 49 print(x) end", 0);
    assert!(!diags.has_errors(), "let_49: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"49
");
}

#[test]
fn let_50() {
    let (out, diags) = interpret("fn main() let x = 50 print(x) end", 0);
    assert!(!diags.has_errors(), "let_50: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"50
");
}

#[test]
fn let_51() {
    let (out, diags) = interpret("fn main() let x = 51 print(x) end", 0);
    assert!(!diags.has_errors(), "let_51: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"51
");
}

#[test]
fn let_52() {
    let (out, diags) = interpret("fn main() let x = 52 print(x) end", 0);
    assert!(!diags.has_errors(), "let_52: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"52
");
}

#[test]
fn let_53() {
    let (out, diags) = interpret("fn main() let x = 53 print(x) end", 0);
    assert!(!diags.has_errors(), "let_53: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"53
");
}

#[test]
fn let_54() {
    let (out, diags) = interpret("fn main() let x = 54 print(x) end", 0);
    assert!(!diags.has_errors(), "let_54: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"54
");
}

#[test]
fn let_55() {
    let (out, diags) = interpret("fn main() let x = 55 print(x) end", 0);
    assert!(!diags.has_errors(), "let_55: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"55
");
}

#[test]
fn let_56() {
    let (out, diags) = interpret("fn main() let x = 56 print(x) end", 0);
    assert!(!diags.has_errors(), "let_56: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"56
");
}

#[test]
fn let_57() {
    let (out, diags) = interpret("fn main() let x = 57 print(x) end", 0);
    assert!(!diags.has_errors(), "let_57: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"57
");
}

#[test]
fn let_58() {
    let (out, diags) = interpret("fn main() let x = 58 print(x) end", 0);
    assert!(!diags.has_errors(), "let_58: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"58
");
}

#[test]
fn let_59() {
    let (out, diags) = interpret("fn main() let x = 59 print(x) end", 0);
    assert!(!diags.has_errors(), "let_59: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"59
");
}

#[test]
fn let_60() {
    let (out, diags) = interpret("fn main() let x = 60 print(x) end", 0);
    assert!(!diags.has_errors(), "let_60: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"60
");
}

#[test]
fn let_61() {
    let (out, diags) = interpret("fn main() let x = 61 print(x) end", 0);
    assert!(!diags.has_errors(), "let_61: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"61
");
}

#[test]
fn let_62() {
    let (out, diags) = interpret("fn main() let x = 62 print(x) end", 0);
    assert!(!diags.has_errors(), "let_62: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"62
");
}

#[test]
fn let_63() {
    let (out, diags) = interpret("fn main() let x = 63 print(x) end", 0);
    assert!(!diags.has_errors(), "let_63: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"63
");
}

#[test]
fn let_64() {
    let (out, diags) = interpret("fn main() let x = 64 print(x) end", 0);
    assert!(!diags.has_errors(), "let_64: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"64
");
}

#[test]
fn let_65() {
    let (out, diags) = interpret("fn main() let x = 65 print(x) end", 0);
    assert!(!diags.has_errors(), "let_65: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"65
");
}

#[test]
fn let_66() {
    let (out, diags) = interpret("fn main() let x = 66 print(x) end", 0);
    assert!(!diags.has_errors(), "let_66: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"66
");
}

#[test]
fn let_67() {
    let (out, diags) = interpret("fn main() let x = 67 print(x) end", 0);
    assert!(!diags.has_errors(), "let_67: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"67
");
}

#[test]
fn let_68() {
    let (out, diags) = interpret("fn main() let x = 68 print(x) end", 0);
    assert!(!diags.has_errors(), "let_68: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"68
");
}

#[test]
fn let_69() {
    let (out, diags) = interpret("fn main() let x = 69 print(x) end", 0);
    assert!(!diags.has_errors(), "let_69: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"69
");
}

#[test]
fn let_70() {
    let (out, diags) = interpret("fn main() let x = 70 print(x) end", 0);
    assert!(!diags.has_errors(), "let_70: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"70
");
}

#[test]
fn let_71() {
    let (out, diags) = interpret("fn main() let x = 71 print(x) end", 0);
    assert!(!diags.has_errors(), "let_71: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"71
");
}

#[test]
fn let_72() {
    let (out, diags) = interpret("fn main() let x = 72 print(x) end", 0);
    assert!(!diags.has_errors(), "let_72: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"72
");
}

#[test]
fn let_73() {
    let (out, diags) = interpret("fn main() let x = 73 print(x) end", 0);
    assert!(!diags.has_errors(), "let_73: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"73
");
}

#[test]
fn let_74() {
    let (out, diags) = interpret("fn main() let x = 74 print(x) end", 0);
    assert!(!diags.has_errors(), "let_74: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"74
");
}

#[test]
fn let_75() {
    let (out, diags) = interpret("fn main() let x = 75 print(x) end", 0);
    assert!(!diags.has_errors(), "let_75: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"75
");
}

#[test]
fn let_76() {
    let (out, diags) = interpret("fn main() let x = 76 print(x) end", 0);
    assert!(!diags.has_errors(), "let_76: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"76
");
}

#[test]
fn let_77() {
    let (out, diags) = interpret("fn main() let x = 77 print(x) end", 0);
    assert!(!diags.has_errors(), "let_77: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"77
");
}

#[test]
fn let_78() {
    let (out, diags) = interpret("fn main() let x = 78 print(x) end", 0);
    assert!(!diags.has_errors(), "let_78: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"78
");
}

#[test]
fn let_79() {
    let (out, diags) = interpret("fn main() let x = 79 print(x) end", 0);
    assert!(!diags.has_errors(), "let_79: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"79
");
}

#[test]
fn let_80() {
    let (out, diags) = interpret("fn main() let x = 80 print(x) end", 0);
    assert!(!diags.has_errors(), "let_80: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"80
");
}

#[test]
fn let_81() {
    let (out, diags) = interpret("fn main() let x = 81 print(x) end", 0);
    assert!(!diags.has_errors(), "let_81: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"81
");
}

#[test]
fn let_82() {
    let (out, diags) = interpret("fn main() let x = 82 print(x) end", 0);
    assert!(!diags.has_errors(), "let_82: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"82
");
}

#[test]
fn let_83() {
    let (out, diags) = interpret("fn main() let x = 83 print(x) end", 0);
    assert!(!diags.has_errors(), "let_83: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"83
");
}

#[test]
fn let_84() {
    let (out, diags) = interpret("fn main() let x = 84 print(x) end", 0);
    assert!(!diags.has_errors(), "let_84: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"84
");
}

#[test]
fn let_85() {
    let (out, diags) = interpret("fn main() let x = 85 print(x) end", 0);
    assert!(!diags.has_errors(), "let_85: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"85
");
}

#[test]
fn let_86() {
    let (out, diags) = interpret("fn main() let x = 86 print(x) end", 0);
    assert!(!diags.has_errors(), "let_86: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"86
");
}

#[test]
fn let_87() {
    let (out, diags) = interpret("fn main() let x = 87 print(x) end", 0);
    assert!(!diags.has_errors(), "let_87: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"87
");
}

#[test]
fn let_88() {
    let (out, diags) = interpret("fn main() let x = 88 print(x) end", 0);
    assert!(!diags.has_errors(), "let_88: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"88
");
}

#[test]
fn let_89() {
    let (out, diags) = interpret("fn main() let x = 89 print(x) end", 0);
    assert!(!diags.has_errors(), "let_89: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"89
");
}

#[test]
fn let_90() {
    let (out, diags) = interpret("fn main() let x = 90 print(x) end", 0);
    assert!(!diags.has_errors(), "let_90: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"90
");
}

#[test]
fn let_91() {
    let (out, diags) = interpret("fn main() let x = 91 print(x) end", 0);
    assert!(!diags.has_errors(), "let_91: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"91
");
}

#[test]
fn let_92() {
    let (out, diags) = interpret("fn main() let x = 92 print(x) end", 0);
    assert!(!diags.has_errors(), "let_92: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"92
");
}

#[test]
fn let_93() {
    let (out, diags) = interpret("fn main() let x = 93 print(x) end", 0);
    assert!(!diags.has_errors(), "let_93: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"93
");
}

#[test]
fn let_94() {
    let (out, diags) = interpret("fn main() let x = 94 print(x) end", 0);
    assert!(!diags.has_errors(), "let_94: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"94
");
}

#[test]
fn let_95() {
    let (out, diags) = interpret("fn main() let x = 95 print(x) end", 0);
    assert!(!diags.has_errors(), "let_95: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"95
");
}

#[test]
fn let_96() {
    let (out, diags) = interpret("fn main() let x = 96 print(x) end", 0);
    assert!(!diags.has_errors(), "let_96: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"96
");
}

#[test]
fn let_97() {
    let (out, diags) = interpret("fn main() let x = 97 print(x) end", 0);
    assert!(!diags.has_errors(), "let_97: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"97
");
}

#[test]
fn let_98() {
    let (out, diags) = interpret("fn main() let x = 98 print(x) end", 0);
    assert!(!diags.has_errors(), "let_98: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"98
");
}

#[test]
fn let_99() {
    let (out, diags) = interpret("fn main() let x = 99 print(x) end", 0);
    assert!(!diags.has_errors(), "let_99: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"99
");
}

#[test]
fn let_100() {
    let (out, diags) = interpret("fn main() let x = 100 print(x) end", 0);
    assert!(!diags.has_errors(), "let_100: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"100
");
}

#[test]
fn let_101() {
    let (out, diags) = interpret("fn main() let x = 101 print(x) end", 0);
    assert!(!diags.has_errors(), "let_101: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"101
");
}

#[test]
fn let_102() {
    let (out, diags) = interpret("fn main() let x = 102 print(x) end", 0);
    assert!(!diags.has_errors(), "let_102: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"102
");
}

#[test]
fn let_103() {
    let (out, diags) = interpret("fn main() let x = 103 print(x) end", 0);
    assert!(!diags.has_errors(), "let_103: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"103
");
}

#[test]
fn let_104() {
    let (out, diags) = interpret("fn main() let x = 104 print(x) end", 0);
    assert!(!diags.has_errors(), "let_104: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"104
");
}

#[test]
fn let_105() {
    let (out, diags) = interpret("fn main() let x = 105 print(x) end", 0);
    assert!(!diags.has_errors(), "let_105: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"105
");
}

#[test]
fn let_106() {
    let (out, diags) = interpret("fn main() let x = 106 print(x) end", 0);
    assert!(!diags.has_errors(), "let_106: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"106
");
}

#[test]
fn let_107() {
    let (out, diags) = interpret("fn main() let x = 107 print(x) end", 0);
    assert!(!diags.has_errors(), "let_107: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"107
");
}

#[test]
fn let_108() {
    let (out, diags) = interpret("fn main() let x = 108 print(x) end", 0);
    assert!(!diags.has_errors(), "let_108: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"108
");
}

#[test]
fn let_109() {
    let (out, diags) = interpret("fn main() let x = 109 print(x) end", 0);
    assert!(!diags.has_errors(), "let_109: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"109
");
}

#[test]
fn let_110() {
    let (out, diags) = interpret("fn main() let x = 110 print(x) end", 0);
    assert!(!diags.has_errors(), "let_110: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"110
");
}

#[test]
fn let_111() {
    let (out, diags) = interpret("fn main() let x = 111 print(x) end", 0);
    assert!(!diags.has_errors(), "let_111: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"111
");
}

#[test]
fn let_112() {
    let (out, diags) = interpret("fn main() let x = 112 print(x) end", 0);
    assert!(!diags.has_errors(), "let_112: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"112
");
}

#[test]
fn let_113() {
    let (out, diags) = interpret("fn main() let x = 113 print(x) end", 0);
    assert!(!diags.has_errors(), "let_113: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"113
");
}

#[test]
fn let_114() {
    let (out, diags) = interpret("fn main() let x = 114 print(x) end", 0);
    assert!(!diags.has_errors(), "let_114: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"114
");
}

#[test]
fn let_115() {
    let (out, diags) = interpret("fn main() let x = 115 print(x) end", 0);
    assert!(!diags.has_errors(), "let_115: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"115
");
}

#[test]
fn let_116() {
    let (out, diags) = interpret("fn main() let x = 116 print(x) end", 0);
    assert!(!diags.has_errors(), "let_116: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"116
");
}

#[test]
fn let_117() {
    let (out, diags) = interpret("fn main() let x = 117 print(x) end", 0);
    assert!(!diags.has_errors(), "let_117: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"117
");
}

#[test]
fn let_118() {
    let (out, diags) = interpret("fn main() let x = 118 print(x) end", 0);
    assert!(!diags.has_errors(), "let_118: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"118
");
}

#[test]
fn let_119() {
    let (out, diags) = interpret("fn main() let x = 119 print(x) end", 0);
    assert!(!diags.has_errors(), "let_119: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"119
");
}

#[test]
fn let_120() {
    let (out, diags) = interpret("fn main() let x = 120 print(x) end", 0);
    assert!(!diags.has_errors(), "let_120: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"120
");
}

#[test]
fn let_121() {
    let (out, diags) = interpret("fn main() let x = 121 print(x) end", 0);
    assert!(!diags.has_errors(), "let_121: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"121
");
}

#[test]
fn let_122() {
    let (out, diags) = interpret("fn main() let x = 122 print(x) end", 0);
    assert!(!diags.has_errors(), "let_122: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"122
");
}

#[test]
fn let_123() {
    let (out, diags) = interpret("fn main() let x = 123 print(x) end", 0);
    assert!(!diags.has_errors(), "let_123: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"123
");
}

#[test]
fn let_124() {
    let (out, diags) = interpret("fn main() let x = 124 print(x) end", 0);
    assert!(!diags.has_errors(), "let_124: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"124
");
}

#[test]
fn let_125() {
    let (out, diags) = interpret("fn main() let x = 125 print(x) end", 0);
    assert!(!diags.has_errors(), "let_125: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"125
");
}

#[test]
fn let_126() {
    let (out, diags) = interpret("fn main() let x = 126 print(x) end", 0);
    assert!(!diags.has_errors(), "let_126: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"126
");
}

#[test]
fn let_127() {
    let (out, diags) = interpret("fn main() let x = 127 print(x) end", 0);
    assert!(!diags.has_errors(), "let_127: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"127
");
}

#[test]
fn let_128() {
    let (out, diags) = interpret("fn main() let x = 128 print(x) end", 0);
    assert!(!diags.has_errors(), "let_128: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"128
");
}

#[test]
fn let_129() {
    let (out, diags) = interpret("fn main() let x = 129 print(x) end", 0);
    assert!(!diags.has_errors(), "let_129: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"129
");
}

#[test]
fn let_130() {
    let (out, diags) = interpret("fn main() let x = 130 print(x) end", 0);
    assert!(!diags.has_errors(), "let_130: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"130
");
}

#[test]
fn let_131() {
    let (out, diags) = interpret("fn main() let x = 131 print(x) end", 0);
    assert!(!diags.has_errors(), "let_131: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"131
");
}

#[test]
fn let_132() {
    let (out, diags) = interpret("fn main() let x = 132 print(x) end", 0);
    assert!(!diags.has_errors(), "let_132: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"132
");
}

#[test]
fn let_133() {
    let (out, diags) = interpret("fn main() let x = 133 print(x) end", 0);
    assert!(!diags.has_errors(), "let_133: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"133
");
}

#[test]
fn let_134() {
    let (out, diags) = interpret("fn main() let x = 134 print(x) end", 0);
    assert!(!diags.has_errors(), "let_134: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"134
");
}

#[test]
fn let_135() {
    let (out, diags) = interpret("fn main() let x = 135 print(x) end", 0);
    assert!(!diags.has_errors(), "let_135: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"135
");
}

#[test]
fn let_136() {
    let (out, diags) = interpret("fn main() let x = 136 print(x) end", 0);
    assert!(!diags.has_errors(), "let_136: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"136
");
}

#[test]
fn let_137() {
    let (out, diags) = interpret("fn main() let x = 137 print(x) end", 0);
    assert!(!diags.has_errors(), "let_137: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"137
");
}

#[test]
fn let_138() {
    let (out, diags) = interpret("fn main() let x = 138 print(x) end", 0);
    assert!(!diags.has_errors(), "let_138: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"138
");
}

#[test]
fn let_139() {
    let (out, diags) = interpret("fn main() let x = 139 print(x) end", 0);
    assert!(!diags.has_errors(), "let_139: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"139
");
}

#[test]
fn let_140() {
    let (out, diags) = interpret("fn main() let x = 140 print(x) end", 0);
    assert!(!diags.has_errors(), "let_140: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"140
");
}

#[test]
fn let_141() {
    let (out, diags) = interpret("fn main() let x = 141 print(x) end", 0);
    assert!(!diags.has_errors(), "let_141: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"141
");
}

#[test]
fn let_142() {
    let (out, diags) = interpret("fn main() let x = 142 print(x) end", 0);
    assert!(!diags.has_errors(), "let_142: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"142
");
}

#[test]
fn let_143() {
    let (out, diags) = interpret("fn main() let x = 143 print(x) end", 0);
    assert!(!diags.has_errors(), "let_143: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"143
");
}

#[test]
fn let_144() {
    let (out, diags) = interpret("fn main() let x = 144 print(x) end", 0);
    assert!(!diags.has_errors(), "let_144: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"144
");
}

#[test]
fn let_145() {
    let (out, diags) = interpret("fn main() let x = 145 print(x) end", 0);
    assert!(!diags.has_errors(), "let_145: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"145
");
}

#[test]
fn let_146() {
    let (out, diags) = interpret("fn main() let x = 146 print(x) end", 0);
    assert!(!diags.has_errors(), "let_146: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"146
");
}

#[test]
fn let_147() {
    let (out, diags) = interpret("fn main() let x = 147 print(x) end", 0);
    assert!(!diags.has_errors(), "let_147: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"147
");
}

#[test]
fn let_148() {
    let (out, diags) = interpret("fn main() let x = 148 print(x) end", 0);
    assert!(!diags.has_errors(), "let_148: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"148
");
}

#[test]
fn let_149() {
    let (out, diags) = interpret("fn main() let x = 149 print(x) end", 0);
    assert!(!diags.has_errors(), "let_149: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"149
");
}

#[test]
fn let_150() {
    let (out, diags) = interpret("fn main() let x = 150 print(x) end", 0);
    assert!(!diags.has_errors(), "let_150: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"150
");
}

#[test]
fn let_151() {
    let (out, diags) = interpret("fn main() let x = 151 print(x) end", 0);
    assert!(!diags.has_errors(), "let_151: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"151
");
}

#[test]
fn let_152() {
    let (out, diags) = interpret("fn main() let x = 152 print(x) end", 0);
    assert!(!diags.has_errors(), "let_152: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"152
");
}

#[test]
fn let_153() {
    let (out, diags) = interpret("fn main() let x = 153 print(x) end", 0);
    assert!(!diags.has_errors(), "let_153: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"153
");
}

#[test]
fn let_154() {
    let (out, diags) = interpret("fn main() let x = 154 print(x) end", 0);
    assert!(!diags.has_errors(), "let_154: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"154
");
}

#[test]
fn let_155() {
    let (out, diags) = interpret("fn main() let x = 155 print(x) end", 0);
    assert!(!diags.has_errors(), "let_155: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"155
");
}

#[test]
fn let_156() {
    let (out, diags) = interpret("fn main() let x = 156 print(x) end", 0);
    assert!(!diags.has_errors(), "let_156: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"156
");
}

#[test]
fn let_157() {
    let (out, diags) = interpret("fn main() let x = 157 print(x) end", 0);
    assert!(!diags.has_errors(), "let_157: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"157
");
}

#[test]
fn let_158() {
    let (out, diags) = interpret("fn main() let x = 158 print(x) end", 0);
    assert!(!diags.has_errors(), "let_158: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"158
");
}

#[test]
fn let_159() {
    let (out, diags) = interpret("fn main() let x = 159 print(x) end", 0);
    assert!(!diags.has_errors(), "let_159: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"159
");
}

#[test]
fn let_160() {
    let (out, diags) = interpret("fn main() let x = 160 print(x) end", 0);
    assert!(!diags.has_errors(), "let_160: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"160
");
}

#[test]
fn let_161() {
    let (out, diags) = interpret("fn main() let x = 161 print(x) end", 0);
    assert!(!diags.has_errors(), "let_161: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"161
");
}

#[test]
fn let_162() {
    let (out, diags) = interpret("fn main() let x = 162 print(x) end", 0);
    assert!(!diags.has_errors(), "let_162: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"162
");
}

#[test]
fn let_163() {
    let (out, diags) = interpret("fn main() let x = 163 print(x) end", 0);
    assert!(!diags.has_errors(), "let_163: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"163
");
}

#[test]
fn let_164() {
    let (out, diags) = interpret("fn main() let x = 164 print(x) end", 0);
    assert!(!diags.has_errors(), "let_164: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"164
");
}

#[test]
fn let_165() {
    let (out, diags) = interpret("fn main() let x = 165 print(x) end", 0);
    assert!(!diags.has_errors(), "let_165: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"165
");
}

#[test]
fn let_166() {
    let (out, diags) = interpret("fn main() let x = 166 print(x) end", 0);
    assert!(!diags.has_errors(), "let_166: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"166
");
}

#[test]
fn let_167() {
    let (out, diags) = interpret("fn main() let x = 167 print(x) end", 0);
    assert!(!diags.has_errors(), "let_167: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"167
");
}

#[test]
fn let_168() {
    let (out, diags) = interpret("fn main() let x = 168 print(x) end", 0);
    assert!(!diags.has_errors(), "let_168: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"168
");
}

#[test]
fn let_169() {
    let (out, diags) = interpret("fn main() let x = 169 print(x) end", 0);
    assert!(!diags.has_errors(), "let_169: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"169
");
}

#[test]
fn let_170() {
    let (out, diags) = interpret("fn main() let x = 170 print(x) end", 0);
    assert!(!diags.has_errors(), "let_170: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"170
");
}

#[test]
fn let_171() {
    let (out, diags) = interpret("fn main() let x = 171 print(x) end", 0);
    assert!(!diags.has_errors(), "let_171: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"171
");
}

#[test]
fn let_172() {
    let (out, diags) = interpret("fn main() let x = 172 print(x) end", 0);
    assert!(!diags.has_errors(), "let_172: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"172
");
}

#[test]
fn let_173() {
    let (out, diags) = interpret("fn main() let x = 173 print(x) end", 0);
    assert!(!diags.has_errors(), "let_173: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"173
");
}

#[test]
fn let_174() {
    let (out, diags) = interpret("fn main() let x = 174 print(x) end", 0);
    assert!(!diags.has_errors(), "let_174: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"174
");
}

#[test]
fn let_175() {
    let (out, diags) = interpret("fn main() let x = 175 print(x) end", 0);
    assert!(!diags.has_errors(), "let_175: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"175
");
}

#[test]
fn let_176() {
    let (out, diags) = interpret("fn main() let x = 176 print(x) end", 0);
    assert!(!diags.has_errors(), "let_176: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"176
");
}

#[test]
fn let_177() {
    let (out, diags) = interpret("fn main() let x = 177 print(x) end", 0);
    assert!(!diags.has_errors(), "let_177: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"177
");
}

#[test]
fn let_178() {
    let (out, diags) = interpret("fn main() let x = 178 print(x) end", 0);
    assert!(!diags.has_errors(), "let_178: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"178
");
}

#[test]
fn let_179() {
    let (out, diags) = interpret("fn main() let x = 179 print(x) end", 0);
    assert!(!diags.has_errors(), "let_179: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"179
");
}

#[test]
fn let_180() {
    let (out, diags) = interpret("fn main() let x = 180 print(x) end", 0);
    assert!(!diags.has_errors(), "let_180: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"180
");
}

#[test]
fn let_181() {
    let (out, diags) = interpret("fn main() let x = 181 print(x) end", 0);
    assert!(!diags.has_errors(), "let_181: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"181
");
}

#[test]
fn let_182() {
    let (out, diags) = interpret("fn main() let x = 182 print(x) end", 0);
    assert!(!diags.has_errors(), "let_182: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"182
");
}

#[test]
fn let_183() {
    let (out, diags) = interpret("fn main() let x = 183 print(x) end", 0);
    assert!(!diags.has_errors(), "let_183: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"183
");
}

#[test]
fn let_184() {
    let (out, diags) = interpret("fn main() let x = 184 print(x) end", 0);
    assert!(!diags.has_errors(), "let_184: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"184
");
}

#[test]
fn let_185() {
    let (out, diags) = interpret("fn main() let x = 185 print(x) end", 0);
    assert!(!diags.has_errors(), "let_185: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"185
");
}

#[test]
fn let_186() {
    let (out, diags) = interpret("fn main() let x = 186 print(x) end", 0);
    assert!(!diags.has_errors(), "let_186: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"186
");
}

#[test]
fn let_187() {
    let (out, diags) = interpret("fn main() let x = 187 print(x) end", 0);
    assert!(!diags.has_errors(), "let_187: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"187
");
}

#[test]
fn let_188() {
    let (out, diags) = interpret("fn main() let x = 188 print(x) end", 0);
    assert!(!diags.has_errors(), "let_188: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"188
");
}

#[test]
fn let_189() {
    let (out, diags) = interpret("fn main() let x = 189 print(x) end", 0);
    assert!(!diags.has_errors(), "let_189: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"189
");
}

#[test]
fn let_190() {
    let (out, diags) = interpret("fn main() let x = 190 print(x) end", 0);
    assert!(!diags.has_errors(), "let_190: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"190
");
}

#[test]
fn let_191() {
    let (out, diags) = interpret("fn main() let x = 191 print(x) end", 0);
    assert!(!diags.has_errors(), "let_191: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"191
");
}

#[test]
fn let_192() {
    let (out, diags) = interpret("fn main() let x = 192 print(x) end", 0);
    assert!(!diags.has_errors(), "let_192: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"192
");
}

#[test]
fn let_193() {
    let (out, diags) = interpret("fn main() let x = 193 print(x) end", 0);
    assert!(!diags.has_errors(), "let_193: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"193
");
}

#[test]
fn let_194() {
    let (out, diags) = interpret("fn main() let x = 194 print(x) end", 0);
    assert!(!diags.has_errors(), "let_194: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"194
");
}

#[test]
fn let_195() {
    let (out, diags) = interpret("fn main() let x = 195 print(x) end", 0);
    assert!(!diags.has_errors(), "let_195: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"195
");
}

#[test]
fn let_196() {
    let (out, diags) = interpret("fn main() let x = 196 print(x) end", 0);
    assert!(!diags.has_errors(), "let_196: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"196
");
}

#[test]
fn let_197() {
    let (out, diags) = interpret("fn main() let x = 197 print(x) end", 0);
    assert!(!diags.has_errors(), "let_197: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"197
");
}

#[test]
fn let_198() {
    let (out, diags) = interpret("fn main() let x = 198 print(x) end", 0);
    assert!(!diags.has_errors(), "let_198: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"198
");
}

#[test]
fn let_199() {
    let (out, diags) = interpret("fn main() let x = 199 print(x) end", 0);
    assert!(!diags.has_errors(), "let_199: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"199
");
}

#[test]
fn var_0() {
    let (out, diags) = interpret("fn main() var x = 0 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_0: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"1
");
}

#[test]
fn var_1() {
    let (out, diags) = interpret("fn main() var x = 1 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_1: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"2
");
}

#[test]
fn var_2() {
    let (out, diags) = interpret("fn main() var x = 2 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_2: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"3
");
}

#[test]
fn var_3() {
    let (out, diags) = interpret("fn main() var x = 3 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_3: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"4
");
}

#[test]
fn var_4() {
    let (out, diags) = interpret("fn main() var x = 4 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_4: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"5
");
}

#[test]
fn var_5() {
    let (out, diags) = interpret("fn main() var x = 5 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_5: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn var_6() {
    let (out, diags) = interpret("fn main() var x = 6 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_6: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn var_7() {
    let (out, diags) = interpret("fn main() var x = 7 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_7: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"8
");
}

#[test]
fn var_8() {
    let (out, diags) = interpret("fn main() var x = 8 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_8: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"9
");
}

#[test]
fn var_9() {
    let (out, diags) = interpret("fn main() var x = 9 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_9: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"10
");
}

#[test]
fn var_10() {
    let (out, diags) = interpret("fn main() var x = 10 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_10: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"11
");
}

#[test]
fn var_11() {
    let (out, diags) = interpret("fn main() var x = 11 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_11: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"12
");
}

#[test]
fn var_12() {
    let (out, diags) = interpret("fn main() var x = 12 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_12: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"13
");
}

#[test]
fn var_13() {
    let (out, diags) = interpret("fn main() var x = 13 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_13: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"14
");
}

#[test]
fn var_14() {
    let (out, diags) = interpret("fn main() var x = 14 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_14: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"15
");
}

#[test]
fn var_15() {
    let (out, diags) = interpret("fn main() var x = 15 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_15: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"16
");
}

#[test]
fn var_16() {
    let (out, diags) = interpret("fn main() var x = 16 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_16: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"17
");
}

#[test]
fn var_17() {
    let (out, diags) = interpret("fn main() var x = 17 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_17: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"18
");
}

#[test]
fn var_18() {
    let (out, diags) = interpret("fn main() var x = 18 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_18: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"19
");
}

#[test]
fn var_19() {
    let (out, diags) = interpret("fn main() var x = 19 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_19: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"20
");
}

#[test]
fn var_20() {
    let (out, diags) = interpret("fn main() var x = 20 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_20: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"21
");
}

#[test]
fn var_21() {
    let (out, diags) = interpret("fn main() var x = 21 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_21: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"22
");
}

#[test]
fn var_22() {
    let (out, diags) = interpret("fn main() var x = 22 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_22: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"23
");
}

#[test]
fn var_23() {
    let (out, diags) = interpret("fn main() var x = 23 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_23: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"24
");
}

#[test]
fn var_24() {
    let (out, diags) = interpret("fn main() var x = 24 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_24: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"25
");
}

#[test]
fn var_25() {
    let (out, diags) = interpret("fn main() var x = 25 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_25: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"26
");
}

#[test]
fn var_26() {
    let (out, diags) = interpret("fn main() var x = 26 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_26: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"27
");
}

#[test]
fn var_27() {
    let (out, diags) = interpret("fn main() var x = 27 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_27: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"28
");
}

#[test]
fn var_28() {
    let (out, diags) = interpret("fn main() var x = 28 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_28: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"29
");
}

#[test]
fn var_29() {
    let (out, diags) = interpret("fn main() var x = 29 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_29: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"30
");
}

#[test]
fn var_30() {
    let (out, diags) = interpret("fn main() var x = 30 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_30: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"31
");
}

#[test]
fn var_31() {
    let (out, diags) = interpret("fn main() var x = 31 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_31: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"32
");
}

#[test]
fn var_32() {
    let (out, diags) = interpret("fn main() var x = 32 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_32: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"33
");
}

#[test]
fn var_33() {
    let (out, diags) = interpret("fn main() var x = 33 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_33: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"34
");
}

#[test]
fn var_34() {
    let (out, diags) = interpret("fn main() var x = 34 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_34: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"35
");
}

#[test]
fn var_35() {
    let (out, diags) = interpret("fn main() var x = 35 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_35: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"36
");
}

#[test]
fn var_36() {
    let (out, diags) = interpret("fn main() var x = 36 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_36: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"37
");
}

#[test]
fn var_37() {
    let (out, diags) = interpret("fn main() var x = 37 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_37: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"38
");
}

#[test]
fn var_38() {
    let (out, diags) = interpret("fn main() var x = 38 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_38: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"39
");
}

#[test]
fn var_39() {
    let (out, diags) = interpret("fn main() var x = 39 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_39: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"40
");
}

#[test]
fn var_40() {
    let (out, diags) = interpret("fn main() var x = 40 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_40: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"41
");
}

#[test]
fn var_41() {
    let (out, diags) = interpret("fn main() var x = 41 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_41: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"42
");
}

#[test]
fn var_42() {
    let (out, diags) = interpret("fn main() var x = 42 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_42: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"43
");
}

#[test]
fn var_43() {
    let (out, diags) = interpret("fn main() var x = 43 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_43: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"44
");
}

#[test]
fn var_44() {
    let (out, diags) = interpret("fn main() var x = 44 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_44: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"45
");
}

#[test]
fn var_45() {
    let (out, diags) = interpret("fn main() var x = 45 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_45: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"46
");
}

#[test]
fn var_46() {
    let (out, diags) = interpret("fn main() var x = 46 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_46: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"47
");
}

#[test]
fn var_47() {
    let (out, diags) = interpret("fn main() var x = 47 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_47: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"48
");
}

#[test]
fn var_48() {
    let (out, diags) = interpret("fn main() var x = 48 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_48: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"49
");
}

#[test]
fn var_49() {
    let (out, diags) = interpret("fn main() var x = 49 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_49: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"50
");
}

#[test]
fn var_50() {
    let (out, diags) = interpret("fn main() var x = 50 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_50: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"51
");
}

#[test]
fn var_51() {
    let (out, diags) = interpret("fn main() var x = 51 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_51: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"52
");
}

#[test]
fn var_52() {
    let (out, diags) = interpret("fn main() var x = 52 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_52: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"53
");
}

#[test]
fn var_53() {
    let (out, diags) = interpret("fn main() var x = 53 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_53: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"54
");
}

#[test]
fn var_54() {
    let (out, diags) = interpret("fn main() var x = 54 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_54: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"55
");
}

#[test]
fn var_55() {
    let (out, diags) = interpret("fn main() var x = 55 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_55: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"56
");
}

#[test]
fn var_56() {
    let (out, diags) = interpret("fn main() var x = 56 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_56: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"57
");
}

#[test]
fn var_57() {
    let (out, diags) = interpret("fn main() var x = 57 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_57: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"58
");
}

#[test]
fn var_58() {
    let (out, diags) = interpret("fn main() var x = 58 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_58: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"59
");
}

#[test]
fn var_59() {
    let (out, diags) = interpret("fn main() var x = 59 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_59: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"60
");
}

#[test]
fn var_60() {
    let (out, diags) = interpret("fn main() var x = 60 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_60: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"61
");
}

#[test]
fn var_61() {
    let (out, diags) = interpret("fn main() var x = 61 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_61: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"62
");
}

#[test]
fn var_62() {
    let (out, diags) = interpret("fn main() var x = 62 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_62: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"63
");
}

#[test]
fn var_63() {
    let (out, diags) = interpret("fn main() var x = 63 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_63: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"64
");
}

#[test]
fn var_64() {
    let (out, diags) = interpret("fn main() var x = 64 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_64: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"65
");
}

#[test]
fn var_65() {
    let (out, diags) = interpret("fn main() var x = 65 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_65: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"66
");
}

#[test]
fn var_66() {
    let (out, diags) = interpret("fn main() var x = 66 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_66: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"67
");
}

#[test]
fn var_67() {
    let (out, diags) = interpret("fn main() var x = 67 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_67: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"68
");
}

#[test]
fn var_68() {
    let (out, diags) = interpret("fn main() var x = 68 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_68: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"69
");
}

#[test]
fn var_69() {
    let (out, diags) = interpret("fn main() var x = 69 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_69: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"70
");
}

#[test]
fn var_70() {
    let (out, diags) = interpret("fn main() var x = 70 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_70: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"71
");
}

#[test]
fn var_71() {
    let (out, diags) = interpret("fn main() var x = 71 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_71: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"72
");
}

#[test]
fn var_72() {
    let (out, diags) = interpret("fn main() var x = 72 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_72: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"73
");
}

#[test]
fn var_73() {
    let (out, diags) = interpret("fn main() var x = 73 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_73: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"74
");
}

#[test]
fn var_74() {
    let (out, diags) = interpret("fn main() var x = 74 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_74: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"75
");
}

#[test]
fn var_75() {
    let (out, diags) = interpret("fn main() var x = 75 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_75: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"76
");
}

#[test]
fn var_76() {
    let (out, diags) = interpret("fn main() var x = 76 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_76: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"77
");
}

#[test]
fn var_77() {
    let (out, diags) = interpret("fn main() var x = 77 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_77: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"78
");
}

#[test]
fn var_78() {
    let (out, diags) = interpret("fn main() var x = 78 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_78: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"79
");
}

#[test]
fn var_79() {
    let (out, diags) = interpret("fn main() var x = 79 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_79: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"80
");
}

#[test]
fn var_80() {
    let (out, diags) = interpret("fn main() var x = 80 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_80: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"81
");
}

#[test]
fn var_81() {
    let (out, diags) = interpret("fn main() var x = 81 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_81: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"82
");
}

#[test]
fn var_82() {
    let (out, diags) = interpret("fn main() var x = 82 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_82: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"83
");
}

#[test]
fn var_83() {
    let (out, diags) = interpret("fn main() var x = 83 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_83: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"84
");
}

#[test]
fn var_84() {
    let (out, diags) = interpret("fn main() var x = 84 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_84: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"85
");
}

#[test]
fn var_85() {
    let (out, diags) = interpret("fn main() var x = 85 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_85: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"86
");
}

#[test]
fn var_86() {
    let (out, diags) = interpret("fn main() var x = 86 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_86: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"87
");
}

#[test]
fn var_87() {
    let (out, diags) = interpret("fn main() var x = 87 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_87: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"88
");
}

#[test]
fn var_88() {
    let (out, diags) = interpret("fn main() var x = 88 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_88: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"89
");
}

#[test]
fn var_89() {
    let (out, diags) = interpret("fn main() var x = 89 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_89: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"90
");
}

#[test]
fn var_90() {
    let (out, diags) = interpret("fn main() var x = 90 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_90: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"91
");
}

#[test]
fn var_91() {
    let (out, diags) = interpret("fn main() var x = 91 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_91: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"92
");
}

#[test]
fn var_92() {
    let (out, diags) = interpret("fn main() var x = 92 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_92: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"93
");
}

#[test]
fn var_93() {
    let (out, diags) = interpret("fn main() var x = 93 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_93: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"94
");
}

#[test]
fn var_94() {
    let (out, diags) = interpret("fn main() var x = 94 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_94: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"95
");
}

#[test]
fn var_95() {
    let (out, diags) = interpret("fn main() var x = 95 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_95: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"96
");
}

#[test]
fn var_96() {
    let (out, diags) = interpret("fn main() var x = 96 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_96: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"97
");
}

#[test]
fn var_97() {
    let (out, diags) = interpret("fn main() var x = 97 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_97: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"98
");
}

#[test]
fn var_98() {
    let (out, diags) = interpret("fn main() var x = 98 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_98: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"99
");
}

#[test]
fn var_99() {
    let (out, diags) = interpret("fn main() var x = 99 x = x + 1 print(x) end", 0);
    assert!(!diags.has_errors(), "var_99: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"100
");
}

#[test]
fn let_chain_0() {
    let (out, diags) = interpret("fn main() let a = 0 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_0: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"2
");
}

#[test]
fn let_chain_1() {
    let (out, diags) = interpret("fn main() let a = 1 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_1: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"3
");
}

#[test]
fn let_chain_2() {
    let (out, diags) = interpret("fn main() let a = 2 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_2: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"4
");
}

#[test]
fn let_chain_3() {
    let (out, diags) = interpret("fn main() let a = 3 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_3: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"5
");
}

#[test]
fn let_chain_4() {
    let (out, diags) = interpret("fn main() let a = 4 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_4: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn let_chain_5() {
    let (out, diags) = interpret("fn main() let a = 5 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_5: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn let_chain_6() {
    let (out, diags) = interpret("fn main() let a = 6 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_6: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"8
");
}

#[test]
fn let_chain_7() {
    let (out, diags) = interpret("fn main() let a = 7 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_7: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"9
");
}

#[test]
fn let_chain_8() {
    let (out, diags) = interpret("fn main() let a = 8 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_8: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"10
");
}

#[test]
fn let_chain_9() {
    let (out, diags) = interpret("fn main() let a = 9 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_9: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"11
");
}

#[test]
fn let_chain_10() {
    let (out, diags) = interpret("fn main() let a = 10 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_10: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"12
");
}

#[test]
fn let_chain_11() {
    let (out, diags) = interpret("fn main() let a = 11 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_11: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"13
");
}

#[test]
fn let_chain_12() {
    let (out, diags) = interpret("fn main() let a = 12 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_12: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"14
");
}

#[test]
fn let_chain_13() {
    let (out, diags) = interpret("fn main() let a = 13 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_13: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"15
");
}

#[test]
fn let_chain_14() {
    let (out, diags) = interpret("fn main() let a = 14 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_14: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"16
");
}

#[test]
fn let_chain_15() {
    let (out, diags) = interpret("fn main() let a = 15 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_15: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"17
");
}

#[test]
fn let_chain_16() {
    let (out, diags) = interpret("fn main() let a = 16 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_16: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"18
");
}

#[test]
fn let_chain_17() {
    let (out, diags) = interpret("fn main() let a = 17 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_17: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"19
");
}

#[test]
fn let_chain_18() {
    let (out, diags) = interpret("fn main() let a = 18 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_18: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"20
");
}

#[test]
fn let_chain_19() {
    let (out, diags) = interpret("fn main() let a = 19 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_19: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"21
");
}

#[test]
fn let_chain_20() {
    let (out, diags) = interpret("fn main() let a = 20 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_20: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"22
");
}

#[test]
fn let_chain_21() {
    let (out, diags) = interpret("fn main() let a = 21 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_21: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"23
");
}

#[test]
fn let_chain_22() {
    let (out, diags) = interpret("fn main() let a = 22 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_22: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"24
");
}

#[test]
fn let_chain_23() {
    let (out, diags) = interpret("fn main() let a = 23 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_23: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"25
");
}

#[test]
fn let_chain_24() {
    let (out, diags) = interpret("fn main() let a = 24 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_24: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"26
");
}

#[test]
fn let_chain_25() {
    let (out, diags) = interpret("fn main() let a = 25 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_25: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"27
");
}

#[test]
fn let_chain_26() {
    let (out, diags) = interpret("fn main() let a = 26 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_26: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"28
");
}

#[test]
fn let_chain_27() {
    let (out, diags) = interpret("fn main() let a = 27 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_27: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"29
");
}

#[test]
fn let_chain_28() {
    let (out, diags) = interpret("fn main() let a = 28 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_28: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"30
");
}

#[test]
fn let_chain_29() {
    let (out, diags) = interpret("fn main() let a = 29 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_29: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"31
");
}

#[test]
fn let_chain_30() {
    let (out, diags) = interpret("fn main() let a = 30 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_30: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"32
");
}

#[test]
fn let_chain_31() {
    let (out, diags) = interpret("fn main() let a = 31 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_31: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"33
");
}

#[test]
fn let_chain_32() {
    let (out, diags) = interpret("fn main() let a = 32 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_32: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"34
");
}

#[test]
fn let_chain_33() {
    let (out, diags) = interpret("fn main() let a = 33 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_33: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"35
");
}

#[test]
fn let_chain_34() {
    let (out, diags) = interpret("fn main() let a = 34 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_34: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"36
");
}

#[test]
fn let_chain_35() {
    let (out, diags) = interpret("fn main() let a = 35 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_35: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"37
");
}

#[test]
fn let_chain_36() {
    let (out, diags) = interpret("fn main() let a = 36 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_36: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"38
");
}

#[test]
fn let_chain_37() {
    let (out, diags) = interpret("fn main() let a = 37 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_37: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"39
");
}

#[test]
fn let_chain_38() {
    let (out, diags) = interpret("fn main() let a = 38 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_38: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"40
");
}

#[test]
fn let_chain_39() {
    let (out, diags) = interpret("fn main() let a = 39 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_39: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"41
");
}

#[test]
fn let_chain_40() {
    let (out, diags) = interpret("fn main() let a = 40 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_40: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"42
");
}

#[test]
fn let_chain_41() {
    let (out, diags) = interpret("fn main() let a = 41 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_41: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"43
");
}

#[test]
fn let_chain_42() {
    let (out, diags) = interpret("fn main() let a = 42 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_42: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"44
");
}

#[test]
fn let_chain_43() {
    let (out, diags) = interpret("fn main() let a = 43 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_43: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"45
");
}

#[test]
fn let_chain_44() {
    let (out, diags) = interpret("fn main() let a = 44 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_44: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"46
");
}

#[test]
fn let_chain_45() {
    let (out, diags) = interpret("fn main() let a = 45 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_45: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"47
");
}

#[test]
fn let_chain_46() {
    let (out, diags) = interpret("fn main() let a = 46 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_46: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"48
");
}

#[test]
fn let_chain_47() {
    let (out, diags) = interpret("fn main() let a = 47 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_47: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"49
");
}

#[test]
fn let_chain_48() {
    let (out, diags) = interpret("fn main() let a = 48 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_48: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"50
");
}

#[test]
fn let_chain_49() {
    let (out, diags) = interpret("fn main() let a = 49 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_49: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"51
");
}

#[test]
fn let_chain_50() {
    let (out, diags) = interpret("fn main() let a = 50 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_50: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"52
");
}

#[test]
fn let_chain_51() {
    let (out, diags) = interpret("fn main() let a = 51 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_51: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"53
");
}

#[test]
fn let_chain_52() {
    let (out, diags) = interpret("fn main() let a = 52 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_52: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"54
");
}

#[test]
fn let_chain_53() {
    let (out, diags) = interpret("fn main() let a = 53 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_53: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"55
");
}

#[test]
fn let_chain_54() {
    let (out, diags) = interpret("fn main() let a = 54 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_54: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"56
");
}

#[test]
fn let_chain_55() {
    let (out, diags) = interpret("fn main() let a = 55 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_55: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"57
");
}

#[test]
fn let_chain_56() {
    let (out, diags) = interpret("fn main() let a = 56 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_56: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"58
");
}

#[test]
fn let_chain_57() {
    let (out, diags) = interpret("fn main() let a = 57 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_57: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"59
");
}

#[test]
fn let_chain_58() {
    let (out, diags) = interpret("fn main() let a = 58 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_58: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"60
");
}

#[test]
fn let_chain_59() {
    let (out, diags) = interpret("fn main() let a = 59 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_59: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"61
");
}

#[test]
fn let_chain_60() {
    let (out, diags) = interpret("fn main() let a = 60 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_60: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"62
");
}

#[test]
fn let_chain_61() {
    let (out, diags) = interpret("fn main() let a = 61 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_61: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"63
");
}

#[test]
fn let_chain_62() {
    let (out, diags) = interpret("fn main() let a = 62 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_62: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"64
");
}

#[test]
fn let_chain_63() {
    let (out, diags) = interpret("fn main() let a = 63 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_63: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"65
");
}

#[test]
fn let_chain_64() {
    let (out, diags) = interpret("fn main() let a = 64 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_64: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"66
");
}

#[test]
fn let_chain_65() {
    let (out, diags) = interpret("fn main() let a = 65 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_65: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"67
");
}

#[test]
fn let_chain_66() {
    let (out, diags) = interpret("fn main() let a = 66 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_66: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"68
");
}

#[test]
fn let_chain_67() {
    let (out, diags) = interpret("fn main() let a = 67 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_67: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"69
");
}

#[test]
fn let_chain_68() {
    let (out, diags) = interpret("fn main() let a = 68 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_68: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"70
");
}

#[test]
fn let_chain_69() {
    let (out, diags) = interpret("fn main() let a = 69 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_69: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"71
");
}

#[test]
fn let_chain_70() {
    let (out, diags) = interpret("fn main() let a = 70 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_70: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"72
");
}

#[test]
fn let_chain_71() {
    let (out, diags) = interpret("fn main() let a = 71 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_71: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"73
");
}

#[test]
fn let_chain_72() {
    let (out, diags) = interpret("fn main() let a = 72 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_72: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"74
");
}

#[test]
fn let_chain_73() {
    let (out, diags) = interpret("fn main() let a = 73 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_73: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"75
");
}

#[test]
fn let_chain_74() {
    let (out, diags) = interpret("fn main() let a = 74 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_74: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"76
");
}

#[test]
fn let_chain_75() {
    let (out, diags) = interpret("fn main() let a = 75 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_75: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"77
");
}

#[test]
fn let_chain_76() {
    let (out, diags) = interpret("fn main() let a = 76 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_76: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"78
");
}

#[test]
fn let_chain_77() {
    let (out, diags) = interpret("fn main() let a = 77 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_77: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"79
");
}

#[test]
fn let_chain_78() {
    let (out, diags) = interpret("fn main() let a = 78 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_78: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"80
");
}

#[test]
fn let_chain_79() {
    let (out, diags) = interpret("fn main() let a = 79 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_79: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"81
");
}

#[test]
fn let_chain_80() {
    let (out, diags) = interpret("fn main() let a = 80 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_80: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"82
");
}

#[test]
fn let_chain_81() {
    let (out, diags) = interpret("fn main() let a = 81 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_81: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"83
");
}

#[test]
fn let_chain_82() {
    let (out, diags) = interpret("fn main() let a = 82 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_82: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"84
");
}

#[test]
fn let_chain_83() {
    let (out, diags) = interpret("fn main() let a = 83 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_83: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"85
");
}

#[test]
fn let_chain_84() {
    let (out, diags) = interpret("fn main() let a = 84 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_84: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"86
");
}

#[test]
fn let_chain_85() {
    let (out, diags) = interpret("fn main() let a = 85 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_85: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"87
");
}

#[test]
fn let_chain_86() {
    let (out, diags) = interpret("fn main() let a = 86 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_86: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"88
");
}

#[test]
fn let_chain_87() {
    let (out, diags) = interpret("fn main() let a = 87 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_87: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"89
");
}

#[test]
fn let_chain_88() {
    let (out, diags) = interpret("fn main() let a = 88 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_88: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"90
");
}

#[test]
fn let_chain_89() {
    let (out, diags) = interpret("fn main() let a = 89 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_89: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"91
");
}

#[test]
fn let_chain_90() {
    let (out, diags) = interpret("fn main() let a = 90 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_90: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"92
");
}

#[test]
fn let_chain_91() {
    let (out, diags) = interpret("fn main() let a = 91 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_91: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"93
");
}

#[test]
fn let_chain_92() {
    let (out, diags) = interpret("fn main() let a = 92 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_92: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"94
");
}

#[test]
fn let_chain_93() {
    let (out, diags) = interpret("fn main() let a = 93 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_93: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"95
");
}

#[test]
fn let_chain_94() {
    let (out, diags) = interpret("fn main() let a = 94 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_94: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"96
");
}

#[test]
fn let_chain_95() {
    let (out, diags) = interpret("fn main() let a = 95 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_95: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"97
");
}

#[test]
fn let_chain_96() {
    let (out, diags) = interpret("fn main() let a = 96 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_96: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"98
");
}

#[test]
fn let_chain_97() {
    let (out, diags) = interpret("fn main() let a = 97 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_97: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"99
");
}

#[test]
fn let_chain_98() {
    let (out, diags) = interpret("fn main() let a = 98 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_98: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"100
");
}

#[test]
fn let_chain_99() {
    let (out, diags) = interpret("fn main() let a = 99 let b = a + 1 let c = b + 1 print(c) end", 0);
    assert!(!diags.has_errors(), "let_chain_99: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"101
");
}

#[test]
fn var_augment_add_0() {
    let (out, diags) = interpret("fn main() var x = 0 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_0: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"5
");
}

#[test]
fn var_augment_add_1() {
    let (out, diags) = interpret("fn main() var x = 1 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_1: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn var_augment_add_2() {
    let (out, diags) = interpret("fn main() var x = 2 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_2: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn var_augment_add_3() {
    let (out, diags) = interpret("fn main() var x = 3 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_3: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"8
");
}

#[test]
fn var_augment_add_4() {
    let (out, diags) = interpret("fn main() var x = 4 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_4: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"9
");
}

#[test]
fn var_augment_add_5() {
    let (out, diags) = interpret("fn main() var x = 5 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_5: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"10
");
}

#[test]
fn var_augment_add_6() {
    let (out, diags) = interpret("fn main() var x = 6 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_6: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"11
");
}

#[test]
fn var_augment_add_7() {
    let (out, diags) = interpret("fn main() var x = 7 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_7: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"12
");
}

#[test]
fn var_augment_add_8() {
    let (out, diags) = interpret("fn main() var x = 8 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_8: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"13
");
}

#[test]
fn var_augment_add_9() {
    let (out, diags) = interpret("fn main() var x = 9 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_9: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"14
");
}

#[test]
fn var_augment_add_10() {
    let (out, diags) = interpret("fn main() var x = 10 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_10: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"15
");
}

#[test]
fn var_augment_add_11() {
    let (out, diags) = interpret("fn main() var x = 11 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_11: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"16
");
}

#[test]
fn var_augment_add_12() {
    let (out, diags) = interpret("fn main() var x = 12 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_12: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"17
");
}

#[test]
fn var_augment_add_13() {
    let (out, diags) = interpret("fn main() var x = 13 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_13: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"18
");
}

#[test]
fn var_augment_add_14() {
    let (out, diags) = interpret("fn main() var x = 14 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_14: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"19
");
}

#[test]
fn var_augment_add_15() {
    let (out, diags) = interpret("fn main() var x = 15 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_15: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"20
");
}

#[test]
fn var_augment_add_16() {
    let (out, diags) = interpret("fn main() var x = 16 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_16: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"21
");
}

#[test]
fn var_augment_add_17() {
    let (out, diags) = interpret("fn main() var x = 17 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_17: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"22
");
}

#[test]
fn var_augment_add_18() {
    let (out, diags) = interpret("fn main() var x = 18 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_18: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"23
");
}

#[test]
fn var_augment_add_19() {
    let (out, diags) = interpret("fn main() var x = 19 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_19: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"24
");
}

#[test]
fn var_augment_add_20() {
    let (out, diags) = interpret("fn main() var x = 20 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_20: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"25
");
}

#[test]
fn var_augment_add_21() {
    let (out, diags) = interpret("fn main() var x = 21 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_21: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"26
");
}

#[test]
fn var_augment_add_22() {
    let (out, diags) = interpret("fn main() var x = 22 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_22: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"27
");
}

#[test]
fn var_augment_add_23() {
    let (out, diags) = interpret("fn main() var x = 23 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_23: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"28
");
}

#[test]
fn var_augment_add_24() {
    let (out, diags) = interpret("fn main() var x = 24 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_24: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"29
");
}

#[test]
fn var_augment_add_25() {
    let (out, diags) = interpret("fn main() var x = 25 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_25: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"30
");
}

#[test]
fn var_augment_add_26() {
    let (out, diags) = interpret("fn main() var x = 26 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_26: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"31
");
}

#[test]
fn var_augment_add_27() {
    let (out, diags) = interpret("fn main() var x = 27 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_27: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"32
");
}

#[test]
fn var_augment_add_28() {
    let (out, diags) = interpret("fn main() var x = 28 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_28: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"33
");
}

#[test]
fn var_augment_add_29() {
    let (out, diags) = interpret("fn main() var x = 29 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_29: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"34
");
}

#[test]
fn var_augment_add_30() {
    let (out, diags) = interpret("fn main() var x = 30 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_30: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"35
");
}

#[test]
fn var_augment_add_31() {
    let (out, diags) = interpret("fn main() var x = 31 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_31: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"36
");
}

#[test]
fn var_augment_add_32() {
    let (out, diags) = interpret("fn main() var x = 32 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_32: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"37
");
}

#[test]
fn var_augment_add_33() {
    let (out, diags) = interpret("fn main() var x = 33 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_33: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"38
");
}

#[test]
fn var_augment_add_34() {
    let (out, diags) = interpret("fn main() var x = 34 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_34: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"39
");
}

#[test]
fn var_augment_add_35() {
    let (out, diags) = interpret("fn main() var x = 35 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_35: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"40
");
}

#[test]
fn var_augment_add_36() {
    let (out, diags) = interpret("fn main() var x = 36 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_36: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"41
");
}

#[test]
fn var_augment_add_37() {
    let (out, diags) = interpret("fn main() var x = 37 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_37: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"42
");
}

#[test]
fn var_augment_add_38() {
    let (out, diags) = interpret("fn main() var x = 38 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_38: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"43
");
}

#[test]
fn var_augment_add_39() {
    let (out, diags) = interpret("fn main() var x = 39 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_39: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"44
");
}

#[test]
fn var_augment_add_40() {
    let (out, diags) = interpret("fn main() var x = 40 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_40: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"45
");
}

#[test]
fn var_augment_add_41() {
    let (out, diags) = interpret("fn main() var x = 41 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_41: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"46
");
}

#[test]
fn var_augment_add_42() {
    let (out, diags) = interpret("fn main() var x = 42 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_42: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"47
");
}

#[test]
fn var_augment_add_43() {
    let (out, diags) = interpret("fn main() var x = 43 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_43: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"48
");
}

#[test]
fn var_augment_add_44() {
    let (out, diags) = interpret("fn main() var x = 44 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_44: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"49
");
}

#[test]
fn var_augment_add_45() {
    let (out, diags) = interpret("fn main() var x = 45 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_45: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"50
");
}

#[test]
fn var_augment_add_46() {
    let (out, diags) = interpret("fn main() var x = 46 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_46: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"51
");
}

#[test]
fn var_augment_add_47() {
    let (out, diags) = interpret("fn main() var x = 47 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_47: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"52
");
}

#[test]
fn var_augment_add_48() {
    let (out, diags) = interpret("fn main() var x = 48 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_48: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"53
");
}

#[test]
fn var_augment_add_49() {
    let (out, diags) = interpret("fn main() var x = 49 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_49: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"54
");
}

#[test]
fn var_augment_add_50() {
    let (out, diags) = interpret("fn main() var x = 50 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_50: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"55
");
}

#[test]
fn var_augment_add_51() {
    let (out, diags) = interpret("fn main() var x = 51 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_51: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"56
");
}

#[test]
fn var_augment_add_52() {
    let (out, diags) = interpret("fn main() var x = 52 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_52: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"57
");
}

#[test]
fn var_augment_add_53() {
    let (out, diags) = interpret("fn main() var x = 53 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_53: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"58
");
}

#[test]
fn var_augment_add_54() {
    let (out, diags) = interpret("fn main() var x = 54 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_54: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"59
");
}

#[test]
fn var_augment_add_55() {
    let (out, diags) = interpret("fn main() var x = 55 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_55: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"60
");
}

#[test]
fn var_augment_add_56() {
    let (out, diags) = interpret("fn main() var x = 56 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_56: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"61
");
}

#[test]
fn var_augment_add_57() {
    let (out, diags) = interpret("fn main() var x = 57 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_57: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"62
");
}

#[test]
fn var_augment_add_58() {
    let (out, diags) = interpret("fn main() var x = 58 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_58: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"63
");
}

#[test]
fn var_augment_add_59() {
    let (out, diags) = interpret("fn main() var x = 59 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_59: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"64
");
}

#[test]
fn var_augment_add_60() {
    let (out, diags) = interpret("fn main() var x = 60 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_60: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"65
");
}

#[test]
fn var_augment_add_61() {
    let (out, diags) = interpret("fn main() var x = 61 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_61: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"66
");
}

#[test]
fn var_augment_add_62() {
    let (out, diags) = interpret("fn main() var x = 62 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_62: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"67
");
}

#[test]
fn var_augment_add_63() {
    let (out, diags) = interpret("fn main() var x = 63 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_63: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"68
");
}

#[test]
fn var_augment_add_64() {
    let (out, diags) = interpret("fn main() var x = 64 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_64: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"69
");
}

#[test]
fn var_augment_add_65() {
    let (out, diags) = interpret("fn main() var x = 65 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_65: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"70
");
}

#[test]
fn var_augment_add_66() {
    let (out, diags) = interpret("fn main() var x = 66 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_66: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"71
");
}

#[test]
fn var_augment_add_67() {
    let (out, diags) = interpret("fn main() var x = 67 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_67: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"72
");
}

#[test]
fn var_augment_add_68() {
    let (out, diags) = interpret("fn main() var x = 68 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_68: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"73
");
}

#[test]
fn var_augment_add_69() {
    let (out, diags) = interpret("fn main() var x = 69 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_69: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"74
");
}

#[test]
fn var_augment_add_70() {
    let (out, diags) = interpret("fn main() var x = 70 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_70: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"75
");
}

#[test]
fn var_augment_add_71() {
    let (out, diags) = interpret("fn main() var x = 71 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_71: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"76
");
}

#[test]
fn var_augment_add_72() {
    let (out, diags) = interpret("fn main() var x = 72 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_72: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"77
");
}

#[test]
fn var_augment_add_73() {
    let (out, diags) = interpret("fn main() var x = 73 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_73: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"78
");
}

#[test]
fn var_augment_add_74() {
    let (out, diags) = interpret("fn main() var x = 74 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_74: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"79
");
}

#[test]
fn var_augment_add_75() {
    let (out, diags) = interpret("fn main() var x = 75 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_75: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"80
");
}

#[test]
fn var_augment_add_76() {
    let (out, diags) = interpret("fn main() var x = 76 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_76: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"81
");
}

#[test]
fn var_augment_add_77() {
    let (out, diags) = interpret("fn main() var x = 77 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_77: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"82
");
}

#[test]
fn var_augment_add_78() {
    let (out, diags) = interpret("fn main() var x = 78 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_78: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"83
");
}

#[test]
fn var_augment_add_79() {
    let (out, diags) = interpret("fn main() var x = 79 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_79: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"84
");
}

#[test]
fn var_augment_add_80() {
    let (out, diags) = interpret("fn main() var x = 80 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_80: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"85
");
}

#[test]
fn var_augment_add_81() {
    let (out, diags) = interpret("fn main() var x = 81 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_81: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"86
");
}

#[test]
fn var_augment_add_82() {
    let (out, diags) = interpret("fn main() var x = 82 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_82: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"87
");
}

#[test]
fn var_augment_add_83() {
    let (out, diags) = interpret("fn main() var x = 83 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_83: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"88
");
}

#[test]
fn var_augment_add_84() {
    let (out, diags) = interpret("fn main() var x = 84 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_84: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"89
");
}

#[test]
fn var_augment_add_85() {
    let (out, diags) = interpret("fn main() var x = 85 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_85: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"90
");
}

#[test]
fn var_augment_add_86() {
    let (out, diags) = interpret("fn main() var x = 86 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_86: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"91
");
}

#[test]
fn var_augment_add_87() {
    let (out, diags) = interpret("fn main() var x = 87 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_87: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"92
");
}

#[test]
fn var_augment_add_88() {
    let (out, diags) = interpret("fn main() var x = 88 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_88: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"93
");
}

#[test]
fn var_augment_add_89() {
    let (out, diags) = interpret("fn main() var x = 89 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_89: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"94
");
}

#[test]
fn var_augment_add_90() {
    let (out, diags) = interpret("fn main() var x = 90 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_90: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"95
");
}

#[test]
fn var_augment_add_91() {
    let (out, diags) = interpret("fn main() var x = 91 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_91: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"96
");
}

#[test]
fn var_augment_add_92() {
    let (out, diags) = interpret("fn main() var x = 92 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_92: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"97
");
}

#[test]
fn var_augment_add_93() {
    let (out, diags) = interpret("fn main() var x = 93 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_93: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"98
");
}

#[test]
fn var_augment_add_94() {
    let (out, diags) = interpret("fn main() var x = 94 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_94: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"99
");
}

#[test]
fn var_augment_add_95() {
    let (out, diags) = interpret("fn main() var x = 95 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_95: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"100
");
}

#[test]
fn var_augment_add_96() {
    let (out, diags) = interpret("fn main() var x = 96 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_96: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"101
");
}

#[test]
fn var_augment_add_97() {
    let (out, diags) = interpret("fn main() var x = 97 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_97: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"102
");
}

#[test]
fn var_augment_add_98() {
    let (out, diags) = interpret("fn main() var x = 98 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_98: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"103
");
}

#[test]
fn var_augment_add_99() {
    let (out, diags) = interpret("fn main() var x = 99 x += 5 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_add_99: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"104
");
}

#[test]
fn var_augment_sub_0() {
    let (out, diags) = interpret("fn main() var x = 100 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_0: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"90
");
}

#[test]
fn var_augment_sub_1() {
    let (out, diags) = interpret("fn main() var x = 101 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_1: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"91
");
}

#[test]
fn var_augment_sub_2() {
    let (out, diags) = interpret("fn main() var x = 102 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_2: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"92
");
}

#[test]
fn var_augment_sub_3() {
    let (out, diags) = interpret("fn main() var x = 103 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_3: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"93
");
}

#[test]
fn var_augment_sub_4() {
    let (out, diags) = interpret("fn main() var x = 104 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_4: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"94
");
}

#[test]
fn var_augment_sub_5() {
    let (out, diags) = interpret("fn main() var x = 105 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_5: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"95
");
}

#[test]
fn var_augment_sub_6() {
    let (out, diags) = interpret("fn main() var x = 106 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_6: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"96
");
}

#[test]
fn var_augment_sub_7() {
    let (out, diags) = interpret("fn main() var x = 107 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_7: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"97
");
}

#[test]
fn var_augment_sub_8() {
    let (out, diags) = interpret("fn main() var x = 108 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_8: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"98
");
}

#[test]
fn var_augment_sub_9() {
    let (out, diags) = interpret("fn main() var x = 109 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_9: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"99
");
}

#[test]
fn var_augment_sub_10() {
    let (out, diags) = interpret("fn main() var x = 110 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_10: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"100
");
}

#[test]
fn var_augment_sub_11() {
    let (out, diags) = interpret("fn main() var x = 111 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_11: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"101
");
}

#[test]
fn var_augment_sub_12() {
    let (out, diags) = interpret("fn main() var x = 112 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_12: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"102
");
}

#[test]
fn var_augment_sub_13() {
    let (out, diags) = interpret("fn main() var x = 113 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_13: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"103
");
}

#[test]
fn var_augment_sub_14() {
    let (out, diags) = interpret("fn main() var x = 114 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_14: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"104
");
}

#[test]
fn var_augment_sub_15() {
    let (out, diags) = interpret("fn main() var x = 115 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_15: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"105
");
}

#[test]
fn var_augment_sub_16() {
    let (out, diags) = interpret("fn main() var x = 116 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_16: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"106
");
}

#[test]
fn var_augment_sub_17() {
    let (out, diags) = interpret("fn main() var x = 117 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_17: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"107
");
}

#[test]
fn var_augment_sub_18() {
    let (out, diags) = interpret("fn main() var x = 118 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_18: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"108
");
}

#[test]
fn var_augment_sub_19() {
    let (out, diags) = interpret("fn main() var x = 119 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_19: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"109
");
}

#[test]
fn var_augment_sub_20() {
    let (out, diags) = interpret("fn main() var x = 120 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_20: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"110
");
}

#[test]
fn var_augment_sub_21() {
    let (out, diags) = interpret("fn main() var x = 121 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_21: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"111
");
}

#[test]
fn var_augment_sub_22() {
    let (out, diags) = interpret("fn main() var x = 122 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_22: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"112
");
}

#[test]
fn var_augment_sub_23() {
    let (out, diags) = interpret("fn main() var x = 123 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_23: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"113
");
}

#[test]
fn var_augment_sub_24() {
    let (out, diags) = interpret("fn main() var x = 124 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_24: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"114
");
}

#[test]
fn var_augment_sub_25() {
    let (out, diags) = interpret("fn main() var x = 125 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_25: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"115
");
}

#[test]
fn var_augment_sub_26() {
    let (out, diags) = interpret("fn main() var x = 126 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_26: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"116
");
}

#[test]
fn var_augment_sub_27() {
    let (out, diags) = interpret("fn main() var x = 127 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_27: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"117
");
}

#[test]
fn var_augment_sub_28() {
    let (out, diags) = interpret("fn main() var x = 128 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_28: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"118
");
}

#[test]
fn var_augment_sub_29() {
    let (out, diags) = interpret("fn main() var x = 129 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_29: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"119
");
}

#[test]
fn var_augment_sub_30() {
    let (out, diags) = interpret("fn main() var x = 130 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_30: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"120
");
}

#[test]
fn var_augment_sub_31() {
    let (out, diags) = interpret("fn main() var x = 131 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_31: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"121
");
}

#[test]
fn var_augment_sub_32() {
    let (out, diags) = interpret("fn main() var x = 132 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_32: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"122
");
}

#[test]
fn var_augment_sub_33() {
    let (out, diags) = interpret("fn main() var x = 133 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_33: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"123
");
}

#[test]
fn var_augment_sub_34() {
    let (out, diags) = interpret("fn main() var x = 134 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_34: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"124
");
}

#[test]
fn var_augment_sub_35() {
    let (out, diags) = interpret("fn main() var x = 135 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_35: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"125
");
}

#[test]
fn var_augment_sub_36() {
    let (out, diags) = interpret("fn main() var x = 136 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_36: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"126
");
}

#[test]
fn var_augment_sub_37() {
    let (out, diags) = interpret("fn main() var x = 137 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_37: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"127
");
}

#[test]
fn var_augment_sub_38() {
    let (out, diags) = interpret("fn main() var x = 138 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_38: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"128
");
}

#[test]
fn var_augment_sub_39() {
    let (out, diags) = interpret("fn main() var x = 139 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_39: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"129
");
}

#[test]
fn var_augment_sub_40() {
    let (out, diags) = interpret("fn main() var x = 140 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_40: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"130
");
}

#[test]
fn var_augment_sub_41() {
    let (out, diags) = interpret("fn main() var x = 141 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_41: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"131
");
}

#[test]
fn var_augment_sub_42() {
    let (out, diags) = interpret("fn main() var x = 142 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_42: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"132
");
}

#[test]
fn var_augment_sub_43() {
    let (out, diags) = interpret("fn main() var x = 143 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_43: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"133
");
}

#[test]
fn var_augment_sub_44() {
    let (out, diags) = interpret("fn main() var x = 144 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_44: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"134
");
}

#[test]
fn var_augment_sub_45() {
    let (out, diags) = interpret("fn main() var x = 145 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_45: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"135
");
}

#[test]
fn var_augment_sub_46() {
    let (out, diags) = interpret("fn main() var x = 146 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_46: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"136
");
}

#[test]
fn var_augment_sub_47() {
    let (out, diags) = interpret("fn main() var x = 147 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_47: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"137
");
}

#[test]
fn var_augment_sub_48() {
    let (out, diags) = interpret("fn main() var x = 148 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_48: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"138
");
}

#[test]
fn var_augment_sub_49() {
    let (out, diags) = interpret("fn main() var x = 149 x -= 10 print(x) end", 0);
    assert!(!diags.has_errors(), "var_augment_sub_49: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"139
");
}

