// Owner: PascalElixir / axolrs (GitHub org)
// File: Auto-generated tests for string operations.

use axolc_core::interpret;

#[test]
fn str_lit_0() {
    let (out, diags) = interpret(r#"fn main() print("hello0") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_0: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello0
");
}

#[test]
fn str_lit_1() {
    let (out, diags) = interpret(r#"fn main() print("hello1") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_1: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello1
");
}

#[test]
fn str_lit_2() {
    let (out, diags) = interpret(r#"fn main() print("hello2") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_2: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello2
");
}

#[test]
fn str_lit_3() {
    let (out, diags) = interpret(r#"fn main() print("hello3") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_3: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello3
");
}

#[test]
fn str_lit_4() {
    let (out, diags) = interpret(r#"fn main() print("hello4") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_4: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello4
");
}

#[test]
fn str_lit_5() {
    let (out, diags) = interpret(r#"fn main() print("hello5") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_5: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello5
");
}

#[test]
fn str_lit_6() {
    let (out, diags) = interpret(r#"fn main() print("hello6") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_6: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello6
");
}

#[test]
fn str_lit_7() {
    let (out, diags) = interpret(r#"fn main() print("hello7") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_7: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello7
");
}

#[test]
fn str_lit_8() {
    let (out, diags) = interpret(r#"fn main() print("hello8") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_8: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello8
");
}

#[test]
fn str_lit_9() {
    let (out, diags) = interpret(r#"fn main() print("hello9") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_9: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello9
");
}

#[test]
fn str_lit_10() {
    let (out, diags) = interpret(r#"fn main() print("hello10") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_10: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello10
");
}

#[test]
fn str_lit_11() {
    let (out, diags) = interpret(r#"fn main() print("hello11") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_11: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello11
");
}

#[test]
fn str_lit_12() {
    let (out, diags) = interpret(r#"fn main() print("hello12") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_12: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello12
");
}

#[test]
fn str_lit_13() {
    let (out, diags) = interpret(r#"fn main() print("hello13") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_13: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello13
");
}

#[test]
fn str_lit_14() {
    let (out, diags) = interpret(r#"fn main() print("hello14") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_14: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello14
");
}

#[test]
fn str_lit_15() {
    let (out, diags) = interpret(r#"fn main() print("hello15") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_15: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello15
");
}

#[test]
fn str_lit_16() {
    let (out, diags) = interpret(r#"fn main() print("hello16") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_16: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello16
");
}

#[test]
fn str_lit_17() {
    let (out, diags) = interpret(r#"fn main() print("hello17") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_17: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello17
");
}

#[test]
fn str_lit_18() {
    let (out, diags) = interpret(r#"fn main() print("hello18") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_18: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello18
");
}

#[test]
fn str_lit_19() {
    let (out, diags) = interpret(r#"fn main() print("hello19") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_19: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello19
");
}

#[test]
fn str_lit_20() {
    let (out, diags) = interpret(r#"fn main() print("hello20") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_20: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello20
");
}

#[test]
fn str_lit_21() {
    let (out, diags) = interpret(r#"fn main() print("hello21") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_21: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello21
");
}

#[test]
fn str_lit_22() {
    let (out, diags) = interpret(r#"fn main() print("hello22") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_22: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello22
");
}

#[test]
fn str_lit_23() {
    let (out, diags) = interpret(r#"fn main() print("hello23") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_23: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello23
");
}

#[test]
fn str_lit_24() {
    let (out, diags) = interpret(r#"fn main() print("hello24") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_24: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello24
");
}

#[test]
fn str_lit_25() {
    let (out, diags) = interpret(r#"fn main() print("hello25") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_25: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello25
");
}

#[test]
fn str_lit_26() {
    let (out, diags) = interpret(r#"fn main() print("hello26") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_26: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello26
");
}

#[test]
fn str_lit_27() {
    let (out, diags) = interpret(r#"fn main() print("hello27") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_27: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello27
");
}

#[test]
fn str_lit_28() {
    let (out, diags) = interpret(r#"fn main() print("hello28") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_28: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello28
");
}

#[test]
fn str_lit_29() {
    let (out, diags) = interpret(r#"fn main() print("hello29") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_29: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello29
");
}

#[test]
fn str_lit_30() {
    let (out, diags) = interpret(r#"fn main() print("hello30") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_30: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello30
");
}

#[test]
fn str_lit_31() {
    let (out, diags) = interpret(r#"fn main() print("hello31") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_31: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello31
");
}

#[test]
fn str_lit_32() {
    let (out, diags) = interpret(r#"fn main() print("hello32") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_32: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello32
");
}

#[test]
fn str_lit_33() {
    let (out, diags) = interpret(r#"fn main() print("hello33") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_33: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello33
");
}

#[test]
fn str_lit_34() {
    let (out, diags) = interpret(r#"fn main() print("hello34") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_34: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello34
");
}

#[test]
fn str_lit_35() {
    let (out, diags) = interpret(r#"fn main() print("hello35") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_35: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello35
");
}

#[test]
fn str_lit_36() {
    let (out, diags) = interpret(r#"fn main() print("hello36") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_36: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello36
");
}

#[test]
fn str_lit_37() {
    let (out, diags) = interpret(r#"fn main() print("hello37") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_37: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello37
");
}

#[test]
fn str_lit_38() {
    let (out, diags) = interpret(r#"fn main() print("hello38") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_38: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello38
");
}

#[test]
fn str_lit_39() {
    let (out, diags) = interpret(r#"fn main() print("hello39") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_39: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello39
");
}

#[test]
fn str_lit_40() {
    let (out, diags) = interpret(r#"fn main() print("hello40") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_40: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello40
");
}

#[test]
fn str_lit_41() {
    let (out, diags) = interpret(r#"fn main() print("hello41") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_41: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello41
");
}

#[test]
fn str_lit_42() {
    let (out, diags) = interpret(r#"fn main() print("hello42") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_42: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello42
");
}

#[test]
fn str_lit_43() {
    let (out, diags) = interpret(r#"fn main() print("hello43") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_43: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello43
");
}

#[test]
fn str_lit_44() {
    let (out, diags) = interpret(r#"fn main() print("hello44") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_44: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello44
");
}

#[test]
fn str_lit_45() {
    let (out, diags) = interpret(r#"fn main() print("hello45") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_45: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello45
");
}

#[test]
fn str_lit_46() {
    let (out, diags) = interpret(r#"fn main() print("hello46") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_46: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello46
");
}

#[test]
fn str_lit_47() {
    let (out, diags) = interpret(r#"fn main() print("hello47") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_47: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello47
");
}

#[test]
fn str_lit_48() {
    let (out, diags) = interpret(r#"fn main() print("hello48") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_48: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello48
");
}

#[test]
fn str_lit_49() {
    let (out, diags) = interpret(r#"fn main() print("hello49") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_49: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello49
");
}

#[test]
fn str_lit_50() {
    let (out, diags) = interpret(r#"fn main() print("hello50") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_50: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello50
");
}

#[test]
fn str_lit_51() {
    let (out, diags) = interpret(r#"fn main() print("hello51") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_51: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello51
");
}

#[test]
fn str_lit_52() {
    let (out, diags) = interpret(r#"fn main() print("hello52") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_52: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello52
");
}

#[test]
fn str_lit_53() {
    let (out, diags) = interpret(r#"fn main() print("hello53") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_53: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello53
");
}

#[test]
fn str_lit_54() {
    let (out, diags) = interpret(r#"fn main() print("hello54") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_54: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello54
");
}

#[test]
fn str_lit_55() {
    let (out, diags) = interpret(r#"fn main() print("hello55") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_55: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello55
");
}

#[test]
fn str_lit_56() {
    let (out, diags) = interpret(r#"fn main() print("hello56") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_56: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello56
");
}

#[test]
fn str_lit_57() {
    let (out, diags) = interpret(r#"fn main() print("hello57") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_57: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello57
");
}

#[test]
fn str_lit_58() {
    let (out, diags) = interpret(r#"fn main() print("hello58") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_58: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello58
");
}

#[test]
fn str_lit_59() {
    let (out, diags) = interpret(r#"fn main() print("hello59") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_59: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello59
");
}

#[test]
fn str_lit_60() {
    let (out, diags) = interpret(r#"fn main() print("hello60") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_60: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello60
");
}

#[test]
fn str_lit_61() {
    let (out, diags) = interpret(r#"fn main() print("hello61") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_61: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello61
");
}

#[test]
fn str_lit_62() {
    let (out, diags) = interpret(r#"fn main() print("hello62") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_62: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello62
");
}

#[test]
fn str_lit_63() {
    let (out, diags) = interpret(r#"fn main() print("hello63") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_63: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello63
");
}

#[test]
fn str_lit_64() {
    let (out, diags) = interpret(r#"fn main() print("hello64") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_64: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello64
");
}

#[test]
fn str_lit_65() {
    let (out, diags) = interpret(r#"fn main() print("hello65") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_65: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello65
");
}

#[test]
fn str_lit_66() {
    let (out, diags) = interpret(r#"fn main() print("hello66") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_66: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello66
");
}

#[test]
fn str_lit_67() {
    let (out, diags) = interpret(r#"fn main() print("hello67") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_67: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello67
");
}

#[test]
fn str_lit_68() {
    let (out, diags) = interpret(r#"fn main() print("hello68") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_68: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello68
");
}

#[test]
fn str_lit_69() {
    let (out, diags) = interpret(r#"fn main() print("hello69") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_69: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello69
");
}

#[test]
fn str_lit_70() {
    let (out, diags) = interpret(r#"fn main() print("hello70") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_70: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello70
");
}

#[test]
fn str_lit_71() {
    let (out, diags) = interpret(r#"fn main() print("hello71") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_71: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello71
");
}

#[test]
fn str_lit_72() {
    let (out, diags) = interpret(r#"fn main() print("hello72") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_72: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello72
");
}

#[test]
fn str_lit_73() {
    let (out, diags) = interpret(r#"fn main() print("hello73") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_73: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello73
");
}

#[test]
fn str_lit_74() {
    let (out, diags) = interpret(r#"fn main() print("hello74") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_74: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello74
");
}

#[test]
fn str_lit_75() {
    let (out, diags) = interpret(r#"fn main() print("hello75") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_75: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello75
");
}

#[test]
fn str_lit_76() {
    let (out, diags) = interpret(r#"fn main() print("hello76") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_76: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello76
");
}

#[test]
fn str_lit_77() {
    let (out, diags) = interpret(r#"fn main() print("hello77") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_77: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello77
");
}

#[test]
fn str_lit_78() {
    let (out, diags) = interpret(r#"fn main() print("hello78") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_78: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello78
");
}

#[test]
fn str_lit_79() {
    let (out, diags) = interpret(r#"fn main() print("hello79") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_79: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello79
");
}

#[test]
fn str_lit_80() {
    let (out, diags) = interpret(r#"fn main() print("hello80") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_80: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello80
");
}

#[test]
fn str_lit_81() {
    let (out, diags) = interpret(r#"fn main() print("hello81") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_81: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello81
");
}

#[test]
fn str_lit_82() {
    let (out, diags) = interpret(r#"fn main() print("hello82") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_82: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello82
");
}

#[test]
fn str_lit_83() {
    let (out, diags) = interpret(r#"fn main() print("hello83") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_83: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello83
");
}

#[test]
fn str_lit_84() {
    let (out, diags) = interpret(r#"fn main() print("hello84") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_84: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello84
");
}

#[test]
fn str_lit_85() {
    let (out, diags) = interpret(r#"fn main() print("hello85") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_85: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello85
");
}

#[test]
fn str_lit_86() {
    let (out, diags) = interpret(r#"fn main() print("hello86") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_86: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello86
");
}

#[test]
fn str_lit_87() {
    let (out, diags) = interpret(r#"fn main() print("hello87") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_87: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello87
");
}

#[test]
fn str_lit_88() {
    let (out, diags) = interpret(r#"fn main() print("hello88") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_88: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello88
");
}

#[test]
fn str_lit_89() {
    let (out, diags) = interpret(r#"fn main() print("hello89") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_89: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello89
");
}

#[test]
fn str_lit_90() {
    let (out, diags) = interpret(r#"fn main() print("hello90") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_90: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello90
");
}

#[test]
fn str_lit_91() {
    let (out, diags) = interpret(r#"fn main() print("hello91") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_91: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello91
");
}

#[test]
fn str_lit_92() {
    let (out, diags) = interpret(r#"fn main() print("hello92") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_92: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello92
");
}

#[test]
fn str_lit_93() {
    let (out, diags) = interpret(r#"fn main() print("hello93") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_93: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello93
");
}

#[test]
fn str_lit_94() {
    let (out, diags) = interpret(r#"fn main() print("hello94") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_94: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello94
");
}

#[test]
fn str_lit_95() {
    let (out, diags) = interpret(r#"fn main() print("hello95") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_95: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello95
");
}

#[test]
fn str_lit_96() {
    let (out, diags) = interpret(r#"fn main() print("hello96") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_96: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello96
");
}

#[test]
fn str_lit_97() {
    let (out, diags) = interpret(r#"fn main() print("hello97") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_97: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello97
");
}

#[test]
fn str_lit_98() {
    let (out, diags) = interpret(r#"fn main() print("hello98") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_98: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello98
");
}

#[test]
fn str_lit_99() {
    let (out, diags) = interpret(r#"fn main() print("hello99") end"#, 0);
    assert!(!diags.has_errors(), "str_lit_99: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"hello99
");
}

#[test]
fn str_len_0() {
    let (out, diags) = interpret(r#"fn main() print(len("hello0")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_0: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_1() {
    let (out, diags) = interpret(r#"fn main() print(len("hello1")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_1: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_2() {
    let (out, diags) = interpret(r#"fn main() print(len("hello2")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_2: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_3() {
    let (out, diags) = interpret(r#"fn main() print(len("hello3")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_3: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_4() {
    let (out, diags) = interpret(r#"fn main() print(len("hello4")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_4: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_5() {
    let (out, diags) = interpret(r#"fn main() print(len("hello5")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_5: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_6() {
    let (out, diags) = interpret(r#"fn main() print(len("hello6")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_6: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_7() {
    let (out, diags) = interpret(r#"fn main() print(len("hello7")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_7: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_8() {
    let (out, diags) = interpret(r#"fn main() print(len("hello8")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_8: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_9() {
    let (out, diags) = interpret(r#"fn main() print(len("hello9")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_9: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"6
");
}

#[test]
fn str_len_10() {
    let (out, diags) = interpret(r#"fn main() print(len("hello10")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_10: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_11() {
    let (out, diags) = interpret(r#"fn main() print(len("hello11")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_11: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_12() {
    let (out, diags) = interpret(r#"fn main() print(len("hello12")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_12: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_13() {
    let (out, diags) = interpret(r#"fn main() print(len("hello13")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_13: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_14() {
    let (out, diags) = interpret(r#"fn main() print(len("hello14")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_14: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_15() {
    let (out, diags) = interpret(r#"fn main() print(len("hello15")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_15: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_16() {
    let (out, diags) = interpret(r#"fn main() print(len("hello16")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_16: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_17() {
    let (out, diags) = interpret(r#"fn main() print(len("hello17")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_17: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_18() {
    let (out, diags) = interpret(r#"fn main() print(len("hello18")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_18: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_19() {
    let (out, diags) = interpret(r#"fn main() print(len("hello19")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_19: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_20() {
    let (out, diags) = interpret(r#"fn main() print(len("hello20")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_20: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_21() {
    let (out, diags) = interpret(r#"fn main() print(len("hello21")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_21: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_22() {
    let (out, diags) = interpret(r#"fn main() print(len("hello22")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_22: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_23() {
    let (out, diags) = interpret(r#"fn main() print(len("hello23")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_23: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_24() {
    let (out, diags) = interpret(r#"fn main() print(len("hello24")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_24: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_25() {
    let (out, diags) = interpret(r#"fn main() print(len("hello25")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_25: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_26() {
    let (out, diags) = interpret(r#"fn main() print(len("hello26")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_26: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_27() {
    let (out, diags) = interpret(r#"fn main() print(len("hello27")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_27: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_28() {
    let (out, diags) = interpret(r#"fn main() print(len("hello28")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_28: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_29() {
    let (out, diags) = interpret(r#"fn main() print(len("hello29")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_29: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_30() {
    let (out, diags) = interpret(r#"fn main() print(len("hello30")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_30: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_31() {
    let (out, diags) = interpret(r#"fn main() print(len("hello31")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_31: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_32() {
    let (out, diags) = interpret(r#"fn main() print(len("hello32")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_32: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_33() {
    let (out, diags) = interpret(r#"fn main() print(len("hello33")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_33: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_34() {
    let (out, diags) = interpret(r#"fn main() print(len("hello34")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_34: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_35() {
    let (out, diags) = interpret(r#"fn main() print(len("hello35")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_35: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_36() {
    let (out, diags) = interpret(r#"fn main() print(len("hello36")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_36: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_37() {
    let (out, diags) = interpret(r#"fn main() print(len("hello37")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_37: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_38() {
    let (out, diags) = interpret(r#"fn main() print(len("hello38")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_38: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_39() {
    let (out, diags) = interpret(r#"fn main() print(len("hello39")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_39: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_40() {
    let (out, diags) = interpret(r#"fn main() print(len("hello40")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_40: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_41() {
    let (out, diags) = interpret(r#"fn main() print(len("hello41")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_41: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_42() {
    let (out, diags) = interpret(r#"fn main() print(len("hello42")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_42: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_43() {
    let (out, diags) = interpret(r#"fn main() print(len("hello43")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_43: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_44() {
    let (out, diags) = interpret(r#"fn main() print(len("hello44")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_44: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_45() {
    let (out, diags) = interpret(r#"fn main() print(len("hello45")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_45: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_46() {
    let (out, diags) = interpret(r#"fn main() print(len("hello46")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_46: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_47() {
    let (out, diags) = interpret(r#"fn main() print(len("hello47")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_47: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_48() {
    let (out, diags) = interpret(r#"fn main() print(len("hello48")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_48: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_len_49() {
    let (out, diags) = interpret(r#"fn main() print(len("hello49")) end"#, 0);
    assert!(!diags.has_errors(), "str_len_49: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"7
");
}

#[test]
fn str_concat_0() {
    let (out, diags) = interpret(r#"fn main() print("a0" + "b0") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_0: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a0b0
");
}

#[test]
fn str_concat_1() {
    let (out, diags) = interpret(r#"fn main() print("a1" + "b1") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_1: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a1b1
");
}

#[test]
fn str_concat_2() {
    let (out, diags) = interpret(r#"fn main() print("a2" + "b2") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_2: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a2b2
");
}

#[test]
fn str_concat_3() {
    let (out, diags) = interpret(r#"fn main() print("a3" + "b3") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_3: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a3b3
");
}

#[test]
fn str_concat_4() {
    let (out, diags) = interpret(r#"fn main() print("a4" + "b4") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_4: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a4b4
");
}

#[test]
fn str_concat_5() {
    let (out, diags) = interpret(r#"fn main() print("a5" + "b5") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_5: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a5b5
");
}

#[test]
fn str_concat_6() {
    let (out, diags) = interpret(r#"fn main() print("a6" + "b6") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_6: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a6b6
");
}

#[test]
fn str_concat_7() {
    let (out, diags) = interpret(r#"fn main() print("a7" + "b7") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_7: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a7b7
");
}

#[test]
fn str_concat_8() {
    let (out, diags) = interpret(r#"fn main() print("a8" + "b8") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_8: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a8b8
");
}

#[test]
fn str_concat_9() {
    let (out, diags) = interpret(r#"fn main() print("a9" + "b9") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_9: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a9b9
");
}

#[test]
fn str_concat_10() {
    let (out, diags) = interpret(r#"fn main() print("a10" + "b10") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_10: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a10b10
");
}

#[test]
fn str_concat_11() {
    let (out, diags) = interpret(r#"fn main() print("a11" + "b11") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_11: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a11b11
");
}

#[test]
fn str_concat_12() {
    let (out, diags) = interpret(r#"fn main() print("a12" + "b12") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_12: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a12b12
");
}

#[test]
fn str_concat_13() {
    let (out, diags) = interpret(r#"fn main() print("a13" + "b13") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_13: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a13b13
");
}

#[test]
fn str_concat_14() {
    let (out, diags) = interpret(r#"fn main() print("a14" + "b14") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_14: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a14b14
");
}

#[test]
fn str_concat_15() {
    let (out, diags) = interpret(r#"fn main() print("a15" + "b15") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_15: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a15b15
");
}

#[test]
fn str_concat_16() {
    let (out, diags) = interpret(r#"fn main() print("a16" + "b16") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_16: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a16b16
");
}

#[test]
fn str_concat_17() {
    let (out, diags) = interpret(r#"fn main() print("a17" + "b17") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_17: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a17b17
");
}

#[test]
fn str_concat_18() {
    let (out, diags) = interpret(r#"fn main() print("a18" + "b18") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_18: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a18b18
");
}

#[test]
fn str_concat_19() {
    let (out, diags) = interpret(r#"fn main() print("a19" + "b19") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_19: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a19b19
");
}

#[test]
fn str_concat_20() {
    let (out, diags) = interpret(r#"fn main() print("a20" + "b20") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_20: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a20b20
");
}

#[test]
fn str_concat_21() {
    let (out, diags) = interpret(r#"fn main() print("a21" + "b21") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_21: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a21b21
");
}

#[test]
fn str_concat_22() {
    let (out, diags) = interpret(r#"fn main() print("a22" + "b22") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_22: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a22b22
");
}

#[test]
fn str_concat_23() {
    let (out, diags) = interpret(r#"fn main() print("a23" + "b23") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_23: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a23b23
");
}

#[test]
fn str_concat_24() {
    let (out, diags) = interpret(r#"fn main() print("a24" + "b24") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_24: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a24b24
");
}

#[test]
fn str_concat_25() {
    let (out, diags) = interpret(r#"fn main() print("a25" + "b25") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_25: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a25b25
");
}

#[test]
fn str_concat_26() {
    let (out, diags) = interpret(r#"fn main() print("a26" + "b26") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_26: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a26b26
");
}

#[test]
fn str_concat_27() {
    let (out, diags) = interpret(r#"fn main() print("a27" + "b27") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_27: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a27b27
");
}

#[test]
fn str_concat_28() {
    let (out, diags) = interpret(r#"fn main() print("a28" + "b28") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_28: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a28b28
");
}

#[test]
fn str_concat_29() {
    let (out, diags) = interpret(r#"fn main() print("a29" + "b29") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_29: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a29b29
");
}

#[test]
fn str_concat_30() {
    let (out, diags) = interpret(r#"fn main() print("a30" + "b30") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_30: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a30b30
");
}

#[test]
fn str_concat_31() {
    let (out, diags) = interpret(r#"fn main() print("a31" + "b31") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_31: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a31b31
");
}

#[test]
fn str_concat_32() {
    let (out, diags) = interpret(r#"fn main() print("a32" + "b32") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_32: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a32b32
");
}

#[test]
fn str_concat_33() {
    let (out, diags) = interpret(r#"fn main() print("a33" + "b33") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_33: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a33b33
");
}

#[test]
fn str_concat_34() {
    let (out, diags) = interpret(r#"fn main() print("a34" + "b34") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_34: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a34b34
");
}

#[test]
fn str_concat_35() {
    let (out, diags) = interpret(r#"fn main() print("a35" + "b35") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_35: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a35b35
");
}

#[test]
fn str_concat_36() {
    let (out, diags) = interpret(r#"fn main() print("a36" + "b36") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_36: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a36b36
");
}

#[test]
fn str_concat_37() {
    let (out, diags) = interpret(r#"fn main() print("a37" + "b37") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_37: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a37b37
");
}

#[test]
fn str_concat_38() {
    let (out, diags) = interpret(r#"fn main() print("a38" + "b38") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_38: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a38b38
");
}

#[test]
fn str_concat_39() {
    let (out, diags) = interpret(r#"fn main() print("a39" + "b39") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_39: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a39b39
");
}

#[test]
fn str_concat_40() {
    let (out, diags) = interpret(r#"fn main() print("a40" + "b40") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_40: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a40b40
");
}

#[test]
fn str_concat_41() {
    let (out, diags) = interpret(r#"fn main() print("a41" + "b41") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_41: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a41b41
");
}

#[test]
fn str_concat_42() {
    let (out, diags) = interpret(r#"fn main() print("a42" + "b42") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_42: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a42b42
");
}

#[test]
fn str_concat_43() {
    let (out, diags) = interpret(r#"fn main() print("a43" + "b43") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_43: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a43b43
");
}

#[test]
fn str_concat_44() {
    let (out, diags) = interpret(r#"fn main() print("a44" + "b44") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_44: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a44b44
");
}

#[test]
fn str_concat_45() {
    let (out, diags) = interpret(r#"fn main() print("a45" + "b45") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_45: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a45b45
");
}

#[test]
fn str_concat_46() {
    let (out, diags) = interpret(r#"fn main() print("a46" + "b46") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_46: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a46b46
");
}

#[test]
fn str_concat_47() {
    let (out, diags) = interpret(r#"fn main() print("a47" + "b47") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_47: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a47b47
");
}

#[test]
fn str_concat_48() {
    let (out, diags) = interpret(r#"fn main() print("a48" + "b48") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_48: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a48b48
");
}

#[test]
fn str_concat_49() {
    let (out, diags) = interpret(r#"fn main() print("a49" + "b49") end"#, 0);
    assert!(!diags.has_errors(), "str_concat_49: expected no errors, got: {:?}", diags.items);
    assert_eq!(out, r"a49b49
");
}

