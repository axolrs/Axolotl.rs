// Owner: PascalElixir / axolrs (GitHub org)
// File: Auto-generated grid tests.


#[test]
fn parse_fn_0() {
    let (m, d) = axolc_core::parse("fn f0() print(0) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1() {
    let (m, d) = axolc_core::parse("fn f1() print(1) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_2() {
    let (m, d) = axolc_core::parse("fn f2() print(2) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_3() {
    let (m, d) = axolc_core::parse("fn f3() print(3) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_4() {
    let (m, d) = axolc_core::parse("fn f4() print(4) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_5() {
    let (m, d) = axolc_core::parse("fn f5() print(5) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_6() {
    let (m, d) = axolc_core::parse("fn f6() print(6) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_7() {
    let (m, d) = axolc_core::parse("fn f7() print(7) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_8() {
    let (m, d) = axolc_core::parse("fn f8() print(8) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_9() {
    let (m, d) = axolc_core::parse("fn f9() print(9) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_10() {
    let (m, d) = axolc_core::parse("fn f10() print(10) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_11() {
    let (m, d) = axolc_core::parse("fn f11() print(11) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_12() {
    let (m, d) = axolc_core::parse("fn f12() print(12) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_13() {
    let (m, d) = axolc_core::parse("fn f13() print(13) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_14() {
    let (m, d) = axolc_core::parse("fn f14() print(14) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_15() {
    let (m, d) = axolc_core::parse("fn f15() print(15) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_16() {
    let (m, d) = axolc_core::parse("fn f16() print(16) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_17() {
    let (m, d) = axolc_core::parse("fn f17() print(17) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_18() {
    let (m, d) = axolc_core::parse("fn f18() print(18) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_19() {
    let (m, d) = axolc_core::parse("fn f19() print(19) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_20() {
    let (m, d) = axolc_core::parse("fn f20() print(20) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_21() {
    let (m, d) = axolc_core::parse("fn f21() print(21) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_22() {
    let (m, d) = axolc_core::parse("fn f22() print(22) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_23() {
    let (m, d) = axolc_core::parse("fn f23() print(23) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_24() {
    let (m, d) = axolc_core::parse("fn f24() print(24) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_25() {
    let (m, d) = axolc_core::parse("fn f25() print(25) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_26() {
    let (m, d) = axolc_core::parse("fn f26() print(26) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_27() {
    let (m, d) = axolc_core::parse("fn f27() print(27) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_28() {
    let (m, d) = axolc_core::parse("fn f28() print(28) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_29() {
    let (m, d) = axolc_core::parse("fn f29() print(29) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_30() {
    let (m, d) = axolc_core::parse("fn f30() print(30) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_31() {
    let (m, d) = axolc_core::parse("fn f31() print(31) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_32() {
    let (m, d) = axolc_core::parse("fn f32() print(32) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_33() {
    let (m, d) = axolc_core::parse("fn f33() print(33) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_34() {
    let (m, d) = axolc_core::parse("fn f34() print(34) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_35() {
    let (m, d) = axolc_core::parse("fn f35() print(35) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_36() {
    let (m, d) = axolc_core::parse("fn f36() print(36) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_37() {
    let (m, d) = axolc_core::parse("fn f37() print(37) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_38() {
    let (m, d) = axolc_core::parse("fn f38() print(38) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_39() {
    let (m, d) = axolc_core::parse("fn f39() print(39) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_40() {
    let (m, d) = axolc_core::parse("fn f40() print(40) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_41() {
    let (m, d) = axolc_core::parse("fn f41() print(41) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_42() {
    let (m, d) = axolc_core::parse("fn f42() print(42) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_43() {
    let (m, d) = axolc_core::parse("fn f43() print(43) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_44() {
    let (m, d) = axolc_core::parse("fn f44() print(44) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_45() {
    let (m, d) = axolc_core::parse("fn f45() print(45) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_46() {
    let (m, d) = axolc_core::parse("fn f46() print(46) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_47() {
    let (m, d) = axolc_core::parse("fn f47() print(47) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_48() {
    let (m, d) = axolc_core::parse("fn f48() print(48) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_49() {
    let (m, d) = axolc_core::parse("fn f49() print(49) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_50() {
    let (m, d) = axolc_core::parse("fn f50() print(50) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_51() {
    let (m, d) = axolc_core::parse("fn f51() print(51) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_52() {
    let (m, d) = axolc_core::parse("fn f52() print(52) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_53() {
    let (m, d) = axolc_core::parse("fn f53() print(53) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_54() {
    let (m, d) = axolc_core::parse("fn f54() print(54) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_55() {
    let (m, d) = axolc_core::parse("fn f55() print(55) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_56() {
    let (m, d) = axolc_core::parse("fn f56() print(56) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_57() {
    let (m, d) = axolc_core::parse("fn f57() print(57) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_58() {
    let (m, d) = axolc_core::parse("fn f58() print(58) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_59() {
    let (m, d) = axolc_core::parse("fn f59() print(59) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_60() {
    let (m, d) = axolc_core::parse("fn f60() print(60) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_61() {
    let (m, d) = axolc_core::parse("fn f61() print(61) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_62() {
    let (m, d) = axolc_core::parse("fn f62() print(62) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_63() {
    let (m, d) = axolc_core::parse("fn f63() print(63) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_64() {
    let (m, d) = axolc_core::parse("fn f64() print(64) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_65() {
    let (m, d) = axolc_core::parse("fn f65() print(65) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_66() {
    let (m, d) = axolc_core::parse("fn f66() print(66) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_67() {
    let (m, d) = axolc_core::parse("fn f67() print(67) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_68() {
    let (m, d) = axolc_core::parse("fn f68() print(68) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_69() {
    let (m, d) = axolc_core::parse("fn f69() print(69) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_70() {
    let (m, d) = axolc_core::parse("fn f70() print(70) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_71() {
    let (m, d) = axolc_core::parse("fn f71() print(71) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_72() {
    let (m, d) = axolc_core::parse("fn f72() print(72) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_73() {
    let (m, d) = axolc_core::parse("fn f73() print(73) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_74() {
    let (m, d) = axolc_core::parse("fn f74() print(74) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_75() {
    let (m, d) = axolc_core::parse("fn f75() print(75) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_76() {
    let (m, d) = axolc_core::parse("fn f76() print(76) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_77() {
    let (m, d) = axolc_core::parse("fn f77() print(77) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_78() {
    let (m, d) = axolc_core::parse("fn f78() print(78) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_79() {
    let (m, d) = axolc_core::parse("fn f79() print(79) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_80() {
    let (m, d) = axolc_core::parse("fn f80() print(80) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_81() {
    let (m, d) = axolc_core::parse("fn f81() print(81) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_82() {
    let (m, d) = axolc_core::parse("fn f82() print(82) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_83() {
    let (m, d) = axolc_core::parse("fn f83() print(83) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_84() {
    let (m, d) = axolc_core::parse("fn f84() print(84) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_85() {
    let (m, d) = axolc_core::parse("fn f85() print(85) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_86() {
    let (m, d) = axolc_core::parse("fn f86() print(86) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_87() {
    let (m, d) = axolc_core::parse("fn f87() print(87) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_88() {
    let (m, d) = axolc_core::parse("fn f88() print(88) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_89() {
    let (m, d) = axolc_core::parse("fn f89() print(89) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_90() {
    let (m, d) = axolc_core::parse("fn f90() print(90) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_91() {
    let (m, d) = axolc_core::parse("fn f91() print(91) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_92() {
    let (m, d) = axolc_core::parse("fn f92() print(92) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_93() {
    let (m, d) = axolc_core::parse("fn f93() print(93) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_94() {
    let (m, d) = axolc_core::parse("fn f94() print(94) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_95() {
    let (m, d) = axolc_core::parse("fn f95() print(95) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_96() {
    let (m, d) = axolc_core::parse("fn f96() print(96) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_97() {
    let (m, d) = axolc_core::parse("fn f97() print(97) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_98() {
    let (m, d) = axolc_core::parse("fn f98() print(98) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_99() {
    let (m, d) = axolc_core::parse("fn f99() print(99) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_100() {
    let (m, d) = axolc_core::parse("fn f100() print(100) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_101() {
    let (m, d) = axolc_core::parse("fn f101() print(101) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_102() {
    let (m, d) = axolc_core::parse("fn f102() print(102) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_103() {
    let (m, d) = axolc_core::parse("fn f103() print(103) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_104() {
    let (m, d) = axolc_core::parse("fn f104() print(104) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_105() {
    let (m, d) = axolc_core::parse("fn f105() print(105) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_106() {
    let (m, d) = axolc_core::parse("fn f106() print(106) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_107() {
    let (m, d) = axolc_core::parse("fn f107() print(107) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_108() {
    let (m, d) = axolc_core::parse("fn f108() print(108) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_109() {
    let (m, d) = axolc_core::parse("fn f109() print(109) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_110() {
    let (m, d) = axolc_core::parse("fn f110() print(110) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_111() {
    let (m, d) = axolc_core::parse("fn f111() print(111) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_112() {
    let (m, d) = axolc_core::parse("fn f112() print(112) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_113() {
    let (m, d) = axolc_core::parse("fn f113() print(113) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_114() {
    let (m, d) = axolc_core::parse("fn f114() print(114) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_115() {
    let (m, d) = axolc_core::parse("fn f115() print(115) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_116() {
    let (m, d) = axolc_core::parse("fn f116() print(116) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_117() {
    let (m, d) = axolc_core::parse("fn f117() print(117) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_118() {
    let (m, d) = axolc_core::parse("fn f118() print(118) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_119() {
    let (m, d) = axolc_core::parse("fn f119() print(119) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_120() {
    let (m, d) = axolc_core::parse("fn f120() print(120) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_121() {
    let (m, d) = axolc_core::parse("fn f121() print(121) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_122() {
    let (m, d) = axolc_core::parse("fn f122() print(122) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_123() {
    let (m, d) = axolc_core::parse("fn f123() print(123) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_124() {
    let (m, d) = axolc_core::parse("fn f124() print(124) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_125() {
    let (m, d) = axolc_core::parse("fn f125() print(125) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_126() {
    let (m, d) = axolc_core::parse("fn f126() print(126) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_127() {
    let (m, d) = axolc_core::parse("fn f127() print(127) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_128() {
    let (m, d) = axolc_core::parse("fn f128() print(128) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_129() {
    let (m, d) = axolc_core::parse("fn f129() print(129) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_130() {
    let (m, d) = axolc_core::parse("fn f130() print(130) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_131() {
    let (m, d) = axolc_core::parse("fn f131() print(131) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_132() {
    let (m, d) = axolc_core::parse("fn f132() print(132) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_133() {
    let (m, d) = axolc_core::parse("fn f133() print(133) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_134() {
    let (m, d) = axolc_core::parse("fn f134() print(134) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_135() {
    let (m, d) = axolc_core::parse("fn f135() print(135) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_136() {
    let (m, d) = axolc_core::parse("fn f136() print(136) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_137() {
    let (m, d) = axolc_core::parse("fn f137() print(137) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_138() {
    let (m, d) = axolc_core::parse("fn f138() print(138) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_139() {
    let (m, d) = axolc_core::parse("fn f139() print(139) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_140() {
    let (m, d) = axolc_core::parse("fn f140() print(140) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_141() {
    let (m, d) = axolc_core::parse("fn f141() print(141) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_142() {
    let (m, d) = axolc_core::parse("fn f142() print(142) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_143() {
    let (m, d) = axolc_core::parse("fn f143() print(143) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_144() {
    let (m, d) = axolc_core::parse("fn f144() print(144) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_145() {
    let (m, d) = axolc_core::parse("fn f145() print(145) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_146() {
    let (m, d) = axolc_core::parse("fn f146() print(146) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_147() {
    let (m, d) = axolc_core::parse("fn f147() print(147) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_148() {
    let (m, d) = axolc_core::parse("fn f148() print(148) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_149() {
    let (m, d) = axolc_core::parse("fn f149() print(149) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_150() {
    let (m, d) = axolc_core::parse("fn f150() print(150) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_151() {
    let (m, d) = axolc_core::parse("fn f151() print(151) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_152() {
    let (m, d) = axolc_core::parse("fn f152() print(152) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_153() {
    let (m, d) = axolc_core::parse("fn f153() print(153) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_154() {
    let (m, d) = axolc_core::parse("fn f154() print(154) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_155() {
    let (m, d) = axolc_core::parse("fn f155() print(155) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_156() {
    let (m, d) = axolc_core::parse("fn f156() print(156) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_157() {
    let (m, d) = axolc_core::parse("fn f157() print(157) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_158() {
    let (m, d) = axolc_core::parse("fn f158() print(158) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_159() {
    let (m, d) = axolc_core::parse("fn f159() print(159) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_160() {
    let (m, d) = axolc_core::parse("fn f160() print(160) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_161() {
    let (m, d) = axolc_core::parse("fn f161() print(161) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_162() {
    let (m, d) = axolc_core::parse("fn f162() print(162) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_163() {
    let (m, d) = axolc_core::parse("fn f163() print(163) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_164() {
    let (m, d) = axolc_core::parse("fn f164() print(164) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_165() {
    let (m, d) = axolc_core::parse("fn f165() print(165) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_166() {
    let (m, d) = axolc_core::parse("fn f166() print(166) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_167() {
    let (m, d) = axolc_core::parse("fn f167() print(167) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_168() {
    let (m, d) = axolc_core::parse("fn f168() print(168) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_169() {
    let (m, d) = axolc_core::parse("fn f169() print(169) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_170() {
    let (m, d) = axolc_core::parse("fn f170() print(170) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_171() {
    let (m, d) = axolc_core::parse("fn f171() print(171) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_172() {
    let (m, d) = axolc_core::parse("fn f172() print(172) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_173() {
    let (m, d) = axolc_core::parse("fn f173() print(173) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_174() {
    let (m, d) = axolc_core::parse("fn f174() print(174) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_175() {
    let (m, d) = axolc_core::parse("fn f175() print(175) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_176() {
    let (m, d) = axolc_core::parse("fn f176() print(176) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_177() {
    let (m, d) = axolc_core::parse("fn f177() print(177) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_178() {
    let (m, d) = axolc_core::parse("fn f178() print(178) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_179() {
    let (m, d) = axolc_core::parse("fn f179() print(179) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_180() {
    let (m, d) = axolc_core::parse("fn f180() print(180) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_181() {
    let (m, d) = axolc_core::parse("fn f181() print(181) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_182() {
    let (m, d) = axolc_core::parse("fn f182() print(182) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_183() {
    let (m, d) = axolc_core::parse("fn f183() print(183) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_184() {
    let (m, d) = axolc_core::parse("fn f184() print(184) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_185() {
    let (m, d) = axolc_core::parse("fn f185() print(185) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_186() {
    let (m, d) = axolc_core::parse("fn f186() print(186) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_187() {
    let (m, d) = axolc_core::parse("fn f187() print(187) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_188() {
    let (m, d) = axolc_core::parse("fn f188() print(188) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_189() {
    let (m, d) = axolc_core::parse("fn f189() print(189) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_190() {
    let (m, d) = axolc_core::parse("fn f190() print(190) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_191() {
    let (m, d) = axolc_core::parse("fn f191() print(191) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_192() {
    let (m, d) = axolc_core::parse("fn f192() print(192) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_193() {
    let (m, d) = axolc_core::parse("fn f193() print(193) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_194() {
    let (m, d) = axolc_core::parse("fn f194() print(194) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_195() {
    let (m, d) = axolc_core::parse("fn f195() print(195) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_196() {
    let (m, d) = axolc_core::parse("fn f196() print(196) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_197() {
    let (m, d) = axolc_core::parse("fn f197() print(197) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_198() {
    let (m, d) = axolc_core::parse("fn f198() print(198) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_199() {
    let (m, d) = axolc_core::parse("fn f199() print(199) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_200() {
    let (m, d) = axolc_core::parse("fn f200() print(200) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_201() {
    let (m, d) = axolc_core::parse("fn f201() print(201) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_202() {
    let (m, d) = axolc_core::parse("fn f202() print(202) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_203() {
    let (m, d) = axolc_core::parse("fn f203() print(203) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_204() {
    let (m, d) = axolc_core::parse("fn f204() print(204) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_205() {
    let (m, d) = axolc_core::parse("fn f205() print(205) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_206() {
    let (m, d) = axolc_core::parse("fn f206() print(206) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_207() {
    let (m, d) = axolc_core::parse("fn f207() print(207) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_208() {
    let (m, d) = axolc_core::parse("fn f208() print(208) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_209() {
    let (m, d) = axolc_core::parse("fn f209() print(209) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_210() {
    let (m, d) = axolc_core::parse("fn f210() print(210) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_211() {
    let (m, d) = axolc_core::parse("fn f211() print(211) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_212() {
    let (m, d) = axolc_core::parse("fn f212() print(212) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_213() {
    let (m, d) = axolc_core::parse("fn f213() print(213) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_214() {
    let (m, d) = axolc_core::parse("fn f214() print(214) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_215() {
    let (m, d) = axolc_core::parse("fn f215() print(215) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_216() {
    let (m, d) = axolc_core::parse("fn f216() print(216) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_217() {
    let (m, d) = axolc_core::parse("fn f217() print(217) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_218() {
    let (m, d) = axolc_core::parse("fn f218() print(218) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_219() {
    let (m, d) = axolc_core::parse("fn f219() print(219) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_220() {
    let (m, d) = axolc_core::parse("fn f220() print(220) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_221() {
    let (m, d) = axolc_core::parse("fn f221() print(221) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_222() {
    let (m, d) = axolc_core::parse("fn f222() print(222) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_223() {
    let (m, d) = axolc_core::parse("fn f223() print(223) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_224() {
    let (m, d) = axolc_core::parse("fn f224() print(224) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_225() {
    let (m, d) = axolc_core::parse("fn f225() print(225) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_226() {
    let (m, d) = axolc_core::parse("fn f226() print(226) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_227() {
    let (m, d) = axolc_core::parse("fn f227() print(227) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_228() {
    let (m, d) = axolc_core::parse("fn f228() print(228) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_229() {
    let (m, d) = axolc_core::parse("fn f229() print(229) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_230() {
    let (m, d) = axolc_core::parse("fn f230() print(230) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_231() {
    let (m, d) = axolc_core::parse("fn f231() print(231) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_232() {
    let (m, d) = axolc_core::parse("fn f232() print(232) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_233() {
    let (m, d) = axolc_core::parse("fn f233() print(233) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_234() {
    let (m, d) = axolc_core::parse("fn f234() print(234) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_235() {
    let (m, d) = axolc_core::parse("fn f235() print(235) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_236() {
    let (m, d) = axolc_core::parse("fn f236() print(236) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_237() {
    let (m, d) = axolc_core::parse("fn f237() print(237) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_238() {
    let (m, d) = axolc_core::parse("fn f238() print(238) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_239() {
    let (m, d) = axolc_core::parse("fn f239() print(239) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_240() {
    let (m, d) = axolc_core::parse("fn f240() print(240) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_241() {
    let (m, d) = axolc_core::parse("fn f241() print(241) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_242() {
    let (m, d) = axolc_core::parse("fn f242() print(242) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_243() {
    let (m, d) = axolc_core::parse("fn f243() print(243) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_244() {
    let (m, d) = axolc_core::parse("fn f244() print(244) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_245() {
    let (m, d) = axolc_core::parse("fn f245() print(245) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_246() {
    let (m, d) = axolc_core::parse("fn f246() print(246) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_247() {
    let (m, d) = axolc_core::parse("fn f247() print(247) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_248() {
    let (m, d) = axolc_core::parse("fn f248() print(248) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_249() {
    let (m, d) = axolc_core::parse("fn f249() print(249) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_250() {
    let (m, d) = axolc_core::parse("fn f250() print(250) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_251() {
    let (m, d) = axolc_core::parse("fn f251() print(251) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_252() {
    let (m, d) = axolc_core::parse("fn f252() print(252) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_253() {
    let (m, d) = axolc_core::parse("fn f253() print(253) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_254() {
    let (m, d) = axolc_core::parse("fn f254() print(254) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_255() {
    let (m, d) = axolc_core::parse("fn f255() print(255) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_256() {
    let (m, d) = axolc_core::parse("fn f256() print(256) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_257() {
    let (m, d) = axolc_core::parse("fn f257() print(257) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_258() {
    let (m, d) = axolc_core::parse("fn f258() print(258) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_259() {
    let (m, d) = axolc_core::parse("fn f259() print(259) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_260() {
    let (m, d) = axolc_core::parse("fn f260() print(260) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_261() {
    let (m, d) = axolc_core::parse("fn f261() print(261) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_262() {
    let (m, d) = axolc_core::parse("fn f262() print(262) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_263() {
    let (m, d) = axolc_core::parse("fn f263() print(263) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_264() {
    let (m, d) = axolc_core::parse("fn f264() print(264) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_265() {
    let (m, d) = axolc_core::parse("fn f265() print(265) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_266() {
    let (m, d) = axolc_core::parse("fn f266() print(266) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_267() {
    let (m, d) = axolc_core::parse("fn f267() print(267) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_268() {
    let (m, d) = axolc_core::parse("fn f268() print(268) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_269() {
    let (m, d) = axolc_core::parse("fn f269() print(269) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_270() {
    let (m, d) = axolc_core::parse("fn f270() print(270) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_271() {
    let (m, d) = axolc_core::parse("fn f271() print(271) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_272() {
    let (m, d) = axolc_core::parse("fn f272() print(272) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_273() {
    let (m, d) = axolc_core::parse("fn f273() print(273) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_274() {
    let (m, d) = axolc_core::parse("fn f274() print(274) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_275() {
    let (m, d) = axolc_core::parse("fn f275() print(275) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_276() {
    let (m, d) = axolc_core::parse("fn f276() print(276) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_277() {
    let (m, d) = axolc_core::parse("fn f277() print(277) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_278() {
    let (m, d) = axolc_core::parse("fn f278() print(278) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_279() {
    let (m, d) = axolc_core::parse("fn f279() print(279) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_280() {
    let (m, d) = axolc_core::parse("fn f280() print(280) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_281() {
    let (m, d) = axolc_core::parse("fn f281() print(281) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_282() {
    let (m, d) = axolc_core::parse("fn f282() print(282) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_283() {
    let (m, d) = axolc_core::parse("fn f283() print(283) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_284() {
    let (m, d) = axolc_core::parse("fn f284() print(284) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_285() {
    let (m, d) = axolc_core::parse("fn f285() print(285) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_286() {
    let (m, d) = axolc_core::parse("fn f286() print(286) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_287() {
    let (m, d) = axolc_core::parse("fn f287() print(287) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_288() {
    let (m, d) = axolc_core::parse("fn f288() print(288) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_289() {
    let (m, d) = axolc_core::parse("fn f289() print(289) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_290() {
    let (m, d) = axolc_core::parse("fn f290() print(290) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_291() {
    let (m, d) = axolc_core::parse("fn f291() print(291) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_292() {
    let (m, d) = axolc_core::parse("fn f292() print(292) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_293() {
    let (m, d) = axolc_core::parse("fn f293() print(293) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_294() {
    let (m, d) = axolc_core::parse("fn f294() print(294) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_295() {
    let (m, d) = axolc_core::parse("fn f295() print(295) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_296() {
    let (m, d) = axolc_core::parse("fn f296() print(296) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_297() {
    let (m, d) = axolc_core::parse("fn f297() print(297) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_298() {
    let (m, d) = axolc_core::parse("fn f298() print(298) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_299() {
    let (m, d) = axolc_core::parse("fn f299() print(299) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_300() {
    let (m, d) = axolc_core::parse("fn f300() print(300) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_301() {
    let (m, d) = axolc_core::parse("fn f301() print(301) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_302() {
    let (m, d) = axolc_core::parse("fn f302() print(302) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_303() {
    let (m, d) = axolc_core::parse("fn f303() print(303) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_304() {
    let (m, d) = axolc_core::parse("fn f304() print(304) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_305() {
    let (m, d) = axolc_core::parse("fn f305() print(305) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_306() {
    let (m, d) = axolc_core::parse("fn f306() print(306) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_307() {
    let (m, d) = axolc_core::parse("fn f307() print(307) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_308() {
    let (m, d) = axolc_core::parse("fn f308() print(308) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_309() {
    let (m, d) = axolc_core::parse("fn f309() print(309) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_310() {
    let (m, d) = axolc_core::parse("fn f310() print(310) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_311() {
    let (m, d) = axolc_core::parse("fn f311() print(311) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_312() {
    let (m, d) = axolc_core::parse("fn f312() print(312) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_313() {
    let (m, d) = axolc_core::parse("fn f313() print(313) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_314() {
    let (m, d) = axolc_core::parse("fn f314() print(314) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_315() {
    let (m, d) = axolc_core::parse("fn f315() print(315) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_316() {
    let (m, d) = axolc_core::parse("fn f316() print(316) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_317() {
    let (m, d) = axolc_core::parse("fn f317() print(317) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_318() {
    let (m, d) = axolc_core::parse("fn f318() print(318) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_319() {
    let (m, d) = axolc_core::parse("fn f319() print(319) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_320() {
    let (m, d) = axolc_core::parse("fn f320() print(320) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_321() {
    let (m, d) = axolc_core::parse("fn f321() print(321) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_322() {
    let (m, d) = axolc_core::parse("fn f322() print(322) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_323() {
    let (m, d) = axolc_core::parse("fn f323() print(323) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_324() {
    let (m, d) = axolc_core::parse("fn f324() print(324) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_325() {
    let (m, d) = axolc_core::parse("fn f325() print(325) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_326() {
    let (m, d) = axolc_core::parse("fn f326() print(326) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_327() {
    let (m, d) = axolc_core::parse("fn f327() print(327) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_328() {
    let (m, d) = axolc_core::parse("fn f328() print(328) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_329() {
    let (m, d) = axolc_core::parse("fn f329() print(329) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_330() {
    let (m, d) = axolc_core::parse("fn f330() print(330) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_331() {
    let (m, d) = axolc_core::parse("fn f331() print(331) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_332() {
    let (m, d) = axolc_core::parse("fn f332() print(332) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_333() {
    let (m, d) = axolc_core::parse("fn f333() print(333) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_334() {
    let (m, d) = axolc_core::parse("fn f334() print(334) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_335() {
    let (m, d) = axolc_core::parse("fn f335() print(335) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_336() {
    let (m, d) = axolc_core::parse("fn f336() print(336) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_337() {
    let (m, d) = axolc_core::parse("fn f337() print(337) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_338() {
    let (m, d) = axolc_core::parse("fn f338() print(338) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_339() {
    let (m, d) = axolc_core::parse("fn f339() print(339) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_340() {
    let (m, d) = axolc_core::parse("fn f340() print(340) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_341() {
    let (m, d) = axolc_core::parse("fn f341() print(341) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_342() {
    let (m, d) = axolc_core::parse("fn f342() print(342) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_343() {
    let (m, d) = axolc_core::parse("fn f343() print(343) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_344() {
    let (m, d) = axolc_core::parse("fn f344() print(344) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_345() {
    let (m, d) = axolc_core::parse("fn f345() print(345) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_346() {
    let (m, d) = axolc_core::parse("fn f346() print(346) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_347() {
    let (m, d) = axolc_core::parse("fn f347() print(347) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_348() {
    let (m, d) = axolc_core::parse("fn f348() print(348) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_349() {
    let (m, d) = axolc_core::parse("fn f349() print(349) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_350() {
    let (m, d) = axolc_core::parse("fn f350() print(350) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_351() {
    let (m, d) = axolc_core::parse("fn f351() print(351) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_352() {
    let (m, d) = axolc_core::parse("fn f352() print(352) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_353() {
    let (m, d) = axolc_core::parse("fn f353() print(353) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_354() {
    let (m, d) = axolc_core::parse("fn f354() print(354) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_355() {
    let (m, d) = axolc_core::parse("fn f355() print(355) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_356() {
    let (m, d) = axolc_core::parse("fn f356() print(356) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_357() {
    let (m, d) = axolc_core::parse("fn f357() print(357) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_358() {
    let (m, d) = axolc_core::parse("fn f358() print(358) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_359() {
    let (m, d) = axolc_core::parse("fn f359() print(359) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_360() {
    let (m, d) = axolc_core::parse("fn f360() print(360) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_361() {
    let (m, d) = axolc_core::parse("fn f361() print(361) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_362() {
    let (m, d) = axolc_core::parse("fn f362() print(362) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_363() {
    let (m, d) = axolc_core::parse("fn f363() print(363) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_364() {
    let (m, d) = axolc_core::parse("fn f364() print(364) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_365() {
    let (m, d) = axolc_core::parse("fn f365() print(365) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_366() {
    let (m, d) = axolc_core::parse("fn f366() print(366) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_367() {
    let (m, d) = axolc_core::parse("fn f367() print(367) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_368() {
    let (m, d) = axolc_core::parse("fn f368() print(368) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_369() {
    let (m, d) = axolc_core::parse("fn f369() print(369) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_370() {
    let (m, d) = axolc_core::parse("fn f370() print(370) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_371() {
    let (m, d) = axolc_core::parse("fn f371() print(371) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_372() {
    let (m, d) = axolc_core::parse("fn f372() print(372) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_373() {
    let (m, d) = axolc_core::parse("fn f373() print(373) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_374() {
    let (m, d) = axolc_core::parse("fn f374() print(374) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_375() {
    let (m, d) = axolc_core::parse("fn f375() print(375) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_376() {
    let (m, d) = axolc_core::parse("fn f376() print(376) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_377() {
    let (m, d) = axolc_core::parse("fn f377() print(377) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_378() {
    let (m, d) = axolc_core::parse("fn f378() print(378) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_379() {
    let (m, d) = axolc_core::parse("fn f379() print(379) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_380() {
    let (m, d) = axolc_core::parse("fn f380() print(380) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_381() {
    let (m, d) = axolc_core::parse("fn f381() print(381) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_382() {
    let (m, d) = axolc_core::parse("fn f382() print(382) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_383() {
    let (m, d) = axolc_core::parse("fn f383() print(383) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_384() {
    let (m, d) = axolc_core::parse("fn f384() print(384) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_385() {
    let (m, d) = axolc_core::parse("fn f385() print(385) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_386() {
    let (m, d) = axolc_core::parse("fn f386() print(386) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_387() {
    let (m, d) = axolc_core::parse("fn f387() print(387) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_388() {
    let (m, d) = axolc_core::parse("fn f388() print(388) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_389() {
    let (m, d) = axolc_core::parse("fn f389() print(389) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_390() {
    let (m, d) = axolc_core::parse("fn f390() print(390) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_391() {
    let (m, d) = axolc_core::parse("fn f391() print(391) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_392() {
    let (m, d) = axolc_core::parse("fn f392() print(392) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_393() {
    let (m, d) = axolc_core::parse("fn f393() print(393) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_394() {
    let (m, d) = axolc_core::parse("fn f394() print(394) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_395() {
    let (m, d) = axolc_core::parse("fn f395() print(395) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_396() {
    let (m, d) = axolc_core::parse("fn f396() print(396) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_397() {
    let (m, d) = axolc_core::parse("fn f397() print(397) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_398() {
    let (m, d) = axolc_core::parse("fn f398() print(398) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_399() {
    let (m, d) = axolc_core::parse("fn f399() print(399) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_400() {
    let (m, d) = axolc_core::parse("fn f400() print(400) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_401() {
    let (m, d) = axolc_core::parse("fn f401() print(401) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_402() {
    let (m, d) = axolc_core::parse("fn f402() print(402) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_403() {
    let (m, d) = axolc_core::parse("fn f403() print(403) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_404() {
    let (m, d) = axolc_core::parse("fn f404() print(404) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_405() {
    let (m, d) = axolc_core::parse("fn f405() print(405) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_406() {
    let (m, d) = axolc_core::parse("fn f406() print(406) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_407() {
    let (m, d) = axolc_core::parse("fn f407() print(407) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_408() {
    let (m, d) = axolc_core::parse("fn f408() print(408) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_409() {
    let (m, d) = axolc_core::parse("fn f409() print(409) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_410() {
    let (m, d) = axolc_core::parse("fn f410() print(410) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_411() {
    let (m, d) = axolc_core::parse("fn f411() print(411) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_412() {
    let (m, d) = axolc_core::parse("fn f412() print(412) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_413() {
    let (m, d) = axolc_core::parse("fn f413() print(413) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_414() {
    let (m, d) = axolc_core::parse("fn f414() print(414) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_415() {
    let (m, d) = axolc_core::parse("fn f415() print(415) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_416() {
    let (m, d) = axolc_core::parse("fn f416() print(416) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_417() {
    let (m, d) = axolc_core::parse("fn f417() print(417) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_418() {
    let (m, d) = axolc_core::parse("fn f418() print(418) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_419() {
    let (m, d) = axolc_core::parse("fn f419() print(419) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_420() {
    let (m, d) = axolc_core::parse("fn f420() print(420) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_421() {
    let (m, d) = axolc_core::parse("fn f421() print(421) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_422() {
    let (m, d) = axolc_core::parse("fn f422() print(422) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_423() {
    let (m, d) = axolc_core::parse("fn f423() print(423) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_424() {
    let (m, d) = axolc_core::parse("fn f424() print(424) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_425() {
    let (m, d) = axolc_core::parse("fn f425() print(425) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_426() {
    let (m, d) = axolc_core::parse("fn f426() print(426) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_427() {
    let (m, d) = axolc_core::parse("fn f427() print(427) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_428() {
    let (m, d) = axolc_core::parse("fn f428() print(428) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_429() {
    let (m, d) = axolc_core::parse("fn f429() print(429) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_430() {
    let (m, d) = axolc_core::parse("fn f430() print(430) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_431() {
    let (m, d) = axolc_core::parse("fn f431() print(431) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_432() {
    let (m, d) = axolc_core::parse("fn f432() print(432) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_433() {
    let (m, d) = axolc_core::parse("fn f433() print(433) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_434() {
    let (m, d) = axolc_core::parse("fn f434() print(434) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_435() {
    let (m, d) = axolc_core::parse("fn f435() print(435) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_436() {
    let (m, d) = axolc_core::parse("fn f436() print(436) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_437() {
    let (m, d) = axolc_core::parse("fn f437() print(437) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_438() {
    let (m, d) = axolc_core::parse("fn f438() print(438) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_439() {
    let (m, d) = axolc_core::parse("fn f439() print(439) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_440() {
    let (m, d) = axolc_core::parse("fn f440() print(440) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_441() {
    let (m, d) = axolc_core::parse("fn f441() print(441) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_442() {
    let (m, d) = axolc_core::parse("fn f442() print(442) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_443() {
    let (m, d) = axolc_core::parse("fn f443() print(443) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_444() {
    let (m, d) = axolc_core::parse("fn f444() print(444) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_445() {
    let (m, d) = axolc_core::parse("fn f445() print(445) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_446() {
    let (m, d) = axolc_core::parse("fn f446() print(446) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_447() {
    let (m, d) = axolc_core::parse("fn f447() print(447) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_448() {
    let (m, d) = axolc_core::parse("fn f448() print(448) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_449() {
    let (m, d) = axolc_core::parse("fn f449() print(449) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_450() {
    let (m, d) = axolc_core::parse("fn f450() print(450) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_451() {
    let (m, d) = axolc_core::parse("fn f451() print(451) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_452() {
    let (m, d) = axolc_core::parse("fn f452() print(452) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_453() {
    let (m, d) = axolc_core::parse("fn f453() print(453) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_454() {
    let (m, d) = axolc_core::parse("fn f454() print(454) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_455() {
    let (m, d) = axolc_core::parse("fn f455() print(455) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_456() {
    let (m, d) = axolc_core::parse("fn f456() print(456) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_457() {
    let (m, d) = axolc_core::parse("fn f457() print(457) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_458() {
    let (m, d) = axolc_core::parse("fn f458() print(458) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_459() {
    let (m, d) = axolc_core::parse("fn f459() print(459) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_460() {
    let (m, d) = axolc_core::parse("fn f460() print(460) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_461() {
    let (m, d) = axolc_core::parse("fn f461() print(461) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_462() {
    let (m, d) = axolc_core::parse("fn f462() print(462) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_463() {
    let (m, d) = axolc_core::parse("fn f463() print(463) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_464() {
    let (m, d) = axolc_core::parse("fn f464() print(464) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_465() {
    let (m, d) = axolc_core::parse("fn f465() print(465) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_466() {
    let (m, d) = axolc_core::parse("fn f466() print(466) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_467() {
    let (m, d) = axolc_core::parse("fn f467() print(467) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_468() {
    let (m, d) = axolc_core::parse("fn f468() print(468) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_469() {
    let (m, d) = axolc_core::parse("fn f469() print(469) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_470() {
    let (m, d) = axolc_core::parse("fn f470() print(470) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_471() {
    let (m, d) = axolc_core::parse("fn f471() print(471) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_472() {
    let (m, d) = axolc_core::parse("fn f472() print(472) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_473() {
    let (m, d) = axolc_core::parse("fn f473() print(473) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_474() {
    let (m, d) = axolc_core::parse("fn f474() print(474) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_475() {
    let (m, d) = axolc_core::parse("fn f475() print(475) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_476() {
    let (m, d) = axolc_core::parse("fn f476() print(476) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_477() {
    let (m, d) = axolc_core::parse("fn f477() print(477) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_478() {
    let (m, d) = axolc_core::parse("fn f478() print(478) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_479() {
    let (m, d) = axolc_core::parse("fn f479() print(479) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_480() {
    let (m, d) = axolc_core::parse("fn f480() print(480) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_481() {
    let (m, d) = axolc_core::parse("fn f481() print(481) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_482() {
    let (m, d) = axolc_core::parse("fn f482() print(482) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_483() {
    let (m, d) = axolc_core::parse("fn f483() print(483) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_484() {
    let (m, d) = axolc_core::parse("fn f484() print(484) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_485() {
    let (m, d) = axolc_core::parse("fn f485() print(485) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_486() {
    let (m, d) = axolc_core::parse("fn f486() print(486) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_487() {
    let (m, d) = axolc_core::parse("fn f487() print(487) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_488() {
    let (m, d) = axolc_core::parse("fn f488() print(488) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_489() {
    let (m, d) = axolc_core::parse("fn f489() print(489) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_490() {
    let (m, d) = axolc_core::parse("fn f490() print(490) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_491() {
    let (m, d) = axolc_core::parse("fn f491() print(491) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_492() {
    let (m, d) = axolc_core::parse("fn f492() print(492) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_493() {
    let (m, d) = axolc_core::parse("fn f493() print(493) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_494() {
    let (m, d) = axolc_core::parse("fn f494() print(494) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_495() {
    let (m, d) = axolc_core::parse("fn f495() print(495) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_496() {
    let (m, d) = axolc_core::parse("fn f496() print(496) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_497() {
    let (m, d) = axolc_core::parse("fn f497() print(497) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_498() {
    let (m, d) = axolc_core::parse("fn f498() print(498) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_499() {
    let (m, d) = axolc_core::parse("fn f499() print(499) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_500() {
    let (m, d) = axolc_core::parse("fn f500() print(500) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_501() {
    let (m, d) = axolc_core::parse("fn f501() print(501) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_502() {
    let (m, d) = axolc_core::parse("fn f502() print(502) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_503() {
    let (m, d) = axolc_core::parse("fn f503() print(503) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_504() {
    let (m, d) = axolc_core::parse("fn f504() print(504) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_505() {
    let (m, d) = axolc_core::parse("fn f505() print(505) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_506() {
    let (m, d) = axolc_core::parse("fn f506() print(506) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_507() {
    let (m, d) = axolc_core::parse("fn f507() print(507) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_508() {
    let (m, d) = axolc_core::parse("fn f508() print(508) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_509() {
    let (m, d) = axolc_core::parse("fn f509() print(509) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_510() {
    let (m, d) = axolc_core::parse("fn f510() print(510) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_511() {
    let (m, d) = axolc_core::parse("fn f511() print(511) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_512() {
    let (m, d) = axolc_core::parse("fn f512() print(512) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_513() {
    let (m, d) = axolc_core::parse("fn f513() print(513) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_514() {
    let (m, d) = axolc_core::parse("fn f514() print(514) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_515() {
    let (m, d) = axolc_core::parse("fn f515() print(515) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_516() {
    let (m, d) = axolc_core::parse("fn f516() print(516) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_517() {
    let (m, d) = axolc_core::parse("fn f517() print(517) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_518() {
    let (m, d) = axolc_core::parse("fn f518() print(518) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_519() {
    let (m, d) = axolc_core::parse("fn f519() print(519) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_520() {
    let (m, d) = axolc_core::parse("fn f520() print(520) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_521() {
    let (m, d) = axolc_core::parse("fn f521() print(521) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_522() {
    let (m, d) = axolc_core::parse("fn f522() print(522) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_523() {
    let (m, d) = axolc_core::parse("fn f523() print(523) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_524() {
    let (m, d) = axolc_core::parse("fn f524() print(524) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_525() {
    let (m, d) = axolc_core::parse("fn f525() print(525) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_526() {
    let (m, d) = axolc_core::parse("fn f526() print(526) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_527() {
    let (m, d) = axolc_core::parse("fn f527() print(527) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_528() {
    let (m, d) = axolc_core::parse("fn f528() print(528) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_529() {
    let (m, d) = axolc_core::parse("fn f529() print(529) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_530() {
    let (m, d) = axolc_core::parse("fn f530() print(530) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_531() {
    let (m, d) = axolc_core::parse("fn f531() print(531) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_532() {
    let (m, d) = axolc_core::parse("fn f532() print(532) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_533() {
    let (m, d) = axolc_core::parse("fn f533() print(533) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_534() {
    let (m, d) = axolc_core::parse("fn f534() print(534) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_535() {
    let (m, d) = axolc_core::parse("fn f535() print(535) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_536() {
    let (m, d) = axolc_core::parse("fn f536() print(536) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_537() {
    let (m, d) = axolc_core::parse("fn f537() print(537) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_538() {
    let (m, d) = axolc_core::parse("fn f538() print(538) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_539() {
    let (m, d) = axolc_core::parse("fn f539() print(539) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_540() {
    let (m, d) = axolc_core::parse("fn f540() print(540) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_541() {
    let (m, d) = axolc_core::parse("fn f541() print(541) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_542() {
    let (m, d) = axolc_core::parse("fn f542() print(542) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_543() {
    let (m, d) = axolc_core::parse("fn f543() print(543) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_544() {
    let (m, d) = axolc_core::parse("fn f544() print(544) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_545() {
    let (m, d) = axolc_core::parse("fn f545() print(545) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_546() {
    let (m, d) = axolc_core::parse("fn f546() print(546) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_547() {
    let (m, d) = axolc_core::parse("fn f547() print(547) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_548() {
    let (m, d) = axolc_core::parse("fn f548() print(548) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_549() {
    let (m, d) = axolc_core::parse("fn f549() print(549) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_550() {
    let (m, d) = axolc_core::parse("fn f550() print(550) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_551() {
    let (m, d) = axolc_core::parse("fn f551() print(551) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_552() {
    let (m, d) = axolc_core::parse("fn f552() print(552) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_553() {
    let (m, d) = axolc_core::parse("fn f553() print(553) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_554() {
    let (m, d) = axolc_core::parse("fn f554() print(554) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_555() {
    let (m, d) = axolc_core::parse("fn f555() print(555) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_556() {
    let (m, d) = axolc_core::parse("fn f556() print(556) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_557() {
    let (m, d) = axolc_core::parse("fn f557() print(557) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_558() {
    let (m, d) = axolc_core::parse("fn f558() print(558) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_559() {
    let (m, d) = axolc_core::parse("fn f559() print(559) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_560() {
    let (m, d) = axolc_core::parse("fn f560() print(560) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_561() {
    let (m, d) = axolc_core::parse("fn f561() print(561) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_562() {
    let (m, d) = axolc_core::parse("fn f562() print(562) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_563() {
    let (m, d) = axolc_core::parse("fn f563() print(563) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_564() {
    let (m, d) = axolc_core::parse("fn f564() print(564) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_565() {
    let (m, d) = axolc_core::parse("fn f565() print(565) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_566() {
    let (m, d) = axolc_core::parse("fn f566() print(566) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_567() {
    let (m, d) = axolc_core::parse("fn f567() print(567) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_568() {
    let (m, d) = axolc_core::parse("fn f568() print(568) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_569() {
    let (m, d) = axolc_core::parse("fn f569() print(569) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_570() {
    let (m, d) = axolc_core::parse("fn f570() print(570) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_571() {
    let (m, d) = axolc_core::parse("fn f571() print(571) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_572() {
    let (m, d) = axolc_core::parse("fn f572() print(572) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_573() {
    let (m, d) = axolc_core::parse("fn f573() print(573) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_574() {
    let (m, d) = axolc_core::parse("fn f574() print(574) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_575() {
    let (m, d) = axolc_core::parse("fn f575() print(575) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_576() {
    let (m, d) = axolc_core::parse("fn f576() print(576) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_577() {
    let (m, d) = axolc_core::parse("fn f577() print(577) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_578() {
    let (m, d) = axolc_core::parse("fn f578() print(578) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_579() {
    let (m, d) = axolc_core::parse("fn f579() print(579) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_580() {
    let (m, d) = axolc_core::parse("fn f580() print(580) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_581() {
    let (m, d) = axolc_core::parse("fn f581() print(581) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_582() {
    let (m, d) = axolc_core::parse("fn f582() print(582) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_583() {
    let (m, d) = axolc_core::parse("fn f583() print(583) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_584() {
    let (m, d) = axolc_core::parse("fn f584() print(584) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_585() {
    let (m, d) = axolc_core::parse("fn f585() print(585) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_586() {
    let (m, d) = axolc_core::parse("fn f586() print(586) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_587() {
    let (m, d) = axolc_core::parse("fn f587() print(587) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_588() {
    let (m, d) = axolc_core::parse("fn f588() print(588) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_589() {
    let (m, d) = axolc_core::parse("fn f589() print(589) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_590() {
    let (m, d) = axolc_core::parse("fn f590() print(590) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_591() {
    let (m, d) = axolc_core::parse("fn f591() print(591) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_592() {
    let (m, d) = axolc_core::parse("fn f592() print(592) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_593() {
    let (m, d) = axolc_core::parse("fn f593() print(593) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_594() {
    let (m, d) = axolc_core::parse("fn f594() print(594) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_595() {
    let (m, d) = axolc_core::parse("fn f595() print(595) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_596() {
    let (m, d) = axolc_core::parse("fn f596() print(596) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_597() {
    let (m, d) = axolc_core::parse("fn f597() print(597) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_598() {
    let (m, d) = axolc_core::parse("fn f598() print(598) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_599() {
    let (m, d) = axolc_core::parse("fn f599() print(599) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_600() {
    let (m, d) = axolc_core::parse("fn f600() print(600) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_601() {
    let (m, d) = axolc_core::parse("fn f601() print(601) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_602() {
    let (m, d) = axolc_core::parse("fn f602() print(602) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_603() {
    let (m, d) = axolc_core::parse("fn f603() print(603) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_604() {
    let (m, d) = axolc_core::parse("fn f604() print(604) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_605() {
    let (m, d) = axolc_core::parse("fn f605() print(605) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_606() {
    let (m, d) = axolc_core::parse("fn f606() print(606) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_607() {
    let (m, d) = axolc_core::parse("fn f607() print(607) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_608() {
    let (m, d) = axolc_core::parse("fn f608() print(608) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_609() {
    let (m, d) = axolc_core::parse("fn f609() print(609) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_610() {
    let (m, d) = axolc_core::parse("fn f610() print(610) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_611() {
    let (m, d) = axolc_core::parse("fn f611() print(611) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_612() {
    let (m, d) = axolc_core::parse("fn f612() print(612) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_613() {
    let (m, d) = axolc_core::parse("fn f613() print(613) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_614() {
    let (m, d) = axolc_core::parse("fn f614() print(614) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_615() {
    let (m, d) = axolc_core::parse("fn f615() print(615) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_616() {
    let (m, d) = axolc_core::parse("fn f616() print(616) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_617() {
    let (m, d) = axolc_core::parse("fn f617() print(617) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_618() {
    let (m, d) = axolc_core::parse("fn f618() print(618) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_619() {
    let (m, d) = axolc_core::parse("fn f619() print(619) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_620() {
    let (m, d) = axolc_core::parse("fn f620() print(620) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_621() {
    let (m, d) = axolc_core::parse("fn f621() print(621) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_622() {
    let (m, d) = axolc_core::parse("fn f622() print(622) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_623() {
    let (m, d) = axolc_core::parse("fn f623() print(623) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_624() {
    let (m, d) = axolc_core::parse("fn f624() print(624) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_625() {
    let (m, d) = axolc_core::parse("fn f625() print(625) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_626() {
    let (m, d) = axolc_core::parse("fn f626() print(626) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_627() {
    let (m, d) = axolc_core::parse("fn f627() print(627) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_628() {
    let (m, d) = axolc_core::parse("fn f628() print(628) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_629() {
    let (m, d) = axolc_core::parse("fn f629() print(629) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_630() {
    let (m, d) = axolc_core::parse("fn f630() print(630) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_631() {
    let (m, d) = axolc_core::parse("fn f631() print(631) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_632() {
    let (m, d) = axolc_core::parse("fn f632() print(632) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_633() {
    let (m, d) = axolc_core::parse("fn f633() print(633) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_634() {
    let (m, d) = axolc_core::parse("fn f634() print(634) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_635() {
    let (m, d) = axolc_core::parse("fn f635() print(635) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_636() {
    let (m, d) = axolc_core::parse("fn f636() print(636) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_637() {
    let (m, d) = axolc_core::parse("fn f637() print(637) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_638() {
    let (m, d) = axolc_core::parse("fn f638() print(638) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_639() {
    let (m, d) = axolc_core::parse("fn f639() print(639) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_640() {
    let (m, d) = axolc_core::parse("fn f640() print(640) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_641() {
    let (m, d) = axolc_core::parse("fn f641() print(641) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_642() {
    let (m, d) = axolc_core::parse("fn f642() print(642) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_643() {
    let (m, d) = axolc_core::parse("fn f643() print(643) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_644() {
    let (m, d) = axolc_core::parse("fn f644() print(644) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_645() {
    let (m, d) = axolc_core::parse("fn f645() print(645) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_646() {
    let (m, d) = axolc_core::parse("fn f646() print(646) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_647() {
    let (m, d) = axolc_core::parse("fn f647() print(647) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_648() {
    let (m, d) = axolc_core::parse("fn f648() print(648) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_649() {
    let (m, d) = axolc_core::parse("fn f649() print(649) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_650() {
    let (m, d) = axolc_core::parse("fn f650() print(650) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_651() {
    let (m, d) = axolc_core::parse("fn f651() print(651) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_652() {
    let (m, d) = axolc_core::parse("fn f652() print(652) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_653() {
    let (m, d) = axolc_core::parse("fn f653() print(653) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_654() {
    let (m, d) = axolc_core::parse("fn f654() print(654) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_655() {
    let (m, d) = axolc_core::parse("fn f655() print(655) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_656() {
    let (m, d) = axolc_core::parse("fn f656() print(656) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_657() {
    let (m, d) = axolc_core::parse("fn f657() print(657) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_658() {
    let (m, d) = axolc_core::parse("fn f658() print(658) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_659() {
    let (m, d) = axolc_core::parse("fn f659() print(659) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_660() {
    let (m, d) = axolc_core::parse("fn f660() print(660) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_661() {
    let (m, d) = axolc_core::parse("fn f661() print(661) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_662() {
    let (m, d) = axolc_core::parse("fn f662() print(662) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_663() {
    let (m, d) = axolc_core::parse("fn f663() print(663) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_664() {
    let (m, d) = axolc_core::parse("fn f664() print(664) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_665() {
    let (m, d) = axolc_core::parse("fn f665() print(665) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_666() {
    let (m, d) = axolc_core::parse("fn f666() print(666) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_667() {
    let (m, d) = axolc_core::parse("fn f667() print(667) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_668() {
    let (m, d) = axolc_core::parse("fn f668() print(668) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_669() {
    let (m, d) = axolc_core::parse("fn f669() print(669) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_670() {
    let (m, d) = axolc_core::parse("fn f670() print(670) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_671() {
    let (m, d) = axolc_core::parse("fn f671() print(671) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_672() {
    let (m, d) = axolc_core::parse("fn f672() print(672) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_673() {
    let (m, d) = axolc_core::parse("fn f673() print(673) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_674() {
    let (m, d) = axolc_core::parse("fn f674() print(674) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_675() {
    let (m, d) = axolc_core::parse("fn f675() print(675) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_676() {
    let (m, d) = axolc_core::parse("fn f676() print(676) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_677() {
    let (m, d) = axolc_core::parse("fn f677() print(677) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_678() {
    let (m, d) = axolc_core::parse("fn f678() print(678) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_679() {
    let (m, d) = axolc_core::parse("fn f679() print(679) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_680() {
    let (m, d) = axolc_core::parse("fn f680() print(680) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_681() {
    let (m, d) = axolc_core::parse("fn f681() print(681) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_682() {
    let (m, d) = axolc_core::parse("fn f682() print(682) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_683() {
    let (m, d) = axolc_core::parse("fn f683() print(683) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_684() {
    let (m, d) = axolc_core::parse("fn f684() print(684) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_685() {
    let (m, d) = axolc_core::parse("fn f685() print(685) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_686() {
    let (m, d) = axolc_core::parse("fn f686() print(686) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_687() {
    let (m, d) = axolc_core::parse("fn f687() print(687) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_688() {
    let (m, d) = axolc_core::parse("fn f688() print(688) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_689() {
    let (m, d) = axolc_core::parse("fn f689() print(689) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_690() {
    let (m, d) = axolc_core::parse("fn f690() print(690) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_691() {
    let (m, d) = axolc_core::parse("fn f691() print(691) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_692() {
    let (m, d) = axolc_core::parse("fn f692() print(692) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_693() {
    let (m, d) = axolc_core::parse("fn f693() print(693) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_694() {
    let (m, d) = axolc_core::parse("fn f694() print(694) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_695() {
    let (m, d) = axolc_core::parse("fn f695() print(695) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_696() {
    let (m, d) = axolc_core::parse("fn f696() print(696) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_697() {
    let (m, d) = axolc_core::parse("fn f697() print(697) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_698() {
    let (m, d) = axolc_core::parse("fn f698() print(698) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_699() {
    let (m, d) = axolc_core::parse("fn f699() print(699) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_700() {
    let (m, d) = axolc_core::parse("fn f700() print(700) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_701() {
    let (m, d) = axolc_core::parse("fn f701() print(701) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_702() {
    let (m, d) = axolc_core::parse("fn f702() print(702) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_703() {
    let (m, d) = axolc_core::parse("fn f703() print(703) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_704() {
    let (m, d) = axolc_core::parse("fn f704() print(704) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_705() {
    let (m, d) = axolc_core::parse("fn f705() print(705) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_706() {
    let (m, d) = axolc_core::parse("fn f706() print(706) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_707() {
    let (m, d) = axolc_core::parse("fn f707() print(707) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_708() {
    let (m, d) = axolc_core::parse("fn f708() print(708) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_709() {
    let (m, d) = axolc_core::parse("fn f709() print(709) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_710() {
    let (m, d) = axolc_core::parse("fn f710() print(710) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_711() {
    let (m, d) = axolc_core::parse("fn f711() print(711) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_712() {
    let (m, d) = axolc_core::parse("fn f712() print(712) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_713() {
    let (m, d) = axolc_core::parse("fn f713() print(713) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_714() {
    let (m, d) = axolc_core::parse("fn f714() print(714) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_715() {
    let (m, d) = axolc_core::parse("fn f715() print(715) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_716() {
    let (m, d) = axolc_core::parse("fn f716() print(716) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_717() {
    let (m, d) = axolc_core::parse("fn f717() print(717) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_718() {
    let (m, d) = axolc_core::parse("fn f718() print(718) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_719() {
    let (m, d) = axolc_core::parse("fn f719() print(719) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_720() {
    let (m, d) = axolc_core::parse("fn f720() print(720) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_721() {
    let (m, d) = axolc_core::parse("fn f721() print(721) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_722() {
    let (m, d) = axolc_core::parse("fn f722() print(722) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_723() {
    let (m, d) = axolc_core::parse("fn f723() print(723) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_724() {
    let (m, d) = axolc_core::parse("fn f724() print(724) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_725() {
    let (m, d) = axolc_core::parse("fn f725() print(725) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_726() {
    let (m, d) = axolc_core::parse("fn f726() print(726) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_727() {
    let (m, d) = axolc_core::parse("fn f727() print(727) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_728() {
    let (m, d) = axolc_core::parse("fn f728() print(728) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_729() {
    let (m, d) = axolc_core::parse("fn f729() print(729) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_730() {
    let (m, d) = axolc_core::parse("fn f730() print(730) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_731() {
    let (m, d) = axolc_core::parse("fn f731() print(731) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_732() {
    let (m, d) = axolc_core::parse("fn f732() print(732) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_733() {
    let (m, d) = axolc_core::parse("fn f733() print(733) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_734() {
    let (m, d) = axolc_core::parse("fn f734() print(734) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_735() {
    let (m, d) = axolc_core::parse("fn f735() print(735) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_736() {
    let (m, d) = axolc_core::parse("fn f736() print(736) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_737() {
    let (m, d) = axolc_core::parse("fn f737() print(737) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_738() {
    let (m, d) = axolc_core::parse("fn f738() print(738) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_739() {
    let (m, d) = axolc_core::parse("fn f739() print(739) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_740() {
    let (m, d) = axolc_core::parse("fn f740() print(740) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_741() {
    let (m, d) = axolc_core::parse("fn f741() print(741) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_742() {
    let (m, d) = axolc_core::parse("fn f742() print(742) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_743() {
    let (m, d) = axolc_core::parse("fn f743() print(743) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_744() {
    let (m, d) = axolc_core::parse("fn f744() print(744) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_745() {
    let (m, d) = axolc_core::parse("fn f745() print(745) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_746() {
    let (m, d) = axolc_core::parse("fn f746() print(746) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_747() {
    let (m, d) = axolc_core::parse("fn f747() print(747) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_748() {
    let (m, d) = axolc_core::parse("fn f748() print(748) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_749() {
    let (m, d) = axolc_core::parse("fn f749() print(749) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_750() {
    let (m, d) = axolc_core::parse("fn f750() print(750) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_751() {
    let (m, d) = axolc_core::parse("fn f751() print(751) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_752() {
    let (m, d) = axolc_core::parse("fn f752() print(752) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_753() {
    let (m, d) = axolc_core::parse("fn f753() print(753) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_754() {
    let (m, d) = axolc_core::parse("fn f754() print(754) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_755() {
    let (m, d) = axolc_core::parse("fn f755() print(755) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_756() {
    let (m, d) = axolc_core::parse("fn f756() print(756) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_757() {
    let (m, d) = axolc_core::parse("fn f757() print(757) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_758() {
    let (m, d) = axolc_core::parse("fn f758() print(758) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_759() {
    let (m, d) = axolc_core::parse("fn f759() print(759) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_760() {
    let (m, d) = axolc_core::parse("fn f760() print(760) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_761() {
    let (m, d) = axolc_core::parse("fn f761() print(761) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_762() {
    let (m, d) = axolc_core::parse("fn f762() print(762) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_763() {
    let (m, d) = axolc_core::parse("fn f763() print(763) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_764() {
    let (m, d) = axolc_core::parse("fn f764() print(764) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_765() {
    let (m, d) = axolc_core::parse("fn f765() print(765) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_766() {
    let (m, d) = axolc_core::parse("fn f766() print(766) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_767() {
    let (m, d) = axolc_core::parse("fn f767() print(767) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_768() {
    let (m, d) = axolc_core::parse("fn f768() print(768) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_769() {
    let (m, d) = axolc_core::parse("fn f769() print(769) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_770() {
    let (m, d) = axolc_core::parse("fn f770() print(770) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_771() {
    let (m, d) = axolc_core::parse("fn f771() print(771) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_772() {
    let (m, d) = axolc_core::parse("fn f772() print(772) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_773() {
    let (m, d) = axolc_core::parse("fn f773() print(773) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_774() {
    let (m, d) = axolc_core::parse("fn f774() print(774) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_775() {
    let (m, d) = axolc_core::parse("fn f775() print(775) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_776() {
    let (m, d) = axolc_core::parse("fn f776() print(776) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_777() {
    let (m, d) = axolc_core::parse("fn f777() print(777) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_778() {
    let (m, d) = axolc_core::parse("fn f778() print(778) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_779() {
    let (m, d) = axolc_core::parse("fn f779() print(779) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_780() {
    let (m, d) = axolc_core::parse("fn f780() print(780) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_781() {
    let (m, d) = axolc_core::parse("fn f781() print(781) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_782() {
    let (m, d) = axolc_core::parse("fn f782() print(782) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_783() {
    let (m, d) = axolc_core::parse("fn f783() print(783) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_784() {
    let (m, d) = axolc_core::parse("fn f784() print(784) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_785() {
    let (m, d) = axolc_core::parse("fn f785() print(785) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_786() {
    let (m, d) = axolc_core::parse("fn f786() print(786) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_787() {
    let (m, d) = axolc_core::parse("fn f787() print(787) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_788() {
    let (m, d) = axolc_core::parse("fn f788() print(788) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_789() {
    let (m, d) = axolc_core::parse("fn f789() print(789) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_790() {
    let (m, d) = axolc_core::parse("fn f790() print(790) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_791() {
    let (m, d) = axolc_core::parse("fn f791() print(791) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_792() {
    let (m, d) = axolc_core::parse("fn f792() print(792) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_793() {
    let (m, d) = axolc_core::parse("fn f793() print(793) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_794() {
    let (m, d) = axolc_core::parse("fn f794() print(794) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_795() {
    let (m, d) = axolc_core::parse("fn f795() print(795) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_796() {
    let (m, d) = axolc_core::parse("fn f796() print(796) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_797() {
    let (m, d) = axolc_core::parse("fn f797() print(797) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_798() {
    let (m, d) = axolc_core::parse("fn f798() print(798) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_799() {
    let (m, d) = axolc_core::parse("fn f799() print(799) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_800() {
    let (m, d) = axolc_core::parse("fn f800() print(800) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_801() {
    let (m, d) = axolc_core::parse("fn f801() print(801) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_802() {
    let (m, d) = axolc_core::parse("fn f802() print(802) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_803() {
    let (m, d) = axolc_core::parse("fn f803() print(803) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_804() {
    let (m, d) = axolc_core::parse("fn f804() print(804) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_805() {
    let (m, d) = axolc_core::parse("fn f805() print(805) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_806() {
    let (m, d) = axolc_core::parse("fn f806() print(806) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_807() {
    let (m, d) = axolc_core::parse("fn f807() print(807) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_808() {
    let (m, d) = axolc_core::parse("fn f808() print(808) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_809() {
    let (m, d) = axolc_core::parse("fn f809() print(809) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_810() {
    let (m, d) = axolc_core::parse("fn f810() print(810) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_811() {
    let (m, d) = axolc_core::parse("fn f811() print(811) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_812() {
    let (m, d) = axolc_core::parse("fn f812() print(812) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_813() {
    let (m, d) = axolc_core::parse("fn f813() print(813) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_814() {
    let (m, d) = axolc_core::parse("fn f814() print(814) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_815() {
    let (m, d) = axolc_core::parse("fn f815() print(815) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_816() {
    let (m, d) = axolc_core::parse("fn f816() print(816) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_817() {
    let (m, d) = axolc_core::parse("fn f817() print(817) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_818() {
    let (m, d) = axolc_core::parse("fn f818() print(818) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_819() {
    let (m, d) = axolc_core::parse("fn f819() print(819) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_820() {
    let (m, d) = axolc_core::parse("fn f820() print(820) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_821() {
    let (m, d) = axolc_core::parse("fn f821() print(821) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_822() {
    let (m, d) = axolc_core::parse("fn f822() print(822) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_823() {
    let (m, d) = axolc_core::parse("fn f823() print(823) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_824() {
    let (m, d) = axolc_core::parse("fn f824() print(824) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_825() {
    let (m, d) = axolc_core::parse("fn f825() print(825) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_826() {
    let (m, d) = axolc_core::parse("fn f826() print(826) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_827() {
    let (m, d) = axolc_core::parse("fn f827() print(827) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_828() {
    let (m, d) = axolc_core::parse("fn f828() print(828) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_829() {
    let (m, d) = axolc_core::parse("fn f829() print(829) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_830() {
    let (m, d) = axolc_core::parse("fn f830() print(830) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_831() {
    let (m, d) = axolc_core::parse("fn f831() print(831) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_832() {
    let (m, d) = axolc_core::parse("fn f832() print(832) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_833() {
    let (m, d) = axolc_core::parse("fn f833() print(833) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_834() {
    let (m, d) = axolc_core::parse("fn f834() print(834) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_835() {
    let (m, d) = axolc_core::parse("fn f835() print(835) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_836() {
    let (m, d) = axolc_core::parse("fn f836() print(836) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_837() {
    let (m, d) = axolc_core::parse("fn f837() print(837) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_838() {
    let (m, d) = axolc_core::parse("fn f838() print(838) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_839() {
    let (m, d) = axolc_core::parse("fn f839() print(839) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_840() {
    let (m, d) = axolc_core::parse("fn f840() print(840) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_841() {
    let (m, d) = axolc_core::parse("fn f841() print(841) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_842() {
    let (m, d) = axolc_core::parse("fn f842() print(842) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_843() {
    let (m, d) = axolc_core::parse("fn f843() print(843) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_844() {
    let (m, d) = axolc_core::parse("fn f844() print(844) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_845() {
    let (m, d) = axolc_core::parse("fn f845() print(845) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_846() {
    let (m, d) = axolc_core::parse("fn f846() print(846) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_847() {
    let (m, d) = axolc_core::parse("fn f847() print(847) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_848() {
    let (m, d) = axolc_core::parse("fn f848() print(848) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_849() {
    let (m, d) = axolc_core::parse("fn f849() print(849) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_850() {
    let (m, d) = axolc_core::parse("fn f850() print(850) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_851() {
    let (m, d) = axolc_core::parse("fn f851() print(851) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_852() {
    let (m, d) = axolc_core::parse("fn f852() print(852) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_853() {
    let (m, d) = axolc_core::parse("fn f853() print(853) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_854() {
    let (m, d) = axolc_core::parse("fn f854() print(854) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_855() {
    let (m, d) = axolc_core::parse("fn f855() print(855) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_856() {
    let (m, d) = axolc_core::parse("fn f856() print(856) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_857() {
    let (m, d) = axolc_core::parse("fn f857() print(857) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_858() {
    let (m, d) = axolc_core::parse("fn f858() print(858) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_859() {
    let (m, d) = axolc_core::parse("fn f859() print(859) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_860() {
    let (m, d) = axolc_core::parse("fn f860() print(860) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_861() {
    let (m, d) = axolc_core::parse("fn f861() print(861) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_862() {
    let (m, d) = axolc_core::parse("fn f862() print(862) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_863() {
    let (m, d) = axolc_core::parse("fn f863() print(863) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_864() {
    let (m, d) = axolc_core::parse("fn f864() print(864) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_865() {
    let (m, d) = axolc_core::parse("fn f865() print(865) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_866() {
    let (m, d) = axolc_core::parse("fn f866() print(866) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_867() {
    let (m, d) = axolc_core::parse("fn f867() print(867) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_868() {
    let (m, d) = axolc_core::parse("fn f868() print(868) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_869() {
    let (m, d) = axolc_core::parse("fn f869() print(869) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_870() {
    let (m, d) = axolc_core::parse("fn f870() print(870) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_871() {
    let (m, d) = axolc_core::parse("fn f871() print(871) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_872() {
    let (m, d) = axolc_core::parse("fn f872() print(872) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_873() {
    let (m, d) = axolc_core::parse("fn f873() print(873) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_874() {
    let (m, d) = axolc_core::parse("fn f874() print(874) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_875() {
    let (m, d) = axolc_core::parse("fn f875() print(875) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_876() {
    let (m, d) = axolc_core::parse("fn f876() print(876) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_877() {
    let (m, d) = axolc_core::parse("fn f877() print(877) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_878() {
    let (m, d) = axolc_core::parse("fn f878() print(878) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_879() {
    let (m, d) = axolc_core::parse("fn f879() print(879) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_880() {
    let (m, d) = axolc_core::parse("fn f880() print(880) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_881() {
    let (m, d) = axolc_core::parse("fn f881() print(881) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_882() {
    let (m, d) = axolc_core::parse("fn f882() print(882) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_883() {
    let (m, d) = axolc_core::parse("fn f883() print(883) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_884() {
    let (m, d) = axolc_core::parse("fn f884() print(884) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_885() {
    let (m, d) = axolc_core::parse("fn f885() print(885) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_886() {
    let (m, d) = axolc_core::parse("fn f886() print(886) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_887() {
    let (m, d) = axolc_core::parse("fn f887() print(887) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_888() {
    let (m, d) = axolc_core::parse("fn f888() print(888) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_889() {
    let (m, d) = axolc_core::parse("fn f889() print(889) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_890() {
    let (m, d) = axolc_core::parse("fn f890() print(890) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_891() {
    let (m, d) = axolc_core::parse("fn f891() print(891) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_892() {
    let (m, d) = axolc_core::parse("fn f892() print(892) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_893() {
    let (m, d) = axolc_core::parse("fn f893() print(893) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_894() {
    let (m, d) = axolc_core::parse("fn f894() print(894) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_895() {
    let (m, d) = axolc_core::parse("fn f895() print(895) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_896() {
    let (m, d) = axolc_core::parse("fn f896() print(896) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_897() {
    let (m, d) = axolc_core::parse("fn f897() print(897) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_898() {
    let (m, d) = axolc_core::parse("fn f898() print(898) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_899() {
    let (m, d) = axolc_core::parse("fn f899() print(899) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_900() {
    let (m, d) = axolc_core::parse("fn f900() print(900) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_901() {
    let (m, d) = axolc_core::parse("fn f901() print(901) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_902() {
    let (m, d) = axolc_core::parse("fn f902() print(902) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_903() {
    let (m, d) = axolc_core::parse("fn f903() print(903) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_904() {
    let (m, d) = axolc_core::parse("fn f904() print(904) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_905() {
    let (m, d) = axolc_core::parse("fn f905() print(905) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_906() {
    let (m, d) = axolc_core::parse("fn f906() print(906) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_907() {
    let (m, d) = axolc_core::parse("fn f907() print(907) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_908() {
    let (m, d) = axolc_core::parse("fn f908() print(908) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_909() {
    let (m, d) = axolc_core::parse("fn f909() print(909) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_910() {
    let (m, d) = axolc_core::parse("fn f910() print(910) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_911() {
    let (m, d) = axolc_core::parse("fn f911() print(911) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_912() {
    let (m, d) = axolc_core::parse("fn f912() print(912) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_913() {
    let (m, d) = axolc_core::parse("fn f913() print(913) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_914() {
    let (m, d) = axolc_core::parse("fn f914() print(914) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_915() {
    let (m, d) = axolc_core::parse("fn f915() print(915) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_916() {
    let (m, d) = axolc_core::parse("fn f916() print(916) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_917() {
    let (m, d) = axolc_core::parse("fn f917() print(917) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_918() {
    let (m, d) = axolc_core::parse("fn f918() print(918) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_919() {
    let (m, d) = axolc_core::parse("fn f919() print(919) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_920() {
    let (m, d) = axolc_core::parse("fn f920() print(920) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_921() {
    let (m, d) = axolc_core::parse("fn f921() print(921) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_922() {
    let (m, d) = axolc_core::parse("fn f922() print(922) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_923() {
    let (m, d) = axolc_core::parse("fn f923() print(923) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_924() {
    let (m, d) = axolc_core::parse("fn f924() print(924) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_925() {
    let (m, d) = axolc_core::parse("fn f925() print(925) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_926() {
    let (m, d) = axolc_core::parse("fn f926() print(926) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_927() {
    let (m, d) = axolc_core::parse("fn f927() print(927) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_928() {
    let (m, d) = axolc_core::parse("fn f928() print(928) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_929() {
    let (m, d) = axolc_core::parse("fn f929() print(929) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_930() {
    let (m, d) = axolc_core::parse("fn f930() print(930) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_931() {
    let (m, d) = axolc_core::parse("fn f931() print(931) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_932() {
    let (m, d) = axolc_core::parse("fn f932() print(932) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_933() {
    let (m, d) = axolc_core::parse("fn f933() print(933) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_934() {
    let (m, d) = axolc_core::parse("fn f934() print(934) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_935() {
    let (m, d) = axolc_core::parse("fn f935() print(935) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_936() {
    let (m, d) = axolc_core::parse("fn f936() print(936) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_937() {
    let (m, d) = axolc_core::parse("fn f937() print(937) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_938() {
    let (m, d) = axolc_core::parse("fn f938() print(938) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_939() {
    let (m, d) = axolc_core::parse("fn f939() print(939) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_940() {
    let (m, d) = axolc_core::parse("fn f940() print(940) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_941() {
    let (m, d) = axolc_core::parse("fn f941() print(941) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_942() {
    let (m, d) = axolc_core::parse("fn f942() print(942) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_943() {
    let (m, d) = axolc_core::parse("fn f943() print(943) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_944() {
    let (m, d) = axolc_core::parse("fn f944() print(944) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_945() {
    let (m, d) = axolc_core::parse("fn f945() print(945) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_946() {
    let (m, d) = axolc_core::parse("fn f946() print(946) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_947() {
    let (m, d) = axolc_core::parse("fn f947() print(947) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_948() {
    let (m, d) = axolc_core::parse("fn f948() print(948) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_949() {
    let (m, d) = axolc_core::parse("fn f949() print(949) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_950() {
    let (m, d) = axolc_core::parse("fn f950() print(950) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_951() {
    let (m, d) = axolc_core::parse("fn f951() print(951) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_952() {
    let (m, d) = axolc_core::parse("fn f952() print(952) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_953() {
    let (m, d) = axolc_core::parse("fn f953() print(953) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_954() {
    let (m, d) = axolc_core::parse("fn f954() print(954) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_955() {
    let (m, d) = axolc_core::parse("fn f955() print(955) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_956() {
    let (m, d) = axolc_core::parse("fn f956() print(956) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_957() {
    let (m, d) = axolc_core::parse("fn f957() print(957) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_958() {
    let (m, d) = axolc_core::parse("fn f958() print(958) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_959() {
    let (m, d) = axolc_core::parse("fn f959() print(959) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_960() {
    let (m, d) = axolc_core::parse("fn f960() print(960) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_961() {
    let (m, d) = axolc_core::parse("fn f961() print(961) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_962() {
    let (m, d) = axolc_core::parse("fn f962() print(962) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_963() {
    let (m, d) = axolc_core::parse("fn f963() print(963) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_964() {
    let (m, d) = axolc_core::parse("fn f964() print(964) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_965() {
    let (m, d) = axolc_core::parse("fn f965() print(965) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_966() {
    let (m, d) = axolc_core::parse("fn f966() print(966) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_967() {
    let (m, d) = axolc_core::parse("fn f967() print(967) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_968() {
    let (m, d) = axolc_core::parse("fn f968() print(968) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_969() {
    let (m, d) = axolc_core::parse("fn f969() print(969) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_970() {
    let (m, d) = axolc_core::parse("fn f970() print(970) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_971() {
    let (m, d) = axolc_core::parse("fn f971() print(971) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_972() {
    let (m, d) = axolc_core::parse("fn f972() print(972) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_973() {
    let (m, d) = axolc_core::parse("fn f973() print(973) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_974() {
    let (m, d) = axolc_core::parse("fn f974() print(974) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_975() {
    let (m, d) = axolc_core::parse("fn f975() print(975) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_976() {
    let (m, d) = axolc_core::parse("fn f976() print(976) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_977() {
    let (m, d) = axolc_core::parse("fn f977() print(977) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_978() {
    let (m, d) = axolc_core::parse("fn f978() print(978) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_979() {
    let (m, d) = axolc_core::parse("fn f979() print(979) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_980() {
    let (m, d) = axolc_core::parse("fn f980() print(980) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_981() {
    let (m, d) = axolc_core::parse("fn f981() print(981) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_982() {
    let (m, d) = axolc_core::parse("fn f982() print(982) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_983() {
    let (m, d) = axolc_core::parse("fn f983() print(983) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_984() {
    let (m, d) = axolc_core::parse("fn f984() print(984) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_985() {
    let (m, d) = axolc_core::parse("fn f985() print(985) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_986() {
    let (m, d) = axolc_core::parse("fn f986() print(986) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_987() {
    let (m, d) = axolc_core::parse("fn f987() print(987) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_988() {
    let (m, d) = axolc_core::parse("fn f988() print(988) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_989() {
    let (m, d) = axolc_core::parse("fn f989() print(989) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_990() {
    let (m, d) = axolc_core::parse("fn f990() print(990) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_991() {
    let (m, d) = axolc_core::parse("fn f991() print(991) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_992() {
    let (m, d) = axolc_core::parse("fn f992() print(992) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_993() {
    let (m, d) = axolc_core::parse("fn f993() print(993) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_994() {
    let (m, d) = axolc_core::parse("fn f994() print(994) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_995() {
    let (m, d) = axolc_core::parse("fn f995() print(995) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_996() {
    let (m, d) = axolc_core::parse("fn f996() print(996) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_997() {
    let (m, d) = axolc_core::parse("fn f997() print(997) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_998() {
    let (m, d) = axolc_core::parse("fn f998() print(998) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_999() {
    let (m, d) = axolc_core::parse("fn f999() print(999) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1000() {
    let (m, d) = axolc_core::parse("fn f1000() print(1000) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1001() {
    let (m, d) = axolc_core::parse("fn f1001() print(1001) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1002() {
    let (m, d) = axolc_core::parse("fn f1002() print(1002) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1003() {
    let (m, d) = axolc_core::parse("fn f1003() print(1003) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1004() {
    let (m, d) = axolc_core::parse("fn f1004() print(1004) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1005() {
    let (m, d) = axolc_core::parse("fn f1005() print(1005) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1006() {
    let (m, d) = axolc_core::parse("fn f1006() print(1006) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1007() {
    let (m, d) = axolc_core::parse("fn f1007() print(1007) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1008() {
    let (m, d) = axolc_core::parse("fn f1008() print(1008) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1009() {
    let (m, d) = axolc_core::parse("fn f1009() print(1009) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1010() {
    let (m, d) = axolc_core::parse("fn f1010() print(1010) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1011() {
    let (m, d) = axolc_core::parse("fn f1011() print(1011) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1012() {
    let (m, d) = axolc_core::parse("fn f1012() print(1012) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1013() {
    let (m, d) = axolc_core::parse("fn f1013() print(1013) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1014() {
    let (m, d) = axolc_core::parse("fn f1014() print(1014) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1015() {
    let (m, d) = axolc_core::parse("fn f1015() print(1015) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1016() {
    let (m, d) = axolc_core::parse("fn f1016() print(1016) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1017() {
    let (m, d) = axolc_core::parse("fn f1017() print(1017) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1018() {
    let (m, d) = axolc_core::parse("fn f1018() print(1018) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1019() {
    let (m, d) = axolc_core::parse("fn f1019() print(1019) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1020() {
    let (m, d) = axolc_core::parse("fn f1020() print(1020) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1021() {
    let (m, d) = axolc_core::parse("fn f1021() print(1021) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1022() {
    let (m, d) = axolc_core::parse("fn f1022() print(1022) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1023() {
    let (m, d) = axolc_core::parse("fn f1023() print(1023) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1024() {
    let (m, d) = axolc_core::parse("fn f1024() print(1024) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1025() {
    let (m, d) = axolc_core::parse("fn f1025() print(1025) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1026() {
    let (m, d) = axolc_core::parse("fn f1026() print(1026) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1027() {
    let (m, d) = axolc_core::parse("fn f1027() print(1027) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1028() {
    let (m, d) = axolc_core::parse("fn f1028() print(1028) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1029() {
    let (m, d) = axolc_core::parse("fn f1029() print(1029) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1030() {
    let (m, d) = axolc_core::parse("fn f1030() print(1030) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1031() {
    let (m, d) = axolc_core::parse("fn f1031() print(1031) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1032() {
    let (m, d) = axolc_core::parse("fn f1032() print(1032) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1033() {
    let (m, d) = axolc_core::parse("fn f1033() print(1033) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1034() {
    let (m, d) = axolc_core::parse("fn f1034() print(1034) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1035() {
    let (m, d) = axolc_core::parse("fn f1035() print(1035) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1036() {
    let (m, d) = axolc_core::parse("fn f1036() print(1036) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1037() {
    let (m, d) = axolc_core::parse("fn f1037() print(1037) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1038() {
    let (m, d) = axolc_core::parse("fn f1038() print(1038) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1039() {
    let (m, d) = axolc_core::parse("fn f1039() print(1039) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1040() {
    let (m, d) = axolc_core::parse("fn f1040() print(1040) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1041() {
    let (m, d) = axolc_core::parse("fn f1041() print(1041) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1042() {
    let (m, d) = axolc_core::parse("fn f1042() print(1042) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1043() {
    let (m, d) = axolc_core::parse("fn f1043() print(1043) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1044() {
    let (m, d) = axolc_core::parse("fn f1044() print(1044) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1045() {
    let (m, d) = axolc_core::parse("fn f1045() print(1045) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1046() {
    let (m, d) = axolc_core::parse("fn f1046() print(1046) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1047() {
    let (m, d) = axolc_core::parse("fn f1047() print(1047) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1048() {
    let (m, d) = axolc_core::parse("fn f1048() print(1048) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1049() {
    let (m, d) = axolc_core::parse("fn f1049() print(1049) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1050() {
    let (m, d) = axolc_core::parse("fn f1050() print(1050) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1051() {
    let (m, d) = axolc_core::parse("fn f1051() print(1051) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1052() {
    let (m, d) = axolc_core::parse("fn f1052() print(1052) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1053() {
    let (m, d) = axolc_core::parse("fn f1053() print(1053) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1054() {
    let (m, d) = axolc_core::parse("fn f1054() print(1054) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1055() {
    let (m, d) = axolc_core::parse("fn f1055() print(1055) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1056() {
    let (m, d) = axolc_core::parse("fn f1056() print(1056) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1057() {
    let (m, d) = axolc_core::parse("fn f1057() print(1057) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1058() {
    let (m, d) = axolc_core::parse("fn f1058() print(1058) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1059() {
    let (m, d) = axolc_core::parse("fn f1059() print(1059) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1060() {
    let (m, d) = axolc_core::parse("fn f1060() print(1060) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1061() {
    let (m, d) = axolc_core::parse("fn f1061() print(1061) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1062() {
    let (m, d) = axolc_core::parse("fn f1062() print(1062) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1063() {
    let (m, d) = axolc_core::parse("fn f1063() print(1063) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1064() {
    let (m, d) = axolc_core::parse("fn f1064() print(1064) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1065() {
    let (m, d) = axolc_core::parse("fn f1065() print(1065) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1066() {
    let (m, d) = axolc_core::parse("fn f1066() print(1066) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1067() {
    let (m, d) = axolc_core::parse("fn f1067() print(1067) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1068() {
    let (m, d) = axolc_core::parse("fn f1068() print(1068) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1069() {
    let (m, d) = axolc_core::parse("fn f1069() print(1069) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1070() {
    let (m, d) = axolc_core::parse("fn f1070() print(1070) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1071() {
    let (m, d) = axolc_core::parse("fn f1071() print(1071) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1072() {
    let (m, d) = axolc_core::parse("fn f1072() print(1072) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1073() {
    let (m, d) = axolc_core::parse("fn f1073() print(1073) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1074() {
    let (m, d) = axolc_core::parse("fn f1074() print(1074) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1075() {
    let (m, d) = axolc_core::parse("fn f1075() print(1075) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1076() {
    let (m, d) = axolc_core::parse("fn f1076() print(1076) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1077() {
    let (m, d) = axolc_core::parse("fn f1077() print(1077) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1078() {
    let (m, d) = axolc_core::parse("fn f1078() print(1078) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1079() {
    let (m, d) = axolc_core::parse("fn f1079() print(1079) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1080() {
    let (m, d) = axolc_core::parse("fn f1080() print(1080) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1081() {
    let (m, d) = axolc_core::parse("fn f1081() print(1081) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1082() {
    let (m, d) = axolc_core::parse("fn f1082() print(1082) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1083() {
    let (m, d) = axolc_core::parse("fn f1083() print(1083) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1084() {
    let (m, d) = axolc_core::parse("fn f1084() print(1084) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1085() {
    let (m, d) = axolc_core::parse("fn f1085() print(1085) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1086() {
    let (m, d) = axolc_core::parse("fn f1086() print(1086) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1087() {
    let (m, d) = axolc_core::parse("fn f1087() print(1087) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1088() {
    let (m, d) = axolc_core::parse("fn f1088() print(1088) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1089() {
    let (m, d) = axolc_core::parse("fn f1089() print(1089) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1090() {
    let (m, d) = axolc_core::parse("fn f1090() print(1090) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1091() {
    let (m, d) = axolc_core::parse("fn f1091() print(1091) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1092() {
    let (m, d) = axolc_core::parse("fn f1092() print(1092) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1093() {
    let (m, d) = axolc_core::parse("fn f1093() print(1093) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1094() {
    let (m, d) = axolc_core::parse("fn f1094() print(1094) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1095() {
    let (m, d) = axolc_core::parse("fn f1095() print(1095) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1096() {
    let (m, d) = axolc_core::parse("fn f1096() print(1096) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1097() {
    let (m, d) = axolc_core::parse("fn f1097() print(1097) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1098() {
    let (m, d) = axolc_core::parse("fn f1098() print(1098) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1099() {
    let (m, d) = axolc_core::parse("fn f1099() print(1099) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1100() {
    let (m, d) = axolc_core::parse("fn f1100() print(1100) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1101() {
    let (m, d) = axolc_core::parse("fn f1101() print(1101) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1102() {
    let (m, d) = axolc_core::parse("fn f1102() print(1102) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1103() {
    let (m, d) = axolc_core::parse("fn f1103() print(1103) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1104() {
    let (m, d) = axolc_core::parse("fn f1104() print(1104) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1105() {
    let (m, d) = axolc_core::parse("fn f1105() print(1105) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1106() {
    let (m, d) = axolc_core::parse("fn f1106() print(1106) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1107() {
    let (m, d) = axolc_core::parse("fn f1107() print(1107) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1108() {
    let (m, d) = axolc_core::parse("fn f1108() print(1108) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1109() {
    let (m, d) = axolc_core::parse("fn f1109() print(1109) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1110() {
    let (m, d) = axolc_core::parse("fn f1110() print(1110) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1111() {
    let (m, d) = axolc_core::parse("fn f1111() print(1111) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1112() {
    let (m, d) = axolc_core::parse("fn f1112() print(1112) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1113() {
    let (m, d) = axolc_core::parse("fn f1113() print(1113) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1114() {
    let (m, d) = axolc_core::parse("fn f1114() print(1114) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1115() {
    let (m, d) = axolc_core::parse("fn f1115() print(1115) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1116() {
    let (m, d) = axolc_core::parse("fn f1116() print(1116) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1117() {
    let (m, d) = axolc_core::parse("fn f1117() print(1117) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1118() {
    let (m, d) = axolc_core::parse("fn f1118() print(1118) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1119() {
    let (m, d) = axolc_core::parse("fn f1119() print(1119) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1120() {
    let (m, d) = axolc_core::parse("fn f1120() print(1120) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1121() {
    let (m, d) = axolc_core::parse("fn f1121() print(1121) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1122() {
    let (m, d) = axolc_core::parse("fn f1122() print(1122) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1123() {
    let (m, d) = axolc_core::parse("fn f1123() print(1123) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1124() {
    let (m, d) = axolc_core::parse("fn f1124() print(1124) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1125() {
    let (m, d) = axolc_core::parse("fn f1125() print(1125) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1126() {
    let (m, d) = axolc_core::parse("fn f1126() print(1126) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1127() {
    let (m, d) = axolc_core::parse("fn f1127() print(1127) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1128() {
    let (m, d) = axolc_core::parse("fn f1128() print(1128) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1129() {
    let (m, d) = axolc_core::parse("fn f1129() print(1129) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1130() {
    let (m, d) = axolc_core::parse("fn f1130() print(1130) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1131() {
    let (m, d) = axolc_core::parse("fn f1131() print(1131) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1132() {
    let (m, d) = axolc_core::parse("fn f1132() print(1132) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1133() {
    let (m, d) = axolc_core::parse("fn f1133() print(1133) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1134() {
    let (m, d) = axolc_core::parse("fn f1134() print(1134) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1135() {
    let (m, d) = axolc_core::parse("fn f1135() print(1135) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1136() {
    let (m, d) = axolc_core::parse("fn f1136() print(1136) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1137() {
    let (m, d) = axolc_core::parse("fn f1137() print(1137) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1138() {
    let (m, d) = axolc_core::parse("fn f1138() print(1138) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1139() {
    let (m, d) = axolc_core::parse("fn f1139() print(1139) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1140() {
    let (m, d) = axolc_core::parse("fn f1140() print(1140) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1141() {
    let (m, d) = axolc_core::parse("fn f1141() print(1141) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1142() {
    let (m, d) = axolc_core::parse("fn f1142() print(1142) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1143() {
    let (m, d) = axolc_core::parse("fn f1143() print(1143) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1144() {
    let (m, d) = axolc_core::parse("fn f1144() print(1144) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1145() {
    let (m, d) = axolc_core::parse("fn f1145() print(1145) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1146() {
    let (m, d) = axolc_core::parse("fn f1146() print(1146) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1147() {
    let (m, d) = axolc_core::parse("fn f1147() print(1147) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1148() {
    let (m, d) = axolc_core::parse("fn f1148() print(1148) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1149() {
    let (m, d) = axolc_core::parse("fn f1149() print(1149) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1150() {
    let (m, d) = axolc_core::parse("fn f1150() print(1150) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1151() {
    let (m, d) = axolc_core::parse("fn f1151() print(1151) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1152() {
    let (m, d) = axolc_core::parse("fn f1152() print(1152) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1153() {
    let (m, d) = axolc_core::parse("fn f1153() print(1153) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1154() {
    let (m, d) = axolc_core::parse("fn f1154() print(1154) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1155() {
    let (m, d) = axolc_core::parse("fn f1155() print(1155) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1156() {
    let (m, d) = axolc_core::parse("fn f1156() print(1156) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1157() {
    let (m, d) = axolc_core::parse("fn f1157() print(1157) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1158() {
    let (m, d) = axolc_core::parse("fn f1158() print(1158) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1159() {
    let (m, d) = axolc_core::parse("fn f1159() print(1159) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1160() {
    let (m, d) = axolc_core::parse("fn f1160() print(1160) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1161() {
    let (m, d) = axolc_core::parse("fn f1161() print(1161) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1162() {
    let (m, d) = axolc_core::parse("fn f1162() print(1162) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1163() {
    let (m, d) = axolc_core::parse("fn f1163() print(1163) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1164() {
    let (m, d) = axolc_core::parse("fn f1164() print(1164) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1165() {
    let (m, d) = axolc_core::parse("fn f1165() print(1165) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1166() {
    let (m, d) = axolc_core::parse("fn f1166() print(1166) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1167() {
    let (m, d) = axolc_core::parse("fn f1167() print(1167) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1168() {
    let (m, d) = axolc_core::parse("fn f1168() print(1168) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1169() {
    let (m, d) = axolc_core::parse("fn f1169() print(1169) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1170() {
    let (m, d) = axolc_core::parse("fn f1170() print(1170) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1171() {
    let (m, d) = axolc_core::parse("fn f1171() print(1171) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1172() {
    let (m, d) = axolc_core::parse("fn f1172() print(1172) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1173() {
    let (m, d) = axolc_core::parse("fn f1173() print(1173) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1174() {
    let (m, d) = axolc_core::parse("fn f1174() print(1174) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1175() {
    let (m, d) = axolc_core::parse("fn f1175() print(1175) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1176() {
    let (m, d) = axolc_core::parse("fn f1176() print(1176) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1177() {
    let (m, d) = axolc_core::parse("fn f1177() print(1177) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1178() {
    let (m, d) = axolc_core::parse("fn f1178() print(1178) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1179() {
    let (m, d) = axolc_core::parse("fn f1179() print(1179) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1180() {
    let (m, d) = axolc_core::parse("fn f1180() print(1180) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1181() {
    let (m, d) = axolc_core::parse("fn f1181() print(1181) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1182() {
    let (m, d) = axolc_core::parse("fn f1182() print(1182) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1183() {
    let (m, d) = axolc_core::parse("fn f1183() print(1183) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1184() {
    let (m, d) = axolc_core::parse("fn f1184() print(1184) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1185() {
    let (m, d) = axolc_core::parse("fn f1185() print(1185) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1186() {
    let (m, d) = axolc_core::parse("fn f1186() print(1186) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1187() {
    let (m, d) = axolc_core::parse("fn f1187() print(1187) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1188() {
    let (m, d) = axolc_core::parse("fn f1188() print(1188) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1189() {
    let (m, d) = axolc_core::parse("fn f1189() print(1189) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1190() {
    let (m, d) = axolc_core::parse("fn f1190() print(1190) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1191() {
    let (m, d) = axolc_core::parse("fn f1191() print(1191) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1192() {
    let (m, d) = axolc_core::parse("fn f1192() print(1192) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1193() {
    let (m, d) = axolc_core::parse("fn f1193() print(1193) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1194() {
    let (m, d) = axolc_core::parse("fn f1194() print(1194) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1195() {
    let (m, d) = axolc_core::parse("fn f1195() print(1195) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1196() {
    let (m, d) = axolc_core::parse("fn f1196() print(1196) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1197() {
    let (m, d) = axolc_core::parse("fn f1197() print(1197) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1198() {
    let (m, d) = axolc_core::parse("fn f1198() print(1198) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1199() {
    let (m, d) = axolc_core::parse("fn f1199() print(1199) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1200() {
    let (m, d) = axolc_core::parse("fn f1200() print(1200) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1201() {
    let (m, d) = axolc_core::parse("fn f1201() print(1201) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1202() {
    let (m, d) = axolc_core::parse("fn f1202() print(1202) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1203() {
    let (m, d) = axolc_core::parse("fn f1203() print(1203) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1204() {
    let (m, d) = axolc_core::parse("fn f1204() print(1204) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1205() {
    let (m, d) = axolc_core::parse("fn f1205() print(1205) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1206() {
    let (m, d) = axolc_core::parse("fn f1206() print(1206) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1207() {
    let (m, d) = axolc_core::parse("fn f1207() print(1207) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1208() {
    let (m, d) = axolc_core::parse("fn f1208() print(1208) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1209() {
    let (m, d) = axolc_core::parse("fn f1209() print(1209) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1210() {
    let (m, d) = axolc_core::parse("fn f1210() print(1210) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1211() {
    let (m, d) = axolc_core::parse("fn f1211() print(1211) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1212() {
    let (m, d) = axolc_core::parse("fn f1212() print(1212) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1213() {
    let (m, d) = axolc_core::parse("fn f1213() print(1213) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1214() {
    let (m, d) = axolc_core::parse("fn f1214() print(1214) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1215() {
    let (m, d) = axolc_core::parse("fn f1215() print(1215) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1216() {
    let (m, d) = axolc_core::parse("fn f1216() print(1216) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1217() {
    let (m, d) = axolc_core::parse("fn f1217() print(1217) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1218() {
    let (m, d) = axolc_core::parse("fn f1218() print(1218) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1219() {
    let (m, d) = axolc_core::parse("fn f1219() print(1219) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1220() {
    let (m, d) = axolc_core::parse("fn f1220() print(1220) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1221() {
    let (m, d) = axolc_core::parse("fn f1221() print(1221) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1222() {
    let (m, d) = axolc_core::parse("fn f1222() print(1222) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1223() {
    let (m, d) = axolc_core::parse("fn f1223() print(1223) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1224() {
    let (m, d) = axolc_core::parse("fn f1224() print(1224) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1225() {
    let (m, d) = axolc_core::parse("fn f1225() print(1225) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1226() {
    let (m, d) = axolc_core::parse("fn f1226() print(1226) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1227() {
    let (m, d) = axolc_core::parse("fn f1227() print(1227) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1228() {
    let (m, d) = axolc_core::parse("fn f1228() print(1228) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1229() {
    let (m, d) = axolc_core::parse("fn f1229() print(1229) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1230() {
    let (m, d) = axolc_core::parse("fn f1230() print(1230) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1231() {
    let (m, d) = axolc_core::parse("fn f1231() print(1231) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1232() {
    let (m, d) = axolc_core::parse("fn f1232() print(1232) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1233() {
    let (m, d) = axolc_core::parse("fn f1233() print(1233) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1234() {
    let (m, d) = axolc_core::parse("fn f1234() print(1234) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1235() {
    let (m, d) = axolc_core::parse("fn f1235() print(1235) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1236() {
    let (m, d) = axolc_core::parse("fn f1236() print(1236) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1237() {
    let (m, d) = axolc_core::parse("fn f1237() print(1237) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1238() {
    let (m, d) = axolc_core::parse("fn f1238() print(1238) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1239() {
    let (m, d) = axolc_core::parse("fn f1239() print(1239) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1240() {
    let (m, d) = axolc_core::parse("fn f1240() print(1240) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1241() {
    let (m, d) = axolc_core::parse("fn f1241() print(1241) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1242() {
    let (m, d) = axolc_core::parse("fn f1242() print(1242) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1243() {
    let (m, d) = axolc_core::parse("fn f1243() print(1243) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1244() {
    let (m, d) = axolc_core::parse("fn f1244() print(1244) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1245() {
    let (m, d) = axolc_core::parse("fn f1245() print(1245) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1246() {
    let (m, d) = axolc_core::parse("fn f1246() print(1246) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1247() {
    let (m, d) = axolc_core::parse("fn f1247() print(1247) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1248() {
    let (m, d) = axolc_core::parse("fn f1248() print(1248) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1249() {
    let (m, d) = axolc_core::parse("fn f1249() print(1249) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1250() {
    let (m, d) = axolc_core::parse("fn f1250() print(1250) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1251() {
    let (m, d) = axolc_core::parse("fn f1251() print(1251) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1252() {
    let (m, d) = axolc_core::parse("fn f1252() print(1252) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1253() {
    let (m, d) = axolc_core::parse("fn f1253() print(1253) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1254() {
    let (m, d) = axolc_core::parse("fn f1254() print(1254) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1255() {
    let (m, d) = axolc_core::parse("fn f1255() print(1255) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1256() {
    let (m, d) = axolc_core::parse("fn f1256() print(1256) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1257() {
    let (m, d) = axolc_core::parse("fn f1257() print(1257) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1258() {
    let (m, d) = axolc_core::parse("fn f1258() print(1258) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1259() {
    let (m, d) = axolc_core::parse("fn f1259() print(1259) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1260() {
    let (m, d) = axolc_core::parse("fn f1260() print(1260) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1261() {
    let (m, d) = axolc_core::parse("fn f1261() print(1261) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1262() {
    let (m, d) = axolc_core::parse("fn f1262() print(1262) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1263() {
    let (m, d) = axolc_core::parse("fn f1263() print(1263) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1264() {
    let (m, d) = axolc_core::parse("fn f1264() print(1264) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1265() {
    let (m, d) = axolc_core::parse("fn f1265() print(1265) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1266() {
    let (m, d) = axolc_core::parse("fn f1266() print(1266) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1267() {
    let (m, d) = axolc_core::parse("fn f1267() print(1267) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1268() {
    let (m, d) = axolc_core::parse("fn f1268() print(1268) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1269() {
    let (m, d) = axolc_core::parse("fn f1269() print(1269) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1270() {
    let (m, d) = axolc_core::parse("fn f1270() print(1270) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1271() {
    let (m, d) = axolc_core::parse("fn f1271() print(1271) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1272() {
    let (m, d) = axolc_core::parse("fn f1272() print(1272) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1273() {
    let (m, d) = axolc_core::parse("fn f1273() print(1273) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1274() {
    let (m, d) = axolc_core::parse("fn f1274() print(1274) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1275() {
    let (m, d) = axolc_core::parse("fn f1275() print(1275) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1276() {
    let (m, d) = axolc_core::parse("fn f1276() print(1276) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1277() {
    let (m, d) = axolc_core::parse("fn f1277() print(1277) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1278() {
    let (m, d) = axolc_core::parse("fn f1278() print(1278) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1279() {
    let (m, d) = axolc_core::parse("fn f1279() print(1279) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1280() {
    let (m, d) = axolc_core::parse("fn f1280() print(1280) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1281() {
    let (m, d) = axolc_core::parse("fn f1281() print(1281) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1282() {
    let (m, d) = axolc_core::parse("fn f1282() print(1282) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1283() {
    let (m, d) = axolc_core::parse("fn f1283() print(1283) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1284() {
    let (m, d) = axolc_core::parse("fn f1284() print(1284) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1285() {
    let (m, d) = axolc_core::parse("fn f1285() print(1285) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1286() {
    let (m, d) = axolc_core::parse("fn f1286() print(1286) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1287() {
    let (m, d) = axolc_core::parse("fn f1287() print(1287) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1288() {
    let (m, d) = axolc_core::parse("fn f1288() print(1288) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1289() {
    let (m, d) = axolc_core::parse("fn f1289() print(1289) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1290() {
    let (m, d) = axolc_core::parse("fn f1290() print(1290) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1291() {
    let (m, d) = axolc_core::parse("fn f1291() print(1291) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1292() {
    let (m, d) = axolc_core::parse("fn f1292() print(1292) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1293() {
    let (m, d) = axolc_core::parse("fn f1293() print(1293) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1294() {
    let (m, d) = axolc_core::parse("fn f1294() print(1294) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1295() {
    let (m, d) = axolc_core::parse("fn f1295() print(1295) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1296() {
    let (m, d) = axolc_core::parse("fn f1296() print(1296) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1297() {
    let (m, d) = axolc_core::parse("fn f1297() print(1297) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1298() {
    let (m, d) = axolc_core::parse("fn f1298() print(1298) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1299() {
    let (m, d) = axolc_core::parse("fn f1299() print(1299) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1300() {
    let (m, d) = axolc_core::parse("fn f1300() print(1300) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1301() {
    let (m, d) = axolc_core::parse("fn f1301() print(1301) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1302() {
    let (m, d) = axolc_core::parse("fn f1302() print(1302) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1303() {
    let (m, d) = axolc_core::parse("fn f1303() print(1303) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1304() {
    let (m, d) = axolc_core::parse("fn f1304() print(1304) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1305() {
    let (m, d) = axolc_core::parse("fn f1305() print(1305) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1306() {
    let (m, d) = axolc_core::parse("fn f1306() print(1306) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1307() {
    let (m, d) = axolc_core::parse("fn f1307() print(1307) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1308() {
    let (m, d) = axolc_core::parse("fn f1308() print(1308) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1309() {
    let (m, d) = axolc_core::parse("fn f1309() print(1309) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1310() {
    let (m, d) = axolc_core::parse("fn f1310() print(1310) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1311() {
    let (m, d) = axolc_core::parse("fn f1311() print(1311) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1312() {
    let (m, d) = axolc_core::parse("fn f1312() print(1312) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1313() {
    let (m, d) = axolc_core::parse("fn f1313() print(1313) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1314() {
    let (m, d) = axolc_core::parse("fn f1314() print(1314) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1315() {
    let (m, d) = axolc_core::parse("fn f1315() print(1315) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1316() {
    let (m, d) = axolc_core::parse("fn f1316() print(1316) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1317() {
    let (m, d) = axolc_core::parse("fn f1317() print(1317) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1318() {
    let (m, d) = axolc_core::parse("fn f1318() print(1318) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1319() {
    let (m, d) = axolc_core::parse("fn f1319() print(1319) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1320() {
    let (m, d) = axolc_core::parse("fn f1320() print(1320) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1321() {
    let (m, d) = axolc_core::parse("fn f1321() print(1321) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1322() {
    let (m, d) = axolc_core::parse("fn f1322() print(1322) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1323() {
    let (m, d) = axolc_core::parse("fn f1323() print(1323) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1324() {
    let (m, d) = axolc_core::parse("fn f1324() print(1324) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1325() {
    let (m, d) = axolc_core::parse("fn f1325() print(1325) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1326() {
    let (m, d) = axolc_core::parse("fn f1326() print(1326) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1327() {
    let (m, d) = axolc_core::parse("fn f1327() print(1327) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1328() {
    let (m, d) = axolc_core::parse("fn f1328() print(1328) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1329() {
    let (m, d) = axolc_core::parse("fn f1329() print(1329) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1330() {
    let (m, d) = axolc_core::parse("fn f1330() print(1330) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1331() {
    let (m, d) = axolc_core::parse("fn f1331() print(1331) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1332() {
    let (m, d) = axolc_core::parse("fn f1332() print(1332) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1333() {
    let (m, d) = axolc_core::parse("fn f1333() print(1333) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1334() {
    let (m, d) = axolc_core::parse("fn f1334() print(1334) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1335() {
    let (m, d) = axolc_core::parse("fn f1335() print(1335) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1336() {
    let (m, d) = axolc_core::parse("fn f1336() print(1336) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1337() {
    let (m, d) = axolc_core::parse("fn f1337() print(1337) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1338() {
    let (m, d) = axolc_core::parse("fn f1338() print(1338) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1339() {
    let (m, d) = axolc_core::parse("fn f1339() print(1339) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1340() {
    let (m, d) = axolc_core::parse("fn f1340() print(1340) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1341() {
    let (m, d) = axolc_core::parse("fn f1341() print(1341) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1342() {
    let (m, d) = axolc_core::parse("fn f1342() print(1342) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1343() {
    let (m, d) = axolc_core::parse("fn f1343() print(1343) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1344() {
    let (m, d) = axolc_core::parse("fn f1344() print(1344) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1345() {
    let (m, d) = axolc_core::parse("fn f1345() print(1345) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1346() {
    let (m, d) = axolc_core::parse("fn f1346() print(1346) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1347() {
    let (m, d) = axolc_core::parse("fn f1347() print(1347) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1348() {
    let (m, d) = axolc_core::parse("fn f1348() print(1348) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1349() {
    let (m, d) = axolc_core::parse("fn f1349() print(1349) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1350() {
    let (m, d) = axolc_core::parse("fn f1350() print(1350) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1351() {
    let (m, d) = axolc_core::parse("fn f1351() print(1351) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1352() {
    let (m, d) = axolc_core::parse("fn f1352() print(1352) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1353() {
    let (m, d) = axolc_core::parse("fn f1353() print(1353) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1354() {
    let (m, d) = axolc_core::parse("fn f1354() print(1354) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1355() {
    let (m, d) = axolc_core::parse("fn f1355() print(1355) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1356() {
    let (m, d) = axolc_core::parse("fn f1356() print(1356) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1357() {
    let (m, d) = axolc_core::parse("fn f1357() print(1357) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1358() {
    let (m, d) = axolc_core::parse("fn f1358() print(1358) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1359() {
    let (m, d) = axolc_core::parse("fn f1359() print(1359) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1360() {
    let (m, d) = axolc_core::parse("fn f1360() print(1360) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1361() {
    let (m, d) = axolc_core::parse("fn f1361() print(1361) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1362() {
    let (m, d) = axolc_core::parse("fn f1362() print(1362) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1363() {
    let (m, d) = axolc_core::parse("fn f1363() print(1363) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1364() {
    let (m, d) = axolc_core::parse("fn f1364() print(1364) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1365() {
    let (m, d) = axolc_core::parse("fn f1365() print(1365) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1366() {
    let (m, d) = axolc_core::parse("fn f1366() print(1366) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1367() {
    let (m, d) = axolc_core::parse("fn f1367() print(1367) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1368() {
    let (m, d) = axolc_core::parse("fn f1368() print(1368) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1369() {
    let (m, d) = axolc_core::parse("fn f1369() print(1369) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1370() {
    let (m, d) = axolc_core::parse("fn f1370() print(1370) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1371() {
    let (m, d) = axolc_core::parse("fn f1371() print(1371) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1372() {
    let (m, d) = axolc_core::parse("fn f1372() print(1372) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1373() {
    let (m, d) = axolc_core::parse("fn f1373() print(1373) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1374() {
    let (m, d) = axolc_core::parse("fn f1374() print(1374) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1375() {
    let (m, d) = axolc_core::parse("fn f1375() print(1375) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1376() {
    let (m, d) = axolc_core::parse("fn f1376() print(1376) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1377() {
    let (m, d) = axolc_core::parse("fn f1377() print(1377) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1378() {
    let (m, d) = axolc_core::parse("fn f1378() print(1378) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1379() {
    let (m, d) = axolc_core::parse("fn f1379() print(1379) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1380() {
    let (m, d) = axolc_core::parse("fn f1380() print(1380) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1381() {
    let (m, d) = axolc_core::parse("fn f1381() print(1381) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1382() {
    let (m, d) = axolc_core::parse("fn f1382() print(1382) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1383() {
    let (m, d) = axolc_core::parse("fn f1383() print(1383) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1384() {
    let (m, d) = axolc_core::parse("fn f1384() print(1384) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1385() {
    let (m, d) = axolc_core::parse("fn f1385() print(1385) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1386() {
    let (m, d) = axolc_core::parse("fn f1386() print(1386) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1387() {
    let (m, d) = axolc_core::parse("fn f1387() print(1387) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1388() {
    let (m, d) = axolc_core::parse("fn f1388() print(1388) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1389() {
    let (m, d) = axolc_core::parse("fn f1389() print(1389) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1390() {
    let (m, d) = axolc_core::parse("fn f1390() print(1390) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1391() {
    let (m, d) = axolc_core::parse("fn f1391() print(1391) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1392() {
    let (m, d) = axolc_core::parse("fn f1392() print(1392) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1393() {
    let (m, d) = axolc_core::parse("fn f1393() print(1393) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1394() {
    let (m, d) = axolc_core::parse("fn f1394() print(1394) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1395() {
    let (m, d) = axolc_core::parse("fn f1395() print(1395) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1396() {
    let (m, d) = axolc_core::parse("fn f1396() print(1396) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1397() {
    let (m, d) = axolc_core::parse("fn f1397() print(1397) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1398() {
    let (m, d) = axolc_core::parse("fn f1398() print(1398) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1399() {
    let (m, d) = axolc_core::parse("fn f1399() print(1399) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1400() {
    let (m, d) = axolc_core::parse("fn f1400() print(1400) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1401() {
    let (m, d) = axolc_core::parse("fn f1401() print(1401) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1402() {
    let (m, d) = axolc_core::parse("fn f1402() print(1402) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1403() {
    let (m, d) = axolc_core::parse("fn f1403() print(1403) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1404() {
    let (m, d) = axolc_core::parse("fn f1404() print(1404) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1405() {
    let (m, d) = axolc_core::parse("fn f1405() print(1405) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1406() {
    let (m, d) = axolc_core::parse("fn f1406() print(1406) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1407() {
    let (m, d) = axolc_core::parse("fn f1407() print(1407) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1408() {
    let (m, d) = axolc_core::parse("fn f1408() print(1408) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1409() {
    let (m, d) = axolc_core::parse("fn f1409() print(1409) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1410() {
    let (m, d) = axolc_core::parse("fn f1410() print(1410) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1411() {
    let (m, d) = axolc_core::parse("fn f1411() print(1411) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1412() {
    let (m, d) = axolc_core::parse("fn f1412() print(1412) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1413() {
    let (m, d) = axolc_core::parse("fn f1413() print(1413) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1414() {
    let (m, d) = axolc_core::parse("fn f1414() print(1414) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1415() {
    let (m, d) = axolc_core::parse("fn f1415() print(1415) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1416() {
    let (m, d) = axolc_core::parse("fn f1416() print(1416) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1417() {
    let (m, d) = axolc_core::parse("fn f1417() print(1417) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1418() {
    let (m, d) = axolc_core::parse("fn f1418() print(1418) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1419() {
    let (m, d) = axolc_core::parse("fn f1419() print(1419) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1420() {
    let (m, d) = axolc_core::parse("fn f1420() print(1420) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1421() {
    let (m, d) = axolc_core::parse("fn f1421() print(1421) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1422() {
    let (m, d) = axolc_core::parse("fn f1422() print(1422) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1423() {
    let (m, d) = axolc_core::parse("fn f1423() print(1423) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1424() {
    let (m, d) = axolc_core::parse("fn f1424() print(1424) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1425() {
    let (m, d) = axolc_core::parse("fn f1425() print(1425) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1426() {
    let (m, d) = axolc_core::parse("fn f1426() print(1426) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1427() {
    let (m, d) = axolc_core::parse("fn f1427() print(1427) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1428() {
    let (m, d) = axolc_core::parse("fn f1428() print(1428) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1429() {
    let (m, d) = axolc_core::parse("fn f1429() print(1429) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1430() {
    let (m, d) = axolc_core::parse("fn f1430() print(1430) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1431() {
    let (m, d) = axolc_core::parse("fn f1431() print(1431) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1432() {
    let (m, d) = axolc_core::parse("fn f1432() print(1432) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1433() {
    let (m, d) = axolc_core::parse("fn f1433() print(1433) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1434() {
    let (m, d) = axolc_core::parse("fn f1434() print(1434) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1435() {
    let (m, d) = axolc_core::parse("fn f1435() print(1435) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1436() {
    let (m, d) = axolc_core::parse("fn f1436() print(1436) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1437() {
    let (m, d) = axolc_core::parse("fn f1437() print(1437) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1438() {
    let (m, d) = axolc_core::parse("fn f1438() print(1438) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1439() {
    let (m, d) = axolc_core::parse("fn f1439() print(1439) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1440() {
    let (m, d) = axolc_core::parse("fn f1440() print(1440) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1441() {
    let (m, d) = axolc_core::parse("fn f1441() print(1441) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1442() {
    let (m, d) = axolc_core::parse("fn f1442() print(1442) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1443() {
    let (m, d) = axolc_core::parse("fn f1443() print(1443) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1444() {
    let (m, d) = axolc_core::parse("fn f1444() print(1444) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1445() {
    let (m, d) = axolc_core::parse("fn f1445() print(1445) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1446() {
    let (m, d) = axolc_core::parse("fn f1446() print(1446) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1447() {
    let (m, d) = axolc_core::parse("fn f1447() print(1447) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1448() {
    let (m, d) = axolc_core::parse("fn f1448() print(1448) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1449() {
    let (m, d) = axolc_core::parse("fn f1449() print(1449) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1450() {
    let (m, d) = axolc_core::parse("fn f1450() print(1450) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1451() {
    let (m, d) = axolc_core::parse("fn f1451() print(1451) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1452() {
    let (m, d) = axolc_core::parse("fn f1452() print(1452) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1453() {
    let (m, d) = axolc_core::parse("fn f1453() print(1453) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1454() {
    let (m, d) = axolc_core::parse("fn f1454() print(1454) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1455() {
    let (m, d) = axolc_core::parse("fn f1455() print(1455) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1456() {
    let (m, d) = axolc_core::parse("fn f1456() print(1456) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1457() {
    let (m, d) = axolc_core::parse("fn f1457() print(1457) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1458() {
    let (m, d) = axolc_core::parse("fn f1458() print(1458) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1459() {
    let (m, d) = axolc_core::parse("fn f1459() print(1459) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1460() {
    let (m, d) = axolc_core::parse("fn f1460() print(1460) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1461() {
    let (m, d) = axolc_core::parse("fn f1461() print(1461) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1462() {
    let (m, d) = axolc_core::parse("fn f1462() print(1462) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1463() {
    let (m, d) = axolc_core::parse("fn f1463() print(1463) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1464() {
    let (m, d) = axolc_core::parse("fn f1464() print(1464) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1465() {
    let (m, d) = axolc_core::parse("fn f1465() print(1465) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1466() {
    let (m, d) = axolc_core::parse("fn f1466() print(1466) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1467() {
    let (m, d) = axolc_core::parse("fn f1467() print(1467) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1468() {
    let (m, d) = axolc_core::parse("fn f1468() print(1468) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1469() {
    let (m, d) = axolc_core::parse("fn f1469() print(1469) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1470() {
    let (m, d) = axolc_core::parse("fn f1470() print(1470) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1471() {
    let (m, d) = axolc_core::parse("fn f1471() print(1471) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1472() {
    let (m, d) = axolc_core::parse("fn f1472() print(1472) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1473() {
    let (m, d) = axolc_core::parse("fn f1473() print(1473) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1474() {
    let (m, d) = axolc_core::parse("fn f1474() print(1474) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1475() {
    let (m, d) = axolc_core::parse("fn f1475() print(1475) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1476() {
    let (m, d) = axolc_core::parse("fn f1476() print(1476) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1477() {
    let (m, d) = axolc_core::parse("fn f1477() print(1477) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1478() {
    let (m, d) = axolc_core::parse("fn f1478() print(1478) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1479() {
    let (m, d) = axolc_core::parse("fn f1479() print(1479) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1480() {
    let (m, d) = axolc_core::parse("fn f1480() print(1480) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1481() {
    let (m, d) = axolc_core::parse("fn f1481() print(1481) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1482() {
    let (m, d) = axolc_core::parse("fn f1482() print(1482) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1483() {
    let (m, d) = axolc_core::parse("fn f1483() print(1483) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1484() {
    let (m, d) = axolc_core::parse("fn f1484() print(1484) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1485() {
    let (m, d) = axolc_core::parse("fn f1485() print(1485) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1486() {
    let (m, d) = axolc_core::parse("fn f1486() print(1486) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1487() {
    let (m, d) = axolc_core::parse("fn f1487() print(1487) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1488() {
    let (m, d) = axolc_core::parse("fn f1488() print(1488) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1489() {
    let (m, d) = axolc_core::parse("fn f1489() print(1489) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1490() {
    let (m, d) = axolc_core::parse("fn f1490() print(1490) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1491() {
    let (m, d) = axolc_core::parse("fn f1491() print(1491) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1492() {
    let (m, d) = axolc_core::parse("fn f1492() print(1492) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1493() {
    let (m, d) = axolc_core::parse("fn f1493() print(1493) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1494() {
    let (m, d) = axolc_core::parse("fn f1494() print(1494) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1495() {
    let (m, d) = axolc_core::parse("fn f1495() print(1495) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1496() {
    let (m, d) = axolc_core::parse("fn f1496() print(1496) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1497() {
    let (m, d) = axolc_core::parse("fn f1497() print(1497) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1498() {
    let (m, d) = axolc_core::parse("fn f1498() print(1498) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1499() {
    let (m, d) = axolc_core::parse("fn f1499() print(1499) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1500() {
    let (m, d) = axolc_core::parse("fn f1500() print(1500) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1501() {
    let (m, d) = axolc_core::parse("fn f1501() print(1501) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1502() {
    let (m, d) = axolc_core::parse("fn f1502() print(1502) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1503() {
    let (m, d) = axolc_core::parse("fn f1503() print(1503) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1504() {
    let (m, d) = axolc_core::parse("fn f1504() print(1504) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1505() {
    let (m, d) = axolc_core::parse("fn f1505() print(1505) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1506() {
    let (m, d) = axolc_core::parse("fn f1506() print(1506) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1507() {
    let (m, d) = axolc_core::parse("fn f1507() print(1507) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1508() {
    let (m, d) = axolc_core::parse("fn f1508() print(1508) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1509() {
    let (m, d) = axolc_core::parse("fn f1509() print(1509) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1510() {
    let (m, d) = axolc_core::parse("fn f1510() print(1510) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1511() {
    let (m, d) = axolc_core::parse("fn f1511() print(1511) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1512() {
    let (m, d) = axolc_core::parse("fn f1512() print(1512) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1513() {
    let (m, d) = axolc_core::parse("fn f1513() print(1513) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1514() {
    let (m, d) = axolc_core::parse("fn f1514() print(1514) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1515() {
    let (m, d) = axolc_core::parse("fn f1515() print(1515) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1516() {
    let (m, d) = axolc_core::parse("fn f1516() print(1516) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1517() {
    let (m, d) = axolc_core::parse("fn f1517() print(1517) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1518() {
    let (m, d) = axolc_core::parse("fn f1518() print(1518) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1519() {
    let (m, d) = axolc_core::parse("fn f1519() print(1519) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1520() {
    let (m, d) = axolc_core::parse("fn f1520() print(1520) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1521() {
    let (m, d) = axolc_core::parse("fn f1521() print(1521) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1522() {
    let (m, d) = axolc_core::parse("fn f1522() print(1522) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1523() {
    let (m, d) = axolc_core::parse("fn f1523() print(1523) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1524() {
    let (m, d) = axolc_core::parse("fn f1524() print(1524) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1525() {
    let (m, d) = axolc_core::parse("fn f1525() print(1525) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1526() {
    let (m, d) = axolc_core::parse("fn f1526() print(1526) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1527() {
    let (m, d) = axolc_core::parse("fn f1527() print(1527) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1528() {
    let (m, d) = axolc_core::parse("fn f1528() print(1528) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1529() {
    let (m, d) = axolc_core::parse("fn f1529() print(1529) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1530() {
    let (m, d) = axolc_core::parse("fn f1530() print(1530) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1531() {
    let (m, d) = axolc_core::parse("fn f1531() print(1531) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1532() {
    let (m, d) = axolc_core::parse("fn f1532() print(1532) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1533() {
    let (m, d) = axolc_core::parse("fn f1533() print(1533) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1534() {
    let (m, d) = axolc_core::parse("fn f1534() print(1534) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1535() {
    let (m, d) = axolc_core::parse("fn f1535() print(1535) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1536() {
    let (m, d) = axolc_core::parse("fn f1536() print(1536) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1537() {
    let (m, d) = axolc_core::parse("fn f1537() print(1537) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1538() {
    let (m, d) = axolc_core::parse("fn f1538() print(1538) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1539() {
    let (m, d) = axolc_core::parse("fn f1539() print(1539) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1540() {
    let (m, d) = axolc_core::parse("fn f1540() print(1540) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1541() {
    let (m, d) = axolc_core::parse("fn f1541() print(1541) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1542() {
    let (m, d) = axolc_core::parse("fn f1542() print(1542) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1543() {
    let (m, d) = axolc_core::parse("fn f1543() print(1543) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1544() {
    let (m, d) = axolc_core::parse("fn f1544() print(1544) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1545() {
    let (m, d) = axolc_core::parse("fn f1545() print(1545) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1546() {
    let (m, d) = axolc_core::parse("fn f1546() print(1546) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1547() {
    let (m, d) = axolc_core::parse("fn f1547() print(1547) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1548() {
    let (m, d) = axolc_core::parse("fn f1548() print(1548) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1549() {
    let (m, d) = axolc_core::parse("fn f1549() print(1549) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1550() {
    let (m, d) = axolc_core::parse("fn f1550() print(1550) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1551() {
    let (m, d) = axolc_core::parse("fn f1551() print(1551) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1552() {
    let (m, d) = axolc_core::parse("fn f1552() print(1552) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1553() {
    let (m, d) = axolc_core::parse("fn f1553() print(1553) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1554() {
    let (m, d) = axolc_core::parse("fn f1554() print(1554) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1555() {
    let (m, d) = axolc_core::parse("fn f1555() print(1555) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1556() {
    let (m, d) = axolc_core::parse("fn f1556() print(1556) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1557() {
    let (m, d) = axolc_core::parse("fn f1557() print(1557) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1558() {
    let (m, d) = axolc_core::parse("fn f1558() print(1558) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1559() {
    let (m, d) = axolc_core::parse("fn f1559() print(1559) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1560() {
    let (m, d) = axolc_core::parse("fn f1560() print(1560) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1561() {
    let (m, d) = axolc_core::parse("fn f1561() print(1561) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1562() {
    let (m, d) = axolc_core::parse("fn f1562() print(1562) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1563() {
    let (m, d) = axolc_core::parse("fn f1563() print(1563) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1564() {
    let (m, d) = axolc_core::parse("fn f1564() print(1564) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1565() {
    let (m, d) = axolc_core::parse("fn f1565() print(1565) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1566() {
    let (m, d) = axolc_core::parse("fn f1566() print(1566) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1567() {
    let (m, d) = axolc_core::parse("fn f1567() print(1567) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1568() {
    let (m, d) = axolc_core::parse("fn f1568() print(1568) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1569() {
    let (m, d) = axolc_core::parse("fn f1569() print(1569) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1570() {
    let (m, d) = axolc_core::parse("fn f1570() print(1570) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1571() {
    let (m, d) = axolc_core::parse("fn f1571() print(1571) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1572() {
    let (m, d) = axolc_core::parse("fn f1572() print(1572) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1573() {
    let (m, d) = axolc_core::parse("fn f1573() print(1573) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1574() {
    let (m, d) = axolc_core::parse("fn f1574() print(1574) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1575() {
    let (m, d) = axolc_core::parse("fn f1575() print(1575) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1576() {
    let (m, d) = axolc_core::parse("fn f1576() print(1576) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1577() {
    let (m, d) = axolc_core::parse("fn f1577() print(1577) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1578() {
    let (m, d) = axolc_core::parse("fn f1578() print(1578) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1579() {
    let (m, d) = axolc_core::parse("fn f1579() print(1579) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1580() {
    let (m, d) = axolc_core::parse("fn f1580() print(1580) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1581() {
    let (m, d) = axolc_core::parse("fn f1581() print(1581) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1582() {
    let (m, d) = axolc_core::parse("fn f1582() print(1582) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1583() {
    let (m, d) = axolc_core::parse("fn f1583() print(1583) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1584() {
    let (m, d) = axolc_core::parse("fn f1584() print(1584) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1585() {
    let (m, d) = axolc_core::parse("fn f1585() print(1585) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1586() {
    let (m, d) = axolc_core::parse("fn f1586() print(1586) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1587() {
    let (m, d) = axolc_core::parse("fn f1587() print(1587) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1588() {
    let (m, d) = axolc_core::parse("fn f1588() print(1588) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1589() {
    let (m, d) = axolc_core::parse("fn f1589() print(1589) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1590() {
    let (m, d) = axolc_core::parse("fn f1590() print(1590) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1591() {
    let (m, d) = axolc_core::parse("fn f1591() print(1591) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1592() {
    let (m, d) = axolc_core::parse("fn f1592() print(1592) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1593() {
    let (m, d) = axolc_core::parse("fn f1593() print(1593) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1594() {
    let (m, d) = axolc_core::parse("fn f1594() print(1594) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1595() {
    let (m, d) = axolc_core::parse("fn f1595() print(1595) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1596() {
    let (m, d) = axolc_core::parse("fn f1596() print(1596) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1597() {
    let (m, d) = axolc_core::parse("fn f1597() print(1597) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1598() {
    let (m, d) = axolc_core::parse("fn f1598() print(1598) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1599() {
    let (m, d) = axolc_core::parse("fn f1599() print(1599) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1600() {
    let (m, d) = axolc_core::parse("fn f1600() print(1600) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1601() {
    let (m, d) = axolc_core::parse("fn f1601() print(1601) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1602() {
    let (m, d) = axolc_core::parse("fn f1602() print(1602) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1603() {
    let (m, d) = axolc_core::parse("fn f1603() print(1603) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1604() {
    let (m, d) = axolc_core::parse("fn f1604() print(1604) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1605() {
    let (m, d) = axolc_core::parse("fn f1605() print(1605) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1606() {
    let (m, d) = axolc_core::parse("fn f1606() print(1606) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1607() {
    let (m, d) = axolc_core::parse("fn f1607() print(1607) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1608() {
    let (m, d) = axolc_core::parse("fn f1608() print(1608) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1609() {
    let (m, d) = axolc_core::parse("fn f1609() print(1609) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1610() {
    let (m, d) = axolc_core::parse("fn f1610() print(1610) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1611() {
    let (m, d) = axolc_core::parse("fn f1611() print(1611) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1612() {
    let (m, d) = axolc_core::parse("fn f1612() print(1612) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1613() {
    let (m, d) = axolc_core::parse("fn f1613() print(1613) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1614() {
    let (m, d) = axolc_core::parse("fn f1614() print(1614) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1615() {
    let (m, d) = axolc_core::parse("fn f1615() print(1615) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1616() {
    let (m, d) = axolc_core::parse("fn f1616() print(1616) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1617() {
    let (m, d) = axolc_core::parse("fn f1617() print(1617) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1618() {
    let (m, d) = axolc_core::parse("fn f1618() print(1618) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1619() {
    let (m, d) = axolc_core::parse("fn f1619() print(1619) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1620() {
    let (m, d) = axolc_core::parse("fn f1620() print(1620) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1621() {
    let (m, d) = axolc_core::parse("fn f1621() print(1621) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1622() {
    let (m, d) = axolc_core::parse("fn f1622() print(1622) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1623() {
    let (m, d) = axolc_core::parse("fn f1623() print(1623) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1624() {
    let (m, d) = axolc_core::parse("fn f1624() print(1624) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1625() {
    let (m, d) = axolc_core::parse("fn f1625() print(1625) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1626() {
    let (m, d) = axolc_core::parse("fn f1626() print(1626) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1627() {
    let (m, d) = axolc_core::parse("fn f1627() print(1627) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1628() {
    let (m, d) = axolc_core::parse("fn f1628() print(1628) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1629() {
    let (m, d) = axolc_core::parse("fn f1629() print(1629) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1630() {
    let (m, d) = axolc_core::parse("fn f1630() print(1630) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1631() {
    let (m, d) = axolc_core::parse("fn f1631() print(1631) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1632() {
    let (m, d) = axolc_core::parse("fn f1632() print(1632) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1633() {
    let (m, d) = axolc_core::parse("fn f1633() print(1633) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1634() {
    let (m, d) = axolc_core::parse("fn f1634() print(1634) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1635() {
    let (m, d) = axolc_core::parse("fn f1635() print(1635) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1636() {
    let (m, d) = axolc_core::parse("fn f1636() print(1636) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1637() {
    let (m, d) = axolc_core::parse("fn f1637() print(1637) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1638() {
    let (m, d) = axolc_core::parse("fn f1638() print(1638) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1639() {
    let (m, d) = axolc_core::parse("fn f1639() print(1639) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1640() {
    let (m, d) = axolc_core::parse("fn f1640() print(1640) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1641() {
    let (m, d) = axolc_core::parse("fn f1641() print(1641) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1642() {
    let (m, d) = axolc_core::parse("fn f1642() print(1642) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1643() {
    let (m, d) = axolc_core::parse("fn f1643() print(1643) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1644() {
    let (m, d) = axolc_core::parse("fn f1644() print(1644) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1645() {
    let (m, d) = axolc_core::parse("fn f1645() print(1645) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1646() {
    let (m, d) = axolc_core::parse("fn f1646() print(1646) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1647() {
    let (m, d) = axolc_core::parse("fn f1647() print(1647) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1648() {
    let (m, d) = axolc_core::parse("fn f1648() print(1648) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1649() {
    let (m, d) = axolc_core::parse("fn f1649() print(1649) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1650() {
    let (m, d) = axolc_core::parse("fn f1650() print(1650) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1651() {
    let (m, d) = axolc_core::parse("fn f1651() print(1651) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1652() {
    let (m, d) = axolc_core::parse("fn f1652() print(1652) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1653() {
    let (m, d) = axolc_core::parse("fn f1653() print(1653) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1654() {
    let (m, d) = axolc_core::parse("fn f1654() print(1654) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1655() {
    let (m, d) = axolc_core::parse("fn f1655() print(1655) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1656() {
    let (m, d) = axolc_core::parse("fn f1656() print(1656) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1657() {
    let (m, d) = axolc_core::parse("fn f1657() print(1657) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1658() {
    let (m, d) = axolc_core::parse("fn f1658() print(1658) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1659() {
    let (m, d) = axolc_core::parse("fn f1659() print(1659) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1660() {
    let (m, d) = axolc_core::parse("fn f1660() print(1660) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1661() {
    let (m, d) = axolc_core::parse("fn f1661() print(1661) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1662() {
    let (m, d) = axolc_core::parse("fn f1662() print(1662) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1663() {
    let (m, d) = axolc_core::parse("fn f1663() print(1663) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1664() {
    let (m, d) = axolc_core::parse("fn f1664() print(1664) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1665() {
    let (m, d) = axolc_core::parse("fn f1665() print(1665) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1666() {
    let (m, d) = axolc_core::parse("fn f1666() print(1666) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1667() {
    let (m, d) = axolc_core::parse("fn f1667() print(1667) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1668() {
    let (m, d) = axolc_core::parse("fn f1668() print(1668) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1669() {
    let (m, d) = axolc_core::parse("fn f1669() print(1669) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1670() {
    let (m, d) = axolc_core::parse("fn f1670() print(1670) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1671() {
    let (m, d) = axolc_core::parse("fn f1671() print(1671) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1672() {
    let (m, d) = axolc_core::parse("fn f1672() print(1672) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1673() {
    let (m, d) = axolc_core::parse("fn f1673() print(1673) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1674() {
    let (m, d) = axolc_core::parse("fn f1674() print(1674) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1675() {
    let (m, d) = axolc_core::parse("fn f1675() print(1675) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1676() {
    let (m, d) = axolc_core::parse("fn f1676() print(1676) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1677() {
    let (m, d) = axolc_core::parse("fn f1677() print(1677) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1678() {
    let (m, d) = axolc_core::parse("fn f1678() print(1678) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1679() {
    let (m, d) = axolc_core::parse("fn f1679() print(1679) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1680() {
    let (m, d) = axolc_core::parse("fn f1680() print(1680) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1681() {
    let (m, d) = axolc_core::parse("fn f1681() print(1681) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1682() {
    let (m, d) = axolc_core::parse("fn f1682() print(1682) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1683() {
    let (m, d) = axolc_core::parse("fn f1683() print(1683) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1684() {
    let (m, d) = axolc_core::parse("fn f1684() print(1684) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1685() {
    let (m, d) = axolc_core::parse("fn f1685() print(1685) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1686() {
    let (m, d) = axolc_core::parse("fn f1686() print(1686) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1687() {
    let (m, d) = axolc_core::parse("fn f1687() print(1687) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1688() {
    let (m, d) = axolc_core::parse("fn f1688() print(1688) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1689() {
    let (m, d) = axolc_core::parse("fn f1689() print(1689) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1690() {
    let (m, d) = axolc_core::parse("fn f1690() print(1690) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1691() {
    let (m, d) = axolc_core::parse("fn f1691() print(1691) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1692() {
    let (m, d) = axolc_core::parse("fn f1692() print(1692) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1693() {
    let (m, d) = axolc_core::parse("fn f1693() print(1693) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1694() {
    let (m, d) = axolc_core::parse("fn f1694() print(1694) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1695() {
    let (m, d) = axolc_core::parse("fn f1695() print(1695) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1696() {
    let (m, d) = axolc_core::parse("fn f1696() print(1696) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1697() {
    let (m, d) = axolc_core::parse("fn f1697() print(1697) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1698() {
    let (m, d) = axolc_core::parse("fn f1698() print(1698) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1699() {
    let (m, d) = axolc_core::parse("fn f1699() print(1699) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1700() {
    let (m, d) = axolc_core::parse("fn f1700() print(1700) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1701() {
    let (m, d) = axolc_core::parse("fn f1701() print(1701) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1702() {
    let (m, d) = axolc_core::parse("fn f1702() print(1702) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1703() {
    let (m, d) = axolc_core::parse("fn f1703() print(1703) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1704() {
    let (m, d) = axolc_core::parse("fn f1704() print(1704) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1705() {
    let (m, d) = axolc_core::parse("fn f1705() print(1705) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1706() {
    let (m, d) = axolc_core::parse("fn f1706() print(1706) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1707() {
    let (m, d) = axolc_core::parse("fn f1707() print(1707) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1708() {
    let (m, d) = axolc_core::parse("fn f1708() print(1708) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1709() {
    let (m, d) = axolc_core::parse("fn f1709() print(1709) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1710() {
    let (m, d) = axolc_core::parse("fn f1710() print(1710) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1711() {
    let (m, d) = axolc_core::parse("fn f1711() print(1711) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1712() {
    let (m, d) = axolc_core::parse("fn f1712() print(1712) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1713() {
    let (m, d) = axolc_core::parse("fn f1713() print(1713) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1714() {
    let (m, d) = axolc_core::parse("fn f1714() print(1714) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1715() {
    let (m, d) = axolc_core::parse("fn f1715() print(1715) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1716() {
    let (m, d) = axolc_core::parse("fn f1716() print(1716) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1717() {
    let (m, d) = axolc_core::parse("fn f1717() print(1717) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1718() {
    let (m, d) = axolc_core::parse("fn f1718() print(1718) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1719() {
    let (m, d) = axolc_core::parse("fn f1719() print(1719) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1720() {
    let (m, d) = axolc_core::parse("fn f1720() print(1720) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1721() {
    let (m, d) = axolc_core::parse("fn f1721() print(1721) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1722() {
    let (m, d) = axolc_core::parse("fn f1722() print(1722) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1723() {
    let (m, d) = axolc_core::parse("fn f1723() print(1723) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1724() {
    let (m, d) = axolc_core::parse("fn f1724() print(1724) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1725() {
    let (m, d) = axolc_core::parse("fn f1725() print(1725) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1726() {
    let (m, d) = axolc_core::parse("fn f1726() print(1726) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1727() {
    let (m, d) = axolc_core::parse("fn f1727() print(1727) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1728() {
    let (m, d) = axolc_core::parse("fn f1728() print(1728) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1729() {
    let (m, d) = axolc_core::parse("fn f1729() print(1729) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1730() {
    let (m, d) = axolc_core::parse("fn f1730() print(1730) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1731() {
    let (m, d) = axolc_core::parse("fn f1731() print(1731) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1732() {
    let (m, d) = axolc_core::parse("fn f1732() print(1732) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1733() {
    let (m, d) = axolc_core::parse("fn f1733() print(1733) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1734() {
    let (m, d) = axolc_core::parse("fn f1734() print(1734) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1735() {
    let (m, d) = axolc_core::parse("fn f1735() print(1735) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1736() {
    let (m, d) = axolc_core::parse("fn f1736() print(1736) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1737() {
    let (m, d) = axolc_core::parse("fn f1737() print(1737) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1738() {
    let (m, d) = axolc_core::parse("fn f1738() print(1738) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1739() {
    let (m, d) = axolc_core::parse("fn f1739() print(1739) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1740() {
    let (m, d) = axolc_core::parse("fn f1740() print(1740) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1741() {
    let (m, d) = axolc_core::parse("fn f1741() print(1741) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1742() {
    let (m, d) = axolc_core::parse("fn f1742() print(1742) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1743() {
    let (m, d) = axolc_core::parse("fn f1743() print(1743) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1744() {
    let (m, d) = axolc_core::parse("fn f1744() print(1744) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1745() {
    let (m, d) = axolc_core::parse("fn f1745() print(1745) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1746() {
    let (m, d) = axolc_core::parse("fn f1746() print(1746) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1747() {
    let (m, d) = axolc_core::parse("fn f1747() print(1747) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1748() {
    let (m, d) = axolc_core::parse("fn f1748() print(1748) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1749() {
    let (m, d) = axolc_core::parse("fn f1749() print(1749) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1750() {
    let (m, d) = axolc_core::parse("fn f1750() print(1750) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1751() {
    let (m, d) = axolc_core::parse("fn f1751() print(1751) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1752() {
    let (m, d) = axolc_core::parse("fn f1752() print(1752) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1753() {
    let (m, d) = axolc_core::parse("fn f1753() print(1753) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1754() {
    let (m, d) = axolc_core::parse("fn f1754() print(1754) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1755() {
    let (m, d) = axolc_core::parse("fn f1755() print(1755) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1756() {
    let (m, d) = axolc_core::parse("fn f1756() print(1756) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1757() {
    let (m, d) = axolc_core::parse("fn f1757() print(1757) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1758() {
    let (m, d) = axolc_core::parse("fn f1758() print(1758) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1759() {
    let (m, d) = axolc_core::parse("fn f1759() print(1759) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1760() {
    let (m, d) = axolc_core::parse("fn f1760() print(1760) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1761() {
    let (m, d) = axolc_core::parse("fn f1761() print(1761) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1762() {
    let (m, d) = axolc_core::parse("fn f1762() print(1762) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1763() {
    let (m, d) = axolc_core::parse("fn f1763() print(1763) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1764() {
    let (m, d) = axolc_core::parse("fn f1764() print(1764) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1765() {
    let (m, d) = axolc_core::parse("fn f1765() print(1765) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1766() {
    let (m, d) = axolc_core::parse("fn f1766() print(1766) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1767() {
    let (m, d) = axolc_core::parse("fn f1767() print(1767) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1768() {
    let (m, d) = axolc_core::parse("fn f1768() print(1768) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1769() {
    let (m, d) = axolc_core::parse("fn f1769() print(1769) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1770() {
    let (m, d) = axolc_core::parse("fn f1770() print(1770) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1771() {
    let (m, d) = axolc_core::parse("fn f1771() print(1771) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1772() {
    let (m, d) = axolc_core::parse("fn f1772() print(1772) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1773() {
    let (m, d) = axolc_core::parse("fn f1773() print(1773) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1774() {
    let (m, d) = axolc_core::parse("fn f1774() print(1774) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1775() {
    let (m, d) = axolc_core::parse("fn f1775() print(1775) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1776() {
    let (m, d) = axolc_core::parse("fn f1776() print(1776) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1777() {
    let (m, d) = axolc_core::parse("fn f1777() print(1777) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1778() {
    let (m, d) = axolc_core::parse("fn f1778() print(1778) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1779() {
    let (m, d) = axolc_core::parse("fn f1779() print(1779) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1780() {
    let (m, d) = axolc_core::parse("fn f1780() print(1780) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1781() {
    let (m, d) = axolc_core::parse("fn f1781() print(1781) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1782() {
    let (m, d) = axolc_core::parse("fn f1782() print(1782) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1783() {
    let (m, d) = axolc_core::parse("fn f1783() print(1783) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1784() {
    let (m, d) = axolc_core::parse("fn f1784() print(1784) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1785() {
    let (m, d) = axolc_core::parse("fn f1785() print(1785) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1786() {
    let (m, d) = axolc_core::parse("fn f1786() print(1786) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1787() {
    let (m, d) = axolc_core::parse("fn f1787() print(1787) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1788() {
    let (m, d) = axolc_core::parse("fn f1788() print(1788) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1789() {
    let (m, d) = axolc_core::parse("fn f1789() print(1789) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1790() {
    let (m, d) = axolc_core::parse("fn f1790() print(1790) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1791() {
    let (m, d) = axolc_core::parse("fn f1791() print(1791) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1792() {
    let (m, d) = axolc_core::parse("fn f1792() print(1792) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1793() {
    let (m, d) = axolc_core::parse("fn f1793() print(1793) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1794() {
    let (m, d) = axolc_core::parse("fn f1794() print(1794) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1795() {
    let (m, d) = axolc_core::parse("fn f1795() print(1795) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1796() {
    let (m, d) = axolc_core::parse("fn f1796() print(1796) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1797() {
    let (m, d) = axolc_core::parse("fn f1797() print(1797) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1798() {
    let (m, d) = axolc_core::parse("fn f1798() print(1798) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1799() {
    let (m, d) = axolc_core::parse("fn f1799() print(1799) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1800() {
    let (m, d) = axolc_core::parse("fn f1800() print(1800) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1801() {
    let (m, d) = axolc_core::parse("fn f1801() print(1801) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1802() {
    let (m, d) = axolc_core::parse("fn f1802() print(1802) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1803() {
    let (m, d) = axolc_core::parse("fn f1803() print(1803) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1804() {
    let (m, d) = axolc_core::parse("fn f1804() print(1804) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1805() {
    let (m, d) = axolc_core::parse("fn f1805() print(1805) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1806() {
    let (m, d) = axolc_core::parse("fn f1806() print(1806) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1807() {
    let (m, d) = axolc_core::parse("fn f1807() print(1807) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1808() {
    let (m, d) = axolc_core::parse("fn f1808() print(1808) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1809() {
    let (m, d) = axolc_core::parse("fn f1809() print(1809) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1810() {
    let (m, d) = axolc_core::parse("fn f1810() print(1810) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1811() {
    let (m, d) = axolc_core::parse("fn f1811() print(1811) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1812() {
    let (m, d) = axolc_core::parse("fn f1812() print(1812) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1813() {
    let (m, d) = axolc_core::parse("fn f1813() print(1813) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1814() {
    let (m, d) = axolc_core::parse("fn f1814() print(1814) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1815() {
    let (m, d) = axolc_core::parse("fn f1815() print(1815) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1816() {
    let (m, d) = axolc_core::parse("fn f1816() print(1816) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1817() {
    let (m, d) = axolc_core::parse("fn f1817() print(1817) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1818() {
    let (m, d) = axolc_core::parse("fn f1818() print(1818) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1819() {
    let (m, d) = axolc_core::parse("fn f1819() print(1819) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1820() {
    let (m, d) = axolc_core::parse("fn f1820() print(1820) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1821() {
    let (m, d) = axolc_core::parse("fn f1821() print(1821) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1822() {
    let (m, d) = axolc_core::parse("fn f1822() print(1822) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1823() {
    let (m, d) = axolc_core::parse("fn f1823() print(1823) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1824() {
    let (m, d) = axolc_core::parse("fn f1824() print(1824) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1825() {
    let (m, d) = axolc_core::parse("fn f1825() print(1825) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1826() {
    let (m, d) = axolc_core::parse("fn f1826() print(1826) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1827() {
    let (m, d) = axolc_core::parse("fn f1827() print(1827) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1828() {
    let (m, d) = axolc_core::parse("fn f1828() print(1828) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1829() {
    let (m, d) = axolc_core::parse("fn f1829() print(1829) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1830() {
    let (m, d) = axolc_core::parse("fn f1830() print(1830) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1831() {
    let (m, d) = axolc_core::parse("fn f1831() print(1831) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1832() {
    let (m, d) = axolc_core::parse("fn f1832() print(1832) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1833() {
    let (m, d) = axolc_core::parse("fn f1833() print(1833) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1834() {
    let (m, d) = axolc_core::parse("fn f1834() print(1834) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1835() {
    let (m, d) = axolc_core::parse("fn f1835() print(1835) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1836() {
    let (m, d) = axolc_core::parse("fn f1836() print(1836) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1837() {
    let (m, d) = axolc_core::parse("fn f1837() print(1837) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1838() {
    let (m, d) = axolc_core::parse("fn f1838() print(1838) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1839() {
    let (m, d) = axolc_core::parse("fn f1839() print(1839) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1840() {
    let (m, d) = axolc_core::parse("fn f1840() print(1840) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1841() {
    let (m, d) = axolc_core::parse("fn f1841() print(1841) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1842() {
    let (m, d) = axolc_core::parse("fn f1842() print(1842) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1843() {
    let (m, d) = axolc_core::parse("fn f1843() print(1843) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1844() {
    let (m, d) = axolc_core::parse("fn f1844() print(1844) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1845() {
    let (m, d) = axolc_core::parse("fn f1845() print(1845) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1846() {
    let (m, d) = axolc_core::parse("fn f1846() print(1846) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1847() {
    let (m, d) = axolc_core::parse("fn f1847() print(1847) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1848() {
    let (m, d) = axolc_core::parse("fn f1848() print(1848) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1849() {
    let (m, d) = axolc_core::parse("fn f1849() print(1849) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1850() {
    let (m, d) = axolc_core::parse("fn f1850() print(1850) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1851() {
    let (m, d) = axolc_core::parse("fn f1851() print(1851) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1852() {
    let (m, d) = axolc_core::parse("fn f1852() print(1852) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1853() {
    let (m, d) = axolc_core::parse("fn f1853() print(1853) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1854() {
    let (m, d) = axolc_core::parse("fn f1854() print(1854) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1855() {
    let (m, d) = axolc_core::parse("fn f1855() print(1855) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1856() {
    let (m, d) = axolc_core::parse("fn f1856() print(1856) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1857() {
    let (m, d) = axolc_core::parse("fn f1857() print(1857) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1858() {
    let (m, d) = axolc_core::parse("fn f1858() print(1858) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1859() {
    let (m, d) = axolc_core::parse("fn f1859() print(1859) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1860() {
    let (m, d) = axolc_core::parse("fn f1860() print(1860) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1861() {
    let (m, d) = axolc_core::parse("fn f1861() print(1861) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1862() {
    let (m, d) = axolc_core::parse("fn f1862() print(1862) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1863() {
    let (m, d) = axolc_core::parse("fn f1863() print(1863) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1864() {
    let (m, d) = axolc_core::parse("fn f1864() print(1864) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1865() {
    let (m, d) = axolc_core::parse("fn f1865() print(1865) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1866() {
    let (m, d) = axolc_core::parse("fn f1866() print(1866) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1867() {
    let (m, d) = axolc_core::parse("fn f1867() print(1867) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1868() {
    let (m, d) = axolc_core::parse("fn f1868() print(1868) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1869() {
    let (m, d) = axolc_core::parse("fn f1869() print(1869) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1870() {
    let (m, d) = axolc_core::parse("fn f1870() print(1870) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1871() {
    let (m, d) = axolc_core::parse("fn f1871() print(1871) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1872() {
    let (m, d) = axolc_core::parse("fn f1872() print(1872) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1873() {
    let (m, d) = axolc_core::parse("fn f1873() print(1873) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1874() {
    let (m, d) = axolc_core::parse("fn f1874() print(1874) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1875() {
    let (m, d) = axolc_core::parse("fn f1875() print(1875) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1876() {
    let (m, d) = axolc_core::parse("fn f1876() print(1876) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1877() {
    let (m, d) = axolc_core::parse("fn f1877() print(1877) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1878() {
    let (m, d) = axolc_core::parse("fn f1878() print(1878) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1879() {
    let (m, d) = axolc_core::parse("fn f1879() print(1879) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1880() {
    let (m, d) = axolc_core::parse("fn f1880() print(1880) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1881() {
    let (m, d) = axolc_core::parse("fn f1881() print(1881) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1882() {
    let (m, d) = axolc_core::parse("fn f1882() print(1882) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1883() {
    let (m, d) = axolc_core::parse("fn f1883() print(1883) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1884() {
    let (m, d) = axolc_core::parse("fn f1884() print(1884) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1885() {
    let (m, d) = axolc_core::parse("fn f1885() print(1885) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1886() {
    let (m, d) = axolc_core::parse("fn f1886() print(1886) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1887() {
    let (m, d) = axolc_core::parse("fn f1887() print(1887) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1888() {
    let (m, d) = axolc_core::parse("fn f1888() print(1888) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1889() {
    let (m, d) = axolc_core::parse("fn f1889() print(1889) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1890() {
    let (m, d) = axolc_core::parse("fn f1890() print(1890) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1891() {
    let (m, d) = axolc_core::parse("fn f1891() print(1891) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1892() {
    let (m, d) = axolc_core::parse("fn f1892() print(1892) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1893() {
    let (m, d) = axolc_core::parse("fn f1893() print(1893) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1894() {
    let (m, d) = axolc_core::parse("fn f1894() print(1894) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1895() {
    let (m, d) = axolc_core::parse("fn f1895() print(1895) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1896() {
    let (m, d) = axolc_core::parse("fn f1896() print(1896) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1897() {
    let (m, d) = axolc_core::parse("fn f1897() print(1897) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1898() {
    let (m, d) = axolc_core::parse("fn f1898() print(1898) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1899() {
    let (m, d) = axolc_core::parse("fn f1899() print(1899) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1900() {
    let (m, d) = axolc_core::parse("fn f1900() print(1900) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1901() {
    let (m, d) = axolc_core::parse("fn f1901() print(1901) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1902() {
    let (m, d) = axolc_core::parse("fn f1902() print(1902) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1903() {
    let (m, d) = axolc_core::parse("fn f1903() print(1903) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1904() {
    let (m, d) = axolc_core::parse("fn f1904() print(1904) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1905() {
    let (m, d) = axolc_core::parse("fn f1905() print(1905) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1906() {
    let (m, d) = axolc_core::parse("fn f1906() print(1906) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1907() {
    let (m, d) = axolc_core::parse("fn f1907() print(1907) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1908() {
    let (m, d) = axolc_core::parse("fn f1908() print(1908) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1909() {
    let (m, d) = axolc_core::parse("fn f1909() print(1909) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1910() {
    let (m, d) = axolc_core::parse("fn f1910() print(1910) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1911() {
    let (m, d) = axolc_core::parse("fn f1911() print(1911) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1912() {
    let (m, d) = axolc_core::parse("fn f1912() print(1912) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1913() {
    let (m, d) = axolc_core::parse("fn f1913() print(1913) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1914() {
    let (m, d) = axolc_core::parse("fn f1914() print(1914) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1915() {
    let (m, d) = axolc_core::parse("fn f1915() print(1915) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1916() {
    let (m, d) = axolc_core::parse("fn f1916() print(1916) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1917() {
    let (m, d) = axolc_core::parse("fn f1917() print(1917) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1918() {
    let (m, d) = axolc_core::parse("fn f1918() print(1918) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1919() {
    let (m, d) = axolc_core::parse("fn f1919() print(1919) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1920() {
    let (m, d) = axolc_core::parse("fn f1920() print(1920) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1921() {
    let (m, d) = axolc_core::parse("fn f1921() print(1921) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1922() {
    let (m, d) = axolc_core::parse("fn f1922() print(1922) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1923() {
    let (m, d) = axolc_core::parse("fn f1923() print(1923) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1924() {
    let (m, d) = axolc_core::parse("fn f1924() print(1924) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1925() {
    let (m, d) = axolc_core::parse("fn f1925() print(1925) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1926() {
    let (m, d) = axolc_core::parse("fn f1926() print(1926) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1927() {
    let (m, d) = axolc_core::parse("fn f1927() print(1927) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1928() {
    let (m, d) = axolc_core::parse("fn f1928() print(1928) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1929() {
    let (m, d) = axolc_core::parse("fn f1929() print(1929) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1930() {
    let (m, d) = axolc_core::parse("fn f1930() print(1930) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1931() {
    let (m, d) = axolc_core::parse("fn f1931() print(1931) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1932() {
    let (m, d) = axolc_core::parse("fn f1932() print(1932) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1933() {
    let (m, d) = axolc_core::parse("fn f1933() print(1933) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1934() {
    let (m, d) = axolc_core::parse("fn f1934() print(1934) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1935() {
    let (m, d) = axolc_core::parse("fn f1935() print(1935) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1936() {
    let (m, d) = axolc_core::parse("fn f1936() print(1936) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1937() {
    let (m, d) = axolc_core::parse("fn f1937() print(1937) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1938() {
    let (m, d) = axolc_core::parse("fn f1938() print(1938) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1939() {
    let (m, d) = axolc_core::parse("fn f1939() print(1939) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1940() {
    let (m, d) = axolc_core::parse("fn f1940() print(1940) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1941() {
    let (m, d) = axolc_core::parse("fn f1941() print(1941) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1942() {
    let (m, d) = axolc_core::parse("fn f1942() print(1942) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1943() {
    let (m, d) = axolc_core::parse("fn f1943() print(1943) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1944() {
    let (m, d) = axolc_core::parse("fn f1944() print(1944) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1945() {
    let (m, d) = axolc_core::parse("fn f1945() print(1945) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1946() {
    let (m, d) = axolc_core::parse("fn f1946() print(1946) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1947() {
    let (m, d) = axolc_core::parse("fn f1947() print(1947) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1948() {
    let (m, d) = axolc_core::parse("fn f1948() print(1948) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1949() {
    let (m, d) = axolc_core::parse("fn f1949() print(1949) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1950() {
    let (m, d) = axolc_core::parse("fn f1950() print(1950) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1951() {
    let (m, d) = axolc_core::parse("fn f1951() print(1951) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1952() {
    let (m, d) = axolc_core::parse("fn f1952() print(1952) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1953() {
    let (m, d) = axolc_core::parse("fn f1953() print(1953) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1954() {
    let (m, d) = axolc_core::parse("fn f1954() print(1954) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1955() {
    let (m, d) = axolc_core::parse("fn f1955() print(1955) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1956() {
    let (m, d) = axolc_core::parse("fn f1956() print(1956) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1957() {
    let (m, d) = axolc_core::parse("fn f1957() print(1957) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1958() {
    let (m, d) = axolc_core::parse("fn f1958() print(1958) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1959() {
    let (m, d) = axolc_core::parse("fn f1959() print(1959) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1960() {
    let (m, d) = axolc_core::parse("fn f1960() print(1960) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1961() {
    let (m, d) = axolc_core::parse("fn f1961() print(1961) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1962() {
    let (m, d) = axolc_core::parse("fn f1962() print(1962) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1963() {
    let (m, d) = axolc_core::parse("fn f1963() print(1963) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1964() {
    let (m, d) = axolc_core::parse("fn f1964() print(1964) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1965() {
    let (m, d) = axolc_core::parse("fn f1965() print(1965) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1966() {
    let (m, d) = axolc_core::parse("fn f1966() print(1966) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1967() {
    let (m, d) = axolc_core::parse("fn f1967() print(1967) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1968() {
    let (m, d) = axolc_core::parse("fn f1968() print(1968) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1969() {
    let (m, d) = axolc_core::parse("fn f1969() print(1969) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1970() {
    let (m, d) = axolc_core::parse("fn f1970() print(1970) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1971() {
    let (m, d) = axolc_core::parse("fn f1971() print(1971) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1972() {
    let (m, d) = axolc_core::parse("fn f1972() print(1972) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1973() {
    let (m, d) = axolc_core::parse("fn f1973() print(1973) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1974() {
    let (m, d) = axolc_core::parse("fn f1974() print(1974) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1975() {
    let (m, d) = axolc_core::parse("fn f1975() print(1975) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1976() {
    let (m, d) = axolc_core::parse("fn f1976() print(1976) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1977() {
    let (m, d) = axolc_core::parse("fn f1977() print(1977) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1978() {
    let (m, d) = axolc_core::parse("fn f1978() print(1978) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1979() {
    let (m, d) = axolc_core::parse("fn f1979() print(1979) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1980() {
    let (m, d) = axolc_core::parse("fn f1980() print(1980) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1981() {
    let (m, d) = axolc_core::parse("fn f1981() print(1981) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1982() {
    let (m, d) = axolc_core::parse("fn f1982() print(1982) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1983() {
    let (m, d) = axolc_core::parse("fn f1983() print(1983) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1984() {
    let (m, d) = axolc_core::parse("fn f1984() print(1984) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1985() {
    let (m, d) = axolc_core::parse("fn f1985() print(1985) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1986() {
    let (m, d) = axolc_core::parse("fn f1986() print(1986) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1987() {
    let (m, d) = axolc_core::parse("fn f1987() print(1987) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1988() {
    let (m, d) = axolc_core::parse("fn f1988() print(1988) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1989() {
    let (m, d) = axolc_core::parse("fn f1989() print(1989) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1990() {
    let (m, d) = axolc_core::parse("fn f1990() print(1990) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1991() {
    let (m, d) = axolc_core::parse("fn f1991() print(1991) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1992() {
    let (m, d) = axolc_core::parse("fn f1992() print(1992) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1993() {
    let (m, d) = axolc_core::parse("fn f1993() print(1993) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1994() {
    let (m, d) = axolc_core::parse("fn f1994() print(1994) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1995() {
    let (m, d) = axolc_core::parse("fn f1995() print(1995) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1996() {
    let (m, d) = axolc_core::parse("fn f1996() print(1996) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1997() {
    let (m, d) = axolc_core::parse("fn f1997() print(1997) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1998() {
    let (m, d) = axolc_core::parse("fn f1998() print(1998) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
#[test]
fn parse_fn_1999() {
    let (m, d) = axolc_core::parse("fn f1999() print(1999) end", 0);
    assert!(!d.has_errors());
    assert_eq!(m.items.len(), 1);
}
