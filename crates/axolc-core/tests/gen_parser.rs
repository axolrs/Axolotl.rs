// Owner: PascalElixir / axolrs (GitHub org)
// File: Auto-generated tests for parser.

use axolc_core::parse;

#[test]
fn parse_fn_0() {
    let (module, diags) = parse("fn f0() print(0) end", 0);
    assert!(!diags.has_errors(), "parse_fn_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_1() {
    let (module, diags) = parse("fn f1() print(1) end", 0);
    assert!(!diags.has_errors(), "parse_fn_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_2() {
    let (module, diags) = parse("fn f2() print(2) end", 0);
    assert!(!diags.has_errors(), "parse_fn_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_3() {
    let (module, diags) = parse("fn f3() print(3) end", 0);
    assert!(!diags.has_errors(), "parse_fn_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_4() {
    let (module, diags) = parse("fn f4() print(4) end", 0);
    assert!(!diags.has_errors(), "parse_fn_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_5() {
    let (module, diags) = parse("fn f5() print(5) end", 0);
    assert!(!diags.has_errors(), "parse_fn_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_6() {
    let (module, diags) = parse("fn f6() print(6) end", 0);
    assert!(!diags.has_errors(), "parse_fn_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_7() {
    let (module, diags) = parse("fn f7() print(7) end", 0);
    assert!(!diags.has_errors(), "parse_fn_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_8() {
    let (module, diags) = parse("fn f8() print(8) end", 0);
    assert!(!diags.has_errors(), "parse_fn_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_9() {
    let (module, diags) = parse("fn f9() print(9) end", 0);
    assert!(!diags.has_errors(), "parse_fn_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_10() {
    let (module, diags) = parse("fn f10() print(10) end", 0);
    assert!(!diags.has_errors(), "parse_fn_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_11() {
    let (module, diags) = parse("fn f11() print(11) end", 0);
    assert!(!diags.has_errors(), "parse_fn_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_12() {
    let (module, diags) = parse("fn f12() print(12) end", 0);
    assert!(!diags.has_errors(), "parse_fn_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_13() {
    let (module, diags) = parse("fn f13() print(13) end", 0);
    assert!(!diags.has_errors(), "parse_fn_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_14() {
    let (module, diags) = parse("fn f14() print(14) end", 0);
    assert!(!diags.has_errors(), "parse_fn_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_15() {
    let (module, diags) = parse("fn f15() print(15) end", 0);
    assert!(!diags.has_errors(), "parse_fn_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_16() {
    let (module, diags) = parse("fn f16() print(16) end", 0);
    assert!(!diags.has_errors(), "parse_fn_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_17() {
    let (module, diags) = parse("fn f17() print(17) end", 0);
    assert!(!diags.has_errors(), "parse_fn_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_18() {
    let (module, diags) = parse("fn f18() print(18) end", 0);
    assert!(!diags.has_errors(), "parse_fn_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_19() {
    let (module, diags) = parse("fn f19() print(19) end", 0);
    assert!(!diags.has_errors(), "parse_fn_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_20() {
    let (module, diags) = parse("fn f20() print(20) end", 0);
    assert!(!diags.has_errors(), "parse_fn_20: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_21() {
    let (module, diags) = parse("fn f21() print(21) end", 0);
    assert!(!diags.has_errors(), "parse_fn_21: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_22() {
    let (module, diags) = parse("fn f22() print(22) end", 0);
    assert!(!diags.has_errors(), "parse_fn_22: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_23() {
    let (module, diags) = parse("fn f23() print(23) end", 0);
    assert!(!diags.has_errors(), "parse_fn_23: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_24() {
    let (module, diags) = parse("fn f24() print(24) end", 0);
    assert!(!diags.has_errors(), "parse_fn_24: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_25() {
    let (module, diags) = parse("fn f25() print(25) end", 0);
    assert!(!diags.has_errors(), "parse_fn_25: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_26() {
    let (module, diags) = parse("fn f26() print(26) end", 0);
    assert!(!diags.has_errors(), "parse_fn_26: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_27() {
    let (module, diags) = parse("fn f27() print(27) end", 0);
    assert!(!diags.has_errors(), "parse_fn_27: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_28() {
    let (module, diags) = parse("fn f28() print(28) end", 0);
    assert!(!diags.has_errors(), "parse_fn_28: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_29() {
    let (module, diags) = parse("fn f29() print(29) end", 0);
    assert!(!diags.has_errors(), "parse_fn_29: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_30() {
    let (module, diags) = parse("fn f30() print(30) end", 0);
    assert!(!diags.has_errors(), "parse_fn_30: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_31() {
    let (module, diags) = parse("fn f31() print(31) end", 0);
    assert!(!diags.has_errors(), "parse_fn_31: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_32() {
    let (module, diags) = parse("fn f32() print(32) end", 0);
    assert!(!diags.has_errors(), "parse_fn_32: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_33() {
    let (module, diags) = parse("fn f33() print(33) end", 0);
    assert!(!diags.has_errors(), "parse_fn_33: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_34() {
    let (module, diags) = parse("fn f34() print(34) end", 0);
    assert!(!diags.has_errors(), "parse_fn_34: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_35() {
    let (module, diags) = parse("fn f35() print(35) end", 0);
    assert!(!diags.has_errors(), "parse_fn_35: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_36() {
    let (module, diags) = parse("fn f36() print(36) end", 0);
    assert!(!diags.has_errors(), "parse_fn_36: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_37() {
    let (module, diags) = parse("fn f37() print(37) end", 0);
    assert!(!diags.has_errors(), "parse_fn_37: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_38() {
    let (module, diags) = parse("fn f38() print(38) end", 0);
    assert!(!diags.has_errors(), "parse_fn_38: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_39() {
    let (module, diags) = parse("fn f39() print(39) end", 0);
    assert!(!diags.has_errors(), "parse_fn_39: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_40() {
    let (module, diags) = parse("fn f40() print(40) end", 0);
    assert!(!diags.has_errors(), "parse_fn_40: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_41() {
    let (module, diags) = parse("fn f41() print(41) end", 0);
    assert!(!diags.has_errors(), "parse_fn_41: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_42() {
    let (module, diags) = parse("fn f42() print(42) end", 0);
    assert!(!diags.has_errors(), "parse_fn_42: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_43() {
    let (module, diags) = parse("fn f43() print(43) end", 0);
    assert!(!diags.has_errors(), "parse_fn_43: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_44() {
    let (module, diags) = parse("fn f44() print(44) end", 0);
    assert!(!diags.has_errors(), "parse_fn_44: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_45() {
    let (module, diags) = parse("fn f45() print(45) end", 0);
    assert!(!diags.has_errors(), "parse_fn_45: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_46() {
    let (module, diags) = parse("fn f46() print(46) end", 0);
    assert!(!diags.has_errors(), "parse_fn_46: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_47() {
    let (module, diags) = parse("fn f47() print(47) end", 0);
    assert!(!diags.has_errors(), "parse_fn_47: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_48() {
    let (module, diags) = parse("fn f48() print(48) end", 0);
    assert!(!diags.has_errors(), "parse_fn_48: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_fn_49() {
    let (module, diags) = parse("fn f49() print(49) end", 0);
    assert!(!diags.has_errors(), "parse_fn_49: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_0() {
    let (module, diags) = parse("fn main() let x = 0 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_1() {
    let (module, diags) = parse("fn main() let x = 1 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_2() {
    let (module, diags) = parse("fn main() let x = 2 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_3() {
    let (module, diags) = parse("fn main() let x = 3 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_4() {
    let (module, diags) = parse("fn main() let x = 4 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_5() {
    let (module, diags) = parse("fn main() let x = 5 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_6() {
    let (module, diags) = parse("fn main() let x = 6 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_7() {
    let (module, diags) = parse("fn main() let x = 7 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_8() {
    let (module, diags) = parse("fn main() let x = 8 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_9() {
    let (module, diags) = parse("fn main() let x = 9 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_10() {
    let (module, diags) = parse("fn main() let x = 10 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_11() {
    let (module, diags) = parse("fn main() let x = 11 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_12() {
    let (module, diags) = parse("fn main() let x = 12 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_13() {
    let (module, diags) = parse("fn main() let x = 13 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_14() {
    let (module, diags) = parse("fn main() let x = 14 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_15() {
    let (module, diags) = parse("fn main() let x = 15 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_16() {
    let (module, diags) = parse("fn main() let x = 16 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_17() {
    let (module, diags) = parse("fn main() let x = 17 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_18() {
    let (module, diags) = parse("fn main() let x = 18 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_19() {
    let (module, diags) = parse("fn main() let x = 19 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_20() {
    let (module, diags) = parse("fn main() let x = 20 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_20: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_21() {
    let (module, diags) = parse("fn main() let x = 21 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_21: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_22() {
    let (module, diags) = parse("fn main() let x = 22 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_22: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_23() {
    let (module, diags) = parse("fn main() let x = 23 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_23: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_24() {
    let (module, diags) = parse("fn main() let x = 24 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_24: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_25() {
    let (module, diags) = parse("fn main() let x = 25 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_25: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_26() {
    let (module, diags) = parse("fn main() let x = 26 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_26: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_27() {
    let (module, diags) = parse("fn main() let x = 27 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_27: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_28() {
    let (module, diags) = parse("fn main() let x = 28 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_28: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_let_29() {
    let (module, diags) = parse("fn main() let x = 29 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_let_29: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_0() {
    let (module, diags) = parse("fn main() var x = 0 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_1() {
    let (module, diags) = parse("fn main() var x = 1 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_2() {
    let (module, diags) = parse("fn main() var x = 2 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_3() {
    let (module, diags) = parse("fn main() var x = 3 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_4() {
    let (module, diags) = parse("fn main() var x = 4 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_5() {
    let (module, diags) = parse("fn main() var x = 5 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_6() {
    let (module, diags) = parse("fn main() var x = 6 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_7() {
    let (module, diags) = parse("fn main() var x = 7 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_8() {
    let (module, diags) = parse("fn main() var x = 8 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_9() {
    let (module, diags) = parse("fn main() var x = 9 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_10() {
    let (module, diags) = parse("fn main() var x = 10 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_11() {
    let (module, diags) = parse("fn main() var x = 11 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_12() {
    let (module, diags) = parse("fn main() var x = 12 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_13() {
    let (module, diags) = parse("fn main() var x = 13 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_14() {
    let (module, diags) = parse("fn main() var x = 14 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_15() {
    let (module, diags) = parse("fn main() var x = 15 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_16() {
    let (module, diags) = parse("fn main() var x = 16 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_17() {
    let (module, diags) = parse("fn main() var x = 17 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_18() {
    let (module, diags) = parse("fn main() var x = 18 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_19() {
    let (module, diags) = parse("fn main() var x = 19 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_20() {
    let (module, diags) = parse("fn main() var x = 20 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_20: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_21() {
    let (module, diags) = parse("fn main() var x = 21 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_21: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_22() {
    let (module, diags) = parse("fn main() var x = 22 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_22: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_23() {
    let (module, diags) = parse("fn main() var x = 23 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_23: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_24() {
    let (module, diags) = parse("fn main() var x = 24 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_24: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_25() {
    let (module, diags) = parse("fn main() var x = 25 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_25: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_26() {
    let (module, diags) = parse("fn main() var x = 26 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_26: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_27() {
    let (module, diags) = parse("fn main() var x = 27 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_27: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_28() {
    let (module, diags) = parse("fn main() var x = 28 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_28: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_var_29() {
    let (module, diags) = parse("fn main() var x = 29 print(x) end", 0);
    assert!(!diags.has_errors(), "parse_var_29: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_0() {
    let (module, diags) = parse(r#"fn main() if 0 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_1() {
    let (module, diags) = parse(r#"fn main() if 1 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_2() {
    let (module, diags) = parse(r#"fn main() if 2 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_3() {
    let (module, diags) = parse(r#"fn main() if 3 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_4() {
    let (module, diags) = parse(r#"fn main() if 4 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_5() {
    let (module, diags) = parse(r#"fn main() if 5 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_6() {
    let (module, diags) = parse(r#"fn main() if 6 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_7() {
    let (module, diags) = parse(r#"fn main() if 7 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_8() {
    let (module, diags) = parse(r#"fn main() if 8 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_9() {
    let (module, diags) = parse(r#"fn main() if 9 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_10() {
    let (module, diags) = parse(r#"fn main() if 10 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_11() {
    let (module, diags) = parse(r#"fn main() if 11 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_12() {
    let (module, diags) = parse(r#"fn main() if 12 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_13() {
    let (module, diags) = parse(r#"fn main() if 13 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_14() {
    let (module, diags) = parse(r#"fn main() if 14 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_15() {
    let (module, diags) = parse(r#"fn main() if 15 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_16() {
    let (module, diags) = parse(r#"fn main() if 16 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_17() {
    let (module, diags) = parse(r#"fn main() if 17 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_18() {
    let (module, diags) = parse(r#"fn main() if 18 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_19() {
    let (module, diags) = parse(r#"fn main() if 19 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_20() {
    let (module, diags) = parse(r#"fn main() if 20 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_20: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_21() {
    let (module, diags) = parse(r#"fn main() if 21 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_21: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_22() {
    let (module, diags) = parse(r#"fn main() if 22 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_22: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_23() {
    let (module, diags) = parse(r#"fn main() if 23 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_23: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_24() {
    let (module, diags) = parse(r#"fn main() if 24 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_24: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_25() {
    let (module, diags) = parse(r#"fn main() if 25 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_25: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_26() {
    let (module, diags) = parse(r#"fn main() if 26 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_26: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_27() {
    let (module, diags) = parse(r#"fn main() if 27 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_27: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_28() {
    let (module, diags) = parse(r#"fn main() if 28 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_28: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_if_29() {
    let (module, diags) = parse(r#"fn main() if 29 > 0 then print("yes") end end"#, 0);
    assert!(!diags.has_errors(), "parse_if_29: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_0() {
    let (module, diags) = parse(r#"fn main() while 0 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_1() {
    let (module, diags) = parse(r#"fn main() while 1 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_2() {
    let (module, diags) = parse(r#"fn main() while 2 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_3() {
    let (module, diags) = parse(r#"fn main() while 3 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_4() {
    let (module, diags) = parse(r#"fn main() while 4 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_5() {
    let (module, diags) = parse(r#"fn main() while 5 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_6() {
    let (module, diags) = parse(r#"fn main() while 6 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_7() {
    let (module, diags) = parse(r#"fn main() while 7 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_8() {
    let (module, diags) = parse(r#"fn main() while 8 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_9() {
    let (module, diags) = parse(r#"fn main() while 9 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_10() {
    let (module, diags) = parse(r#"fn main() while 10 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_11() {
    let (module, diags) = parse(r#"fn main() while 11 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_12() {
    let (module, diags) = parse(r#"fn main() while 12 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_13() {
    let (module, diags) = parse(r#"fn main() while 13 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_14() {
    let (module, diags) = parse(r#"fn main() while 14 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_15() {
    let (module, diags) = parse(r#"fn main() while 15 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_16() {
    let (module, diags) = parse(r#"fn main() while 16 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_17() {
    let (module, diags) = parse(r#"fn main() while 17 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_18() {
    let (module, diags) = parse(r#"fn main() while 18 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_19() {
    let (module, diags) = parse(r#"fn main() while 19 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_20() {
    let (module, diags) = parse(r#"fn main() while 20 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_20: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_21() {
    let (module, diags) = parse(r#"fn main() while 21 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_21: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_22() {
    let (module, diags) = parse(r#"fn main() while 22 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_22: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_23() {
    let (module, diags) = parse(r#"fn main() while 23 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_23: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_24() {
    let (module, diags) = parse(r#"fn main() while 24 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_24: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_25() {
    let (module, diags) = parse(r#"fn main() while 25 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_25: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_26() {
    let (module, diags) = parse(r#"fn main() while 26 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_26: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_27() {
    let (module, diags) = parse(r#"fn main() while 27 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_27: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_28() {
    let (module, diags) = parse(r#"fn main() while 28 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_28: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_while_29() {
    let (module, diags) = parse(r#"fn main() while 29 > 0 do print("loop") end end"#, 0);
    assert!(!diags.has_errors(), "parse_while_29: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_0() {
    let (module, diags) = parse("S0 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_1() {
    let (module, diags) = parse("S1 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_2() {
    let (module, diags) = parse("S2 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_3() {
    let (module, diags) = parse("S3 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_4() {
    let (module, diags) = parse("S4 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_5() {
    let (module, diags) = parse("S5 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_6() {
    let (module, diags) = parse("S6 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_7() {
    let (module, diags) = parse("S7 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_8() {
    let (module, diags) = parse("S8 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_9() {
    let (module, diags) = parse("S9 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_10() {
    let (module, diags) = parse("S10 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_11() {
    let (module, diags) = parse("S11 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_12() {
    let (module, diags) = parse("S12 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_13() {
    let (module, diags) = parse("S13 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_14() {
    let (module, diags) = parse("S14 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_15() {
    let (module, diags) = parse("S15 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_16() {
    let (module, diags) = parse("S16 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_17() {
    let (module, diags) = parse("S17 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_18() {
    let (module, diags) = parse("S18 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_struct_19() {
    let (module, diags) = parse("S19 = struct end", 0);
    assert!(!diags.has_errors(), "parse_struct_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_0() {
    let (module, diags) = parse("E0 = enum A0 B0 end", 0);
    assert!(!diags.has_errors(), "parse_enum_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_1() {
    let (module, diags) = parse("E1 = enum A1 B1 end", 0);
    assert!(!diags.has_errors(), "parse_enum_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_2() {
    let (module, diags) = parse("E2 = enum A2 B2 end", 0);
    assert!(!diags.has_errors(), "parse_enum_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_3() {
    let (module, diags) = parse("E3 = enum A3 B3 end", 0);
    assert!(!diags.has_errors(), "parse_enum_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_4() {
    let (module, diags) = parse("E4 = enum A4 B4 end", 0);
    assert!(!diags.has_errors(), "parse_enum_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_5() {
    let (module, diags) = parse("E5 = enum A5 B5 end", 0);
    assert!(!diags.has_errors(), "parse_enum_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_6() {
    let (module, diags) = parse("E6 = enum A6 B6 end", 0);
    assert!(!diags.has_errors(), "parse_enum_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_7() {
    let (module, diags) = parse("E7 = enum A7 B7 end", 0);
    assert!(!diags.has_errors(), "parse_enum_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_8() {
    let (module, diags) = parse("E8 = enum A8 B8 end", 0);
    assert!(!diags.has_errors(), "parse_enum_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_9() {
    let (module, diags) = parse("E9 = enum A9 B9 end", 0);
    assert!(!diags.has_errors(), "parse_enum_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_10() {
    let (module, diags) = parse("E10 = enum A10 B10 end", 0);
    assert!(!diags.has_errors(), "parse_enum_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_11() {
    let (module, diags) = parse("E11 = enum A11 B11 end", 0);
    assert!(!diags.has_errors(), "parse_enum_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_12() {
    let (module, diags) = parse("E12 = enum A12 B12 end", 0);
    assert!(!diags.has_errors(), "parse_enum_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_13() {
    let (module, diags) = parse("E13 = enum A13 B13 end", 0);
    assert!(!diags.has_errors(), "parse_enum_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_14() {
    let (module, diags) = parse("E14 = enum A14 B14 end", 0);
    assert!(!diags.has_errors(), "parse_enum_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_15() {
    let (module, diags) = parse("E15 = enum A15 B15 end", 0);
    assert!(!diags.has_errors(), "parse_enum_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_16() {
    let (module, diags) = parse("E16 = enum A16 B16 end", 0);
    assert!(!diags.has_errors(), "parse_enum_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_17() {
    let (module, diags) = parse("E17 = enum A17 B17 end", 0);
    assert!(!diags.has_errors(), "parse_enum_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_18() {
    let (module, diags) = parse("E18 = enum A18 B18 end", 0);
    assert!(!diags.has_errors(), "parse_enum_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_enum_19() {
    let (module, diags) = parse("E19 = enum A19 B19 end", 0);
    assert!(!diags.has_errors(), "parse_enum_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_0() {
    let (module, diags) = parse("Ifc0 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_1() {
    let (module, diags) = parse("Ifc1 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_2() {
    let (module, diags) = parse("Ifc2 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_3() {
    let (module, diags) = parse("Ifc3 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_4() {
    let (module, diags) = parse("Ifc4 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_5() {
    let (module, diags) = parse("Ifc5 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_6() {
    let (module, diags) = parse("Ifc6 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_7() {
    let (module, diags) = parse("Ifc7 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_8() {
    let (module, diags) = parse("Ifc8 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_9() {
    let (module, diags) = parse("Ifc9 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_10() {
    let (module, diags) = parse("Ifc10 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_11() {
    let (module, diags) = parse("Ifc11 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_12() {
    let (module, diags) = parse("Ifc12 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_13() {
    let (module, diags) = parse("Ifc13 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_14() {
    let (module, diags) = parse("Ifc14 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_15() {
    let (module, diags) = parse("Ifc15 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_16() {
    let (module, diags) = parse("Ifc16 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_17() {
    let (module, diags) = parse("Ifc17 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_18() {
    let (module, diags) = parse("Ifc18 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_interface_19() {
    let (module, diags) = parse("Ifc19 = interface end", 0);
    assert!(!diags.has_errors(), "parse_interface_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_0() {
    let (module, diags) = parse("const C0 = 0", 0);
    assert!(!diags.has_errors(), "parse_const_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_1() {
    let (module, diags) = parse("const C1 = 1", 0);
    assert!(!diags.has_errors(), "parse_const_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_2() {
    let (module, diags) = parse("const C2 = 2", 0);
    assert!(!diags.has_errors(), "parse_const_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_3() {
    let (module, diags) = parse("const C3 = 3", 0);
    assert!(!diags.has_errors(), "parse_const_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_4() {
    let (module, diags) = parse("const C4 = 4", 0);
    assert!(!diags.has_errors(), "parse_const_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_5() {
    let (module, diags) = parse("const C5 = 5", 0);
    assert!(!diags.has_errors(), "parse_const_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_6() {
    let (module, diags) = parse("const C6 = 6", 0);
    assert!(!diags.has_errors(), "parse_const_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_7() {
    let (module, diags) = parse("const C7 = 7", 0);
    assert!(!diags.has_errors(), "parse_const_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_8() {
    let (module, diags) = parse("const C8 = 8", 0);
    assert!(!diags.has_errors(), "parse_const_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_9() {
    let (module, diags) = parse("const C9 = 9", 0);
    assert!(!diags.has_errors(), "parse_const_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_10() {
    let (module, diags) = parse("const C10 = 10", 0);
    assert!(!diags.has_errors(), "parse_const_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_11() {
    let (module, diags) = parse("const C11 = 11", 0);
    assert!(!diags.has_errors(), "parse_const_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_12() {
    let (module, diags) = parse("const C12 = 12", 0);
    assert!(!diags.has_errors(), "parse_const_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_13() {
    let (module, diags) = parse("const C13 = 13", 0);
    assert!(!diags.has_errors(), "parse_const_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_14() {
    let (module, diags) = parse("const C14 = 14", 0);
    assert!(!diags.has_errors(), "parse_const_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_15() {
    let (module, diags) = parse("const C15 = 15", 0);
    assert!(!diags.has_errors(), "parse_const_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_16() {
    let (module, diags) = parse("const C16 = 16", 0);
    assert!(!diags.has_errors(), "parse_const_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_17() {
    let (module, diags) = parse("const C17 = 17", 0);
    assert!(!diags.has_errors(), "parse_const_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_18() {
    let (module, diags) = parse("const C18 = 18", 0);
    assert!(!diags.has_errors(), "parse_const_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_const_19() {
    let (module, diags) = parse("const C19 = 19", 0);
    assert!(!diags.has_errors(), "parse_const_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_0() {
    let (module, diags) = parse(r#"use "pkg0""#, 0);
    assert!(!diags.has_errors(), "parse_use_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_1() {
    let (module, diags) = parse(r#"use "pkg1""#, 0);
    assert!(!diags.has_errors(), "parse_use_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_2() {
    let (module, diags) = parse(r#"use "pkg2""#, 0);
    assert!(!diags.has_errors(), "parse_use_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_3() {
    let (module, diags) = parse(r#"use "pkg3""#, 0);
    assert!(!diags.has_errors(), "parse_use_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_4() {
    let (module, diags) = parse(r#"use "pkg4""#, 0);
    assert!(!diags.has_errors(), "parse_use_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_5() {
    let (module, diags) = parse(r#"use "pkg5""#, 0);
    assert!(!diags.has_errors(), "parse_use_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_6() {
    let (module, diags) = parse(r#"use "pkg6""#, 0);
    assert!(!diags.has_errors(), "parse_use_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_7() {
    let (module, diags) = parse(r#"use "pkg7""#, 0);
    assert!(!diags.has_errors(), "parse_use_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_8() {
    let (module, diags) = parse(r#"use "pkg8""#, 0);
    assert!(!diags.has_errors(), "parse_use_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_9() {
    let (module, diags) = parse(r#"use "pkg9""#, 0);
    assert!(!diags.has_errors(), "parse_use_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_10() {
    let (module, diags) = parse(r#"use "pkg10""#, 0);
    assert!(!diags.has_errors(), "parse_use_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_11() {
    let (module, diags) = parse(r#"use "pkg11""#, 0);
    assert!(!diags.has_errors(), "parse_use_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_12() {
    let (module, diags) = parse(r#"use "pkg12""#, 0);
    assert!(!diags.has_errors(), "parse_use_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_13() {
    let (module, diags) = parse(r#"use "pkg13""#, 0);
    assert!(!diags.has_errors(), "parse_use_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_14() {
    let (module, diags) = parse(r#"use "pkg14""#, 0);
    assert!(!diags.has_errors(), "parse_use_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_15() {
    let (module, diags) = parse(r#"use "pkg15""#, 0);
    assert!(!diags.has_errors(), "parse_use_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_16() {
    let (module, diags) = parse(r#"use "pkg16""#, 0);
    assert!(!diags.has_errors(), "parse_use_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_17() {
    let (module, diags) = parse(r#"use "pkg17""#, 0);
    assert!(!diags.has_errors(), "parse_use_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_18() {
    let (module, diags) = parse(r#"use "pkg18""#, 0);
    assert!(!diags.has_errors(), "parse_use_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_use_19() {
    let (module, diags) = parse(r#"use "pkg19""#, 0);
    assert!(!diags.has_errors(), "parse_use_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_0() {
    let (module, diags) = parse(r#"fn main() match 0 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_0: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_1() {
    let (module, diags) = parse(r#"fn main() match 1 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_1: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_2() {
    let (module, diags) = parse(r#"fn main() match 2 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_2: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_3() {
    let (module, diags) = parse(r#"fn main() match 3 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_3: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_4() {
    let (module, diags) = parse(r#"fn main() match 4 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_4: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_5() {
    let (module, diags) = parse(r#"fn main() match 5 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_5: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_6() {
    let (module, diags) = parse(r#"fn main() match 6 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_6: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_7() {
    let (module, diags) = parse(r#"fn main() match 7 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_7: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_8() {
    let (module, diags) = parse(r#"fn main() match 8 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_8: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_9() {
    let (module, diags) = parse(r#"fn main() match 9 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_9: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_10() {
    let (module, diags) = parse(r#"fn main() match 10 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_10: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_11() {
    let (module, diags) = parse(r#"fn main() match 11 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_11: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_12() {
    let (module, diags) = parse(r#"fn main() match 12 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_12: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_13() {
    let (module, diags) = parse(r#"fn main() match 13 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_13: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_14() {
    let (module, diags) = parse(r#"fn main() match 14 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_14: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_15() {
    let (module, diags) = parse(r#"fn main() match 15 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_15: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_16() {
    let (module, diags) = parse(r#"fn main() match 16 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_16: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_17() {
    let (module, diags) = parse(r#"fn main() match 17 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_17: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_18() {
    let (module, diags) = parse(r#"fn main() match 18 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_18: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

#[test]
fn parse_match_19() {
    let (module, diags) = parse(r#"fn main() match 19 0 => print("z") _ => print("o") end end"#, 0);
    assert!(!diags.has_errors(), "parse_match_19: expected no errors, got: {:?}", diags.items);
    assert!(module.items.len() > 0);
}

