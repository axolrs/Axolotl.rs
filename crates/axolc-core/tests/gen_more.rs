// Owner: PascalElixir / axolrs (GitHub org)
// File: Auto-generated additional tests for axolc-core language features.

#[test]
fn number_lit_0() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("0", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "0");
}
#[test]
fn number_lit_1() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("1", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "1");
}
#[test]
fn number_lit_2() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("2", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "2");
}
#[test]
fn number_lit_3() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("3", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "3");
}
#[test]
fn number_lit_4() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("4", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "4");
}
#[test]
fn number_lit_5() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("5", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "5");
}
#[test]
fn number_lit_6() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("6", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "6");
}
#[test]
fn number_lit_7() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("7", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "7");
}
#[test]
fn number_lit_8() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("8", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "8");
}
#[test]
fn number_lit_9() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("9", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "9");
}
#[test]
fn number_lit_10() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("10", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "10");
}
#[test]
fn number_lit_11() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("11", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "11");
}
#[test]
fn number_lit_12() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("12", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "12");
}
#[test]
fn number_lit_13() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("13", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "13");
}
#[test]
fn number_lit_14() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("14", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "14");
}
#[test]
fn number_lit_15() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("15", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "15");
}
#[test]
fn number_lit_16() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("16", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "16");
}
#[test]
fn number_lit_17() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("17", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "17");
}
#[test]
fn number_lit_18() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("18", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "18");
}
#[test]
fn number_lit_19() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("19", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "19");
}
#[test]
fn number_lit_20() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("20", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "20");
}
#[test]
fn number_lit_21() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("21", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "21");
}
#[test]
fn number_lit_22() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("22", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "22");
}
#[test]
fn number_lit_23() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("23", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "23");
}
#[test]
fn number_lit_24() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("24", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "24");
}
#[test]
fn number_lit_25() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("25", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "25");
}
#[test]
fn number_lit_26() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("26", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "26");
}
#[test]
fn number_lit_27() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("27", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "27");
}
#[test]
fn number_lit_28() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("28", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "28");
}
#[test]
fn number_lit_29() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("29", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "29");
}
#[test]
fn number_lit_30() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("30", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "30");
}
#[test]
fn number_lit_31() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("31", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "31");
}
#[test]
fn number_lit_32() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("32", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "32");
}
#[test]
fn number_lit_33() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("33", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "33");
}
#[test]
fn number_lit_34() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("34", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "34");
}
#[test]
fn number_lit_35() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("35", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "35");
}
#[test]
fn number_lit_36() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("36", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "36");
}
#[test]
fn number_lit_37() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("37", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "37");
}
#[test]
fn number_lit_38() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("38", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "38");
}
#[test]
fn number_lit_39() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("39", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "39");
}
#[test]
fn number_lit_40() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("40", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "40");
}
#[test]
fn number_lit_41() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("41", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "41");
}
#[test]
fn number_lit_42() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("42", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "42");
}
#[test]
fn number_lit_43() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("43", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "43");
}
#[test]
fn number_lit_44() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("44", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "44");
}
#[test]
fn number_lit_45() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("45", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "45");
}
#[test]
fn number_lit_46() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("46", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "46");
}
#[test]
fn number_lit_47() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("47", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "47");
}
#[test]
fn number_lit_48() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("48", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "48");
}
#[test]
fn number_lit_49() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("49", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "49");
}
#[test]
fn number_lit_50() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("50", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "50");
}
#[test]
fn number_lit_51() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("51", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "51");
}
#[test]
fn number_lit_52() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("52", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "52");
}
#[test]
fn number_lit_53() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("53", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "53");
}
#[test]
fn number_lit_54() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("54", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "54");
}
#[test]
fn number_lit_55() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("55", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "55");
}
#[test]
fn number_lit_56() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("56", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "56");
}
#[test]
fn number_lit_57() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("57", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "57");
}
#[test]
fn number_lit_58() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("58", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "58");
}
#[test]
fn number_lit_59() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("59", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "59");
}
#[test]
fn number_lit_60() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("60", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "60");
}
#[test]
fn number_lit_61() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("61", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "61");
}
#[test]
fn number_lit_62() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("62", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "62");
}
#[test]
fn number_lit_63() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("63", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "63");
}
#[test]
fn number_lit_64() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("64", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "64");
}
#[test]
fn number_lit_65() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("65", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "65");
}
#[test]
fn number_lit_66() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("66", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "66");
}
#[test]
fn number_lit_67() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("67", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "67");
}
#[test]
fn number_lit_68() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("68", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "68");
}
#[test]
fn number_lit_69() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("69", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "69");
}
#[test]
fn number_lit_70() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("70", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "70");
}
#[test]
fn number_lit_71() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("71", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "71");
}
#[test]
fn number_lit_72() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("72", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "72");
}
#[test]
fn number_lit_73() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("73", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "73");
}
#[test]
fn number_lit_74() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("74", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "74");
}
#[test]
fn number_lit_75() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("75", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "75");
}
#[test]
fn number_lit_76() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("76", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "76");
}
#[test]
fn number_lit_77() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("77", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "77");
}
#[test]
fn number_lit_78() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("78", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "78");
}
#[test]
fn number_lit_79() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("79", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "79");
}
#[test]
fn number_lit_80() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("80", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "80");
}
#[test]
fn number_lit_81() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("81", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "81");
}
#[test]
fn number_lit_82() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("82", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "82");
}
#[test]
fn number_lit_83() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("83", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "83");
}
#[test]
fn number_lit_84() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("84", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "84");
}
#[test]
fn number_lit_85() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("85", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "85");
}
#[test]
fn number_lit_86() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("86", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "86");
}
#[test]
fn number_lit_87() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("87", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "87");
}
#[test]
fn number_lit_88() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("88", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "88");
}
#[test]
fn number_lit_89() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("89", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "89");
}
#[test]
fn number_lit_90() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("90", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "90");
}
#[test]
fn number_lit_91() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("91", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "91");
}
#[test]
fn number_lit_92() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("92", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "92");
}
#[test]
fn number_lit_93() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("93", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "93");
}
#[test]
fn number_lit_94() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("94", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "94");
}
#[test]
fn number_lit_95() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("95", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "95");
}
#[test]
fn number_lit_96() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("96", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "96");
}
#[test]
fn number_lit_97() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("97", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "97");
}
#[test]
fn number_lit_98() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("98", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "98");
}
#[test]
fn number_lit_99() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("99", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "99");
}
#[test]
fn number_lit_100() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("100", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "100");
}
#[test]
fn number_lit_101() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("101", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "101");
}
#[test]
fn number_lit_102() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("102", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "102");
}
#[test]
fn number_lit_103() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("103", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "103");
}
#[test]
fn number_lit_104() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("104", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "104");
}
#[test]
fn number_lit_105() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("105", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "105");
}
#[test]
fn number_lit_106() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("106", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "106");
}
#[test]
fn number_lit_107() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("107", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "107");
}
#[test]
fn number_lit_108() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("108", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "108");
}
#[test]
fn number_lit_109() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("109", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "109");
}
#[test]
fn number_lit_110() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("110", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "110");
}
#[test]
fn number_lit_111() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("111", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "111");
}
#[test]
fn number_lit_112() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("112", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "112");
}
#[test]
fn number_lit_113() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("113", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "113");
}
#[test]
fn number_lit_114() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("114", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "114");
}
#[test]
fn number_lit_115() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("115", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "115");
}
#[test]
fn number_lit_116() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("116", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "116");
}
#[test]
fn number_lit_117() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("117", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "117");
}
#[test]
fn number_lit_118() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("118", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "118");
}
#[test]
fn number_lit_119() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("119", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "119");
}
#[test]
fn number_lit_120() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("120", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "120");
}
#[test]
fn number_lit_121() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("121", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "121");
}
#[test]
fn number_lit_122() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("122", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "122");
}
#[test]
fn number_lit_123() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("123", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "123");
}
#[test]
fn number_lit_124() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("124", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "124");
}
#[test]
fn number_lit_125() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("125", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "125");
}
#[test]
fn number_lit_126() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("126", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "126");
}
#[test]
fn number_lit_127() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("127", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "127");
}
#[test]
fn number_lit_128() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("128", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "128");
}
#[test]
fn number_lit_129() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("129", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "129");
}
#[test]
fn number_lit_130() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("130", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "130");
}
#[test]
fn number_lit_131() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("131", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "131");
}
#[test]
fn number_lit_132() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("132", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "132");
}
#[test]
fn number_lit_133() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("133", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "133");
}
#[test]
fn number_lit_134() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("134", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "134");
}
#[test]
fn number_lit_135() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("135", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "135");
}
#[test]
fn number_lit_136() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("136", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "136");
}
#[test]
fn number_lit_137() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("137", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "137");
}
#[test]
fn number_lit_138() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("138", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "138");
}
#[test]
fn number_lit_139() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("139", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "139");
}
#[test]
fn number_lit_140() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("140", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "140");
}
#[test]
fn number_lit_141() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("141", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "141");
}
#[test]
fn number_lit_142() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("142", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "142");
}
#[test]
fn number_lit_143() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("143", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "143");
}
#[test]
fn number_lit_144() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("144", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "144");
}
#[test]
fn number_lit_145() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("145", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "145");
}
#[test]
fn number_lit_146() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("146", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "146");
}
#[test]
fn number_lit_147() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("147", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "147");
}
#[test]
fn number_lit_148() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("148", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "148");
}
#[test]
fn number_lit_149() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("149", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "149");
}
#[test]
fn number_lit_150() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("150", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "150");
}
#[test]
fn number_lit_151() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("151", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "151");
}
#[test]
fn number_lit_152() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("152", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "152");
}
#[test]
fn number_lit_153() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("153", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "153");
}
#[test]
fn number_lit_154() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("154", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "154");
}
#[test]
fn number_lit_155() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("155", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "155");
}
#[test]
fn number_lit_156() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("156", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "156");
}
#[test]
fn number_lit_157() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("157", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "157");
}
#[test]
fn number_lit_158() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("158", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "158");
}
#[test]
fn number_lit_159() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("159", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "159");
}
#[test]
fn number_lit_160() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("160", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "160");
}
#[test]
fn number_lit_161() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("161", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "161");
}
#[test]
fn number_lit_162() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("162", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "162");
}
#[test]
fn number_lit_163() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("163", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "163");
}
#[test]
fn number_lit_164() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("164", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "164");
}
#[test]
fn number_lit_165() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("165", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "165");
}
#[test]
fn number_lit_166() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("166", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "166");
}
#[test]
fn number_lit_167() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("167", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "167");
}
#[test]
fn number_lit_168() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("168", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "168");
}
#[test]
fn number_lit_169() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("169", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "169");
}
#[test]
fn number_lit_170() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("170", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "170");
}
#[test]
fn number_lit_171() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("171", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "171");
}
#[test]
fn number_lit_172() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("172", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "172");
}
#[test]
fn number_lit_173() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("173", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "173");
}
#[test]
fn number_lit_174() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("174", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "174");
}
#[test]
fn number_lit_175() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("175", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "175");
}
#[test]
fn number_lit_176() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("176", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "176");
}
#[test]
fn number_lit_177() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("177", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "177");
}
#[test]
fn number_lit_178() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("178", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "178");
}
#[test]
fn number_lit_179() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("179", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "179");
}
#[test]
fn number_lit_180() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("180", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "180");
}
#[test]
fn number_lit_181() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("181", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "181");
}
#[test]
fn number_lit_182() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("182", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "182");
}
#[test]
fn number_lit_183() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("183", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "183");
}
#[test]
fn number_lit_184() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("184", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "184");
}
#[test]
fn number_lit_185() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("185", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "185");
}
#[test]
fn number_lit_186() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("186", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "186");
}
#[test]
fn number_lit_187() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("187", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "187");
}
#[test]
fn number_lit_188() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("188", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "188");
}
#[test]
fn number_lit_189() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("189", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "189");
}
#[test]
fn number_lit_190() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("190", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "190");
}
#[test]
fn number_lit_191() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("191", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "191");
}
#[test]
fn number_lit_192() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("192", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "192");
}
#[test]
fn number_lit_193() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("193", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "193");
}
#[test]
fn number_lit_194() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("194", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "194");
}
#[test]
fn number_lit_195() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("195", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "195");
}
#[test]
fn number_lit_196() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("196", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "196");
}
#[test]
fn number_lit_197() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("197", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "197");
}
#[test]
fn number_lit_198() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("198", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "198");
}
#[test]
fn number_lit_199() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("199", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "199");
}
#[test]
fn number_lit_200() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("200", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "200");
}
#[test]
fn number_lit_201() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("201", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "201");
}
#[test]
fn number_lit_202() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("202", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "202");
}
#[test]
fn number_lit_203() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("203", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "203");
}
#[test]
fn number_lit_204() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("204", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "204");
}
#[test]
fn number_lit_205() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("205", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "205");
}
#[test]
fn number_lit_206() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("206", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "206");
}
#[test]
fn number_lit_207() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("207", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "207");
}
#[test]
fn number_lit_208() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("208", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "208");
}
#[test]
fn number_lit_209() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("209", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "209");
}
#[test]
fn number_lit_210() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("210", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "210");
}
#[test]
fn number_lit_211() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("211", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "211");
}
#[test]
fn number_lit_212() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("212", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "212");
}
#[test]
fn number_lit_213() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("213", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "213");
}
#[test]
fn number_lit_214() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("214", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "214");
}
#[test]
fn number_lit_215() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("215", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "215");
}
#[test]
fn number_lit_216() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("216", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "216");
}
#[test]
fn number_lit_217() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("217", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "217");
}
#[test]
fn number_lit_218() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("218", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "218");
}
#[test]
fn number_lit_219() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("219", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "219");
}
#[test]
fn number_lit_220() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("220", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "220");
}
#[test]
fn number_lit_221() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("221", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "221");
}
#[test]
fn number_lit_222() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("222", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "222");
}
#[test]
fn number_lit_223() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("223", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "223");
}
#[test]
fn number_lit_224() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("224", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "224");
}
#[test]
fn number_lit_225() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("225", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "225");
}
#[test]
fn number_lit_226() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("226", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "226");
}
#[test]
fn number_lit_227() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("227", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "227");
}
#[test]
fn number_lit_228() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("228", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "228");
}
#[test]
fn number_lit_229() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("229", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "229");
}
#[test]
fn number_lit_230() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("230", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "230");
}
#[test]
fn number_lit_231() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("231", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "231");
}
#[test]
fn number_lit_232() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("232", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "232");
}
#[test]
fn number_lit_233() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("233", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "233");
}
#[test]
fn number_lit_234() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("234", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "234");
}
#[test]
fn number_lit_235() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("235", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "235");
}
#[test]
fn number_lit_236() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("236", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "236");
}
#[test]
fn number_lit_237() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("237", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "237");
}
#[test]
fn number_lit_238() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("238", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "238");
}
#[test]
fn number_lit_239() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("239", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "239");
}
#[test]
fn number_lit_240() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("240", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "240");
}
#[test]
fn number_lit_241() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("241", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "241");
}
#[test]
fn number_lit_242() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("242", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "242");
}
#[test]
fn number_lit_243() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("243", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "243");
}
#[test]
fn number_lit_244() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("244", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "244");
}
#[test]
fn number_lit_245() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("245", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "245");
}
#[test]
fn number_lit_246() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("246", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "246");
}
#[test]
fn number_lit_247() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("247", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "247");
}
#[test]
fn number_lit_248() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("248", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "248");
}
#[test]
fn number_lit_249() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("249", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "249");
}
#[test]
fn number_lit_250() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("250", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "250");
}
#[test]
fn number_lit_251() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("251", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "251");
}
#[test]
fn number_lit_252() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("252", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "252");
}
#[test]
fn number_lit_253() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("253", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "253");
}
#[test]
fn number_lit_254() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("254", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "254");
}
#[test]
fn number_lit_255() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("255", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "255");
}
#[test]
fn number_lit_256() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("256", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "256");
}
#[test]
fn number_lit_257() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("257", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "257");
}
#[test]
fn number_lit_258() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("258", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "258");
}
#[test]
fn number_lit_259() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("259", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "259");
}
#[test]
fn number_lit_260() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("260", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "260");
}
#[test]
fn number_lit_261() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("261", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "261");
}
#[test]
fn number_lit_262() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("262", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "262");
}
#[test]
fn number_lit_263() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("263", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "263");
}
#[test]
fn number_lit_264() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("264", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "264");
}
#[test]
fn number_lit_265() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("265", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "265");
}
#[test]
fn number_lit_266() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("266", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "266");
}
#[test]
fn number_lit_267() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("267", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "267");
}
#[test]
fn number_lit_268() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("268", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "268");
}
#[test]
fn number_lit_269() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("269", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "269");
}
#[test]
fn number_lit_270() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("270", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "270");
}
#[test]
fn number_lit_271() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("271", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "271");
}
#[test]
fn number_lit_272() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("272", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "272");
}
#[test]
fn number_lit_273() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("273", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "273");
}
#[test]
fn number_lit_274() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("274", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "274");
}
#[test]
fn number_lit_275() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("275", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "275");
}
#[test]
fn number_lit_276() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("276", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "276");
}
#[test]
fn number_lit_277() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("277", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "277");
}
#[test]
fn number_lit_278() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("278", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "278");
}
#[test]
fn number_lit_279() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("279", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "279");
}
#[test]
fn number_lit_280() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("280", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "280");
}
#[test]
fn number_lit_281() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("281", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "281");
}
#[test]
fn number_lit_282() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("282", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "282");
}
#[test]
fn number_lit_283() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("283", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "283");
}
#[test]
fn number_lit_284() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("284", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "284");
}
#[test]
fn number_lit_285() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("285", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "285");
}
#[test]
fn number_lit_286() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("286", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "286");
}
#[test]
fn number_lit_287() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("287", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "287");
}
#[test]
fn number_lit_288() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("288", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "288");
}
#[test]
fn number_lit_289() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("289", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "289");
}
#[test]
fn number_lit_290() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("290", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "290");
}
#[test]
fn number_lit_291() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("291", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "291");
}
#[test]
fn number_lit_292() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("292", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "292");
}
#[test]
fn number_lit_293() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("293", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "293");
}
#[test]
fn number_lit_294() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("294", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "294");
}
#[test]
fn number_lit_295() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("295", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "295");
}
#[test]
fn number_lit_296() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("296", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "296");
}
#[test]
fn number_lit_297() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("297", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "297");
}
#[test]
fn number_lit_298() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("298", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "298");
}
#[test]
fn number_lit_299() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("299", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "299");
}
#[test]
fn number_lit_300() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("300", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "300");
}
#[test]
fn number_lit_301() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("301", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "301");
}
#[test]
fn number_lit_302() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("302", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "302");
}
#[test]
fn number_lit_303() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("303", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "303");
}
#[test]
fn number_lit_304() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("304", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "304");
}
#[test]
fn number_lit_305() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("305", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "305");
}
#[test]
fn number_lit_306() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("306", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "306");
}
#[test]
fn number_lit_307() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("307", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "307");
}
#[test]
fn number_lit_308() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("308", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "308");
}
#[test]
fn number_lit_309() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("309", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "309");
}
#[test]
fn number_lit_310() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("310", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "310");
}
#[test]
fn number_lit_311() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("311", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "311");
}
#[test]
fn number_lit_312() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("312", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "312");
}
#[test]
fn number_lit_313() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("313", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "313");
}
#[test]
fn number_lit_314() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("314", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "314");
}
#[test]
fn number_lit_315() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("315", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "315");
}
#[test]
fn number_lit_316() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("316", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "316");
}
#[test]
fn number_lit_317() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("317", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "317");
}
#[test]
fn number_lit_318() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("318", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "318");
}
#[test]
fn number_lit_319() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("319", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "319");
}
#[test]
fn number_lit_320() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("320", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "320");
}
#[test]
fn number_lit_321() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("321", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "321");
}
#[test]
fn number_lit_322() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("322", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "322");
}
#[test]
fn number_lit_323() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("323", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "323");
}
#[test]
fn number_lit_324() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("324", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "324");
}
#[test]
fn number_lit_325() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("325", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "325");
}
#[test]
fn number_lit_326() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("326", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "326");
}
#[test]
fn number_lit_327() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("327", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "327");
}
#[test]
fn number_lit_328() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("328", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "328");
}
#[test]
fn number_lit_329() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("329", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "329");
}
#[test]
fn number_lit_330() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("330", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "330");
}
#[test]
fn number_lit_331() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("331", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "331");
}
#[test]
fn number_lit_332() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("332", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "332");
}
#[test]
fn number_lit_333() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("333", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "333");
}
#[test]
fn number_lit_334() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("334", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "334");
}
#[test]
fn number_lit_335() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("335", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "335");
}
#[test]
fn number_lit_336() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("336", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "336");
}
#[test]
fn number_lit_337() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("337", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "337");
}
#[test]
fn number_lit_338() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("338", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "338");
}
#[test]
fn number_lit_339() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("339", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "339");
}
#[test]
fn number_lit_340() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("340", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "340");
}
#[test]
fn number_lit_341() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("341", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "341");
}
#[test]
fn number_lit_342() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("342", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "342");
}
#[test]
fn number_lit_343() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("343", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "343");
}
#[test]
fn number_lit_344() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("344", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "344");
}
#[test]
fn number_lit_345() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("345", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "345");
}
#[test]
fn number_lit_346() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("346", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "346");
}
#[test]
fn number_lit_347() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("347", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "347");
}
#[test]
fn number_lit_348() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("348", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "348");
}
#[test]
fn number_lit_349() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("349", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "349");
}
#[test]
fn number_lit_350() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("350", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "350");
}
#[test]
fn number_lit_351() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("351", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "351");
}
#[test]
fn number_lit_352() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("352", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "352");
}
#[test]
fn number_lit_353() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("353", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "353");
}
#[test]
fn number_lit_354() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("354", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "354");
}
#[test]
fn number_lit_355() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("355", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "355");
}
#[test]
fn number_lit_356() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("356", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "356");
}
#[test]
fn number_lit_357() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("357", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "357");
}
#[test]
fn number_lit_358() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("358", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "358");
}
#[test]
fn number_lit_359() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("359", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "359");
}
#[test]
fn number_lit_360() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("360", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "360");
}
#[test]
fn number_lit_361() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("361", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "361");
}
#[test]
fn number_lit_362() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("362", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "362");
}
#[test]
fn number_lit_363() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("363", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "363");
}
#[test]
fn number_lit_364() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("364", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "364");
}
#[test]
fn number_lit_365() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("365", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "365");
}
#[test]
fn number_lit_366() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("366", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "366");
}
#[test]
fn number_lit_367() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("367", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "367");
}
#[test]
fn number_lit_368() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("368", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "368");
}
#[test]
fn number_lit_369() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("369", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "369");
}
#[test]
fn number_lit_370() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("370", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "370");
}
#[test]
fn number_lit_371() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("371", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "371");
}
#[test]
fn number_lit_372() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("372", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "372");
}
#[test]
fn number_lit_373() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("373", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "373");
}
#[test]
fn number_lit_374() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("374", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "374");
}
#[test]
fn number_lit_375() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("375", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "375");
}
#[test]
fn number_lit_376() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("376", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "376");
}
#[test]
fn number_lit_377() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("377", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "377");
}
#[test]
fn number_lit_378() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("378", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "378");
}
#[test]
fn number_lit_379() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("379", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "379");
}
#[test]
fn number_lit_380() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("380", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "380");
}
#[test]
fn number_lit_381() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("381", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "381");
}
#[test]
fn number_lit_382() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("382", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "382");
}
#[test]
fn number_lit_383() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("383", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "383");
}
#[test]
fn number_lit_384() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("384", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "384");
}
#[test]
fn number_lit_385() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("385", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "385");
}
#[test]
fn number_lit_386() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("386", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "386");
}
#[test]
fn number_lit_387() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("387", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "387");
}
#[test]
fn number_lit_388() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("388", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "388");
}
#[test]
fn number_lit_389() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("389", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "389");
}
#[test]
fn number_lit_390() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("390", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "390");
}
#[test]
fn number_lit_391() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("391", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "391");
}
#[test]
fn number_lit_392() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("392", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "392");
}
#[test]
fn number_lit_393() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("393", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "393");
}
#[test]
fn number_lit_394() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("394", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "394");
}
#[test]
fn number_lit_395() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("395", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "395");
}
#[test]
fn number_lit_396() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("396", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "396");
}
#[test]
fn number_lit_397() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("397", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "397");
}
#[test]
fn number_lit_398() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("398", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "398");
}
#[test]
fn number_lit_399() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("399", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "399");
}
#[test]
fn number_lit_400() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("400", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "400");
}
#[test]
fn number_lit_401() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("401", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "401");
}
#[test]
fn number_lit_402() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("402", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "402");
}
#[test]
fn number_lit_403() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("403", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "403");
}
#[test]
fn number_lit_404() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("404", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "404");
}
#[test]
fn number_lit_405() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("405", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "405");
}
#[test]
fn number_lit_406() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("406", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "406");
}
#[test]
fn number_lit_407() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("407", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "407");
}
#[test]
fn number_lit_408() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("408", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "408");
}
#[test]
fn number_lit_409() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("409", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "409");
}
#[test]
fn number_lit_410() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("410", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "410");
}
#[test]
fn number_lit_411() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("411", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "411");
}
#[test]
fn number_lit_412() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("412", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "412");
}
#[test]
fn number_lit_413() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("413", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "413");
}
#[test]
fn number_lit_414() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("414", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "414");
}
#[test]
fn number_lit_415() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("415", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "415");
}
#[test]
fn number_lit_416() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("416", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "416");
}
#[test]
fn number_lit_417() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("417", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "417");
}
#[test]
fn number_lit_418() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("418", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "418");
}
#[test]
fn number_lit_419() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("419", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "419");
}
#[test]
fn number_lit_420() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("420", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "420");
}
#[test]
fn number_lit_421() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("421", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "421");
}
#[test]
fn number_lit_422() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("422", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "422");
}
#[test]
fn number_lit_423() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("423", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "423");
}
#[test]
fn number_lit_424() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("424", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "424");
}
#[test]
fn number_lit_425() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("425", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "425");
}
#[test]
fn number_lit_426() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("426", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "426");
}
#[test]
fn number_lit_427() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("427", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "427");
}
#[test]
fn number_lit_428() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("428", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "428");
}
#[test]
fn number_lit_429() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("429", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "429");
}
#[test]
fn number_lit_430() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("430", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "430");
}
#[test]
fn number_lit_431() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("431", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "431");
}
#[test]
fn number_lit_432() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("432", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "432");
}
#[test]
fn number_lit_433() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("433", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "433");
}
#[test]
fn number_lit_434() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("434", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "434");
}
#[test]
fn number_lit_435() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("435", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "435");
}
#[test]
fn number_lit_436() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("436", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "436");
}
#[test]
fn number_lit_437() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("437", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "437");
}
#[test]
fn number_lit_438() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("438", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "438");
}
#[test]
fn number_lit_439() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("439", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "439");
}
#[test]
fn number_lit_440() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("440", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "440");
}
#[test]
fn number_lit_441() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("441", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "441");
}
#[test]
fn number_lit_442() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("442", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "442");
}
#[test]
fn number_lit_443() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("443", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "443");
}
#[test]
fn number_lit_444() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("444", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "444");
}
#[test]
fn number_lit_445() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("445", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "445");
}
#[test]
fn number_lit_446() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("446", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "446");
}
#[test]
fn number_lit_447() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("447", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "447");
}
#[test]
fn number_lit_448() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("448", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "448");
}
#[test]
fn number_lit_449() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("449", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "449");
}
#[test]
fn number_lit_450() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("450", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "450");
}
#[test]
fn number_lit_451() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("451", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "451");
}
#[test]
fn number_lit_452() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("452", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "452");
}
#[test]
fn number_lit_453() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("453", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "453");
}
#[test]
fn number_lit_454() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("454", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "454");
}
#[test]
fn number_lit_455() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("455", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "455");
}
#[test]
fn number_lit_456() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("456", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "456");
}
#[test]
fn number_lit_457() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("457", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "457");
}
#[test]
fn number_lit_458() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("458", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "458");
}
#[test]
fn number_lit_459() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("459", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "459");
}
#[test]
fn number_lit_460() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("460", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "460");
}
#[test]
fn number_lit_461() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("461", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "461");
}
#[test]
fn number_lit_462() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("462", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "462");
}
#[test]
fn number_lit_463() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("463", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "463");
}
#[test]
fn number_lit_464() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("464", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "464");
}
#[test]
fn number_lit_465() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("465", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "465");
}
#[test]
fn number_lit_466() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("466", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "466");
}
#[test]
fn number_lit_467() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("467", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "467");
}
#[test]
fn number_lit_468() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("468", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "468");
}
#[test]
fn number_lit_469() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("469", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "469");
}
#[test]
fn number_lit_470() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("470", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "470");
}
#[test]
fn number_lit_471() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("471", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "471");
}
#[test]
fn number_lit_472() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("472", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "472");
}
#[test]
fn number_lit_473() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("473", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "473");
}
#[test]
fn number_lit_474() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("474", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "474");
}
#[test]
fn number_lit_475() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("475", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "475");
}
#[test]
fn number_lit_476() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("476", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "476");
}
#[test]
fn number_lit_477() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("477", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "477");
}
#[test]
fn number_lit_478() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("478", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "478");
}
#[test]
fn number_lit_479() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("479", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "479");
}
#[test]
fn number_lit_480() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("480", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "480");
}
#[test]
fn number_lit_481() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("481", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "481");
}
#[test]
fn number_lit_482() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("482", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "482");
}
#[test]
fn number_lit_483() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("483", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "483");
}
#[test]
fn number_lit_484() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("484", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "484");
}
#[test]
fn number_lit_485() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("485", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "485");
}
#[test]
fn number_lit_486() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("486", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "486");
}
#[test]
fn number_lit_487() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("487", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "487");
}
#[test]
fn number_lit_488() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("488", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "488");
}
#[test]
fn number_lit_489() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("489", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "489");
}
#[test]
fn number_lit_490() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("490", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "490");
}
#[test]
fn number_lit_491() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("491", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "491");
}
#[test]
fn number_lit_492() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("492", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "492");
}
#[test]
fn number_lit_493() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("493", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "493");
}
#[test]
fn number_lit_494() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("494", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "494");
}
#[test]
fn number_lit_495() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("495", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "495");
}
#[test]
fn number_lit_496() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("496", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "496");
}
#[test]
fn number_lit_497() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("497", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "497");
}
#[test]
fn number_lit_498() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("498", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "498");
}
#[test]
fn number_lit_499() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize("499", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::IntLit);
    assert_eq!(tokens[0].text, "499");
}
#[test]
fn string_lit_0() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello0""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello0");
}
#[test]
fn string_lit_1() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello1""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello1");
}
#[test]
fn string_lit_2() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello2""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello2");
}
#[test]
fn string_lit_3() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello3""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello3");
}
#[test]
fn string_lit_4() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello4""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello4");
}
#[test]
fn string_lit_5() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello5""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello5");
}
#[test]
fn string_lit_6() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello6""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello6");
}
#[test]
fn string_lit_7() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello7""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello7");
}
#[test]
fn string_lit_8() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello8""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello8");
}
#[test]
fn string_lit_9() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello9""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello9");
}
#[test]
fn string_lit_10() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello10""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello10");
}
#[test]
fn string_lit_11() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello11""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello11");
}
#[test]
fn string_lit_12() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello12""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello12");
}
#[test]
fn string_lit_13() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello13""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello13");
}
#[test]
fn string_lit_14() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello14""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello14");
}
#[test]
fn string_lit_15() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello15""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello15");
}
#[test]
fn string_lit_16() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello16""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello16");
}
#[test]
fn string_lit_17() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello17""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello17");
}
#[test]
fn string_lit_18() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello18""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello18");
}
#[test]
fn string_lit_19() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello19""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello19");
}
#[test]
fn string_lit_20() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello20""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello20");
}
#[test]
fn string_lit_21() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello21""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello21");
}
#[test]
fn string_lit_22() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello22""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello22");
}
#[test]
fn string_lit_23() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello23""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello23");
}
#[test]
fn string_lit_24() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello24""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello24");
}
#[test]
fn string_lit_25() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello25""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello25");
}
#[test]
fn string_lit_26() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello26""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello26");
}
#[test]
fn string_lit_27() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello27""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello27");
}
#[test]
fn string_lit_28() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello28""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello28");
}
#[test]
fn string_lit_29() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello29""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello29");
}
#[test]
fn string_lit_30() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello30""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello30");
}
#[test]
fn string_lit_31() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello31""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello31");
}
#[test]
fn string_lit_32() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello32""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello32");
}
#[test]
fn string_lit_33() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello33""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello33");
}
#[test]
fn string_lit_34() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello34""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello34");
}
#[test]
fn string_lit_35() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello35""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello35");
}
#[test]
fn string_lit_36() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello36""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello36");
}
#[test]
fn string_lit_37() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello37""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello37");
}
#[test]
fn string_lit_38() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello38""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello38");
}
#[test]
fn string_lit_39() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello39""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello39");
}
#[test]
fn string_lit_40() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello40""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello40");
}
#[test]
fn string_lit_41() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello41""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello41");
}
#[test]
fn string_lit_42() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello42""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello42");
}
#[test]
fn string_lit_43() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello43""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello43");
}
#[test]
fn string_lit_44() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello44""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello44");
}
#[test]
fn string_lit_45() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello45""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello45");
}
#[test]
fn string_lit_46() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello46""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello46");
}
#[test]
fn string_lit_47() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello47""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello47");
}
#[test]
fn string_lit_48() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello48""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello48");
}
#[test]
fn string_lit_49() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello49""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello49");
}
#[test]
fn string_lit_50() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello50""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello50");
}
#[test]
fn string_lit_51() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello51""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello51");
}
#[test]
fn string_lit_52() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello52""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello52");
}
#[test]
fn string_lit_53() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello53""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello53");
}
#[test]
fn string_lit_54() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello54""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello54");
}
#[test]
fn string_lit_55() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello55""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello55");
}
#[test]
fn string_lit_56() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello56""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello56");
}
#[test]
fn string_lit_57() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello57""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello57");
}
#[test]
fn string_lit_58() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello58""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello58");
}
#[test]
fn string_lit_59() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello59""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello59");
}
#[test]
fn string_lit_60() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello60""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello60");
}
#[test]
fn string_lit_61() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello61""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello61");
}
#[test]
fn string_lit_62() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello62""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello62");
}
#[test]
fn string_lit_63() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello63""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello63");
}
#[test]
fn string_lit_64() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello64""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello64");
}
#[test]
fn string_lit_65() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello65""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello65");
}
#[test]
fn string_lit_66() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello66""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello66");
}
#[test]
fn string_lit_67() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello67""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello67");
}
#[test]
fn string_lit_68() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello68""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello68");
}
#[test]
fn string_lit_69() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello69""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello69");
}
#[test]
fn string_lit_70() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello70""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello70");
}
#[test]
fn string_lit_71() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello71""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello71");
}
#[test]
fn string_lit_72() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello72""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello72");
}
#[test]
fn string_lit_73() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello73""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello73");
}
#[test]
fn string_lit_74() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello74""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello74");
}
#[test]
fn string_lit_75() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello75""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello75");
}
#[test]
fn string_lit_76() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello76""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello76");
}
#[test]
fn string_lit_77() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello77""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello77");
}
#[test]
fn string_lit_78() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello78""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello78");
}
#[test]
fn string_lit_79() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello79""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello79");
}
#[test]
fn string_lit_80() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello80""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello80");
}
#[test]
fn string_lit_81() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello81""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello81");
}
#[test]
fn string_lit_82() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello82""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello82");
}
#[test]
fn string_lit_83() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello83""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello83");
}
#[test]
fn string_lit_84() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello84""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello84");
}
#[test]
fn string_lit_85() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello85""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello85");
}
#[test]
fn string_lit_86() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello86""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello86");
}
#[test]
fn string_lit_87() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello87""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello87");
}
#[test]
fn string_lit_88() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello88""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello88");
}
#[test]
fn string_lit_89() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello89""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello89");
}
#[test]
fn string_lit_90() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello90""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello90");
}
#[test]
fn string_lit_91() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello91""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello91");
}
#[test]
fn string_lit_92() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello92""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello92");
}
#[test]
fn string_lit_93() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello93""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello93");
}
#[test]
fn string_lit_94() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello94""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello94");
}
#[test]
fn string_lit_95() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello95""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello95");
}
#[test]
fn string_lit_96() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello96""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello96");
}
#[test]
fn string_lit_97() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello97""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello97");
}
#[test]
fn string_lit_98() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello98""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello98");
}
#[test]
fn string_lit_99() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello99""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello99");
}
#[test]
fn string_lit_100() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello100""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello100");
}
#[test]
fn string_lit_101() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello101""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello101");
}
#[test]
fn string_lit_102() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello102""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello102");
}
#[test]
fn string_lit_103() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello103""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello103");
}
#[test]
fn string_lit_104() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello104""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello104");
}
#[test]
fn string_lit_105() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello105""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello105");
}
#[test]
fn string_lit_106() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello106""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello106");
}
#[test]
fn string_lit_107() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello107""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello107");
}
#[test]
fn string_lit_108() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello108""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello108");
}
#[test]
fn string_lit_109() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello109""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello109");
}
#[test]
fn string_lit_110() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello110""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello110");
}
#[test]
fn string_lit_111() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello111""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello111");
}
#[test]
fn string_lit_112() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello112""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello112");
}
#[test]
fn string_lit_113() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello113""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello113");
}
#[test]
fn string_lit_114() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello114""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello114");
}
#[test]
fn string_lit_115() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello115""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello115");
}
#[test]
fn string_lit_116() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello116""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello116");
}
#[test]
fn string_lit_117() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello117""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello117");
}
#[test]
fn string_lit_118() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello118""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello118");
}
#[test]
fn string_lit_119() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello119""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello119");
}
#[test]
fn string_lit_120() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello120""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello120");
}
#[test]
fn string_lit_121() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello121""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello121");
}
#[test]
fn string_lit_122() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello122""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello122");
}
#[test]
fn string_lit_123() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello123""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello123");
}
#[test]
fn string_lit_124() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello124""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello124");
}
#[test]
fn string_lit_125() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello125""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello125");
}
#[test]
fn string_lit_126() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello126""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello126");
}
#[test]
fn string_lit_127() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello127""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello127");
}
#[test]
fn string_lit_128() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello128""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello128");
}
#[test]
fn string_lit_129() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello129""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello129");
}
#[test]
fn string_lit_130() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello130""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello130");
}
#[test]
fn string_lit_131() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello131""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello131");
}
#[test]
fn string_lit_132() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello132""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello132");
}
#[test]
fn string_lit_133() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello133""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello133");
}
#[test]
fn string_lit_134() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello134""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello134");
}
#[test]
fn string_lit_135() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello135""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello135");
}
#[test]
fn string_lit_136() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello136""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello136");
}
#[test]
fn string_lit_137() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello137""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello137");
}
#[test]
fn string_lit_138() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello138""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello138");
}
#[test]
fn string_lit_139() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello139""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello139");
}
#[test]
fn string_lit_140() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello140""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello140");
}
#[test]
fn string_lit_141() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello141""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello141");
}
#[test]
fn string_lit_142() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello142""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello142");
}
#[test]
fn string_lit_143() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello143""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello143");
}
#[test]
fn string_lit_144() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello144""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello144");
}
#[test]
fn string_lit_145() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello145""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello145");
}
#[test]
fn string_lit_146() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello146""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello146");
}
#[test]
fn string_lit_147() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello147""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello147");
}
#[test]
fn string_lit_148() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello148""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello148");
}
#[test]
fn string_lit_149() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello149""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello149");
}
#[test]
fn string_lit_150() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello150""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello150");
}
#[test]
fn string_lit_151() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello151""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello151");
}
#[test]
fn string_lit_152() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello152""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello152");
}
#[test]
fn string_lit_153() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello153""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello153");
}
#[test]
fn string_lit_154() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello154""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello154");
}
#[test]
fn string_lit_155() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello155""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello155");
}
#[test]
fn string_lit_156() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello156""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello156");
}
#[test]
fn string_lit_157() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello157""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello157");
}
#[test]
fn string_lit_158() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello158""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello158");
}
#[test]
fn string_lit_159() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello159""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello159");
}
#[test]
fn string_lit_160() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello160""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello160");
}
#[test]
fn string_lit_161() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello161""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello161");
}
#[test]
fn string_lit_162() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello162""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello162");
}
#[test]
fn string_lit_163() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello163""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello163");
}
#[test]
fn string_lit_164() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello164""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello164");
}
#[test]
fn string_lit_165() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello165""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello165");
}
#[test]
fn string_lit_166() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello166""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello166");
}
#[test]
fn string_lit_167() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello167""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello167");
}
#[test]
fn string_lit_168() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello168""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello168");
}
#[test]
fn string_lit_169() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello169""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello169");
}
#[test]
fn string_lit_170() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello170""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello170");
}
#[test]
fn string_lit_171() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello171""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello171");
}
#[test]
fn string_lit_172() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello172""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello172");
}
#[test]
fn string_lit_173() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello173""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello173");
}
#[test]
fn string_lit_174() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello174""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello174");
}
#[test]
fn string_lit_175() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello175""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello175");
}
#[test]
fn string_lit_176() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello176""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello176");
}
#[test]
fn string_lit_177() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello177""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello177");
}
#[test]
fn string_lit_178() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello178""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello178");
}
#[test]
fn string_lit_179() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello179""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello179");
}
#[test]
fn string_lit_180() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello180""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello180");
}
#[test]
fn string_lit_181() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello181""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello181");
}
#[test]
fn string_lit_182() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello182""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello182");
}
#[test]
fn string_lit_183() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello183""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello183");
}
#[test]
fn string_lit_184() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello184""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello184");
}
#[test]
fn string_lit_185() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello185""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello185");
}
#[test]
fn string_lit_186() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello186""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello186");
}
#[test]
fn string_lit_187() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello187""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello187");
}
#[test]
fn string_lit_188() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello188""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello188");
}
#[test]
fn string_lit_189() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello189""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello189");
}
#[test]
fn string_lit_190() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello190""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello190");
}
#[test]
fn string_lit_191() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello191""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello191");
}
#[test]
fn string_lit_192() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello192""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello192");
}
#[test]
fn string_lit_193() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello193""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello193");
}
#[test]
fn string_lit_194() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello194""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello194");
}
#[test]
fn string_lit_195() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello195""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello195");
}
#[test]
fn string_lit_196() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello196""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello196");
}
#[test]
fn string_lit_197() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello197""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello197");
}
#[test]
fn string_lit_198() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello198""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello198");
}
#[test]
fn string_lit_199() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello199""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello199");
}
#[test]
fn string_lit_200() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello200""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello200");
}
#[test]
fn string_lit_201() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello201""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello201");
}
#[test]
fn string_lit_202() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello202""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello202");
}
#[test]
fn string_lit_203() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello203""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello203");
}
#[test]
fn string_lit_204() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello204""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello204");
}
#[test]
fn string_lit_205() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello205""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello205");
}
#[test]
fn string_lit_206() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello206""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello206");
}
#[test]
fn string_lit_207() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello207""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello207");
}
#[test]
fn string_lit_208() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello208""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello208");
}
#[test]
fn string_lit_209() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello209""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello209");
}
#[test]
fn string_lit_210() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello210""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello210");
}
#[test]
fn string_lit_211() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello211""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello211");
}
#[test]
fn string_lit_212() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello212""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello212");
}
#[test]
fn string_lit_213() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello213""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello213");
}
#[test]
fn string_lit_214() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello214""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello214");
}
#[test]
fn string_lit_215() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello215""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello215");
}
#[test]
fn string_lit_216() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello216""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello216");
}
#[test]
fn string_lit_217() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello217""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello217");
}
#[test]
fn string_lit_218() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello218""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello218");
}
#[test]
fn string_lit_219() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello219""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello219");
}
#[test]
fn string_lit_220() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello220""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello220");
}
#[test]
fn string_lit_221() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello221""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello221");
}
#[test]
fn string_lit_222() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello222""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello222");
}
#[test]
fn string_lit_223() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello223""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello223");
}
#[test]
fn string_lit_224() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello224""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello224");
}
#[test]
fn string_lit_225() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello225""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello225");
}
#[test]
fn string_lit_226() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello226""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello226");
}
#[test]
fn string_lit_227() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello227""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello227");
}
#[test]
fn string_lit_228() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello228""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello228");
}
#[test]
fn string_lit_229() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello229""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello229");
}
#[test]
fn string_lit_230() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello230""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello230");
}
#[test]
fn string_lit_231() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello231""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello231");
}
#[test]
fn string_lit_232() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello232""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello232");
}
#[test]
fn string_lit_233() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello233""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello233");
}
#[test]
fn string_lit_234() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello234""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello234");
}
#[test]
fn string_lit_235() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello235""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello235");
}
#[test]
fn string_lit_236() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello236""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello236");
}
#[test]
fn string_lit_237() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello237""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello237");
}
#[test]
fn string_lit_238() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello238""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello238");
}
#[test]
fn string_lit_239() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello239""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello239");
}
#[test]
fn string_lit_240() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello240""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello240");
}
#[test]
fn string_lit_241() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello241""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello241");
}
#[test]
fn string_lit_242() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello242""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello242");
}
#[test]
fn string_lit_243() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello243""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello243");
}
#[test]
fn string_lit_244() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello244""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello244");
}
#[test]
fn string_lit_245() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello245""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello245");
}
#[test]
fn string_lit_246() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello246""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello246");
}
#[test]
fn string_lit_247() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello247""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello247");
}
#[test]
fn string_lit_248() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello248""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello248");
}
#[test]
fn string_lit_249() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello249""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello249");
}
#[test]
fn string_lit_250() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello250""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello250");
}
#[test]
fn string_lit_251() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello251""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello251");
}
#[test]
fn string_lit_252() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello252""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello252");
}
#[test]
fn string_lit_253() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello253""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello253");
}
#[test]
fn string_lit_254() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello254""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello254");
}
#[test]
fn string_lit_255() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello255""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello255");
}
#[test]
fn string_lit_256() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello256""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello256");
}
#[test]
fn string_lit_257() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello257""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello257");
}
#[test]
fn string_lit_258() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello258""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello258");
}
#[test]
fn string_lit_259() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello259""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello259");
}
#[test]
fn string_lit_260() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello260""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello260");
}
#[test]
fn string_lit_261() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello261""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello261");
}
#[test]
fn string_lit_262() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello262""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello262");
}
#[test]
fn string_lit_263() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello263""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello263");
}
#[test]
fn string_lit_264() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello264""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello264");
}
#[test]
fn string_lit_265() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello265""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello265");
}
#[test]
fn string_lit_266() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello266""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello266");
}
#[test]
fn string_lit_267() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello267""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello267");
}
#[test]
fn string_lit_268() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello268""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello268");
}
#[test]
fn string_lit_269() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello269""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello269");
}
#[test]
fn string_lit_270() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello270""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello270");
}
#[test]
fn string_lit_271() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello271""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello271");
}
#[test]
fn string_lit_272() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello272""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello272");
}
#[test]
fn string_lit_273() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello273""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello273");
}
#[test]
fn string_lit_274() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello274""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello274");
}
#[test]
fn string_lit_275() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello275""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello275");
}
#[test]
fn string_lit_276() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello276""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello276");
}
#[test]
fn string_lit_277() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello277""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello277");
}
#[test]
fn string_lit_278() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello278""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello278");
}
#[test]
fn string_lit_279() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello279""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello279");
}
#[test]
fn string_lit_280() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello280""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello280");
}
#[test]
fn string_lit_281() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello281""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello281");
}
#[test]
fn string_lit_282() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello282""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello282");
}
#[test]
fn string_lit_283() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello283""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello283");
}
#[test]
fn string_lit_284() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello284""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello284");
}
#[test]
fn string_lit_285() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello285""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello285");
}
#[test]
fn string_lit_286() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello286""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello286");
}
#[test]
fn string_lit_287() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello287""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello287");
}
#[test]
fn string_lit_288() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello288""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello288");
}
#[test]
fn string_lit_289() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello289""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello289");
}
#[test]
fn string_lit_290() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello290""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello290");
}
#[test]
fn string_lit_291() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello291""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello291");
}
#[test]
fn string_lit_292() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello292""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello292");
}
#[test]
fn string_lit_293() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello293""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello293");
}
#[test]
fn string_lit_294() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello294""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello294");
}
#[test]
fn string_lit_295() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello295""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello295");
}
#[test]
fn string_lit_296() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello296""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello296");
}
#[test]
fn string_lit_297() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello297""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello297");
}
#[test]
fn string_lit_298() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello298""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello298");
}
#[test]
fn string_lit_299() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello299""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello299");
}
#[test]
fn string_lit_300() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello300""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello300");
}
#[test]
fn string_lit_301() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello301""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello301");
}
#[test]
fn string_lit_302() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello302""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello302");
}
#[test]
fn string_lit_303() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello303""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello303");
}
#[test]
fn string_lit_304() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello304""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello304");
}
#[test]
fn string_lit_305() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello305""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello305");
}
#[test]
fn string_lit_306() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello306""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello306");
}
#[test]
fn string_lit_307() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello307""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello307");
}
#[test]
fn string_lit_308() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello308""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello308");
}
#[test]
fn string_lit_309() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello309""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello309");
}
#[test]
fn string_lit_310() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello310""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello310");
}
#[test]
fn string_lit_311() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello311""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello311");
}
#[test]
fn string_lit_312() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello312""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello312");
}
#[test]
fn string_lit_313() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello313""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello313");
}
#[test]
fn string_lit_314() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello314""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello314");
}
#[test]
fn string_lit_315() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello315""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello315");
}
#[test]
fn string_lit_316() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello316""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello316");
}
#[test]
fn string_lit_317() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello317""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello317");
}
#[test]
fn string_lit_318() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello318""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello318");
}
#[test]
fn string_lit_319() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello319""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello319");
}
#[test]
fn string_lit_320() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello320""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello320");
}
#[test]
fn string_lit_321() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello321""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello321");
}
#[test]
fn string_lit_322() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello322""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello322");
}
#[test]
fn string_lit_323() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello323""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello323");
}
#[test]
fn string_lit_324() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello324""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello324");
}
#[test]
fn string_lit_325() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello325""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello325");
}
#[test]
fn string_lit_326() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello326""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello326");
}
#[test]
fn string_lit_327() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello327""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello327");
}
#[test]
fn string_lit_328() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello328""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello328");
}
#[test]
fn string_lit_329() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello329""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello329");
}
#[test]
fn string_lit_330() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello330""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello330");
}
#[test]
fn string_lit_331() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello331""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello331");
}
#[test]
fn string_lit_332() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello332""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello332");
}
#[test]
fn string_lit_333() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello333""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello333");
}
#[test]
fn string_lit_334() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello334""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello334");
}
#[test]
fn string_lit_335() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello335""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello335");
}
#[test]
fn string_lit_336() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello336""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello336");
}
#[test]
fn string_lit_337() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello337""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello337");
}
#[test]
fn string_lit_338() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello338""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello338");
}
#[test]
fn string_lit_339() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello339""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello339");
}
#[test]
fn string_lit_340() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello340""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello340");
}
#[test]
fn string_lit_341() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello341""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello341");
}
#[test]
fn string_lit_342() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello342""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello342");
}
#[test]
fn string_lit_343() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello343""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello343");
}
#[test]
fn string_lit_344() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello344""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello344");
}
#[test]
fn string_lit_345() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello345""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello345");
}
#[test]
fn string_lit_346() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello346""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello346");
}
#[test]
fn string_lit_347() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello347""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello347");
}
#[test]
fn string_lit_348() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello348""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello348");
}
#[test]
fn string_lit_349() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello349""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello349");
}
#[test]
fn string_lit_350() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello350""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello350");
}
#[test]
fn string_lit_351() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello351""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello351");
}
#[test]
fn string_lit_352() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello352""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello352");
}
#[test]
fn string_lit_353() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello353""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello353");
}
#[test]
fn string_lit_354() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello354""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello354");
}
#[test]
fn string_lit_355() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello355""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello355");
}
#[test]
fn string_lit_356() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello356""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello356");
}
#[test]
fn string_lit_357() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello357""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello357");
}
#[test]
fn string_lit_358() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello358""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello358");
}
#[test]
fn string_lit_359() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello359""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello359");
}
#[test]
fn string_lit_360() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello360""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello360");
}
#[test]
fn string_lit_361() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello361""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello361");
}
#[test]
fn string_lit_362() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello362""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello362");
}
#[test]
fn string_lit_363() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello363""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello363");
}
#[test]
fn string_lit_364() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello364""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello364");
}
#[test]
fn string_lit_365() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello365""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello365");
}
#[test]
fn string_lit_366() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello366""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello366");
}
#[test]
fn string_lit_367() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello367""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello367");
}
#[test]
fn string_lit_368() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello368""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello368");
}
#[test]
fn string_lit_369() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello369""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello369");
}
#[test]
fn string_lit_370() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello370""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello370");
}
#[test]
fn string_lit_371() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello371""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello371");
}
#[test]
fn string_lit_372() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello372""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello372");
}
#[test]
fn string_lit_373() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello373""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello373");
}
#[test]
fn string_lit_374() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello374""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello374");
}
#[test]
fn string_lit_375() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello375""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello375");
}
#[test]
fn string_lit_376() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello376""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello376");
}
#[test]
fn string_lit_377() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello377""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello377");
}
#[test]
fn string_lit_378() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello378""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello378");
}
#[test]
fn string_lit_379() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello379""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello379");
}
#[test]
fn string_lit_380() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello380""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello380");
}
#[test]
fn string_lit_381() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello381""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello381");
}
#[test]
fn string_lit_382() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello382""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello382");
}
#[test]
fn string_lit_383() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello383""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello383");
}
#[test]
fn string_lit_384() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello384""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello384");
}
#[test]
fn string_lit_385() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello385""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello385");
}
#[test]
fn string_lit_386() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello386""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello386");
}
#[test]
fn string_lit_387() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello387""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello387");
}
#[test]
fn string_lit_388() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello388""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello388");
}
#[test]
fn string_lit_389() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello389""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello389");
}
#[test]
fn string_lit_390() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello390""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello390");
}
#[test]
fn string_lit_391() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello391""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello391");
}
#[test]
fn string_lit_392() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello392""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello392");
}
#[test]
fn string_lit_393() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello393""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello393");
}
#[test]
fn string_lit_394() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello394""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello394");
}
#[test]
fn string_lit_395() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello395""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello395");
}
#[test]
fn string_lit_396() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello396""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello396");
}
#[test]
fn string_lit_397() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello397""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello397");
}
#[test]
fn string_lit_398() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello398""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello398");
}
#[test]
fn string_lit_399() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello399""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello399");
}
#[test]
fn string_lit_400() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello400""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello400");
}
#[test]
fn string_lit_401() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello401""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello401");
}
#[test]
fn string_lit_402() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello402""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello402");
}
#[test]
fn string_lit_403() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello403""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello403");
}
#[test]
fn string_lit_404() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello404""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello404");
}
#[test]
fn string_lit_405() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello405""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello405");
}
#[test]
fn string_lit_406() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello406""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello406");
}
#[test]
fn string_lit_407() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello407""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello407");
}
#[test]
fn string_lit_408() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello408""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello408");
}
#[test]
fn string_lit_409() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello409""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello409");
}
#[test]
fn string_lit_410() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello410""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello410");
}
#[test]
fn string_lit_411() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello411""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello411");
}
#[test]
fn string_lit_412() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello412""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello412");
}
#[test]
fn string_lit_413() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello413""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello413");
}
#[test]
fn string_lit_414() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello414""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello414");
}
#[test]
fn string_lit_415() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello415""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello415");
}
#[test]
fn string_lit_416() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello416""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello416");
}
#[test]
fn string_lit_417() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello417""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello417");
}
#[test]
fn string_lit_418() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello418""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello418");
}
#[test]
fn string_lit_419() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello419""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello419");
}
#[test]
fn string_lit_420() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello420""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello420");
}
#[test]
fn string_lit_421() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello421""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello421");
}
#[test]
fn string_lit_422() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello422""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello422");
}
#[test]
fn string_lit_423() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello423""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello423");
}
#[test]
fn string_lit_424() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello424""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello424");
}
#[test]
fn string_lit_425() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello425""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello425");
}
#[test]
fn string_lit_426() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello426""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello426");
}
#[test]
fn string_lit_427() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello427""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello427");
}
#[test]
fn string_lit_428() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello428""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello428");
}
#[test]
fn string_lit_429() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello429""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello429");
}
#[test]
fn string_lit_430() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello430""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello430");
}
#[test]
fn string_lit_431() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello431""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello431");
}
#[test]
fn string_lit_432() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello432""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello432");
}
#[test]
fn string_lit_433() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello433""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello433");
}
#[test]
fn string_lit_434() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello434""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello434");
}
#[test]
fn string_lit_435() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello435""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello435");
}
#[test]
fn string_lit_436() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello436""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello436");
}
#[test]
fn string_lit_437() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello437""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello437");
}
#[test]
fn string_lit_438() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello438""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello438");
}
#[test]
fn string_lit_439() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello439""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello439");
}
#[test]
fn string_lit_440() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello440""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello440");
}
#[test]
fn string_lit_441() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello441""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello441");
}
#[test]
fn string_lit_442() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello442""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello442");
}
#[test]
fn string_lit_443() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello443""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello443");
}
#[test]
fn string_lit_444() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello444""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello444");
}
#[test]
fn string_lit_445() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello445""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello445");
}
#[test]
fn string_lit_446() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello446""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello446");
}
#[test]
fn string_lit_447() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello447""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello447");
}
#[test]
fn string_lit_448() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello448""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello448");
}
#[test]
fn string_lit_449() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello449""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello449");
}
#[test]
fn string_lit_450() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello450""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello450");
}
#[test]
fn string_lit_451() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello451""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello451");
}
#[test]
fn string_lit_452() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello452""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello452");
}
#[test]
fn string_lit_453() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello453""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello453");
}
#[test]
fn string_lit_454() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello454""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello454");
}
#[test]
fn string_lit_455() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello455""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello455");
}
#[test]
fn string_lit_456() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello456""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello456");
}
#[test]
fn string_lit_457() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello457""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello457");
}
#[test]
fn string_lit_458() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello458""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello458");
}
#[test]
fn string_lit_459() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello459""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello459");
}
#[test]
fn string_lit_460() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello460""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello460");
}
#[test]
fn string_lit_461() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello461""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello461");
}
#[test]
fn string_lit_462() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello462""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello462");
}
#[test]
fn string_lit_463() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello463""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello463");
}
#[test]
fn string_lit_464() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello464""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello464");
}
#[test]
fn string_lit_465() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello465""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello465");
}
#[test]
fn string_lit_466() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello466""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello466");
}
#[test]
fn string_lit_467() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello467""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello467");
}
#[test]
fn string_lit_468() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello468""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello468");
}
#[test]
fn string_lit_469() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello469""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello469");
}
#[test]
fn string_lit_470() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello470""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello470");
}
#[test]
fn string_lit_471() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello471""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello471");
}
#[test]
fn string_lit_472() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello472""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello472");
}
#[test]
fn string_lit_473() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello473""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello473");
}
#[test]
fn string_lit_474() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello474""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello474");
}
#[test]
fn string_lit_475() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello475""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello475");
}
#[test]
fn string_lit_476() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello476""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello476");
}
#[test]
fn string_lit_477() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello477""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello477");
}
#[test]
fn string_lit_478() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello478""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello478");
}
#[test]
fn string_lit_479() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello479""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello479");
}
#[test]
fn string_lit_480() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello480""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello480");
}
#[test]
fn string_lit_481() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello481""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello481");
}
#[test]
fn string_lit_482() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello482""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello482");
}
#[test]
fn string_lit_483() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello483""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello483");
}
#[test]
fn string_lit_484() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello484""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello484");
}
#[test]
fn string_lit_485() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello485""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello485");
}
#[test]
fn string_lit_486() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello486""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello486");
}
#[test]
fn string_lit_487() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello487""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello487");
}
#[test]
fn string_lit_488() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello488""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello488");
}
#[test]
fn string_lit_489() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello489""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello489");
}
#[test]
fn string_lit_490() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello490""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello490");
}
#[test]
fn string_lit_491() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello491""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello491");
}
#[test]
fn string_lit_492() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello492""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello492");
}
#[test]
fn string_lit_493() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello493""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello493");
}
#[test]
fn string_lit_494() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello494""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello494");
}
#[test]
fn string_lit_495() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello495""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello495");
}
#[test]
fn string_lit_496() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello496""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello496");
}
#[test]
fn string_lit_497() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello497""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello497");
}
#[test]
fn string_lit_498() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello498""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello498");
}
#[test]
fn string_lit_499() {
    use axolc_core::lexer::tokenize;
    let (tokens, _) = tokenize(r#""hello499""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::TokenKind::StrLit);
    assert_eq!(tokens[0].text, "hello499");
}
#[test]
fn interp_call_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(0)) end", 0);
    assert_eq!(out, "0\n");
}
#[test]
fn interp_call_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(1)) end", 0);
    assert_eq!(out, "2\n");
}
#[test]
fn interp_call_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(2)) end", 0);
    assert_eq!(out, "4\n");
}
#[test]
fn interp_call_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(3)) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn interp_call_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(4)) end", 0);
    assert_eq!(out, "8\n");
}
#[test]
fn interp_call_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(5)) end", 0);
    assert_eq!(out, "10\n");
}
#[test]
fn interp_call_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(6)) end", 0);
    assert_eq!(out, "12\n");
}
#[test]
fn interp_call_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(7)) end", 0);
    assert_eq!(out, "14\n");
}
#[test]
fn interp_call_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(8)) end", 0);
    assert_eq!(out, "16\n");
}
#[test]
fn interp_call_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(9)) end", 0);
    assert_eq!(out, "18\n");
}
#[test]
fn interp_call_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(10)) end", 0);
    assert_eq!(out, "20\n");
}
#[test]
fn interp_call_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(11)) end", 0);
    assert_eq!(out, "22\n");
}
#[test]
fn interp_call_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(12)) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn interp_call_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(13)) end", 0);
    assert_eq!(out, "26\n");
}
#[test]
fn interp_call_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(14)) end", 0);
    assert_eq!(out, "28\n");
}
#[test]
fn interp_call_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(15)) end", 0);
    assert_eq!(out, "30\n");
}
#[test]
fn interp_call_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(16)) end", 0);
    assert_eq!(out, "32\n");
}
#[test]
fn interp_call_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(17)) end", 0);
    assert_eq!(out, "34\n");
}
#[test]
fn interp_call_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(18)) end", 0);
    assert_eq!(out, "36\n");
}
#[test]
fn interp_call_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(19)) end", 0);
    assert_eq!(out, "38\n");
}
#[test]
fn interp_call_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(20)) end", 0);
    assert_eq!(out, "40\n");
}
#[test]
fn interp_call_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(21)) end", 0);
    assert_eq!(out, "42\n");
}
#[test]
fn interp_call_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(22)) end", 0);
    assert_eq!(out, "44\n");
}
#[test]
fn interp_call_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(23)) end", 0);
    assert_eq!(out, "46\n");
}
#[test]
fn interp_call_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(24)) end", 0);
    assert_eq!(out, "48\n");
}
#[test]
fn interp_call_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(25)) end", 0);
    assert_eq!(out, "50\n");
}
#[test]
fn interp_call_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(26)) end", 0);
    assert_eq!(out, "52\n");
}
#[test]
fn interp_call_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(27)) end", 0);
    assert_eq!(out, "54\n");
}
#[test]
fn interp_call_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(28)) end", 0);
    assert_eq!(out, "56\n");
}
#[test]
fn interp_call_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(29)) end", 0);
    assert_eq!(out, "58\n");
}
#[test]
fn interp_call_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(30)) end", 0);
    assert_eq!(out, "60\n");
}
#[test]
fn interp_call_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(31)) end", 0);
    assert_eq!(out, "62\n");
}
#[test]
fn interp_call_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(32)) end", 0);
    assert_eq!(out, "64\n");
}
#[test]
fn interp_call_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(33)) end", 0);
    assert_eq!(out, "66\n");
}
#[test]
fn interp_call_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(34)) end", 0);
    assert_eq!(out, "68\n");
}
#[test]
fn interp_call_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(35)) end", 0);
    assert_eq!(out, "70\n");
}
#[test]
fn interp_call_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(36)) end", 0);
    assert_eq!(out, "72\n");
}
#[test]
fn interp_call_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(37)) end", 0);
    assert_eq!(out, "74\n");
}
#[test]
fn interp_call_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(38)) end", 0);
    assert_eq!(out, "76\n");
}
#[test]
fn interp_call_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(39)) end", 0);
    assert_eq!(out, "78\n");
}
#[test]
fn interp_call_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(40)) end", 0);
    assert_eq!(out, "80\n");
}
#[test]
fn interp_call_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(41)) end", 0);
    assert_eq!(out, "82\n");
}
#[test]
fn interp_call_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(42)) end", 0);
    assert_eq!(out, "84\n");
}
#[test]
fn interp_call_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(43)) end", 0);
    assert_eq!(out, "86\n");
}
#[test]
fn interp_call_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(44)) end", 0);
    assert_eq!(out, "88\n");
}
#[test]
fn interp_call_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(45)) end", 0);
    assert_eq!(out, "90\n");
}
#[test]
fn interp_call_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(46)) end", 0);
    assert_eq!(out, "92\n");
}
#[test]
fn interp_call_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(47)) end", 0);
    assert_eq!(out, "94\n");
}
#[test]
fn interp_call_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(48)) end", 0);
    assert_eq!(out, "96\n");
}
#[test]
fn interp_call_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(49)) end", 0);
    assert_eq!(out, "98\n");
}
#[test]
fn interp_call_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(50)) end", 0);
    assert_eq!(out, "100\n");
}
#[test]
fn interp_call_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(51)) end", 0);
    assert_eq!(out, "102\n");
}
#[test]
fn interp_call_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(52)) end", 0);
    assert_eq!(out, "104\n");
}
#[test]
fn interp_call_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(53)) end", 0);
    assert_eq!(out, "106\n");
}
#[test]
fn interp_call_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(54)) end", 0);
    assert_eq!(out, "108\n");
}
#[test]
fn interp_call_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(55)) end", 0);
    assert_eq!(out, "110\n");
}
#[test]
fn interp_call_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(56)) end", 0);
    assert_eq!(out, "112\n");
}
#[test]
fn interp_call_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(57)) end", 0);
    assert_eq!(out, "114\n");
}
#[test]
fn interp_call_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(58)) end", 0);
    assert_eq!(out, "116\n");
}
#[test]
fn interp_call_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(59)) end", 0);
    assert_eq!(out, "118\n");
}
#[test]
fn interp_call_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(60)) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn interp_call_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(61)) end", 0);
    assert_eq!(out, "122\n");
}
#[test]
fn interp_call_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(62)) end", 0);
    assert_eq!(out, "124\n");
}
#[test]
fn interp_call_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(63)) end", 0);
    assert_eq!(out, "126\n");
}
#[test]
fn interp_call_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(64)) end", 0);
    assert_eq!(out, "128\n");
}
#[test]
fn interp_call_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(65)) end", 0);
    assert_eq!(out, "130\n");
}
#[test]
fn interp_call_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(66)) end", 0);
    assert_eq!(out, "132\n");
}
#[test]
fn interp_call_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(67)) end", 0);
    assert_eq!(out, "134\n");
}
#[test]
fn interp_call_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(68)) end", 0);
    assert_eq!(out, "136\n");
}
#[test]
fn interp_call_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(69)) end", 0);
    assert_eq!(out, "138\n");
}
#[test]
fn interp_call_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(70)) end", 0);
    assert_eq!(out, "140\n");
}
#[test]
fn interp_call_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(71)) end", 0);
    assert_eq!(out, "142\n");
}
#[test]
fn interp_call_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(72)) end", 0);
    assert_eq!(out, "144\n");
}
#[test]
fn interp_call_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(73)) end", 0);
    assert_eq!(out, "146\n");
}
#[test]
fn interp_call_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(74)) end", 0);
    assert_eq!(out, "148\n");
}
#[test]
fn interp_call_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(75)) end", 0);
    assert_eq!(out, "150\n");
}
#[test]
fn interp_call_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(76)) end", 0);
    assert_eq!(out, "152\n");
}
#[test]
fn interp_call_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(77)) end", 0);
    assert_eq!(out, "154\n");
}
#[test]
fn interp_call_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(78)) end", 0);
    assert_eq!(out, "156\n");
}
#[test]
fn interp_call_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(79)) end", 0);
    assert_eq!(out, "158\n");
}
#[test]
fn interp_call_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(80)) end", 0);
    assert_eq!(out, "160\n");
}
#[test]
fn interp_call_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(81)) end", 0);
    assert_eq!(out, "162\n");
}
#[test]
fn interp_call_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(82)) end", 0);
    assert_eq!(out, "164\n");
}
#[test]
fn interp_call_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(83)) end", 0);
    assert_eq!(out, "166\n");
}
#[test]
fn interp_call_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(84)) end", 0);
    assert_eq!(out, "168\n");
}
#[test]
fn interp_call_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(85)) end", 0);
    assert_eq!(out, "170\n");
}
#[test]
fn interp_call_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(86)) end", 0);
    assert_eq!(out, "172\n");
}
#[test]
fn interp_call_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(87)) end", 0);
    assert_eq!(out, "174\n");
}
#[test]
fn interp_call_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(88)) end", 0);
    assert_eq!(out, "176\n");
}
#[test]
fn interp_call_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(89)) end", 0);
    assert_eq!(out, "178\n");
}
#[test]
fn interp_call_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(90)) end", 0);
    assert_eq!(out, "180\n");
}
#[test]
fn interp_call_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(91)) end", 0);
    assert_eq!(out, "182\n");
}
#[test]
fn interp_call_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(92)) end", 0);
    assert_eq!(out, "184\n");
}
#[test]
fn interp_call_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(93)) end", 0);
    assert_eq!(out, "186\n");
}
#[test]
fn interp_call_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(94)) end", 0);
    assert_eq!(out, "188\n");
}
#[test]
fn interp_call_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(95)) end", 0);
    assert_eq!(out, "190\n");
}
#[test]
fn interp_call_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(96)) end", 0);
    assert_eq!(out, "192\n");
}
#[test]
fn interp_call_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(97)) end", 0);
    assert_eq!(out, "194\n");
}
#[test]
fn interp_call_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(98)) end", 0);
    assert_eq!(out, "196\n");
}
#[test]
fn interp_call_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(99)) end", 0);
    assert_eq!(out, "198\n");
}
#[test]
fn interp_call_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(100)) end", 0);
    assert_eq!(out, "200\n");
}
#[test]
fn interp_call_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(101)) end", 0);
    assert_eq!(out, "202\n");
}
#[test]
fn interp_call_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(102)) end", 0);
    assert_eq!(out, "204\n");
}
#[test]
fn interp_call_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(103)) end", 0);
    assert_eq!(out, "206\n");
}
#[test]
fn interp_call_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(104)) end", 0);
    assert_eq!(out, "208\n");
}
#[test]
fn interp_call_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(105)) end", 0);
    assert_eq!(out, "210\n");
}
#[test]
fn interp_call_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(106)) end", 0);
    assert_eq!(out, "212\n");
}
#[test]
fn interp_call_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(107)) end", 0);
    assert_eq!(out, "214\n");
}
#[test]
fn interp_call_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(108)) end", 0);
    assert_eq!(out, "216\n");
}
#[test]
fn interp_call_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(109)) end", 0);
    assert_eq!(out, "218\n");
}
#[test]
fn interp_call_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(110)) end", 0);
    assert_eq!(out, "220\n");
}
#[test]
fn interp_call_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(111)) end", 0);
    assert_eq!(out, "222\n");
}
#[test]
fn interp_call_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(112)) end", 0);
    assert_eq!(out, "224\n");
}
#[test]
fn interp_call_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(113)) end", 0);
    assert_eq!(out, "226\n");
}
#[test]
fn interp_call_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(114)) end", 0);
    assert_eq!(out, "228\n");
}
#[test]
fn interp_call_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(115)) end", 0);
    assert_eq!(out, "230\n");
}
#[test]
fn interp_call_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(116)) end", 0);
    assert_eq!(out, "232\n");
}
#[test]
fn interp_call_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(117)) end", 0);
    assert_eq!(out, "234\n");
}
#[test]
fn interp_call_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(118)) end", 0);
    assert_eq!(out, "236\n");
}
#[test]
fn interp_call_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(119)) end", 0);
    assert_eq!(out, "238\n");
}
#[test]
fn interp_call_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(120)) end", 0);
    assert_eq!(out, "240\n");
}
#[test]
fn interp_call_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(121)) end", 0);
    assert_eq!(out, "242\n");
}
#[test]
fn interp_call_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(122)) end", 0);
    assert_eq!(out, "244\n");
}
#[test]
fn interp_call_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(123)) end", 0);
    assert_eq!(out, "246\n");
}
#[test]
fn interp_call_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(124)) end", 0);
    assert_eq!(out, "248\n");
}
#[test]
fn interp_call_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(125)) end", 0);
    assert_eq!(out, "250\n");
}
#[test]
fn interp_call_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(126)) end", 0);
    assert_eq!(out, "252\n");
}
#[test]
fn interp_call_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(127)) end", 0);
    assert_eq!(out, "254\n");
}
#[test]
fn interp_call_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(128)) end", 0);
    assert_eq!(out, "256\n");
}
#[test]
fn interp_call_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(129)) end", 0);
    assert_eq!(out, "258\n");
}
#[test]
fn interp_call_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(130)) end", 0);
    assert_eq!(out, "260\n");
}
#[test]
fn interp_call_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(131)) end", 0);
    assert_eq!(out, "262\n");
}
#[test]
fn interp_call_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(132)) end", 0);
    assert_eq!(out, "264\n");
}
#[test]
fn interp_call_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(133)) end", 0);
    assert_eq!(out, "266\n");
}
#[test]
fn interp_call_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(134)) end", 0);
    assert_eq!(out, "268\n");
}
#[test]
fn interp_call_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(135)) end", 0);
    assert_eq!(out, "270\n");
}
#[test]
fn interp_call_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(136)) end", 0);
    assert_eq!(out, "272\n");
}
#[test]
fn interp_call_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(137)) end", 0);
    assert_eq!(out, "274\n");
}
#[test]
fn interp_call_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(138)) end", 0);
    assert_eq!(out, "276\n");
}
#[test]
fn interp_call_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(139)) end", 0);
    assert_eq!(out, "278\n");
}
#[test]
fn interp_call_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(140)) end", 0);
    assert_eq!(out, "280\n");
}
#[test]
fn interp_call_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(141)) end", 0);
    assert_eq!(out, "282\n");
}
#[test]
fn interp_call_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(142)) end", 0);
    assert_eq!(out, "284\n");
}
#[test]
fn interp_call_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(143)) end", 0);
    assert_eq!(out, "286\n");
}
#[test]
fn interp_call_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(144)) end", 0);
    assert_eq!(out, "288\n");
}
#[test]
fn interp_call_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(145)) end", 0);
    assert_eq!(out, "290\n");
}
#[test]
fn interp_call_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(146)) end", 0);
    assert_eq!(out, "292\n");
}
#[test]
fn interp_call_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(147)) end", 0);
    assert_eq!(out, "294\n");
}
#[test]
fn interp_call_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(148)) end", 0);
    assert_eq!(out, "296\n");
}
#[test]
fn interp_call_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(149)) end", 0);
    assert_eq!(out, "298\n");
}
#[test]
fn interp_call_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(150)) end", 0);
    assert_eq!(out, "300\n");
}
#[test]
fn interp_call_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(151)) end", 0);
    assert_eq!(out, "302\n");
}
#[test]
fn interp_call_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(152)) end", 0);
    assert_eq!(out, "304\n");
}
#[test]
fn interp_call_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(153)) end", 0);
    assert_eq!(out, "306\n");
}
#[test]
fn interp_call_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(154)) end", 0);
    assert_eq!(out, "308\n");
}
#[test]
fn interp_call_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(155)) end", 0);
    assert_eq!(out, "310\n");
}
#[test]
fn interp_call_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(156)) end", 0);
    assert_eq!(out, "312\n");
}
#[test]
fn interp_call_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(157)) end", 0);
    assert_eq!(out, "314\n");
}
#[test]
fn interp_call_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(158)) end", 0);
    assert_eq!(out, "316\n");
}
#[test]
fn interp_call_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(159)) end", 0);
    assert_eq!(out, "318\n");
}
#[test]
fn interp_call_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(160)) end", 0);
    assert_eq!(out, "320\n");
}
#[test]
fn interp_call_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(161)) end", 0);
    assert_eq!(out, "322\n");
}
#[test]
fn interp_call_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(162)) end", 0);
    assert_eq!(out, "324\n");
}
#[test]
fn interp_call_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(163)) end", 0);
    assert_eq!(out, "326\n");
}
#[test]
fn interp_call_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(164)) end", 0);
    assert_eq!(out, "328\n");
}
#[test]
fn interp_call_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(165)) end", 0);
    assert_eq!(out, "330\n");
}
#[test]
fn interp_call_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(166)) end", 0);
    assert_eq!(out, "332\n");
}
#[test]
fn interp_call_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(167)) end", 0);
    assert_eq!(out, "334\n");
}
#[test]
fn interp_call_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(168)) end", 0);
    assert_eq!(out, "336\n");
}
#[test]
fn interp_call_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(169)) end", 0);
    assert_eq!(out, "338\n");
}
#[test]
fn interp_call_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(170)) end", 0);
    assert_eq!(out, "340\n");
}
#[test]
fn interp_call_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(171)) end", 0);
    assert_eq!(out, "342\n");
}
#[test]
fn interp_call_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(172)) end", 0);
    assert_eq!(out, "344\n");
}
#[test]
fn interp_call_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(173)) end", 0);
    assert_eq!(out, "346\n");
}
#[test]
fn interp_call_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(174)) end", 0);
    assert_eq!(out, "348\n");
}
#[test]
fn interp_call_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(175)) end", 0);
    assert_eq!(out, "350\n");
}
#[test]
fn interp_call_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(176)) end", 0);
    assert_eq!(out, "352\n");
}
#[test]
fn interp_call_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(177)) end", 0);
    assert_eq!(out, "354\n");
}
#[test]
fn interp_call_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(178)) end", 0);
    assert_eq!(out, "356\n");
}
#[test]
fn interp_call_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(179)) end", 0);
    assert_eq!(out, "358\n");
}
#[test]
fn interp_call_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(180)) end", 0);
    assert_eq!(out, "360\n");
}
#[test]
fn interp_call_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(181)) end", 0);
    assert_eq!(out, "362\n");
}
#[test]
fn interp_call_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(182)) end", 0);
    assert_eq!(out, "364\n");
}
#[test]
fn interp_call_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(183)) end", 0);
    assert_eq!(out, "366\n");
}
#[test]
fn interp_call_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(184)) end", 0);
    assert_eq!(out, "368\n");
}
#[test]
fn interp_call_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(185)) end", 0);
    assert_eq!(out, "370\n");
}
#[test]
fn interp_call_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(186)) end", 0);
    assert_eq!(out, "372\n");
}
#[test]
fn interp_call_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(187)) end", 0);
    assert_eq!(out, "374\n");
}
#[test]
fn interp_call_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(188)) end", 0);
    assert_eq!(out, "376\n");
}
#[test]
fn interp_call_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(189)) end", 0);
    assert_eq!(out, "378\n");
}
#[test]
fn interp_call_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(190)) end", 0);
    assert_eq!(out, "380\n");
}
#[test]
fn interp_call_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(191)) end", 0);
    assert_eq!(out, "382\n");
}
#[test]
fn interp_call_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(192)) end", 0);
    assert_eq!(out, "384\n");
}
#[test]
fn interp_call_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(193)) end", 0);
    assert_eq!(out, "386\n");
}
#[test]
fn interp_call_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(194)) end", 0);
    assert_eq!(out, "388\n");
}
#[test]
fn interp_call_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(195)) end", 0);
    assert_eq!(out, "390\n");
}
#[test]
fn interp_call_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(196)) end", 0);
    assert_eq!(out, "392\n");
}
#[test]
fn interp_call_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(197)) end", 0);
    assert_eq!(out, "394\n");
}
#[test]
fn interp_call_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(198)) end", 0);
    assert_eq!(out, "396\n");
}
#[test]
fn interp_call_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(199)) end", 0);
    assert_eq!(out, "398\n");
}
#[test]
fn interp_call_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(200)) end", 0);
    assert_eq!(out, "400\n");
}
#[test]
fn interp_call_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(201)) end", 0);
    assert_eq!(out, "402\n");
}
#[test]
fn interp_call_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(202)) end", 0);
    assert_eq!(out, "404\n");
}
#[test]
fn interp_call_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(203)) end", 0);
    assert_eq!(out, "406\n");
}
#[test]
fn interp_call_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(204)) end", 0);
    assert_eq!(out, "408\n");
}
#[test]
fn interp_call_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(205)) end", 0);
    assert_eq!(out, "410\n");
}
#[test]
fn interp_call_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(206)) end", 0);
    assert_eq!(out, "412\n");
}
#[test]
fn interp_call_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(207)) end", 0);
    assert_eq!(out, "414\n");
}
#[test]
fn interp_call_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(208)) end", 0);
    assert_eq!(out, "416\n");
}
#[test]
fn interp_call_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(209)) end", 0);
    assert_eq!(out, "418\n");
}
#[test]
fn interp_call_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(210)) end", 0);
    assert_eq!(out, "420\n");
}
#[test]
fn interp_call_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(211)) end", 0);
    assert_eq!(out, "422\n");
}
#[test]
fn interp_call_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(212)) end", 0);
    assert_eq!(out, "424\n");
}
#[test]
fn interp_call_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(213)) end", 0);
    assert_eq!(out, "426\n");
}
#[test]
fn interp_call_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(214)) end", 0);
    assert_eq!(out, "428\n");
}
#[test]
fn interp_call_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(215)) end", 0);
    assert_eq!(out, "430\n");
}
#[test]
fn interp_call_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(216)) end", 0);
    assert_eq!(out, "432\n");
}
#[test]
fn interp_call_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(217)) end", 0);
    assert_eq!(out, "434\n");
}
#[test]
fn interp_call_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(218)) end", 0);
    assert_eq!(out, "436\n");
}
#[test]
fn interp_call_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(219)) end", 0);
    assert_eq!(out, "438\n");
}
#[test]
fn interp_call_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(220)) end", 0);
    assert_eq!(out, "440\n");
}
#[test]
fn interp_call_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(221)) end", 0);
    assert_eq!(out, "442\n");
}
#[test]
fn interp_call_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(222)) end", 0);
    assert_eq!(out, "444\n");
}
#[test]
fn interp_call_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(223)) end", 0);
    assert_eq!(out, "446\n");
}
#[test]
fn interp_call_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(224)) end", 0);
    assert_eq!(out, "448\n");
}
#[test]
fn interp_call_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(225)) end", 0);
    assert_eq!(out, "450\n");
}
#[test]
fn interp_call_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(226)) end", 0);
    assert_eq!(out, "452\n");
}
#[test]
fn interp_call_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(227)) end", 0);
    assert_eq!(out, "454\n");
}
#[test]
fn interp_call_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(228)) end", 0);
    assert_eq!(out, "456\n");
}
#[test]
fn interp_call_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(229)) end", 0);
    assert_eq!(out, "458\n");
}
#[test]
fn interp_call_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(230)) end", 0);
    assert_eq!(out, "460\n");
}
#[test]
fn interp_call_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(231)) end", 0);
    assert_eq!(out, "462\n");
}
#[test]
fn interp_call_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(232)) end", 0);
    assert_eq!(out, "464\n");
}
#[test]
fn interp_call_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(233)) end", 0);
    assert_eq!(out, "466\n");
}
#[test]
fn interp_call_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(234)) end", 0);
    assert_eq!(out, "468\n");
}
#[test]
fn interp_call_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(235)) end", 0);
    assert_eq!(out, "470\n");
}
#[test]
fn interp_call_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(236)) end", 0);
    assert_eq!(out, "472\n");
}
#[test]
fn interp_call_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(237)) end", 0);
    assert_eq!(out, "474\n");
}
#[test]
fn interp_call_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(238)) end", 0);
    assert_eq!(out, "476\n");
}
#[test]
fn interp_call_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(239)) end", 0);
    assert_eq!(out, "478\n");
}
#[test]
fn interp_call_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(240)) end", 0);
    assert_eq!(out, "480\n");
}
#[test]
fn interp_call_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(241)) end", 0);
    assert_eq!(out, "482\n");
}
#[test]
fn interp_call_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(242)) end", 0);
    assert_eq!(out, "484\n");
}
#[test]
fn interp_call_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(243)) end", 0);
    assert_eq!(out, "486\n");
}
#[test]
fn interp_call_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(244)) end", 0);
    assert_eq!(out, "488\n");
}
#[test]
fn interp_call_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(245)) end", 0);
    assert_eq!(out, "490\n");
}
#[test]
fn interp_call_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(246)) end", 0);
    assert_eq!(out, "492\n");
}
#[test]
fn interp_call_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(247)) end", 0);
    assert_eq!(out, "494\n");
}
#[test]
fn interp_call_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(248)) end", 0);
    assert_eq!(out, "496\n");
}
#[test]
fn interp_call_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(249)) end", 0);
    assert_eq!(out, "498\n");
}
#[test]
fn interp_call_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(250)) end", 0);
    assert_eq!(out, "500\n");
}
#[test]
fn interp_call_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(251)) end", 0);
    assert_eq!(out, "502\n");
}
#[test]
fn interp_call_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(252)) end", 0);
    assert_eq!(out, "504\n");
}
#[test]
fn interp_call_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(253)) end", 0);
    assert_eq!(out, "506\n");
}
#[test]
fn interp_call_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(254)) end", 0);
    assert_eq!(out, "508\n");
}
#[test]
fn interp_call_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(255)) end", 0);
    assert_eq!(out, "510\n");
}
#[test]
fn interp_call_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(256)) end", 0);
    assert_eq!(out, "512\n");
}
#[test]
fn interp_call_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(257)) end", 0);
    assert_eq!(out, "514\n");
}
#[test]
fn interp_call_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(258)) end", 0);
    assert_eq!(out, "516\n");
}
#[test]
fn interp_call_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(259)) end", 0);
    assert_eq!(out, "518\n");
}
#[test]
fn interp_call_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(260)) end", 0);
    assert_eq!(out, "520\n");
}
#[test]
fn interp_call_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(261)) end", 0);
    assert_eq!(out, "522\n");
}
#[test]
fn interp_call_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(262)) end", 0);
    assert_eq!(out, "524\n");
}
#[test]
fn interp_call_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(263)) end", 0);
    assert_eq!(out, "526\n");
}
#[test]
fn interp_call_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(264)) end", 0);
    assert_eq!(out, "528\n");
}
#[test]
fn interp_call_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(265)) end", 0);
    assert_eq!(out, "530\n");
}
#[test]
fn interp_call_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(266)) end", 0);
    assert_eq!(out, "532\n");
}
#[test]
fn interp_call_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(267)) end", 0);
    assert_eq!(out, "534\n");
}
#[test]
fn interp_call_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(268)) end", 0);
    assert_eq!(out, "536\n");
}
#[test]
fn interp_call_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(269)) end", 0);
    assert_eq!(out, "538\n");
}
#[test]
fn interp_call_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(270)) end", 0);
    assert_eq!(out, "540\n");
}
#[test]
fn interp_call_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(271)) end", 0);
    assert_eq!(out, "542\n");
}
#[test]
fn interp_call_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(272)) end", 0);
    assert_eq!(out, "544\n");
}
#[test]
fn interp_call_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(273)) end", 0);
    assert_eq!(out, "546\n");
}
#[test]
fn interp_call_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(274)) end", 0);
    assert_eq!(out, "548\n");
}
#[test]
fn interp_call_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(275)) end", 0);
    assert_eq!(out, "550\n");
}
#[test]
fn interp_call_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(276)) end", 0);
    assert_eq!(out, "552\n");
}
#[test]
fn interp_call_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(277)) end", 0);
    assert_eq!(out, "554\n");
}
#[test]
fn interp_call_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(278)) end", 0);
    assert_eq!(out, "556\n");
}
#[test]
fn interp_call_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(279)) end", 0);
    assert_eq!(out, "558\n");
}
#[test]
fn interp_call_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(280)) end", 0);
    assert_eq!(out, "560\n");
}
#[test]
fn interp_call_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(281)) end", 0);
    assert_eq!(out, "562\n");
}
#[test]
fn interp_call_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(282)) end", 0);
    assert_eq!(out, "564\n");
}
#[test]
fn interp_call_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(283)) end", 0);
    assert_eq!(out, "566\n");
}
#[test]
fn interp_call_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(284)) end", 0);
    assert_eq!(out, "568\n");
}
#[test]
fn interp_call_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(285)) end", 0);
    assert_eq!(out, "570\n");
}
#[test]
fn interp_call_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(286)) end", 0);
    assert_eq!(out, "572\n");
}
#[test]
fn interp_call_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(287)) end", 0);
    assert_eq!(out, "574\n");
}
#[test]
fn interp_call_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(288)) end", 0);
    assert_eq!(out, "576\n");
}
#[test]
fn interp_call_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(289)) end", 0);
    assert_eq!(out, "578\n");
}
#[test]
fn interp_call_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(290)) end", 0);
    assert_eq!(out, "580\n");
}
#[test]
fn interp_call_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(291)) end", 0);
    assert_eq!(out, "582\n");
}
#[test]
fn interp_call_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(292)) end", 0);
    assert_eq!(out, "584\n");
}
#[test]
fn interp_call_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(293)) end", 0);
    assert_eq!(out, "586\n");
}
#[test]
fn interp_call_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(294)) end", 0);
    assert_eq!(out, "588\n");
}
#[test]
fn interp_call_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(295)) end", 0);
    assert_eq!(out, "590\n");
}
#[test]
fn interp_call_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(296)) end", 0);
    assert_eq!(out, "592\n");
}
#[test]
fn interp_call_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(297)) end", 0);
    assert_eq!(out, "594\n");
}
#[test]
fn interp_call_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(298)) end", 0);
    assert_eq!(out, "596\n");
}
#[test]
fn interp_call_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(299)) end", 0);
    assert_eq!(out, "598\n");
}
#[test]
fn interp_call_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(300)) end", 0);
    assert_eq!(out, "600\n");
}
#[test]
fn interp_call_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(301)) end", 0);
    assert_eq!(out, "602\n");
}
#[test]
fn interp_call_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(302)) end", 0);
    assert_eq!(out, "604\n");
}
#[test]
fn interp_call_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(303)) end", 0);
    assert_eq!(out, "606\n");
}
#[test]
fn interp_call_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(304)) end", 0);
    assert_eq!(out, "608\n");
}
#[test]
fn interp_call_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(305)) end", 0);
    assert_eq!(out, "610\n");
}
#[test]
fn interp_call_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(306)) end", 0);
    assert_eq!(out, "612\n");
}
#[test]
fn interp_call_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(307)) end", 0);
    assert_eq!(out, "614\n");
}
#[test]
fn interp_call_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(308)) end", 0);
    assert_eq!(out, "616\n");
}
#[test]
fn interp_call_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(309)) end", 0);
    assert_eq!(out, "618\n");
}
#[test]
fn interp_call_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(310)) end", 0);
    assert_eq!(out, "620\n");
}
#[test]
fn interp_call_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(311)) end", 0);
    assert_eq!(out, "622\n");
}
#[test]
fn interp_call_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(312)) end", 0);
    assert_eq!(out, "624\n");
}
#[test]
fn interp_call_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(313)) end", 0);
    assert_eq!(out, "626\n");
}
#[test]
fn interp_call_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(314)) end", 0);
    assert_eq!(out, "628\n");
}
#[test]
fn interp_call_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(315)) end", 0);
    assert_eq!(out, "630\n");
}
#[test]
fn interp_call_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(316)) end", 0);
    assert_eq!(out, "632\n");
}
#[test]
fn interp_call_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(317)) end", 0);
    assert_eq!(out, "634\n");
}
#[test]
fn interp_call_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(318)) end", 0);
    assert_eq!(out, "636\n");
}
#[test]
fn interp_call_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(319)) end", 0);
    assert_eq!(out, "638\n");
}
#[test]
fn interp_call_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(320)) end", 0);
    assert_eq!(out, "640\n");
}
#[test]
fn interp_call_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(321)) end", 0);
    assert_eq!(out, "642\n");
}
#[test]
fn interp_call_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(322)) end", 0);
    assert_eq!(out, "644\n");
}
#[test]
fn interp_call_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(323)) end", 0);
    assert_eq!(out, "646\n");
}
#[test]
fn interp_call_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(324)) end", 0);
    assert_eq!(out, "648\n");
}
#[test]
fn interp_call_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(325)) end", 0);
    assert_eq!(out, "650\n");
}
#[test]
fn interp_call_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(326)) end", 0);
    assert_eq!(out, "652\n");
}
#[test]
fn interp_call_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(327)) end", 0);
    assert_eq!(out, "654\n");
}
#[test]
fn interp_call_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(328)) end", 0);
    assert_eq!(out, "656\n");
}
#[test]
fn interp_call_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(329)) end", 0);
    assert_eq!(out, "658\n");
}
#[test]
fn interp_call_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(330)) end", 0);
    assert_eq!(out, "660\n");
}
#[test]
fn interp_call_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(331)) end", 0);
    assert_eq!(out, "662\n");
}
#[test]
fn interp_call_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(332)) end", 0);
    assert_eq!(out, "664\n");
}
#[test]
fn interp_call_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(333)) end", 0);
    assert_eq!(out, "666\n");
}
#[test]
fn interp_call_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(334)) end", 0);
    assert_eq!(out, "668\n");
}
#[test]
fn interp_call_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(335)) end", 0);
    assert_eq!(out, "670\n");
}
#[test]
fn interp_call_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(336)) end", 0);
    assert_eq!(out, "672\n");
}
#[test]
fn interp_call_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(337)) end", 0);
    assert_eq!(out, "674\n");
}
#[test]
fn interp_call_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(338)) end", 0);
    assert_eq!(out, "676\n");
}
#[test]
fn interp_call_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(339)) end", 0);
    assert_eq!(out, "678\n");
}
#[test]
fn interp_call_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(340)) end", 0);
    assert_eq!(out, "680\n");
}
#[test]
fn interp_call_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(341)) end", 0);
    assert_eq!(out, "682\n");
}
#[test]
fn interp_call_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(342)) end", 0);
    assert_eq!(out, "684\n");
}
#[test]
fn interp_call_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(343)) end", 0);
    assert_eq!(out, "686\n");
}
#[test]
fn interp_call_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(344)) end", 0);
    assert_eq!(out, "688\n");
}
#[test]
fn interp_call_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(345)) end", 0);
    assert_eq!(out, "690\n");
}
#[test]
fn interp_call_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(346)) end", 0);
    assert_eq!(out, "692\n");
}
#[test]
fn interp_call_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(347)) end", 0);
    assert_eq!(out, "694\n");
}
#[test]
fn interp_call_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(348)) end", 0);
    assert_eq!(out, "696\n");
}
#[test]
fn interp_call_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(349)) end", 0);
    assert_eq!(out, "698\n");
}
#[test]
fn interp_call_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(350)) end", 0);
    assert_eq!(out, "700\n");
}
#[test]
fn interp_call_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(351)) end", 0);
    assert_eq!(out, "702\n");
}
#[test]
fn interp_call_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(352)) end", 0);
    assert_eq!(out, "704\n");
}
#[test]
fn interp_call_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(353)) end", 0);
    assert_eq!(out, "706\n");
}
#[test]
fn interp_call_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(354)) end", 0);
    assert_eq!(out, "708\n");
}
#[test]
fn interp_call_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(355)) end", 0);
    assert_eq!(out, "710\n");
}
#[test]
fn interp_call_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(356)) end", 0);
    assert_eq!(out, "712\n");
}
#[test]
fn interp_call_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(357)) end", 0);
    assert_eq!(out, "714\n");
}
#[test]
fn interp_call_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(358)) end", 0);
    assert_eq!(out, "716\n");
}
#[test]
fn interp_call_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(359)) end", 0);
    assert_eq!(out, "718\n");
}
#[test]
fn interp_call_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(360)) end", 0);
    assert_eq!(out, "720\n");
}
#[test]
fn interp_call_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(361)) end", 0);
    assert_eq!(out, "722\n");
}
#[test]
fn interp_call_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(362)) end", 0);
    assert_eq!(out, "724\n");
}
#[test]
fn interp_call_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(363)) end", 0);
    assert_eq!(out, "726\n");
}
#[test]
fn interp_call_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(364)) end", 0);
    assert_eq!(out, "728\n");
}
#[test]
fn interp_call_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(365)) end", 0);
    assert_eq!(out, "730\n");
}
#[test]
fn interp_call_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(366)) end", 0);
    assert_eq!(out, "732\n");
}
#[test]
fn interp_call_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(367)) end", 0);
    assert_eq!(out, "734\n");
}
#[test]
fn interp_call_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(368)) end", 0);
    assert_eq!(out, "736\n");
}
#[test]
fn interp_call_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(369)) end", 0);
    assert_eq!(out, "738\n");
}
#[test]
fn interp_call_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(370)) end", 0);
    assert_eq!(out, "740\n");
}
#[test]
fn interp_call_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(371)) end", 0);
    assert_eq!(out, "742\n");
}
#[test]
fn interp_call_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(372)) end", 0);
    assert_eq!(out, "744\n");
}
#[test]
fn interp_call_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(373)) end", 0);
    assert_eq!(out, "746\n");
}
#[test]
fn interp_call_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(374)) end", 0);
    assert_eq!(out, "748\n");
}
#[test]
fn interp_call_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(375)) end", 0);
    assert_eq!(out, "750\n");
}
#[test]
fn interp_call_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(376)) end", 0);
    assert_eq!(out, "752\n");
}
#[test]
fn interp_call_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(377)) end", 0);
    assert_eq!(out, "754\n");
}
#[test]
fn interp_call_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(378)) end", 0);
    assert_eq!(out, "756\n");
}
#[test]
fn interp_call_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(379)) end", 0);
    assert_eq!(out, "758\n");
}
#[test]
fn interp_call_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(380)) end", 0);
    assert_eq!(out, "760\n");
}
#[test]
fn interp_call_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(381)) end", 0);
    assert_eq!(out, "762\n");
}
#[test]
fn interp_call_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(382)) end", 0);
    assert_eq!(out, "764\n");
}
#[test]
fn interp_call_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(383)) end", 0);
    assert_eq!(out, "766\n");
}
#[test]
fn interp_call_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(384)) end", 0);
    assert_eq!(out, "768\n");
}
#[test]
fn interp_call_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(385)) end", 0);
    assert_eq!(out, "770\n");
}
#[test]
fn interp_call_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(386)) end", 0);
    assert_eq!(out, "772\n");
}
#[test]
fn interp_call_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(387)) end", 0);
    assert_eq!(out, "774\n");
}
#[test]
fn interp_call_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(388)) end", 0);
    assert_eq!(out, "776\n");
}
#[test]
fn interp_call_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(389)) end", 0);
    assert_eq!(out, "778\n");
}
#[test]
fn interp_call_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(390)) end", 0);
    assert_eq!(out, "780\n");
}
#[test]
fn interp_call_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(391)) end", 0);
    assert_eq!(out, "782\n");
}
#[test]
fn interp_call_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(392)) end", 0);
    assert_eq!(out, "784\n");
}
#[test]
fn interp_call_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(393)) end", 0);
    assert_eq!(out, "786\n");
}
#[test]
fn interp_call_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(394)) end", 0);
    assert_eq!(out, "788\n");
}
#[test]
fn interp_call_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(395)) end", 0);
    assert_eq!(out, "790\n");
}
#[test]
fn interp_call_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(396)) end", 0);
    assert_eq!(out, "792\n");
}
#[test]
fn interp_call_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(397)) end", 0);
    assert_eq!(out, "794\n");
}
#[test]
fn interp_call_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(398)) end", 0);
    assert_eq!(out, "796\n");
}
#[test]
fn interp_call_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(399)) end", 0);
    assert_eq!(out, "798\n");
}
#[test]
fn interp_call_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(400)) end", 0);
    assert_eq!(out, "800\n");
}
#[test]
fn interp_call_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(401)) end", 0);
    assert_eq!(out, "802\n");
}
#[test]
fn interp_call_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(402)) end", 0);
    assert_eq!(out, "804\n");
}
#[test]
fn interp_call_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(403)) end", 0);
    assert_eq!(out, "806\n");
}
#[test]
fn interp_call_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(404)) end", 0);
    assert_eq!(out, "808\n");
}
#[test]
fn interp_call_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(405)) end", 0);
    assert_eq!(out, "810\n");
}
#[test]
fn interp_call_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(406)) end", 0);
    assert_eq!(out, "812\n");
}
#[test]
fn interp_call_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(407)) end", 0);
    assert_eq!(out, "814\n");
}
#[test]
fn interp_call_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(408)) end", 0);
    assert_eq!(out, "816\n");
}
#[test]
fn interp_call_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(409)) end", 0);
    assert_eq!(out, "818\n");
}
#[test]
fn interp_call_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(410)) end", 0);
    assert_eq!(out, "820\n");
}
#[test]
fn interp_call_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(411)) end", 0);
    assert_eq!(out, "822\n");
}
#[test]
fn interp_call_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(412)) end", 0);
    assert_eq!(out, "824\n");
}
#[test]
fn interp_call_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(413)) end", 0);
    assert_eq!(out, "826\n");
}
#[test]
fn interp_call_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(414)) end", 0);
    assert_eq!(out, "828\n");
}
#[test]
fn interp_call_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(415)) end", 0);
    assert_eq!(out, "830\n");
}
#[test]
fn interp_call_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(416)) end", 0);
    assert_eq!(out, "832\n");
}
#[test]
fn interp_call_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(417)) end", 0);
    assert_eq!(out, "834\n");
}
#[test]
fn interp_call_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(418)) end", 0);
    assert_eq!(out, "836\n");
}
#[test]
fn interp_call_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(419)) end", 0);
    assert_eq!(out, "838\n");
}
#[test]
fn interp_call_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(420)) end", 0);
    assert_eq!(out, "840\n");
}
#[test]
fn interp_call_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(421)) end", 0);
    assert_eq!(out, "842\n");
}
#[test]
fn interp_call_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(422)) end", 0);
    assert_eq!(out, "844\n");
}
#[test]
fn interp_call_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(423)) end", 0);
    assert_eq!(out, "846\n");
}
#[test]
fn interp_call_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(424)) end", 0);
    assert_eq!(out, "848\n");
}
#[test]
fn interp_call_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(425)) end", 0);
    assert_eq!(out, "850\n");
}
#[test]
fn interp_call_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(426)) end", 0);
    assert_eq!(out, "852\n");
}
#[test]
fn interp_call_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(427)) end", 0);
    assert_eq!(out, "854\n");
}
#[test]
fn interp_call_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(428)) end", 0);
    assert_eq!(out, "856\n");
}
#[test]
fn interp_call_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(429)) end", 0);
    assert_eq!(out, "858\n");
}
#[test]
fn interp_call_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(430)) end", 0);
    assert_eq!(out, "860\n");
}
#[test]
fn interp_call_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(431)) end", 0);
    assert_eq!(out, "862\n");
}
#[test]
fn interp_call_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(432)) end", 0);
    assert_eq!(out, "864\n");
}
#[test]
fn interp_call_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(433)) end", 0);
    assert_eq!(out, "866\n");
}
#[test]
fn interp_call_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(434)) end", 0);
    assert_eq!(out, "868\n");
}
#[test]
fn interp_call_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(435)) end", 0);
    assert_eq!(out, "870\n");
}
#[test]
fn interp_call_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(436)) end", 0);
    assert_eq!(out, "872\n");
}
#[test]
fn interp_call_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(437)) end", 0);
    assert_eq!(out, "874\n");
}
#[test]
fn interp_call_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(438)) end", 0);
    assert_eq!(out, "876\n");
}
#[test]
fn interp_call_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(439)) end", 0);
    assert_eq!(out, "878\n");
}
#[test]
fn interp_call_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(440)) end", 0);
    assert_eq!(out, "880\n");
}
#[test]
fn interp_call_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(441)) end", 0);
    assert_eq!(out, "882\n");
}
#[test]
fn interp_call_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(442)) end", 0);
    assert_eq!(out, "884\n");
}
#[test]
fn interp_call_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(443)) end", 0);
    assert_eq!(out, "886\n");
}
#[test]
fn interp_call_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(444)) end", 0);
    assert_eq!(out, "888\n");
}
#[test]
fn interp_call_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(445)) end", 0);
    assert_eq!(out, "890\n");
}
#[test]
fn interp_call_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(446)) end", 0);
    assert_eq!(out, "892\n");
}
#[test]
fn interp_call_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(447)) end", 0);
    assert_eq!(out, "894\n");
}
#[test]
fn interp_call_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(448)) end", 0);
    assert_eq!(out, "896\n");
}
#[test]
fn interp_call_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(449)) end", 0);
    assert_eq!(out, "898\n");
}
#[test]
fn interp_call_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(450)) end", 0);
    assert_eq!(out, "900\n");
}
#[test]
fn interp_call_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(451)) end", 0);
    assert_eq!(out, "902\n");
}
#[test]
fn interp_call_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(452)) end", 0);
    assert_eq!(out, "904\n");
}
#[test]
fn interp_call_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(453)) end", 0);
    assert_eq!(out, "906\n");
}
#[test]
fn interp_call_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(454)) end", 0);
    assert_eq!(out, "908\n");
}
#[test]
fn interp_call_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(455)) end", 0);
    assert_eq!(out, "910\n");
}
#[test]
fn interp_call_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(456)) end", 0);
    assert_eq!(out, "912\n");
}
#[test]
fn interp_call_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(457)) end", 0);
    assert_eq!(out, "914\n");
}
#[test]
fn interp_call_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(458)) end", 0);
    assert_eq!(out, "916\n");
}
#[test]
fn interp_call_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(459)) end", 0);
    assert_eq!(out, "918\n");
}
#[test]
fn interp_call_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(460)) end", 0);
    assert_eq!(out, "920\n");
}
#[test]
fn interp_call_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(461)) end", 0);
    assert_eq!(out, "922\n");
}
#[test]
fn interp_call_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(462)) end", 0);
    assert_eq!(out, "924\n");
}
#[test]
fn interp_call_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(463)) end", 0);
    assert_eq!(out, "926\n");
}
#[test]
fn interp_call_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(464)) end", 0);
    assert_eq!(out, "928\n");
}
#[test]
fn interp_call_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(465)) end", 0);
    assert_eq!(out, "930\n");
}
#[test]
fn interp_call_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(466)) end", 0);
    assert_eq!(out, "932\n");
}
#[test]
fn interp_call_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(467)) end", 0);
    assert_eq!(out, "934\n");
}
#[test]
fn interp_call_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(468)) end", 0);
    assert_eq!(out, "936\n");
}
#[test]
fn interp_call_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(469)) end", 0);
    assert_eq!(out, "938\n");
}
#[test]
fn interp_call_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(470)) end", 0);
    assert_eq!(out, "940\n");
}
#[test]
fn interp_call_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(471)) end", 0);
    assert_eq!(out, "942\n");
}
#[test]
fn interp_call_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(472)) end", 0);
    assert_eq!(out, "944\n");
}
#[test]
fn interp_call_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(473)) end", 0);
    assert_eq!(out, "946\n");
}
#[test]
fn interp_call_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(474)) end", 0);
    assert_eq!(out, "948\n");
}
#[test]
fn interp_call_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(475)) end", 0);
    assert_eq!(out, "950\n");
}
#[test]
fn interp_call_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(476)) end", 0);
    assert_eq!(out, "952\n");
}
#[test]
fn interp_call_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(477)) end", 0);
    assert_eq!(out, "954\n");
}
#[test]
fn interp_call_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(478)) end", 0);
    assert_eq!(out, "956\n");
}
#[test]
fn interp_call_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(479)) end", 0);
    assert_eq!(out, "958\n");
}
#[test]
fn interp_call_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(480)) end", 0);
    assert_eq!(out, "960\n");
}
#[test]
fn interp_call_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(481)) end", 0);
    assert_eq!(out, "962\n");
}
#[test]
fn interp_call_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(482)) end", 0);
    assert_eq!(out, "964\n");
}
#[test]
fn interp_call_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(483)) end", 0);
    assert_eq!(out, "966\n");
}
#[test]
fn interp_call_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(484)) end", 0);
    assert_eq!(out, "968\n");
}
#[test]
fn interp_call_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(485)) end", 0);
    assert_eq!(out, "970\n");
}
#[test]
fn interp_call_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(486)) end", 0);
    assert_eq!(out, "972\n");
}
#[test]
fn interp_call_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(487)) end", 0);
    assert_eq!(out, "974\n");
}
#[test]
fn interp_call_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(488)) end", 0);
    assert_eq!(out, "976\n");
}
#[test]
fn interp_call_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(489)) end", 0);
    assert_eq!(out, "978\n");
}
#[test]
fn interp_call_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(490)) end", 0);
    assert_eq!(out, "980\n");
}
#[test]
fn interp_call_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(491)) end", 0);
    assert_eq!(out, "982\n");
}
#[test]
fn interp_call_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(492)) end", 0);
    assert_eq!(out, "984\n");
}
#[test]
fn interp_call_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(493)) end", 0);
    assert_eq!(out, "986\n");
}
#[test]
fn interp_call_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(494)) end", 0);
    assert_eq!(out, "988\n");
}
#[test]
fn interp_call_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(495)) end", 0);
    assert_eq!(out, "990\n");
}
#[test]
fn interp_call_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(496)) end", 0);
    assert_eq!(out, "992\n");
}
#[test]
fn interp_call_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(497)) end", 0);
    assert_eq!(out, "994\n");
}
#[test]
fn interp_call_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(498)) end", 0);
    assert_eq!(out, "996\n");
}
#[test]
fn interp_call_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn double(x) return x * 2 end fn main() print(double(499)) end", 0);
    assert_eq!(out, "998\n");
}
#[test]
fn interp_str_concat_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "0") end"#, 0);
    assert_eq!(out, "a0\n");
}
#[test]
fn interp_str_concat_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "1") end"#, 0);
    assert_eq!(out, "a1\n");
}
#[test]
fn interp_str_concat_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "2") end"#, 0);
    assert_eq!(out, "a2\n");
}
#[test]
fn interp_str_concat_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "3") end"#, 0);
    assert_eq!(out, "a3\n");
}
#[test]
fn interp_str_concat_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "4") end"#, 0);
    assert_eq!(out, "a4\n");
}
#[test]
fn interp_str_concat_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "5") end"#, 0);
    assert_eq!(out, "a5\n");
}
#[test]
fn interp_str_concat_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "6") end"#, 0);
    assert_eq!(out, "a6\n");
}
#[test]
fn interp_str_concat_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "7") end"#, 0);
    assert_eq!(out, "a7\n");
}
#[test]
fn interp_str_concat_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "8") end"#, 0);
    assert_eq!(out, "a8\n");
}
#[test]
fn interp_str_concat_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "9") end"#, 0);
    assert_eq!(out, "a9\n");
}
#[test]
fn interp_str_concat_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "10") end"#, 0);
    assert_eq!(out, "a10\n");
}
#[test]
fn interp_str_concat_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "11") end"#, 0);
    assert_eq!(out, "a11\n");
}
#[test]
fn interp_str_concat_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "12") end"#, 0);
    assert_eq!(out, "a12\n");
}
#[test]
fn interp_str_concat_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "13") end"#, 0);
    assert_eq!(out, "a13\n");
}
#[test]
fn interp_str_concat_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "14") end"#, 0);
    assert_eq!(out, "a14\n");
}
#[test]
fn interp_str_concat_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "15") end"#, 0);
    assert_eq!(out, "a15\n");
}
#[test]
fn interp_str_concat_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "16") end"#, 0);
    assert_eq!(out, "a16\n");
}
#[test]
fn interp_str_concat_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "17") end"#, 0);
    assert_eq!(out, "a17\n");
}
#[test]
fn interp_str_concat_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "18") end"#, 0);
    assert_eq!(out, "a18\n");
}
#[test]
fn interp_str_concat_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "19") end"#, 0);
    assert_eq!(out, "a19\n");
}
#[test]
fn interp_str_concat_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "20") end"#, 0);
    assert_eq!(out, "a20\n");
}
#[test]
fn interp_str_concat_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "21") end"#, 0);
    assert_eq!(out, "a21\n");
}
#[test]
fn interp_str_concat_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "22") end"#, 0);
    assert_eq!(out, "a22\n");
}
#[test]
fn interp_str_concat_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "23") end"#, 0);
    assert_eq!(out, "a23\n");
}
#[test]
fn interp_str_concat_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "24") end"#, 0);
    assert_eq!(out, "a24\n");
}
#[test]
fn interp_str_concat_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "25") end"#, 0);
    assert_eq!(out, "a25\n");
}
#[test]
fn interp_str_concat_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "26") end"#, 0);
    assert_eq!(out, "a26\n");
}
#[test]
fn interp_str_concat_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "27") end"#, 0);
    assert_eq!(out, "a27\n");
}
#[test]
fn interp_str_concat_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "28") end"#, 0);
    assert_eq!(out, "a28\n");
}
#[test]
fn interp_str_concat_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "29") end"#, 0);
    assert_eq!(out, "a29\n");
}
#[test]
fn interp_str_concat_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "30") end"#, 0);
    assert_eq!(out, "a30\n");
}
#[test]
fn interp_str_concat_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "31") end"#, 0);
    assert_eq!(out, "a31\n");
}
#[test]
fn interp_str_concat_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "32") end"#, 0);
    assert_eq!(out, "a32\n");
}
#[test]
fn interp_str_concat_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "33") end"#, 0);
    assert_eq!(out, "a33\n");
}
#[test]
fn interp_str_concat_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "34") end"#, 0);
    assert_eq!(out, "a34\n");
}
#[test]
fn interp_str_concat_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "35") end"#, 0);
    assert_eq!(out, "a35\n");
}
#[test]
fn interp_str_concat_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "36") end"#, 0);
    assert_eq!(out, "a36\n");
}
#[test]
fn interp_str_concat_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "37") end"#, 0);
    assert_eq!(out, "a37\n");
}
#[test]
fn interp_str_concat_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "38") end"#, 0);
    assert_eq!(out, "a38\n");
}
#[test]
fn interp_str_concat_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "39") end"#, 0);
    assert_eq!(out, "a39\n");
}
#[test]
fn interp_str_concat_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "40") end"#, 0);
    assert_eq!(out, "a40\n");
}
#[test]
fn interp_str_concat_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "41") end"#, 0);
    assert_eq!(out, "a41\n");
}
#[test]
fn interp_str_concat_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "42") end"#, 0);
    assert_eq!(out, "a42\n");
}
#[test]
fn interp_str_concat_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "43") end"#, 0);
    assert_eq!(out, "a43\n");
}
#[test]
fn interp_str_concat_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "44") end"#, 0);
    assert_eq!(out, "a44\n");
}
#[test]
fn interp_str_concat_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "45") end"#, 0);
    assert_eq!(out, "a45\n");
}
#[test]
fn interp_str_concat_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "46") end"#, 0);
    assert_eq!(out, "a46\n");
}
#[test]
fn interp_str_concat_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "47") end"#, 0);
    assert_eq!(out, "a47\n");
}
#[test]
fn interp_str_concat_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "48") end"#, 0);
    assert_eq!(out, "a48\n");
}
#[test]
fn interp_str_concat_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "49") end"#, 0);
    assert_eq!(out, "a49\n");
}
#[test]
fn interp_str_concat_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "50") end"#, 0);
    assert_eq!(out, "a50\n");
}
#[test]
fn interp_str_concat_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "51") end"#, 0);
    assert_eq!(out, "a51\n");
}
#[test]
fn interp_str_concat_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "52") end"#, 0);
    assert_eq!(out, "a52\n");
}
#[test]
fn interp_str_concat_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "53") end"#, 0);
    assert_eq!(out, "a53\n");
}
#[test]
fn interp_str_concat_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "54") end"#, 0);
    assert_eq!(out, "a54\n");
}
#[test]
fn interp_str_concat_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "55") end"#, 0);
    assert_eq!(out, "a55\n");
}
#[test]
fn interp_str_concat_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "56") end"#, 0);
    assert_eq!(out, "a56\n");
}
#[test]
fn interp_str_concat_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "57") end"#, 0);
    assert_eq!(out, "a57\n");
}
#[test]
fn interp_str_concat_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "58") end"#, 0);
    assert_eq!(out, "a58\n");
}
#[test]
fn interp_str_concat_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "59") end"#, 0);
    assert_eq!(out, "a59\n");
}
#[test]
fn interp_str_concat_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "60") end"#, 0);
    assert_eq!(out, "a60\n");
}
#[test]
fn interp_str_concat_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "61") end"#, 0);
    assert_eq!(out, "a61\n");
}
#[test]
fn interp_str_concat_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "62") end"#, 0);
    assert_eq!(out, "a62\n");
}
#[test]
fn interp_str_concat_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "63") end"#, 0);
    assert_eq!(out, "a63\n");
}
#[test]
fn interp_str_concat_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "64") end"#, 0);
    assert_eq!(out, "a64\n");
}
#[test]
fn interp_str_concat_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "65") end"#, 0);
    assert_eq!(out, "a65\n");
}
#[test]
fn interp_str_concat_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "66") end"#, 0);
    assert_eq!(out, "a66\n");
}
#[test]
fn interp_str_concat_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "67") end"#, 0);
    assert_eq!(out, "a67\n");
}
#[test]
fn interp_str_concat_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "68") end"#, 0);
    assert_eq!(out, "a68\n");
}
#[test]
fn interp_str_concat_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "69") end"#, 0);
    assert_eq!(out, "a69\n");
}
#[test]
fn interp_str_concat_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "70") end"#, 0);
    assert_eq!(out, "a70\n");
}
#[test]
fn interp_str_concat_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "71") end"#, 0);
    assert_eq!(out, "a71\n");
}
#[test]
fn interp_str_concat_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "72") end"#, 0);
    assert_eq!(out, "a72\n");
}
#[test]
fn interp_str_concat_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "73") end"#, 0);
    assert_eq!(out, "a73\n");
}
#[test]
fn interp_str_concat_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "74") end"#, 0);
    assert_eq!(out, "a74\n");
}
#[test]
fn interp_str_concat_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "75") end"#, 0);
    assert_eq!(out, "a75\n");
}
#[test]
fn interp_str_concat_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "76") end"#, 0);
    assert_eq!(out, "a76\n");
}
#[test]
fn interp_str_concat_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "77") end"#, 0);
    assert_eq!(out, "a77\n");
}
#[test]
fn interp_str_concat_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "78") end"#, 0);
    assert_eq!(out, "a78\n");
}
#[test]
fn interp_str_concat_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "79") end"#, 0);
    assert_eq!(out, "a79\n");
}
#[test]
fn interp_str_concat_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "80") end"#, 0);
    assert_eq!(out, "a80\n");
}
#[test]
fn interp_str_concat_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "81") end"#, 0);
    assert_eq!(out, "a81\n");
}
#[test]
fn interp_str_concat_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "82") end"#, 0);
    assert_eq!(out, "a82\n");
}
#[test]
fn interp_str_concat_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "83") end"#, 0);
    assert_eq!(out, "a83\n");
}
#[test]
fn interp_str_concat_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "84") end"#, 0);
    assert_eq!(out, "a84\n");
}
#[test]
fn interp_str_concat_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "85") end"#, 0);
    assert_eq!(out, "a85\n");
}
#[test]
fn interp_str_concat_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "86") end"#, 0);
    assert_eq!(out, "a86\n");
}
#[test]
fn interp_str_concat_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "87") end"#, 0);
    assert_eq!(out, "a87\n");
}
#[test]
fn interp_str_concat_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "88") end"#, 0);
    assert_eq!(out, "a88\n");
}
#[test]
fn interp_str_concat_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "89") end"#, 0);
    assert_eq!(out, "a89\n");
}
#[test]
fn interp_str_concat_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "90") end"#, 0);
    assert_eq!(out, "a90\n");
}
#[test]
fn interp_str_concat_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "91") end"#, 0);
    assert_eq!(out, "a91\n");
}
#[test]
fn interp_str_concat_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "92") end"#, 0);
    assert_eq!(out, "a92\n");
}
#[test]
fn interp_str_concat_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "93") end"#, 0);
    assert_eq!(out, "a93\n");
}
#[test]
fn interp_str_concat_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "94") end"#, 0);
    assert_eq!(out, "a94\n");
}
#[test]
fn interp_str_concat_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "95") end"#, 0);
    assert_eq!(out, "a95\n");
}
#[test]
fn interp_str_concat_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "96") end"#, 0);
    assert_eq!(out, "a96\n");
}
#[test]
fn interp_str_concat_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "97") end"#, 0);
    assert_eq!(out, "a97\n");
}
#[test]
fn interp_str_concat_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "98") end"#, 0);
    assert_eq!(out, "a98\n");
}
#[test]
fn interp_str_concat_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "99") end"#, 0);
    assert_eq!(out, "a99\n");
}
#[test]
fn interp_str_concat_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "100") end"#, 0);
    assert_eq!(out, "a100\n");
}
#[test]
fn interp_str_concat_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "101") end"#, 0);
    assert_eq!(out, "a101\n");
}
#[test]
fn interp_str_concat_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "102") end"#, 0);
    assert_eq!(out, "a102\n");
}
#[test]
fn interp_str_concat_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "103") end"#, 0);
    assert_eq!(out, "a103\n");
}
#[test]
fn interp_str_concat_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "104") end"#, 0);
    assert_eq!(out, "a104\n");
}
#[test]
fn interp_str_concat_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "105") end"#, 0);
    assert_eq!(out, "a105\n");
}
#[test]
fn interp_str_concat_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "106") end"#, 0);
    assert_eq!(out, "a106\n");
}
#[test]
fn interp_str_concat_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "107") end"#, 0);
    assert_eq!(out, "a107\n");
}
#[test]
fn interp_str_concat_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "108") end"#, 0);
    assert_eq!(out, "a108\n");
}
#[test]
fn interp_str_concat_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "109") end"#, 0);
    assert_eq!(out, "a109\n");
}
#[test]
fn interp_str_concat_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "110") end"#, 0);
    assert_eq!(out, "a110\n");
}
#[test]
fn interp_str_concat_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "111") end"#, 0);
    assert_eq!(out, "a111\n");
}
#[test]
fn interp_str_concat_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "112") end"#, 0);
    assert_eq!(out, "a112\n");
}
#[test]
fn interp_str_concat_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "113") end"#, 0);
    assert_eq!(out, "a113\n");
}
#[test]
fn interp_str_concat_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "114") end"#, 0);
    assert_eq!(out, "a114\n");
}
#[test]
fn interp_str_concat_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "115") end"#, 0);
    assert_eq!(out, "a115\n");
}
#[test]
fn interp_str_concat_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "116") end"#, 0);
    assert_eq!(out, "a116\n");
}
#[test]
fn interp_str_concat_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "117") end"#, 0);
    assert_eq!(out, "a117\n");
}
#[test]
fn interp_str_concat_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "118") end"#, 0);
    assert_eq!(out, "a118\n");
}
#[test]
fn interp_str_concat_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "119") end"#, 0);
    assert_eq!(out, "a119\n");
}
#[test]
fn interp_str_concat_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "120") end"#, 0);
    assert_eq!(out, "a120\n");
}
#[test]
fn interp_str_concat_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "121") end"#, 0);
    assert_eq!(out, "a121\n");
}
#[test]
fn interp_str_concat_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "122") end"#, 0);
    assert_eq!(out, "a122\n");
}
#[test]
fn interp_str_concat_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "123") end"#, 0);
    assert_eq!(out, "a123\n");
}
#[test]
fn interp_str_concat_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "124") end"#, 0);
    assert_eq!(out, "a124\n");
}
#[test]
fn interp_str_concat_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "125") end"#, 0);
    assert_eq!(out, "a125\n");
}
#[test]
fn interp_str_concat_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "126") end"#, 0);
    assert_eq!(out, "a126\n");
}
#[test]
fn interp_str_concat_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "127") end"#, 0);
    assert_eq!(out, "a127\n");
}
#[test]
fn interp_str_concat_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "128") end"#, 0);
    assert_eq!(out, "a128\n");
}
#[test]
fn interp_str_concat_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "129") end"#, 0);
    assert_eq!(out, "a129\n");
}
#[test]
fn interp_str_concat_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "130") end"#, 0);
    assert_eq!(out, "a130\n");
}
#[test]
fn interp_str_concat_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "131") end"#, 0);
    assert_eq!(out, "a131\n");
}
#[test]
fn interp_str_concat_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "132") end"#, 0);
    assert_eq!(out, "a132\n");
}
#[test]
fn interp_str_concat_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "133") end"#, 0);
    assert_eq!(out, "a133\n");
}
#[test]
fn interp_str_concat_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "134") end"#, 0);
    assert_eq!(out, "a134\n");
}
#[test]
fn interp_str_concat_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "135") end"#, 0);
    assert_eq!(out, "a135\n");
}
#[test]
fn interp_str_concat_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "136") end"#, 0);
    assert_eq!(out, "a136\n");
}
#[test]
fn interp_str_concat_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "137") end"#, 0);
    assert_eq!(out, "a137\n");
}
#[test]
fn interp_str_concat_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "138") end"#, 0);
    assert_eq!(out, "a138\n");
}
#[test]
fn interp_str_concat_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "139") end"#, 0);
    assert_eq!(out, "a139\n");
}
#[test]
fn interp_str_concat_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "140") end"#, 0);
    assert_eq!(out, "a140\n");
}
#[test]
fn interp_str_concat_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "141") end"#, 0);
    assert_eq!(out, "a141\n");
}
#[test]
fn interp_str_concat_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "142") end"#, 0);
    assert_eq!(out, "a142\n");
}
#[test]
fn interp_str_concat_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "143") end"#, 0);
    assert_eq!(out, "a143\n");
}
#[test]
fn interp_str_concat_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "144") end"#, 0);
    assert_eq!(out, "a144\n");
}
#[test]
fn interp_str_concat_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "145") end"#, 0);
    assert_eq!(out, "a145\n");
}
#[test]
fn interp_str_concat_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "146") end"#, 0);
    assert_eq!(out, "a146\n");
}
#[test]
fn interp_str_concat_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "147") end"#, 0);
    assert_eq!(out, "a147\n");
}
#[test]
fn interp_str_concat_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "148") end"#, 0);
    assert_eq!(out, "a148\n");
}
#[test]
fn interp_str_concat_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "149") end"#, 0);
    assert_eq!(out, "a149\n");
}
#[test]
fn interp_str_concat_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "150") end"#, 0);
    assert_eq!(out, "a150\n");
}
#[test]
fn interp_str_concat_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "151") end"#, 0);
    assert_eq!(out, "a151\n");
}
#[test]
fn interp_str_concat_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "152") end"#, 0);
    assert_eq!(out, "a152\n");
}
#[test]
fn interp_str_concat_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "153") end"#, 0);
    assert_eq!(out, "a153\n");
}
#[test]
fn interp_str_concat_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "154") end"#, 0);
    assert_eq!(out, "a154\n");
}
#[test]
fn interp_str_concat_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "155") end"#, 0);
    assert_eq!(out, "a155\n");
}
#[test]
fn interp_str_concat_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "156") end"#, 0);
    assert_eq!(out, "a156\n");
}
#[test]
fn interp_str_concat_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "157") end"#, 0);
    assert_eq!(out, "a157\n");
}
#[test]
fn interp_str_concat_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "158") end"#, 0);
    assert_eq!(out, "a158\n");
}
#[test]
fn interp_str_concat_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "159") end"#, 0);
    assert_eq!(out, "a159\n");
}
#[test]
fn interp_str_concat_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "160") end"#, 0);
    assert_eq!(out, "a160\n");
}
#[test]
fn interp_str_concat_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "161") end"#, 0);
    assert_eq!(out, "a161\n");
}
#[test]
fn interp_str_concat_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "162") end"#, 0);
    assert_eq!(out, "a162\n");
}
#[test]
fn interp_str_concat_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "163") end"#, 0);
    assert_eq!(out, "a163\n");
}
#[test]
fn interp_str_concat_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "164") end"#, 0);
    assert_eq!(out, "a164\n");
}
#[test]
fn interp_str_concat_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "165") end"#, 0);
    assert_eq!(out, "a165\n");
}
#[test]
fn interp_str_concat_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "166") end"#, 0);
    assert_eq!(out, "a166\n");
}
#[test]
fn interp_str_concat_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "167") end"#, 0);
    assert_eq!(out, "a167\n");
}
#[test]
fn interp_str_concat_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "168") end"#, 0);
    assert_eq!(out, "a168\n");
}
#[test]
fn interp_str_concat_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "169") end"#, 0);
    assert_eq!(out, "a169\n");
}
#[test]
fn interp_str_concat_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "170") end"#, 0);
    assert_eq!(out, "a170\n");
}
#[test]
fn interp_str_concat_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "171") end"#, 0);
    assert_eq!(out, "a171\n");
}
#[test]
fn interp_str_concat_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "172") end"#, 0);
    assert_eq!(out, "a172\n");
}
#[test]
fn interp_str_concat_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "173") end"#, 0);
    assert_eq!(out, "a173\n");
}
#[test]
fn interp_str_concat_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "174") end"#, 0);
    assert_eq!(out, "a174\n");
}
#[test]
fn interp_str_concat_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "175") end"#, 0);
    assert_eq!(out, "a175\n");
}
#[test]
fn interp_str_concat_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "176") end"#, 0);
    assert_eq!(out, "a176\n");
}
#[test]
fn interp_str_concat_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "177") end"#, 0);
    assert_eq!(out, "a177\n");
}
#[test]
fn interp_str_concat_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "178") end"#, 0);
    assert_eq!(out, "a178\n");
}
#[test]
fn interp_str_concat_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "179") end"#, 0);
    assert_eq!(out, "a179\n");
}
#[test]
fn interp_str_concat_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "180") end"#, 0);
    assert_eq!(out, "a180\n");
}
#[test]
fn interp_str_concat_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "181") end"#, 0);
    assert_eq!(out, "a181\n");
}
#[test]
fn interp_str_concat_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "182") end"#, 0);
    assert_eq!(out, "a182\n");
}
#[test]
fn interp_str_concat_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "183") end"#, 0);
    assert_eq!(out, "a183\n");
}
#[test]
fn interp_str_concat_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "184") end"#, 0);
    assert_eq!(out, "a184\n");
}
#[test]
fn interp_str_concat_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "185") end"#, 0);
    assert_eq!(out, "a185\n");
}
#[test]
fn interp_str_concat_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "186") end"#, 0);
    assert_eq!(out, "a186\n");
}
#[test]
fn interp_str_concat_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "187") end"#, 0);
    assert_eq!(out, "a187\n");
}
#[test]
fn interp_str_concat_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "188") end"#, 0);
    assert_eq!(out, "a188\n");
}
#[test]
fn interp_str_concat_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "189") end"#, 0);
    assert_eq!(out, "a189\n");
}
#[test]
fn interp_str_concat_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "190") end"#, 0);
    assert_eq!(out, "a190\n");
}
#[test]
fn interp_str_concat_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "191") end"#, 0);
    assert_eq!(out, "a191\n");
}
#[test]
fn interp_str_concat_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "192") end"#, 0);
    assert_eq!(out, "a192\n");
}
#[test]
fn interp_str_concat_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "193") end"#, 0);
    assert_eq!(out, "a193\n");
}
#[test]
fn interp_str_concat_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "194") end"#, 0);
    assert_eq!(out, "a194\n");
}
#[test]
fn interp_str_concat_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "195") end"#, 0);
    assert_eq!(out, "a195\n");
}
#[test]
fn interp_str_concat_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "196") end"#, 0);
    assert_eq!(out, "a196\n");
}
#[test]
fn interp_str_concat_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "197") end"#, 0);
    assert_eq!(out, "a197\n");
}
#[test]
fn interp_str_concat_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "198") end"#, 0);
    assert_eq!(out, "a198\n");
}
#[test]
fn interp_str_concat_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "199") end"#, 0);
    assert_eq!(out, "a199\n");
}
#[test]
fn interp_str_concat_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "200") end"#, 0);
    assert_eq!(out, "a200\n");
}
#[test]
fn interp_str_concat_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "201") end"#, 0);
    assert_eq!(out, "a201\n");
}
#[test]
fn interp_str_concat_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "202") end"#, 0);
    assert_eq!(out, "a202\n");
}
#[test]
fn interp_str_concat_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "203") end"#, 0);
    assert_eq!(out, "a203\n");
}
#[test]
fn interp_str_concat_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "204") end"#, 0);
    assert_eq!(out, "a204\n");
}
#[test]
fn interp_str_concat_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "205") end"#, 0);
    assert_eq!(out, "a205\n");
}
#[test]
fn interp_str_concat_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "206") end"#, 0);
    assert_eq!(out, "a206\n");
}
#[test]
fn interp_str_concat_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "207") end"#, 0);
    assert_eq!(out, "a207\n");
}
#[test]
fn interp_str_concat_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "208") end"#, 0);
    assert_eq!(out, "a208\n");
}
#[test]
fn interp_str_concat_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "209") end"#, 0);
    assert_eq!(out, "a209\n");
}
#[test]
fn interp_str_concat_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "210") end"#, 0);
    assert_eq!(out, "a210\n");
}
#[test]
fn interp_str_concat_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "211") end"#, 0);
    assert_eq!(out, "a211\n");
}
#[test]
fn interp_str_concat_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "212") end"#, 0);
    assert_eq!(out, "a212\n");
}
#[test]
fn interp_str_concat_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "213") end"#, 0);
    assert_eq!(out, "a213\n");
}
#[test]
fn interp_str_concat_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "214") end"#, 0);
    assert_eq!(out, "a214\n");
}
#[test]
fn interp_str_concat_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "215") end"#, 0);
    assert_eq!(out, "a215\n");
}
#[test]
fn interp_str_concat_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "216") end"#, 0);
    assert_eq!(out, "a216\n");
}
#[test]
fn interp_str_concat_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "217") end"#, 0);
    assert_eq!(out, "a217\n");
}
#[test]
fn interp_str_concat_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "218") end"#, 0);
    assert_eq!(out, "a218\n");
}
#[test]
fn interp_str_concat_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "219") end"#, 0);
    assert_eq!(out, "a219\n");
}
#[test]
fn interp_str_concat_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "220") end"#, 0);
    assert_eq!(out, "a220\n");
}
#[test]
fn interp_str_concat_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "221") end"#, 0);
    assert_eq!(out, "a221\n");
}
#[test]
fn interp_str_concat_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "222") end"#, 0);
    assert_eq!(out, "a222\n");
}
#[test]
fn interp_str_concat_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "223") end"#, 0);
    assert_eq!(out, "a223\n");
}
#[test]
fn interp_str_concat_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "224") end"#, 0);
    assert_eq!(out, "a224\n");
}
#[test]
fn interp_str_concat_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "225") end"#, 0);
    assert_eq!(out, "a225\n");
}
#[test]
fn interp_str_concat_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "226") end"#, 0);
    assert_eq!(out, "a226\n");
}
#[test]
fn interp_str_concat_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "227") end"#, 0);
    assert_eq!(out, "a227\n");
}
#[test]
fn interp_str_concat_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "228") end"#, 0);
    assert_eq!(out, "a228\n");
}
#[test]
fn interp_str_concat_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "229") end"#, 0);
    assert_eq!(out, "a229\n");
}
#[test]
fn interp_str_concat_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "230") end"#, 0);
    assert_eq!(out, "a230\n");
}
#[test]
fn interp_str_concat_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "231") end"#, 0);
    assert_eq!(out, "a231\n");
}
#[test]
fn interp_str_concat_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "232") end"#, 0);
    assert_eq!(out, "a232\n");
}
#[test]
fn interp_str_concat_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "233") end"#, 0);
    assert_eq!(out, "a233\n");
}
#[test]
fn interp_str_concat_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "234") end"#, 0);
    assert_eq!(out, "a234\n");
}
#[test]
fn interp_str_concat_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "235") end"#, 0);
    assert_eq!(out, "a235\n");
}
#[test]
fn interp_str_concat_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "236") end"#, 0);
    assert_eq!(out, "a236\n");
}
#[test]
fn interp_str_concat_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "237") end"#, 0);
    assert_eq!(out, "a237\n");
}
#[test]
fn interp_str_concat_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "238") end"#, 0);
    assert_eq!(out, "a238\n");
}
#[test]
fn interp_str_concat_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "239") end"#, 0);
    assert_eq!(out, "a239\n");
}
#[test]
fn interp_str_concat_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "240") end"#, 0);
    assert_eq!(out, "a240\n");
}
#[test]
fn interp_str_concat_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "241") end"#, 0);
    assert_eq!(out, "a241\n");
}
#[test]
fn interp_str_concat_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "242") end"#, 0);
    assert_eq!(out, "a242\n");
}
#[test]
fn interp_str_concat_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "243") end"#, 0);
    assert_eq!(out, "a243\n");
}
#[test]
fn interp_str_concat_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "244") end"#, 0);
    assert_eq!(out, "a244\n");
}
#[test]
fn interp_str_concat_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "245") end"#, 0);
    assert_eq!(out, "a245\n");
}
#[test]
fn interp_str_concat_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "246") end"#, 0);
    assert_eq!(out, "a246\n");
}
#[test]
fn interp_str_concat_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "247") end"#, 0);
    assert_eq!(out, "a247\n");
}
#[test]
fn interp_str_concat_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "248") end"#, 0);
    assert_eq!(out, "a248\n");
}
#[test]
fn interp_str_concat_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "249") end"#, 0);
    assert_eq!(out, "a249\n");
}
#[test]
fn interp_str_concat_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "250") end"#, 0);
    assert_eq!(out, "a250\n");
}
#[test]
fn interp_str_concat_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "251") end"#, 0);
    assert_eq!(out, "a251\n");
}
#[test]
fn interp_str_concat_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "252") end"#, 0);
    assert_eq!(out, "a252\n");
}
#[test]
fn interp_str_concat_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "253") end"#, 0);
    assert_eq!(out, "a253\n");
}
#[test]
fn interp_str_concat_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "254") end"#, 0);
    assert_eq!(out, "a254\n");
}
#[test]
fn interp_str_concat_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "255") end"#, 0);
    assert_eq!(out, "a255\n");
}
#[test]
fn interp_str_concat_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "256") end"#, 0);
    assert_eq!(out, "a256\n");
}
#[test]
fn interp_str_concat_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "257") end"#, 0);
    assert_eq!(out, "a257\n");
}
#[test]
fn interp_str_concat_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "258") end"#, 0);
    assert_eq!(out, "a258\n");
}
#[test]
fn interp_str_concat_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "259") end"#, 0);
    assert_eq!(out, "a259\n");
}
#[test]
fn interp_str_concat_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "260") end"#, 0);
    assert_eq!(out, "a260\n");
}
#[test]
fn interp_str_concat_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "261") end"#, 0);
    assert_eq!(out, "a261\n");
}
#[test]
fn interp_str_concat_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "262") end"#, 0);
    assert_eq!(out, "a262\n");
}
#[test]
fn interp_str_concat_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "263") end"#, 0);
    assert_eq!(out, "a263\n");
}
#[test]
fn interp_str_concat_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "264") end"#, 0);
    assert_eq!(out, "a264\n");
}
#[test]
fn interp_str_concat_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "265") end"#, 0);
    assert_eq!(out, "a265\n");
}
#[test]
fn interp_str_concat_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "266") end"#, 0);
    assert_eq!(out, "a266\n");
}
#[test]
fn interp_str_concat_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "267") end"#, 0);
    assert_eq!(out, "a267\n");
}
#[test]
fn interp_str_concat_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "268") end"#, 0);
    assert_eq!(out, "a268\n");
}
#[test]
fn interp_str_concat_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "269") end"#, 0);
    assert_eq!(out, "a269\n");
}
#[test]
fn interp_str_concat_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "270") end"#, 0);
    assert_eq!(out, "a270\n");
}
#[test]
fn interp_str_concat_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "271") end"#, 0);
    assert_eq!(out, "a271\n");
}
#[test]
fn interp_str_concat_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "272") end"#, 0);
    assert_eq!(out, "a272\n");
}
#[test]
fn interp_str_concat_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "273") end"#, 0);
    assert_eq!(out, "a273\n");
}
#[test]
fn interp_str_concat_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "274") end"#, 0);
    assert_eq!(out, "a274\n");
}
#[test]
fn interp_str_concat_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "275") end"#, 0);
    assert_eq!(out, "a275\n");
}
#[test]
fn interp_str_concat_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "276") end"#, 0);
    assert_eq!(out, "a276\n");
}
#[test]
fn interp_str_concat_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "277") end"#, 0);
    assert_eq!(out, "a277\n");
}
#[test]
fn interp_str_concat_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "278") end"#, 0);
    assert_eq!(out, "a278\n");
}
#[test]
fn interp_str_concat_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "279") end"#, 0);
    assert_eq!(out, "a279\n");
}
#[test]
fn interp_str_concat_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "280") end"#, 0);
    assert_eq!(out, "a280\n");
}
#[test]
fn interp_str_concat_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "281") end"#, 0);
    assert_eq!(out, "a281\n");
}
#[test]
fn interp_str_concat_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "282") end"#, 0);
    assert_eq!(out, "a282\n");
}
#[test]
fn interp_str_concat_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "283") end"#, 0);
    assert_eq!(out, "a283\n");
}
#[test]
fn interp_str_concat_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "284") end"#, 0);
    assert_eq!(out, "a284\n");
}
#[test]
fn interp_str_concat_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "285") end"#, 0);
    assert_eq!(out, "a285\n");
}
#[test]
fn interp_str_concat_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "286") end"#, 0);
    assert_eq!(out, "a286\n");
}
#[test]
fn interp_str_concat_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "287") end"#, 0);
    assert_eq!(out, "a287\n");
}
#[test]
fn interp_str_concat_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "288") end"#, 0);
    assert_eq!(out, "a288\n");
}
#[test]
fn interp_str_concat_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "289") end"#, 0);
    assert_eq!(out, "a289\n");
}
#[test]
fn interp_str_concat_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "290") end"#, 0);
    assert_eq!(out, "a290\n");
}
#[test]
fn interp_str_concat_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "291") end"#, 0);
    assert_eq!(out, "a291\n");
}
#[test]
fn interp_str_concat_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "292") end"#, 0);
    assert_eq!(out, "a292\n");
}
#[test]
fn interp_str_concat_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "293") end"#, 0);
    assert_eq!(out, "a293\n");
}
#[test]
fn interp_str_concat_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "294") end"#, 0);
    assert_eq!(out, "a294\n");
}
#[test]
fn interp_str_concat_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "295") end"#, 0);
    assert_eq!(out, "a295\n");
}
#[test]
fn interp_str_concat_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "296") end"#, 0);
    assert_eq!(out, "a296\n");
}
#[test]
fn interp_str_concat_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "297") end"#, 0);
    assert_eq!(out, "a297\n");
}
#[test]
fn interp_str_concat_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "298") end"#, 0);
    assert_eq!(out, "a298\n");
}
#[test]
fn interp_str_concat_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "299") end"#, 0);
    assert_eq!(out, "a299\n");
}
#[test]
fn interp_str_concat_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "300") end"#, 0);
    assert_eq!(out, "a300\n");
}
#[test]
fn interp_str_concat_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "301") end"#, 0);
    assert_eq!(out, "a301\n");
}
#[test]
fn interp_str_concat_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "302") end"#, 0);
    assert_eq!(out, "a302\n");
}
#[test]
fn interp_str_concat_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "303") end"#, 0);
    assert_eq!(out, "a303\n");
}
#[test]
fn interp_str_concat_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "304") end"#, 0);
    assert_eq!(out, "a304\n");
}
#[test]
fn interp_str_concat_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "305") end"#, 0);
    assert_eq!(out, "a305\n");
}
#[test]
fn interp_str_concat_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "306") end"#, 0);
    assert_eq!(out, "a306\n");
}
#[test]
fn interp_str_concat_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "307") end"#, 0);
    assert_eq!(out, "a307\n");
}
#[test]
fn interp_str_concat_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "308") end"#, 0);
    assert_eq!(out, "a308\n");
}
#[test]
fn interp_str_concat_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "309") end"#, 0);
    assert_eq!(out, "a309\n");
}
#[test]
fn interp_str_concat_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "310") end"#, 0);
    assert_eq!(out, "a310\n");
}
#[test]
fn interp_str_concat_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "311") end"#, 0);
    assert_eq!(out, "a311\n");
}
#[test]
fn interp_str_concat_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "312") end"#, 0);
    assert_eq!(out, "a312\n");
}
#[test]
fn interp_str_concat_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "313") end"#, 0);
    assert_eq!(out, "a313\n");
}
#[test]
fn interp_str_concat_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "314") end"#, 0);
    assert_eq!(out, "a314\n");
}
#[test]
fn interp_str_concat_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "315") end"#, 0);
    assert_eq!(out, "a315\n");
}
#[test]
fn interp_str_concat_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "316") end"#, 0);
    assert_eq!(out, "a316\n");
}
#[test]
fn interp_str_concat_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "317") end"#, 0);
    assert_eq!(out, "a317\n");
}
#[test]
fn interp_str_concat_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "318") end"#, 0);
    assert_eq!(out, "a318\n");
}
#[test]
fn interp_str_concat_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "319") end"#, 0);
    assert_eq!(out, "a319\n");
}
#[test]
fn interp_str_concat_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "320") end"#, 0);
    assert_eq!(out, "a320\n");
}
#[test]
fn interp_str_concat_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "321") end"#, 0);
    assert_eq!(out, "a321\n");
}
#[test]
fn interp_str_concat_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "322") end"#, 0);
    assert_eq!(out, "a322\n");
}
#[test]
fn interp_str_concat_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "323") end"#, 0);
    assert_eq!(out, "a323\n");
}
#[test]
fn interp_str_concat_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "324") end"#, 0);
    assert_eq!(out, "a324\n");
}
#[test]
fn interp_str_concat_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "325") end"#, 0);
    assert_eq!(out, "a325\n");
}
#[test]
fn interp_str_concat_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "326") end"#, 0);
    assert_eq!(out, "a326\n");
}
#[test]
fn interp_str_concat_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "327") end"#, 0);
    assert_eq!(out, "a327\n");
}
#[test]
fn interp_str_concat_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "328") end"#, 0);
    assert_eq!(out, "a328\n");
}
#[test]
fn interp_str_concat_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "329") end"#, 0);
    assert_eq!(out, "a329\n");
}
#[test]
fn interp_str_concat_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "330") end"#, 0);
    assert_eq!(out, "a330\n");
}
#[test]
fn interp_str_concat_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "331") end"#, 0);
    assert_eq!(out, "a331\n");
}
#[test]
fn interp_str_concat_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "332") end"#, 0);
    assert_eq!(out, "a332\n");
}
#[test]
fn interp_str_concat_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "333") end"#, 0);
    assert_eq!(out, "a333\n");
}
#[test]
fn interp_str_concat_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "334") end"#, 0);
    assert_eq!(out, "a334\n");
}
#[test]
fn interp_str_concat_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "335") end"#, 0);
    assert_eq!(out, "a335\n");
}
#[test]
fn interp_str_concat_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "336") end"#, 0);
    assert_eq!(out, "a336\n");
}
#[test]
fn interp_str_concat_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "337") end"#, 0);
    assert_eq!(out, "a337\n");
}
#[test]
fn interp_str_concat_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "338") end"#, 0);
    assert_eq!(out, "a338\n");
}
#[test]
fn interp_str_concat_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "339") end"#, 0);
    assert_eq!(out, "a339\n");
}
#[test]
fn interp_str_concat_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "340") end"#, 0);
    assert_eq!(out, "a340\n");
}
#[test]
fn interp_str_concat_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "341") end"#, 0);
    assert_eq!(out, "a341\n");
}
#[test]
fn interp_str_concat_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "342") end"#, 0);
    assert_eq!(out, "a342\n");
}
#[test]
fn interp_str_concat_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "343") end"#, 0);
    assert_eq!(out, "a343\n");
}
#[test]
fn interp_str_concat_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "344") end"#, 0);
    assert_eq!(out, "a344\n");
}
#[test]
fn interp_str_concat_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "345") end"#, 0);
    assert_eq!(out, "a345\n");
}
#[test]
fn interp_str_concat_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "346") end"#, 0);
    assert_eq!(out, "a346\n");
}
#[test]
fn interp_str_concat_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "347") end"#, 0);
    assert_eq!(out, "a347\n");
}
#[test]
fn interp_str_concat_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "348") end"#, 0);
    assert_eq!(out, "a348\n");
}
#[test]
fn interp_str_concat_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "349") end"#, 0);
    assert_eq!(out, "a349\n");
}
#[test]
fn interp_str_concat_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "350") end"#, 0);
    assert_eq!(out, "a350\n");
}
#[test]
fn interp_str_concat_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "351") end"#, 0);
    assert_eq!(out, "a351\n");
}
#[test]
fn interp_str_concat_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "352") end"#, 0);
    assert_eq!(out, "a352\n");
}
#[test]
fn interp_str_concat_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "353") end"#, 0);
    assert_eq!(out, "a353\n");
}
#[test]
fn interp_str_concat_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "354") end"#, 0);
    assert_eq!(out, "a354\n");
}
#[test]
fn interp_str_concat_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "355") end"#, 0);
    assert_eq!(out, "a355\n");
}
#[test]
fn interp_str_concat_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "356") end"#, 0);
    assert_eq!(out, "a356\n");
}
#[test]
fn interp_str_concat_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "357") end"#, 0);
    assert_eq!(out, "a357\n");
}
#[test]
fn interp_str_concat_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "358") end"#, 0);
    assert_eq!(out, "a358\n");
}
#[test]
fn interp_str_concat_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "359") end"#, 0);
    assert_eq!(out, "a359\n");
}
#[test]
fn interp_str_concat_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "360") end"#, 0);
    assert_eq!(out, "a360\n");
}
#[test]
fn interp_str_concat_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "361") end"#, 0);
    assert_eq!(out, "a361\n");
}
#[test]
fn interp_str_concat_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "362") end"#, 0);
    assert_eq!(out, "a362\n");
}
#[test]
fn interp_str_concat_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "363") end"#, 0);
    assert_eq!(out, "a363\n");
}
#[test]
fn interp_str_concat_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "364") end"#, 0);
    assert_eq!(out, "a364\n");
}
#[test]
fn interp_str_concat_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "365") end"#, 0);
    assert_eq!(out, "a365\n");
}
#[test]
fn interp_str_concat_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "366") end"#, 0);
    assert_eq!(out, "a366\n");
}
#[test]
fn interp_str_concat_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "367") end"#, 0);
    assert_eq!(out, "a367\n");
}
#[test]
fn interp_str_concat_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "368") end"#, 0);
    assert_eq!(out, "a368\n");
}
#[test]
fn interp_str_concat_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "369") end"#, 0);
    assert_eq!(out, "a369\n");
}
#[test]
fn interp_str_concat_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "370") end"#, 0);
    assert_eq!(out, "a370\n");
}
#[test]
fn interp_str_concat_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "371") end"#, 0);
    assert_eq!(out, "a371\n");
}
#[test]
fn interp_str_concat_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "372") end"#, 0);
    assert_eq!(out, "a372\n");
}
#[test]
fn interp_str_concat_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "373") end"#, 0);
    assert_eq!(out, "a373\n");
}
#[test]
fn interp_str_concat_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "374") end"#, 0);
    assert_eq!(out, "a374\n");
}
#[test]
fn interp_str_concat_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "375") end"#, 0);
    assert_eq!(out, "a375\n");
}
#[test]
fn interp_str_concat_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "376") end"#, 0);
    assert_eq!(out, "a376\n");
}
#[test]
fn interp_str_concat_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "377") end"#, 0);
    assert_eq!(out, "a377\n");
}
#[test]
fn interp_str_concat_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "378") end"#, 0);
    assert_eq!(out, "a378\n");
}
#[test]
fn interp_str_concat_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "379") end"#, 0);
    assert_eq!(out, "a379\n");
}
#[test]
fn interp_str_concat_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "380") end"#, 0);
    assert_eq!(out, "a380\n");
}
#[test]
fn interp_str_concat_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "381") end"#, 0);
    assert_eq!(out, "a381\n");
}
#[test]
fn interp_str_concat_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "382") end"#, 0);
    assert_eq!(out, "a382\n");
}
#[test]
fn interp_str_concat_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "383") end"#, 0);
    assert_eq!(out, "a383\n");
}
#[test]
fn interp_str_concat_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "384") end"#, 0);
    assert_eq!(out, "a384\n");
}
#[test]
fn interp_str_concat_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "385") end"#, 0);
    assert_eq!(out, "a385\n");
}
#[test]
fn interp_str_concat_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "386") end"#, 0);
    assert_eq!(out, "a386\n");
}
#[test]
fn interp_str_concat_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "387") end"#, 0);
    assert_eq!(out, "a387\n");
}
#[test]
fn interp_str_concat_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "388") end"#, 0);
    assert_eq!(out, "a388\n");
}
#[test]
fn interp_str_concat_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "389") end"#, 0);
    assert_eq!(out, "a389\n");
}
#[test]
fn interp_str_concat_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "390") end"#, 0);
    assert_eq!(out, "a390\n");
}
#[test]
fn interp_str_concat_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "391") end"#, 0);
    assert_eq!(out, "a391\n");
}
#[test]
fn interp_str_concat_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "392") end"#, 0);
    assert_eq!(out, "a392\n");
}
#[test]
fn interp_str_concat_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "393") end"#, 0);
    assert_eq!(out, "a393\n");
}
#[test]
fn interp_str_concat_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "394") end"#, 0);
    assert_eq!(out, "a394\n");
}
#[test]
fn interp_str_concat_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "395") end"#, 0);
    assert_eq!(out, "a395\n");
}
#[test]
fn interp_str_concat_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "396") end"#, 0);
    assert_eq!(out, "a396\n");
}
#[test]
fn interp_str_concat_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "397") end"#, 0);
    assert_eq!(out, "a397\n");
}
#[test]
fn interp_str_concat_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "398") end"#, 0);
    assert_eq!(out, "a398\n");
}
#[test]
fn interp_str_concat_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "399") end"#, 0);
    assert_eq!(out, "a399\n");
}
#[test]
fn interp_str_concat_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "400") end"#, 0);
    assert_eq!(out, "a400\n");
}
#[test]
fn interp_str_concat_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "401") end"#, 0);
    assert_eq!(out, "a401\n");
}
#[test]
fn interp_str_concat_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "402") end"#, 0);
    assert_eq!(out, "a402\n");
}
#[test]
fn interp_str_concat_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "403") end"#, 0);
    assert_eq!(out, "a403\n");
}
#[test]
fn interp_str_concat_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "404") end"#, 0);
    assert_eq!(out, "a404\n");
}
#[test]
fn interp_str_concat_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "405") end"#, 0);
    assert_eq!(out, "a405\n");
}
#[test]
fn interp_str_concat_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "406") end"#, 0);
    assert_eq!(out, "a406\n");
}
#[test]
fn interp_str_concat_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "407") end"#, 0);
    assert_eq!(out, "a407\n");
}
#[test]
fn interp_str_concat_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "408") end"#, 0);
    assert_eq!(out, "a408\n");
}
#[test]
fn interp_str_concat_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "409") end"#, 0);
    assert_eq!(out, "a409\n");
}
#[test]
fn interp_str_concat_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "410") end"#, 0);
    assert_eq!(out, "a410\n");
}
#[test]
fn interp_str_concat_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "411") end"#, 0);
    assert_eq!(out, "a411\n");
}
#[test]
fn interp_str_concat_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "412") end"#, 0);
    assert_eq!(out, "a412\n");
}
#[test]
fn interp_str_concat_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "413") end"#, 0);
    assert_eq!(out, "a413\n");
}
#[test]
fn interp_str_concat_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "414") end"#, 0);
    assert_eq!(out, "a414\n");
}
#[test]
fn interp_str_concat_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "415") end"#, 0);
    assert_eq!(out, "a415\n");
}
#[test]
fn interp_str_concat_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "416") end"#, 0);
    assert_eq!(out, "a416\n");
}
#[test]
fn interp_str_concat_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "417") end"#, 0);
    assert_eq!(out, "a417\n");
}
#[test]
fn interp_str_concat_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "418") end"#, 0);
    assert_eq!(out, "a418\n");
}
#[test]
fn interp_str_concat_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "419") end"#, 0);
    assert_eq!(out, "a419\n");
}
#[test]
fn interp_str_concat_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "420") end"#, 0);
    assert_eq!(out, "a420\n");
}
#[test]
fn interp_str_concat_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "421") end"#, 0);
    assert_eq!(out, "a421\n");
}
#[test]
fn interp_str_concat_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "422") end"#, 0);
    assert_eq!(out, "a422\n");
}
#[test]
fn interp_str_concat_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "423") end"#, 0);
    assert_eq!(out, "a423\n");
}
#[test]
fn interp_str_concat_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "424") end"#, 0);
    assert_eq!(out, "a424\n");
}
#[test]
fn interp_str_concat_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "425") end"#, 0);
    assert_eq!(out, "a425\n");
}
#[test]
fn interp_str_concat_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "426") end"#, 0);
    assert_eq!(out, "a426\n");
}
#[test]
fn interp_str_concat_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "427") end"#, 0);
    assert_eq!(out, "a427\n");
}
#[test]
fn interp_str_concat_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "428") end"#, 0);
    assert_eq!(out, "a428\n");
}
#[test]
fn interp_str_concat_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "429") end"#, 0);
    assert_eq!(out, "a429\n");
}
#[test]
fn interp_str_concat_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "430") end"#, 0);
    assert_eq!(out, "a430\n");
}
#[test]
fn interp_str_concat_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "431") end"#, 0);
    assert_eq!(out, "a431\n");
}
#[test]
fn interp_str_concat_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "432") end"#, 0);
    assert_eq!(out, "a432\n");
}
#[test]
fn interp_str_concat_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "433") end"#, 0);
    assert_eq!(out, "a433\n");
}
#[test]
fn interp_str_concat_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "434") end"#, 0);
    assert_eq!(out, "a434\n");
}
#[test]
fn interp_str_concat_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "435") end"#, 0);
    assert_eq!(out, "a435\n");
}
#[test]
fn interp_str_concat_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "436") end"#, 0);
    assert_eq!(out, "a436\n");
}
#[test]
fn interp_str_concat_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "437") end"#, 0);
    assert_eq!(out, "a437\n");
}
#[test]
fn interp_str_concat_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "438") end"#, 0);
    assert_eq!(out, "a438\n");
}
#[test]
fn interp_str_concat_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "439") end"#, 0);
    assert_eq!(out, "a439\n");
}
#[test]
fn interp_str_concat_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "440") end"#, 0);
    assert_eq!(out, "a440\n");
}
#[test]
fn interp_str_concat_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "441") end"#, 0);
    assert_eq!(out, "a441\n");
}
#[test]
fn interp_str_concat_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "442") end"#, 0);
    assert_eq!(out, "a442\n");
}
#[test]
fn interp_str_concat_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "443") end"#, 0);
    assert_eq!(out, "a443\n");
}
#[test]
fn interp_str_concat_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "444") end"#, 0);
    assert_eq!(out, "a444\n");
}
#[test]
fn interp_str_concat_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "445") end"#, 0);
    assert_eq!(out, "a445\n");
}
#[test]
fn interp_str_concat_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "446") end"#, 0);
    assert_eq!(out, "a446\n");
}
#[test]
fn interp_str_concat_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "447") end"#, 0);
    assert_eq!(out, "a447\n");
}
#[test]
fn interp_str_concat_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "448") end"#, 0);
    assert_eq!(out, "a448\n");
}
#[test]
fn interp_str_concat_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "449") end"#, 0);
    assert_eq!(out, "a449\n");
}
#[test]
fn interp_str_concat_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "450") end"#, 0);
    assert_eq!(out, "a450\n");
}
#[test]
fn interp_str_concat_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "451") end"#, 0);
    assert_eq!(out, "a451\n");
}
#[test]
fn interp_str_concat_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "452") end"#, 0);
    assert_eq!(out, "a452\n");
}
#[test]
fn interp_str_concat_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "453") end"#, 0);
    assert_eq!(out, "a453\n");
}
#[test]
fn interp_str_concat_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "454") end"#, 0);
    assert_eq!(out, "a454\n");
}
#[test]
fn interp_str_concat_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "455") end"#, 0);
    assert_eq!(out, "a455\n");
}
#[test]
fn interp_str_concat_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "456") end"#, 0);
    assert_eq!(out, "a456\n");
}
#[test]
fn interp_str_concat_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "457") end"#, 0);
    assert_eq!(out, "a457\n");
}
#[test]
fn interp_str_concat_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "458") end"#, 0);
    assert_eq!(out, "a458\n");
}
#[test]
fn interp_str_concat_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "459") end"#, 0);
    assert_eq!(out, "a459\n");
}
#[test]
fn interp_str_concat_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "460") end"#, 0);
    assert_eq!(out, "a460\n");
}
#[test]
fn interp_str_concat_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "461") end"#, 0);
    assert_eq!(out, "a461\n");
}
#[test]
fn interp_str_concat_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "462") end"#, 0);
    assert_eq!(out, "a462\n");
}
#[test]
fn interp_str_concat_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "463") end"#, 0);
    assert_eq!(out, "a463\n");
}
#[test]
fn interp_str_concat_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "464") end"#, 0);
    assert_eq!(out, "a464\n");
}
#[test]
fn interp_str_concat_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "465") end"#, 0);
    assert_eq!(out, "a465\n");
}
#[test]
fn interp_str_concat_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "466") end"#, 0);
    assert_eq!(out, "a466\n");
}
#[test]
fn interp_str_concat_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "467") end"#, 0);
    assert_eq!(out, "a467\n");
}
#[test]
fn interp_str_concat_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "468") end"#, 0);
    assert_eq!(out, "a468\n");
}
#[test]
fn interp_str_concat_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "469") end"#, 0);
    assert_eq!(out, "a469\n");
}
#[test]
fn interp_str_concat_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "470") end"#, 0);
    assert_eq!(out, "a470\n");
}
#[test]
fn interp_str_concat_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "471") end"#, 0);
    assert_eq!(out, "a471\n");
}
#[test]
fn interp_str_concat_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "472") end"#, 0);
    assert_eq!(out, "a472\n");
}
#[test]
fn interp_str_concat_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "473") end"#, 0);
    assert_eq!(out, "a473\n");
}
#[test]
fn interp_str_concat_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "474") end"#, 0);
    assert_eq!(out, "a474\n");
}
#[test]
fn interp_str_concat_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "475") end"#, 0);
    assert_eq!(out, "a475\n");
}
#[test]
fn interp_str_concat_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "476") end"#, 0);
    assert_eq!(out, "a476\n");
}
#[test]
fn interp_str_concat_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "477") end"#, 0);
    assert_eq!(out, "a477\n");
}
#[test]
fn interp_str_concat_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "478") end"#, 0);
    assert_eq!(out, "a478\n");
}
#[test]
fn interp_str_concat_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "479") end"#, 0);
    assert_eq!(out, "a479\n");
}
#[test]
fn interp_str_concat_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "480") end"#, 0);
    assert_eq!(out, "a480\n");
}
#[test]
fn interp_str_concat_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "481") end"#, 0);
    assert_eq!(out, "a481\n");
}
#[test]
fn interp_str_concat_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "482") end"#, 0);
    assert_eq!(out, "a482\n");
}
#[test]
fn interp_str_concat_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "483") end"#, 0);
    assert_eq!(out, "a483\n");
}
#[test]
fn interp_str_concat_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "484") end"#, 0);
    assert_eq!(out, "a484\n");
}
#[test]
fn interp_str_concat_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "485") end"#, 0);
    assert_eq!(out, "a485\n");
}
#[test]
fn interp_str_concat_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "486") end"#, 0);
    assert_eq!(out, "a486\n");
}
#[test]
fn interp_str_concat_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "487") end"#, 0);
    assert_eq!(out, "a487\n");
}
#[test]
fn interp_str_concat_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "488") end"#, 0);
    assert_eq!(out, "a488\n");
}
#[test]
fn interp_str_concat_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "489") end"#, 0);
    assert_eq!(out, "a489\n");
}
#[test]
fn interp_str_concat_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "490") end"#, 0);
    assert_eq!(out, "a490\n");
}
#[test]
fn interp_str_concat_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "491") end"#, 0);
    assert_eq!(out, "a491\n");
}
#[test]
fn interp_str_concat_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "492") end"#, 0);
    assert_eq!(out, "a492\n");
}
#[test]
fn interp_str_concat_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "493") end"#, 0);
    assert_eq!(out, "a493\n");
}
#[test]
fn interp_str_concat_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "494") end"#, 0);
    assert_eq!(out, "a494\n");
}
#[test]
fn interp_str_concat_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "495") end"#, 0);
    assert_eq!(out, "a495\n");
}
#[test]
fn interp_str_concat_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "496") end"#, 0);
    assert_eq!(out, "a496\n");
}
#[test]
fn interp_str_concat_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "497") end"#, 0);
    assert_eq!(out, "a497\n");
}
#[test]
fn interp_str_concat_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "498") end"#, 0);
    assert_eq!(out, "a498\n");
}
#[test]
fn interp_str_concat_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret(r#"fn main() print("a" + "499") end"#, 0);
    assert_eq!(out, "a499\n");
}
#[test]
fn interp_compare_chain_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(0 < 1) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(1 < 2) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(2 < 3) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(3 < 4) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(4 < 5) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(5 < 6) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(6 < 7) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(7 < 8) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(8 < 9) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(9 < 10) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(10 < 11) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(11 < 12) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(12 < 13) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(13 < 14) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(14 < 15) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(15 < 16) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(16 < 17) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(17 < 18) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(18 < 19) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(19 < 20) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(20 < 21) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(21 < 22) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(22 < 23) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(23 < 24) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(24 < 25) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(25 < 26) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(26 < 27) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(27 < 28) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(28 < 29) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(29 < 30) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(30 < 31) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(31 < 32) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(32 < 33) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(33 < 34) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(34 < 35) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(35 < 36) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(36 < 37) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(37 < 38) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(38 < 39) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(39 < 40) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(40 < 41) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(41 < 42) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(42 < 43) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(43 < 44) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(44 < 45) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(45 < 46) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(46 < 47) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(47 < 48) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(48 < 49) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(49 < 50) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(50 < 51) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(51 < 52) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(52 < 53) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(53 < 54) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(54 < 55) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(55 < 56) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(56 < 57) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(57 < 58) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(58 < 59) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(59 < 60) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(60 < 61) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(61 < 62) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(62 < 63) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(63 < 64) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(64 < 65) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(65 < 66) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(66 < 67) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(67 < 68) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(68 < 69) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(69 < 70) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(70 < 71) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(71 < 72) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(72 < 73) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(73 < 74) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(74 < 75) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(75 < 76) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(76 < 77) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(77 < 78) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(78 < 79) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(79 < 80) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(80 < 81) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(81 < 82) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(82 < 83) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(83 < 84) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(84 < 85) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(85 < 86) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(86 < 87) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(87 < 88) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(88 < 89) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(89 < 90) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(90 < 91) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(91 < 92) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(92 < 93) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(93 < 94) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(94 < 95) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(95 < 96) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(96 < 97) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(97 < 98) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(98 < 99) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(99 < 100) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(100 < 101) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(101 < 102) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(102 < 103) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(103 < 104) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(104 < 105) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(105 < 106) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(106 < 107) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(107 < 108) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(108 < 109) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(109 < 110) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(110 < 111) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(111 < 112) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(112 < 113) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(113 < 114) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(114 < 115) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(115 < 116) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(116 < 117) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(117 < 118) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(118 < 119) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(119 < 120) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(120 < 121) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(121 < 122) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(122 < 123) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(123 < 124) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(124 < 125) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(125 < 126) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(126 < 127) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(127 < 128) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(128 < 129) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(129 < 130) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(130 < 131) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(131 < 132) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(132 < 133) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(133 < 134) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(134 < 135) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(135 < 136) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(136 < 137) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(137 < 138) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(138 < 139) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(139 < 140) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(140 < 141) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(141 < 142) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(142 < 143) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(143 < 144) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(144 < 145) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(145 < 146) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(146 < 147) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(147 < 148) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(148 < 149) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(149 < 150) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(150 < 151) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(151 < 152) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(152 < 153) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(153 < 154) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(154 < 155) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(155 < 156) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(156 < 157) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(157 < 158) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(158 < 159) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(159 < 160) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(160 < 161) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(161 < 162) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(162 < 163) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(163 < 164) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(164 < 165) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(165 < 166) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(166 < 167) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(167 < 168) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(168 < 169) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(169 < 170) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(170 < 171) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(171 < 172) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(172 < 173) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(173 < 174) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(174 < 175) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(175 < 176) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(176 < 177) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(177 < 178) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(178 < 179) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(179 < 180) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(180 < 181) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(181 < 182) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(182 < 183) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(183 < 184) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(184 < 185) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(185 < 186) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(186 < 187) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(187 < 188) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(188 < 189) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(189 < 190) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(190 < 191) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(191 < 192) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(192 < 193) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(193 < 194) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(194 < 195) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(195 < 196) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(196 < 197) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(197 < 198) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(198 < 199) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(199 < 200) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(200 < 201) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(201 < 202) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(202 < 203) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(203 < 204) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(204 < 205) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(205 < 206) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(206 < 207) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(207 < 208) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(208 < 209) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(209 < 210) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(210 < 211) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(211 < 212) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(212 < 213) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(213 < 214) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(214 < 215) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(215 < 216) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(216 < 217) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(217 < 218) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(218 < 219) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(219 < 220) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(220 < 221) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(221 < 222) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(222 < 223) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(223 < 224) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(224 < 225) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(225 < 226) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(226 < 227) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(227 < 228) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(228 < 229) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(229 < 230) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(230 < 231) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(231 < 232) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(232 < 233) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(233 < 234) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(234 < 235) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(235 < 236) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(236 < 237) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(237 < 238) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(238 < 239) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(239 < 240) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(240 < 241) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(241 < 242) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(242 < 243) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(243 < 244) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(244 < 245) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(245 < 246) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(246 < 247) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(247 < 248) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(248 < 249) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(249 < 250) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(250 < 251) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(251 < 252) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(252 < 253) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(253 < 254) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(254 < 255) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(255 < 256) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(256 < 257) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(257 < 258) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(258 < 259) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(259 < 260) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(260 < 261) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(261 < 262) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(262 < 263) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(263 < 264) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(264 < 265) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(265 < 266) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(266 < 267) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(267 < 268) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(268 < 269) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(269 < 270) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(270 < 271) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(271 < 272) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(272 < 273) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(273 < 274) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(274 < 275) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(275 < 276) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(276 < 277) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(277 < 278) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(278 < 279) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(279 < 280) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(280 < 281) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(281 < 282) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(282 < 283) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(283 < 284) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(284 < 285) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(285 < 286) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(286 < 287) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(287 < 288) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(288 < 289) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(289 < 290) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(290 < 291) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(291 < 292) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(292 < 293) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(293 < 294) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(294 < 295) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(295 < 296) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(296 < 297) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(297 < 298) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(298 < 299) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(299 < 300) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(300 < 301) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(301 < 302) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(302 < 303) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(303 < 304) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(304 < 305) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(305 < 306) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(306 < 307) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(307 < 308) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(308 < 309) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(309 < 310) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(310 < 311) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(311 < 312) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(312 < 313) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(313 < 314) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(314 < 315) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(315 < 316) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(316 < 317) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(317 < 318) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(318 < 319) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(319 < 320) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(320 < 321) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(321 < 322) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(322 < 323) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(323 < 324) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(324 < 325) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(325 < 326) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(326 < 327) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(327 < 328) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(328 < 329) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(329 < 330) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(330 < 331) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(331 < 332) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(332 < 333) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(333 < 334) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(334 < 335) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(335 < 336) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(336 < 337) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(337 < 338) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(338 < 339) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(339 < 340) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(340 < 341) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(341 < 342) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(342 < 343) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(343 < 344) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(344 < 345) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(345 < 346) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(346 < 347) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(347 < 348) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(348 < 349) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(349 < 350) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(350 < 351) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(351 < 352) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(352 < 353) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(353 < 354) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(354 < 355) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(355 < 356) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(356 < 357) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(357 < 358) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(358 < 359) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(359 < 360) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(360 < 361) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(361 < 362) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(362 < 363) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(363 < 364) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(364 < 365) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(365 < 366) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(366 < 367) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(367 < 368) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(368 < 369) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(369 < 370) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(370 < 371) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(371 < 372) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(372 < 373) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(373 < 374) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(374 < 375) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(375 < 376) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(376 < 377) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(377 < 378) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(378 < 379) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(379 < 380) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(380 < 381) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(381 < 382) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(382 < 383) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(383 < 384) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(384 < 385) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(385 < 386) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(386 < 387) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(387 < 388) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(388 < 389) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(389 < 390) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(390 < 391) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(391 < 392) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(392 < 393) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(393 < 394) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(394 < 395) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(395 < 396) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(396 < 397) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(397 < 398) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(398 < 399) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(399 < 400) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(400 < 401) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(401 < 402) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(402 < 403) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(403 < 404) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(404 < 405) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(405 < 406) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(406 < 407) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(407 < 408) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(408 < 409) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(409 < 410) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(410 < 411) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(411 < 412) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(412 < 413) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(413 < 414) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(414 < 415) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(415 < 416) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(416 < 417) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(417 < 418) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(418 < 419) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(419 < 420) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(420 < 421) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(421 < 422) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(422 < 423) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(423 < 424) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(424 < 425) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(425 < 426) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(426 < 427) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(427 < 428) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(428 < 429) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(429 < 430) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(430 < 431) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(431 < 432) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(432 < 433) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(433 < 434) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(434 < 435) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(435 < 436) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(436 < 437) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(437 < 438) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(438 < 439) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(439 < 440) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(440 < 441) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(441 < 442) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(442 < 443) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(443 < 444) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(444 < 445) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(445 < 446) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(446 < 447) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(447 < 448) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(448 < 449) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(449 < 450) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(450 < 451) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(451 < 452) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(452 < 453) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(453 < 454) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(454 < 455) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(455 < 456) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(456 < 457) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(457 < 458) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(458 < 459) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(459 < 460) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(460 < 461) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(461 < 462) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(462 < 463) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(463 < 464) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(464 < 465) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(465 < 466) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(466 < 467) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(467 < 468) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(468 < 469) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(469 < 470) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(470 < 471) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(471 < 472) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(472 < 473) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(473 < 474) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(474 < 475) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(475 < 476) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(476 < 477) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(477 < 478) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(478 < 479) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(479 < 480) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(480 < 481) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(481 < 482) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(482 < 483) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(483 < 484) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(484 < 485) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(485 < 486) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(486 < 487) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(487 < 488) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(488 < 489) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(489 < 490) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(490 < 491) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(491 < 492) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(492 < 493) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(493 < 494) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(494 < 495) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(495 < 496) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(496 < 497) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(497 < 498) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(498 < 499) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_compare_chain_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(499 < 500) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn arith_precedence_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(0 + 0 * 2) end", 0);
    assert_eq!(out, "0\n");
}
#[test]
fn arith_precedence_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(1 + 1 * 2) end", 0);
    assert_eq!(out, "3\n");
}
#[test]
fn arith_precedence_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(2 + 2 * 2) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn arith_precedence_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(3 + 3 * 2) end", 0);
    assert_eq!(out, "9\n");
}
#[test]
fn arith_precedence_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(4 + 4 * 2) end", 0);
    assert_eq!(out, "12\n");
}
#[test]
fn arith_precedence_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(5 + 5 * 2) end", 0);
    assert_eq!(out, "15\n");
}
#[test]
fn arith_precedence_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(6 + 6 * 2) end", 0);
    assert_eq!(out, "18\n");
}
#[test]
fn arith_precedence_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(7 + 7 * 2) end", 0);
    assert_eq!(out, "21\n");
}
#[test]
fn arith_precedence_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(8 + 8 * 2) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn arith_precedence_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(9 + 9 * 2) end", 0);
    assert_eq!(out, "27\n");
}
#[test]
fn arith_precedence_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(10 + 10 * 2) end", 0);
    assert_eq!(out, "30\n");
}
#[test]
fn arith_precedence_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(11 + 11 * 2) end", 0);
    assert_eq!(out, "33\n");
}
#[test]
fn arith_precedence_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(12 + 12 * 2) end", 0);
    assert_eq!(out, "36\n");
}
#[test]
fn arith_precedence_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(13 + 13 * 2) end", 0);
    assert_eq!(out, "39\n");
}
#[test]
fn arith_precedence_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(14 + 14 * 2) end", 0);
    assert_eq!(out, "42\n");
}
#[test]
fn arith_precedence_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(15 + 15 * 2) end", 0);
    assert_eq!(out, "45\n");
}
#[test]
fn arith_precedence_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(16 + 16 * 2) end", 0);
    assert_eq!(out, "48\n");
}
#[test]
fn arith_precedence_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(17 + 17 * 2) end", 0);
    assert_eq!(out, "51\n");
}
#[test]
fn arith_precedence_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(18 + 18 * 2) end", 0);
    assert_eq!(out, "54\n");
}
#[test]
fn arith_precedence_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(19 + 19 * 2) end", 0);
    assert_eq!(out, "57\n");
}
#[test]
fn arith_precedence_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(20 + 20 * 2) end", 0);
    assert_eq!(out, "60\n");
}
#[test]
fn arith_precedence_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(21 + 21 * 2) end", 0);
    assert_eq!(out, "63\n");
}
#[test]
fn arith_precedence_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(22 + 22 * 2) end", 0);
    assert_eq!(out, "66\n");
}
#[test]
fn arith_precedence_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(23 + 23 * 2) end", 0);
    assert_eq!(out, "69\n");
}
#[test]
fn arith_precedence_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(24 + 24 * 2) end", 0);
    assert_eq!(out, "72\n");
}
#[test]
fn arith_precedence_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(25 + 25 * 2) end", 0);
    assert_eq!(out, "75\n");
}
#[test]
fn arith_precedence_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(26 + 26 * 2) end", 0);
    assert_eq!(out, "78\n");
}
#[test]
fn arith_precedence_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(27 + 27 * 2) end", 0);
    assert_eq!(out, "81\n");
}
#[test]
fn arith_precedence_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(28 + 28 * 2) end", 0);
    assert_eq!(out, "84\n");
}
#[test]
fn arith_precedence_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(29 + 29 * 2) end", 0);
    assert_eq!(out, "87\n");
}
#[test]
fn arith_precedence_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(30 + 30 * 2) end", 0);
    assert_eq!(out, "90\n");
}
#[test]
fn arith_precedence_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(31 + 31 * 2) end", 0);
    assert_eq!(out, "93\n");
}
#[test]
fn arith_precedence_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(32 + 32 * 2) end", 0);
    assert_eq!(out, "96\n");
}
#[test]
fn arith_precedence_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(33 + 33 * 2) end", 0);
    assert_eq!(out, "99\n");
}
#[test]
fn arith_precedence_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(34 + 34 * 2) end", 0);
    assert_eq!(out, "102\n");
}
#[test]
fn arith_precedence_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(35 + 35 * 2) end", 0);
    assert_eq!(out, "105\n");
}
#[test]
fn arith_precedence_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(36 + 36 * 2) end", 0);
    assert_eq!(out, "108\n");
}
#[test]
fn arith_precedence_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(37 + 37 * 2) end", 0);
    assert_eq!(out, "111\n");
}
#[test]
fn arith_precedence_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(38 + 38 * 2) end", 0);
    assert_eq!(out, "114\n");
}
#[test]
fn arith_precedence_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(39 + 39 * 2) end", 0);
    assert_eq!(out, "117\n");
}
#[test]
fn arith_precedence_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(40 + 40 * 2) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn arith_precedence_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(41 + 41 * 2) end", 0);
    assert_eq!(out, "123\n");
}
#[test]
fn arith_precedence_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(42 + 42 * 2) end", 0);
    assert_eq!(out, "126\n");
}
#[test]
fn arith_precedence_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(43 + 43 * 2) end", 0);
    assert_eq!(out, "129\n");
}
#[test]
fn arith_precedence_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(44 + 44 * 2) end", 0);
    assert_eq!(out, "132\n");
}
#[test]
fn arith_precedence_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(45 + 45 * 2) end", 0);
    assert_eq!(out, "135\n");
}
#[test]
fn arith_precedence_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(46 + 46 * 2) end", 0);
    assert_eq!(out, "138\n");
}
#[test]
fn arith_precedence_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(47 + 47 * 2) end", 0);
    assert_eq!(out, "141\n");
}
#[test]
fn arith_precedence_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(48 + 48 * 2) end", 0);
    assert_eq!(out, "144\n");
}
#[test]
fn arith_precedence_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(49 + 49 * 2) end", 0);
    assert_eq!(out, "147\n");
}
#[test]
fn arith_precedence_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(50 + 50 * 2) end", 0);
    assert_eq!(out, "150\n");
}
#[test]
fn arith_precedence_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(51 + 51 * 2) end", 0);
    assert_eq!(out, "153\n");
}
#[test]
fn arith_precedence_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(52 + 52 * 2) end", 0);
    assert_eq!(out, "156\n");
}
#[test]
fn arith_precedence_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(53 + 53 * 2) end", 0);
    assert_eq!(out, "159\n");
}
#[test]
fn arith_precedence_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(54 + 54 * 2) end", 0);
    assert_eq!(out, "162\n");
}
#[test]
fn arith_precedence_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(55 + 55 * 2) end", 0);
    assert_eq!(out, "165\n");
}
#[test]
fn arith_precedence_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(56 + 56 * 2) end", 0);
    assert_eq!(out, "168\n");
}
#[test]
fn arith_precedence_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(57 + 57 * 2) end", 0);
    assert_eq!(out, "171\n");
}
#[test]
fn arith_precedence_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(58 + 58 * 2) end", 0);
    assert_eq!(out, "174\n");
}
#[test]
fn arith_precedence_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(59 + 59 * 2) end", 0);
    assert_eq!(out, "177\n");
}
#[test]
fn arith_precedence_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(60 + 60 * 2) end", 0);
    assert_eq!(out, "180\n");
}
#[test]
fn arith_precedence_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(61 + 61 * 2) end", 0);
    assert_eq!(out, "183\n");
}
#[test]
fn arith_precedence_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(62 + 62 * 2) end", 0);
    assert_eq!(out, "186\n");
}
#[test]
fn arith_precedence_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(63 + 63 * 2) end", 0);
    assert_eq!(out, "189\n");
}
#[test]
fn arith_precedence_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(64 + 64 * 2) end", 0);
    assert_eq!(out, "192\n");
}
#[test]
fn arith_precedence_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(65 + 65 * 2) end", 0);
    assert_eq!(out, "195\n");
}
#[test]
fn arith_precedence_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(66 + 66 * 2) end", 0);
    assert_eq!(out, "198\n");
}
#[test]
fn arith_precedence_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(67 + 67 * 2) end", 0);
    assert_eq!(out, "201\n");
}
#[test]
fn arith_precedence_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(68 + 68 * 2) end", 0);
    assert_eq!(out, "204\n");
}
#[test]
fn arith_precedence_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(69 + 69 * 2) end", 0);
    assert_eq!(out, "207\n");
}
#[test]
fn arith_precedence_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(70 + 70 * 2) end", 0);
    assert_eq!(out, "210\n");
}
#[test]
fn arith_precedence_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(71 + 71 * 2) end", 0);
    assert_eq!(out, "213\n");
}
#[test]
fn arith_precedence_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(72 + 72 * 2) end", 0);
    assert_eq!(out, "216\n");
}
#[test]
fn arith_precedence_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(73 + 73 * 2) end", 0);
    assert_eq!(out, "219\n");
}
#[test]
fn arith_precedence_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(74 + 74 * 2) end", 0);
    assert_eq!(out, "222\n");
}
#[test]
fn arith_precedence_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(75 + 75 * 2) end", 0);
    assert_eq!(out, "225\n");
}
#[test]
fn arith_precedence_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(76 + 76 * 2) end", 0);
    assert_eq!(out, "228\n");
}
#[test]
fn arith_precedence_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(77 + 77 * 2) end", 0);
    assert_eq!(out, "231\n");
}
#[test]
fn arith_precedence_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(78 + 78 * 2) end", 0);
    assert_eq!(out, "234\n");
}
#[test]
fn arith_precedence_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(79 + 79 * 2) end", 0);
    assert_eq!(out, "237\n");
}
#[test]
fn arith_precedence_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(80 + 80 * 2) end", 0);
    assert_eq!(out, "240\n");
}
#[test]
fn arith_precedence_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(81 + 81 * 2) end", 0);
    assert_eq!(out, "243\n");
}
#[test]
fn arith_precedence_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(82 + 82 * 2) end", 0);
    assert_eq!(out, "246\n");
}
#[test]
fn arith_precedence_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(83 + 83 * 2) end", 0);
    assert_eq!(out, "249\n");
}
#[test]
fn arith_precedence_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(84 + 84 * 2) end", 0);
    assert_eq!(out, "252\n");
}
#[test]
fn arith_precedence_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(85 + 85 * 2) end", 0);
    assert_eq!(out, "255\n");
}
#[test]
fn arith_precedence_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(86 + 86 * 2) end", 0);
    assert_eq!(out, "258\n");
}
#[test]
fn arith_precedence_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(87 + 87 * 2) end", 0);
    assert_eq!(out, "261\n");
}
#[test]
fn arith_precedence_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(88 + 88 * 2) end", 0);
    assert_eq!(out, "264\n");
}
#[test]
fn arith_precedence_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(89 + 89 * 2) end", 0);
    assert_eq!(out, "267\n");
}
#[test]
fn arith_precedence_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(90 + 90 * 2) end", 0);
    assert_eq!(out, "270\n");
}
#[test]
fn arith_precedence_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(91 + 91 * 2) end", 0);
    assert_eq!(out, "273\n");
}
#[test]
fn arith_precedence_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(92 + 92 * 2) end", 0);
    assert_eq!(out, "276\n");
}
#[test]
fn arith_precedence_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(93 + 93 * 2) end", 0);
    assert_eq!(out, "279\n");
}
#[test]
fn arith_precedence_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(94 + 94 * 2) end", 0);
    assert_eq!(out, "282\n");
}
#[test]
fn arith_precedence_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(95 + 95 * 2) end", 0);
    assert_eq!(out, "285\n");
}
#[test]
fn arith_precedence_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(96 + 96 * 2) end", 0);
    assert_eq!(out, "288\n");
}
#[test]
fn arith_precedence_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(97 + 97 * 2) end", 0);
    assert_eq!(out, "291\n");
}
#[test]
fn arith_precedence_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(98 + 98 * 2) end", 0);
    assert_eq!(out, "294\n");
}
#[test]
fn arith_precedence_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(99 + 99 * 2) end", 0);
    assert_eq!(out, "297\n");
}
#[test]
fn arith_precedence_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(100 + 100 * 2) end", 0);
    assert_eq!(out, "300\n");
}
#[test]
fn arith_precedence_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(101 + 101 * 2) end", 0);
    assert_eq!(out, "303\n");
}
#[test]
fn arith_precedence_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(102 + 102 * 2) end", 0);
    assert_eq!(out, "306\n");
}
#[test]
fn arith_precedence_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(103 + 103 * 2) end", 0);
    assert_eq!(out, "309\n");
}
#[test]
fn arith_precedence_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(104 + 104 * 2) end", 0);
    assert_eq!(out, "312\n");
}
#[test]
fn arith_precedence_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(105 + 105 * 2) end", 0);
    assert_eq!(out, "315\n");
}
#[test]
fn arith_precedence_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(106 + 106 * 2) end", 0);
    assert_eq!(out, "318\n");
}
#[test]
fn arith_precedence_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(107 + 107 * 2) end", 0);
    assert_eq!(out, "321\n");
}
#[test]
fn arith_precedence_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(108 + 108 * 2) end", 0);
    assert_eq!(out, "324\n");
}
#[test]
fn arith_precedence_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(109 + 109 * 2) end", 0);
    assert_eq!(out, "327\n");
}
#[test]
fn arith_precedence_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(110 + 110 * 2) end", 0);
    assert_eq!(out, "330\n");
}
#[test]
fn arith_precedence_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(111 + 111 * 2) end", 0);
    assert_eq!(out, "333\n");
}
#[test]
fn arith_precedence_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(112 + 112 * 2) end", 0);
    assert_eq!(out, "336\n");
}
#[test]
fn arith_precedence_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(113 + 113 * 2) end", 0);
    assert_eq!(out, "339\n");
}
#[test]
fn arith_precedence_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(114 + 114 * 2) end", 0);
    assert_eq!(out, "342\n");
}
#[test]
fn arith_precedence_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(115 + 115 * 2) end", 0);
    assert_eq!(out, "345\n");
}
#[test]
fn arith_precedence_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(116 + 116 * 2) end", 0);
    assert_eq!(out, "348\n");
}
#[test]
fn arith_precedence_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(117 + 117 * 2) end", 0);
    assert_eq!(out, "351\n");
}
#[test]
fn arith_precedence_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(118 + 118 * 2) end", 0);
    assert_eq!(out, "354\n");
}
#[test]
fn arith_precedence_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(119 + 119 * 2) end", 0);
    assert_eq!(out, "357\n");
}
#[test]
fn arith_precedence_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(120 + 120 * 2) end", 0);
    assert_eq!(out, "360\n");
}
#[test]
fn arith_precedence_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(121 + 121 * 2) end", 0);
    assert_eq!(out, "363\n");
}
#[test]
fn arith_precedence_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(122 + 122 * 2) end", 0);
    assert_eq!(out, "366\n");
}
#[test]
fn arith_precedence_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(123 + 123 * 2) end", 0);
    assert_eq!(out, "369\n");
}
#[test]
fn arith_precedence_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(124 + 124 * 2) end", 0);
    assert_eq!(out, "372\n");
}
#[test]
fn arith_precedence_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(125 + 125 * 2) end", 0);
    assert_eq!(out, "375\n");
}
#[test]
fn arith_precedence_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(126 + 126 * 2) end", 0);
    assert_eq!(out, "378\n");
}
#[test]
fn arith_precedence_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(127 + 127 * 2) end", 0);
    assert_eq!(out, "381\n");
}
#[test]
fn arith_precedence_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(128 + 128 * 2) end", 0);
    assert_eq!(out, "384\n");
}
#[test]
fn arith_precedence_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(129 + 129 * 2) end", 0);
    assert_eq!(out, "387\n");
}
#[test]
fn arith_precedence_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(130 + 130 * 2) end", 0);
    assert_eq!(out, "390\n");
}
#[test]
fn arith_precedence_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(131 + 131 * 2) end", 0);
    assert_eq!(out, "393\n");
}
#[test]
fn arith_precedence_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(132 + 132 * 2) end", 0);
    assert_eq!(out, "396\n");
}
#[test]
fn arith_precedence_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(133 + 133 * 2) end", 0);
    assert_eq!(out, "399\n");
}
#[test]
fn arith_precedence_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(134 + 134 * 2) end", 0);
    assert_eq!(out, "402\n");
}
#[test]
fn arith_precedence_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(135 + 135 * 2) end", 0);
    assert_eq!(out, "405\n");
}
#[test]
fn arith_precedence_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(136 + 136 * 2) end", 0);
    assert_eq!(out, "408\n");
}
#[test]
fn arith_precedence_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(137 + 137 * 2) end", 0);
    assert_eq!(out, "411\n");
}
#[test]
fn arith_precedence_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(138 + 138 * 2) end", 0);
    assert_eq!(out, "414\n");
}
#[test]
fn arith_precedence_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(139 + 139 * 2) end", 0);
    assert_eq!(out, "417\n");
}
#[test]
fn arith_precedence_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(140 + 140 * 2) end", 0);
    assert_eq!(out, "420\n");
}
#[test]
fn arith_precedence_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(141 + 141 * 2) end", 0);
    assert_eq!(out, "423\n");
}
#[test]
fn arith_precedence_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(142 + 142 * 2) end", 0);
    assert_eq!(out, "426\n");
}
#[test]
fn arith_precedence_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(143 + 143 * 2) end", 0);
    assert_eq!(out, "429\n");
}
#[test]
fn arith_precedence_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(144 + 144 * 2) end", 0);
    assert_eq!(out, "432\n");
}
#[test]
fn arith_precedence_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(145 + 145 * 2) end", 0);
    assert_eq!(out, "435\n");
}
#[test]
fn arith_precedence_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(146 + 146 * 2) end", 0);
    assert_eq!(out, "438\n");
}
#[test]
fn arith_precedence_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(147 + 147 * 2) end", 0);
    assert_eq!(out, "441\n");
}
#[test]
fn arith_precedence_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(148 + 148 * 2) end", 0);
    assert_eq!(out, "444\n");
}
#[test]
fn arith_precedence_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(149 + 149 * 2) end", 0);
    assert_eq!(out, "447\n");
}
#[test]
fn arith_precedence_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(150 + 150 * 2) end", 0);
    assert_eq!(out, "450\n");
}
#[test]
fn arith_precedence_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(151 + 151 * 2) end", 0);
    assert_eq!(out, "453\n");
}
#[test]
fn arith_precedence_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(152 + 152 * 2) end", 0);
    assert_eq!(out, "456\n");
}
#[test]
fn arith_precedence_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(153 + 153 * 2) end", 0);
    assert_eq!(out, "459\n");
}
#[test]
fn arith_precedence_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(154 + 154 * 2) end", 0);
    assert_eq!(out, "462\n");
}
#[test]
fn arith_precedence_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(155 + 155 * 2) end", 0);
    assert_eq!(out, "465\n");
}
#[test]
fn arith_precedence_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(156 + 156 * 2) end", 0);
    assert_eq!(out, "468\n");
}
#[test]
fn arith_precedence_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(157 + 157 * 2) end", 0);
    assert_eq!(out, "471\n");
}
#[test]
fn arith_precedence_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(158 + 158 * 2) end", 0);
    assert_eq!(out, "474\n");
}
#[test]
fn arith_precedence_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(159 + 159 * 2) end", 0);
    assert_eq!(out, "477\n");
}
#[test]
fn arith_precedence_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(160 + 160 * 2) end", 0);
    assert_eq!(out, "480\n");
}
#[test]
fn arith_precedence_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(161 + 161 * 2) end", 0);
    assert_eq!(out, "483\n");
}
#[test]
fn arith_precedence_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(162 + 162 * 2) end", 0);
    assert_eq!(out, "486\n");
}
#[test]
fn arith_precedence_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(163 + 163 * 2) end", 0);
    assert_eq!(out, "489\n");
}
#[test]
fn arith_precedence_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(164 + 164 * 2) end", 0);
    assert_eq!(out, "492\n");
}
#[test]
fn arith_precedence_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(165 + 165 * 2) end", 0);
    assert_eq!(out, "495\n");
}
#[test]
fn arith_precedence_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(166 + 166 * 2) end", 0);
    assert_eq!(out, "498\n");
}
#[test]
fn arith_precedence_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(167 + 167 * 2) end", 0);
    assert_eq!(out, "501\n");
}
#[test]
fn arith_precedence_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(168 + 168 * 2) end", 0);
    assert_eq!(out, "504\n");
}
#[test]
fn arith_precedence_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(169 + 169 * 2) end", 0);
    assert_eq!(out, "507\n");
}
#[test]
fn arith_precedence_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(170 + 170 * 2) end", 0);
    assert_eq!(out, "510\n");
}
#[test]
fn arith_precedence_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(171 + 171 * 2) end", 0);
    assert_eq!(out, "513\n");
}
#[test]
fn arith_precedence_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(172 + 172 * 2) end", 0);
    assert_eq!(out, "516\n");
}
#[test]
fn arith_precedence_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(173 + 173 * 2) end", 0);
    assert_eq!(out, "519\n");
}
#[test]
fn arith_precedence_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(174 + 174 * 2) end", 0);
    assert_eq!(out, "522\n");
}
#[test]
fn arith_precedence_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(175 + 175 * 2) end", 0);
    assert_eq!(out, "525\n");
}
#[test]
fn arith_precedence_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(176 + 176 * 2) end", 0);
    assert_eq!(out, "528\n");
}
#[test]
fn arith_precedence_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(177 + 177 * 2) end", 0);
    assert_eq!(out, "531\n");
}
#[test]
fn arith_precedence_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(178 + 178 * 2) end", 0);
    assert_eq!(out, "534\n");
}
#[test]
fn arith_precedence_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(179 + 179 * 2) end", 0);
    assert_eq!(out, "537\n");
}
#[test]
fn arith_precedence_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(180 + 180 * 2) end", 0);
    assert_eq!(out, "540\n");
}
#[test]
fn arith_precedence_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(181 + 181 * 2) end", 0);
    assert_eq!(out, "543\n");
}
#[test]
fn arith_precedence_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(182 + 182 * 2) end", 0);
    assert_eq!(out, "546\n");
}
#[test]
fn arith_precedence_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(183 + 183 * 2) end", 0);
    assert_eq!(out, "549\n");
}
#[test]
fn arith_precedence_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(184 + 184 * 2) end", 0);
    assert_eq!(out, "552\n");
}
#[test]
fn arith_precedence_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(185 + 185 * 2) end", 0);
    assert_eq!(out, "555\n");
}
#[test]
fn arith_precedence_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(186 + 186 * 2) end", 0);
    assert_eq!(out, "558\n");
}
#[test]
fn arith_precedence_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(187 + 187 * 2) end", 0);
    assert_eq!(out, "561\n");
}
#[test]
fn arith_precedence_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(188 + 188 * 2) end", 0);
    assert_eq!(out, "564\n");
}
#[test]
fn arith_precedence_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(189 + 189 * 2) end", 0);
    assert_eq!(out, "567\n");
}
#[test]
fn arith_precedence_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(190 + 190 * 2) end", 0);
    assert_eq!(out, "570\n");
}
#[test]
fn arith_precedence_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(191 + 191 * 2) end", 0);
    assert_eq!(out, "573\n");
}
#[test]
fn arith_precedence_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(192 + 192 * 2) end", 0);
    assert_eq!(out, "576\n");
}
#[test]
fn arith_precedence_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(193 + 193 * 2) end", 0);
    assert_eq!(out, "579\n");
}
#[test]
fn arith_precedence_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(194 + 194 * 2) end", 0);
    assert_eq!(out, "582\n");
}
#[test]
fn arith_precedence_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(195 + 195 * 2) end", 0);
    assert_eq!(out, "585\n");
}
#[test]
fn arith_precedence_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(196 + 196 * 2) end", 0);
    assert_eq!(out, "588\n");
}
#[test]
fn arith_precedence_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(197 + 197 * 2) end", 0);
    assert_eq!(out, "591\n");
}
#[test]
fn arith_precedence_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(198 + 198 * 2) end", 0);
    assert_eq!(out, "594\n");
}
#[test]
fn arith_precedence_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(199 + 199 * 2) end", 0);
    assert_eq!(out, "597\n");
}
#[test]
fn arith_precedence_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(200 + 200 * 2) end", 0);
    assert_eq!(out, "600\n");
}
#[test]
fn arith_precedence_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(201 + 201 * 2) end", 0);
    assert_eq!(out, "603\n");
}
#[test]
fn arith_precedence_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(202 + 202 * 2) end", 0);
    assert_eq!(out, "606\n");
}
#[test]
fn arith_precedence_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(203 + 203 * 2) end", 0);
    assert_eq!(out, "609\n");
}
#[test]
fn arith_precedence_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(204 + 204 * 2) end", 0);
    assert_eq!(out, "612\n");
}
#[test]
fn arith_precedence_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(205 + 205 * 2) end", 0);
    assert_eq!(out, "615\n");
}
#[test]
fn arith_precedence_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(206 + 206 * 2) end", 0);
    assert_eq!(out, "618\n");
}
#[test]
fn arith_precedence_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(207 + 207 * 2) end", 0);
    assert_eq!(out, "621\n");
}
#[test]
fn arith_precedence_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(208 + 208 * 2) end", 0);
    assert_eq!(out, "624\n");
}
#[test]
fn arith_precedence_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(209 + 209 * 2) end", 0);
    assert_eq!(out, "627\n");
}
#[test]
fn arith_precedence_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(210 + 210 * 2) end", 0);
    assert_eq!(out, "630\n");
}
#[test]
fn arith_precedence_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(211 + 211 * 2) end", 0);
    assert_eq!(out, "633\n");
}
#[test]
fn arith_precedence_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(212 + 212 * 2) end", 0);
    assert_eq!(out, "636\n");
}
#[test]
fn arith_precedence_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(213 + 213 * 2) end", 0);
    assert_eq!(out, "639\n");
}
#[test]
fn arith_precedence_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(214 + 214 * 2) end", 0);
    assert_eq!(out, "642\n");
}
#[test]
fn arith_precedence_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(215 + 215 * 2) end", 0);
    assert_eq!(out, "645\n");
}
#[test]
fn arith_precedence_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(216 + 216 * 2) end", 0);
    assert_eq!(out, "648\n");
}
#[test]
fn arith_precedence_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(217 + 217 * 2) end", 0);
    assert_eq!(out, "651\n");
}
#[test]
fn arith_precedence_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(218 + 218 * 2) end", 0);
    assert_eq!(out, "654\n");
}
#[test]
fn arith_precedence_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(219 + 219 * 2) end", 0);
    assert_eq!(out, "657\n");
}
#[test]
fn arith_precedence_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(220 + 220 * 2) end", 0);
    assert_eq!(out, "660\n");
}
#[test]
fn arith_precedence_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(221 + 221 * 2) end", 0);
    assert_eq!(out, "663\n");
}
#[test]
fn arith_precedence_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(222 + 222 * 2) end", 0);
    assert_eq!(out, "666\n");
}
#[test]
fn arith_precedence_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(223 + 223 * 2) end", 0);
    assert_eq!(out, "669\n");
}
#[test]
fn arith_precedence_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(224 + 224 * 2) end", 0);
    assert_eq!(out, "672\n");
}
#[test]
fn arith_precedence_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(225 + 225 * 2) end", 0);
    assert_eq!(out, "675\n");
}
#[test]
fn arith_precedence_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(226 + 226 * 2) end", 0);
    assert_eq!(out, "678\n");
}
#[test]
fn arith_precedence_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(227 + 227 * 2) end", 0);
    assert_eq!(out, "681\n");
}
#[test]
fn arith_precedence_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(228 + 228 * 2) end", 0);
    assert_eq!(out, "684\n");
}
#[test]
fn arith_precedence_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(229 + 229 * 2) end", 0);
    assert_eq!(out, "687\n");
}
#[test]
fn arith_precedence_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(230 + 230 * 2) end", 0);
    assert_eq!(out, "690\n");
}
#[test]
fn arith_precedence_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(231 + 231 * 2) end", 0);
    assert_eq!(out, "693\n");
}
#[test]
fn arith_precedence_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(232 + 232 * 2) end", 0);
    assert_eq!(out, "696\n");
}
#[test]
fn arith_precedence_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(233 + 233 * 2) end", 0);
    assert_eq!(out, "699\n");
}
#[test]
fn arith_precedence_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(234 + 234 * 2) end", 0);
    assert_eq!(out, "702\n");
}
#[test]
fn arith_precedence_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(235 + 235 * 2) end", 0);
    assert_eq!(out, "705\n");
}
#[test]
fn arith_precedence_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(236 + 236 * 2) end", 0);
    assert_eq!(out, "708\n");
}
#[test]
fn arith_precedence_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(237 + 237 * 2) end", 0);
    assert_eq!(out, "711\n");
}
#[test]
fn arith_precedence_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(238 + 238 * 2) end", 0);
    assert_eq!(out, "714\n");
}
#[test]
fn arith_precedence_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(239 + 239 * 2) end", 0);
    assert_eq!(out, "717\n");
}
#[test]
fn arith_precedence_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(240 + 240 * 2) end", 0);
    assert_eq!(out, "720\n");
}
#[test]
fn arith_precedence_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(241 + 241 * 2) end", 0);
    assert_eq!(out, "723\n");
}
#[test]
fn arith_precedence_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(242 + 242 * 2) end", 0);
    assert_eq!(out, "726\n");
}
#[test]
fn arith_precedence_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(243 + 243 * 2) end", 0);
    assert_eq!(out, "729\n");
}
#[test]
fn arith_precedence_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(244 + 244 * 2) end", 0);
    assert_eq!(out, "732\n");
}
#[test]
fn arith_precedence_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(245 + 245 * 2) end", 0);
    assert_eq!(out, "735\n");
}
#[test]
fn arith_precedence_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(246 + 246 * 2) end", 0);
    assert_eq!(out, "738\n");
}
#[test]
fn arith_precedence_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(247 + 247 * 2) end", 0);
    assert_eq!(out, "741\n");
}
#[test]
fn arith_precedence_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(248 + 248 * 2) end", 0);
    assert_eq!(out, "744\n");
}
#[test]
fn arith_precedence_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(249 + 249 * 2) end", 0);
    assert_eq!(out, "747\n");
}
#[test]
fn arith_precedence_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(250 + 250 * 2) end", 0);
    assert_eq!(out, "750\n");
}
#[test]
fn arith_precedence_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(251 + 251 * 2) end", 0);
    assert_eq!(out, "753\n");
}
#[test]
fn arith_precedence_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(252 + 252 * 2) end", 0);
    assert_eq!(out, "756\n");
}
#[test]
fn arith_precedence_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(253 + 253 * 2) end", 0);
    assert_eq!(out, "759\n");
}
#[test]
fn arith_precedence_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(254 + 254 * 2) end", 0);
    assert_eq!(out, "762\n");
}
#[test]
fn arith_precedence_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(255 + 255 * 2) end", 0);
    assert_eq!(out, "765\n");
}
#[test]
fn arith_precedence_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(256 + 256 * 2) end", 0);
    assert_eq!(out, "768\n");
}
#[test]
fn arith_precedence_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(257 + 257 * 2) end", 0);
    assert_eq!(out, "771\n");
}
#[test]
fn arith_precedence_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(258 + 258 * 2) end", 0);
    assert_eq!(out, "774\n");
}
#[test]
fn arith_precedence_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(259 + 259 * 2) end", 0);
    assert_eq!(out, "777\n");
}
#[test]
fn arith_precedence_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(260 + 260 * 2) end", 0);
    assert_eq!(out, "780\n");
}
#[test]
fn arith_precedence_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(261 + 261 * 2) end", 0);
    assert_eq!(out, "783\n");
}
#[test]
fn arith_precedence_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(262 + 262 * 2) end", 0);
    assert_eq!(out, "786\n");
}
#[test]
fn arith_precedence_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(263 + 263 * 2) end", 0);
    assert_eq!(out, "789\n");
}
#[test]
fn arith_precedence_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(264 + 264 * 2) end", 0);
    assert_eq!(out, "792\n");
}
#[test]
fn arith_precedence_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(265 + 265 * 2) end", 0);
    assert_eq!(out, "795\n");
}
#[test]
fn arith_precedence_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(266 + 266 * 2) end", 0);
    assert_eq!(out, "798\n");
}
#[test]
fn arith_precedence_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(267 + 267 * 2) end", 0);
    assert_eq!(out, "801\n");
}
#[test]
fn arith_precedence_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(268 + 268 * 2) end", 0);
    assert_eq!(out, "804\n");
}
#[test]
fn arith_precedence_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(269 + 269 * 2) end", 0);
    assert_eq!(out, "807\n");
}
#[test]
fn arith_precedence_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(270 + 270 * 2) end", 0);
    assert_eq!(out, "810\n");
}
#[test]
fn arith_precedence_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(271 + 271 * 2) end", 0);
    assert_eq!(out, "813\n");
}
#[test]
fn arith_precedence_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(272 + 272 * 2) end", 0);
    assert_eq!(out, "816\n");
}
#[test]
fn arith_precedence_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(273 + 273 * 2) end", 0);
    assert_eq!(out, "819\n");
}
#[test]
fn arith_precedence_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(274 + 274 * 2) end", 0);
    assert_eq!(out, "822\n");
}
#[test]
fn arith_precedence_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(275 + 275 * 2) end", 0);
    assert_eq!(out, "825\n");
}
#[test]
fn arith_precedence_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(276 + 276 * 2) end", 0);
    assert_eq!(out, "828\n");
}
#[test]
fn arith_precedence_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(277 + 277 * 2) end", 0);
    assert_eq!(out, "831\n");
}
#[test]
fn arith_precedence_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(278 + 278 * 2) end", 0);
    assert_eq!(out, "834\n");
}
#[test]
fn arith_precedence_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(279 + 279 * 2) end", 0);
    assert_eq!(out, "837\n");
}
#[test]
fn arith_precedence_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(280 + 280 * 2) end", 0);
    assert_eq!(out, "840\n");
}
#[test]
fn arith_precedence_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(281 + 281 * 2) end", 0);
    assert_eq!(out, "843\n");
}
#[test]
fn arith_precedence_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(282 + 282 * 2) end", 0);
    assert_eq!(out, "846\n");
}
#[test]
fn arith_precedence_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(283 + 283 * 2) end", 0);
    assert_eq!(out, "849\n");
}
#[test]
fn arith_precedence_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(284 + 284 * 2) end", 0);
    assert_eq!(out, "852\n");
}
#[test]
fn arith_precedence_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(285 + 285 * 2) end", 0);
    assert_eq!(out, "855\n");
}
#[test]
fn arith_precedence_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(286 + 286 * 2) end", 0);
    assert_eq!(out, "858\n");
}
#[test]
fn arith_precedence_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(287 + 287 * 2) end", 0);
    assert_eq!(out, "861\n");
}
#[test]
fn arith_precedence_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(288 + 288 * 2) end", 0);
    assert_eq!(out, "864\n");
}
#[test]
fn arith_precedence_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(289 + 289 * 2) end", 0);
    assert_eq!(out, "867\n");
}
#[test]
fn arith_precedence_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(290 + 290 * 2) end", 0);
    assert_eq!(out, "870\n");
}
#[test]
fn arith_precedence_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(291 + 291 * 2) end", 0);
    assert_eq!(out, "873\n");
}
#[test]
fn arith_precedence_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(292 + 292 * 2) end", 0);
    assert_eq!(out, "876\n");
}
#[test]
fn arith_precedence_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(293 + 293 * 2) end", 0);
    assert_eq!(out, "879\n");
}
#[test]
fn arith_precedence_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(294 + 294 * 2) end", 0);
    assert_eq!(out, "882\n");
}
#[test]
fn arith_precedence_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(295 + 295 * 2) end", 0);
    assert_eq!(out, "885\n");
}
#[test]
fn arith_precedence_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(296 + 296 * 2) end", 0);
    assert_eq!(out, "888\n");
}
#[test]
fn arith_precedence_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(297 + 297 * 2) end", 0);
    assert_eq!(out, "891\n");
}
#[test]
fn arith_precedence_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(298 + 298 * 2) end", 0);
    assert_eq!(out, "894\n");
}
#[test]
fn arith_precedence_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(299 + 299 * 2) end", 0);
    assert_eq!(out, "897\n");
}
#[test]
fn arith_precedence_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(300 + 300 * 2) end", 0);
    assert_eq!(out, "900\n");
}
#[test]
fn arith_precedence_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(301 + 301 * 2) end", 0);
    assert_eq!(out, "903\n");
}
#[test]
fn arith_precedence_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(302 + 302 * 2) end", 0);
    assert_eq!(out, "906\n");
}
#[test]
fn arith_precedence_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(303 + 303 * 2) end", 0);
    assert_eq!(out, "909\n");
}
#[test]
fn arith_precedence_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(304 + 304 * 2) end", 0);
    assert_eq!(out, "912\n");
}
#[test]
fn arith_precedence_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(305 + 305 * 2) end", 0);
    assert_eq!(out, "915\n");
}
#[test]
fn arith_precedence_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(306 + 306 * 2) end", 0);
    assert_eq!(out, "918\n");
}
#[test]
fn arith_precedence_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(307 + 307 * 2) end", 0);
    assert_eq!(out, "921\n");
}
#[test]
fn arith_precedence_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(308 + 308 * 2) end", 0);
    assert_eq!(out, "924\n");
}
#[test]
fn arith_precedence_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(309 + 309 * 2) end", 0);
    assert_eq!(out, "927\n");
}
#[test]
fn arith_precedence_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(310 + 310 * 2) end", 0);
    assert_eq!(out, "930\n");
}
#[test]
fn arith_precedence_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(311 + 311 * 2) end", 0);
    assert_eq!(out, "933\n");
}
#[test]
fn arith_precedence_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(312 + 312 * 2) end", 0);
    assert_eq!(out, "936\n");
}
#[test]
fn arith_precedence_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(313 + 313 * 2) end", 0);
    assert_eq!(out, "939\n");
}
#[test]
fn arith_precedence_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(314 + 314 * 2) end", 0);
    assert_eq!(out, "942\n");
}
#[test]
fn arith_precedence_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(315 + 315 * 2) end", 0);
    assert_eq!(out, "945\n");
}
#[test]
fn arith_precedence_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(316 + 316 * 2) end", 0);
    assert_eq!(out, "948\n");
}
#[test]
fn arith_precedence_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(317 + 317 * 2) end", 0);
    assert_eq!(out, "951\n");
}
#[test]
fn arith_precedence_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(318 + 318 * 2) end", 0);
    assert_eq!(out, "954\n");
}
#[test]
fn arith_precedence_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(319 + 319 * 2) end", 0);
    assert_eq!(out, "957\n");
}
#[test]
fn arith_precedence_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(320 + 320 * 2) end", 0);
    assert_eq!(out, "960\n");
}
#[test]
fn arith_precedence_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(321 + 321 * 2) end", 0);
    assert_eq!(out, "963\n");
}
#[test]
fn arith_precedence_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(322 + 322 * 2) end", 0);
    assert_eq!(out, "966\n");
}
#[test]
fn arith_precedence_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(323 + 323 * 2) end", 0);
    assert_eq!(out, "969\n");
}
#[test]
fn arith_precedence_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(324 + 324 * 2) end", 0);
    assert_eq!(out, "972\n");
}
#[test]
fn arith_precedence_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(325 + 325 * 2) end", 0);
    assert_eq!(out, "975\n");
}
#[test]
fn arith_precedence_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(326 + 326 * 2) end", 0);
    assert_eq!(out, "978\n");
}
#[test]
fn arith_precedence_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(327 + 327 * 2) end", 0);
    assert_eq!(out, "981\n");
}
#[test]
fn arith_precedence_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(328 + 328 * 2) end", 0);
    assert_eq!(out, "984\n");
}
#[test]
fn arith_precedence_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(329 + 329 * 2) end", 0);
    assert_eq!(out, "987\n");
}
#[test]
fn arith_precedence_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(330 + 330 * 2) end", 0);
    assert_eq!(out, "990\n");
}
#[test]
fn arith_precedence_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(331 + 331 * 2) end", 0);
    assert_eq!(out, "993\n");
}
#[test]
fn arith_precedence_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(332 + 332 * 2) end", 0);
    assert_eq!(out, "996\n");
}
#[test]
fn arith_precedence_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(333 + 333 * 2) end", 0);
    assert_eq!(out, "999\n");
}
#[test]
fn arith_precedence_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(334 + 334 * 2) end", 0);
    assert_eq!(out, "1002\n");
}
#[test]
fn arith_precedence_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(335 + 335 * 2) end", 0);
    assert_eq!(out, "1005\n");
}
#[test]
fn arith_precedence_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(336 + 336 * 2) end", 0);
    assert_eq!(out, "1008\n");
}
#[test]
fn arith_precedence_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(337 + 337 * 2) end", 0);
    assert_eq!(out, "1011\n");
}
#[test]
fn arith_precedence_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(338 + 338 * 2) end", 0);
    assert_eq!(out, "1014\n");
}
#[test]
fn arith_precedence_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(339 + 339 * 2) end", 0);
    assert_eq!(out, "1017\n");
}
#[test]
fn arith_precedence_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(340 + 340 * 2) end", 0);
    assert_eq!(out, "1020\n");
}
#[test]
fn arith_precedence_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(341 + 341 * 2) end", 0);
    assert_eq!(out, "1023\n");
}
#[test]
fn arith_precedence_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(342 + 342 * 2) end", 0);
    assert_eq!(out, "1026\n");
}
#[test]
fn arith_precedence_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(343 + 343 * 2) end", 0);
    assert_eq!(out, "1029\n");
}
#[test]
fn arith_precedence_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(344 + 344 * 2) end", 0);
    assert_eq!(out, "1032\n");
}
#[test]
fn arith_precedence_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(345 + 345 * 2) end", 0);
    assert_eq!(out, "1035\n");
}
#[test]
fn arith_precedence_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(346 + 346 * 2) end", 0);
    assert_eq!(out, "1038\n");
}
#[test]
fn arith_precedence_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(347 + 347 * 2) end", 0);
    assert_eq!(out, "1041\n");
}
#[test]
fn arith_precedence_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(348 + 348 * 2) end", 0);
    assert_eq!(out, "1044\n");
}
#[test]
fn arith_precedence_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(349 + 349 * 2) end", 0);
    assert_eq!(out, "1047\n");
}
#[test]
fn arith_precedence_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(350 + 350 * 2) end", 0);
    assert_eq!(out, "1050\n");
}
#[test]
fn arith_precedence_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(351 + 351 * 2) end", 0);
    assert_eq!(out, "1053\n");
}
#[test]
fn arith_precedence_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(352 + 352 * 2) end", 0);
    assert_eq!(out, "1056\n");
}
#[test]
fn arith_precedence_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(353 + 353 * 2) end", 0);
    assert_eq!(out, "1059\n");
}
#[test]
fn arith_precedence_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(354 + 354 * 2) end", 0);
    assert_eq!(out, "1062\n");
}
#[test]
fn arith_precedence_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(355 + 355 * 2) end", 0);
    assert_eq!(out, "1065\n");
}
#[test]
fn arith_precedence_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(356 + 356 * 2) end", 0);
    assert_eq!(out, "1068\n");
}
#[test]
fn arith_precedence_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(357 + 357 * 2) end", 0);
    assert_eq!(out, "1071\n");
}
#[test]
fn arith_precedence_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(358 + 358 * 2) end", 0);
    assert_eq!(out, "1074\n");
}
#[test]
fn arith_precedence_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(359 + 359 * 2) end", 0);
    assert_eq!(out, "1077\n");
}
#[test]
fn arith_precedence_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(360 + 360 * 2) end", 0);
    assert_eq!(out, "1080\n");
}
#[test]
fn arith_precedence_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(361 + 361 * 2) end", 0);
    assert_eq!(out, "1083\n");
}
#[test]
fn arith_precedence_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(362 + 362 * 2) end", 0);
    assert_eq!(out, "1086\n");
}
#[test]
fn arith_precedence_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(363 + 363 * 2) end", 0);
    assert_eq!(out, "1089\n");
}
#[test]
fn arith_precedence_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(364 + 364 * 2) end", 0);
    assert_eq!(out, "1092\n");
}
#[test]
fn arith_precedence_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(365 + 365 * 2) end", 0);
    assert_eq!(out, "1095\n");
}
#[test]
fn arith_precedence_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(366 + 366 * 2) end", 0);
    assert_eq!(out, "1098\n");
}
#[test]
fn arith_precedence_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(367 + 367 * 2) end", 0);
    assert_eq!(out, "1101\n");
}
#[test]
fn arith_precedence_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(368 + 368 * 2) end", 0);
    assert_eq!(out, "1104\n");
}
#[test]
fn arith_precedence_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(369 + 369 * 2) end", 0);
    assert_eq!(out, "1107\n");
}
#[test]
fn arith_precedence_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(370 + 370 * 2) end", 0);
    assert_eq!(out, "1110\n");
}
#[test]
fn arith_precedence_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(371 + 371 * 2) end", 0);
    assert_eq!(out, "1113\n");
}
#[test]
fn arith_precedence_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(372 + 372 * 2) end", 0);
    assert_eq!(out, "1116\n");
}
#[test]
fn arith_precedence_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(373 + 373 * 2) end", 0);
    assert_eq!(out, "1119\n");
}
#[test]
fn arith_precedence_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(374 + 374 * 2) end", 0);
    assert_eq!(out, "1122\n");
}
#[test]
fn arith_precedence_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(375 + 375 * 2) end", 0);
    assert_eq!(out, "1125\n");
}
#[test]
fn arith_precedence_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(376 + 376 * 2) end", 0);
    assert_eq!(out, "1128\n");
}
#[test]
fn arith_precedence_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(377 + 377 * 2) end", 0);
    assert_eq!(out, "1131\n");
}
#[test]
fn arith_precedence_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(378 + 378 * 2) end", 0);
    assert_eq!(out, "1134\n");
}
#[test]
fn arith_precedence_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(379 + 379 * 2) end", 0);
    assert_eq!(out, "1137\n");
}
#[test]
fn arith_precedence_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(380 + 380 * 2) end", 0);
    assert_eq!(out, "1140\n");
}
#[test]
fn arith_precedence_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(381 + 381 * 2) end", 0);
    assert_eq!(out, "1143\n");
}
#[test]
fn arith_precedence_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(382 + 382 * 2) end", 0);
    assert_eq!(out, "1146\n");
}
#[test]
fn arith_precedence_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(383 + 383 * 2) end", 0);
    assert_eq!(out, "1149\n");
}
#[test]
fn arith_precedence_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(384 + 384 * 2) end", 0);
    assert_eq!(out, "1152\n");
}
#[test]
fn arith_precedence_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(385 + 385 * 2) end", 0);
    assert_eq!(out, "1155\n");
}
#[test]
fn arith_precedence_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(386 + 386 * 2) end", 0);
    assert_eq!(out, "1158\n");
}
#[test]
fn arith_precedence_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(387 + 387 * 2) end", 0);
    assert_eq!(out, "1161\n");
}
#[test]
fn arith_precedence_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(388 + 388 * 2) end", 0);
    assert_eq!(out, "1164\n");
}
#[test]
fn arith_precedence_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(389 + 389 * 2) end", 0);
    assert_eq!(out, "1167\n");
}
#[test]
fn arith_precedence_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(390 + 390 * 2) end", 0);
    assert_eq!(out, "1170\n");
}
#[test]
fn arith_precedence_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(391 + 391 * 2) end", 0);
    assert_eq!(out, "1173\n");
}
#[test]
fn arith_precedence_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(392 + 392 * 2) end", 0);
    assert_eq!(out, "1176\n");
}
#[test]
fn arith_precedence_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(393 + 393 * 2) end", 0);
    assert_eq!(out, "1179\n");
}
#[test]
fn arith_precedence_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(394 + 394 * 2) end", 0);
    assert_eq!(out, "1182\n");
}
#[test]
fn arith_precedence_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(395 + 395 * 2) end", 0);
    assert_eq!(out, "1185\n");
}
#[test]
fn arith_precedence_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(396 + 396 * 2) end", 0);
    assert_eq!(out, "1188\n");
}
#[test]
fn arith_precedence_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(397 + 397 * 2) end", 0);
    assert_eq!(out, "1191\n");
}
#[test]
fn arith_precedence_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(398 + 398 * 2) end", 0);
    assert_eq!(out, "1194\n");
}
#[test]
fn arith_precedence_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(399 + 399 * 2) end", 0);
    assert_eq!(out, "1197\n");
}
#[test]
fn arith_precedence_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(400 + 400 * 2) end", 0);
    assert_eq!(out, "1200\n");
}
#[test]
fn arith_precedence_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(401 + 401 * 2) end", 0);
    assert_eq!(out, "1203\n");
}
#[test]
fn arith_precedence_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(402 + 402 * 2) end", 0);
    assert_eq!(out, "1206\n");
}
#[test]
fn arith_precedence_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(403 + 403 * 2) end", 0);
    assert_eq!(out, "1209\n");
}
#[test]
fn arith_precedence_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(404 + 404 * 2) end", 0);
    assert_eq!(out, "1212\n");
}
#[test]
fn arith_precedence_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(405 + 405 * 2) end", 0);
    assert_eq!(out, "1215\n");
}
#[test]
fn arith_precedence_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(406 + 406 * 2) end", 0);
    assert_eq!(out, "1218\n");
}
#[test]
fn arith_precedence_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(407 + 407 * 2) end", 0);
    assert_eq!(out, "1221\n");
}
#[test]
fn arith_precedence_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(408 + 408 * 2) end", 0);
    assert_eq!(out, "1224\n");
}
#[test]
fn arith_precedence_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(409 + 409 * 2) end", 0);
    assert_eq!(out, "1227\n");
}
#[test]
fn arith_precedence_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(410 + 410 * 2) end", 0);
    assert_eq!(out, "1230\n");
}
#[test]
fn arith_precedence_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(411 + 411 * 2) end", 0);
    assert_eq!(out, "1233\n");
}
#[test]
fn arith_precedence_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(412 + 412 * 2) end", 0);
    assert_eq!(out, "1236\n");
}
#[test]
fn arith_precedence_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(413 + 413 * 2) end", 0);
    assert_eq!(out, "1239\n");
}
#[test]
fn arith_precedence_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(414 + 414 * 2) end", 0);
    assert_eq!(out, "1242\n");
}
#[test]
fn arith_precedence_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(415 + 415 * 2) end", 0);
    assert_eq!(out, "1245\n");
}
#[test]
fn arith_precedence_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(416 + 416 * 2) end", 0);
    assert_eq!(out, "1248\n");
}
#[test]
fn arith_precedence_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(417 + 417 * 2) end", 0);
    assert_eq!(out, "1251\n");
}
#[test]
fn arith_precedence_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(418 + 418 * 2) end", 0);
    assert_eq!(out, "1254\n");
}
#[test]
fn arith_precedence_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(419 + 419 * 2) end", 0);
    assert_eq!(out, "1257\n");
}
#[test]
fn arith_precedence_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(420 + 420 * 2) end", 0);
    assert_eq!(out, "1260\n");
}
#[test]
fn arith_precedence_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(421 + 421 * 2) end", 0);
    assert_eq!(out, "1263\n");
}
#[test]
fn arith_precedence_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(422 + 422 * 2) end", 0);
    assert_eq!(out, "1266\n");
}
#[test]
fn arith_precedence_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(423 + 423 * 2) end", 0);
    assert_eq!(out, "1269\n");
}
#[test]
fn arith_precedence_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(424 + 424 * 2) end", 0);
    assert_eq!(out, "1272\n");
}
#[test]
fn arith_precedence_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(425 + 425 * 2) end", 0);
    assert_eq!(out, "1275\n");
}
#[test]
fn arith_precedence_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(426 + 426 * 2) end", 0);
    assert_eq!(out, "1278\n");
}
#[test]
fn arith_precedence_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(427 + 427 * 2) end", 0);
    assert_eq!(out, "1281\n");
}
#[test]
fn arith_precedence_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(428 + 428 * 2) end", 0);
    assert_eq!(out, "1284\n");
}
#[test]
fn arith_precedence_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(429 + 429 * 2) end", 0);
    assert_eq!(out, "1287\n");
}
#[test]
fn arith_precedence_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(430 + 430 * 2) end", 0);
    assert_eq!(out, "1290\n");
}
#[test]
fn arith_precedence_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(431 + 431 * 2) end", 0);
    assert_eq!(out, "1293\n");
}
#[test]
fn arith_precedence_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(432 + 432 * 2) end", 0);
    assert_eq!(out, "1296\n");
}
#[test]
fn arith_precedence_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(433 + 433 * 2) end", 0);
    assert_eq!(out, "1299\n");
}
#[test]
fn arith_precedence_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(434 + 434 * 2) end", 0);
    assert_eq!(out, "1302\n");
}
#[test]
fn arith_precedence_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(435 + 435 * 2) end", 0);
    assert_eq!(out, "1305\n");
}
#[test]
fn arith_precedence_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(436 + 436 * 2) end", 0);
    assert_eq!(out, "1308\n");
}
#[test]
fn arith_precedence_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(437 + 437 * 2) end", 0);
    assert_eq!(out, "1311\n");
}
#[test]
fn arith_precedence_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(438 + 438 * 2) end", 0);
    assert_eq!(out, "1314\n");
}
#[test]
fn arith_precedence_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(439 + 439 * 2) end", 0);
    assert_eq!(out, "1317\n");
}
#[test]
fn arith_precedence_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(440 + 440 * 2) end", 0);
    assert_eq!(out, "1320\n");
}
#[test]
fn arith_precedence_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(441 + 441 * 2) end", 0);
    assert_eq!(out, "1323\n");
}
#[test]
fn arith_precedence_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(442 + 442 * 2) end", 0);
    assert_eq!(out, "1326\n");
}
#[test]
fn arith_precedence_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(443 + 443 * 2) end", 0);
    assert_eq!(out, "1329\n");
}
#[test]
fn arith_precedence_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(444 + 444 * 2) end", 0);
    assert_eq!(out, "1332\n");
}
#[test]
fn arith_precedence_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(445 + 445 * 2) end", 0);
    assert_eq!(out, "1335\n");
}
#[test]
fn arith_precedence_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(446 + 446 * 2) end", 0);
    assert_eq!(out, "1338\n");
}
#[test]
fn arith_precedence_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(447 + 447 * 2) end", 0);
    assert_eq!(out, "1341\n");
}
#[test]
fn arith_precedence_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(448 + 448 * 2) end", 0);
    assert_eq!(out, "1344\n");
}
#[test]
fn arith_precedence_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(449 + 449 * 2) end", 0);
    assert_eq!(out, "1347\n");
}
#[test]
fn arith_precedence_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(450 + 450 * 2) end", 0);
    assert_eq!(out, "1350\n");
}
#[test]
fn arith_precedence_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(451 + 451 * 2) end", 0);
    assert_eq!(out, "1353\n");
}
#[test]
fn arith_precedence_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(452 + 452 * 2) end", 0);
    assert_eq!(out, "1356\n");
}
#[test]
fn arith_precedence_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(453 + 453 * 2) end", 0);
    assert_eq!(out, "1359\n");
}
#[test]
fn arith_precedence_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(454 + 454 * 2) end", 0);
    assert_eq!(out, "1362\n");
}
#[test]
fn arith_precedence_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(455 + 455 * 2) end", 0);
    assert_eq!(out, "1365\n");
}
#[test]
fn arith_precedence_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(456 + 456 * 2) end", 0);
    assert_eq!(out, "1368\n");
}
#[test]
fn arith_precedence_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(457 + 457 * 2) end", 0);
    assert_eq!(out, "1371\n");
}
#[test]
fn arith_precedence_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(458 + 458 * 2) end", 0);
    assert_eq!(out, "1374\n");
}
#[test]
fn arith_precedence_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(459 + 459 * 2) end", 0);
    assert_eq!(out, "1377\n");
}
#[test]
fn arith_precedence_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(460 + 460 * 2) end", 0);
    assert_eq!(out, "1380\n");
}
#[test]
fn arith_precedence_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(461 + 461 * 2) end", 0);
    assert_eq!(out, "1383\n");
}
#[test]
fn arith_precedence_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(462 + 462 * 2) end", 0);
    assert_eq!(out, "1386\n");
}
#[test]
fn arith_precedence_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(463 + 463 * 2) end", 0);
    assert_eq!(out, "1389\n");
}
#[test]
fn arith_precedence_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(464 + 464 * 2) end", 0);
    assert_eq!(out, "1392\n");
}
#[test]
fn arith_precedence_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(465 + 465 * 2) end", 0);
    assert_eq!(out, "1395\n");
}
#[test]
fn arith_precedence_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(466 + 466 * 2) end", 0);
    assert_eq!(out, "1398\n");
}
#[test]
fn arith_precedence_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(467 + 467 * 2) end", 0);
    assert_eq!(out, "1401\n");
}
#[test]
fn arith_precedence_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(468 + 468 * 2) end", 0);
    assert_eq!(out, "1404\n");
}
#[test]
fn arith_precedence_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(469 + 469 * 2) end", 0);
    assert_eq!(out, "1407\n");
}
#[test]
fn arith_precedence_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(470 + 470 * 2) end", 0);
    assert_eq!(out, "1410\n");
}
#[test]
fn arith_precedence_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(471 + 471 * 2) end", 0);
    assert_eq!(out, "1413\n");
}
#[test]
fn arith_precedence_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(472 + 472 * 2) end", 0);
    assert_eq!(out, "1416\n");
}
#[test]
fn arith_precedence_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(473 + 473 * 2) end", 0);
    assert_eq!(out, "1419\n");
}
#[test]
fn arith_precedence_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(474 + 474 * 2) end", 0);
    assert_eq!(out, "1422\n");
}
#[test]
fn arith_precedence_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(475 + 475 * 2) end", 0);
    assert_eq!(out, "1425\n");
}
#[test]
fn arith_precedence_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(476 + 476 * 2) end", 0);
    assert_eq!(out, "1428\n");
}
#[test]
fn arith_precedence_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(477 + 477 * 2) end", 0);
    assert_eq!(out, "1431\n");
}
#[test]
fn arith_precedence_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(478 + 478 * 2) end", 0);
    assert_eq!(out, "1434\n");
}
#[test]
fn arith_precedence_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(479 + 479 * 2) end", 0);
    assert_eq!(out, "1437\n");
}
#[test]
fn arith_precedence_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(480 + 480 * 2) end", 0);
    assert_eq!(out, "1440\n");
}
#[test]
fn arith_precedence_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(481 + 481 * 2) end", 0);
    assert_eq!(out, "1443\n");
}
#[test]
fn arith_precedence_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(482 + 482 * 2) end", 0);
    assert_eq!(out, "1446\n");
}
#[test]
fn arith_precedence_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(483 + 483 * 2) end", 0);
    assert_eq!(out, "1449\n");
}
#[test]
fn arith_precedence_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(484 + 484 * 2) end", 0);
    assert_eq!(out, "1452\n");
}
#[test]
fn arith_precedence_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(485 + 485 * 2) end", 0);
    assert_eq!(out, "1455\n");
}
#[test]
fn arith_precedence_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(486 + 486 * 2) end", 0);
    assert_eq!(out, "1458\n");
}
#[test]
fn arith_precedence_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(487 + 487 * 2) end", 0);
    assert_eq!(out, "1461\n");
}
#[test]
fn arith_precedence_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(488 + 488 * 2) end", 0);
    assert_eq!(out, "1464\n");
}
#[test]
fn arith_precedence_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(489 + 489 * 2) end", 0);
    assert_eq!(out, "1467\n");
}
#[test]
fn arith_precedence_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(490 + 490 * 2) end", 0);
    assert_eq!(out, "1470\n");
}
#[test]
fn arith_precedence_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(491 + 491 * 2) end", 0);
    assert_eq!(out, "1473\n");
}
#[test]
fn arith_precedence_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(492 + 492 * 2) end", 0);
    assert_eq!(out, "1476\n");
}
#[test]
fn arith_precedence_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(493 + 493 * 2) end", 0);
    assert_eq!(out, "1479\n");
}
#[test]
fn arith_precedence_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(494 + 494 * 2) end", 0);
    assert_eq!(out, "1482\n");
}
#[test]
fn arith_precedence_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(495 + 495 * 2) end", 0);
    assert_eq!(out, "1485\n");
}
#[test]
fn arith_precedence_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(496 + 496 * 2) end", 0);
    assert_eq!(out, "1488\n");
}
#[test]
fn arith_precedence_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(497 + 497 * 2) end", 0);
    assert_eq!(out, "1491\n");
}
#[test]
fn arith_precedence_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(498 + 498 * 2) end", 0);
    assert_eq!(out, "1494\n");
}
#[test]
fn arith_precedence_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(499 + 499 * 2) end", 0);
    assert_eq!(out, "1497\n");
}
#[test]
fn interp_if_elseif_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 0 % 3 == 0 then print(\"a\") elseif 0 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 1 % 3 == 0 then print(\"a\") elseif 1 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 2 % 3 == 0 then print(\"a\") elseif 2 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 3 % 3 == 0 then print(\"a\") elseif 3 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 4 % 3 == 0 then print(\"a\") elseif 4 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 5 % 3 == 0 then print(\"a\") elseif 5 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 6 % 3 == 0 then print(\"a\") elseif 6 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 7 % 3 == 0 then print(\"a\") elseif 7 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 8 % 3 == 0 then print(\"a\") elseif 8 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 9 % 3 == 0 then print(\"a\") elseif 9 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 10 % 3 == 0 then print(\"a\") elseif 10 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 11 % 3 == 0 then print(\"a\") elseif 11 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 12 % 3 == 0 then print(\"a\") elseif 12 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 13 % 3 == 0 then print(\"a\") elseif 13 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 14 % 3 == 0 then print(\"a\") elseif 14 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 15 % 3 == 0 then print(\"a\") elseif 15 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 16 % 3 == 0 then print(\"a\") elseif 16 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 17 % 3 == 0 then print(\"a\") elseif 17 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 18 % 3 == 0 then print(\"a\") elseif 18 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 19 % 3 == 0 then print(\"a\") elseif 19 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 20 % 3 == 0 then print(\"a\") elseif 20 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 21 % 3 == 0 then print(\"a\") elseif 21 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 22 % 3 == 0 then print(\"a\") elseif 22 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 23 % 3 == 0 then print(\"a\") elseif 23 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 24 % 3 == 0 then print(\"a\") elseif 24 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 25 % 3 == 0 then print(\"a\") elseif 25 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 26 % 3 == 0 then print(\"a\") elseif 26 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 27 % 3 == 0 then print(\"a\") elseif 27 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 28 % 3 == 0 then print(\"a\") elseif 28 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 29 % 3 == 0 then print(\"a\") elseif 29 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 30 % 3 == 0 then print(\"a\") elseif 30 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 31 % 3 == 0 then print(\"a\") elseif 31 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 32 % 3 == 0 then print(\"a\") elseif 32 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 33 % 3 == 0 then print(\"a\") elseif 33 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 34 % 3 == 0 then print(\"a\") elseif 34 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 35 % 3 == 0 then print(\"a\") elseif 35 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 36 % 3 == 0 then print(\"a\") elseif 36 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 37 % 3 == 0 then print(\"a\") elseif 37 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 38 % 3 == 0 then print(\"a\") elseif 38 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 39 % 3 == 0 then print(\"a\") elseif 39 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 40 % 3 == 0 then print(\"a\") elseif 40 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 41 % 3 == 0 then print(\"a\") elseif 41 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 42 % 3 == 0 then print(\"a\") elseif 42 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 43 % 3 == 0 then print(\"a\") elseif 43 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 44 % 3 == 0 then print(\"a\") elseif 44 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 45 % 3 == 0 then print(\"a\") elseif 45 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 46 % 3 == 0 then print(\"a\") elseif 46 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 47 % 3 == 0 then print(\"a\") elseif 47 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 48 % 3 == 0 then print(\"a\") elseif 48 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 49 % 3 == 0 then print(\"a\") elseif 49 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 50 % 3 == 0 then print(\"a\") elseif 50 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 51 % 3 == 0 then print(\"a\") elseif 51 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 52 % 3 == 0 then print(\"a\") elseif 52 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 53 % 3 == 0 then print(\"a\") elseif 53 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 54 % 3 == 0 then print(\"a\") elseif 54 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 55 % 3 == 0 then print(\"a\") elseif 55 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 56 % 3 == 0 then print(\"a\") elseif 56 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 57 % 3 == 0 then print(\"a\") elseif 57 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 58 % 3 == 0 then print(\"a\") elseif 58 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 59 % 3 == 0 then print(\"a\") elseif 59 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 60 % 3 == 0 then print(\"a\") elseif 60 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 61 % 3 == 0 then print(\"a\") elseif 61 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 62 % 3 == 0 then print(\"a\") elseif 62 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 63 % 3 == 0 then print(\"a\") elseif 63 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 64 % 3 == 0 then print(\"a\") elseif 64 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 65 % 3 == 0 then print(\"a\") elseif 65 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 66 % 3 == 0 then print(\"a\") elseif 66 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 67 % 3 == 0 then print(\"a\") elseif 67 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 68 % 3 == 0 then print(\"a\") elseif 68 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 69 % 3 == 0 then print(\"a\") elseif 69 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 70 % 3 == 0 then print(\"a\") elseif 70 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 71 % 3 == 0 then print(\"a\") elseif 71 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 72 % 3 == 0 then print(\"a\") elseif 72 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 73 % 3 == 0 then print(\"a\") elseif 73 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 74 % 3 == 0 then print(\"a\") elseif 74 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 75 % 3 == 0 then print(\"a\") elseif 75 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 76 % 3 == 0 then print(\"a\") elseif 76 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 77 % 3 == 0 then print(\"a\") elseif 77 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 78 % 3 == 0 then print(\"a\") elseif 78 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 79 % 3 == 0 then print(\"a\") elseif 79 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 80 % 3 == 0 then print(\"a\") elseif 80 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 81 % 3 == 0 then print(\"a\") elseif 81 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 82 % 3 == 0 then print(\"a\") elseif 82 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 83 % 3 == 0 then print(\"a\") elseif 83 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 84 % 3 == 0 then print(\"a\") elseif 84 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 85 % 3 == 0 then print(\"a\") elseif 85 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 86 % 3 == 0 then print(\"a\") elseif 86 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 87 % 3 == 0 then print(\"a\") elseif 87 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 88 % 3 == 0 then print(\"a\") elseif 88 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 89 % 3 == 0 then print(\"a\") elseif 89 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 90 % 3 == 0 then print(\"a\") elseif 90 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 91 % 3 == 0 then print(\"a\") elseif 91 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 92 % 3 == 0 then print(\"a\") elseif 92 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 93 % 3 == 0 then print(\"a\") elseif 93 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 94 % 3 == 0 then print(\"a\") elseif 94 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 95 % 3 == 0 then print(\"a\") elseif 95 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 96 % 3 == 0 then print(\"a\") elseif 96 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 97 % 3 == 0 then print(\"a\") elseif 97 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 98 % 3 == 0 then print(\"a\") elseif 98 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 99 % 3 == 0 then print(\"a\") elseif 99 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 100 % 3 == 0 then print(\"a\") elseif 100 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 101 % 3 == 0 then print(\"a\") elseif 101 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 102 % 3 == 0 then print(\"a\") elseif 102 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 103 % 3 == 0 then print(\"a\") elseif 103 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 104 % 3 == 0 then print(\"a\") elseif 104 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 105 % 3 == 0 then print(\"a\") elseif 105 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 106 % 3 == 0 then print(\"a\") elseif 106 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 107 % 3 == 0 then print(\"a\") elseif 107 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 108 % 3 == 0 then print(\"a\") elseif 108 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 109 % 3 == 0 then print(\"a\") elseif 109 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 110 % 3 == 0 then print(\"a\") elseif 110 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 111 % 3 == 0 then print(\"a\") elseif 111 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 112 % 3 == 0 then print(\"a\") elseif 112 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 113 % 3 == 0 then print(\"a\") elseif 113 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 114 % 3 == 0 then print(\"a\") elseif 114 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 115 % 3 == 0 then print(\"a\") elseif 115 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 116 % 3 == 0 then print(\"a\") elseif 116 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 117 % 3 == 0 then print(\"a\") elseif 117 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 118 % 3 == 0 then print(\"a\") elseif 118 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 119 % 3 == 0 then print(\"a\") elseif 119 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 120 % 3 == 0 then print(\"a\") elseif 120 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 121 % 3 == 0 then print(\"a\") elseif 121 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 122 % 3 == 0 then print(\"a\") elseif 122 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 123 % 3 == 0 then print(\"a\") elseif 123 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 124 % 3 == 0 then print(\"a\") elseif 124 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 125 % 3 == 0 then print(\"a\") elseif 125 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 126 % 3 == 0 then print(\"a\") elseif 126 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 127 % 3 == 0 then print(\"a\") elseif 127 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 128 % 3 == 0 then print(\"a\") elseif 128 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 129 % 3 == 0 then print(\"a\") elseif 129 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 130 % 3 == 0 then print(\"a\") elseif 130 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 131 % 3 == 0 then print(\"a\") elseif 131 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 132 % 3 == 0 then print(\"a\") elseif 132 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 133 % 3 == 0 then print(\"a\") elseif 133 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 134 % 3 == 0 then print(\"a\") elseif 134 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 135 % 3 == 0 then print(\"a\") elseif 135 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 136 % 3 == 0 then print(\"a\") elseif 136 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 137 % 3 == 0 then print(\"a\") elseif 137 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 138 % 3 == 0 then print(\"a\") elseif 138 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 139 % 3 == 0 then print(\"a\") elseif 139 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 140 % 3 == 0 then print(\"a\") elseif 140 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 141 % 3 == 0 then print(\"a\") elseif 141 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 142 % 3 == 0 then print(\"a\") elseif 142 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 143 % 3 == 0 then print(\"a\") elseif 143 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 144 % 3 == 0 then print(\"a\") elseif 144 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 145 % 3 == 0 then print(\"a\") elseif 145 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 146 % 3 == 0 then print(\"a\") elseif 146 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 147 % 3 == 0 then print(\"a\") elseif 147 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 148 % 3 == 0 then print(\"a\") elseif 148 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 149 % 3 == 0 then print(\"a\") elseif 149 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 150 % 3 == 0 then print(\"a\") elseif 150 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 151 % 3 == 0 then print(\"a\") elseif 151 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 152 % 3 == 0 then print(\"a\") elseif 152 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 153 % 3 == 0 then print(\"a\") elseif 153 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 154 % 3 == 0 then print(\"a\") elseif 154 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 155 % 3 == 0 then print(\"a\") elseif 155 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 156 % 3 == 0 then print(\"a\") elseif 156 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 157 % 3 == 0 then print(\"a\") elseif 157 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 158 % 3 == 0 then print(\"a\") elseif 158 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 159 % 3 == 0 then print(\"a\") elseif 159 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 160 % 3 == 0 then print(\"a\") elseif 160 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 161 % 3 == 0 then print(\"a\") elseif 161 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 162 % 3 == 0 then print(\"a\") elseif 162 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 163 % 3 == 0 then print(\"a\") elseif 163 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 164 % 3 == 0 then print(\"a\") elseif 164 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 165 % 3 == 0 then print(\"a\") elseif 165 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 166 % 3 == 0 then print(\"a\") elseif 166 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 167 % 3 == 0 then print(\"a\") elseif 167 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 168 % 3 == 0 then print(\"a\") elseif 168 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 169 % 3 == 0 then print(\"a\") elseif 169 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 170 % 3 == 0 then print(\"a\") elseif 170 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 171 % 3 == 0 then print(\"a\") elseif 171 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 172 % 3 == 0 then print(\"a\") elseif 172 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 173 % 3 == 0 then print(\"a\") elseif 173 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 174 % 3 == 0 then print(\"a\") elseif 174 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 175 % 3 == 0 then print(\"a\") elseif 175 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 176 % 3 == 0 then print(\"a\") elseif 176 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 177 % 3 == 0 then print(\"a\") elseif 177 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 178 % 3 == 0 then print(\"a\") elseif 178 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 179 % 3 == 0 then print(\"a\") elseif 179 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 180 % 3 == 0 then print(\"a\") elseif 180 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 181 % 3 == 0 then print(\"a\") elseif 181 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 182 % 3 == 0 then print(\"a\") elseif 182 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 183 % 3 == 0 then print(\"a\") elseif 183 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 184 % 3 == 0 then print(\"a\") elseif 184 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 185 % 3 == 0 then print(\"a\") elseif 185 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 186 % 3 == 0 then print(\"a\") elseif 186 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 187 % 3 == 0 then print(\"a\") elseif 187 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 188 % 3 == 0 then print(\"a\") elseif 188 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 189 % 3 == 0 then print(\"a\") elseif 189 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 190 % 3 == 0 then print(\"a\") elseif 190 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 191 % 3 == 0 then print(\"a\") elseif 191 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 192 % 3 == 0 then print(\"a\") elseif 192 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 193 % 3 == 0 then print(\"a\") elseif 193 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 194 % 3 == 0 then print(\"a\") elseif 194 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 195 % 3 == 0 then print(\"a\") elseif 195 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 196 % 3 == 0 then print(\"a\") elseif 196 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 197 % 3 == 0 then print(\"a\") elseif 197 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 198 % 3 == 0 then print(\"a\") elseif 198 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 199 % 3 == 0 then print(\"a\") elseif 199 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 200 % 3 == 0 then print(\"a\") elseif 200 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 201 % 3 == 0 then print(\"a\") elseif 201 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 202 % 3 == 0 then print(\"a\") elseif 202 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 203 % 3 == 0 then print(\"a\") elseif 203 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 204 % 3 == 0 then print(\"a\") elseif 204 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 205 % 3 == 0 then print(\"a\") elseif 205 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 206 % 3 == 0 then print(\"a\") elseif 206 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 207 % 3 == 0 then print(\"a\") elseif 207 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 208 % 3 == 0 then print(\"a\") elseif 208 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 209 % 3 == 0 then print(\"a\") elseif 209 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 210 % 3 == 0 then print(\"a\") elseif 210 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 211 % 3 == 0 then print(\"a\") elseif 211 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 212 % 3 == 0 then print(\"a\") elseif 212 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 213 % 3 == 0 then print(\"a\") elseif 213 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 214 % 3 == 0 then print(\"a\") elseif 214 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 215 % 3 == 0 then print(\"a\") elseif 215 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 216 % 3 == 0 then print(\"a\") elseif 216 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 217 % 3 == 0 then print(\"a\") elseif 217 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 218 % 3 == 0 then print(\"a\") elseif 218 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 219 % 3 == 0 then print(\"a\") elseif 219 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 220 % 3 == 0 then print(\"a\") elseif 220 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 221 % 3 == 0 then print(\"a\") elseif 221 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 222 % 3 == 0 then print(\"a\") elseif 222 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 223 % 3 == 0 then print(\"a\") elseif 223 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 224 % 3 == 0 then print(\"a\") elseif 224 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 225 % 3 == 0 then print(\"a\") elseif 225 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 226 % 3 == 0 then print(\"a\") elseif 226 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 227 % 3 == 0 then print(\"a\") elseif 227 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 228 % 3 == 0 then print(\"a\") elseif 228 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 229 % 3 == 0 then print(\"a\") elseif 229 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 230 % 3 == 0 then print(\"a\") elseif 230 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 231 % 3 == 0 then print(\"a\") elseif 231 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 232 % 3 == 0 then print(\"a\") elseif 232 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 233 % 3 == 0 then print(\"a\") elseif 233 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 234 % 3 == 0 then print(\"a\") elseif 234 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 235 % 3 == 0 then print(\"a\") elseif 235 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 236 % 3 == 0 then print(\"a\") elseif 236 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 237 % 3 == 0 then print(\"a\") elseif 237 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 238 % 3 == 0 then print(\"a\") elseif 238 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 239 % 3 == 0 then print(\"a\") elseif 239 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 240 % 3 == 0 then print(\"a\") elseif 240 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 241 % 3 == 0 then print(\"a\") elseif 241 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 242 % 3 == 0 then print(\"a\") elseif 242 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 243 % 3 == 0 then print(\"a\") elseif 243 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 244 % 3 == 0 then print(\"a\") elseif 244 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 245 % 3 == 0 then print(\"a\") elseif 245 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 246 % 3 == 0 then print(\"a\") elseif 246 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 247 % 3 == 0 then print(\"a\") elseif 247 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 248 % 3 == 0 then print(\"a\") elseif 248 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 249 % 3 == 0 then print(\"a\") elseif 249 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 250 % 3 == 0 then print(\"a\") elseif 250 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 251 % 3 == 0 then print(\"a\") elseif 251 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 252 % 3 == 0 then print(\"a\") elseif 252 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 253 % 3 == 0 then print(\"a\") elseif 253 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 254 % 3 == 0 then print(\"a\") elseif 254 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 255 % 3 == 0 then print(\"a\") elseif 255 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 256 % 3 == 0 then print(\"a\") elseif 256 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 257 % 3 == 0 then print(\"a\") elseif 257 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 258 % 3 == 0 then print(\"a\") elseif 258 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 259 % 3 == 0 then print(\"a\") elseif 259 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 260 % 3 == 0 then print(\"a\") elseif 260 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 261 % 3 == 0 then print(\"a\") elseif 261 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 262 % 3 == 0 then print(\"a\") elseif 262 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 263 % 3 == 0 then print(\"a\") elseif 263 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 264 % 3 == 0 then print(\"a\") elseif 264 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 265 % 3 == 0 then print(\"a\") elseif 265 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 266 % 3 == 0 then print(\"a\") elseif 266 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 267 % 3 == 0 then print(\"a\") elseif 267 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 268 % 3 == 0 then print(\"a\") elseif 268 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 269 % 3 == 0 then print(\"a\") elseif 269 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 270 % 3 == 0 then print(\"a\") elseif 270 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 271 % 3 == 0 then print(\"a\") elseif 271 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 272 % 3 == 0 then print(\"a\") elseif 272 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 273 % 3 == 0 then print(\"a\") elseif 273 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 274 % 3 == 0 then print(\"a\") elseif 274 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 275 % 3 == 0 then print(\"a\") elseif 275 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 276 % 3 == 0 then print(\"a\") elseif 276 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 277 % 3 == 0 then print(\"a\") elseif 277 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 278 % 3 == 0 then print(\"a\") elseif 278 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 279 % 3 == 0 then print(\"a\") elseif 279 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 280 % 3 == 0 then print(\"a\") elseif 280 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 281 % 3 == 0 then print(\"a\") elseif 281 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 282 % 3 == 0 then print(\"a\") elseif 282 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 283 % 3 == 0 then print(\"a\") elseif 283 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 284 % 3 == 0 then print(\"a\") elseif 284 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 285 % 3 == 0 then print(\"a\") elseif 285 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 286 % 3 == 0 then print(\"a\") elseif 286 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 287 % 3 == 0 then print(\"a\") elseif 287 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 288 % 3 == 0 then print(\"a\") elseif 288 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 289 % 3 == 0 then print(\"a\") elseif 289 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 290 % 3 == 0 then print(\"a\") elseif 290 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 291 % 3 == 0 then print(\"a\") elseif 291 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 292 % 3 == 0 then print(\"a\") elseif 292 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 293 % 3 == 0 then print(\"a\") elseif 293 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 294 % 3 == 0 then print(\"a\") elseif 294 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 295 % 3 == 0 then print(\"a\") elseif 295 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 296 % 3 == 0 then print(\"a\") elseif 296 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 297 % 3 == 0 then print(\"a\") elseif 297 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 298 % 3 == 0 then print(\"a\") elseif 298 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 299 % 3 == 0 then print(\"a\") elseif 299 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 300 % 3 == 0 then print(\"a\") elseif 300 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 301 % 3 == 0 then print(\"a\") elseif 301 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 302 % 3 == 0 then print(\"a\") elseif 302 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 303 % 3 == 0 then print(\"a\") elseif 303 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 304 % 3 == 0 then print(\"a\") elseif 304 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 305 % 3 == 0 then print(\"a\") elseif 305 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 306 % 3 == 0 then print(\"a\") elseif 306 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 307 % 3 == 0 then print(\"a\") elseif 307 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 308 % 3 == 0 then print(\"a\") elseif 308 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 309 % 3 == 0 then print(\"a\") elseif 309 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 310 % 3 == 0 then print(\"a\") elseif 310 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 311 % 3 == 0 then print(\"a\") elseif 311 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 312 % 3 == 0 then print(\"a\") elseif 312 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 313 % 3 == 0 then print(\"a\") elseif 313 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 314 % 3 == 0 then print(\"a\") elseif 314 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 315 % 3 == 0 then print(\"a\") elseif 315 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 316 % 3 == 0 then print(\"a\") elseif 316 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 317 % 3 == 0 then print(\"a\") elseif 317 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 318 % 3 == 0 then print(\"a\") elseif 318 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 319 % 3 == 0 then print(\"a\") elseif 319 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 320 % 3 == 0 then print(\"a\") elseif 320 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 321 % 3 == 0 then print(\"a\") elseif 321 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 322 % 3 == 0 then print(\"a\") elseif 322 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 323 % 3 == 0 then print(\"a\") elseif 323 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 324 % 3 == 0 then print(\"a\") elseif 324 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 325 % 3 == 0 then print(\"a\") elseif 325 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 326 % 3 == 0 then print(\"a\") elseif 326 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 327 % 3 == 0 then print(\"a\") elseif 327 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 328 % 3 == 0 then print(\"a\") elseif 328 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 329 % 3 == 0 then print(\"a\") elseif 329 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 330 % 3 == 0 then print(\"a\") elseif 330 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 331 % 3 == 0 then print(\"a\") elseif 331 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 332 % 3 == 0 then print(\"a\") elseif 332 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 333 % 3 == 0 then print(\"a\") elseif 333 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 334 % 3 == 0 then print(\"a\") elseif 334 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 335 % 3 == 0 then print(\"a\") elseif 335 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 336 % 3 == 0 then print(\"a\") elseif 336 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 337 % 3 == 0 then print(\"a\") elseif 337 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 338 % 3 == 0 then print(\"a\") elseif 338 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 339 % 3 == 0 then print(\"a\") elseif 339 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 340 % 3 == 0 then print(\"a\") elseif 340 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 341 % 3 == 0 then print(\"a\") elseif 341 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 342 % 3 == 0 then print(\"a\") elseif 342 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 343 % 3 == 0 then print(\"a\") elseif 343 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 344 % 3 == 0 then print(\"a\") elseif 344 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 345 % 3 == 0 then print(\"a\") elseif 345 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 346 % 3 == 0 then print(\"a\") elseif 346 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 347 % 3 == 0 then print(\"a\") elseif 347 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 348 % 3 == 0 then print(\"a\") elseif 348 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 349 % 3 == 0 then print(\"a\") elseif 349 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 350 % 3 == 0 then print(\"a\") elseif 350 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 351 % 3 == 0 then print(\"a\") elseif 351 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 352 % 3 == 0 then print(\"a\") elseif 352 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 353 % 3 == 0 then print(\"a\") elseif 353 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 354 % 3 == 0 then print(\"a\") elseif 354 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 355 % 3 == 0 then print(\"a\") elseif 355 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 356 % 3 == 0 then print(\"a\") elseif 356 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 357 % 3 == 0 then print(\"a\") elseif 357 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 358 % 3 == 0 then print(\"a\") elseif 358 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 359 % 3 == 0 then print(\"a\") elseif 359 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 360 % 3 == 0 then print(\"a\") elseif 360 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 361 % 3 == 0 then print(\"a\") elseif 361 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 362 % 3 == 0 then print(\"a\") elseif 362 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 363 % 3 == 0 then print(\"a\") elseif 363 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 364 % 3 == 0 then print(\"a\") elseif 364 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 365 % 3 == 0 then print(\"a\") elseif 365 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 366 % 3 == 0 then print(\"a\") elseif 366 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 367 % 3 == 0 then print(\"a\") elseif 367 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 368 % 3 == 0 then print(\"a\") elseif 368 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 369 % 3 == 0 then print(\"a\") elseif 369 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 370 % 3 == 0 then print(\"a\") elseif 370 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 371 % 3 == 0 then print(\"a\") elseif 371 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 372 % 3 == 0 then print(\"a\") elseif 372 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 373 % 3 == 0 then print(\"a\") elseif 373 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 374 % 3 == 0 then print(\"a\") elseif 374 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 375 % 3 == 0 then print(\"a\") elseif 375 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 376 % 3 == 0 then print(\"a\") elseif 376 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 377 % 3 == 0 then print(\"a\") elseif 377 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 378 % 3 == 0 then print(\"a\") elseif 378 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 379 % 3 == 0 then print(\"a\") elseif 379 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 380 % 3 == 0 then print(\"a\") elseif 380 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 381 % 3 == 0 then print(\"a\") elseif 381 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 382 % 3 == 0 then print(\"a\") elseif 382 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 383 % 3 == 0 then print(\"a\") elseif 383 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 384 % 3 == 0 then print(\"a\") elseif 384 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 385 % 3 == 0 then print(\"a\") elseif 385 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 386 % 3 == 0 then print(\"a\") elseif 386 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 387 % 3 == 0 then print(\"a\") elseif 387 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 388 % 3 == 0 then print(\"a\") elseif 388 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 389 % 3 == 0 then print(\"a\") elseif 389 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 390 % 3 == 0 then print(\"a\") elseif 390 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 391 % 3 == 0 then print(\"a\") elseif 391 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 392 % 3 == 0 then print(\"a\") elseif 392 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 393 % 3 == 0 then print(\"a\") elseif 393 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 394 % 3 == 0 then print(\"a\") elseif 394 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 395 % 3 == 0 then print(\"a\") elseif 395 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 396 % 3 == 0 then print(\"a\") elseif 396 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 397 % 3 == 0 then print(\"a\") elseif 397 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 398 % 3 == 0 then print(\"a\") elseif 398 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 399 % 3 == 0 then print(\"a\") elseif 399 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 400 % 3 == 0 then print(\"a\") elseif 400 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 401 % 3 == 0 then print(\"a\") elseif 401 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 402 % 3 == 0 then print(\"a\") elseif 402 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 403 % 3 == 0 then print(\"a\") elseif 403 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 404 % 3 == 0 then print(\"a\") elseif 404 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 405 % 3 == 0 then print(\"a\") elseif 405 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 406 % 3 == 0 then print(\"a\") elseif 406 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 407 % 3 == 0 then print(\"a\") elseif 407 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 408 % 3 == 0 then print(\"a\") elseif 408 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 409 % 3 == 0 then print(\"a\") elseif 409 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 410 % 3 == 0 then print(\"a\") elseif 410 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 411 % 3 == 0 then print(\"a\") elseif 411 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 412 % 3 == 0 then print(\"a\") elseif 412 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 413 % 3 == 0 then print(\"a\") elseif 413 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 414 % 3 == 0 then print(\"a\") elseif 414 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 415 % 3 == 0 then print(\"a\") elseif 415 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 416 % 3 == 0 then print(\"a\") elseif 416 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 417 % 3 == 0 then print(\"a\") elseif 417 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 418 % 3 == 0 then print(\"a\") elseif 418 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 419 % 3 == 0 then print(\"a\") elseif 419 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 420 % 3 == 0 then print(\"a\") elseif 420 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 421 % 3 == 0 then print(\"a\") elseif 421 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 422 % 3 == 0 then print(\"a\") elseif 422 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 423 % 3 == 0 then print(\"a\") elseif 423 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 424 % 3 == 0 then print(\"a\") elseif 424 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 425 % 3 == 0 then print(\"a\") elseif 425 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 426 % 3 == 0 then print(\"a\") elseif 426 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 427 % 3 == 0 then print(\"a\") elseif 427 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 428 % 3 == 0 then print(\"a\") elseif 428 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 429 % 3 == 0 then print(\"a\") elseif 429 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 430 % 3 == 0 then print(\"a\") elseif 430 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 431 % 3 == 0 then print(\"a\") elseif 431 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 432 % 3 == 0 then print(\"a\") elseif 432 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 433 % 3 == 0 then print(\"a\") elseif 433 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 434 % 3 == 0 then print(\"a\") elseif 434 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 435 % 3 == 0 then print(\"a\") elseif 435 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 436 % 3 == 0 then print(\"a\") elseif 436 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 437 % 3 == 0 then print(\"a\") elseif 437 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 438 % 3 == 0 then print(\"a\") elseif 438 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 439 % 3 == 0 then print(\"a\") elseif 439 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 440 % 3 == 0 then print(\"a\") elseif 440 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 441 % 3 == 0 then print(\"a\") elseif 441 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 442 % 3 == 0 then print(\"a\") elseif 442 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 443 % 3 == 0 then print(\"a\") elseif 443 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 444 % 3 == 0 then print(\"a\") elseif 444 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 445 % 3 == 0 then print(\"a\") elseif 445 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 446 % 3 == 0 then print(\"a\") elseif 446 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 447 % 3 == 0 then print(\"a\") elseif 447 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 448 % 3 == 0 then print(\"a\") elseif 448 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 449 % 3 == 0 then print(\"a\") elseif 449 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 450 % 3 == 0 then print(\"a\") elseif 450 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 451 % 3 == 0 then print(\"a\") elseif 451 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 452 % 3 == 0 then print(\"a\") elseif 452 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 453 % 3 == 0 then print(\"a\") elseif 453 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 454 % 3 == 0 then print(\"a\") elseif 454 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 455 % 3 == 0 then print(\"a\") elseif 455 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 456 % 3 == 0 then print(\"a\") elseif 456 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 457 % 3 == 0 then print(\"a\") elseif 457 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 458 % 3 == 0 then print(\"a\") elseif 458 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 459 % 3 == 0 then print(\"a\") elseif 459 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 460 % 3 == 0 then print(\"a\") elseif 460 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 461 % 3 == 0 then print(\"a\") elseif 461 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 462 % 3 == 0 then print(\"a\") elseif 462 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 463 % 3 == 0 then print(\"a\") elseif 463 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 464 % 3 == 0 then print(\"a\") elseif 464 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 465 % 3 == 0 then print(\"a\") elseif 465 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 466 % 3 == 0 then print(\"a\") elseif 466 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 467 % 3 == 0 then print(\"a\") elseif 467 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 468 % 3 == 0 then print(\"a\") elseif 468 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 469 % 3 == 0 then print(\"a\") elseif 469 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 470 % 3 == 0 then print(\"a\") elseif 470 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 471 % 3 == 0 then print(\"a\") elseif 471 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 472 % 3 == 0 then print(\"a\") elseif 472 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 473 % 3 == 0 then print(\"a\") elseif 473 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 474 % 3 == 0 then print(\"a\") elseif 474 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 475 % 3 == 0 then print(\"a\") elseif 475 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 476 % 3 == 0 then print(\"a\") elseif 476 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 477 % 3 == 0 then print(\"a\") elseif 477 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 478 % 3 == 0 then print(\"a\") elseif 478 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 479 % 3 == 0 then print(\"a\") elseif 479 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 480 % 3 == 0 then print(\"a\") elseif 480 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 481 % 3 == 0 then print(\"a\") elseif 481 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 482 % 3 == 0 then print(\"a\") elseif 482 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 483 % 3 == 0 then print(\"a\") elseif 483 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 484 % 3 == 0 then print(\"a\") elseif 484 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 485 % 3 == 0 then print(\"a\") elseif 485 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 486 % 3 == 0 then print(\"a\") elseif 486 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 487 % 3 == 0 then print(\"a\") elseif 487 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 488 % 3 == 0 then print(\"a\") elseif 488 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 489 % 3 == 0 then print(\"a\") elseif 489 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 490 % 3 == 0 then print(\"a\") elseif 490 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 491 % 3 == 0 then print(\"a\") elseif 491 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 492 % 3 == 0 then print(\"a\") elseif 492 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 493 % 3 == 0 then print(\"a\") elseif 493 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 494 % 3 == 0 then print(\"a\") elseif 494 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 495 % 3 == 0 then print(\"a\") elseif 495 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 496 % 3 == 0 then print(\"a\") elseif 496 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_if_elseif_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 497 % 3 == 0 then print(\"a\") elseif 497 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "c\n");
}
#[test]
fn interp_if_elseif_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 498 % 3 == 0 then print(\"a\") elseif 498 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "a\n");
}
#[test]
fn interp_if_elseif_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() if 499 % 3 == 0 then print(\"a\") elseif 499 % 3 == 1 then print(\"b\") else print(\"c\") end end", 0);
    assert_eq!(out, "b\n");
}
#[test]
fn interp_var_update_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 0 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "200\n");
}
#[test]
fn interp_var_update_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 1 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "202\n");
}
#[test]
fn interp_var_update_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 2 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "204\n");
}
#[test]
fn interp_var_update_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 3 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "206\n");
}
#[test]
fn interp_var_update_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 4 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "208\n");
}
#[test]
fn interp_var_update_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 5 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "210\n");
}
#[test]
fn interp_var_update_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 6 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "212\n");
}
#[test]
fn interp_var_update_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 7 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "214\n");
}
#[test]
fn interp_var_update_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 8 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "216\n");
}
#[test]
fn interp_var_update_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 9 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "218\n");
}
#[test]
fn interp_var_update_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 10 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "220\n");
}
#[test]
fn interp_var_update_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 11 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "222\n");
}
#[test]
fn interp_var_update_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 12 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "224\n");
}
#[test]
fn interp_var_update_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 13 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "226\n");
}
#[test]
fn interp_var_update_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 14 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "228\n");
}
#[test]
fn interp_var_update_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 15 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "230\n");
}
#[test]
fn interp_var_update_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 16 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "232\n");
}
#[test]
fn interp_var_update_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 17 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "234\n");
}
#[test]
fn interp_var_update_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 18 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "236\n");
}
#[test]
fn interp_var_update_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 19 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "238\n");
}
#[test]
fn interp_var_update_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 20 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "240\n");
}
#[test]
fn interp_var_update_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 21 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "242\n");
}
#[test]
fn interp_var_update_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 22 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "244\n");
}
#[test]
fn interp_var_update_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 23 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "246\n");
}
#[test]
fn interp_var_update_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 24 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "248\n");
}
#[test]
fn interp_var_update_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 25 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "250\n");
}
#[test]
fn interp_var_update_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 26 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "252\n");
}
#[test]
fn interp_var_update_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 27 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "254\n");
}
#[test]
fn interp_var_update_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 28 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "256\n");
}
#[test]
fn interp_var_update_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 29 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "258\n");
}
#[test]
fn interp_var_update_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 30 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "260\n");
}
#[test]
fn interp_var_update_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 31 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "262\n");
}
#[test]
fn interp_var_update_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 32 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "264\n");
}
#[test]
fn interp_var_update_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 33 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "266\n");
}
#[test]
fn interp_var_update_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 34 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "268\n");
}
#[test]
fn interp_var_update_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 35 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "270\n");
}
#[test]
fn interp_var_update_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 36 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "272\n");
}
#[test]
fn interp_var_update_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 37 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "274\n");
}
#[test]
fn interp_var_update_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 38 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "276\n");
}
#[test]
fn interp_var_update_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 39 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "278\n");
}
#[test]
fn interp_var_update_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 40 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "280\n");
}
#[test]
fn interp_var_update_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 41 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "282\n");
}
#[test]
fn interp_var_update_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 42 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "284\n");
}
#[test]
fn interp_var_update_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 43 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "286\n");
}
#[test]
fn interp_var_update_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 44 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "288\n");
}
#[test]
fn interp_var_update_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 45 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "290\n");
}
#[test]
fn interp_var_update_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 46 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "292\n");
}
#[test]
fn interp_var_update_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 47 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "294\n");
}
#[test]
fn interp_var_update_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 48 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "296\n");
}
#[test]
fn interp_var_update_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 49 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "298\n");
}
#[test]
fn interp_var_update_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 50 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "300\n");
}
#[test]
fn interp_var_update_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 51 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "302\n");
}
#[test]
fn interp_var_update_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 52 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "304\n");
}
#[test]
fn interp_var_update_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 53 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "306\n");
}
#[test]
fn interp_var_update_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 54 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "308\n");
}
#[test]
fn interp_var_update_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 55 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "310\n");
}
#[test]
fn interp_var_update_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 56 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "312\n");
}
#[test]
fn interp_var_update_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 57 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "314\n");
}
#[test]
fn interp_var_update_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 58 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "316\n");
}
#[test]
fn interp_var_update_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 59 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "318\n");
}
#[test]
fn interp_var_update_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 60 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "320\n");
}
#[test]
fn interp_var_update_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 61 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "322\n");
}
#[test]
fn interp_var_update_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 62 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "324\n");
}
#[test]
fn interp_var_update_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 63 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "326\n");
}
#[test]
fn interp_var_update_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 64 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "328\n");
}
#[test]
fn interp_var_update_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 65 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "330\n");
}
#[test]
fn interp_var_update_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 66 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "332\n");
}
#[test]
fn interp_var_update_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 67 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "334\n");
}
#[test]
fn interp_var_update_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 68 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "336\n");
}
#[test]
fn interp_var_update_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 69 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "338\n");
}
#[test]
fn interp_var_update_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 70 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "340\n");
}
#[test]
fn interp_var_update_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 71 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "342\n");
}
#[test]
fn interp_var_update_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 72 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "344\n");
}
#[test]
fn interp_var_update_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 73 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "346\n");
}
#[test]
fn interp_var_update_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 74 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "348\n");
}
#[test]
fn interp_var_update_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 75 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "350\n");
}
#[test]
fn interp_var_update_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 76 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "352\n");
}
#[test]
fn interp_var_update_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 77 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "354\n");
}
#[test]
fn interp_var_update_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 78 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "356\n");
}
#[test]
fn interp_var_update_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 79 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "358\n");
}
#[test]
fn interp_var_update_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 80 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "360\n");
}
#[test]
fn interp_var_update_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 81 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "362\n");
}
#[test]
fn interp_var_update_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 82 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "364\n");
}
#[test]
fn interp_var_update_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 83 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "366\n");
}
#[test]
fn interp_var_update_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 84 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "368\n");
}
#[test]
fn interp_var_update_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 85 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "370\n");
}
#[test]
fn interp_var_update_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 86 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "372\n");
}
#[test]
fn interp_var_update_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 87 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "374\n");
}
#[test]
fn interp_var_update_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 88 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "376\n");
}
#[test]
fn interp_var_update_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 89 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "378\n");
}
#[test]
fn interp_var_update_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 90 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "380\n");
}
#[test]
fn interp_var_update_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 91 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "382\n");
}
#[test]
fn interp_var_update_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 92 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "384\n");
}
#[test]
fn interp_var_update_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 93 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "386\n");
}
#[test]
fn interp_var_update_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 94 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "388\n");
}
#[test]
fn interp_var_update_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 95 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "390\n");
}
#[test]
fn interp_var_update_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 96 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "392\n");
}
#[test]
fn interp_var_update_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 97 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "394\n");
}
#[test]
fn interp_var_update_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 98 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "396\n");
}
#[test]
fn interp_var_update_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 99 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "398\n");
}
#[test]
fn interp_var_update_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 100 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "400\n");
}
#[test]
fn interp_var_update_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 101 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "402\n");
}
#[test]
fn interp_var_update_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 102 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "404\n");
}
#[test]
fn interp_var_update_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 103 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "406\n");
}
#[test]
fn interp_var_update_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 104 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "408\n");
}
#[test]
fn interp_var_update_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 105 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "410\n");
}
#[test]
fn interp_var_update_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 106 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "412\n");
}
#[test]
fn interp_var_update_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 107 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "414\n");
}
#[test]
fn interp_var_update_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 108 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "416\n");
}
#[test]
fn interp_var_update_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 109 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "418\n");
}
#[test]
fn interp_var_update_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 110 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "420\n");
}
#[test]
fn interp_var_update_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 111 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "422\n");
}
#[test]
fn interp_var_update_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 112 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "424\n");
}
#[test]
fn interp_var_update_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 113 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "426\n");
}
#[test]
fn interp_var_update_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 114 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "428\n");
}
#[test]
fn interp_var_update_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 115 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "430\n");
}
#[test]
fn interp_var_update_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 116 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "432\n");
}
#[test]
fn interp_var_update_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 117 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "434\n");
}
#[test]
fn interp_var_update_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 118 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "436\n");
}
#[test]
fn interp_var_update_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 119 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "438\n");
}
#[test]
fn interp_var_update_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 120 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "440\n");
}
#[test]
fn interp_var_update_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 121 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "442\n");
}
#[test]
fn interp_var_update_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 122 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "444\n");
}
#[test]
fn interp_var_update_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 123 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "446\n");
}
#[test]
fn interp_var_update_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 124 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "448\n");
}
#[test]
fn interp_var_update_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 125 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "450\n");
}
#[test]
fn interp_var_update_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 126 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "452\n");
}
#[test]
fn interp_var_update_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 127 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "454\n");
}
#[test]
fn interp_var_update_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 128 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "456\n");
}
#[test]
fn interp_var_update_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 129 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "458\n");
}
#[test]
fn interp_var_update_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 130 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "460\n");
}
#[test]
fn interp_var_update_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 131 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "462\n");
}
#[test]
fn interp_var_update_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 132 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "464\n");
}
#[test]
fn interp_var_update_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 133 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "466\n");
}
#[test]
fn interp_var_update_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 134 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "468\n");
}
#[test]
fn interp_var_update_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 135 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "470\n");
}
#[test]
fn interp_var_update_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 136 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "472\n");
}
#[test]
fn interp_var_update_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 137 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "474\n");
}
#[test]
fn interp_var_update_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 138 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "476\n");
}
#[test]
fn interp_var_update_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 139 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "478\n");
}
#[test]
fn interp_var_update_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 140 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "480\n");
}
#[test]
fn interp_var_update_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 141 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "482\n");
}
#[test]
fn interp_var_update_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 142 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "484\n");
}
#[test]
fn interp_var_update_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 143 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "486\n");
}
#[test]
fn interp_var_update_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 144 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "488\n");
}
#[test]
fn interp_var_update_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 145 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "490\n");
}
#[test]
fn interp_var_update_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 146 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "492\n");
}
#[test]
fn interp_var_update_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 147 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "494\n");
}
#[test]
fn interp_var_update_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 148 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "496\n");
}
#[test]
fn interp_var_update_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 149 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "498\n");
}
#[test]
fn interp_var_update_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 150 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "500\n");
}
#[test]
fn interp_var_update_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 151 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "502\n");
}
#[test]
fn interp_var_update_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 152 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "504\n");
}
#[test]
fn interp_var_update_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 153 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "506\n");
}
#[test]
fn interp_var_update_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 154 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "508\n");
}
#[test]
fn interp_var_update_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 155 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "510\n");
}
#[test]
fn interp_var_update_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 156 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "512\n");
}
#[test]
fn interp_var_update_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 157 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "514\n");
}
#[test]
fn interp_var_update_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 158 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "516\n");
}
#[test]
fn interp_var_update_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 159 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "518\n");
}
#[test]
fn interp_var_update_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 160 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "520\n");
}
#[test]
fn interp_var_update_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 161 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "522\n");
}
#[test]
fn interp_var_update_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 162 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "524\n");
}
#[test]
fn interp_var_update_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 163 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "526\n");
}
#[test]
fn interp_var_update_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 164 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "528\n");
}
#[test]
fn interp_var_update_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 165 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "530\n");
}
#[test]
fn interp_var_update_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 166 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "532\n");
}
#[test]
fn interp_var_update_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 167 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "534\n");
}
#[test]
fn interp_var_update_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 168 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "536\n");
}
#[test]
fn interp_var_update_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 169 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "538\n");
}
#[test]
fn interp_var_update_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 170 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "540\n");
}
#[test]
fn interp_var_update_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 171 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "542\n");
}
#[test]
fn interp_var_update_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 172 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "544\n");
}
#[test]
fn interp_var_update_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 173 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "546\n");
}
#[test]
fn interp_var_update_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 174 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "548\n");
}
#[test]
fn interp_var_update_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 175 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "550\n");
}
#[test]
fn interp_var_update_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 176 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "552\n");
}
#[test]
fn interp_var_update_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 177 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "554\n");
}
#[test]
fn interp_var_update_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 178 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "556\n");
}
#[test]
fn interp_var_update_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 179 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "558\n");
}
#[test]
fn interp_var_update_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 180 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "560\n");
}
#[test]
fn interp_var_update_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 181 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "562\n");
}
#[test]
fn interp_var_update_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 182 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "564\n");
}
#[test]
fn interp_var_update_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 183 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "566\n");
}
#[test]
fn interp_var_update_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 184 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "568\n");
}
#[test]
fn interp_var_update_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 185 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "570\n");
}
#[test]
fn interp_var_update_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 186 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "572\n");
}
#[test]
fn interp_var_update_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 187 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "574\n");
}
#[test]
fn interp_var_update_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 188 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "576\n");
}
#[test]
fn interp_var_update_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 189 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "578\n");
}
#[test]
fn interp_var_update_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 190 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "580\n");
}
#[test]
fn interp_var_update_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 191 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "582\n");
}
#[test]
fn interp_var_update_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 192 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "584\n");
}
#[test]
fn interp_var_update_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 193 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "586\n");
}
#[test]
fn interp_var_update_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 194 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "588\n");
}
#[test]
fn interp_var_update_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 195 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "590\n");
}
#[test]
fn interp_var_update_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 196 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "592\n");
}
#[test]
fn interp_var_update_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 197 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "594\n");
}
#[test]
fn interp_var_update_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 198 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "596\n");
}
#[test]
fn interp_var_update_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 199 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "598\n");
}
#[test]
fn interp_var_update_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 200 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "600\n");
}
#[test]
fn interp_var_update_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 201 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "602\n");
}
#[test]
fn interp_var_update_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 202 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "604\n");
}
#[test]
fn interp_var_update_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 203 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "606\n");
}
#[test]
fn interp_var_update_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 204 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "608\n");
}
#[test]
fn interp_var_update_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 205 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "610\n");
}
#[test]
fn interp_var_update_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 206 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "612\n");
}
#[test]
fn interp_var_update_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 207 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "614\n");
}
#[test]
fn interp_var_update_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 208 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "616\n");
}
#[test]
fn interp_var_update_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 209 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "618\n");
}
#[test]
fn interp_var_update_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 210 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "620\n");
}
#[test]
fn interp_var_update_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 211 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "622\n");
}
#[test]
fn interp_var_update_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 212 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "624\n");
}
#[test]
fn interp_var_update_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 213 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "626\n");
}
#[test]
fn interp_var_update_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 214 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "628\n");
}
#[test]
fn interp_var_update_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 215 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "630\n");
}
#[test]
fn interp_var_update_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 216 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "632\n");
}
#[test]
fn interp_var_update_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 217 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "634\n");
}
#[test]
fn interp_var_update_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 218 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "636\n");
}
#[test]
fn interp_var_update_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 219 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "638\n");
}
#[test]
fn interp_var_update_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 220 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "640\n");
}
#[test]
fn interp_var_update_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 221 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "642\n");
}
#[test]
fn interp_var_update_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 222 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "644\n");
}
#[test]
fn interp_var_update_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 223 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "646\n");
}
#[test]
fn interp_var_update_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 224 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "648\n");
}
#[test]
fn interp_var_update_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 225 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "650\n");
}
#[test]
fn interp_var_update_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 226 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "652\n");
}
#[test]
fn interp_var_update_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 227 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "654\n");
}
#[test]
fn interp_var_update_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 228 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "656\n");
}
#[test]
fn interp_var_update_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 229 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "658\n");
}
#[test]
fn interp_var_update_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 230 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "660\n");
}
#[test]
fn interp_var_update_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 231 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "662\n");
}
#[test]
fn interp_var_update_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 232 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "664\n");
}
#[test]
fn interp_var_update_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 233 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "666\n");
}
#[test]
fn interp_var_update_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 234 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "668\n");
}
#[test]
fn interp_var_update_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 235 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "670\n");
}
#[test]
fn interp_var_update_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 236 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "672\n");
}
#[test]
fn interp_var_update_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 237 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "674\n");
}
#[test]
fn interp_var_update_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 238 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "676\n");
}
#[test]
fn interp_var_update_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 239 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "678\n");
}
#[test]
fn interp_var_update_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 240 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "680\n");
}
#[test]
fn interp_var_update_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 241 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "682\n");
}
#[test]
fn interp_var_update_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 242 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "684\n");
}
#[test]
fn interp_var_update_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 243 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "686\n");
}
#[test]
fn interp_var_update_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 244 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "688\n");
}
#[test]
fn interp_var_update_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 245 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "690\n");
}
#[test]
fn interp_var_update_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 246 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "692\n");
}
#[test]
fn interp_var_update_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 247 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "694\n");
}
#[test]
fn interp_var_update_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 248 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "696\n");
}
#[test]
fn interp_var_update_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 249 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "698\n");
}
#[test]
fn interp_var_update_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 250 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "700\n");
}
#[test]
fn interp_var_update_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 251 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "702\n");
}
#[test]
fn interp_var_update_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 252 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "704\n");
}
#[test]
fn interp_var_update_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 253 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "706\n");
}
#[test]
fn interp_var_update_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 254 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "708\n");
}
#[test]
fn interp_var_update_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 255 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "710\n");
}
#[test]
fn interp_var_update_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 256 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "712\n");
}
#[test]
fn interp_var_update_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 257 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "714\n");
}
#[test]
fn interp_var_update_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 258 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "716\n");
}
#[test]
fn interp_var_update_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 259 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "718\n");
}
#[test]
fn interp_var_update_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 260 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "720\n");
}
#[test]
fn interp_var_update_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 261 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "722\n");
}
#[test]
fn interp_var_update_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 262 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "724\n");
}
#[test]
fn interp_var_update_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 263 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "726\n");
}
#[test]
fn interp_var_update_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 264 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "728\n");
}
#[test]
fn interp_var_update_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 265 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "730\n");
}
#[test]
fn interp_var_update_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 266 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "732\n");
}
#[test]
fn interp_var_update_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 267 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "734\n");
}
#[test]
fn interp_var_update_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 268 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "736\n");
}
#[test]
fn interp_var_update_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 269 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "738\n");
}
#[test]
fn interp_var_update_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 270 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "740\n");
}
#[test]
fn interp_var_update_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 271 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "742\n");
}
#[test]
fn interp_var_update_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 272 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "744\n");
}
#[test]
fn interp_var_update_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 273 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "746\n");
}
#[test]
fn interp_var_update_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 274 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "748\n");
}
#[test]
fn interp_var_update_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 275 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "750\n");
}
#[test]
fn interp_var_update_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 276 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "752\n");
}
#[test]
fn interp_var_update_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 277 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "754\n");
}
#[test]
fn interp_var_update_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 278 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "756\n");
}
#[test]
fn interp_var_update_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 279 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "758\n");
}
#[test]
fn interp_var_update_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 280 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "760\n");
}
#[test]
fn interp_var_update_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 281 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "762\n");
}
#[test]
fn interp_var_update_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 282 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "764\n");
}
#[test]
fn interp_var_update_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 283 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "766\n");
}
#[test]
fn interp_var_update_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 284 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "768\n");
}
#[test]
fn interp_var_update_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 285 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "770\n");
}
#[test]
fn interp_var_update_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 286 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "772\n");
}
#[test]
fn interp_var_update_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 287 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "774\n");
}
#[test]
fn interp_var_update_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 288 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "776\n");
}
#[test]
fn interp_var_update_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 289 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "778\n");
}
#[test]
fn interp_var_update_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 290 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "780\n");
}
#[test]
fn interp_var_update_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 291 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "782\n");
}
#[test]
fn interp_var_update_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 292 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "784\n");
}
#[test]
fn interp_var_update_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 293 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "786\n");
}
#[test]
fn interp_var_update_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 294 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "788\n");
}
#[test]
fn interp_var_update_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 295 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "790\n");
}
#[test]
fn interp_var_update_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 296 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "792\n");
}
#[test]
fn interp_var_update_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 297 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "794\n");
}
#[test]
fn interp_var_update_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 298 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "796\n");
}
#[test]
fn interp_var_update_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 299 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "798\n");
}
#[test]
fn interp_var_update_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 300 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "800\n");
}
#[test]
fn interp_var_update_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 301 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "802\n");
}
#[test]
fn interp_var_update_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 302 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "804\n");
}
#[test]
fn interp_var_update_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 303 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "806\n");
}
#[test]
fn interp_var_update_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 304 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "808\n");
}
#[test]
fn interp_var_update_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 305 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "810\n");
}
#[test]
fn interp_var_update_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 306 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "812\n");
}
#[test]
fn interp_var_update_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 307 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "814\n");
}
#[test]
fn interp_var_update_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 308 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "816\n");
}
#[test]
fn interp_var_update_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 309 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "818\n");
}
#[test]
fn interp_var_update_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 310 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "820\n");
}
#[test]
fn interp_var_update_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 311 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "822\n");
}
#[test]
fn interp_var_update_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 312 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "824\n");
}
#[test]
fn interp_var_update_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 313 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "826\n");
}
#[test]
fn interp_var_update_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 314 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "828\n");
}
#[test]
fn interp_var_update_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 315 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "830\n");
}
#[test]
fn interp_var_update_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 316 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "832\n");
}
#[test]
fn interp_var_update_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 317 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "834\n");
}
#[test]
fn interp_var_update_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 318 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "836\n");
}
#[test]
fn interp_var_update_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 319 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "838\n");
}
#[test]
fn interp_var_update_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 320 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "840\n");
}
#[test]
fn interp_var_update_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 321 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "842\n");
}
#[test]
fn interp_var_update_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 322 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "844\n");
}
#[test]
fn interp_var_update_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 323 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "846\n");
}
#[test]
fn interp_var_update_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 324 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "848\n");
}
#[test]
fn interp_var_update_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 325 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "850\n");
}
#[test]
fn interp_var_update_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 326 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "852\n");
}
#[test]
fn interp_var_update_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 327 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "854\n");
}
#[test]
fn interp_var_update_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 328 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "856\n");
}
#[test]
fn interp_var_update_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 329 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "858\n");
}
#[test]
fn interp_var_update_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 330 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "860\n");
}
#[test]
fn interp_var_update_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 331 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "862\n");
}
#[test]
fn interp_var_update_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 332 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "864\n");
}
#[test]
fn interp_var_update_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 333 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "866\n");
}
#[test]
fn interp_var_update_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 334 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "868\n");
}
#[test]
fn interp_var_update_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 335 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "870\n");
}
#[test]
fn interp_var_update_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 336 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "872\n");
}
#[test]
fn interp_var_update_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 337 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "874\n");
}
#[test]
fn interp_var_update_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 338 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "876\n");
}
#[test]
fn interp_var_update_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 339 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "878\n");
}
#[test]
fn interp_var_update_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 340 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "880\n");
}
#[test]
fn interp_var_update_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 341 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "882\n");
}
#[test]
fn interp_var_update_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 342 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "884\n");
}
#[test]
fn interp_var_update_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 343 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "886\n");
}
#[test]
fn interp_var_update_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 344 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "888\n");
}
#[test]
fn interp_var_update_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 345 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "890\n");
}
#[test]
fn interp_var_update_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 346 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "892\n");
}
#[test]
fn interp_var_update_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 347 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "894\n");
}
#[test]
fn interp_var_update_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 348 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "896\n");
}
#[test]
fn interp_var_update_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 349 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "898\n");
}
#[test]
fn interp_var_update_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 350 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "900\n");
}
#[test]
fn interp_var_update_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 351 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "902\n");
}
#[test]
fn interp_var_update_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 352 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "904\n");
}
#[test]
fn interp_var_update_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 353 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "906\n");
}
#[test]
fn interp_var_update_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 354 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "908\n");
}
#[test]
fn interp_var_update_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 355 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "910\n");
}
#[test]
fn interp_var_update_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 356 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "912\n");
}
#[test]
fn interp_var_update_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 357 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "914\n");
}
#[test]
fn interp_var_update_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 358 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "916\n");
}
#[test]
fn interp_var_update_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 359 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "918\n");
}
#[test]
fn interp_var_update_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 360 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "920\n");
}
#[test]
fn interp_var_update_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 361 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "922\n");
}
#[test]
fn interp_var_update_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 362 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "924\n");
}
#[test]
fn interp_var_update_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 363 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "926\n");
}
#[test]
fn interp_var_update_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 364 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "928\n");
}
#[test]
fn interp_var_update_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 365 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "930\n");
}
#[test]
fn interp_var_update_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 366 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "932\n");
}
#[test]
fn interp_var_update_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 367 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "934\n");
}
#[test]
fn interp_var_update_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 368 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "936\n");
}
#[test]
fn interp_var_update_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 369 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "938\n");
}
#[test]
fn interp_var_update_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 370 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "940\n");
}
#[test]
fn interp_var_update_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 371 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "942\n");
}
#[test]
fn interp_var_update_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 372 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "944\n");
}
#[test]
fn interp_var_update_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 373 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "946\n");
}
#[test]
fn interp_var_update_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 374 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "948\n");
}
#[test]
fn interp_var_update_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 375 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "950\n");
}
#[test]
fn interp_var_update_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 376 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "952\n");
}
#[test]
fn interp_var_update_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 377 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "954\n");
}
#[test]
fn interp_var_update_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 378 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "956\n");
}
#[test]
fn interp_var_update_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 379 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "958\n");
}
#[test]
fn interp_var_update_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 380 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "960\n");
}
#[test]
fn interp_var_update_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 381 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "962\n");
}
#[test]
fn interp_var_update_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 382 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "964\n");
}
#[test]
fn interp_var_update_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 383 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "966\n");
}
#[test]
fn interp_var_update_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 384 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "968\n");
}
#[test]
fn interp_var_update_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 385 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "970\n");
}
#[test]
fn interp_var_update_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 386 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "972\n");
}
#[test]
fn interp_var_update_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 387 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "974\n");
}
#[test]
fn interp_var_update_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 388 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "976\n");
}
#[test]
fn interp_var_update_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 389 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "978\n");
}
#[test]
fn interp_var_update_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 390 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "980\n");
}
#[test]
fn interp_var_update_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 391 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "982\n");
}
#[test]
fn interp_var_update_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 392 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "984\n");
}
#[test]
fn interp_var_update_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 393 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "986\n");
}
#[test]
fn interp_var_update_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 394 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "988\n");
}
#[test]
fn interp_var_update_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 395 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "990\n");
}
#[test]
fn interp_var_update_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 396 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "992\n");
}
#[test]
fn interp_var_update_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 397 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "994\n");
}
#[test]
fn interp_var_update_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 398 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "996\n");
}
#[test]
fn interp_var_update_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 399 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "998\n");
}
#[test]
fn interp_var_update_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 400 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1000\n");
}
#[test]
fn interp_var_update_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 401 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1002\n");
}
#[test]
fn interp_var_update_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 402 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1004\n");
}
#[test]
fn interp_var_update_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 403 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1006\n");
}
#[test]
fn interp_var_update_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 404 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1008\n");
}
#[test]
fn interp_var_update_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 405 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1010\n");
}
#[test]
fn interp_var_update_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 406 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1012\n");
}
#[test]
fn interp_var_update_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 407 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1014\n");
}
#[test]
fn interp_var_update_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 408 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1016\n");
}
#[test]
fn interp_var_update_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 409 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1018\n");
}
#[test]
fn interp_var_update_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 410 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1020\n");
}
#[test]
fn interp_var_update_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 411 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1022\n");
}
#[test]
fn interp_var_update_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 412 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1024\n");
}
#[test]
fn interp_var_update_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 413 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1026\n");
}
#[test]
fn interp_var_update_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 414 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1028\n");
}
#[test]
fn interp_var_update_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 415 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1030\n");
}
#[test]
fn interp_var_update_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 416 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1032\n");
}
#[test]
fn interp_var_update_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 417 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1034\n");
}
#[test]
fn interp_var_update_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 418 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1036\n");
}
#[test]
fn interp_var_update_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 419 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1038\n");
}
#[test]
fn interp_var_update_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 420 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1040\n");
}
#[test]
fn interp_var_update_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 421 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1042\n");
}
#[test]
fn interp_var_update_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 422 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1044\n");
}
#[test]
fn interp_var_update_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 423 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1046\n");
}
#[test]
fn interp_var_update_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 424 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1048\n");
}
#[test]
fn interp_var_update_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 425 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1050\n");
}
#[test]
fn interp_var_update_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 426 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1052\n");
}
#[test]
fn interp_var_update_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 427 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1054\n");
}
#[test]
fn interp_var_update_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 428 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1056\n");
}
#[test]
fn interp_var_update_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 429 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1058\n");
}
#[test]
fn interp_var_update_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 430 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1060\n");
}
#[test]
fn interp_var_update_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 431 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1062\n");
}
#[test]
fn interp_var_update_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 432 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1064\n");
}
#[test]
fn interp_var_update_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 433 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1066\n");
}
#[test]
fn interp_var_update_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 434 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1068\n");
}
#[test]
fn interp_var_update_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 435 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1070\n");
}
#[test]
fn interp_var_update_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 436 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1072\n");
}
#[test]
fn interp_var_update_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 437 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1074\n");
}
#[test]
fn interp_var_update_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 438 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1076\n");
}
#[test]
fn interp_var_update_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 439 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1078\n");
}
#[test]
fn interp_var_update_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 440 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1080\n");
}
#[test]
fn interp_var_update_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 441 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1082\n");
}
#[test]
fn interp_var_update_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 442 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1084\n");
}
#[test]
fn interp_var_update_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 443 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1086\n");
}
#[test]
fn interp_var_update_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 444 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1088\n");
}
#[test]
fn interp_var_update_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 445 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1090\n");
}
#[test]
fn interp_var_update_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 446 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1092\n");
}
#[test]
fn interp_var_update_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 447 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1094\n");
}
#[test]
fn interp_var_update_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 448 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1096\n");
}
#[test]
fn interp_var_update_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 449 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1098\n");
}
#[test]
fn interp_var_update_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 450 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1100\n");
}
#[test]
fn interp_var_update_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 451 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1102\n");
}
#[test]
fn interp_var_update_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 452 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1104\n");
}
#[test]
fn interp_var_update_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 453 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1106\n");
}
#[test]
fn interp_var_update_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 454 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1108\n");
}
#[test]
fn interp_var_update_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 455 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1110\n");
}
#[test]
fn interp_var_update_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 456 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1112\n");
}
#[test]
fn interp_var_update_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 457 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1114\n");
}
#[test]
fn interp_var_update_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 458 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1116\n");
}
#[test]
fn interp_var_update_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 459 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1118\n");
}
#[test]
fn interp_var_update_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 460 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1120\n");
}
#[test]
fn interp_var_update_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 461 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1122\n");
}
#[test]
fn interp_var_update_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 462 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1124\n");
}
#[test]
fn interp_var_update_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 463 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1126\n");
}
#[test]
fn interp_var_update_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 464 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1128\n");
}
#[test]
fn interp_var_update_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 465 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1130\n");
}
#[test]
fn interp_var_update_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 466 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1132\n");
}
#[test]
fn interp_var_update_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 467 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1134\n");
}
#[test]
fn interp_var_update_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 468 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1136\n");
}
#[test]
fn interp_var_update_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 469 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1138\n");
}
#[test]
fn interp_var_update_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 470 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1140\n");
}
#[test]
fn interp_var_update_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 471 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1142\n");
}
#[test]
fn interp_var_update_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 472 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1144\n");
}
#[test]
fn interp_var_update_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 473 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1146\n");
}
#[test]
fn interp_var_update_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 474 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1148\n");
}
#[test]
fn interp_var_update_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 475 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1150\n");
}
#[test]
fn interp_var_update_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 476 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1152\n");
}
#[test]
fn interp_var_update_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 477 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1154\n");
}
#[test]
fn interp_var_update_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 478 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1156\n");
}
#[test]
fn interp_var_update_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 479 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1158\n");
}
#[test]
fn interp_var_update_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 480 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1160\n");
}
#[test]
fn interp_var_update_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 481 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1162\n");
}
#[test]
fn interp_var_update_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 482 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1164\n");
}
#[test]
fn interp_var_update_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 483 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1166\n");
}
#[test]
fn interp_var_update_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 484 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1168\n");
}
#[test]
fn interp_var_update_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 485 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1170\n");
}
#[test]
fn interp_var_update_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 486 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1172\n");
}
#[test]
fn interp_var_update_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 487 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1174\n");
}
#[test]
fn interp_var_update_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 488 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1176\n");
}
#[test]
fn interp_var_update_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 489 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1178\n");
}
#[test]
fn interp_var_update_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 490 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1180\n");
}
#[test]
fn interp_var_update_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 491 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1182\n");
}
#[test]
fn interp_var_update_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 492 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1184\n");
}
#[test]
fn interp_var_update_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 493 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1186\n");
}
#[test]
fn interp_var_update_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 494 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1188\n");
}
#[test]
fn interp_var_update_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 495 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1190\n");
}
#[test]
fn interp_var_update_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 496 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1192\n");
}
#[test]
fn interp_var_update_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 497 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1194\n");
}
#[test]
fn interp_var_update_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 498 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1196\n");
}
#[test]
fn interp_var_update_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() var x = 499 x = x + 100 x = x * 2 print(x) end", 0);
    assert_eq!(out, "1198\n");
}
#[test]
fn interp_recursive_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(1)) end", 0);
    assert_eq!(out, "1\n");
}
#[test]
fn interp_recursive_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(2)) end", 0);
    assert_eq!(out, "2\n");
}
#[test]
fn interp_recursive_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(3)) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn interp_recursive_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(4)) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn interp_recursive_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(5)) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn interp_recursive_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(6)) end", 0);
    assert_eq!(out, "720\n");
}
#[test]
fn interp_recursive_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(7)) end", 0);
    assert_eq!(out, "5040\n");
}
#[test]
fn interp_recursive_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(8)) end", 0);
    assert_eq!(out, "40320\n");
}
#[test]
fn interp_recursive_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(9)) end", 0);
    assert_eq!(out, "362880\n");
}
#[test]
fn interp_recursive_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(10)) end", 0);
    assert_eq!(out, "3628800\n");
}
#[test]
fn interp_recursive_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(1)) end", 0);
    assert_eq!(out, "1\n");
}
#[test]
fn interp_recursive_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(2)) end", 0);
    assert_eq!(out, "2\n");
}
#[test]
fn interp_recursive_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(3)) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn interp_recursive_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(4)) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn interp_recursive_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(5)) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn interp_recursive_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(6)) end", 0);
    assert_eq!(out, "720\n");
}
#[test]
fn interp_recursive_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(7)) end", 0);
    assert_eq!(out, "5040\n");
}
#[test]
fn interp_recursive_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(8)) end", 0);
    assert_eq!(out, "40320\n");
}
#[test]
fn interp_recursive_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(9)) end", 0);
    assert_eq!(out, "362880\n");
}
#[test]
fn interp_recursive_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(10)) end", 0);
    assert_eq!(out, "3628800\n");
}
#[test]
fn interp_recursive_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(1)) end", 0);
    assert_eq!(out, "1\n");
}
#[test]
fn interp_recursive_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(2)) end", 0);
    assert_eq!(out, "2\n");
}
#[test]
fn interp_recursive_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(3)) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn interp_recursive_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(4)) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn interp_recursive_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(5)) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn interp_recursive_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(6)) end", 0);
    assert_eq!(out, "720\n");
}
#[test]
fn interp_recursive_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(7)) end", 0);
    assert_eq!(out, "5040\n");
}
#[test]
fn interp_recursive_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(8)) end", 0);
    assert_eq!(out, "40320\n");
}
#[test]
fn interp_recursive_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(9)) end", 0);
    assert_eq!(out, "362880\n");
}
#[test]
fn interp_recursive_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(10)) end", 0);
    assert_eq!(out, "3628800\n");
}
#[test]
fn interp_recursive_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(1)) end", 0);
    assert_eq!(out, "1\n");
}
#[test]
fn interp_recursive_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(2)) end", 0);
    assert_eq!(out, "2\n");
}
#[test]
fn interp_recursive_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(3)) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn interp_recursive_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(4)) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn interp_recursive_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(5)) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn interp_recursive_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(6)) end", 0);
    assert_eq!(out, "720\n");
}
#[test]
fn interp_recursive_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(7)) end", 0);
    assert_eq!(out, "5040\n");
}
#[test]
fn interp_recursive_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(8)) end", 0);
    assert_eq!(out, "40320\n");
}
#[test]
fn interp_recursive_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(9)) end", 0);
    assert_eq!(out, "362880\n");
}
#[test]
fn interp_recursive_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(10)) end", 0);
    assert_eq!(out, "3628800\n");
}
#[test]
fn interp_recursive_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(1)) end", 0);
    assert_eq!(out, "1\n");
}
#[test]
fn interp_recursive_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(2)) end", 0);
    assert_eq!(out, "2\n");
}
#[test]
fn interp_recursive_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(3)) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn interp_recursive_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(4)) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn interp_recursive_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(5)) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn interp_recursive_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(6)) end", 0);
    assert_eq!(out, "720\n");
}
#[test]
fn interp_recursive_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(7)) end", 0);
    assert_eq!(out, "5040\n");
}
#[test]
fn interp_recursive_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(8)) end", 0);
    assert_eq!(out, "40320\n");
}
#[test]
fn interp_recursive_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(9)) end", 0);
    assert_eq!(out, "362880\n");
}
#[test]
fn interp_recursive_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn fact(n) if n <= 1 then return 1 end return n * fact(n - 1) end fn main() print(fact(10)) end", 0);
    assert_eq!(out, "3628800\n");
}
#[test]
fn interp_tuple_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (0, 1, 2) print(t[0]) end", 0);
    assert_eq!(out, "0\n");
}
#[test]
fn interp_tuple_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (1, 2, 3) print(t[0]) end", 0);
    assert_eq!(out, "1\n");
}
#[test]
fn interp_tuple_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (2, 3, 4) print(t[0]) end", 0);
    assert_eq!(out, "2\n");
}
#[test]
fn interp_tuple_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (3, 4, 5) print(t[0]) end", 0);
    assert_eq!(out, "3\n");
}
#[test]
fn interp_tuple_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (4, 5, 6) print(t[0]) end", 0);
    assert_eq!(out, "4\n");
}
#[test]
fn interp_tuple_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (5, 6, 7) print(t[0]) end", 0);
    assert_eq!(out, "5\n");
}
#[test]
fn interp_tuple_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (6, 7, 8) print(t[0]) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn interp_tuple_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (7, 8, 9) print(t[0]) end", 0);
    assert_eq!(out, "7\n");
}
#[test]
fn interp_tuple_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (8, 9, 10) print(t[0]) end", 0);
    assert_eq!(out, "8\n");
}
#[test]
fn interp_tuple_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (9, 10, 11) print(t[0]) end", 0);
    assert_eq!(out, "9\n");
}
#[test]
fn interp_tuple_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (10, 11, 12) print(t[0]) end", 0);
    assert_eq!(out, "10\n");
}
#[test]
fn interp_tuple_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (11, 12, 13) print(t[0]) end", 0);
    assert_eq!(out, "11\n");
}
#[test]
fn interp_tuple_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (12, 13, 14) print(t[0]) end", 0);
    assert_eq!(out, "12\n");
}
#[test]
fn interp_tuple_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (13, 14, 15) print(t[0]) end", 0);
    assert_eq!(out, "13\n");
}
#[test]
fn interp_tuple_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (14, 15, 16) print(t[0]) end", 0);
    assert_eq!(out, "14\n");
}
#[test]
fn interp_tuple_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (15, 16, 17) print(t[0]) end", 0);
    assert_eq!(out, "15\n");
}
#[test]
fn interp_tuple_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (16, 17, 18) print(t[0]) end", 0);
    assert_eq!(out, "16\n");
}
#[test]
fn interp_tuple_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (17, 18, 19) print(t[0]) end", 0);
    assert_eq!(out, "17\n");
}
#[test]
fn interp_tuple_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (18, 19, 20) print(t[0]) end", 0);
    assert_eq!(out, "18\n");
}
#[test]
fn interp_tuple_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (19, 20, 21) print(t[0]) end", 0);
    assert_eq!(out, "19\n");
}
#[test]
fn interp_tuple_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (20, 21, 22) print(t[0]) end", 0);
    assert_eq!(out, "20\n");
}
#[test]
fn interp_tuple_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (21, 22, 23) print(t[0]) end", 0);
    assert_eq!(out, "21\n");
}
#[test]
fn interp_tuple_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (22, 23, 24) print(t[0]) end", 0);
    assert_eq!(out, "22\n");
}
#[test]
fn interp_tuple_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (23, 24, 25) print(t[0]) end", 0);
    assert_eq!(out, "23\n");
}
#[test]
fn interp_tuple_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (24, 25, 26) print(t[0]) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn interp_tuple_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (25, 26, 27) print(t[0]) end", 0);
    assert_eq!(out, "25\n");
}
#[test]
fn interp_tuple_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (26, 27, 28) print(t[0]) end", 0);
    assert_eq!(out, "26\n");
}
#[test]
fn interp_tuple_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (27, 28, 29) print(t[0]) end", 0);
    assert_eq!(out, "27\n");
}
#[test]
fn interp_tuple_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (28, 29, 30) print(t[0]) end", 0);
    assert_eq!(out, "28\n");
}
#[test]
fn interp_tuple_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (29, 30, 31) print(t[0]) end", 0);
    assert_eq!(out, "29\n");
}
#[test]
fn interp_tuple_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (30, 31, 32) print(t[0]) end", 0);
    assert_eq!(out, "30\n");
}
#[test]
fn interp_tuple_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (31, 32, 33) print(t[0]) end", 0);
    assert_eq!(out, "31\n");
}
#[test]
fn interp_tuple_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (32, 33, 34) print(t[0]) end", 0);
    assert_eq!(out, "32\n");
}
#[test]
fn interp_tuple_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (33, 34, 35) print(t[0]) end", 0);
    assert_eq!(out, "33\n");
}
#[test]
fn interp_tuple_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (34, 35, 36) print(t[0]) end", 0);
    assert_eq!(out, "34\n");
}
#[test]
fn interp_tuple_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (35, 36, 37) print(t[0]) end", 0);
    assert_eq!(out, "35\n");
}
#[test]
fn interp_tuple_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (36, 37, 38) print(t[0]) end", 0);
    assert_eq!(out, "36\n");
}
#[test]
fn interp_tuple_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (37, 38, 39) print(t[0]) end", 0);
    assert_eq!(out, "37\n");
}
#[test]
fn interp_tuple_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (38, 39, 40) print(t[0]) end", 0);
    assert_eq!(out, "38\n");
}
#[test]
fn interp_tuple_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (39, 40, 41) print(t[0]) end", 0);
    assert_eq!(out, "39\n");
}
#[test]
fn interp_tuple_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (40, 41, 42) print(t[0]) end", 0);
    assert_eq!(out, "40\n");
}
#[test]
fn interp_tuple_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (41, 42, 43) print(t[0]) end", 0);
    assert_eq!(out, "41\n");
}
#[test]
fn interp_tuple_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (42, 43, 44) print(t[0]) end", 0);
    assert_eq!(out, "42\n");
}
#[test]
fn interp_tuple_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (43, 44, 45) print(t[0]) end", 0);
    assert_eq!(out, "43\n");
}
#[test]
fn interp_tuple_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (44, 45, 46) print(t[0]) end", 0);
    assert_eq!(out, "44\n");
}
#[test]
fn interp_tuple_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (45, 46, 47) print(t[0]) end", 0);
    assert_eq!(out, "45\n");
}
#[test]
fn interp_tuple_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (46, 47, 48) print(t[0]) end", 0);
    assert_eq!(out, "46\n");
}
#[test]
fn interp_tuple_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (47, 48, 49) print(t[0]) end", 0);
    assert_eq!(out, "47\n");
}
#[test]
fn interp_tuple_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (48, 49, 50) print(t[0]) end", 0);
    assert_eq!(out, "48\n");
}
#[test]
fn interp_tuple_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (49, 50, 51) print(t[0]) end", 0);
    assert_eq!(out, "49\n");
}
#[test]
fn interp_tuple_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (50, 51, 52) print(t[0]) end", 0);
    assert_eq!(out, "50\n");
}
#[test]
fn interp_tuple_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (51, 52, 53) print(t[0]) end", 0);
    assert_eq!(out, "51\n");
}
#[test]
fn interp_tuple_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (52, 53, 54) print(t[0]) end", 0);
    assert_eq!(out, "52\n");
}
#[test]
fn interp_tuple_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (53, 54, 55) print(t[0]) end", 0);
    assert_eq!(out, "53\n");
}
#[test]
fn interp_tuple_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (54, 55, 56) print(t[0]) end", 0);
    assert_eq!(out, "54\n");
}
#[test]
fn interp_tuple_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (55, 56, 57) print(t[0]) end", 0);
    assert_eq!(out, "55\n");
}
#[test]
fn interp_tuple_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (56, 57, 58) print(t[0]) end", 0);
    assert_eq!(out, "56\n");
}
#[test]
fn interp_tuple_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (57, 58, 59) print(t[0]) end", 0);
    assert_eq!(out, "57\n");
}
#[test]
fn interp_tuple_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (58, 59, 60) print(t[0]) end", 0);
    assert_eq!(out, "58\n");
}
#[test]
fn interp_tuple_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (59, 60, 61) print(t[0]) end", 0);
    assert_eq!(out, "59\n");
}
#[test]
fn interp_tuple_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (60, 61, 62) print(t[0]) end", 0);
    assert_eq!(out, "60\n");
}
#[test]
fn interp_tuple_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (61, 62, 63) print(t[0]) end", 0);
    assert_eq!(out, "61\n");
}
#[test]
fn interp_tuple_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (62, 63, 64) print(t[0]) end", 0);
    assert_eq!(out, "62\n");
}
#[test]
fn interp_tuple_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (63, 64, 65) print(t[0]) end", 0);
    assert_eq!(out, "63\n");
}
#[test]
fn interp_tuple_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (64, 65, 66) print(t[0]) end", 0);
    assert_eq!(out, "64\n");
}
#[test]
fn interp_tuple_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (65, 66, 67) print(t[0]) end", 0);
    assert_eq!(out, "65\n");
}
#[test]
fn interp_tuple_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (66, 67, 68) print(t[0]) end", 0);
    assert_eq!(out, "66\n");
}
#[test]
fn interp_tuple_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (67, 68, 69) print(t[0]) end", 0);
    assert_eq!(out, "67\n");
}
#[test]
fn interp_tuple_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (68, 69, 70) print(t[0]) end", 0);
    assert_eq!(out, "68\n");
}
#[test]
fn interp_tuple_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (69, 70, 71) print(t[0]) end", 0);
    assert_eq!(out, "69\n");
}
#[test]
fn interp_tuple_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (70, 71, 72) print(t[0]) end", 0);
    assert_eq!(out, "70\n");
}
#[test]
fn interp_tuple_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (71, 72, 73) print(t[0]) end", 0);
    assert_eq!(out, "71\n");
}
#[test]
fn interp_tuple_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (72, 73, 74) print(t[0]) end", 0);
    assert_eq!(out, "72\n");
}
#[test]
fn interp_tuple_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (73, 74, 75) print(t[0]) end", 0);
    assert_eq!(out, "73\n");
}
#[test]
fn interp_tuple_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (74, 75, 76) print(t[0]) end", 0);
    assert_eq!(out, "74\n");
}
#[test]
fn interp_tuple_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (75, 76, 77) print(t[0]) end", 0);
    assert_eq!(out, "75\n");
}
#[test]
fn interp_tuple_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (76, 77, 78) print(t[0]) end", 0);
    assert_eq!(out, "76\n");
}
#[test]
fn interp_tuple_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (77, 78, 79) print(t[0]) end", 0);
    assert_eq!(out, "77\n");
}
#[test]
fn interp_tuple_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (78, 79, 80) print(t[0]) end", 0);
    assert_eq!(out, "78\n");
}
#[test]
fn interp_tuple_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (79, 80, 81) print(t[0]) end", 0);
    assert_eq!(out, "79\n");
}
#[test]
fn interp_tuple_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (80, 81, 82) print(t[0]) end", 0);
    assert_eq!(out, "80\n");
}
#[test]
fn interp_tuple_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (81, 82, 83) print(t[0]) end", 0);
    assert_eq!(out, "81\n");
}
#[test]
fn interp_tuple_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (82, 83, 84) print(t[0]) end", 0);
    assert_eq!(out, "82\n");
}
#[test]
fn interp_tuple_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (83, 84, 85) print(t[0]) end", 0);
    assert_eq!(out, "83\n");
}
#[test]
fn interp_tuple_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (84, 85, 86) print(t[0]) end", 0);
    assert_eq!(out, "84\n");
}
#[test]
fn interp_tuple_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (85, 86, 87) print(t[0]) end", 0);
    assert_eq!(out, "85\n");
}
#[test]
fn interp_tuple_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (86, 87, 88) print(t[0]) end", 0);
    assert_eq!(out, "86\n");
}
#[test]
fn interp_tuple_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (87, 88, 89) print(t[0]) end", 0);
    assert_eq!(out, "87\n");
}
#[test]
fn interp_tuple_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (88, 89, 90) print(t[0]) end", 0);
    assert_eq!(out, "88\n");
}
#[test]
fn interp_tuple_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (89, 90, 91) print(t[0]) end", 0);
    assert_eq!(out, "89\n");
}
#[test]
fn interp_tuple_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (90, 91, 92) print(t[0]) end", 0);
    assert_eq!(out, "90\n");
}
#[test]
fn interp_tuple_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (91, 92, 93) print(t[0]) end", 0);
    assert_eq!(out, "91\n");
}
#[test]
fn interp_tuple_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (92, 93, 94) print(t[0]) end", 0);
    assert_eq!(out, "92\n");
}
#[test]
fn interp_tuple_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (93, 94, 95) print(t[0]) end", 0);
    assert_eq!(out, "93\n");
}
#[test]
fn interp_tuple_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (94, 95, 96) print(t[0]) end", 0);
    assert_eq!(out, "94\n");
}
#[test]
fn interp_tuple_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (95, 96, 97) print(t[0]) end", 0);
    assert_eq!(out, "95\n");
}
#[test]
fn interp_tuple_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (96, 97, 98) print(t[0]) end", 0);
    assert_eq!(out, "96\n");
}
#[test]
fn interp_tuple_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (97, 98, 99) print(t[0]) end", 0);
    assert_eq!(out, "97\n");
}
#[test]
fn interp_tuple_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (98, 99, 100) print(t[0]) end", 0);
    assert_eq!(out, "98\n");
}
#[test]
fn interp_tuple_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (99, 100, 101) print(t[0]) end", 0);
    assert_eq!(out, "99\n");
}
#[test]
fn interp_tuple_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (100, 101, 102) print(t[0]) end", 0);
    assert_eq!(out, "100\n");
}
#[test]
fn interp_tuple_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (101, 102, 103) print(t[0]) end", 0);
    assert_eq!(out, "101\n");
}
#[test]
fn interp_tuple_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (102, 103, 104) print(t[0]) end", 0);
    assert_eq!(out, "102\n");
}
#[test]
fn interp_tuple_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (103, 104, 105) print(t[0]) end", 0);
    assert_eq!(out, "103\n");
}
#[test]
fn interp_tuple_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (104, 105, 106) print(t[0]) end", 0);
    assert_eq!(out, "104\n");
}
#[test]
fn interp_tuple_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (105, 106, 107) print(t[0]) end", 0);
    assert_eq!(out, "105\n");
}
#[test]
fn interp_tuple_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (106, 107, 108) print(t[0]) end", 0);
    assert_eq!(out, "106\n");
}
#[test]
fn interp_tuple_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (107, 108, 109) print(t[0]) end", 0);
    assert_eq!(out, "107\n");
}
#[test]
fn interp_tuple_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (108, 109, 110) print(t[0]) end", 0);
    assert_eq!(out, "108\n");
}
#[test]
fn interp_tuple_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (109, 110, 111) print(t[0]) end", 0);
    assert_eq!(out, "109\n");
}
#[test]
fn interp_tuple_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (110, 111, 112) print(t[0]) end", 0);
    assert_eq!(out, "110\n");
}
#[test]
fn interp_tuple_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (111, 112, 113) print(t[0]) end", 0);
    assert_eq!(out, "111\n");
}
#[test]
fn interp_tuple_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (112, 113, 114) print(t[0]) end", 0);
    assert_eq!(out, "112\n");
}
#[test]
fn interp_tuple_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (113, 114, 115) print(t[0]) end", 0);
    assert_eq!(out, "113\n");
}
#[test]
fn interp_tuple_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (114, 115, 116) print(t[0]) end", 0);
    assert_eq!(out, "114\n");
}
#[test]
fn interp_tuple_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (115, 116, 117) print(t[0]) end", 0);
    assert_eq!(out, "115\n");
}
#[test]
fn interp_tuple_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (116, 117, 118) print(t[0]) end", 0);
    assert_eq!(out, "116\n");
}
#[test]
fn interp_tuple_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (117, 118, 119) print(t[0]) end", 0);
    assert_eq!(out, "117\n");
}
#[test]
fn interp_tuple_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (118, 119, 120) print(t[0]) end", 0);
    assert_eq!(out, "118\n");
}
#[test]
fn interp_tuple_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (119, 120, 121) print(t[0]) end", 0);
    assert_eq!(out, "119\n");
}
#[test]
fn interp_tuple_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (120, 121, 122) print(t[0]) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn interp_tuple_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (121, 122, 123) print(t[0]) end", 0);
    assert_eq!(out, "121\n");
}
#[test]
fn interp_tuple_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (122, 123, 124) print(t[0]) end", 0);
    assert_eq!(out, "122\n");
}
#[test]
fn interp_tuple_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (123, 124, 125) print(t[0]) end", 0);
    assert_eq!(out, "123\n");
}
#[test]
fn interp_tuple_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (124, 125, 126) print(t[0]) end", 0);
    assert_eq!(out, "124\n");
}
#[test]
fn interp_tuple_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (125, 126, 127) print(t[0]) end", 0);
    assert_eq!(out, "125\n");
}
#[test]
fn interp_tuple_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (126, 127, 128) print(t[0]) end", 0);
    assert_eq!(out, "126\n");
}
#[test]
fn interp_tuple_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (127, 128, 129) print(t[0]) end", 0);
    assert_eq!(out, "127\n");
}
#[test]
fn interp_tuple_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (128, 129, 130) print(t[0]) end", 0);
    assert_eq!(out, "128\n");
}
#[test]
fn interp_tuple_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (129, 130, 131) print(t[0]) end", 0);
    assert_eq!(out, "129\n");
}
#[test]
fn interp_tuple_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (130, 131, 132) print(t[0]) end", 0);
    assert_eq!(out, "130\n");
}
#[test]
fn interp_tuple_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (131, 132, 133) print(t[0]) end", 0);
    assert_eq!(out, "131\n");
}
#[test]
fn interp_tuple_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (132, 133, 134) print(t[0]) end", 0);
    assert_eq!(out, "132\n");
}
#[test]
fn interp_tuple_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (133, 134, 135) print(t[0]) end", 0);
    assert_eq!(out, "133\n");
}
#[test]
fn interp_tuple_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (134, 135, 136) print(t[0]) end", 0);
    assert_eq!(out, "134\n");
}
#[test]
fn interp_tuple_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (135, 136, 137) print(t[0]) end", 0);
    assert_eq!(out, "135\n");
}
#[test]
fn interp_tuple_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (136, 137, 138) print(t[0]) end", 0);
    assert_eq!(out, "136\n");
}
#[test]
fn interp_tuple_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (137, 138, 139) print(t[0]) end", 0);
    assert_eq!(out, "137\n");
}
#[test]
fn interp_tuple_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (138, 139, 140) print(t[0]) end", 0);
    assert_eq!(out, "138\n");
}
#[test]
fn interp_tuple_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (139, 140, 141) print(t[0]) end", 0);
    assert_eq!(out, "139\n");
}
#[test]
fn interp_tuple_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (140, 141, 142) print(t[0]) end", 0);
    assert_eq!(out, "140\n");
}
#[test]
fn interp_tuple_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (141, 142, 143) print(t[0]) end", 0);
    assert_eq!(out, "141\n");
}
#[test]
fn interp_tuple_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (142, 143, 144) print(t[0]) end", 0);
    assert_eq!(out, "142\n");
}
#[test]
fn interp_tuple_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (143, 144, 145) print(t[0]) end", 0);
    assert_eq!(out, "143\n");
}
#[test]
fn interp_tuple_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (144, 145, 146) print(t[0]) end", 0);
    assert_eq!(out, "144\n");
}
#[test]
fn interp_tuple_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (145, 146, 147) print(t[0]) end", 0);
    assert_eq!(out, "145\n");
}
#[test]
fn interp_tuple_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (146, 147, 148) print(t[0]) end", 0);
    assert_eq!(out, "146\n");
}
#[test]
fn interp_tuple_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (147, 148, 149) print(t[0]) end", 0);
    assert_eq!(out, "147\n");
}
#[test]
fn interp_tuple_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (148, 149, 150) print(t[0]) end", 0);
    assert_eq!(out, "148\n");
}
#[test]
fn interp_tuple_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (149, 150, 151) print(t[0]) end", 0);
    assert_eq!(out, "149\n");
}
#[test]
fn interp_tuple_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (150, 151, 152) print(t[0]) end", 0);
    assert_eq!(out, "150\n");
}
#[test]
fn interp_tuple_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (151, 152, 153) print(t[0]) end", 0);
    assert_eq!(out, "151\n");
}
#[test]
fn interp_tuple_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (152, 153, 154) print(t[0]) end", 0);
    assert_eq!(out, "152\n");
}
#[test]
fn interp_tuple_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (153, 154, 155) print(t[0]) end", 0);
    assert_eq!(out, "153\n");
}
#[test]
fn interp_tuple_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (154, 155, 156) print(t[0]) end", 0);
    assert_eq!(out, "154\n");
}
#[test]
fn interp_tuple_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (155, 156, 157) print(t[0]) end", 0);
    assert_eq!(out, "155\n");
}
#[test]
fn interp_tuple_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (156, 157, 158) print(t[0]) end", 0);
    assert_eq!(out, "156\n");
}
#[test]
fn interp_tuple_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (157, 158, 159) print(t[0]) end", 0);
    assert_eq!(out, "157\n");
}
#[test]
fn interp_tuple_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (158, 159, 160) print(t[0]) end", 0);
    assert_eq!(out, "158\n");
}
#[test]
fn interp_tuple_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (159, 160, 161) print(t[0]) end", 0);
    assert_eq!(out, "159\n");
}
#[test]
fn interp_tuple_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (160, 161, 162) print(t[0]) end", 0);
    assert_eq!(out, "160\n");
}
#[test]
fn interp_tuple_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (161, 162, 163) print(t[0]) end", 0);
    assert_eq!(out, "161\n");
}
#[test]
fn interp_tuple_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (162, 163, 164) print(t[0]) end", 0);
    assert_eq!(out, "162\n");
}
#[test]
fn interp_tuple_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (163, 164, 165) print(t[0]) end", 0);
    assert_eq!(out, "163\n");
}
#[test]
fn interp_tuple_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (164, 165, 166) print(t[0]) end", 0);
    assert_eq!(out, "164\n");
}
#[test]
fn interp_tuple_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (165, 166, 167) print(t[0]) end", 0);
    assert_eq!(out, "165\n");
}
#[test]
fn interp_tuple_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (166, 167, 168) print(t[0]) end", 0);
    assert_eq!(out, "166\n");
}
#[test]
fn interp_tuple_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (167, 168, 169) print(t[0]) end", 0);
    assert_eq!(out, "167\n");
}
#[test]
fn interp_tuple_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (168, 169, 170) print(t[0]) end", 0);
    assert_eq!(out, "168\n");
}
#[test]
fn interp_tuple_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (169, 170, 171) print(t[0]) end", 0);
    assert_eq!(out, "169\n");
}
#[test]
fn interp_tuple_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (170, 171, 172) print(t[0]) end", 0);
    assert_eq!(out, "170\n");
}
#[test]
fn interp_tuple_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (171, 172, 173) print(t[0]) end", 0);
    assert_eq!(out, "171\n");
}
#[test]
fn interp_tuple_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (172, 173, 174) print(t[0]) end", 0);
    assert_eq!(out, "172\n");
}
#[test]
fn interp_tuple_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (173, 174, 175) print(t[0]) end", 0);
    assert_eq!(out, "173\n");
}
#[test]
fn interp_tuple_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (174, 175, 176) print(t[0]) end", 0);
    assert_eq!(out, "174\n");
}
#[test]
fn interp_tuple_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (175, 176, 177) print(t[0]) end", 0);
    assert_eq!(out, "175\n");
}
#[test]
fn interp_tuple_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (176, 177, 178) print(t[0]) end", 0);
    assert_eq!(out, "176\n");
}
#[test]
fn interp_tuple_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (177, 178, 179) print(t[0]) end", 0);
    assert_eq!(out, "177\n");
}
#[test]
fn interp_tuple_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (178, 179, 180) print(t[0]) end", 0);
    assert_eq!(out, "178\n");
}
#[test]
fn interp_tuple_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (179, 180, 181) print(t[0]) end", 0);
    assert_eq!(out, "179\n");
}
#[test]
fn interp_tuple_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (180, 181, 182) print(t[0]) end", 0);
    assert_eq!(out, "180\n");
}
#[test]
fn interp_tuple_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (181, 182, 183) print(t[0]) end", 0);
    assert_eq!(out, "181\n");
}
#[test]
fn interp_tuple_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (182, 183, 184) print(t[0]) end", 0);
    assert_eq!(out, "182\n");
}
#[test]
fn interp_tuple_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (183, 184, 185) print(t[0]) end", 0);
    assert_eq!(out, "183\n");
}
#[test]
fn interp_tuple_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (184, 185, 186) print(t[0]) end", 0);
    assert_eq!(out, "184\n");
}
#[test]
fn interp_tuple_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (185, 186, 187) print(t[0]) end", 0);
    assert_eq!(out, "185\n");
}
#[test]
fn interp_tuple_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (186, 187, 188) print(t[0]) end", 0);
    assert_eq!(out, "186\n");
}
#[test]
fn interp_tuple_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (187, 188, 189) print(t[0]) end", 0);
    assert_eq!(out, "187\n");
}
#[test]
fn interp_tuple_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (188, 189, 190) print(t[0]) end", 0);
    assert_eq!(out, "188\n");
}
#[test]
fn interp_tuple_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (189, 190, 191) print(t[0]) end", 0);
    assert_eq!(out, "189\n");
}
#[test]
fn interp_tuple_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (190, 191, 192) print(t[0]) end", 0);
    assert_eq!(out, "190\n");
}
#[test]
fn interp_tuple_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (191, 192, 193) print(t[0]) end", 0);
    assert_eq!(out, "191\n");
}
#[test]
fn interp_tuple_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (192, 193, 194) print(t[0]) end", 0);
    assert_eq!(out, "192\n");
}
#[test]
fn interp_tuple_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (193, 194, 195) print(t[0]) end", 0);
    assert_eq!(out, "193\n");
}
#[test]
fn interp_tuple_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (194, 195, 196) print(t[0]) end", 0);
    assert_eq!(out, "194\n");
}
#[test]
fn interp_tuple_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (195, 196, 197) print(t[0]) end", 0);
    assert_eq!(out, "195\n");
}
#[test]
fn interp_tuple_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (196, 197, 198) print(t[0]) end", 0);
    assert_eq!(out, "196\n");
}
#[test]
fn interp_tuple_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (197, 198, 199) print(t[0]) end", 0);
    assert_eq!(out, "197\n");
}
#[test]
fn interp_tuple_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (198, 199, 200) print(t[0]) end", 0);
    assert_eq!(out, "198\n");
}
#[test]
fn interp_tuple_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (199, 200, 201) print(t[0]) end", 0);
    assert_eq!(out, "199\n");
}
#[test]
fn interp_tuple_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (200, 201, 202) print(t[0]) end", 0);
    assert_eq!(out, "200\n");
}
#[test]
fn interp_tuple_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (201, 202, 203) print(t[0]) end", 0);
    assert_eq!(out, "201\n");
}
#[test]
fn interp_tuple_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (202, 203, 204) print(t[0]) end", 0);
    assert_eq!(out, "202\n");
}
#[test]
fn interp_tuple_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (203, 204, 205) print(t[0]) end", 0);
    assert_eq!(out, "203\n");
}
#[test]
fn interp_tuple_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (204, 205, 206) print(t[0]) end", 0);
    assert_eq!(out, "204\n");
}
#[test]
fn interp_tuple_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (205, 206, 207) print(t[0]) end", 0);
    assert_eq!(out, "205\n");
}
#[test]
fn interp_tuple_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (206, 207, 208) print(t[0]) end", 0);
    assert_eq!(out, "206\n");
}
#[test]
fn interp_tuple_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (207, 208, 209) print(t[0]) end", 0);
    assert_eq!(out, "207\n");
}
#[test]
fn interp_tuple_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (208, 209, 210) print(t[0]) end", 0);
    assert_eq!(out, "208\n");
}
#[test]
fn interp_tuple_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (209, 210, 211) print(t[0]) end", 0);
    assert_eq!(out, "209\n");
}
#[test]
fn interp_tuple_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (210, 211, 212) print(t[0]) end", 0);
    assert_eq!(out, "210\n");
}
#[test]
fn interp_tuple_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (211, 212, 213) print(t[0]) end", 0);
    assert_eq!(out, "211\n");
}
#[test]
fn interp_tuple_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (212, 213, 214) print(t[0]) end", 0);
    assert_eq!(out, "212\n");
}
#[test]
fn interp_tuple_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (213, 214, 215) print(t[0]) end", 0);
    assert_eq!(out, "213\n");
}
#[test]
fn interp_tuple_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (214, 215, 216) print(t[0]) end", 0);
    assert_eq!(out, "214\n");
}
#[test]
fn interp_tuple_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (215, 216, 217) print(t[0]) end", 0);
    assert_eq!(out, "215\n");
}
#[test]
fn interp_tuple_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (216, 217, 218) print(t[0]) end", 0);
    assert_eq!(out, "216\n");
}
#[test]
fn interp_tuple_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (217, 218, 219) print(t[0]) end", 0);
    assert_eq!(out, "217\n");
}
#[test]
fn interp_tuple_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (218, 219, 220) print(t[0]) end", 0);
    assert_eq!(out, "218\n");
}
#[test]
fn interp_tuple_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (219, 220, 221) print(t[0]) end", 0);
    assert_eq!(out, "219\n");
}
#[test]
fn interp_tuple_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (220, 221, 222) print(t[0]) end", 0);
    assert_eq!(out, "220\n");
}
#[test]
fn interp_tuple_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (221, 222, 223) print(t[0]) end", 0);
    assert_eq!(out, "221\n");
}
#[test]
fn interp_tuple_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (222, 223, 224) print(t[0]) end", 0);
    assert_eq!(out, "222\n");
}
#[test]
fn interp_tuple_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (223, 224, 225) print(t[0]) end", 0);
    assert_eq!(out, "223\n");
}
#[test]
fn interp_tuple_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (224, 225, 226) print(t[0]) end", 0);
    assert_eq!(out, "224\n");
}
#[test]
fn interp_tuple_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (225, 226, 227) print(t[0]) end", 0);
    assert_eq!(out, "225\n");
}
#[test]
fn interp_tuple_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (226, 227, 228) print(t[0]) end", 0);
    assert_eq!(out, "226\n");
}
#[test]
fn interp_tuple_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (227, 228, 229) print(t[0]) end", 0);
    assert_eq!(out, "227\n");
}
#[test]
fn interp_tuple_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (228, 229, 230) print(t[0]) end", 0);
    assert_eq!(out, "228\n");
}
#[test]
fn interp_tuple_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (229, 230, 231) print(t[0]) end", 0);
    assert_eq!(out, "229\n");
}
#[test]
fn interp_tuple_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (230, 231, 232) print(t[0]) end", 0);
    assert_eq!(out, "230\n");
}
#[test]
fn interp_tuple_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (231, 232, 233) print(t[0]) end", 0);
    assert_eq!(out, "231\n");
}
#[test]
fn interp_tuple_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (232, 233, 234) print(t[0]) end", 0);
    assert_eq!(out, "232\n");
}
#[test]
fn interp_tuple_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (233, 234, 235) print(t[0]) end", 0);
    assert_eq!(out, "233\n");
}
#[test]
fn interp_tuple_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (234, 235, 236) print(t[0]) end", 0);
    assert_eq!(out, "234\n");
}
#[test]
fn interp_tuple_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (235, 236, 237) print(t[0]) end", 0);
    assert_eq!(out, "235\n");
}
#[test]
fn interp_tuple_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (236, 237, 238) print(t[0]) end", 0);
    assert_eq!(out, "236\n");
}
#[test]
fn interp_tuple_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (237, 238, 239) print(t[0]) end", 0);
    assert_eq!(out, "237\n");
}
#[test]
fn interp_tuple_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (238, 239, 240) print(t[0]) end", 0);
    assert_eq!(out, "238\n");
}
#[test]
fn interp_tuple_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (239, 240, 241) print(t[0]) end", 0);
    assert_eq!(out, "239\n");
}
#[test]
fn interp_tuple_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (240, 241, 242) print(t[0]) end", 0);
    assert_eq!(out, "240\n");
}
#[test]
fn interp_tuple_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (241, 242, 243) print(t[0]) end", 0);
    assert_eq!(out, "241\n");
}
#[test]
fn interp_tuple_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (242, 243, 244) print(t[0]) end", 0);
    assert_eq!(out, "242\n");
}
#[test]
fn interp_tuple_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (243, 244, 245) print(t[0]) end", 0);
    assert_eq!(out, "243\n");
}
#[test]
fn interp_tuple_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (244, 245, 246) print(t[0]) end", 0);
    assert_eq!(out, "244\n");
}
#[test]
fn interp_tuple_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (245, 246, 247) print(t[0]) end", 0);
    assert_eq!(out, "245\n");
}
#[test]
fn interp_tuple_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (246, 247, 248) print(t[0]) end", 0);
    assert_eq!(out, "246\n");
}
#[test]
fn interp_tuple_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (247, 248, 249) print(t[0]) end", 0);
    assert_eq!(out, "247\n");
}
#[test]
fn interp_tuple_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (248, 249, 250) print(t[0]) end", 0);
    assert_eq!(out, "248\n");
}
#[test]
fn interp_tuple_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (249, 250, 251) print(t[0]) end", 0);
    assert_eq!(out, "249\n");
}
#[test]
fn interp_tuple_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (250, 251, 252) print(t[0]) end", 0);
    assert_eq!(out, "250\n");
}
#[test]
fn interp_tuple_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (251, 252, 253) print(t[0]) end", 0);
    assert_eq!(out, "251\n");
}
#[test]
fn interp_tuple_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (252, 253, 254) print(t[0]) end", 0);
    assert_eq!(out, "252\n");
}
#[test]
fn interp_tuple_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (253, 254, 255) print(t[0]) end", 0);
    assert_eq!(out, "253\n");
}
#[test]
fn interp_tuple_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (254, 255, 256) print(t[0]) end", 0);
    assert_eq!(out, "254\n");
}
#[test]
fn interp_tuple_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (255, 256, 257) print(t[0]) end", 0);
    assert_eq!(out, "255\n");
}
#[test]
fn interp_tuple_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (256, 257, 258) print(t[0]) end", 0);
    assert_eq!(out, "256\n");
}
#[test]
fn interp_tuple_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (257, 258, 259) print(t[0]) end", 0);
    assert_eq!(out, "257\n");
}
#[test]
fn interp_tuple_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (258, 259, 260) print(t[0]) end", 0);
    assert_eq!(out, "258\n");
}
#[test]
fn interp_tuple_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (259, 260, 261) print(t[0]) end", 0);
    assert_eq!(out, "259\n");
}
#[test]
fn interp_tuple_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (260, 261, 262) print(t[0]) end", 0);
    assert_eq!(out, "260\n");
}
#[test]
fn interp_tuple_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (261, 262, 263) print(t[0]) end", 0);
    assert_eq!(out, "261\n");
}
#[test]
fn interp_tuple_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (262, 263, 264) print(t[0]) end", 0);
    assert_eq!(out, "262\n");
}
#[test]
fn interp_tuple_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (263, 264, 265) print(t[0]) end", 0);
    assert_eq!(out, "263\n");
}
#[test]
fn interp_tuple_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (264, 265, 266) print(t[0]) end", 0);
    assert_eq!(out, "264\n");
}
#[test]
fn interp_tuple_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (265, 266, 267) print(t[0]) end", 0);
    assert_eq!(out, "265\n");
}
#[test]
fn interp_tuple_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (266, 267, 268) print(t[0]) end", 0);
    assert_eq!(out, "266\n");
}
#[test]
fn interp_tuple_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (267, 268, 269) print(t[0]) end", 0);
    assert_eq!(out, "267\n");
}
#[test]
fn interp_tuple_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (268, 269, 270) print(t[0]) end", 0);
    assert_eq!(out, "268\n");
}
#[test]
fn interp_tuple_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (269, 270, 271) print(t[0]) end", 0);
    assert_eq!(out, "269\n");
}
#[test]
fn interp_tuple_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (270, 271, 272) print(t[0]) end", 0);
    assert_eq!(out, "270\n");
}
#[test]
fn interp_tuple_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (271, 272, 273) print(t[0]) end", 0);
    assert_eq!(out, "271\n");
}
#[test]
fn interp_tuple_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (272, 273, 274) print(t[0]) end", 0);
    assert_eq!(out, "272\n");
}
#[test]
fn interp_tuple_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (273, 274, 275) print(t[0]) end", 0);
    assert_eq!(out, "273\n");
}
#[test]
fn interp_tuple_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (274, 275, 276) print(t[0]) end", 0);
    assert_eq!(out, "274\n");
}
#[test]
fn interp_tuple_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (275, 276, 277) print(t[0]) end", 0);
    assert_eq!(out, "275\n");
}
#[test]
fn interp_tuple_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (276, 277, 278) print(t[0]) end", 0);
    assert_eq!(out, "276\n");
}
#[test]
fn interp_tuple_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (277, 278, 279) print(t[0]) end", 0);
    assert_eq!(out, "277\n");
}
#[test]
fn interp_tuple_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (278, 279, 280) print(t[0]) end", 0);
    assert_eq!(out, "278\n");
}
#[test]
fn interp_tuple_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (279, 280, 281) print(t[0]) end", 0);
    assert_eq!(out, "279\n");
}
#[test]
fn interp_tuple_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (280, 281, 282) print(t[0]) end", 0);
    assert_eq!(out, "280\n");
}
#[test]
fn interp_tuple_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (281, 282, 283) print(t[0]) end", 0);
    assert_eq!(out, "281\n");
}
#[test]
fn interp_tuple_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (282, 283, 284) print(t[0]) end", 0);
    assert_eq!(out, "282\n");
}
#[test]
fn interp_tuple_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (283, 284, 285) print(t[0]) end", 0);
    assert_eq!(out, "283\n");
}
#[test]
fn interp_tuple_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (284, 285, 286) print(t[0]) end", 0);
    assert_eq!(out, "284\n");
}
#[test]
fn interp_tuple_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (285, 286, 287) print(t[0]) end", 0);
    assert_eq!(out, "285\n");
}
#[test]
fn interp_tuple_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (286, 287, 288) print(t[0]) end", 0);
    assert_eq!(out, "286\n");
}
#[test]
fn interp_tuple_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (287, 288, 289) print(t[0]) end", 0);
    assert_eq!(out, "287\n");
}
#[test]
fn interp_tuple_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (288, 289, 290) print(t[0]) end", 0);
    assert_eq!(out, "288\n");
}
#[test]
fn interp_tuple_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (289, 290, 291) print(t[0]) end", 0);
    assert_eq!(out, "289\n");
}
#[test]
fn interp_tuple_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (290, 291, 292) print(t[0]) end", 0);
    assert_eq!(out, "290\n");
}
#[test]
fn interp_tuple_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (291, 292, 293) print(t[0]) end", 0);
    assert_eq!(out, "291\n");
}
#[test]
fn interp_tuple_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (292, 293, 294) print(t[0]) end", 0);
    assert_eq!(out, "292\n");
}
#[test]
fn interp_tuple_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (293, 294, 295) print(t[0]) end", 0);
    assert_eq!(out, "293\n");
}
#[test]
fn interp_tuple_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (294, 295, 296) print(t[0]) end", 0);
    assert_eq!(out, "294\n");
}
#[test]
fn interp_tuple_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (295, 296, 297) print(t[0]) end", 0);
    assert_eq!(out, "295\n");
}
#[test]
fn interp_tuple_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (296, 297, 298) print(t[0]) end", 0);
    assert_eq!(out, "296\n");
}
#[test]
fn interp_tuple_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (297, 298, 299) print(t[0]) end", 0);
    assert_eq!(out, "297\n");
}
#[test]
fn interp_tuple_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (298, 299, 300) print(t[0]) end", 0);
    assert_eq!(out, "298\n");
}
#[test]
fn interp_tuple_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (299, 300, 301) print(t[0]) end", 0);
    assert_eq!(out, "299\n");
}
#[test]
fn interp_tuple_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (300, 301, 302) print(t[0]) end", 0);
    assert_eq!(out, "300\n");
}
#[test]
fn interp_tuple_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (301, 302, 303) print(t[0]) end", 0);
    assert_eq!(out, "301\n");
}
#[test]
fn interp_tuple_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (302, 303, 304) print(t[0]) end", 0);
    assert_eq!(out, "302\n");
}
#[test]
fn interp_tuple_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (303, 304, 305) print(t[0]) end", 0);
    assert_eq!(out, "303\n");
}
#[test]
fn interp_tuple_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (304, 305, 306) print(t[0]) end", 0);
    assert_eq!(out, "304\n");
}
#[test]
fn interp_tuple_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (305, 306, 307) print(t[0]) end", 0);
    assert_eq!(out, "305\n");
}
#[test]
fn interp_tuple_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (306, 307, 308) print(t[0]) end", 0);
    assert_eq!(out, "306\n");
}
#[test]
fn interp_tuple_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (307, 308, 309) print(t[0]) end", 0);
    assert_eq!(out, "307\n");
}
#[test]
fn interp_tuple_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (308, 309, 310) print(t[0]) end", 0);
    assert_eq!(out, "308\n");
}
#[test]
fn interp_tuple_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (309, 310, 311) print(t[0]) end", 0);
    assert_eq!(out, "309\n");
}
#[test]
fn interp_tuple_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (310, 311, 312) print(t[0]) end", 0);
    assert_eq!(out, "310\n");
}
#[test]
fn interp_tuple_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (311, 312, 313) print(t[0]) end", 0);
    assert_eq!(out, "311\n");
}
#[test]
fn interp_tuple_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (312, 313, 314) print(t[0]) end", 0);
    assert_eq!(out, "312\n");
}
#[test]
fn interp_tuple_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (313, 314, 315) print(t[0]) end", 0);
    assert_eq!(out, "313\n");
}
#[test]
fn interp_tuple_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (314, 315, 316) print(t[0]) end", 0);
    assert_eq!(out, "314\n");
}
#[test]
fn interp_tuple_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (315, 316, 317) print(t[0]) end", 0);
    assert_eq!(out, "315\n");
}
#[test]
fn interp_tuple_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (316, 317, 318) print(t[0]) end", 0);
    assert_eq!(out, "316\n");
}
#[test]
fn interp_tuple_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (317, 318, 319) print(t[0]) end", 0);
    assert_eq!(out, "317\n");
}
#[test]
fn interp_tuple_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (318, 319, 320) print(t[0]) end", 0);
    assert_eq!(out, "318\n");
}
#[test]
fn interp_tuple_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (319, 320, 321) print(t[0]) end", 0);
    assert_eq!(out, "319\n");
}
#[test]
fn interp_tuple_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (320, 321, 322) print(t[0]) end", 0);
    assert_eq!(out, "320\n");
}
#[test]
fn interp_tuple_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (321, 322, 323) print(t[0]) end", 0);
    assert_eq!(out, "321\n");
}
#[test]
fn interp_tuple_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (322, 323, 324) print(t[0]) end", 0);
    assert_eq!(out, "322\n");
}
#[test]
fn interp_tuple_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (323, 324, 325) print(t[0]) end", 0);
    assert_eq!(out, "323\n");
}
#[test]
fn interp_tuple_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (324, 325, 326) print(t[0]) end", 0);
    assert_eq!(out, "324\n");
}
#[test]
fn interp_tuple_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (325, 326, 327) print(t[0]) end", 0);
    assert_eq!(out, "325\n");
}
#[test]
fn interp_tuple_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (326, 327, 328) print(t[0]) end", 0);
    assert_eq!(out, "326\n");
}
#[test]
fn interp_tuple_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (327, 328, 329) print(t[0]) end", 0);
    assert_eq!(out, "327\n");
}
#[test]
fn interp_tuple_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (328, 329, 330) print(t[0]) end", 0);
    assert_eq!(out, "328\n");
}
#[test]
fn interp_tuple_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (329, 330, 331) print(t[0]) end", 0);
    assert_eq!(out, "329\n");
}
#[test]
fn interp_tuple_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (330, 331, 332) print(t[0]) end", 0);
    assert_eq!(out, "330\n");
}
#[test]
fn interp_tuple_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (331, 332, 333) print(t[0]) end", 0);
    assert_eq!(out, "331\n");
}
#[test]
fn interp_tuple_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (332, 333, 334) print(t[0]) end", 0);
    assert_eq!(out, "332\n");
}
#[test]
fn interp_tuple_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (333, 334, 335) print(t[0]) end", 0);
    assert_eq!(out, "333\n");
}
#[test]
fn interp_tuple_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (334, 335, 336) print(t[0]) end", 0);
    assert_eq!(out, "334\n");
}
#[test]
fn interp_tuple_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (335, 336, 337) print(t[0]) end", 0);
    assert_eq!(out, "335\n");
}
#[test]
fn interp_tuple_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (336, 337, 338) print(t[0]) end", 0);
    assert_eq!(out, "336\n");
}
#[test]
fn interp_tuple_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (337, 338, 339) print(t[0]) end", 0);
    assert_eq!(out, "337\n");
}
#[test]
fn interp_tuple_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (338, 339, 340) print(t[0]) end", 0);
    assert_eq!(out, "338\n");
}
#[test]
fn interp_tuple_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (339, 340, 341) print(t[0]) end", 0);
    assert_eq!(out, "339\n");
}
#[test]
fn interp_tuple_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (340, 341, 342) print(t[0]) end", 0);
    assert_eq!(out, "340\n");
}
#[test]
fn interp_tuple_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (341, 342, 343) print(t[0]) end", 0);
    assert_eq!(out, "341\n");
}
#[test]
fn interp_tuple_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (342, 343, 344) print(t[0]) end", 0);
    assert_eq!(out, "342\n");
}
#[test]
fn interp_tuple_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (343, 344, 345) print(t[0]) end", 0);
    assert_eq!(out, "343\n");
}
#[test]
fn interp_tuple_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (344, 345, 346) print(t[0]) end", 0);
    assert_eq!(out, "344\n");
}
#[test]
fn interp_tuple_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (345, 346, 347) print(t[0]) end", 0);
    assert_eq!(out, "345\n");
}
#[test]
fn interp_tuple_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (346, 347, 348) print(t[0]) end", 0);
    assert_eq!(out, "346\n");
}
#[test]
fn interp_tuple_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (347, 348, 349) print(t[0]) end", 0);
    assert_eq!(out, "347\n");
}
#[test]
fn interp_tuple_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (348, 349, 350) print(t[0]) end", 0);
    assert_eq!(out, "348\n");
}
#[test]
fn interp_tuple_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (349, 350, 351) print(t[0]) end", 0);
    assert_eq!(out, "349\n");
}
#[test]
fn interp_tuple_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (350, 351, 352) print(t[0]) end", 0);
    assert_eq!(out, "350\n");
}
#[test]
fn interp_tuple_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (351, 352, 353) print(t[0]) end", 0);
    assert_eq!(out, "351\n");
}
#[test]
fn interp_tuple_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (352, 353, 354) print(t[0]) end", 0);
    assert_eq!(out, "352\n");
}
#[test]
fn interp_tuple_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (353, 354, 355) print(t[0]) end", 0);
    assert_eq!(out, "353\n");
}
#[test]
fn interp_tuple_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (354, 355, 356) print(t[0]) end", 0);
    assert_eq!(out, "354\n");
}
#[test]
fn interp_tuple_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (355, 356, 357) print(t[0]) end", 0);
    assert_eq!(out, "355\n");
}
#[test]
fn interp_tuple_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (356, 357, 358) print(t[0]) end", 0);
    assert_eq!(out, "356\n");
}
#[test]
fn interp_tuple_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (357, 358, 359) print(t[0]) end", 0);
    assert_eq!(out, "357\n");
}
#[test]
fn interp_tuple_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (358, 359, 360) print(t[0]) end", 0);
    assert_eq!(out, "358\n");
}
#[test]
fn interp_tuple_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (359, 360, 361) print(t[0]) end", 0);
    assert_eq!(out, "359\n");
}
#[test]
fn interp_tuple_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (360, 361, 362) print(t[0]) end", 0);
    assert_eq!(out, "360\n");
}
#[test]
fn interp_tuple_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (361, 362, 363) print(t[0]) end", 0);
    assert_eq!(out, "361\n");
}
#[test]
fn interp_tuple_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (362, 363, 364) print(t[0]) end", 0);
    assert_eq!(out, "362\n");
}
#[test]
fn interp_tuple_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (363, 364, 365) print(t[0]) end", 0);
    assert_eq!(out, "363\n");
}
#[test]
fn interp_tuple_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (364, 365, 366) print(t[0]) end", 0);
    assert_eq!(out, "364\n");
}
#[test]
fn interp_tuple_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (365, 366, 367) print(t[0]) end", 0);
    assert_eq!(out, "365\n");
}
#[test]
fn interp_tuple_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (366, 367, 368) print(t[0]) end", 0);
    assert_eq!(out, "366\n");
}
#[test]
fn interp_tuple_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (367, 368, 369) print(t[0]) end", 0);
    assert_eq!(out, "367\n");
}
#[test]
fn interp_tuple_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (368, 369, 370) print(t[0]) end", 0);
    assert_eq!(out, "368\n");
}
#[test]
fn interp_tuple_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (369, 370, 371) print(t[0]) end", 0);
    assert_eq!(out, "369\n");
}
#[test]
fn interp_tuple_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (370, 371, 372) print(t[0]) end", 0);
    assert_eq!(out, "370\n");
}
#[test]
fn interp_tuple_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (371, 372, 373) print(t[0]) end", 0);
    assert_eq!(out, "371\n");
}
#[test]
fn interp_tuple_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (372, 373, 374) print(t[0]) end", 0);
    assert_eq!(out, "372\n");
}
#[test]
fn interp_tuple_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (373, 374, 375) print(t[0]) end", 0);
    assert_eq!(out, "373\n");
}
#[test]
fn interp_tuple_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (374, 375, 376) print(t[0]) end", 0);
    assert_eq!(out, "374\n");
}
#[test]
fn interp_tuple_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (375, 376, 377) print(t[0]) end", 0);
    assert_eq!(out, "375\n");
}
#[test]
fn interp_tuple_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (376, 377, 378) print(t[0]) end", 0);
    assert_eq!(out, "376\n");
}
#[test]
fn interp_tuple_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (377, 378, 379) print(t[0]) end", 0);
    assert_eq!(out, "377\n");
}
#[test]
fn interp_tuple_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (378, 379, 380) print(t[0]) end", 0);
    assert_eq!(out, "378\n");
}
#[test]
fn interp_tuple_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (379, 380, 381) print(t[0]) end", 0);
    assert_eq!(out, "379\n");
}
#[test]
fn interp_tuple_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (380, 381, 382) print(t[0]) end", 0);
    assert_eq!(out, "380\n");
}
#[test]
fn interp_tuple_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (381, 382, 383) print(t[0]) end", 0);
    assert_eq!(out, "381\n");
}
#[test]
fn interp_tuple_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (382, 383, 384) print(t[0]) end", 0);
    assert_eq!(out, "382\n");
}
#[test]
fn interp_tuple_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (383, 384, 385) print(t[0]) end", 0);
    assert_eq!(out, "383\n");
}
#[test]
fn interp_tuple_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (384, 385, 386) print(t[0]) end", 0);
    assert_eq!(out, "384\n");
}
#[test]
fn interp_tuple_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (385, 386, 387) print(t[0]) end", 0);
    assert_eq!(out, "385\n");
}
#[test]
fn interp_tuple_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (386, 387, 388) print(t[0]) end", 0);
    assert_eq!(out, "386\n");
}
#[test]
fn interp_tuple_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (387, 388, 389) print(t[0]) end", 0);
    assert_eq!(out, "387\n");
}
#[test]
fn interp_tuple_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (388, 389, 390) print(t[0]) end", 0);
    assert_eq!(out, "388\n");
}
#[test]
fn interp_tuple_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (389, 390, 391) print(t[0]) end", 0);
    assert_eq!(out, "389\n");
}
#[test]
fn interp_tuple_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (390, 391, 392) print(t[0]) end", 0);
    assert_eq!(out, "390\n");
}
#[test]
fn interp_tuple_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (391, 392, 393) print(t[0]) end", 0);
    assert_eq!(out, "391\n");
}
#[test]
fn interp_tuple_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (392, 393, 394) print(t[0]) end", 0);
    assert_eq!(out, "392\n");
}
#[test]
fn interp_tuple_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (393, 394, 395) print(t[0]) end", 0);
    assert_eq!(out, "393\n");
}
#[test]
fn interp_tuple_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (394, 395, 396) print(t[0]) end", 0);
    assert_eq!(out, "394\n");
}
#[test]
fn interp_tuple_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (395, 396, 397) print(t[0]) end", 0);
    assert_eq!(out, "395\n");
}
#[test]
fn interp_tuple_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (396, 397, 398) print(t[0]) end", 0);
    assert_eq!(out, "396\n");
}
#[test]
fn interp_tuple_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (397, 398, 399) print(t[0]) end", 0);
    assert_eq!(out, "397\n");
}
#[test]
fn interp_tuple_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (398, 399, 400) print(t[0]) end", 0);
    assert_eq!(out, "398\n");
}
#[test]
fn interp_tuple_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (399, 400, 401) print(t[0]) end", 0);
    assert_eq!(out, "399\n");
}
#[test]
fn interp_tuple_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (400, 401, 402) print(t[0]) end", 0);
    assert_eq!(out, "400\n");
}
#[test]
fn interp_tuple_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (401, 402, 403) print(t[0]) end", 0);
    assert_eq!(out, "401\n");
}
#[test]
fn interp_tuple_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (402, 403, 404) print(t[0]) end", 0);
    assert_eq!(out, "402\n");
}
#[test]
fn interp_tuple_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (403, 404, 405) print(t[0]) end", 0);
    assert_eq!(out, "403\n");
}
#[test]
fn interp_tuple_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (404, 405, 406) print(t[0]) end", 0);
    assert_eq!(out, "404\n");
}
#[test]
fn interp_tuple_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (405, 406, 407) print(t[0]) end", 0);
    assert_eq!(out, "405\n");
}
#[test]
fn interp_tuple_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (406, 407, 408) print(t[0]) end", 0);
    assert_eq!(out, "406\n");
}
#[test]
fn interp_tuple_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (407, 408, 409) print(t[0]) end", 0);
    assert_eq!(out, "407\n");
}
#[test]
fn interp_tuple_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (408, 409, 410) print(t[0]) end", 0);
    assert_eq!(out, "408\n");
}
#[test]
fn interp_tuple_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (409, 410, 411) print(t[0]) end", 0);
    assert_eq!(out, "409\n");
}
#[test]
fn interp_tuple_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (410, 411, 412) print(t[0]) end", 0);
    assert_eq!(out, "410\n");
}
#[test]
fn interp_tuple_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (411, 412, 413) print(t[0]) end", 0);
    assert_eq!(out, "411\n");
}
#[test]
fn interp_tuple_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (412, 413, 414) print(t[0]) end", 0);
    assert_eq!(out, "412\n");
}
#[test]
fn interp_tuple_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (413, 414, 415) print(t[0]) end", 0);
    assert_eq!(out, "413\n");
}
#[test]
fn interp_tuple_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (414, 415, 416) print(t[0]) end", 0);
    assert_eq!(out, "414\n");
}
#[test]
fn interp_tuple_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (415, 416, 417) print(t[0]) end", 0);
    assert_eq!(out, "415\n");
}
#[test]
fn interp_tuple_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (416, 417, 418) print(t[0]) end", 0);
    assert_eq!(out, "416\n");
}
#[test]
fn interp_tuple_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (417, 418, 419) print(t[0]) end", 0);
    assert_eq!(out, "417\n");
}
#[test]
fn interp_tuple_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (418, 419, 420) print(t[0]) end", 0);
    assert_eq!(out, "418\n");
}
#[test]
fn interp_tuple_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (419, 420, 421) print(t[0]) end", 0);
    assert_eq!(out, "419\n");
}
#[test]
fn interp_tuple_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (420, 421, 422) print(t[0]) end", 0);
    assert_eq!(out, "420\n");
}
#[test]
fn interp_tuple_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (421, 422, 423) print(t[0]) end", 0);
    assert_eq!(out, "421\n");
}
#[test]
fn interp_tuple_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (422, 423, 424) print(t[0]) end", 0);
    assert_eq!(out, "422\n");
}
#[test]
fn interp_tuple_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (423, 424, 425) print(t[0]) end", 0);
    assert_eq!(out, "423\n");
}
#[test]
fn interp_tuple_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (424, 425, 426) print(t[0]) end", 0);
    assert_eq!(out, "424\n");
}
#[test]
fn interp_tuple_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (425, 426, 427) print(t[0]) end", 0);
    assert_eq!(out, "425\n");
}
#[test]
fn interp_tuple_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (426, 427, 428) print(t[0]) end", 0);
    assert_eq!(out, "426\n");
}
#[test]
fn interp_tuple_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (427, 428, 429) print(t[0]) end", 0);
    assert_eq!(out, "427\n");
}
#[test]
fn interp_tuple_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (428, 429, 430) print(t[0]) end", 0);
    assert_eq!(out, "428\n");
}
#[test]
fn interp_tuple_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (429, 430, 431) print(t[0]) end", 0);
    assert_eq!(out, "429\n");
}
#[test]
fn interp_tuple_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (430, 431, 432) print(t[0]) end", 0);
    assert_eq!(out, "430\n");
}
#[test]
fn interp_tuple_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (431, 432, 433) print(t[0]) end", 0);
    assert_eq!(out, "431\n");
}
#[test]
fn interp_tuple_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (432, 433, 434) print(t[0]) end", 0);
    assert_eq!(out, "432\n");
}
#[test]
fn interp_tuple_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (433, 434, 435) print(t[0]) end", 0);
    assert_eq!(out, "433\n");
}
#[test]
fn interp_tuple_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (434, 435, 436) print(t[0]) end", 0);
    assert_eq!(out, "434\n");
}
#[test]
fn interp_tuple_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (435, 436, 437) print(t[0]) end", 0);
    assert_eq!(out, "435\n");
}
#[test]
fn interp_tuple_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (436, 437, 438) print(t[0]) end", 0);
    assert_eq!(out, "436\n");
}
#[test]
fn interp_tuple_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (437, 438, 439) print(t[0]) end", 0);
    assert_eq!(out, "437\n");
}
#[test]
fn interp_tuple_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (438, 439, 440) print(t[0]) end", 0);
    assert_eq!(out, "438\n");
}
#[test]
fn interp_tuple_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (439, 440, 441) print(t[0]) end", 0);
    assert_eq!(out, "439\n");
}
#[test]
fn interp_tuple_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (440, 441, 442) print(t[0]) end", 0);
    assert_eq!(out, "440\n");
}
#[test]
fn interp_tuple_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (441, 442, 443) print(t[0]) end", 0);
    assert_eq!(out, "441\n");
}
#[test]
fn interp_tuple_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (442, 443, 444) print(t[0]) end", 0);
    assert_eq!(out, "442\n");
}
#[test]
fn interp_tuple_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (443, 444, 445) print(t[0]) end", 0);
    assert_eq!(out, "443\n");
}
#[test]
fn interp_tuple_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (444, 445, 446) print(t[0]) end", 0);
    assert_eq!(out, "444\n");
}
#[test]
fn interp_tuple_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (445, 446, 447) print(t[0]) end", 0);
    assert_eq!(out, "445\n");
}
#[test]
fn interp_tuple_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (446, 447, 448) print(t[0]) end", 0);
    assert_eq!(out, "446\n");
}
#[test]
fn interp_tuple_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (447, 448, 449) print(t[0]) end", 0);
    assert_eq!(out, "447\n");
}
#[test]
fn interp_tuple_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (448, 449, 450) print(t[0]) end", 0);
    assert_eq!(out, "448\n");
}
#[test]
fn interp_tuple_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (449, 450, 451) print(t[0]) end", 0);
    assert_eq!(out, "449\n");
}
#[test]
fn interp_tuple_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (450, 451, 452) print(t[0]) end", 0);
    assert_eq!(out, "450\n");
}
#[test]
fn interp_tuple_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (451, 452, 453) print(t[0]) end", 0);
    assert_eq!(out, "451\n");
}
#[test]
fn interp_tuple_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (452, 453, 454) print(t[0]) end", 0);
    assert_eq!(out, "452\n");
}
#[test]
fn interp_tuple_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (453, 454, 455) print(t[0]) end", 0);
    assert_eq!(out, "453\n");
}
#[test]
fn interp_tuple_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (454, 455, 456) print(t[0]) end", 0);
    assert_eq!(out, "454\n");
}
#[test]
fn interp_tuple_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (455, 456, 457) print(t[0]) end", 0);
    assert_eq!(out, "455\n");
}
#[test]
fn interp_tuple_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (456, 457, 458) print(t[0]) end", 0);
    assert_eq!(out, "456\n");
}
#[test]
fn interp_tuple_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (457, 458, 459) print(t[0]) end", 0);
    assert_eq!(out, "457\n");
}
#[test]
fn interp_tuple_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (458, 459, 460) print(t[0]) end", 0);
    assert_eq!(out, "458\n");
}
#[test]
fn interp_tuple_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (459, 460, 461) print(t[0]) end", 0);
    assert_eq!(out, "459\n");
}
#[test]
fn interp_tuple_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (460, 461, 462) print(t[0]) end", 0);
    assert_eq!(out, "460\n");
}
#[test]
fn interp_tuple_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (461, 462, 463) print(t[0]) end", 0);
    assert_eq!(out, "461\n");
}
#[test]
fn interp_tuple_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (462, 463, 464) print(t[0]) end", 0);
    assert_eq!(out, "462\n");
}
#[test]
fn interp_tuple_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (463, 464, 465) print(t[0]) end", 0);
    assert_eq!(out, "463\n");
}
#[test]
fn interp_tuple_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (464, 465, 466) print(t[0]) end", 0);
    assert_eq!(out, "464\n");
}
#[test]
fn interp_tuple_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (465, 466, 467) print(t[0]) end", 0);
    assert_eq!(out, "465\n");
}
#[test]
fn interp_tuple_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (466, 467, 468) print(t[0]) end", 0);
    assert_eq!(out, "466\n");
}
#[test]
fn interp_tuple_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (467, 468, 469) print(t[0]) end", 0);
    assert_eq!(out, "467\n");
}
#[test]
fn interp_tuple_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (468, 469, 470) print(t[0]) end", 0);
    assert_eq!(out, "468\n");
}
#[test]
fn interp_tuple_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (469, 470, 471) print(t[0]) end", 0);
    assert_eq!(out, "469\n");
}
#[test]
fn interp_tuple_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (470, 471, 472) print(t[0]) end", 0);
    assert_eq!(out, "470\n");
}
#[test]
fn interp_tuple_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (471, 472, 473) print(t[0]) end", 0);
    assert_eq!(out, "471\n");
}
#[test]
fn interp_tuple_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (472, 473, 474) print(t[0]) end", 0);
    assert_eq!(out, "472\n");
}
#[test]
fn interp_tuple_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (473, 474, 475) print(t[0]) end", 0);
    assert_eq!(out, "473\n");
}
#[test]
fn interp_tuple_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (474, 475, 476) print(t[0]) end", 0);
    assert_eq!(out, "474\n");
}
#[test]
fn interp_tuple_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (475, 476, 477) print(t[0]) end", 0);
    assert_eq!(out, "475\n");
}
#[test]
fn interp_tuple_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (476, 477, 478) print(t[0]) end", 0);
    assert_eq!(out, "476\n");
}
#[test]
fn interp_tuple_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (477, 478, 479) print(t[0]) end", 0);
    assert_eq!(out, "477\n");
}
#[test]
fn interp_tuple_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (478, 479, 480) print(t[0]) end", 0);
    assert_eq!(out, "478\n");
}
#[test]
fn interp_tuple_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (479, 480, 481) print(t[0]) end", 0);
    assert_eq!(out, "479\n");
}
#[test]
fn interp_tuple_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (480, 481, 482) print(t[0]) end", 0);
    assert_eq!(out, "480\n");
}
#[test]
fn interp_tuple_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (481, 482, 483) print(t[0]) end", 0);
    assert_eq!(out, "481\n");
}
#[test]
fn interp_tuple_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (482, 483, 484) print(t[0]) end", 0);
    assert_eq!(out, "482\n");
}
#[test]
fn interp_tuple_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (483, 484, 485) print(t[0]) end", 0);
    assert_eq!(out, "483\n");
}
#[test]
fn interp_tuple_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (484, 485, 486) print(t[0]) end", 0);
    assert_eq!(out, "484\n");
}
#[test]
fn interp_tuple_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (485, 486, 487) print(t[0]) end", 0);
    assert_eq!(out, "485\n");
}
#[test]
fn interp_tuple_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (486, 487, 488) print(t[0]) end", 0);
    assert_eq!(out, "486\n");
}
#[test]
fn interp_tuple_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (487, 488, 489) print(t[0]) end", 0);
    assert_eq!(out, "487\n");
}
#[test]
fn interp_tuple_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (488, 489, 490) print(t[0]) end", 0);
    assert_eq!(out, "488\n");
}
#[test]
fn interp_tuple_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (489, 490, 491) print(t[0]) end", 0);
    assert_eq!(out, "489\n");
}
#[test]
fn interp_tuple_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (490, 491, 492) print(t[0]) end", 0);
    assert_eq!(out, "490\n");
}
#[test]
fn interp_tuple_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (491, 492, 493) print(t[0]) end", 0);
    assert_eq!(out, "491\n");
}
#[test]
fn interp_tuple_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (492, 493, 494) print(t[0]) end", 0);
    assert_eq!(out, "492\n");
}
#[test]
fn interp_tuple_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (493, 494, 495) print(t[0]) end", 0);
    assert_eq!(out, "493\n");
}
#[test]
fn interp_tuple_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (494, 495, 496) print(t[0]) end", 0);
    assert_eq!(out, "494\n");
}
#[test]
fn interp_tuple_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (495, 496, 497) print(t[0]) end", 0);
    assert_eq!(out, "495\n");
}
#[test]
fn interp_tuple_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (496, 497, 498) print(t[0]) end", 0);
    assert_eq!(out, "496\n");
}
#[test]
fn interp_tuple_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (497, 498, 499) print(t[0]) end", 0);
    assert_eq!(out, "497\n");
}
#[test]
fn interp_tuple_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (498, 499, 500) print(t[0]) end", 0);
    assert_eq!(out, "498\n");
}
#[test]
fn interp_tuple_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let t = (499, 500, 501) print(t[0]) end", 0);
    assert_eq!(out, "499\n");
}
#[test]
fn interp_array_index_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [0, 1, 2] print(xs[2]) end", 0);
    assert_eq!(out, "2\n");
}
#[test]
fn interp_array_index_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [1, 2, 3] print(xs[2]) end", 0);
    assert_eq!(out, "3\n");
}
#[test]
fn interp_array_index_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [2, 3, 4] print(xs[2]) end", 0);
    assert_eq!(out, "4\n");
}
#[test]
fn interp_array_index_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [3, 4, 5] print(xs[2]) end", 0);
    assert_eq!(out, "5\n");
}
#[test]
fn interp_array_index_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [4, 5, 6] print(xs[2]) end", 0);
    assert_eq!(out, "6\n");
}
#[test]
fn interp_array_index_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [5, 6, 7] print(xs[2]) end", 0);
    assert_eq!(out, "7\n");
}
#[test]
fn interp_array_index_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [6, 7, 8] print(xs[2]) end", 0);
    assert_eq!(out, "8\n");
}
#[test]
fn interp_array_index_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [7, 8, 9] print(xs[2]) end", 0);
    assert_eq!(out, "9\n");
}
#[test]
fn interp_array_index_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [8, 9, 10] print(xs[2]) end", 0);
    assert_eq!(out, "10\n");
}
#[test]
fn interp_array_index_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [9, 10, 11] print(xs[2]) end", 0);
    assert_eq!(out, "11\n");
}
#[test]
fn interp_array_index_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [10, 11, 12] print(xs[2]) end", 0);
    assert_eq!(out, "12\n");
}
#[test]
fn interp_array_index_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [11, 12, 13] print(xs[2]) end", 0);
    assert_eq!(out, "13\n");
}
#[test]
fn interp_array_index_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [12, 13, 14] print(xs[2]) end", 0);
    assert_eq!(out, "14\n");
}
#[test]
fn interp_array_index_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [13, 14, 15] print(xs[2]) end", 0);
    assert_eq!(out, "15\n");
}
#[test]
fn interp_array_index_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [14, 15, 16] print(xs[2]) end", 0);
    assert_eq!(out, "16\n");
}
#[test]
fn interp_array_index_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [15, 16, 17] print(xs[2]) end", 0);
    assert_eq!(out, "17\n");
}
#[test]
fn interp_array_index_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [16, 17, 18] print(xs[2]) end", 0);
    assert_eq!(out, "18\n");
}
#[test]
fn interp_array_index_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [17, 18, 19] print(xs[2]) end", 0);
    assert_eq!(out, "19\n");
}
#[test]
fn interp_array_index_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [18, 19, 20] print(xs[2]) end", 0);
    assert_eq!(out, "20\n");
}
#[test]
fn interp_array_index_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [19, 20, 21] print(xs[2]) end", 0);
    assert_eq!(out, "21\n");
}
#[test]
fn interp_array_index_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [20, 21, 22] print(xs[2]) end", 0);
    assert_eq!(out, "22\n");
}
#[test]
fn interp_array_index_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [21, 22, 23] print(xs[2]) end", 0);
    assert_eq!(out, "23\n");
}
#[test]
fn interp_array_index_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [22, 23, 24] print(xs[2]) end", 0);
    assert_eq!(out, "24\n");
}
#[test]
fn interp_array_index_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [23, 24, 25] print(xs[2]) end", 0);
    assert_eq!(out, "25\n");
}
#[test]
fn interp_array_index_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [24, 25, 26] print(xs[2]) end", 0);
    assert_eq!(out, "26\n");
}
#[test]
fn interp_array_index_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [25, 26, 27] print(xs[2]) end", 0);
    assert_eq!(out, "27\n");
}
#[test]
fn interp_array_index_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [26, 27, 28] print(xs[2]) end", 0);
    assert_eq!(out, "28\n");
}
#[test]
fn interp_array_index_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [27, 28, 29] print(xs[2]) end", 0);
    assert_eq!(out, "29\n");
}
#[test]
fn interp_array_index_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [28, 29, 30] print(xs[2]) end", 0);
    assert_eq!(out, "30\n");
}
#[test]
fn interp_array_index_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [29, 30, 31] print(xs[2]) end", 0);
    assert_eq!(out, "31\n");
}
#[test]
fn interp_array_index_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [30, 31, 32] print(xs[2]) end", 0);
    assert_eq!(out, "32\n");
}
#[test]
fn interp_array_index_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [31, 32, 33] print(xs[2]) end", 0);
    assert_eq!(out, "33\n");
}
#[test]
fn interp_array_index_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [32, 33, 34] print(xs[2]) end", 0);
    assert_eq!(out, "34\n");
}
#[test]
fn interp_array_index_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [33, 34, 35] print(xs[2]) end", 0);
    assert_eq!(out, "35\n");
}
#[test]
fn interp_array_index_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [34, 35, 36] print(xs[2]) end", 0);
    assert_eq!(out, "36\n");
}
#[test]
fn interp_array_index_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [35, 36, 37] print(xs[2]) end", 0);
    assert_eq!(out, "37\n");
}
#[test]
fn interp_array_index_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [36, 37, 38] print(xs[2]) end", 0);
    assert_eq!(out, "38\n");
}
#[test]
fn interp_array_index_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [37, 38, 39] print(xs[2]) end", 0);
    assert_eq!(out, "39\n");
}
#[test]
fn interp_array_index_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [38, 39, 40] print(xs[2]) end", 0);
    assert_eq!(out, "40\n");
}
#[test]
fn interp_array_index_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [39, 40, 41] print(xs[2]) end", 0);
    assert_eq!(out, "41\n");
}
#[test]
fn interp_array_index_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [40, 41, 42] print(xs[2]) end", 0);
    assert_eq!(out, "42\n");
}
#[test]
fn interp_array_index_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [41, 42, 43] print(xs[2]) end", 0);
    assert_eq!(out, "43\n");
}
#[test]
fn interp_array_index_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [42, 43, 44] print(xs[2]) end", 0);
    assert_eq!(out, "44\n");
}
#[test]
fn interp_array_index_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [43, 44, 45] print(xs[2]) end", 0);
    assert_eq!(out, "45\n");
}
#[test]
fn interp_array_index_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [44, 45, 46] print(xs[2]) end", 0);
    assert_eq!(out, "46\n");
}
#[test]
fn interp_array_index_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [45, 46, 47] print(xs[2]) end", 0);
    assert_eq!(out, "47\n");
}
#[test]
fn interp_array_index_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [46, 47, 48] print(xs[2]) end", 0);
    assert_eq!(out, "48\n");
}
#[test]
fn interp_array_index_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [47, 48, 49] print(xs[2]) end", 0);
    assert_eq!(out, "49\n");
}
#[test]
fn interp_array_index_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [48, 49, 50] print(xs[2]) end", 0);
    assert_eq!(out, "50\n");
}
#[test]
fn interp_array_index_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [49, 50, 51] print(xs[2]) end", 0);
    assert_eq!(out, "51\n");
}
#[test]
fn interp_array_index_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [50, 51, 52] print(xs[2]) end", 0);
    assert_eq!(out, "52\n");
}
#[test]
fn interp_array_index_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [51, 52, 53] print(xs[2]) end", 0);
    assert_eq!(out, "53\n");
}
#[test]
fn interp_array_index_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [52, 53, 54] print(xs[2]) end", 0);
    assert_eq!(out, "54\n");
}
#[test]
fn interp_array_index_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [53, 54, 55] print(xs[2]) end", 0);
    assert_eq!(out, "55\n");
}
#[test]
fn interp_array_index_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [54, 55, 56] print(xs[2]) end", 0);
    assert_eq!(out, "56\n");
}
#[test]
fn interp_array_index_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [55, 56, 57] print(xs[2]) end", 0);
    assert_eq!(out, "57\n");
}
#[test]
fn interp_array_index_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [56, 57, 58] print(xs[2]) end", 0);
    assert_eq!(out, "58\n");
}
#[test]
fn interp_array_index_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [57, 58, 59] print(xs[2]) end", 0);
    assert_eq!(out, "59\n");
}
#[test]
fn interp_array_index_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [58, 59, 60] print(xs[2]) end", 0);
    assert_eq!(out, "60\n");
}
#[test]
fn interp_array_index_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [59, 60, 61] print(xs[2]) end", 0);
    assert_eq!(out, "61\n");
}
#[test]
fn interp_array_index_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [60, 61, 62] print(xs[2]) end", 0);
    assert_eq!(out, "62\n");
}
#[test]
fn interp_array_index_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [61, 62, 63] print(xs[2]) end", 0);
    assert_eq!(out, "63\n");
}
#[test]
fn interp_array_index_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [62, 63, 64] print(xs[2]) end", 0);
    assert_eq!(out, "64\n");
}
#[test]
fn interp_array_index_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [63, 64, 65] print(xs[2]) end", 0);
    assert_eq!(out, "65\n");
}
#[test]
fn interp_array_index_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [64, 65, 66] print(xs[2]) end", 0);
    assert_eq!(out, "66\n");
}
#[test]
fn interp_array_index_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [65, 66, 67] print(xs[2]) end", 0);
    assert_eq!(out, "67\n");
}
#[test]
fn interp_array_index_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [66, 67, 68] print(xs[2]) end", 0);
    assert_eq!(out, "68\n");
}
#[test]
fn interp_array_index_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [67, 68, 69] print(xs[2]) end", 0);
    assert_eq!(out, "69\n");
}
#[test]
fn interp_array_index_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [68, 69, 70] print(xs[2]) end", 0);
    assert_eq!(out, "70\n");
}
#[test]
fn interp_array_index_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [69, 70, 71] print(xs[2]) end", 0);
    assert_eq!(out, "71\n");
}
#[test]
fn interp_array_index_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [70, 71, 72] print(xs[2]) end", 0);
    assert_eq!(out, "72\n");
}
#[test]
fn interp_array_index_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [71, 72, 73] print(xs[2]) end", 0);
    assert_eq!(out, "73\n");
}
#[test]
fn interp_array_index_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [72, 73, 74] print(xs[2]) end", 0);
    assert_eq!(out, "74\n");
}
#[test]
fn interp_array_index_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [73, 74, 75] print(xs[2]) end", 0);
    assert_eq!(out, "75\n");
}
#[test]
fn interp_array_index_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [74, 75, 76] print(xs[2]) end", 0);
    assert_eq!(out, "76\n");
}
#[test]
fn interp_array_index_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [75, 76, 77] print(xs[2]) end", 0);
    assert_eq!(out, "77\n");
}
#[test]
fn interp_array_index_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [76, 77, 78] print(xs[2]) end", 0);
    assert_eq!(out, "78\n");
}
#[test]
fn interp_array_index_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [77, 78, 79] print(xs[2]) end", 0);
    assert_eq!(out, "79\n");
}
#[test]
fn interp_array_index_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [78, 79, 80] print(xs[2]) end", 0);
    assert_eq!(out, "80\n");
}
#[test]
fn interp_array_index_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [79, 80, 81] print(xs[2]) end", 0);
    assert_eq!(out, "81\n");
}
#[test]
fn interp_array_index_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [80, 81, 82] print(xs[2]) end", 0);
    assert_eq!(out, "82\n");
}
#[test]
fn interp_array_index_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [81, 82, 83] print(xs[2]) end", 0);
    assert_eq!(out, "83\n");
}
#[test]
fn interp_array_index_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [82, 83, 84] print(xs[2]) end", 0);
    assert_eq!(out, "84\n");
}
#[test]
fn interp_array_index_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [83, 84, 85] print(xs[2]) end", 0);
    assert_eq!(out, "85\n");
}
#[test]
fn interp_array_index_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [84, 85, 86] print(xs[2]) end", 0);
    assert_eq!(out, "86\n");
}
#[test]
fn interp_array_index_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [85, 86, 87] print(xs[2]) end", 0);
    assert_eq!(out, "87\n");
}
#[test]
fn interp_array_index_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [86, 87, 88] print(xs[2]) end", 0);
    assert_eq!(out, "88\n");
}
#[test]
fn interp_array_index_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [87, 88, 89] print(xs[2]) end", 0);
    assert_eq!(out, "89\n");
}
#[test]
fn interp_array_index_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [88, 89, 90] print(xs[2]) end", 0);
    assert_eq!(out, "90\n");
}
#[test]
fn interp_array_index_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [89, 90, 91] print(xs[2]) end", 0);
    assert_eq!(out, "91\n");
}
#[test]
fn interp_array_index_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [90, 91, 92] print(xs[2]) end", 0);
    assert_eq!(out, "92\n");
}
#[test]
fn interp_array_index_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [91, 92, 93] print(xs[2]) end", 0);
    assert_eq!(out, "93\n");
}
#[test]
fn interp_array_index_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [92, 93, 94] print(xs[2]) end", 0);
    assert_eq!(out, "94\n");
}
#[test]
fn interp_array_index_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [93, 94, 95] print(xs[2]) end", 0);
    assert_eq!(out, "95\n");
}
#[test]
fn interp_array_index_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [94, 95, 96] print(xs[2]) end", 0);
    assert_eq!(out, "96\n");
}
#[test]
fn interp_array_index_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [95, 96, 97] print(xs[2]) end", 0);
    assert_eq!(out, "97\n");
}
#[test]
fn interp_array_index_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [96, 97, 98] print(xs[2]) end", 0);
    assert_eq!(out, "98\n");
}
#[test]
fn interp_array_index_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [97, 98, 99] print(xs[2]) end", 0);
    assert_eq!(out, "99\n");
}
#[test]
fn interp_array_index_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [98, 99, 100] print(xs[2]) end", 0);
    assert_eq!(out, "100\n");
}
#[test]
fn interp_array_index_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [99, 100, 101] print(xs[2]) end", 0);
    assert_eq!(out, "101\n");
}
#[test]
fn interp_array_index_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [100, 101, 102] print(xs[2]) end", 0);
    assert_eq!(out, "102\n");
}
#[test]
fn interp_array_index_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [101, 102, 103] print(xs[2]) end", 0);
    assert_eq!(out, "103\n");
}
#[test]
fn interp_array_index_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [102, 103, 104] print(xs[2]) end", 0);
    assert_eq!(out, "104\n");
}
#[test]
fn interp_array_index_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [103, 104, 105] print(xs[2]) end", 0);
    assert_eq!(out, "105\n");
}
#[test]
fn interp_array_index_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [104, 105, 106] print(xs[2]) end", 0);
    assert_eq!(out, "106\n");
}
#[test]
fn interp_array_index_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [105, 106, 107] print(xs[2]) end", 0);
    assert_eq!(out, "107\n");
}
#[test]
fn interp_array_index_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [106, 107, 108] print(xs[2]) end", 0);
    assert_eq!(out, "108\n");
}
#[test]
fn interp_array_index_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [107, 108, 109] print(xs[2]) end", 0);
    assert_eq!(out, "109\n");
}
#[test]
fn interp_array_index_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [108, 109, 110] print(xs[2]) end", 0);
    assert_eq!(out, "110\n");
}
#[test]
fn interp_array_index_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [109, 110, 111] print(xs[2]) end", 0);
    assert_eq!(out, "111\n");
}
#[test]
fn interp_array_index_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [110, 111, 112] print(xs[2]) end", 0);
    assert_eq!(out, "112\n");
}
#[test]
fn interp_array_index_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [111, 112, 113] print(xs[2]) end", 0);
    assert_eq!(out, "113\n");
}
#[test]
fn interp_array_index_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [112, 113, 114] print(xs[2]) end", 0);
    assert_eq!(out, "114\n");
}
#[test]
fn interp_array_index_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [113, 114, 115] print(xs[2]) end", 0);
    assert_eq!(out, "115\n");
}
#[test]
fn interp_array_index_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [114, 115, 116] print(xs[2]) end", 0);
    assert_eq!(out, "116\n");
}
#[test]
fn interp_array_index_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [115, 116, 117] print(xs[2]) end", 0);
    assert_eq!(out, "117\n");
}
#[test]
fn interp_array_index_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [116, 117, 118] print(xs[2]) end", 0);
    assert_eq!(out, "118\n");
}
#[test]
fn interp_array_index_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [117, 118, 119] print(xs[2]) end", 0);
    assert_eq!(out, "119\n");
}
#[test]
fn interp_array_index_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [118, 119, 120] print(xs[2]) end", 0);
    assert_eq!(out, "120\n");
}
#[test]
fn interp_array_index_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [119, 120, 121] print(xs[2]) end", 0);
    assert_eq!(out, "121\n");
}
#[test]
fn interp_array_index_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [120, 121, 122] print(xs[2]) end", 0);
    assert_eq!(out, "122\n");
}
#[test]
fn interp_array_index_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [121, 122, 123] print(xs[2]) end", 0);
    assert_eq!(out, "123\n");
}
#[test]
fn interp_array_index_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [122, 123, 124] print(xs[2]) end", 0);
    assert_eq!(out, "124\n");
}
#[test]
fn interp_array_index_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [123, 124, 125] print(xs[2]) end", 0);
    assert_eq!(out, "125\n");
}
#[test]
fn interp_array_index_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [124, 125, 126] print(xs[2]) end", 0);
    assert_eq!(out, "126\n");
}
#[test]
fn interp_array_index_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [125, 126, 127] print(xs[2]) end", 0);
    assert_eq!(out, "127\n");
}
#[test]
fn interp_array_index_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [126, 127, 128] print(xs[2]) end", 0);
    assert_eq!(out, "128\n");
}
#[test]
fn interp_array_index_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [127, 128, 129] print(xs[2]) end", 0);
    assert_eq!(out, "129\n");
}
#[test]
fn interp_array_index_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [128, 129, 130] print(xs[2]) end", 0);
    assert_eq!(out, "130\n");
}
#[test]
fn interp_array_index_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [129, 130, 131] print(xs[2]) end", 0);
    assert_eq!(out, "131\n");
}
#[test]
fn interp_array_index_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [130, 131, 132] print(xs[2]) end", 0);
    assert_eq!(out, "132\n");
}
#[test]
fn interp_array_index_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [131, 132, 133] print(xs[2]) end", 0);
    assert_eq!(out, "133\n");
}
#[test]
fn interp_array_index_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [132, 133, 134] print(xs[2]) end", 0);
    assert_eq!(out, "134\n");
}
#[test]
fn interp_array_index_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [133, 134, 135] print(xs[2]) end", 0);
    assert_eq!(out, "135\n");
}
#[test]
fn interp_array_index_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [134, 135, 136] print(xs[2]) end", 0);
    assert_eq!(out, "136\n");
}
#[test]
fn interp_array_index_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [135, 136, 137] print(xs[2]) end", 0);
    assert_eq!(out, "137\n");
}
#[test]
fn interp_array_index_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [136, 137, 138] print(xs[2]) end", 0);
    assert_eq!(out, "138\n");
}
#[test]
fn interp_array_index_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [137, 138, 139] print(xs[2]) end", 0);
    assert_eq!(out, "139\n");
}
#[test]
fn interp_array_index_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [138, 139, 140] print(xs[2]) end", 0);
    assert_eq!(out, "140\n");
}
#[test]
fn interp_array_index_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [139, 140, 141] print(xs[2]) end", 0);
    assert_eq!(out, "141\n");
}
#[test]
fn interp_array_index_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [140, 141, 142] print(xs[2]) end", 0);
    assert_eq!(out, "142\n");
}
#[test]
fn interp_array_index_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [141, 142, 143] print(xs[2]) end", 0);
    assert_eq!(out, "143\n");
}
#[test]
fn interp_array_index_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [142, 143, 144] print(xs[2]) end", 0);
    assert_eq!(out, "144\n");
}
#[test]
fn interp_array_index_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [143, 144, 145] print(xs[2]) end", 0);
    assert_eq!(out, "145\n");
}
#[test]
fn interp_array_index_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [144, 145, 146] print(xs[2]) end", 0);
    assert_eq!(out, "146\n");
}
#[test]
fn interp_array_index_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [145, 146, 147] print(xs[2]) end", 0);
    assert_eq!(out, "147\n");
}
#[test]
fn interp_array_index_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [146, 147, 148] print(xs[2]) end", 0);
    assert_eq!(out, "148\n");
}
#[test]
fn interp_array_index_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [147, 148, 149] print(xs[2]) end", 0);
    assert_eq!(out, "149\n");
}
#[test]
fn interp_array_index_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [148, 149, 150] print(xs[2]) end", 0);
    assert_eq!(out, "150\n");
}
#[test]
fn interp_array_index_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [149, 150, 151] print(xs[2]) end", 0);
    assert_eq!(out, "151\n");
}
#[test]
fn interp_array_index_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [150, 151, 152] print(xs[2]) end", 0);
    assert_eq!(out, "152\n");
}
#[test]
fn interp_array_index_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [151, 152, 153] print(xs[2]) end", 0);
    assert_eq!(out, "153\n");
}
#[test]
fn interp_array_index_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [152, 153, 154] print(xs[2]) end", 0);
    assert_eq!(out, "154\n");
}
#[test]
fn interp_array_index_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [153, 154, 155] print(xs[2]) end", 0);
    assert_eq!(out, "155\n");
}
#[test]
fn interp_array_index_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [154, 155, 156] print(xs[2]) end", 0);
    assert_eq!(out, "156\n");
}
#[test]
fn interp_array_index_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [155, 156, 157] print(xs[2]) end", 0);
    assert_eq!(out, "157\n");
}
#[test]
fn interp_array_index_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [156, 157, 158] print(xs[2]) end", 0);
    assert_eq!(out, "158\n");
}
#[test]
fn interp_array_index_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [157, 158, 159] print(xs[2]) end", 0);
    assert_eq!(out, "159\n");
}
#[test]
fn interp_array_index_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [158, 159, 160] print(xs[2]) end", 0);
    assert_eq!(out, "160\n");
}
#[test]
fn interp_array_index_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [159, 160, 161] print(xs[2]) end", 0);
    assert_eq!(out, "161\n");
}
#[test]
fn interp_array_index_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [160, 161, 162] print(xs[2]) end", 0);
    assert_eq!(out, "162\n");
}
#[test]
fn interp_array_index_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [161, 162, 163] print(xs[2]) end", 0);
    assert_eq!(out, "163\n");
}
#[test]
fn interp_array_index_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [162, 163, 164] print(xs[2]) end", 0);
    assert_eq!(out, "164\n");
}
#[test]
fn interp_array_index_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [163, 164, 165] print(xs[2]) end", 0);
    assert_eq!(out, "165\n");
}
#[test]
fn interp_array_index_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [164, 165, 166] print(xs[2]) end", 0);
    assert_eq!(out, "166\n");
}
#[test]
fn interp_array_index_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [165, 166, 167] print(xs[2]) end", 0);
    assert_eq!(out, "167\n");
}
#[test]
fn interp_array_index_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [166, 167, 168] print(xs[2]) end", 0);
    assert_eq!(out, "168\n");
}
#[test]
fn interp_array_index_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [167, 168, 169] print(xs[2]) end", 0);
    assert_eq!(out, "169\n");
}
#[test]
fn interp_array_index_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [168, 169, 170] print(xs[2]) end", 0);
    assert_eq!(out, "170\n");
}
#[test]
fn interp_array_index_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [169, 170, 171] print(xs[2]) end", 0);
    assert_eq!(out, "171\n");
}
#[test]
fn interp_array_index_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [170, 171, 172] print(xs[2]) end", 0);
    assert_eq!(out, "172\n");
}
#[test]
fn interp_array_index_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [171, 172, 173] print(xs[2]) end", 0);
    assert_eq!(out, "173\n");
}
#[test]
fn interp_array_index_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [172, 173, 174] print(xs[2]) end", 0);
    assert_eq!(out, "174\n");
}
#[test]
fn interp_array_index_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [173, 174, 175] print(xs[2]) end", 0);
    assert_eq!(out, "175\n");
}
#[test]
fn interp_array_index_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [174, 175, 176] print(xs[2]) end", 0);
    assert_eq!(out, "176\n");
}
#[test]
fn interp_array_index_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [175, 176, 177] print(xs[2]) end", 0);
    assert_eq!(out, "177\n");
}
#[test]
fn interp_array_index_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [176, 177, 178] print(xs[2]) end", 0);
    assert_eq!(out, "178\n");
}
#[test]
fn interp_array_index_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [177, 178, 179] print(xs[2]) end", 0);
    assert_eq!(out, "179\n");
}
#[test]
fn interp_array_index_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [178, 179, 180] print(xs[2]) end", 0);
    assert_eq!(out, "180\n");
}
#[test]
fn interp_array_index_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [179, 180, 181] print(xs[2]) end", 0);
    assert_eq!(out, "181\n");
}
#[test]
fn interp_array_index_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [180, 181, 182] print(xs[2]) end", 0);
    assert_eq!(out, "182\n");
}
#[test]
fn interp_array_index_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [181, 182, 183] print(xs[2]) end", 0);
    assert_eq!(out, "183\n");
}
#[test]
fn interp_array_index_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [182, 183, 184] print(xs[2]) end", 0);
    assert_eq!(out, "184\n");
}
#[test]
fn interp_array_index_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [183, 184, 185] print(xs[2]) end", 0);
    assert_eq!(out, "185\n");
}
#[test]
fn interp_array_index_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [184, 185, 186] print(xs[2]) end", 0);
    assert_eq!(out, "186\n");
}
#[test]
fn interp_array_index_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [185, 186, 187] print(xs[2]) end", 0);
    assert_eq!(out, "187\n");
}
#[test]
fn interp_array_index_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [186, 187, 188] print(xs[2]) end", 0);
    assert_eq!(out, "188\n");
}
#[test]
fn interp_array_index_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [187, 188, 189] print(xs[2]) end", 0);
    assert_eq!(out, "189\n");
}
#[test]
fn interp_array_index_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [188, 189, 190] print(xs[2]) end", 0);
    assert_eq!(out, "190\n");
}
#[test]
fn interp_array_index_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [189, 190, 191] print(xs[2]) end", 0);
    assert_eq!(out, "191\n");
}
#[test]
fn interp_array_index_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [190, 191, 192] print(xs[2]) end", 0);
    assert_eq!(out, "192\n");
}
#[test]
fn interp_array_index_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [191, 192, 193] print(xs[2]) end", 0);
    assert_eq!(out, "193\n");
}
#[test]
fn interp_array_index_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [192, 193, 194] print(xs[2]) end", 0);
    assert_eq!(out, "194\n");
}
#[test]
fn interp_array_index_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [193, 194, 195] print(xs[2]) end", 0);
    assert_eq!(out, "195\n");
}
#[test]
fn interp_array_index_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [194, 195, 196] print(xs[2]) end", 0);
    assert_eq!(out, "196\n");
}
#[test]
fn interp_array_index_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [195, 196, 197] print(xs[2]) end", 0);
    assert_eq!(out, "197\n");
}
#[test]
fn interp_array_index_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [196, 197, 198] print(xs[2]) end", 0);
    assert_eq!(out, "198\n");
}
#[test]
fn interp_array_index_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [197, 198, 199] print(xs[2]) end", 0);
    assert_eq!(out, "199\n");
}
#[test]
fn interp_array_index_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [198, 199, 200] print(xs[2]) end", 0);
    assert_eq!(out, "200\n");
}
#[test]
fn interp_array_index_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [199, 200, 201] print(xs[2]) end", 0);
    assert_eq!(out, "201\n");
}
#[test]
fn interp_array_index_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [200, 201, 202] print(xs[2]) end", 0);
    assert_eq!(out, "202\n");
}
#[test]
fn interp_array_index_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [201, 202, 203] print(xs[2]) end", 0);
    assert_eq!(out, "203\n");
}
#[test]
fn interp_array_index_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [202, 203, 204] print(xs[2]) end", 0);
    assert_eq!(out, "204\n");
}
#[test]
fn interp_array_index_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [203, 204, 205] print(xs[2]) end", 0);
    assert_eq!(out, "205\n");
}
#[test]
fn interp_array_index_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [204, 205, 206] print(xs[2]) end", 0);
    assert_eq!(out, "206\n");
}
#[test]
fn interp_array_index_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [205, 206, 207] print(xs[2]) end", 0);
    assert_eq!(out, "207\n");
}
#[test]
fn interp_array_index_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [206, 207, 208] print(xs[2]) end", 0);
    assert_eq!(out, "208\n");
}
#[test]
fn interp_array_index_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [207, 208, 209] print(xs[2]) end", 0);
    assert_eq!(out, "209\n");
}
#[test]
fn interp_array_index_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [208, 209, 210] print(xs[2]) end", 0);
    assert_eq!(out, "210\n");
}
#[test]
fn interp_array_index_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [209, 210, 211] print(xs[2]) end", 0);
    assert_eq!(out, "211\n");
}
#[test]
fn interp_array_index_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [210, 211, 212] print(xs[2]) end", 0);
    assert_eq!(out, "212\n");
}
#[test]
fn interp_array_index_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [211, 212, 213] print(xs[2]) end", 0);
    assert_eq!(out, "213\n");
}
#[test]
fn interp_array_index_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [212, 213, 214] print(xs[2]) end", 0);
    assert_eq!(out, "214\n");
}
#[test]
fn interp_array_index_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [213, 214, 215] print(xs[2]) end", 0);
    assert_eq!(out, "215\n");
}
#[test]
fn interp_array_index_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [214, 215, 216] print(xs[2]) end", 0);
    assert_eq!(out, "216\n");
}
#[test]
fn interp_array_index_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [215, 216, 217] print(xs[2]) end", 0);
    assert_eq!(out, "217\n");
}
#[test]
fn interp_array_index_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [216, 217, 218] print(xs[2]) end", 0);
    assert_eq!(out, "218\n");
}
#[test]
fn interp_array_index_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [217, 218, 219] print(xs[2]) end", 0);
    assert_eq!(out, "219\n");
}
#[test]
fn interp_array_index_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [218, 219, 220] print(xs[2]) end", 0);
    assert_eq!(out, "220\n");
}
#[test]
fn interp_array_index_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [219, 220, 221] print(xs[2]) end", 0);
    assert_eq!(out, "221\n");
}
#[test]
fn interp_array_index_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [220, 221, 222] print(xs[2]) end", 0);
    assert_eq!(out, "222\n");
}
#[test]
fn interp_array_index_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [221, 222, 223] print(xs[2]) end", 0);
    assert_eq!(out, "223\n");
}
#[test]
fn interp_array_index_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [222, 223, 224] print(xs[2]) end", 0);
    assert_eq!(out, "224\n");
}
#[test]
fn interp_array_index_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [223, 224, 225] print(xs[2]) end", 0);
    assert_eq!(out, "225\n");
}
#[test]
fn interp_array_index_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [224, 225, 226] print(xs[2]) end", 0);
    assert_eq!(out, "226\n");
}
#[test]
fn interp_array_index_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [225, 226, 227] print(xs[2]) end", 0);
    assert_eq!(out, "227\n");
}
#[test]
fn interp_array_index_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [226, 227, 228] print(xs[2]) end", 0);
    assert_eq!(out, "228\n");
}
#[test]
fn interp_array_index_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [227, 228, 229] print(xs[2]) end", 0);
    assert_eq!(out, "229\n");
}
#[test]
fn interp_array_index_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [228, 229, 230] print(xs[2]) end", 0);
    assert_eq!(out, "230\n");
}
#[test]
fn interp_array_index_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [229, 230, 231] print(xs[2]) end", 0);
    assert_eq!(out, "231\n");
}
#[test]
fn interp_array_index_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [230, 231, 232] print(xs[2]) end", 0);
    assert_eq!(out, "232\n");
}
#[test]
fn interp_array_index_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [231, 232, 233] print(xs[2]) end", 0);
    assert_eq!(out, "233\n");
}
#[test]
fn interp_array_index_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [232, 233, 234] print(xs[2]) end", 0);
    assert_eq!(out, "234\n");
}
#[test]
fn interp_array_index_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [233, 234, 235] print(xs[2]) end", 0);
    assert_eq!(out, "235\n");
}
#[test]
fn interp_array_index_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [234, 235, 236] print(xs[2]) end", 0);
    assert_eq!(out, "236\n");
}
#[test]
fn interp_array_index_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [235, 236, 237] print(xs[2]) end", 0);
    assert_eq!(out, "237\n");
}
#[test]
fn interp_array_index_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [236, 237, 238] print(xs[2]) end", 0);
    assert_eq!(out, "238\n");
}
#[test]
fn interp_array_index_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [237, 238, 239] print(xs[2]) end", 0);
    assert_eq!(out, "239\n");
}
#[test]
fn interp_array_index_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [238, 239, 240] print(xs[2]) end", 0);
    assert_eq!(out, "240\n");
}
#[test]
fn interp_array_index_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [239, 240, 241] print(xs[2]) end", 0);
    assert_eq!(out, "241\n");
}
#[test]
fn interp_array_index_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [240, 241, 242] print(xs[2]) end", 0);
    assert_eq!(out, "242\n");
}
#[test]
fn interp_array_index_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [241, 242, 243] print(xs[2]) end", 0);
    assert_eq!(out, "243\n");
}
#[test]
fn interp_array_index_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [242, 243, 244] print(xs[2]) end", 0);
    assert_eq!(out, "244\n");
}
#[test]
fn interp_array_index_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [243, 244, 245] print(xs[2]) end", 0);
    assert_eq!(out, "245\n");
}
#[test]
fn interp_array_index_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [244, 245, 246] print(xs[2]) end", 0);
    assert_eq!(out, "246\n");
}
#[test]
fn interp_array_index_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [245, 246, 247] print(xs[2]) end", 0);
    assert_eq!(out, "247\n");
}
#[test]
fn interp_array_index_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [246, 247, 248] print(xs[2]) end", 0);
    assert_eq!(out, "248\n");
}
#[test]
fn interp_array_index_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [247, 248, 249] print(xs[2]) end", 0);
    assert_eq!(out, "249\n");
}
#[test]
fn interp_array_index_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [248, 249, 250] print(xs[2]) end", 0);
    assert_eq!(out, "250\n");
}
#[test]
fn interp_array_index_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [249, 250, 251] print(xs[2]) end", 0);
    assert_eq!(out, "251\n");
}
#[test]
fn interp_array_index_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [250, 251, 252] print(xs[2]) end", 0);
    assert_eq!(out, "252\n");
}
#[test]
fn interp_array_index_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [251, 252, 253] print(xs[2]) end", 0);
    assert_eq!(out, "253\n");
}
#[test]
fn interp_array_index_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [252, 253, 254] print(xs[2]) end", 0);
    assert_eq!(out, "254\n");
}
#[test]
fn interp_array_index_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [253, 254, 255] print(xs[2]) end", 0);
    assert_eq!(out, "255\n");
}
#[test]
fn interp_array_index_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [254, 255, 256] print(xs[2]) end", 0);
    assert_eq!(out, "256\n");
}
#[test]
fn interp_array_index_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [255, 256, 257] print(xs[2]) end", 0);
    assert_eq!(out, "257\n");
}
#[test]
fn interp_array_index_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [256, 257, 258] print(xs[2]) end", 0);
    assert_eq!(out, "258\n");
}
#[test]
fn interp_array_index_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [257, 258, 259] print(xs[2]) end", 0);
    assert_eq!(out, "259\n");
}
#[test]
fn interp_array_index_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [258, 259, 260] print(xs[2]) end", 0);
    assert_eq!(out, "260\n");
}
#[test]
fn interp_array_index_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [259, 260, 261] print(xs[2]) end", 0);
    assert_eq!(out, "261\n");
}
#[test]
fn interp_array_index_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [260, 261, 262] print(xs[2]) end", 0);
    assert_eq!(out, "262\n");
}
#[test]
fn interp_array_index_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [261, 262, 263] print(xs[2]) end", 0);
    assert_eq!(out, "263\n");
}
#[test]
fn interp_array_index_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [262, 263, 264] print(xs[2]) end", 0);
    assert_eq!(out, "264\n");
}
#[test]
fn interp_array_index_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [263, 264, 265] print(xs[2]) end", 0);
    assert_eq!(out, "265\n");
}
#[test]
fn interp_array_index_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [264, 265, 266] print(xs[2]) end", 0);
    assert_eq!(out, "266\n");
}
#[test]
fn interp_array_index_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [265, 266, 267] print(xs[2]) end", 0);
    assert_eq!(out, "267\n");
}
#[test]
fn interp_array_index_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [266, 267, 268] print(xs[2]) end", 0);
    assert_eq!(out, "268\n");
}
#[test]
fn interp_array_index_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [267, 268, 269] print(xs[2]) end", 0);
    assert_eq!(out, "269\n");
}
#[test]
fn interp_array_index_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [268, 269, 270] print(xs[2]) end", 0);
    assert_eq!(out, "270\n");
}
#[test]
fn interp_array_index_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [269, 270, 271] print(xs[2]) end", 0);
    assert_eq!(out, "271\n");
}
#[test]
fn interp_array_index_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [270, 271, 272] print(xs[2]) end", 0);
    assert_eq!(out, "272\n");
}
#[test]
fn interp_array_index_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [271, 272, 273] print(xs[2]) end", 0);
    assert_eq!(out, "273\n");
}
#[test]
fn interp_array_index_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [272, 273, 274] print(xs[2]) end", 0);
    assert_eq!(out, "274\n");
}
#[test]
fn interp_array_index_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [273, 274, 275] print(xs[2]) end", 0);
    assert_eq!(out, "275\n");
}
#[test]
fn interp_array_index_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [274, 275, 276] print(xs[2]) end", 0);
    assert_eq!(out, "276\n");
}
#[test]
fn interp_array_index_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [275, 276, 277] print(xs[2]) end", 0);
    assert_eq!(out, "277\n");
}
#[test]
fn interp_array_index_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [276, 277, 278] print(xs[2]) end", 0);
    assert_eq!(out, "278\n");
}
#[test]
fn interp_array_index_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [277, 278, 279] print(xs[2]) end", 0);
    assert_eq!(out, "279\n");
}
#[test]
fn interp_array_index_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [278, 279, 280] print(xs[2]) end", 0);
    assert_eq!(out, "280\n");
}
#[test]
fn interp_array_index_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [279, 280, 281] print(xs[2]) end", 0);
    assert_eq!(out, "281\n");
}
#[test]
fn interp_array_index_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [280, 281, 282] print(xs[2]) end", 0);
    assert_eq!(out, "282\n");
}
#[test]
fn interp_array_index_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [281, 282, 283] print(xs[2]) end", 0);
    assert_eq!(out, "283\n");
}
#[test]
fn interp_array_index_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [282, 283, 284] print(xs[2]) end", 0);
    assert_eq!(out, "284\n");
}
#[test]
fn interp_array_index_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [283, 284, 285] print(xs[2]) end", 0);
    assert_eq!(out, "285\n");
}
#[test]
fn interp_array_index_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [284, 285, 286] print(xs[2]) end", 0);
    assert_eq!(out, "286\n");
}
#[test]
fn interp_array_index_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [285, 286, 287] print(xs[2]) end", 0);
    assert_eq!(out, "287\n");
}
#[test]
fn interp_array_index_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [286, 287, 288] print(xs[2]) end", 0);
    assert_eq!(out, "288\n");
}
#[test]
fn interp_array_index_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [287, 288, 289] print(xs[2]) end", 0);
    assert_eq!(out, "289\n");
}
#[test]
fn interp_array_index_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [288, 289, 290] print(xs[2]) end", 0);
    assert_eq!(out, "290\n");
}
#[test]
fn interp_array_index_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [289, 290, 291] print(xs[2]) end", 0);
    assert_eq!(out, "291\n");
}
#[test]
fn interp_array_index_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [290, 291, 292] print(xs[2]) end", 0);
    assert_eq!(out, "292\n");
}
#[test]
fn interp_array_index_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [291, 292, 293] print(xs[2]) end", 0);
    assert_eq!(out, "293\n");
}
#[test]
fn interp_array_index_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [292, 293, 294] print(xs[2]) end", 0);
    assert_eq!(out, "294\n");
}
#[test]
fn interp_array_index_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [293, 294, 295] print(xs[2]) end", 0);
    assert_eq!(out, "295\n");
}
#[test]
fn interp_array_index_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [294, 295, 296] print(xs[2]) end", 0);
    assert_eq!(out, "296\n");
}
#[test]
fn interp_array_index_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [295, 296, 297] print(xs[2]) end", 0);
    assert_eq!(out, "297\n");
}
#[test]
fn interp_array_index_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [296, 297, 298] print(xs[2]) end", 0);
    assert_eq!(out, "298\n");
}
#[test]
fn interp_array_index_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [297, 298, 299] print(xs[2]) end", 0);
    assert_eq!(out, "299\n");
}
#[test]
fn interp_array_index_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [298, 299, 300] print(xs[2]) end", 0);
    assert_eq!(out, "300\n");
}
#[test]
fn interp_array_index_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [299, 300, 301] print(xs[2]) end", 0);
    assert_eq!(out, "301\n");
}
#[test]
fn interp_array_index_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [300, 301, 302] print(xs[2]) end", 0);
    assert_eq!(out, "302\n");
}
#[test]
fn interp_array_index_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [301, 302, 303] print(xs[2]) end", 0);
    assert_eq!(out, "303\n");
}
#[test]
fn interp_array_index_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [302, 303, 304] print(xs[2]) end", 0);
    assert_eq!(out, "304\n");
}
#[test]
fn interp_array_index_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [303, 304, 305] print(xs[2]) end", 0);
    assert_eq!(out, "305\n");
}
#[test]
fn interp_array_index_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [304, 305, 306] print(xs[2]) end", 0);
    assert_eq!(out, "306\n");
}
#[test]
fn interp_array_index_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [305, 306, 307] print(xs[2]) end", 0);
    assert_eq!(out, "307\n");
}
#[test]
fn interp_array_index_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [306, 307, 308] print(xs[2]) end", 0);
    assert_eq!(out, "308\n");
}
#[test]
fn interp_array_index_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [307, 308, 309] print(xs[2]) end", 0);
    assert_eq!(out, "309\n");
}
#[test]
fn interp_array_index_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [308, 309, 310] print(xs[2]) end", 0);
    assert_eq!(out, "310\n");
}
#[test]
fn interp_array_index_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [309, 310, 311] print(xs[2]) end", 0);
    assert_eq!(out, "311\n");
}
#[test]
fn interp_array_index_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [310, 311, 312] print(xs[2]) end", 0);
    assert_eq!(out, "312\n");
}
#[test]
fn interp_array_index_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [311, 312, 313] print(xs[2]) end", 0);
    assert_eq!(out, "313\n");
}
#[test]
fn interp_array_index_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [312, 313, 314] print(xs[2]) end", 0);
    assert_eq!(out, "314\n");
}
#[test]
fn interp_array_index_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [313, 314, 315] print(xs[2]) end", 0);
    assert_eq!(out, "315\n");
}
#[test]
fn interp_array_index_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [314, 315, 316] print(xs[2]) end", 0);
    assert_eq!(out, "316\n");
}
#[test]
fn interp_array_index_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [315, 316, 317] print(xs[2]) end", 0);
    assert_eq!(out, "317\n");
}
#[test]
fn interp_array_index_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [316, 317, 318] print(xs[2]) end", 0);
    assert_eq!(out, "318\n");
}
#[test]
fn interp_array_index_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [317, 318, 319] print(xs[2]) end", 0);
    assert_eq!(out, "319\n");
}
#[test]
fn interp_array_index_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [318, 319, 320] print(xs[2]) end", 0);
    assert_eq!(out, "320\n");
}
#[test]
fn interp_array_index_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [319, 320, 321] print(xs[2]) end", 0);
    assert_eq!(out, "321\n");
}
#[test]
fn interp_array_index_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [320, 321, 322] print(xs[2]) end", 0);
    assert_eq!(out, "322\n");
}
#[test]
fn interp_array_index_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [321, 322, 323] print(xs[2]) end", 0);
    assert_eq!(out, "323\n");
}
#[test]
fn interp_array_index_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [322, 323, 324] print(xs[2]) end", 0);
    assert_eq!(out, "324\n");
}
#[test]
fn interp_array_index_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [323, 324, 325] print(xs[2]) end", 0);
    assert_eq!(out, "325\n");
}
#[test]
fn interp_array_index_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [324, 325, 326] print(xs[2]) end", 0);
    assert_eq!(out, "326\n");
}
#[test]
fn interp_array_index_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [325, 326, 327] print(xs[2]) end", 0);
    assert_eq!(out, "327\n");
}
#[test]
fn interp_array_index_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [326, 327, 328] print(xs[2]) end", 0);
    assert_eq!(out, "328\n");
}
#[test]
fn interp_array_index_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [327, 328, 329] print(xs[2]) end", 0);
    assert_eq!(out, "329\n");
}
#[test]
fn interp_array_index_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [328, 329, 330] print(xs[2]) end", 0);
    assert_eq!(out, "330\n");
}
#[test]
fn interp_array_index_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [329, 330, 331] print(xs[2]) end", 0);
    assert_eq!(out, "331\n");
}
#[test]
fn interp_array_index_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [330, 331, 332] print(xs[2]) end", 0);
    assert_eq!(out, "332\n");
}
#[test]
fn interp_array_index_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [331, 332, 333] print(xs[2]) end", 0);
    assert_eq!(out, "333\n");
}
#[test]
fn interp_array_index_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [332, 333, 334] print(xs[2]) end", 0);
    assert_eq!(out, "334\n");
}
#[test]
fn interp_array_index_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [333, 334, 335] print(xs[2]) end", 0);
    assert_eq!(out, "335\n");
}
#[test]
fn interp_array_index_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [334, 335, 336] print(xs[2]) end", 0);
    assert_eq!(out, "336\n");
}
#[test]
fn interp_array_index_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [335, 336, 337] print(xs[2]) end", 0);
    assert_eq!(out, "337\n");
}
#[test]
fn interp_array_index_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [336, 337, 338] print(xs[2]) end", 0);
    assert_eq!(out, "338\n");
}
#[test]
fn interp_array_index_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [337, 338, 339] print(xs[2]) end", 0);
    assert_eq!(out, "339\n");
}
#[test]
fn interp_array_index_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [338, 339, 340] print(xs[2]) end", 0);
    assert_eq!(out, "340\n");
}
#[test]
fn interp_array_index_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [339, 340, 341] print(xs[2]) end", 0);
    assert_eq!(out, "341\n");
}
#[test]
fn interp_array_index_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [340, 341, 342] print(xs[2]) end", 0);
    assert_eq!(out, "342\n");
}
#[test]
fn interp_array_index_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [341, 342, 343] print(xs[2]) end", 0);
    assert_eq!(out, "343\n");
}
#[test]
fn interp_array_index_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [342, 343, 344] print(xs[2]) end", 0);
    assert_eq!(out, "344\n");
}
#[test]
fn interp_array_index_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [343, 344, 345] print(xs[2]) end", 0);
    assert_eq!(out, "345\n");
}
#[test]
fn interp_array_index_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [344, 345, 346] print(xs[2]) end", 0);
    assert_eq!(out, "346\n");
}
#[test]
fn interp_array_index_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [345, 346, 347] print(xs[2]) end", 0);
    assert_eq!(out, "347\n");
}
#[test]
fn interp_array_index_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [346, 347, 348] print(xs[2]) end", 0);
    assert_eq!(out, "348\n");
}
#[test]
fn interp_array_index_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [347, 348, 349] print(xs[2]) end", 0);
    assert_eq!(out, "349\n");
}
#[test]
fn interp_array_index_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [348, 349, 350] print(xs[2]) end", 0);
    assert_eq!(out, "350\n");
}
#[test]
fn interp_array_index_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [349, 350, 351] print(xs[2]) end", 0);
    assert_eq!(out, "351\n");
}
#[test]
fn interp_array_index_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [350, 351, 352] print(xs[2]) end", 0);
    assert_eq!(out, "352\n");
}
#[test]
fn interp_array_index_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [351, 352, 353] print(xs[2]) end", 0);
    assert_eq!(out, "353\n");
}
#[test]
fn interp_array_index_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [352, 353, 354] print(xs[2]) end", 0);
    assert_eq!(out, "354\n");
}
#[test]
fn interp_array_index_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [353, 354, 355] print(xs[2]) end", 0);
    assert_eq!(out, "355\n");
}
#[test]
fn interp_array_index_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [354, 355, 356] print(xs[2]) end", 0);
    assert_eq!(out, "356\n");
}
#[test]
fn interp_array_index_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [355, 356, 357] print(xs[2]) end", 0);
    assert_eq!(out, "357\n");
}
#[test]
fn interp_array_index_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [356, 357, 358] print(xs[2]) end", 0);
    assert_eq!(out, "358\n");
}
#[test]
fn interp_array_index_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [357, 358, 359] print(xs[2]) end", 0);
    assert_eq!(out, "359\n");
}
#[test]
fn interp_array_index_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [358, 359, 360] print(xs[2]) end", 0);
    assert_eq!(out, "360\n");
}
#[test]
fn interp_array_index_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [359, 360, 361] print(xs[2]) end", 0);
    assert_eq!(out, "361\n");
}
#[test]
fn interp_array_index_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [360, 361, 362] print(xs[2]) end", 0);
    assert_eq!(out, "362\n");
}
#[test]
fn interp_array_index_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [361, 362, 363] print(xs[2]) end", 0);
    assert_eq!(out, "363\n");
}
#[test]
fn interp_array_index_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [362, 363, 364] print(xs[2]) end", 0);
    assert_eq!(out, "364\n");
}
#[test]
fn interp_array_index_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [363, 364, 365] print(xs[2]) end", 0);
    assert_eq!(out, "365\n");
}
#[test]
fn interp_array_index_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [364, 365, 366] print(xs[2]) end", 0);
    assert_eq!(out, "366\n");
}
#[test]
fn interp_array_index_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [365, 366, 367] print(xs[2]) end", 0);
    assert_eq!(out, "367\n");
}
#[test]
fn interp_array_index_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [366, 367, 368] print(xs[2]) end", 0);
    assert_eq!(out, "368\n");
}
#[test]
fn interp_array_index_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [367, 368, 369] print(xs[2]) end", 0);
    assert_eq!(out, "369\n");
}
#[test]
fn interp_array_index_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [368, 369, 370] print(xs[2]) end", 0);
    assert_eq!(out, "370\n");
}
#[test]
fn interp_array_index_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [369, 370, 371] print(xs[2]) end", 0);
    assert_eq!(out, "371\n");
}
#[test]
fn interp_array_index_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [370, 371, 372] print(xs[2]) end", 0);
    assert_eq!(out, "372\n");
}
#[test]
fn interp_array_index_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [371, 372, 373] print(xs[2]) end", 0);
    assert_eq!(out, "373\n");
}
#[test]
fn interp_array_index_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [372, 373, 374] print(xs[2]) end", 0);
    assert_eq!(out, "374\n");
}
#[test]
fn interp_array_index_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [373, 374, 375] print(xs[2]) end", 0);
    assert_eq!(out, "375\n");
}
#[test]
fn interp_array_index_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [374, 375, 376] print(xs[2]) end", 0);
    assert_eq!(out, "376\n");
}
#[test]
fn interp_array_index_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [375, 376, 377] print(xs[2]) end", 0);
    assert_eq!(out, "377\n");
}
#[test]
fn interp_array_index_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [376, 377, 378] print(xs[2]) end", 0);
    assert_eq!(out, "378\n");
}
#[test]
fn interp_array_index_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [377, 378, 379] print(xs[2]) end", 0);
    assert_eq!(out, "379\n");
}
#[test]
fn interp_array_index_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [378, 379, 380] print(xs[2]) end", 0);
    assert_eq!(out, "380\n");
}
#[test]
fn interp_array_index_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [379, 380, 381] print(xs[2]) end", 0);
    assert_eq!(out, "381\n");
}
#[test]
fn interp_array_index_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [380, 381, 382] print(xs[2]) end", 0);
    assert_eq!(out, "382\n");
}
#[test]
fn interp_array_index_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [381, 382, 383] print(xs[2]) end", 0);
    assert_eq!(out, "383\n");
}
#[test]
fn interp_array_index_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [382, 383, 384] print(xs[2]) end", 0);
    assert_eq!(out, "384\n");
}
#[test]
fn interp_array_index_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [383, 384, 385] print(xs[2]) end", 0);
    assert_eq!(out, "385\n");
}
#[test]
fn interp_array_index_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [384, 385, 386] print(xs[2]) end", 0);
    assert_eq!(out, "386\n");
}
#[test]
fn interp_array_index_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [385, 386, 387] print(xs[2]) end", 0);
    assert_eq!(out, "387\n");
}
#[test]
fn interp_array_index_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [386, 387, 388] print(xs[2]) end", 0);
    assert_eq!(out, "388\n");
}
#[test]
fn interp_array_index_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [387, 388, 389] print(xs[2]) end", 0);
    assert_eq!(out, "389\n");
}
#[test]
fn interp_array_index_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [388, 389, 390] print(xs[2]) end", 0);
    assert_eq!(out, "390\n");
}
#[test]
fn interp_array_index_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [389, 390, 391] print(xs[2]) end", 0);
    assert_eq!(out, "391\n");
}
#[test]
fn interp_array_index_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [390, 391, 392] print(xs[2]) end", 0);
    assert_eq!(out, "392\n");
}
#[test]
fn interp_array_index_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [391, 392, 393] print(xs[2]) end", 0);
    assert_eq!(out, "393\n");
}
#[test]
fn interp_array_index_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [392, 393, 394] print(xs[2]) end", 0);
    assert_eq!(out, "394\n");
}
#[test]
fn interp_array_index_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [393, 394, 395] print(xs[2]) end", 0);
    assert_eq!(out, "395\n");
}
#[test]
fn interp_array_index_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [394, 395, 396] print(xs[2]) end", 0);
    assert_eq!(out, "396\n");
}
#[test]
fn interp_array_index_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [395, 396, 397] print(xs[2]) end", 0);
    assert_eq!(out, "397\n");
}
#[test]
fn interp_array_index_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [396, 397, 398] print(xs[2]) end", 0);
    assert_eq!(out, "398\n");
}
#[test]
fn interp_array_index_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [397, 398, 399] print(xs[2]) end", 0);
    assert_eq!(out, "399\n");
}
#[test]
fn interp_array_index_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [398, 399, 400] print(xs[2]) end", 0);
    assert_eq!(out, "400\n");
}
#[test]
fn interp_array_index_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [399, 400, 401] print(xs[2]) end", 0);
    assert_eq!(out, "401\n");
}
#[test]
fn interp_array_index_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [400, 401, 402] print(xs[2]) end", 0);
    assert_eq!(out, "402\n");
}
#[test]
fn interp_array_index_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [401, 402, 403] print(xs[2]) end", 0);
    assert_eq!(out, "403\n");
}
#[test]
fn interp_array_index_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [402, 403, 404] print(xs[2]) end", 0);
    assert_eq!(out, "404\n");
}
#[test]
fn interp_array_index_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [403, 404, 405] print(xs[2]) end", 0);
    assert_eq!(out, "405\n");
}
#[test]
fn interp_array_index_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [404, 405, 406] print(xs[2]) end", 0);
    assert_eq!(out, "406\n");
}
#[test]
fn interp_array_index_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [405, 406, 407] print(xs[2]) end", 0);
    assert_eq!(out, "407\n");
}
#[test]
fn interp_array_index_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [406, 407, 408] print(xs[2]) end", 0);
    assert_eq!(out, "408\n");
}
#[test]
fn interp_array_index_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [407, 408, 409] print(xs[2]) end", 0);
    assert_eq!(out, "409\n");
}
#[test]
fn interp_array_index_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [408, 409, 410] print(xs[2]) end", 0);
    assert_eq!(out, "410\n");
}
#[test]
fn interp_array_index_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [409, 410, 411] print(xs[2]) end", 0);
    assert_eq!(out, "411\n");
}
#[test]
fn interp_array_index_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [410, 411, 412] print(xs[2]) end", 0);
    assert_eq!(out, "412\n");
}
#[test]
fn interp_array_index_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [411, 412, 413] print(xs[2]) end", 0);
    assert_eq!(out, "413\n");
}
#[test]
fn interp_array_index_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [412, 413, 414] print(xs[2]) end", 0);
    assert_eq!(out, "414\n");
}
#[test]
fn interp_array_index_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [413, 414, 415] print(xs[2]) end", 0);
    assert_eq!(out, "415\n");
}
#[test]
fn interp_array_index_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [414, 415, 416] print(xs[2]) end", 0);
    assert_eq!(out, "416\n");
}
#[test]
fn interp_array_index_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [415, 416, 417] print(xs[2]) end", 0);
    assert_eq!(out, "417\n");
}
#[test]
fn interp_array_index_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [416, 417, 418] print(xs[2]) end", 0);
    assert_eq!(out, "418\n");
}
#[test]
fn interp_array_index_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [417, 418, 419] print(xs[2]) end", 0);
    assert_eq!(out, "419\n");
}
#[test]
fn interp_array_index_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [418, 419, 420] print(xs[2]) end", 0);
    assert_eq!(out, "420\n");
}
#[test]
fn interp_array_index_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [419, 420, 421] print(xs[2]) end", 0);
    assert_eq!(out, "421\n");
}
#[test]
fn interp_array_index_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [420, 421, 422] print(xs[2]) end", 0);
    assert_eq!(out, "422\n");
}
#[test]
fn interp_array_index_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [421, 422, 423] print(xs[2]) end", 0);
    assert_eq!(out, "423\n");
}
#[test]
fn interp_array_index_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [422, 423, 424] print(xs[2]) end", 0);
    assert_eq!(out, "424\n");
}
#[test]
fn interp_array_index_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [423, 424, 425] print(xs[2]) end", 0);
    assert_eq!(out, "425\n");
}
#[test]
fn interp_array_index_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [424, 425, 426] print(xs[2]) end", 0);
    assert_eq!(out, "426\n");
}
#[test]
fn interp_array_index_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [425, 426, 427] print(xs[2]) end", 0);
    assert_eq!(out, "427\n");
}
#[test]
fn interp_array_index_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [426, 427, 428] print(xs[2]) end", 0);
    assert_eq!(out, "428\n");
}
#[test]
fn interp_array_index_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [427, 428, 429] print(xs[2]) end", 0);
    assert_eq!(out, "429\n");
}
#[test]
fn interp_array_index_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [428, 429, 430] print(xs[2]) end", 0);
    assert_eq!(out, "430\n");
}
#[test]
fn interp_array_index_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [429, 430, 431] print(xs[2]) end", 0);
    assert_eq!(out, "431\n");
}
#[test]
fn interp_array_index_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [430, 431, 432] print(xs[2]) end", 0);
    assert_eq!(out, "432\n");
}
#[test]
fn interp_array_index_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [431, 432, 433] print(xs[2]) end", 0);
    assert_eq!(out, "433\n");
}
#[test]
fn interp_array_index_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [432, 433, 434] print(xs[2]) end", 0);
    assert_eq!(out, "434\n");
}
#[test]
fn interp_array_index_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [433, 434, 435] print(xs[2]) end", 0);
    assert_eq!(out, "435\n");
}
#[test]
fn interp_array_index_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [434, 435, 436] print(xs[2]) end", 0);
    assert_eq!(out, "436\n");
}
#[test]
fn interp_array_index_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [435, 436, 437] print(xs[2]) end", 0);
    assert_eq!(out, "437\n");
}
#[test]
fn interp_array_index_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [436, 437, 438] print(xs[2]) end", 0);
    assert_eq!(out, "438\n");
}
#[test]
fn interp_array_index_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [437, 438, 439] print(xs[2]) end", 0);
    assert_eq!(out, "439\n");
}
#[test]
fn interp_array_index_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [438, 439, 440] print(xs[2]) end", 0);
    assert_eq!(out, "440\n");
}
#[test]
fn interp_array_index_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [439, 440, 441] print(xs[2]) end", 0);
    assert_eq!(out, "441\n");
}
#[test]
fn interp_array_index_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [440, 441, 442] print(xs[2]) end", 0);
    assert_eq!(out, "442\n");
}
#[test]
fn interp_array_index_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [441, 442, 443] print(xs[2]) end", 0);
    assert_eq!(out, "443\n");
}
#[test]
fn interp_array_index_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [442, 443, 444] print(xs[2]) end", 0);
    assert_eq!(out, "444\n");
}
#[test]
fn interp_array_index_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [443, 444, 445] print(xs[2]) end", 0);
    assert_eq!(out, "445\n");
}
#[test]
fn interp_array_index_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [444, 445, 446] print(xs[2]) end", 0);
    assert_eq!(out, "446\n");
}
#[test]
fn interp_array_index_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [445, 446, 447] print(xs[2]) end", 0);
    assert_eq!(out, "447\n");
}
#[test]
fn interp_array_index_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [446, 447, 448] print(xs[2]) end", 0);
    assert_eq!(out, "448\n");
}
#[test]
fn interp_array_index_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [447, 448, 449] print(xs[2]) end", 0);
    assert_eq!(out, "449\n");
}
#[test]
fn interp_array_index_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [448, 449, 450] print(xs[2]) end", 0);
    assert_eq!(out, "450\n");
}
#[test]
fn interp_array_index_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [449, 450, 451] print(xs[2]) end", 0);
    assert_eq!(out, "451\n");
}
#[test]
fn interp_array_index_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [450, 451, 452] print(xs[2]) end", 0);
    assert_eq!(out, "452\n");
}
#[test]
fn interp_array_index_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [451, 452, 453] print(xs[2]) end", 0);
    assert_eq!(out, "453\n");
}
#[test]
fn interp_array_index_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [452, 453, 454] print(xs[2]) end", 0);
    assert_eq!(out, "454\n");
}
#[test]
fn interp_array_index_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [453, 454, 455] print(xs[2]) end", 0);
    assert_eq!(out, "455\n");
}
#[test]
fn interp_array_index_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [454, 455, 456] print(xs[2]) end", 0);
    assert_eq!(out, "456\n");
}
#[test]
fn interp_array_index_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [455, 456, 457] print(xs[2]) end", 0);
    assert_eq!(out, "457\n");
}
#[test]
fn interp_array_index_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [456, 457, 458] print(xs[2]) end", 0);
    assert_eq!(out, "458\n");
}
#[test]
fn interp_array_index_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [457, 458, 459] print(xs[2]) end", 0);
    assert_eq!(out, "459\n");
}
#[test]
fn interp_array_index_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [458, 459, 460] print(xs[2]) end", 0);
    assert_eq!(out, "460\n");
}
#[test]
fn interp_array_index_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [459, 460, 461] print(xs[2]) end", 0);
    assert_eq!(out, "461\n");
}
#[test]
fn interp_array_index_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [460, 461, 462] print(xs[2]) end", 0);
    assert_eq!(out, "462\n");
}
#[test]
fn interp_array_index_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [461, 462, 463] print(xs[2]) end", 0);
    assert_eq!(out, "463\n");
}
#[test]
fn interp_array_index_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [462, 463, 464] print(xs[2]) end", 0);
    assert_eq!(out, "464\n");
}
#[test]
fn interp_array_index_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [463, 464, 465] print(xs[2]) end", 0);
    assert_eq!(out, "465\n");
}
#[test]
fn interp_array_index_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [464, 465, 466] print(xs[2]) end", 0);
    assert_eq!(out, "466\n");
}
#[test]
fn interp_array_index_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [465, 466, 467] print(xs[2]) end", 0);
    assert_eq!(out, "467\n");
}
#[test]
fn interp_array_index_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [466, 467, 468] print(xs[2]) end", 0);
    assert_eq!(out, "468\n");
}
#[test]
fn interp_array_index_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [467, 468, 469] print(xs[2]) end", 0);
    assert_eq!(out, "469\n");
}
#[test]
fn interp_array_index_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [468, 469, 470] print(xs[2]) end", 0);
    assert_eq!(out, "470\n");
}
#[test]
fn interp_array_index_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [469, 470, 471] print(xs[2]) end", 0);
    assert_eq!(out, "471\n");
}
#[test]
fn interp_array_index_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [470, 471, 472] print(xs[2]) end", 0);
    assert_eq!(out, "472\n");
}
#[test]
fn interp_array_index_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [471, 472, 473] print(xs[2]) end", 0);
    assert_eq!(out, "473\n");
}
#[test]
fn interp_array_index_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [472, 473, 474] print(xs[2]) end", 0);
    assert_eq!(out, "474\n");
}
#[test]
fn interp_array_index_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [473, 474, 475] print(xs[2]) end", 0);
    assert_eq!(out, "475\n");
}
#[test]
fn interp_array_index_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [474, 475, 476] print(xs[2]) end", 0);
    assert_eq!(out, "476\n");
}
#[test]
fn interp_array_index_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [475, 476, 477] print(xs[2]) end", 0);
    assert_eq!(out, "477\n");
}
#[test]
fn interp_array_index_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [476, 477, 478] print(xs[2]) end", 0);
    assert_eq!(out, "478\n");
}
#[test]
fn interp_array_index_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [477, 478, 479] print(xs[2]) end", 0);
    assert_eq!(out, "479\n");
}
#[test]
fn interp_array_index_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [478, 479, 480] print(xs[2]) end", 0);
    assert_eq!(out, "480\n");
}
#[test]
fn interp_array_index_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [479, 480, 481] print(xs[2]) end", 0);
    assert_eq!(out, "481\n");
}
#[test]
fn interp_array_index_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [480, 481, 482] print(xs[2]) end", 0);
    assert_eq!(out, "482\n");
}
#[test]
fn interp_array_index_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [481, 482, 483] print(xs[2]) end", 0);
    assert_eq!(out, "483\n");
}
#[test]
fn interp_array_index_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [482, 483, 484] print(xs[2]) end", 0);
    assert_eq!(out, "484\n");
}
#[test]
fn interp_array_index_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [483, 484, 485] print(xs[2]) end", 0);
    assert_eq!(out, "485\n");
}
#[test]
fn interp_array_index_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [484, 485, 486] print(xs[2]) end", 0);
    assert_eq!(out, "486\n");
}
#[test]
fn interp_array_index_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [485, 486, 487] print(xs[2]) end", 0);
    assert_eq!(out, "487\n");
}
#[test]
fn interp_array_index_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [486, 487, 488] print(xs[2]) end", 0);
    assert_eq!(out, "488\n");
}
#[test]
fn interp_array_index_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [487, 488, 489] print(xs[2]) end", 0);
    assert_eq!(out, "489\n");
}
#[test]
fn interp_array_index_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [488, 489, 490] print(xs[2]) end", 0);
    assert_eq!(out, "490\n");
}
#[test]
fn interp_array_index_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [489, 490, 491] print(xs[2]) end", 0);
    assert_eq!(out, "491\n");
}
#[test]
fn interp_array_index_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [490, 491, 492] print(xs[2]) end", 0);
    assert_eq!(out, "492\n");
}
#[test]
fn interp_array_index_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [491, 492, 493] print(xs[2]) end", 0);
    assert_eq!(out, "493\n");
}
#[test]
fn interp_array_index_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [492, 493, 494] print(xs[2]) end", 0);
    assert_eq!(out, "494\n");
}
#[test]
fn interp_array_index_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [493, 494, 495] print(xs[2]) end", 0);
    assert_eq!(out, "495\n");
}
#[test]
fn interp_array_index_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [494, 495, 496] print(xs[2]) end", 0);
    assert_eq!(out, "496\n");
}
#[test]
fn interp_array_index_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [495, 496, 497] print(xs[2]) end", 0);
    assert_eq!(out, "497\n");
}
#[test]
fn interp_array_index_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [496, 497, 498] print(xs[2]) end", 0);
    assert_eq!(out, "498\n");
}
#[test]
fn interp_array_index_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [497, 498, 499] print(xs[2]) end", 0);
    assert_eq!(out, "499\n");
}
#[test]
fn interp_array_index_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [498, 499, 500] print(xs[2]) end", 0);
    assert_eq!(out, "500\n");
}
#[test]
fn interp_array_index_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let xs = [499, 500, 501] print(xs[2]) end", 0);
    assert_eq!(out, "501\n");
}
#[test]
fn interp_closure_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 0 let add = fn(x) x + base end print(add(5)) end", 0);
    assert_eq!(out, "5\n");
}
#[test]
fn interp_closure_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 1 let add = fn(x) x + base end print(add(6)) end", 0);
    assert_eq!(out, "7\n");
}
#[test]
fn interp_closure_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 2 let add = fn(x) x + base end print(add(7)) end", 0);
    assert_eq!(out, "9\n");
}
#[test]
fn interp_closure_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 3 let add = fn(x) x + base end print(add(8)) end", 0);
    assert_eq!(out, "11\n");
}
#[test]
fn interp_closure_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 4 let add = fn(x) x + base end print(add(9)) end", 0);
    assert_eq!(out, "13\n");
}
#[test]
fn interp_closure_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 5 let add = fn(x) x + base end print(add(10)) end", 0);
    assert_eq!(out, "15\n");
}
#[test]
fn interp_closure_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 6 let add = fn(x) x + base end print(add(11)) end", 0);
    assert_eq!(out, "17\n");
}
#[test]
fn interp_closure_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 7 let add = fn(x) x + base end print(add(12)) end", 0);
    assert_eq!(out, "19\n");
}
#[test]
fn interp_closure_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 8 let add = fn(x) x + base end print(add(13)) end", 0);
    assert_eq!(out, "21\n");
}
#[test]
fn interp_closure_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 9 let add = fn(x) x + base end print(add(14)) end", 0);
    assert_eq!(out, "23\n");
}
#[test]
fn interp_closure_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 10 let add = fn(x) x + base end print(add(15)) end", 0);
    assert_eq!(out, "25\n");
}
#[test]
fn interp_closure_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 11 let add = fn(x) x + base end print(add(16)) end", 0);
    assert_eq!(out, "27\n");
}
#[test]
fn interp_closure_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 12 let add = fn(x) x + base end print(add(17)) end", 0);
    assert_eq!(out, "29\n");
}
#[test]
fn interp_closure_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 13 let add = fn(x) x + base end print(add(18)) end", 0);
    assert_eq!(out, "31\n");
}
#[test]
fn interp_closure_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 14 let add = fn(x) x + base end print(add(19)) end", 0);
    assert_eq!(out, "33\n");
}
#[test]
fn interp_closure_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 15 let add = fn(x) x + base end print(add(20)) end", 0);
    assert_eq!(out, "35\n");
}
#[test]
fn interp_closure_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 16 let add = fn(x) x + base end print(add(21)) end", 0);
    assert_eq!(out, "37\n");
}
#[test]
fn interp_closure_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 17 let add = fn(x) x + base end print(add(22)) end", 0);
    assert_eq!(out, "39\n");
}
#[test]
fn interp_closure_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 18 let add = fn(x) x + base end print(add(23)) end", 0);
    assert_eq!(out, "41\n");
}
#[test]
fn interp_closure_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 19 let add = fn(x) x + base end print(add(24)) end", 0);
    assert_eq!(out, "43\n");
}
#[test]
fn interp_closure_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 20 let add = fn(x) x + base end print(add(25)) end", 0);
    assert_eq!(out, "45\n");
}
#[test]
fn interp_closure_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 21 let add = fn(x) x + base end print(add(26)) end", 0);
    assert_eq!(out, "47\n");
}
#[test]
fn interp_closure_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 22 let add = fn(x) x + base end print(add(27)) end", 0);
    assert_eq!(out, "49\n");
}
#[test]
fn interp_closure_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 23 let add = fn(x) x + base end print(add(28)) end", 0);
    assert_eq!(out, "51\n");
}
#[test]
fn interp_closure_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 24 let add = fn(x) x + base end print(add(29)) end", 0);
    assert_eq!(out, "53\n");
}
#[test]
fn interp_closure_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 25 let add = fn(x) x + base end print(add(30)) end", 0);
    assert_eq!(out, "55\n");
}
#[test]
fn interp_closure_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 26 let add = fn(x) x + base end print(add(31)) end", 0);
    assert_eq!(out, "57\n");
}
#[test]
fn interp_closure_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 27 let add = fn(x) x + base end print(add(32)) end", 0);
    assert_eq!(out, "59\n");
}
#[test]
fn interp_closure_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 28 let add = fn(x) x + base end print(add(33)) end", 0);
    assert_eq!(out, "61\n");
}
#[test]
fn interp_closure_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 29 let add = fn(x) x + base end print(add(34)) end", 0);
    assert_eq!(out, "63\n");
}
#[test]
fn interp_closure_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 30 let add = fn(x) x + base end print(add(35)) end", 0);
    assert_eq!(out, "65\n");
}
#[test]
fn interp_closure_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 31 let add = fn(x) x + base end print(add(36)) end", 0);
    assert_eq!(out, "67\n");
}
#[test]
fn interp_closure_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 32 let add = fn(x) x + base end print(add(37)) end", 0);
    assert_eq!(out, "69\n");
}
#[test]
fn interp_closure_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 33 let add = fn(x) x + base end print(add(38)) end", 0);
    assert_eq!(out, "71\n");
}
#[test]
fn interp_closure_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 34 let add = fn(x) x + base end print(add(39)) end", 0);
    assert_eq!(out, "73\n");
}
#[test]
fn interp_closure_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 35 let add = fn(x) x + base end print(add(40)) end", 0);
    assert_eq!(out, "75\n");
}
#[test]
fn interp_closure_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 36 let add = fn(x) x + base end print(add(41)) end", 0);
    assert_eq!(out, "77\n");
}
#[test]
fn interp_closure_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 37 let add = fn(x) x + base end print(add(42)) end", 0);
    assert_eq!(out, "79\n");
}
#[test]
fn interp_closure_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 38 let add = fn(x) x + base end print(add(43)) end", 0);
    assert_eq!(out, "81\n");
}
#[test]
fn interp_closure_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 39 let add = fn(x) x + base end print(add(44)) end", 0);
    assert_eq!(out, "83\n");
}
#[test]
fn interp_closure_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 40 let add = fn(x) x + base end print(add(45)) end", 0);
    assert_eq!(out, "85\n");
}
#[test]
fn interp_closure_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 41 let add = fn(x) x + base end print(add(46)) end", 0);
    assert_eq!(out, "87\n");
}
#[test]
fn interp_closure_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 42 let add = fn(x) x + base end print(add(47)) end", 0);
    assert_eq!(out, "89\n");
}
#[test]
fn interp_closure_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 43 let add = fn(x) x + base end print(add(48)) end", 0);
    assert_eq!(out, "91\n");
}
#[test]
fn interp_closure_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 44 let add = fn(x) x + base end print(add(49)) end", 0);
    assert_eq!(out, "93\n");
}
#[test]
fn interp_closure_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 45 let add = fn(x) x + base end print(add(50)) end", 0);
    assert_eq!(out, "95\n");
}
#[test]
fn interp_closure_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 46 let add = fn(x) x + base end print(add(51)) end", 0);
    assert_eq!(out, "97\n");
}
#[test]
fn interp_closure_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 47 let add = fn(x) x + base end print(add(52)) end", 0);
    assert_eq!(out, "99\n");
}
#[test]
fn interp_closure_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 48 let add = fn(x) x + base end print(add(53)) end", 0);
    assert_eq!(out, "101\n");
}
#[test]
fn interp_closure_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 49 let add = fn(x) x + base end print(add(54)) end", 0);
    assert_eq!(out, "103\n");
}
#[test]
fn interp_closure_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 50 let add = fn(x) x + base end print(add(55)) end", 0);
    assert_eq!(out, "105\n");
}
#[test]
fn interp_closure_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 51 let add = fn(x) x + base end print(add(56)) end", 0);
    assert_eq!(out, "107\n");
}
#[test]
fn interp_closure_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 52 let add = fn(x) x + base end print(add(57)) end", 0);
    assert_eq!(out, "109\n");
}
#[test]
fn interp_closure_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 53 let add = fn(x) x + base end print(add(58)) end", 0);
    assert_eq!(out, "111\n");
}
#[test]
fn interp_closure_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 54 let add = fn(x) x + base end print(add(59)) end", 0);
    assert_eq!(out, "113\n");
}
#[test]
fn interp_closure_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 55 let add = fn(x) x + base end print(add(60)) end", 0);
    assert_eq!(out, "115\n");
}
#[test]
fn interp_closure_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 56 let add = fn(x) x + base end print(add(61)) end", 0);
    assert_eq!(out, "117\n");
}
#[test]
fn interp_closure_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 57 let add = fn(x) x + base end print(add(62)) end", 0);
    assert_eq!(out, "119\n");
}
#[test]
fn interp_closure_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 58 let add = fn(x) x + base end print(add(63)) end", 0);
    assert_eq!(out, "121\n");
}
#[test]
fn interp_closure_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 59 let add = fn(x) x + base end print(add(64)) end", 0);
    assert_eq!(out, "123\n");
}
#[test]
fn interp_closure_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 60 let add = fn(x) x + base end print(add(65)) end", 0);
    assert_eq!(out, "125\n");
}
#[test]
fn interp_closure_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 61 let add = fn(x) x + base end print(add(66)) end", 0);
    assert_eq!(out, "127\n");
}
#[test]
fn interp_closure_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 62 let add = fn(x) x + base end print(add(67)) end", 0);
    assert_eq!(out, "129\n");
}
#[test]
fn interp_closure_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 63 let add = fn(x) x + base end print(add(68)) end", 0);
    assert_eq!(out, "131\n");
}
#[test]
fn interp_closure_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 64 let add = fn(x) x + base end print(add(69)) end", 0);
    assert_eq!(out, "133\n");
}
#[test]
fn interp_closure_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 65 let add = fn(x) x + base end print(add(70)) end", 0);
    assert_eq!(out, "135\n");
}
#[test]
fn interp_closure_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 66 let add = fn(x) x + base end print(add(71)) end", 0);
    assert_eq!(out, "137\n");
}
#[test]
fn interp_closure_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 67 let add = fn(x) x + base end print(add(72)) end", 0);
    assert_eq!(out, "139\n");
}
#[test]
fn interp_closure_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 68 let add = fn(x) x + base end print(add(73)) end", 0);
    assert_eq!(out, "141\n");
}
#[test]
fn interp_closure_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 69 let add = fn(x) x + base end print(add(74)) end", 0);
    assert_eq!(out, "143\n");
}
#[test]
fn interp_closure_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 70 let add = fn(x) x + base end print(add(75)) end", 0);
    assert_eq!(out, "145\n");
}
#[test]
fn interp_closure_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 71 let add = fn(x) x + base end print(add(76)) end", 0);
    assert_eq!(out, "147\n");
}
#[test]
fn interp_closure_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 72 let add = fn(x) x + base end print(add(77)) end", 0);
    assert_eq!(out, "149\n");
}
#[test]
fn interp_closure_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 73 let add = fn(x) x + base end print(add(78)) end", 0);
    assert_eq!(out, "151\n");
}
#[test]
fn interp_closure_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 74 let add = fn(x) x + base end print(add(79)) end", 0);
    assert_eq!(out, "153\n");
}
#[test]
fn interp_closure_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 75 let add = fn(x) x + base end print(add(80)) end", 0);
    assert_eq!(out, "155\n");
}
#[test]
fn interp_closure_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 76 let add = fn(x) x + base end print(add(81)) end", 0);
    assert_eq!(out, "157\n");
}
#[test]
fn interp_closure_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 77 let add = fn(x) x + base end print(add(82)) end", 0);
    assert_eq!(out, "159\n");
}
#[test]
fn interp_closure_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 78 let add = fn(x) x + base end print(add(83)) end", 0);
    assert_eq!(out, "161\n");
}
#[test]
fn interp_closure_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 79 let add = fn(x) x + base end print(add(84)) end", 0);
    assert_eq!(out, "163\n");
}
#[test]
fn interp_closure_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 80 let add = fn(x) x + base end print(add(85)) end", 0);
    assert_eq!(out, "165\n");
}
#[test]
fn interp_closure_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 81 let add = fn(x) x + base end print(add(86)) end", 0);
    assert_eq!(out, "167\n");
}
#[test]
fn interp_closure_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 82 let add = fn(x) x + base end print(add(87)) end", 0);
    assert_eq!(out, "169\n");
}
#[test]
fn interp_closure_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 83 let add = fn(x) x + base end print(add(88)) end", 0);
    assert_eq!(out, "171\n");
}
#[test]
fn interp_closure_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 84 let add = fn(x) x + base end print(add(89)) end", 0);
    assert_eq!(out, "173\n");
}
#[test]
fn interp_closure_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 85 let add = fn(x) x + base end print(add(90)) end", 0);
    assert_eq!(out, "175\n");
}
#[test]
fn interp_closure_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 86 let add = fn(x) x + base end print(add(91)) end", 0);
    assert_eq!(out, "177\n");
}
#[test]
fn interp_closure_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 87 let add = fn(x) x + base end print(add(92)) end", 0);
    assert_eq!(out, "179\n");
}
#[test]
fn interp_closure_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 88 let add = fn(x) x + base end print(add(93)) end", 0);
    assert_eq!(out, "181\n");
}
#[test]
fn interp_closure_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 89 let add = fn(x) x + base end print(add(94)) end", 0);
    assert_eq!(out, "183\n");
}
#[test]
fn interp_closure_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 90 let add = fn(x) x + base end print(add(95)) end", 0);
    assert_eq!(out, "185\n");
}
#[test]
fn interp_closure_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 91 let add = fn(x) x + base end print(add(96)) end", 0);
    assert_eq!(out, "187\n");
}
#[test]
fn interp_closure_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 92 let add = fn(x) x + base end print(add(97)) end", 0);
    assert_eq!(out, "189\n");
}
#[test]
fn interp_closure_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 93 let add = fn(x) x + base end print(add(98)) end", 0);
    assert_eq!(out, "191\n");
}
#[test]
fn interp_closure_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 94 let add = fn(x) x + base end print(add(99)) end", 0);
    assert_eq!(out, "193\n");
}
#[test]
fn interp_closure_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 95 let add = fn(x) x + base end print(add(100)) end", 0);
    assert_eq!(out, "195\n");
}
#[test]
fn interp_closure_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 96 let add = fn(x) x + base end print(add(101)) end", 0);
    assert_eq!(out, "197\n");
}
#[test]
fn interp_closure_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 97 let add = fn(x) x + base end print(add(102)) end", 0);
    assert_eq!(out, "199\n");
}
#[test]
fn interp_closure_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 98 let add = fn(x) x + base end print(add(103)) end", 0);
    assert_eq!(out, "201\n");
}
#[test]
fn interp_closure_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 99 let add = fn(x) x + base end print(add(104)) end", 0);
    assert_eq!(out, "203\n");
}
#[test]
fn interp_closure_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 100 let add = fn(x) x + base end print(add(105)) end", 0);
    assert_eq!(out, "205\n");
}
#[test]
fn interp_closure_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 101 let add = fn(x) x + base end print(add(106)) end", 0);
    assert_eq!(out, "207\n");
}
#[test]
fn interp_closure_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 102 let add = fn(x) x + base end print(add(107)) end", 0);
    assert_eq!(out, "209\n");
}
#[test]
fn interp_closure_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 103 let add = fn(x) x + base end print(add(108)) end", 0);
    assert_eq!(out, "211\n");
}
#[test]
fn interp_closure_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 104 let add = fn(x) x + base end print(add(109)) end", 0);
    assert_eq!(out, "213\n");
}
#[test]
fn interp_closure_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 105 let add = fn(x) x + base end print(add(110)) end", 0);
    assert_eq!(out, "215\n");
}
#[test]
fn interp_closure_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 106 let add = fn(x) x + base end print(add(111)) end", 0);
    assert_eq!(out, "217\n");
}
#[test]
fn interp_closure_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 107 let add = fn(x) x + base end print(add(112)) end", 0);
    assert_eq!(out, "219\n");
}
#[test]
fn interp_closure_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 108 let add = fn(x) x + base end print(add(113)) end", 0);
    assert_eq!(out, "221\n");
}
#[test]
fn interp_closure_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 109 let add = fn(x) x + base end print(add(114)) end", 0);
    assert_eq!(out, "223\n");
}
#[test]
fn interp_closure_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 110 let add = fn(x) x + base end print(add(115)) end", 0);
    assert_eq!(out, "225\n");
}
#[test]
fn interp_closure_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 111 let add = fn(x) x + base end print(add(116)) end", 0);
    assert_eq!(out, "227\n");
}
#[test]
fn interp_closure_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 112 let add = fn(x) x + base end print(add(117)) end", 0);
    assert_eq!(out, "229\n");
}
#[test]
fn interp_closure_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 113 let add = fn(x) x + base end print(add(118)) end", 0);
    assert_eq!(out, "231\n");
}
#[test]
fn interp_closure_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 114 let add = fn(x) x + base end print(add(119)) end", 0);
    assert_eq!(out, "233\n");
}
#[test]
fn interp_closure_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 115 let add = fn(x) x + base end print(add(120)) end", 0);
    assert_eq!(out, "235\n");
}
#[test]
fn interp_closure_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 116 let add = fn(x) x + base end print(add(121)) end", 0);
    assert_eq!(out, "237\n");
}
#[test]
fn interp_closure_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 117 let add = fn(x) x + base end print(add(122)) end", 0);
    assert_eq!(out, "239\n");
}
#[test]
fn interp_closure_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 118 let add = fn(x) x + base end print(add(123)) end", 0);
    assert_eq!(out, "241\n");
}
#[test]
fn interp_closure_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 119 let add = fn(x) x + base end print(add(124)) end", 0);
    assert_eq!(out, "243\n");
}
#[test]
fn interp_closure_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 120 let add = fn(x) x + base end print(add(125)) end", 0);
    assert_eq!(out, "245\n");
}
#[test]
fn interp_closure_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 121 let add = fn(x) x + base end print(add(126)) end", 0);
    assert_eq!(out, "247\n");
}
#[test]
fn interp_closure_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 122 let add = fn(x) x + base end print(add(127)) end", 0);
    assert_eq!(out, "249\n");
}
#[test]
fn interp_closure_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 123 let add = fn(x) x + base end print(add(128)) end", 0);
    assert_eq!(out, "251\n");
}
#[test]
fn interp_closure_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 124 let add = fn(x) x + base end print(add(129)) end", 0);
    assert_eq!(out, "253\n");
}
#[test]
fn interp_closure_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 125 let add = fn(x) x + base end print(add(130)) end", 0);
    assert_eq!(out, "255\n");
}
#[test]
fn interp_closure_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 126 let add = fn(x) x + base end print(add(131)) end", 0);
    assert_eq!(out, "257\n");
}
#[test]
fn interp_closure_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 127 let add = fn(x) x + base end print(add(132)) end", 0);
    assert_eq!(out, "259\n");
}
#[test]
fn interp_closure_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 128 let add = fn(x) x + base end print(add(133)) end", 0);
    assert_eq!(out, "261\n");
}
#[test]
fn interp_closure_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 129 let add = fn(x) x + base end print(add(134)) end", 0);
    assert_eq!(out, "263\n");
}
#[test]
fn interp_closure_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 130 let add = fn(x) x + base end print(add(135)) end", 0);
    assert_eq!(out, "265\n");
}
#[test]
fn interp_closure_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 131 let add = fn(x) x + base end print(add(136)) end", 0);
    assert_eq!(out, "267\n");
}
#[test]
fn interp_closure_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 132 let add = fn(x) x + base end print(add(137)) end", 0);
    assert_eq!(out, "269\n");
}
#[test]
fn interp_closure_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 133 let add = fn(x) x + base end print(add(138)) end", 0);
    assert_eq!(out, "271\n");
}
#[test]
fn interp_closure_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 134 let add = fn(x) x + base end print(add(139)) end", 0);
    assert_eq!(out, "273\n");
}
#[test]
fn interp_closure_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 135 let add = fn(x) x + base end print(add(140)) end", 0);
    assert_eq!(out, "275\n");
}
#[test]
fn interp_closure_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 136 let add = fn(x) x + base end print(add(141)) end", 0);
    assert_eq!(out, "277\n");
}
#[test]
fn interp_closure_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 137 let add = fn(x) x + base end print(add(142)) end", 0);
    assert_eq!(out, "279\n");
}
#[test]
fn interp_closure_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 138 let add = fn(x) x + base end print(add(143)) end", 0);
    assert_eq!(out, "281\n");
}
#[test]
fn interp_closure_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 139 let add = fn(x) x + base end print(add(144)) end", 0);
    assert_eq!(out, "283\n");
}
#[test]
fn interp_closure_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 140 let add = fn(x) x + base end print(add(145)) end", 0);
    assert_eq!(out, "285\n");
}
#[test]
fn interp_closure_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 141 let add = fn(x) x + base end print(add(146)) end", 0);
    assert_eq!(out, "287\n");
}
#[test]
fn interp_closure_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 142 let add = fn(x) x + base end print(add(147)) end", 0);
    assert_eq!(out, "289\n");
}
#[test]
fn interp_closure_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 143 let add = fn(x) x + base end print(add(148)) end", 0);
    assert_eq!(out, "291\n");
}
#[test]
fn interp_closure_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 144 let add = fn(x) x + base end print(add(149)) end", 0);
    assert_eq!(out, "293\n");
}
#[test]
fn interp_closure_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 145 let add = fn(x) x + base end print(add(150)) end", 0);
    assert_eq!(out, "295\n");
}
#[test]
fn interp_closure_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 146 let add = fn(x) x + base end print(add(151)) end", 0);
    assert_eq!(out, "297\n");
}
#[test]
fn interp_closure_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 147 let add = fn(x) x + base end print(add(152)) end", 0);
    assert_eq!(out, "299\n");
}
#[test]
fn interp_closure_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 148 let add = fn(x) x + base end print(add(153)) end", 0);
    assert_eq!(out, "301\n");
}
#[test]
fn interp_closure_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 149 let add = fn(x) x + base end print(add(154)) end", 0);
    assert_eq!(out, "303\n");
}
#[test]
fn interp_closure_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 150 let add = fn(x) x + base end print(add(155)) end", 0);
    assert_eq!(out, "305\n");
}
#[test]
fn interp_closure_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 151 let add = fn(x) x + base end print(add(156)) end", 0);
    assert_eq!(out, "307\n");
}
#[test]
fn interp_closure_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 152 let add = fn(x) x + base end print(add(157)) end", 0);
    assert_eq!(out, "309\n");
}
#[test]
fn interp_closure_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 153 let add = fn(x) x + base end print(add(158)) end", 0);
    assert_eq!(out, "311\n");
}
#[test]
fn interp_closure_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 154 let add = fn(x) x + base end print(add(159)) end", 0);
    assert_eq!(out, "313\n");
}
#[test]
fn interp_closure_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 155 let add = fn(x) x + base end print(add(160)) end", 0);
    assert_eq!(out, "315\n");
}
#[test]
fn interp_closure_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 156 let add = fn(x) x + base end print(add(161)) end", 0);
    assert_eq!(out, "317\n");
}
#[test]
fn interp_closure_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 157 let add = fn(x) x + base end print(add(162)) end", 0);
    assert_eq!(out, "319\n");
}
#[test]
fn interp_closure_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 158 let add = fn(x) x + base end print(add(163)) end", 0);
    assert_eq!(out, "321\n");
}
#[test]
fn interp_closure_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 159 let add = fn(x) x + base end print(add(164)) end", 0);
    assert_eq!(out, "323\n");
}
#[test]
fn interp_closure_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 160 let add = fn(x) x + base end print(add(165)) end", 0);
    assert_eq!(out, "325\n");
}
#[test]
fn interp_closure_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 161 let add = fn(x) x + base end print(add(166)) end", 0);
    assert_eq!(out, "327\n");
}
#[test]
fn interp_closure_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 162 let add = fn(x) x + base end print(add(167)) end", 0);
    assert_eq!(out, "329\n");
}
#[test]
fn interp_closure_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 163 let add = fn(x) x + base end print(add(168)) end", 0);
    assert_eq!(out, "331\n");
}
#[test]
fn interp_closure_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 164 let add = fn(x) x + base end print(add(169)) end", 0);
    assert_eq!(out, "333\n");
}
#[test]
fn interp_closure_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 165 let add = fn(x) x + base end print(add(170)) end", 0);
    assert_eq!(out, "335\n");
}
#[test]
fn interp_closure_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 166 let add = fn(x) x + base end print(add(171)) end", 0);
    assert_eq!(out, "337\n");
}
#[test]
fn interp_closure_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 167 let add = fn(x) x + base end print(add(172)) end", 0);
    assert_eq!(out, "339\n");
}
#[test]
fn interp_closure_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 168 let add = fn(x) x + base end print(add(173)) end", 0);
    assert_eq!(out, "341\n");
}
#[test]
fn interp_closure_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 169 let add = fn(x) x + base end print(add(174)) end", 0);
    assert_eq!(out, "343\n");
}
#[test]
fn interp_closure_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 170 let add = fn(x) x + base end print(add(175)) end", 0);
    assert_eq!(out, "345\n");
}
#[test]
fn interp_closure_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 171 let add = fn(x) x + base end print(add(176)) end", 0);
    assert_eq!(out, "347\n");
}
#[test]
fn interp_closure_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 172 let add = fn(x) x + base end print(add(177)) end", 0);
    assert_eq!(out, "349\n");
}
#[test]
fn interp_closure_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 173 let add = fn(x) x + base end print(add(178)) end", 0);
    assert_eq!(out, "351\n");
}
#[test]
fn interp_closure_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 174 let add = fn(x) x + base end print(add(179)) end", 0);
    assert_eq!(out, "353\n");
}
#[test]
fn interp_closure_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 175 let add = fn(x) x + base end print(add(180)) end", 0);
    assert_eq!(out, "355\n");
}
#[test]
fn interp_closure_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 176 let add = fn(x) x + base end print(add(181)) end", 0);
    assert_eq!(out, "357\n");
}
#[test]
fn interp_closure_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 177 let add = fn(x) x + base end print(add(182)) end", 0);
    assert_eq!(out, "359\n");
}
#[test]
fn interp_closure_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 178 let add = fn(x) x + base end print(add(183)) end", 0);
    assert_eq!(out, "361\n");
}
#[test]
fn interp_closure_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 179 let add = fn(x) x + base end print(add(184)) end", 0);
    assert_eq!(out, "363\n");
}
#[test]
fn interp_closure_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 180 let add = fn(x) x + base end print(add(185)) end", 0);
    assert_eq!(out, "365\n");
}
#[test]
fn interp_closure_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 181 let add = fn(x) x + base end print(add(186)) end", 0);
    assert_eq!(out, "367\n");
}
#[test]
fn interp_closure_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 182 let add = fn(x) x + base end print(add(187)) end", 0);
    assert_eq!(out, "369\n");
}
#[test]
fn interp_closure_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 183 let add = fn(x) x + base end print(add(188)) end", 0);
    assert_eq!(out, "371\n");
}
#[test]
fn interp_closure_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 184 let add = fn(x) x + base end print(add(189)) end", 0);
    assert_eq!(out, "373\n");
}
#[test]
fn interp_closure_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 185 let add = fn(x) x + base end print(add(190)) end", 0);
    assert_eq!(out, "375\n");
}
#[test]
fn interp_closure_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 186 let add = fn(x) x + base end print(add(191)) end", 0);
    assert_eq!(out, "377\n");
}
#[test]
fn interp_closure_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 187 let add = fn(x) x + base end print(add(192)) end", 0);
    assert_eq!(out, "379\n");
}
#[test]
fn interp_closure_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 188 let add = fn(x) x + base end print(add(193)) end", 0);
    assert_eq!(out, "381\n");
}
#[test]
fn interp_closure_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 189 let add = fn(x) x + base end print(add(194)) end", 0);
    assert_eq!(out, "383\n");
}
#[test]
fn interp_closure_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 190 let add = fn(x) x + base end print(add(195)) end", 0);
    assert_eq!(out, "385\n");
}
#[test]
fn interp_closure_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 191 let add = fn(x) x + base end print(add(196)) end", 0);
    assert_eq!(out, "387\n");
}
#[test]
fn interp_closure_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 192 let add = fn(x) x + base end print(add(197)) end", 0);
    assert_eq!(out, "389\n");
}
#[test]
fn interp_closure_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 193 let add = fn(x) x + base end print(add(198)) end", 0);
    assert_eq!(out, "391\n");
}
#[test]
fn interp_closure_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 194 let add = fn(x) x + base end print(add(199)) end", 0);
    assert_eq!(out, "393\n");
}
#[test]
fn interp_closure_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 195 let add = fn(x) x + base end print(add(200)) end", 0);
    assert_eq!(out, "395\n");
}
#[test]
fn interp_closure_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 196 let add = fn(x) x + base end print(add(201)) end", 0);
    assert_eq!(out, "397\n");
}
#[test]
fn interp_closure_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 197 let add = fn(x) x + base end print(add(202)) end", 0);
    assert_eq!(out, "399\n");
}
#[test]
fn interp_closure_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 198 let add = fn(x) x + base end print(add(203)) end", 0);
    assert_eq!(out, "401\n");
}
#[test]
fn interp_closure_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 199 let add = fn(x) x + base end print(add(204)) end", 0);
    assert_eq!(out, "403\n");
}
#[test]
fn interp_closure_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 200 let add = fn(x) x + base end print(add(205)) end", 0);
    assert_eq!(out, "405\n");
}
#[test]
fn interp_closure_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 201 let add = fn(x) x + base end print(add(206)) end", 0);
    assert_eq!(out, "407\n");
}
#[test]
fn interp_closure_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 202 let add = fn(x) x + base end print(add(207)) end", 0);
    assert_eq!(out, "409\n");
}
#[test]
fn interp_closure_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 203 let add = fn(x) x + base end print(add(208)) end", 0);
    assert_eq!(out, "411\n");
}
#[test]
fn interp_closure_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 204 let add = fn(x) x + base end print(add(209)) end", 0);
    assert_eq!(out, "413\n");
}
#[test]
fn interp_closure_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 205 let add = fn(x) x + base end print(add(210)) end", 0);
    assert_eq!(out, "415\n");
}
#[test]
fn interp_closure_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 206 let add = fn(x) x + base end print(add(211)) end", 0);
    assert_eq!(out, "417\n");
}
#[test]
fn interp_closure_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 207 let add = fn(x) x + base end print(add(212)) end", 0);
    assert_eq!(out, "419\n");
}
#[test]
fn interp_closure_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 208 let add = fn(x) x + base end print(add(213)) end", 0);
    assert_eq!(out, "421\n");
}
#[test]
fn interp_closure_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 209 let add = fn(x) x + base end print(add(214)) end", 0);
    assert_eq!(out, "423\n");
}
#[test]
fn interp_closure_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 210 let add = fn(x) x + base end print(add(215)) end", 0);
    assert_eq!(out, "425\n");
}
#[test]
fn interp_closure_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 211 let add = fn(x) x + base end print(add(216)) end", 0);
    assert_eq!(out, "427\n");
}
#[test]
fn interp_closure_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 212 let add = fn(x) x + base end print(add(217)) end", 0);
    assert_eq!(out, "429\n");
}
#[test]
fn interp_closure_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 213 let add = fn(x) x + base end print(add(218)) end", 0);
    assert_eq!(out, "431\n");
}
#[test]
fn interp_closure_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 214 let add = fn(x) x + base end print(add(219)) end", 0);
    assert_eq!(out, "433\n");
}
#[test]
fn interp_closure_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 215 let add = fn(x) x + base end print(add(220)) end", 0);
    assert_eq!(out, "435\n");
}
#[test]
fn interp_closure_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 216 let add = fn(x) x + base end print(add(221)) end", 0);
    assert_eq!(out, "437\n");
}
#[test]
fn interp_closure_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 217 let add = fn(x) x + base end print(add(222)) end", 0);
    assert_eq!(out, "439\n");
}
#[test]
fn interp_closure_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 218 let add = fn(x) x + base end print(add(223)) end", 0);
    assert_eq!(out, "441\n");
}
#[test]
fn interp_closure_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 219 let add = fn(x) x + base end print(add(224)) end", 0);
    assert_eq!(out, "443\n");
}
#[test]
fn interp_closure_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 220 let add = fn(x) x + base end print(add(225)) end", 0);
    assert_eq!(out, "445\n");
}
#[test]
fn interp_closure_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 221 let add = fn(x) x + base end print(add(226)) end", 0);
    assert_eq!(out, "447\n");
}
#[test]
fn interp_closure_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 222 let add = fn(x) x + base end print(add(227)) end", 0);
    assert_eq!(out, "449\n");
}
#[test]
fn interp_closure_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 223 let add = fn(x) x + base end print(add(228)) end", 0);
    assert_eq!(out, "451\n");
}
#[test]
fn interp_closure_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 224 let add = fn(x) x + base end print(add(229)) end", 0);
    assert_eq!(out, "453\n");
}
#[test]
fn interp_closure_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 225 let add = fn(x) x + base end print(add(230)) end", 0);
    assert_eq!(out, "455\n");
}
#[test]
fn interp_closure_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 226 let add = fn(x) x + base end print(add(231)) end", 0);
    assert_eq!(out, "457\n");
}
#[test]
fn interp_closure_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 227 let add = fn(x) x + base end print(add(232)) end", 0);
    assert_eq!(out, "459\n");
}
#[test]
fn interp_closure_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 228 let add = fn(x) x + base end print(add(233)) end", 0);
    assert_eq!(out, "461\n");
}
#[test]
fn interp_closure_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 229 let add = fn(x) x + base end print(add(234)) end", 0);
    assert_eq!(out, "463\n");
}
#[test]
fn interp_closure_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 230 let add = fn(x) x + base end print(add(235)) end", 0);
    assert_eq!(out, "465\n");
}
#[test]
fn interp_closure_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 231 let add = fn(x) x + base end print(add(236)) end", 0);
    assert_eq!(out, "467\n");
}
#[test]
fn interp_closure_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 232 let add = fn(x) x + base end print(add(237)) end", 0);
    assert_eq!(out, "469\n");
}
#[test]
fn interp_closure_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 233 let add = fn(x) x + base end print(add(238)) end", 0);
    assert_eq!(out, "471\n");
}
#[test]
fn interp_closure_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 234 let add = fn(x) x + base end print(add(239)) end", 0);
    assert_eq!(out, "473\n");
}
#[test]
fn interp_closure_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 235 let add = fn(x) x + base end print(add(240)) end", 0);
    assert_eq!(out, "475\n");
}
#[test]
fn interp_closure_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 236 let add = fn(x) x + base end print(add(241)) end", 0);
    assert_eq!(out, "477\n");
}
#[test]
fn interp_closure_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 237 let add = fn(x) x + base end print(add(242)) end", 0);
    assert_eq!(out, "479\n");
}
#[test]
fn interp_closure_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 238 let add = fn(x) x + base end print(add(243)) end", 0);
    assert_eq!(out, "481\n");
}
#[test]
fn interp_closure_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 239 let add = fn(x) x + base end print(add(244)) end", 0);
    assert_eq!(out, "483\n");
}
#[test]
fn interp_closure_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 240 let add = fn(x) x + base end print(add(245)) end", 0);
    assert_eq!(out, "485\n");
}
#[test]
fn interp_closure_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 241 let add = fn(x) x + base end print(add(246)) end", 0);
    assert_eq!(out, "487\n");
}
#[test]
fn interp_closure_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 242 let add = fn(x) x + base end print(add(247)) end", 0);
    assert_eq!(out, "489\n");
}
#[test]
fn interp_closure_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 243 let add = fn(x) x + base end print(add(248)) end", 0);
    assert_eq!(out, "491\n");
}
#[test]
fn interp_closure_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 244 let add = fn(x) x + base end print(add(249)) end", 0);
    assert_eq!(out, "493\n");
}
#[test]
fn interp_closure_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 245 let add = fn(x) x + base end print(add(250)) end", 0);
    assert_eq!(out, "495\n");
}
#[test]
fn interp_closure_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 246 let add = fn(x) x + base end print(add(251)) end", 0);
    assert_eq!(out, "497\n");
}
#[test]
fn interp_closure_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 247 let add = fn(x) x + base end print(add(252)) end", 0);
    assert_eq!(out, "499\n");
}
#[test]
fn interp_closure_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 248 let add = fn(x) x + base end print(add(253)) end", 0);
    assert_eq!(out, "501\n");
}
#[test]
fn interp_closure_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 249 let add = fn(x) x + base end print(add(254)) end", 0);
    assert_eq!(out, "503\n");
}
#[test]
fn interp_closure_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 250 let add = fn(x) x + base end print(add(255)) end", 0);
    assert_eq!(out, "505\n");
}
#[test]
fn interp_closure_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 251 let add = fn(x) x + base end print(add(256)) end", 0);
    assert_eq!(out, "507\n");
}
#[test]
fn interp_closure_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 252 let add = fn(x) x + base end print(add(257)) end", 0);
    assert_eq!(out, "509\n");
}
#[test]
fn interp_closure_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 253 let add = fn(x) x + base end print(add(258)) end", 0);
    assert_eq!(out, "511\n");
}
#[test]
fn interp_closure_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 254 let add = fn(x) x + base end print(add(259)) end", 0);
    assert_eq!(out, "513\n");
}
#[test]
fn interp_closure_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 255 let add = fn(x) x + base end print(add(260)) end", 0);
    assert_eq!(out, "515\n");
}
#[test]
fn interp_closure_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 256 let add = fn(x) x + base end print(add(261)) end", 0);
    assert_eq!(out, "517\n");
}
#[test]
fn interp_closure_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 257 let add = fn(x) x + base end print(add(262)) end", 0);
    assert_eq!(out, "519\n");
}
#[test]
fn interp_closure_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 258 let add = fn(x) x + base end print(add(263)) end", 0);
    assert_eq!(out, "521\n");
}
#[test]
fn interp_closure_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 259 let add = fn(x) x + base end print(add(264)) end", 0);
    assert_eq!(out, "523\n");
}
#[test]
fn interp_closure_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 260 let add = fn(x) x + base end print(add(265)) end", 0);
    assert_eq!(out, "525\n");
}
#[test]
fn interp_closure_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 261 let add = fn(x) x + base end print(add(266)) end", 0);
    assert_eq!(out, "527\n");
}
#[test]
fn interp_closure_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 262 let add = fn(x) x + base end print(add(267)) end", 0);
    assert_eq!(out, "529\n");
}
#[test]
fn interp_closure_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 263 let add = fn(x) x + base end print(add(268)) end", 0);
    assert_eq!(out, "531\n");
}
#[test]
fn interp_closure_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 264 let add = fn(x) x + base end print(add(269)) end", 0);
    assert_eq!(out, "533\n");
}
#[test]
fn interp_closure_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 265 let add = fn(x) x + base end print(add(270)) end", 0);
    assert_eq!(out, "535\n");
}
#[test]
fn interp_closure_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 266 let add = fn(x) x + base end print(add(271)) end", 0);
    assert_eq!(out, "537\n");
}
#[test]
fn interp_closure_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 267 let add = fn(x) x + base end print(add(272)) end", 0);
    assert_eq!(out, "539\n");
}
#[test]
fn interp_closure_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 268 let add = fn(x) x + base end print(add(273)) end", 0);
    assert_eq!(out, "541\n");
}
#[test]
fn interp_closure_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 269 let add = fn(x) x + base end print(add(274)) end", 0);
    assert_eq!(out, "543\n");
}
#[test]
fn interp_closure_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 270 let add = fn(x) x + base end print(add(275)) end", 0);
    assert_eq!(out, "545\n");
}
#[test]
fn interp_closure_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 271 let add = fn(x) x + base end print(add(276)) end", 0);
    assert_eq!(out, "547\n");
}
#[test]
fn interp_closure_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 272 let add = fn(x) x + base end print(add(277)) end", 0);
    assert_eq!(out, "549\n");
}
#[test]
fn interp_closure_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 273 let add = fn(x) x + base end print(add(278)) end", 0);
    assert_eq!(out, "551\n");
}
#[test]
fn interp_closure_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 274 let add = fn(x) x + base end print(add(279)) end", 0);
    assert_eq!(out, "553\n");
}
#[test]
fn interp_closure_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 275 let add = fn(x) x + base end print(add(280)) end", 0);
    assert_eq!(out, "555\n");
}
#[test]
fn interp_closure_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 276 let add = fn(x) x + base end print(add(281)) end", 0);
    assert_eq!(out, "557\n");
}
#[test]
fn interp_closure_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 277 let add = fn(x) x + base end print(add(282)) end", 0);
    assert_eq!(out, "559\n");
}
#[test]
fn interp_closure_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 278 let add = fn(x) x + base end print(add(283)) end", 0);
    assert_eq!(out, "561\n");
}
#[test]
fn interp_closure_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 279 let add = fn(x) x + base end print(add(284)) end", 0);
    assert_eq!(out, "563\n");
}
#[test]
fn interp_closure_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 280 let add = fn(x) x + base end print(add(285)) end", 0);
    assert_eq!(out, "565\n");
}
#[test]
fn interp_closure_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 281 let add = fn(x) x + base end print(add(286)) end", 0);
    assert_eq!(out, "567\n");
}
#[test]
fn interp_closure_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 282 let add = fn(x) x + base end print(add(287)) end", 0);
    assert_eq!(out, "569\n");
}
#[test]
fn interp_closure_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 283 let add = fn(x) x + base end print(add(288)) end", 0);
    assert_eq!(out, "571\n");
}
#[test]
fn interp_closure_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 284 let add = fn(x) x + base end print(add(289)) end", 0);
    assert_eq!(out, "573\n");
}
#[test]
fn interp_closure_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 285 let add = fn(x) x + base end print(add(290)) end", 0);
    assert_eq!(out, "575\n");
}
#[test]
fn interp_closure_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 286 let add = fn(x) x + base end print(add(291)) end", 0);
    assert_eq!(out, "577\n");
}
#[test]
fn interp_closure_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 287 let add = fn(x) x + base end print(add(292)) end", 0);
    assert_eq!(out, "579\n");
}
#[test]
fn interp_closure_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 288 let add = fn(x) x + base end print(add(293)) end", 0);
    assert_eq!(out, "581\n");
}
#[test]
fn interp_closure_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 289 let add = fn(x) x + base end print(add(294)) end", 0);
    assert_eq!(out, "583\n");
}
#[test]
fn interp_closure_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 290 let add = fn(x) x + base end print(add(295)) end", 0);
    assert_eq!(out, "585\n");
}
#[test]
fn interp_closure_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 291 let add = fn(x) x + base end print(add(296)) end", 0);
    assert_eq!(out, "587\n");
}
#[test]
fn interp_closure_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 292 let add = fn(x) x + base end print(add(297)) end", 0);
    assert_eq!(out, "589\n");
}
#[test]
fn interp_closure_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 293 let add = fn(x) x + base end print(add(298)) end", 0);
    assert_eq!(out, "591\n");
}
#[test]
fn interp_closure_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 294 let add = fn(x) x + base end print(add(299)) end", 0);
    assert_eq!(out, "593\n");
}
#[test]
fn interp_closure_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 295 let add = fn(x) x + base end print(add(300)) end", 0);
    assert_eq!(out, "595\n");
}
#[test]
fn interp_closure_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 296 let add = fn(x) x + base end print(add(301)) end", 0);
    assert_eq!(out, "597\n");
}
#[test]
fn interp_closure_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 297 let add = fn(x) x + base end print(add(302)) end", 0);
    assert_eq!(out, "599\n");
}
#[test]
fn interp_closure_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 298 let add = fn(x) x + base end print(add(303)) end", 0);
    assert_eq!(out, "601\n");
}
#[test]
fn interp_closure_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 299 let add = fn(x) x + base end print(add(304)) end", 0);
    assert_eq!(out, "603\n");
}
#[test]
fn interp_closure_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 300 let add = fn(x) x + base end print(add(305)) end", 0);
    assert_eq!(out, "605\n");
}
#[test]
fn interp_closure_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 301 let add = fn(x) x + base end print(add(306)) end", 0);
    assert_eq!(out, "607\n");
}
#[test]
fn interp_closure_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 302 let add = fn(x) x + base end print(add(307)) end", 0);
    assert_eq!(out, "609\n");
}
#[test]
fn interp_closure_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 303 let add = fn(x) x + base end print(add(308)) end", 0);
    assert_eq!(out, "611\n");
}
#[test]
fn interp_closure_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 304 let add = fn(x) x + base end print(add(309)) end", 0);
    assert_eq!(out, "613\n");
}
#[test]
fn interp_closure_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 305 let add = fn(x) x + base end print(add(310)) end", 0);
    assert_eq!(out, "615\n");
}
#[test]
fn interp_closure_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 306 let add = fn(x) x + base end print(add(311)) end", 0);
    assert_eq!(out, "617\n");
}
#[test]
fn interp_closure_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 307 let add = fn(x) x + base end print(add(312)) end", 0);
    assert_eq!(out, "619\n");
}
#[test]
fn interp_closure_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 308 let add = fn(x) x + base end print(add(313)) end", 0);
    assert_eq!(out, "621\n");
}
#[test]
fn interp_closure_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 309 let add = fn(x) x + base end print(add(314)) end", 0);
    assert_eq!(out, "623\n");
}
#[test]
fn interp_closure_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 310 let add = fn(x) x + base end print(add(315)) end", 0);
    assert_eq!(out, "625\n");
}
#[test]
fn interp_closure_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 311 let add = fn(x) x + base end print(add(316)) end", 0);
    assert_eq!(out, "627\n");
}
#[test]
fn interp_closure_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 312 let add = fn(x) x + base end print(add(317)) end", 0);
    assert_eq!(out, "629\n");
}
#[test]
fn interp_closure_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 313 let add = fn(x) x + base end print(add(318)) end", 0);
    assert_eq!(out, "631\n");
}
#[test]
fn interp_closure_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 314 let add = fn(x) x + base end print(add(319)) end", 0);
    assert_eq!(out, "633\n");
}
#[test]
fn interp_closure_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 315 let add = fn(x) x + base end print(add(320)) end", 0);
    assert_eq!(out, "635\n");
}
#[test]
fn interp_closure_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 316 let add = fn(x) x + base end print(add(321)) end", 0);
    assert_eq!(out, "637\n");
}
#[test]
fn interp_closure_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 317 let add = fn(x) x + base end print(add(322)) end", 0);
    assert_eq!(out, "639\n");
}
#[test]
fn interp_closure_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 318 let add = fn(x) x + base end print(add(323)) end", 0);
    assert_eq!(out, "641\n");
}
#[test]
fn interp_closure_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 319 let add = fn(x) x + base end print(add(324)) end", 0);
    assert_eq!(out, "643\n");
}
#[test]
fn interp_closure_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 320 let add = fn(x) x + base end print(add(325)) end", 0);
    assert_eq!(out, "645\n");
}
#[test]
fn interp_closure_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 321 let add = fn(x) x + base end print(add(326)) end", 0);
    assert_eq!(out, "647\n");
}
#[test]
fn interp_closure_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 322 let add = fn(x) x + base end print(add(327)) end", 0);
    assert_eq!(out, "649\n");
}
#[test]
fn interp_closure_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 323 let add = fn(x) x + base end print(add(328)) end", 0);
    assert_eq!(out, "651\n");
}
#[test]
fn interp_closure_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 324 let add = fn(x) x + base end print(add(329)) end", 0);
    assert_eq!(out, "653\n");
}
#[test]
fn interp_closure_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 325 let add = fn(x) x + base end print(add(330)) end", 0);
    assert_eq!(out, "655\n");
}
#[test]
fn interp_closure_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 326 let add = fn(x) x + base end print(add(331)) end", 0);
    assert_eq!(out, "657\n");
}
#[test]
fn interp_closure_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 327 let add = fn(x) x + base end print(add(332)) end", 0);
    assert_eq!(out, "659\n");
}
#[test]
fn interp_closure_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 328 let add = fn(x) x + base end print(add(333)) end", 0);
    assert_eq!(out, "661\n");
}
#[test]
fn interp_closure_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 329 let add = fn(x) x + base end print(add(334)) end", 0);
    assert_eq!(out, "663\n");
}
#[test]
fn interp_closure_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 330 let add = fn(x) x + base end print(add(335)) end", 0);
    assert_eq!(out, "665\n");
}
#[test]
fn interp_closure_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 331 let add = fn(x) x + base end print(add(336)) end", 0);
    assert_eq!(out, "667\n");
}
#[test]
fn interp_closure_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 332 let add = fn(x) x + base end print(add(337)) end", 0);
    assert_eq!(out, "669\n");
}
#[test]
fn interp_closure_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 333 let add = fn(x) x + base end print(add(338)) end", 0);
    assert_eq!(out, "671\n");
}
#[test]
fn interp_closure_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 334 let add = fn(x) x + base end print(add(339)) end", 0);
    assert_eq!(out, "673\n");
}
#[test]
fn interp_closure_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 335 let add = fn(x) x + base end print(add(340)) end", 0);
    assert_eq!(out, "675\n");
}
#[test]
fn interp_closure_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 336 let add = fn(x) x + base end print(add(341)) end", 0);
    assert_eq!(out, "677\n");
}
#[test]
fn interp_closure_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 337 let add = fn(x) x + base end print(add(342)) end", 0);
    assert_eq!(out, "679\n");
}
#[test]
fn interp_closure_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 338 let add = fn(x) x + base end print(add(343)) end", 0);
    assert_eq!(out, "681\n");
}
#[test]
fn interp_closure_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 339 let add = fn(x) x + base end print(add(344)) end", 0);
    assert_eq!(out, "683\n");
}
#[test]
fn interp_closure_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 340 let add = fn(x) x + base end print(add(345)) end", 0);
    assert_eq!(out, "685\n");
}
#[test]
fn interp_closure_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 341 let add = fn(x) x + base end print(add(346)) end", 0);
    assert_eq!(out, "687\n");
}
#[test]
fn interp_closure_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 342 let add = fn(x) x + base end print(add(347)) end", 0);
    assert_eq!(out, "689\n");
}
#[test]
fn interp_closure_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 343 let add = fn(x) x + base end print(add(348)) end", 0);
    assert_eq!(out, "691\n");
}
#[test]
fn interp_closure_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 344 let add = fn(x) x + base end print(add(349)) end", 0);
    assert_eq!(out, "693\n");
}
#[test]
fn interp_closure_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 345 let add = fn(x) x + base end print(add(350)) end", 0);
    assert_eq!(out, "695\n");
}
#[test]
fn interp_closure_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 346 let add = fn(x) x + base end print(add(351)) end", 0);
    assert_eq!(out, "697\n");
}
#[test]
fn interp_closure_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 347 let add = fn(x) x + base end print(add(352)) end", 0);
    assert_eq!(out, "699\n");
}
#[test]
fn interp_closure_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 348 let add = fn(x) x + base end print(add(353)) end", 0);
    assert_eq!(out, "701\n");
}
#[test]
fn interp_closure_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 349 let add = fn(x) x + base end print(add(354)) end", 0);
    assert_eq!(out, "703\n");
}
#[test]
fn interp_closure_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 350 let add = fn(x) x + base end print(add(355)) end", 0);
    assert_eq!(out, "705\n");
}
#[test]
fn interp_closure_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 351 let add = fn(x) x + base end print(add(356)) end", 0);
    assert_eq!(out, "707\n");
}
#[test]
fn interp_closure_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 352 let add = fn(x) x + base end print(add(357)) end", 0);
    assert_eq!(out, "709\n");
}
#[test]
fn interp_closure_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 353 let add = fn(x) x + base end print(add(358)) end", 0);
    assert_eq!(out, "711\n");
}
#[test]
fn interp_closure_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 354 let add = fn(x) x + base end print(add(359)) end", 0);
    assert_eq!(out, "713\n");
}
#[test]
fn interp_closure_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 355 let add = fn(x) x + base end print(add(360)) end", 0);
    assert_eq!(out, "715\n");
}
#[test]
fn interp_closure_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 356 let add = fn(x) x + base end print(add(361)) end", 0);
    assert_eq!(out, "717\n");
}
#[test]
fn interp_closure_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 357 let add = fn(x) x + base end print(add(362)) end", 0);
    assert_eq!(out, "719\n");
}
#[test]
fn interp_closure_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 358 let add = fn(x) x + base end print(add(363)) end", 0);
    assert_eq!(out, "721\n");
}
#[test]
fn interp_closure_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 359 let add = fn(x) x + base end print(add(364)) end", 0);
    assert_eq!(out, "723\n");
}
#[test]
fn interp_closure_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 360 let add = fn(x) x + base end print(add(365)) end", 0);
    assert_eq!(out, "725\n");
}
#[test]
fn interp_closure_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 361 let add = fn(x) x + base end print(add(366)) end", 0);
    assert_eq!(out, "727\n");
}
#[test]
fn interp_closure_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 362 let add = fn(x) x + base end print(add(367)) end", 0);
    assert_eq!(out, "729\n");
}
#[test]
fn interp_closure_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 363 let add = fn(x) x + base end print(add(368)) end", 0);
    assert_eq!(out, "731\n");
}
#[test]
fn interp_closure_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 364 let add = fn(x) x + base end print(add(369)) end", 0);
    assert_eq!(out, "733\n");
}
#[test]
fn interp_closure_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 365 let add = fn(x) x + base end print(add(370)) end", 0);
    assert_eq!(out, "735\n");
}
#[test]
fn interp_closure_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 366 let add = fn(x) x + base end print(add(371)) end", 0);
    assert_eq!(out, "737\n");
}
#[test]
fn interp_closure_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 367 let add = fn(x) x + base end print(add(372)) end", 0);
    assert_eq!(out, "739\n");
}
#[test]
fn interp_closure_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 368 let add = fn(x) x + base end print(add(373)) end", 0);
    assert_eq!(out, "741\n");
}
#[test]
fn interp_closure_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 369 let add = fn(x) x + base end print(add(374)) end", 0);
    assert_eq!(out, "743\n");
}
#[test]
fn interp_closure_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 370 let add = fn(x) x + base end print(add(375)) end", 0);
    assert_eq!(out, "745\n");
}
#[test]
fn interp_closure_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 371 let add = fn(x) x + base end print(add(376)) end", 0);
    assert_eq!(out, "747\n");
}
#[test]
fn interp_closure_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 372 let add = fn(x) x + base end print(add(377)) end", 0);
    assert_eq!(out, "749\n");
}
#[test]
fn interp_closure_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 373 let add = fn(x) x + base end print(add(378)) end", 0);
    assert_eq!(out, "751\n");
}
#[test]
fn interp_closure_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 374 let add = fn(x) x + base end print(add(379)) end", 0);
    assert_eq!(out, "753\n");
}
#[test]
fn interp_closure_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 375 let add = fn(x) x + base end print(add(380)) end", 0);
    assert_eq!(out, "755\n");
}
#[test]
fn interp_closure_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 376 let add = fn(x) x + base end print(add(381)) end", 0);
    assert_eq!(out, "757\n");
}
#[test]
fn interp_closure_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 377 let add = fn(x) x + base end print(add(382)) end", 0);
    assert_eq!(out, "759\n");
}
#[test]
fn interp_closure_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 378 let add = fn(x) x + base end print(add(383)) end", 0);
    assert_eq!(out, "761\n");
}
#[test]
fn interp_closure_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 379 let add = fn(x) x + base end print(add(384)) end", 0);
    assert_eq!(out, "763\n");
}
#[test]
fn interp_closure_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 380 let add = fn(x) x + base end print(add(385)) end", 0);
    assert_eq!(out, "765\n");
}
#[test]
fn interp_closure_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 381 let add = fn(x) x + base end print(add(386)) end", 0);
    assert_eq!(out, "767\n");
}
#[test]
fn interp_closure_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 382 let add = fn(x) x + base end print(add(387)) end", 0);
    assert_eq!(out, "769\n");
}
#[test]
fn interp_closure_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 383 let add = fn(x) x + base end print(add(388)) end", 0);
    assert_eq!(out, "771\n");
}
#[test]
fn interp_closure_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 384 let add = fn(x) x + base end print(add(389)) end", 0);
    assert_eq!(out, "773\n");
}
#[test]
fn interp_closure_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 385 let add = fn(x) x + base end print(add(390)) end", 0);
    assert_eq!(out, "775\n");
}
#[test]
fn interp_closure_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 386 let add = fn(x) x + base end print(add(391)) end", 0);
    assert_eq!(out, "777\n");
}
#[test]
fn interp_closure_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 387 let add = fn(x) x + base end print(add(392)) end", 0);
    assert_eq!(out, "779\n");
}
#[test]
fn interp_closure_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 388 let add = fn(x) x + base end print(add(393)) end", 0);
    assert_eq!(out, "781\n");
}
#[test]
fn interp_closure_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 389 let add = fn(x) x + base end print(add(394)) end", 0);
    assert_eq!(out, "783\n");
}
#[test]
fn interp_closure_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 390 let add = fn(x) x + base end print(add(395)) end", 0);
    assert_eq!(out, "785\n");
}
#[test]
fn interp_closure_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 391 let add = fn(x) x + base end print(add(396)) end", 0);
    assert_eq!(out, "787\n");
}
#[test]
fn interp_closure_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 392 let add = fn(x) x + base end print(add(397)) end", 0);
    assert_eq!(out, "789\n");
}
#[test]
fn interp_closure_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 393 let add = fn(x) x + base end print(add(398)) end", 0);
    assert_eq!(out, "791\n");
}
#[test]
fn interp_closure_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 394 let add = fn(x) x + base end print(add(399)) end", 0);
    assert_eq!(out, "793\n");
}
#[test]
fn interp_closure_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 395 let add = fn(x) x + base end print(add(400)) end", 0);
    assert_eq!(out, "795\n");
}
#[test]
fn interp_closure_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 396 let add = fn(x) x + base end print(add(401)) end", 0);
    assert_eq!(out, "797\n");
}
#[test]
fn interp_closure_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 397 let add = fn(x) x + base end print(add(402)) end", 0);
    assert_eq!(out, "799\n");
}
#[test]
fn interp_closure_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 398 let add = fn(x) x + base end print(add(403)) end", 0);
    assert_eq!(out, "801\n");
}
#[test]
fn interp_closure_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 399 let add = fn(x) x + base end print(add(404)) end", 0);
    assert_eq!(out, "803\n");
}
#[test]
fn interp_closure_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 400 let add = fn(x) x + base end print(add(405)) end", 0);
    assert_eq!(out, "805\n");
}
#[test]
fn interp_closure_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 401 let add = fn(x) x + base end print(add(406)) end", 0);
    assert_eq!(out, "807\n");
}
#[test]
fn interp_closure_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 402 let add = fn(x) x + base end print(add(407)) end", 0);
    assert_eq!(out, "809\n");
}
#[test]
fn interp_closure_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 403 let add = fn(x) x + base end print(add(408)) end", 0);
    assert_eq!(out, "811\n");
}
#[test]
fn interp_closure_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 404 let add = fn(x) x + base end print(add(409)) end", 0);
    assert_eq!(out, "813\n");
}
#[test]
fn interp_closure_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 405 let add = fn(x) x + base end print(add(410)) end", 0);
    assert_eq!(out, "815\n");
}
#[test]
fn interp_closure_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 406 let add = fn(x) x + base end print(add(411)) end", 0);
    assert_eq!(out, "817\n");
}
#[test]
fn interp_closure_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 407 let add = fn(x) x + base end print(add(412)) end", 0);
    assert_eq!(out, "819\n");
}
#[test]
fn interp_closure_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 408 let add = fn(x) x + base end print(add(413)) end", 0);
    assert_eq!(out, "821\n");
}
#[test]
fn interp_closure_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 409 let add = fn(x) x + base end print(add(414)) end", 0);
    assert_eq!(out, "823\n");
}
#[test]
fn interp_closure_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 410 let add = fn(x) x + base end print(add(415)) end", 0);
    assert_eq!(out, "825\n");
}
#[test]
fn interp_closure_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 411 let add = fn(x) x + base end print(add(416)) end", 0);
    assert_eq!(out, "827\n");
}
#[test]
fn interp_closure_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 412 let add = fn(x) x + base end print(add(417)) end", 0);
    assert_eq!(out, "829\n");
}
#[test]
fn interp_closure_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 413 let add = fn(x) x + base end print(add(418)) end", 0);
    assert_eq!(out, "831\n");
}
#[test]
fn interp_closure_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 414 let add = fn(x) x + base end print(add(419)) end", 0);
    assert_eq!(out, "833\n");
}
#[test]
fn interp_closure_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 415 let add = fn(x) x + base end print(add(420)) end", 0);
    assert_eq!(out, "835\n");
}
#[test]
fn interp_closure_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 416 let add = fn(x) x + base end print(add(421)) end", 0);
    assert_eq!(out, "837\n");
}
#[test]
fn interp_closure_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 417 let add = fn(x) x + base end print(add(422)) end", 0);
    assert_eq!(out, "839\n");
}
#[test]
fn interp_closure_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 418 let add = fn(x) x + base end print(add(423)) end", 0);
    assert_eq!(out, "841\n");
}
#[test]
fn interp_closure_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 419 let add = fn(x) x + base end print(add(424)) end", 0);
    assert_eq!(out, "843\n");
}
#[test]
fn interp_closure_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 420 let add = fn(x) x + base end print(add(425)) end", 0);
    assert_eq!(out, "845\n");
}
#[test]
fn interp_closure_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 421 let add = fn(x) x + base end print(add(426)) end", 0);
    assert_eq!(out, "847\n");
}
#[test]
fn interp_closure_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 422 let add = fn(x) x + base end print(add(427)) end", 0);
    assert_eq!(out, "849\n");
}
#[test]
fn interp_closure_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 423 let add = fn(x) x + base end print(add(428)) end", 0);
    assert_eq!(out, "851\n");
}
#[test]
fn interp_closure_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 424 let add = fn(x) x + base end print(add(429)) end", 0);
    assert_eq!(out, "853\n");
}
#[test]
fn interp_closure_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 425 let add = fn(x) x + base end print(add(430)) end", 0);
    assert_eq!(out, "855\n");
}
#[test]
fn interp_closure_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 426 let add = fn(x) x + base end print(add(431)) end", 0);
    assert_eq!(out, "857\n");
}
#[test]
fn interp_closure_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 427 let add = fn(x) x + base end print(add(432)) end", 0);
    assert_eq!(out, "859\n");
}
#[test]
fn interp_closure_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 428 let add = fn(x) x + base end print(add(433)) end", 0);
    assert_eq!(out, "861\n");
}
#[test]
fn interp_closure_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 429 let add = fn(x) x + base end print(add(434)) end", 0);
    assert_eq!(out, "863\n");
}
#[test]
fn interp_closure_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 430 let add = fn(x) x + base end print(add(435)) end", 0);
    assert_eq!(out, "865\n");
}
#[test]
fn interp_closure_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 431 let add = fn(x) x + base end print(add(436)) end", 0);
    assert_eq!(out, "867\n");
}
#[test]
fn interp_closure_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 432 let add = fn(x) x + base end print(add(437)) end", 0);
    assert_eq!(out, "869\n");
}
#[test]
fn interp_closure_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 433 let add = fn(x) x + base end print(add(438)) end", 0);
    assert_eq!(out, "871\n");
}
#[test]
fn interp_closure_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 434 let add = fn(x) x + base end print(add(439)) end", 0);
    assert_eq!(out, "873\n");
}
#[test]
fn interp_closure_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 435 let add = fn(x) x + base end print(add(440)) end", 0);
    assert_eq!(out, "875\n");
}
#[test]
fn interp_closure_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 436 let add = fn(x) x + base end print(add(441)) end", 0);
    assert_eq!(out, "877\n");
}
#[test]
fn interp_closure_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 437 let add = fn(x) x + base end print(add(442)) end", 0);
    assert_eq!(out, "879\n");
}
#[test]
fn interp_closure_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 438 let add = fn(x) x + base end print(add(443)) end", 0);
    assert_eq!(out, "881\n");
}
#[test]
fn interp_closure_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 439 let add = fn(x) x + base end print(add(444)) end", 0);
    assert_eq!(out, "883\n");
}
#[test]
fn interp_closure_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 440 let add = fn(x) x + base end print(add(445)) end", 0);
    assert_eq!(out, "885\n");
}
#[test]
fn interp_closure_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 441 let add = fn(x) x + base end print(add(446)) end", 0);
    assert_eq!(out, "887\n");
}
#[test]
fn interp_closure_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 442 let add = fn(x) x + base end print(add(447)) end", 0);
    assert_eq!(out, "889\n");
}
#[test]
fn interp_closure_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 443 let add = fn(x) x + base end print(add(448)) end", 0);
    assert_eq!(out, "891\n");
}
#[test]
fn interp_closure_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 444 let add = fn(x) x + base end print(add(449)) end", 0);
    assert_eq!(out, "893\n");
}
#[test]
fn interp_closure_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 445 let add = fn(x) x + base end print(add(450)) end", 0);
    assert_eq!(out, "895\n");
}
#[test]
fn interp_closure_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 446 let add = fn(x) x + base end print(add(451)) end", 0);
    assert_eq!(out, "897\n");
}
#[test]
fn interp_closure_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 447 let add = fn(x) x + base end print(add(452)) end", 0);
    assert_eq!(out, "899\n");
}
#[test]
fn interp_closure_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 448 let add = fn(x) x + base end print(add(453)) end", 0);
    assert_eq!(out, "901\n");
}
#[test]
fn interp_closure_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 449 let add = fn(x) x + base end print(add(454)) end", 0);
    assert_eq!(out, "903\n");
}
#[test]
fn interp_closure_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 450 let add = fn(x) x + base end print(add(455)) end", 0);
    assert_eq!(out, "905\n");
}
#[test]
fn interp_closure_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 451 let add = fn(x) x + base end print(add(456)) end", 0);
    assert_eq!(out, "907\n");
}
#[test]
fn interp_closure_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 452 let add = fn(x) x + base end print(add(457)) end", 0);
    assert_eq!(out, "909\n");
}
#[test]
fn interp_closure_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 453 let add = fn(x) x + base end print(add(458)) end", 0);
    assert_eq!(out, "911\n");
}
#[test]
fn interp_closure_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 454 let add = fn(x) x + base end print(add(459)) end", 0);
    assert_eq!(out, "913\n");
}
#[test]
fn interp_closure_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 455 let add = fn(x) x + base end print(add(460)) end", 0);
    assert_eq!(out, "915\n");
}
#[test]
fn interp_closure_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 456 let add = fn(x) x + base end print(add(461)) end", 0);
    assert_eq!(out, "917\n");
}
#[test]
fn interp_closure_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 457 let add = fn(x) x + base end print(add(462)) end", 0);
    assert_eq!(out, "919\n");
}
#[test]
fn interp_closure_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 458 let add = fn(x) x + base end print(add(463)) end", 0);
    assert_eq!(out, "921\n");
}
#[test]
fn interp_closure_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 459 let add = fn(x) x + base end print(add(464)) end", 0);
    assert_eq!(out, "923\n");
}
#[test]
fn interp_closure_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 460 let add = fn(x) x + base end print(add(465)) end", 0);
    assert_eq!(out, "925\n");
}
#[test]
fn interp_closure_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 461 let add = fn(x) x + base end print(add(466)) end", 0);
    assert_eq!(out, "927\n");
}
#[test]
fn interp_closure_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 462 let add = fn(x) x + base end print(add(467)) end", 0);
    assert_eq!(out, "929\n");
}
#[test]
fn interp_closure_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 463 let add = fn(x) x + base end print(add(468)) end", 0);
    assert_eq!(out, "931\n");
}
#[test]
fn interp_closure_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 464 let add = fn(x) x + base end print(add(469)) end", 0);
    assert_eq!(out, "933\n");
}
#[test]
fn interp_closure_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 465 let add = fn(x) x + base end print(add(470)) end", 0);
    assert_eq!(out, "935\n");
}
#[test]
fn interp_closure_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 466 let add = fn(x) x + base end print(add(471)) end", 0);
    assert_eq!(out, "937\n");
}
#[test]
fn interp_closure_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 467 let add = fn(x) x + base end print(add(472)) end", 0);
    assert_eq!(out, "939\n");
}
#[test]
fn interp_closure_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 468 let add = fn(x) x + base end print(add(473)) end", 0);
    assert_eq!(out, "941\n");
}
#[test]
fn interp_closure_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 469 let add = fn(x) x + base end print(add(474)) end", 0);
    assert_eq!(out, "943\n");
}
#[test]
fn interp_closure_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 470 let add = fn(x) x + base end print(add(475)) end", 0);
    assert_eq!(out, "945\n");
}
#[test]
fn interp_closure_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 471 let add = fn(x) x + base end print(add(476)) end", 0);
    assert_eq!(out, "947\n");
}
#[test]
fn interp_closure_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 472 let add = fn(x) x + base end print(add(477)) end", 0);
    assert_eq!(out, "949\n");
}
#[test]
fn interp_closure_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 473 let add = fn(x) x + base end print(add(478)) end", 0);
    assert_eq!(out, "951\n");
}
#[test]
fn interp_closure_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 474 let add = fn(x) x + base end print(add(479)) end", 0);
    assert_eq!(out, "953\n");
}
#[test]
fn interp_closure_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 475 let add = fn(x) x + base end print(add(480)) end", 0);
    assert_eq!(out, "955\n");
}
#[test]
fn interp_closure_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 476 let add = fn(x) x + base end print(add(481)) end", 0);
    assert_eq!(out, "957\n");
}
#[test]
fn interp_closure_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 477 let add = fn(x) x + base end print(add(482)) end", 0);
    assert_eq!(out, "959\n");
}
#[test]
fn interp_closure_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 478 let add = fn(x) x + base end print(add(483)) end", 0);
    assert_eq!(out, "961\n");
}
#[test]
fn interp_closure_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 479 let add = fn(x) x + base end print(add(484)) end", 0);
    assert_eq!(out, "963\n");
}
#[test]
fn interp_closure_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 480 let add = fn(x) x + base end print(add(485)) end", 0);
    assert_eq!(out, "965\n");
}
#[test]
fn interp_closure_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 481 let add = fn(x) x + base end print(add(486)) end", 0);
    assert_eq!(out, "967\n");
}
#[test]
fn interp_closure_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 482 let add = fn(x) x + base end print(add(487)) end", 0);
    assert_eq!(out, "969\n");
}
#[test]
fn interp_closure_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 483 let add = fn(x) x + base end print(add(488)) end", 0);
    assert_eq!(out, "971\n");
}
#[test]
fn interp_closure_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 484 let add = fn(x) x + base end print(add(489)) end", 0);
    assert_eq!(out, "973\n");
}
#[test]
fn interp_closure_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 485 let add = fn(x) x + base end print(add(490)) end", 0);
    assert_eq!(out, "975\n");
}
#[test]
fn interp_closure_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 486 let add = fn(x) x + base end print(add(491)) end", 0);
    assert_eq!(out, "977\n");
}
#[test]
fn interp_closure_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 487 let add = fn(x) x + base end print(add(492)) end", 0);
    assert_eq!(out, "979\n");
}
#[test]
fn interp_closure_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 488 let add = fn(x) x + base end print(add(493)) end", 0);
    assert_eq!(out, "981\n");
}
#[test]
fn interp_closure_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 489 let add = fn(x) x + base end print(add(494)) end", 0);
    assert_eq!(out, "983\n");
}
#[test]
fn interp_closure_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 490 let add = fn(x) x + base end print(add(495)) end", 0);
    assert_eq!(out, "985\n");
}
#[test]
fn interp_closure_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 491 let add = fn(x) x + base end print(add(496)) end", 0);
    assert_eq!(out, "987\n");
}
#[test]
fn interp_closure_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 492 let add = fn(x) x + base end print(add(497)) end", 0);
    assert_eq!(out, "989\n");
}
#[test]
fn interp_closure_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 493 let add = fn(x) x + base end print(add(498)) end", 0);
    assert_eq!(out, "991\n");
}
#[test]
fn interp_closure_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 494 let add = fn(x) x + base end print(add(499)) end", 0);
    assert_eq!(out, "993\n");
}
#[test]
fn interp_closure_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 495 let add = fn(x) x + base end print(add(500)) end", 0);
    assert_eq!(out, "995\n");
}
#[test]
fn interp_closure_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 496 let add = fn(x) x + base end print(add(501)) end", 0);
    assert_eq!(out, "997\n");
}
#[test]
fn interp_closure_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 497 let add = fn(x) x + base end print(add(502)) end", 0);
    assert_eq!(out, "999\n");
}
#[test]
fn interp_closure_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 498 let add = fn(x) x + base end print(add(503)) end", 0);
    assert_eq!(out, "1001\n");
}
#[test]
fn interp_closure_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() let base = 499 let add = fn(x) x + base end print(add(504)) end", 0);
    assert_eq!(out, "1003\n");
}
#[test]
fn interp_bool_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and true) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_bool_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(true and true) end", 0);
    assert_eq!(out, "true\n");
}
#[test]
fn interp_bool_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(false and false) end", 0);
    assert_eq!(out, "false\n");
}
#[test]
fn interp_print_args_0() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(0, 1, 2) end", 0);
    assert_eq!(out, "0 1 2\n");
}
#[test]
fn interp_print_args_1() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(1, 2, 3) end", 0);
    assert_eq!(out, "1 2 3\n");
}
#[test]
fn interp_print_args_2() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(2, 3, 4) end", 0);
    assert_eq!(out, "2 3 4\n");
}
#[test]
fn interp_print_args_3() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(3, 4, 5) end", 0);
    assert_eq!(out, "3 4 5\n");
}
#[test]
fn interp_print_args_4() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(4, 5, 6) end", 0);
    assert_eq!(out, "4 5 6\n");
}
#[test]
fn interp_print_args_5() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(5, 6, 7) end", 0);
    assert_eq!(out, "5 6 7\n");
}
#[test]
fn interp_print_args_6() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(6, 7, 8) end", 0);
    assert_eq!(out, "6 7 8\n");
}
#[test]
fn interp_print_args_7() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(7, 8, 9) end", 0);
    assert_eq!(out, "7 8 9\n");
}
#[test]
fn interp_print_args_8() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(8, 9, 10) end", 0);
    assert_eq!(out, "8 9 10\n");
}
#[test]
fn interp_print_args_9() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(9, 10, 11) end", 0);
    assert_eq!(out, "9 10 11\n");
}
#[test]
fn interp_print_args_10() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(10, 11, 12) end", 0);
    assert_eq!(out, "10 11 12\n");
}
#[test]
fn interp_print_args_11() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(11, 12, 13) end", 0);
    assert_eq!(out, "11 12 13\n");
}
#[test]
fn interp_print_args_12() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(12, 13, 14) end", 0);
    assert_eq!(out, "12 13 14\n");
}
#[test]
fn interp_print_args_13() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(13, 14, 15) end", 0);
    assert_eq!(out, "13 14 15\n");
}
#[test]
fn interp_print_args_14() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(14, 15, 16) end", 0);
    assert_eq!(out, "14 15 16\n");
}
#[test]
fn interp_print_args_15() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(15, 16, 17) end", 0);
    assert_eq!(out, "15 16 17\n");
}
#[test]
fn interp_print_args_16() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(16, 17, 18) end", 0);
    assert_eq!(out, "16 17 18\n");
}
#[test]
fn interp_print_args_17() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(17, 18, 19) end", 0);
    assert_eq!(out, "17 18 19\n");
}
#[test]
fn interp_print_args_18() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(18, 19, 20) end", 0);
    assert_eq!(out, "18 19 20\n");
}
#[test]
fn interp_print_args_19() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(19, 20, 21) end", 0);
    assert_eq!(out, "19 20 21\n");
}
#[test]
fn interp_print_args_20() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(20, 21, 22) end", 0);
    assert_eq!(out, "20 21 22\n");
}
#[test]
fn interp_print_args_21() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(21, 22, 23) end", 0);
    assert_eq!(out, "21 22 23\n");
}
#[test]
fn interp_print_args_22() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(22, 23, 24) end", 0);
    assert_eq!(out, "22 23 24\n");
}
#[test]
fn interp_print_args_23() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(23, 24, 25) end", 0);
    assert_eq!(out, "23 24 25\n");
}
#[test]
fn interp_print_args_24() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(24, 25, 26) end", 0);
    assert_eq!(out, "24 25 26\n");
}
#[test]
fn interp_print_args_25() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(25, 26, 27) end", 0);
    assert_eq!(out, "25 26 27\n");
}
#[test]
fn interp_print_args_26() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(26, 27, 28) end", 0);
    assert_eq!(out, "26 27 28\n");
}
#[test]
fn interp_print_args_27() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(27, 28, 29) end", 0);
    assert_eq!(out, "27 28 29\n");
}
#[test]
fn interp_print_args_28() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(28, 29, 30) end", 0);
    assert_eq!(out, "28 29 30\n");
}
#[test]
fn interp_print_args_29() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(29, 30, 31) end", 0);
    assert_eq!(out, "29 30 31\n");
}
#[test]
fn interp_print_args_30() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(30, 31, 32) end", 0);
    assert_eq!(out, "30 31 32\n");
}
#[test]
fn interp_print_args_31() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(31, 32, 33) end", 0);
    assert_eq!(out, "31 32 33\n");
}
#[test]
fn interp_print_args_32() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(32, 33, 34) end", 0);
    assert_eq!(out, "32 33 34\n");
}
#[test]
fn interp_print_args_33() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(33, 34, 35) end", 0);
    assert_eq!(out, "33 34 35\n");
}
#[test]
fn interp_print_args_34() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(34, 35, 36) end", 0);
    assert_eq!(out, "34 35 36\n");
}
#[test]
fn interp_print_args_35() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(35, 36, 37) end", 0);
    assert_eq!(out, "35 36 37\n");
}
#[test]
fn interp_print_args_36() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(36, 37, 38) end", 0);
    assert_eq!(out, "36 37 38\n");
}
#[test]
fn interp_print_args_37() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(37, 38, 39) end", 0);
    assert_eq!(out, "37 38 39\n");
}
#[test]
fn interp_print_args_38() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(38, 39, 40) end", 0);
    assert_eq!(out, "38 39 40\n");
}
#[test]
fn interp_print_args_39() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(39, 40, 41) end", 0);
    assert_eq!(out, "39 40 41\n");
}
#[test]
fn interp_print_args_40() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(40, 41, 42) end", 0);
    assert_eq!(out, "40 41 42\n");
}
#[test]
fn interp_print_args_41() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(41, 42, 43) end", 0);
    assert_eq!(out, "41 42 43\n");
}
#[test]
fn interp_print_args_42() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(42, 43, 44) end", 0);
    assert_eq!(out, "42 43 44\n");
}
#[test]
fn interp_print_args_43() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(43, 44, 45) end", 0);
    assert_eq!(out, "43 44 45\n");
}
#[test]
fn interp_print_args_44() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(44, 45, 46) end", 0);
    assert_eq!(out, "44 45 46\n");
}
#[test]
fn interp_print_args_45() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(45, 46, 47) end", 0);
    assert_eq!(out, "45 46 47\n");
}
#[test]
fn interp_print_args_46() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(46, 47, 48) end", 0);
    assert_eq!(out, "46 47 48\n");
}
#[test]
fn interp_print_args_47() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(47, 48, 49) end", 0);
    assert_eq!(out, "47 48 49\n");
}
#[test]
fn interp_print_args_48() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(48, 49, 50) end", 0);
    assert_eq!(out, "48 49 50\n");
}
#[test]
fn interp_print_args_49() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(49, 50, 51) end", 0);
    assert_eq!(out, "49 50 51\n");
}
#[test]
fn interp_print_args_50() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(50, 51, 52) end", 0);
    assert_eq!(out, "50 51 52\n");
}
#[test]
fn interp_print_args_51() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(51, 52, 53) end", 0);
    assert_eq!(out, "51 52 53\n");
}
#[test]
fn interp_print_args_52() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(52, 53, 54) end", 0);
    assert_eq!(out, "52 53 54\n");
}
#[test]
fn interp_print_args_53() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(53, 54, 55) end", 0);
    assert_eq!(out, "53 54 55\n");
}
#[test]
fn interp_print_args_54() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(54, 55, 56) end", 0);
    assert_eq!(out, "54 55 56\n");
}
#[test]
fn interp_print_args_55() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(55, 56, 57) end", 0);
    assert_eq!(out, "55 56 57\n");
}
#[test]
fn interp_print_args_56() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(56, 57, 58) end", 0);
    assert_eq!(out, "56 57 58\n");
}
#[test]
fn interp_print_args_57() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(57, 58, 59) end", 0);
    assert_eq!(out, "57 58 59\n");
}
#[test]
fn interp_print_args_58() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(58, 59, 60) end", 0);
    assert_eq!(out, "58 59 60\n");
}
#[test]
fn interp_print_args_59() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(59, 60, 61) end", 0);
    assert_eq!(out, "59 60 61\n");
}
#[test]
fn interp_print_args_60() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(60, 61, 62) end", 0);
    assert_eq!(out, "60 61 62\n");
}
#[test]
fn interp_print_args_61() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(61, 62, 63) end", 0);
    assert_eq!(out, "61 62 63\n");
}
#[test]
fn interp_print_args_62() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(62, 63, 64) end", 0);
    assert_eq!(out, "62 63 64\n");
}
#[test]
fn interp_print_args_63() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(63, 64, 65) end", 0);
    assert_eq!(out, "63 64 65\n");
}
#[test]
fn interp_print_args_64() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(64, 65, 66) end", 0);
    assert_eq!(out, "64 65 66\n");
}
#[test]
fn interp_print_args_65() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(65, 66, 67) end", 0);
    assert_eq!(out, "65 66 67\n");
}
#[test]
fn interp_print_args_66() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(66, 67, 68) end", 0);
    assert_eq!(out, "66 67 68\n");
}
#[test]
fn interp_print_args_67() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(67, 68, 69) end", 0);
    assert_eq!(out, "67 68 69\n");
}
#[test]
fn interp_print_args_68() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(68, 69, 70) end", 0);
    assert_eq!(out, "68 69 70\n");
}
#[test]
fn interp_print_args_69() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(69, 70, 71) end", 0);
    assert_eq!(out, "69 70 71\n");
}
#[test]
fn interp_print_args_70() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(70, 71, 72) end", 0);
    assert_eq!(out, "70 71 72\n");
}
#[test]
fn interp_print_args_71() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(71, 72, 73) end", 0);
    assert_eq!(out, "71 72 73\n");
}
#[test]
fn interp_print_args_72() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(72, 73, 74) end", 0);
    assert_eq!(out, "72 73 74\n");
}
#[test]
fn interp_print_args_73() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(73, 74, 75) end", 0);
    assert_eq!(out, "73 74 75\n");
}
#[test]
fn interp_print_args_74() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(74, 75, 76) end", 0);
    assert_eq!(out, "74 75 76\n");
}
#[test]
fn interp_print_args_75() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(75, 76, 77) end", 0);
    assert_eq!(out, "75 76 77\n");
}
#[test]
fn interp_print_args_76() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(76, 77, 78) end", 0);
    assert_eq!(out, "76 77 78\n");
}
#[test]
fn interp_print_args_77() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(77, 78, 79) end", 0);
    assert_eq!(out, "77 78 79\n");
}
#[test]
fn interp_print_args_78() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(78, 79, 80) end", 0);
    assert_eq!(out, "78 79 80\n");
}
#[test]
fn interp_print_args_79() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(79, 80, 81) end", 0);
    assert_eq!(out, "79 80 81\n");
}
#[test]
fn interp_print_args_80() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(80, 81, 82) end", 0);
    assert_eq!(out, "80 81 82\n");
}
#[test]
fn interp_print_args_81() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(81, 82, 83) end", 0);
    assert_eq!(out, "81 82 83\n");
}
#[test]
fn interp_print_args_82() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(82, 83, 84) end", 0);
    assert_eq!(out, "82 83 84\n");
}
#[test]
fn interp_print_args_83() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(83, 84, 85) end", 0);
    assert_eq!(out, "83 84 85\n");
}
#[test]
fn interp_print_args_84() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(84, 85, 86) end", 0);
    assert_eq!(out, "84 85 86\n");
}
#[test]
fn interp_print_args_85() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(85, 86, 87) end", 0);
    assert_eq!(out, "85 86 87\n");
}
#[test]
fn interp_print_args_86() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(86, 87, 88) end", 0);
    assert_eq!(out, "86 87 88\n");
}
#[test]
fn interp_print_args_87() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(87, 88, 89) end", 0);
    assert_eq!(out, "87 88 89\n");
}
#[test]
fn interp_print_args_88() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(88, 89, 90) end", 0);
    assert_eq!(out, "88 89 90\n");
}
#[test]
fn interp_print_args_89() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(89, 90, 91) end", 0);
    assert_eq!(out, "89 90 91\n");
}
#[test]
fn interp_print_args_90() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(90, 91, 92) end", 0);
    assert_eq!(out, "90 91 92\n");
}
#[test]
fn interp_print_args_91() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(91, 92, 93) end", 0);
    assert_eq!(out, "91 92 93\n");
}
#[test]
fn interp_print_args_92() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(92, 93, 94) end", 0);
    assert_eq!(out, "92 93 94\n");
}
#[test]
fn interp_print_args_93() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(93, 94, 95) end", 0);
    assert_eq!(out, "93 94 95\n");
}
#[test]
fn interp_print_args_94() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(94, 95, 96) end", 0);
    assert_eq!(out, "94 95 96\n");
}
#[test]
fn interp_print_args_95() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(95, 96, 97) end", 0);
    assert_eq!(out, "95 96 97\n");
}
#[test]
fn interp_print_args_96() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(96, 97, 98) end", 0);
    assert_eq!(out, "96 97 98\n");
}
#[test]
fn interp_print_args_97() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(97, 98, 99) end", 0);
    assert_eq!(out, "97 98 99\n");
}
#[test]
fn interp_print_args_98() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(98, 99, 100) end", 0);
    assert_eq!(out, "98 99 100\n");
}
#[test]
fn interp_print_args_99() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(99, 100, 101) end", 0);
    assert_eq!(out, "99 100 101\n");
}
#[test]
fn interp_print_args_100() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(100, 101, 102) end", 0);
    assert_eq!(out, "100 101 102\n");
}
#[test]
fn interp_print_args_101() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(101, 102, 103) end", 0);
    assert_eq!(out, "101 102 103\n");
}
#[test]
fn interp_print_args_102() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(102, 103, 104) end", 0);
    assert_eq!(out, "102 103 104\n");
}
#[test]
fn interp_print_args_103() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(103, 104, 105) end", 0);
    assert_eq!(out, "103 104 105\n");
}
#[test]
fn interp_print_args_104() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(104, 105, 106) end", 0);
    assert_eq!(out, "104 105 106\n");
}
#[test]
fn interp_print_args_105() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(105, 106, 107) end", 0);
    assert_eq!(out, "105 106 107\n");
}
#[test]
fn interp_print_args_106() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(106, 107, 108) end", 0);
    assert_eq!(out, "106 107 108\n");
}
#[test]
fn interp_print_args_107() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(107, 108, 109) end", 0);
    assert_eq!(out, "107 108 109\n");
}
#[test]
fn interp_print_args_108() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(108, 109, 110) end", 0);
    assert_eq!(out, "108 109 110\n");
}
#[test]
fn interp_print_args_109() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(109, 110, 111) end", 0);
    assert_eq!(out, "109 110 111\n");
}
#[test]
fn interp_print_args_110() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(110, 111, 112) end", 0);
    assert_eq!(out, "110 111 112\n");
}
#[test]
fn interp_print_args_111() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(111, 112, 113) end", 0);
    assert_eq!(out, "111 112 113\n");
}
#[test]
fn interp_print_args_112() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(112, 113, 114) end", 0);
    assert_eq!(out, "112 113 114\n");
}
#[test]
fn interp_print_args_113() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(113, 114, 115) end", 0);
    assert_eq!(out, "113 114 115\n");
}
#[test]
fn interp_print_args_114() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(114, 115, 116) end", 0);
    assert_eq!(out, "114 115 116\n");
}
#[test]
fn interp_print_args_115() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(115, 116, 117) end", 0);
    assert_eq!(out, "115 116 117\n");
}
#[test]
fn interp_print_args_116() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(116, 117, 118) end", 0);
    assert_eq!(out, "116 117 118\n");
}
#[test]
fn interp_print_args_117() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(117, 118, 119) end", 0);
    assert_eq!(out, "117 118 119\n");
}
#[test]
fn interp_print_args_118() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(118, 119, 120) end", 0);
    assert_eq!(out, "118 119 120\n");
}
#[test]
fn interp_print_args_119() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(119, 120, 121) end", 0);
    assert_eq!(out, "119 120 121\n");
}
#[test]
fn interp_print_args_120() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(120, 121, 122) end", 0);
    assert_eq!(out, "120 121 122\n");
}
#[test]
fn interp_print_args_121() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(121, 122, 123) end", 0);
    assert_eq!(out, "121 122 123\n");
}
#[test]
fn interp_print_args_122() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(122, 123, 124) end", 0);
    assert_eq!(out, "122 123 124\n");
}
#[test]
fn interp_print_args_123() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(123, 124, 125) end", 0);
    assert_eq!(out, "123 124 125\n");
}
#[test]
fn interp_print_args_124() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(124, 125, 126) end", 0);
    assert_eq!(out, "124 125 126\n");
}
#[test]
fn interp_print_args_125() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(125, 126, 127) end", 0);
    assert_eq!(out, "125 126 127\n");
}
#[test]
fn interp_print_args_126() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(126, 127, 128) end", 0);
    assert_eq!(out, "126 127 128\n");
}
#[test]
fn interp_print_args_127() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(127, 128, 129) end", 0);
    assert_eq!(out, "127 128 129\n");
}
#[test]
fn interp_print_args_128() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(128, 129, 130) end", 0);
    assert_eq!(out, "128 129 130\n");
}
#[test]
fn interp_print_args_129() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(129, 130, 131) end", 0);
    assert_eq!(out, "129 130 131\n");
}
#[test]
fn interp_print_args_130() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(130, 131, 132) end", 0);
    assert_eq!(out, "130 131 132\n");
}
#[test]
fn interp_print_args_131() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(131, 132, 133) end", 0);
    assert_eq!(out, "131 132 133\n");
}
#[test]
fn interp_print_args_132() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(132, 133, 134) end", 0);
    assert_eq!(out, "132 133 134\n");
}
#[test]
fn interp_print_args_133() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(133, 134, 135) end", 0);
    assert_eq!(out, "133 134 135\n");
}
#[test]
fn interp_print_args_134() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(134, 135, 136) end", 0);
    assert_eq!(out, "134 135 136\n");
}
#[test]
fn interp_print_args_135() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(135, 136, 137) end", 0);
    assert_eq!(out, "135 136 137\n");
}
#[test]
fn interp_print_args_136() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(136, 137, 138) end", 0);
    assert_eq!(out, "136 137 138\n");
}
#[test]
fn interp_print_args_137() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(137, 138, 139) end", 0);
    assert_eq!(out, "137 138 139\n");
}
#[test]
fn interp_print_args_138() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(138, 139, 140) end", 0);
    assert_eq!(out, "138 139 140\n");
}
#[test]
fn interp_print_args_139() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(139, 140, 141) end", 0);
    assert_eq!(out, "139 140 141\n");
}
#[test]
fn interp_print_args_140() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(140, 141, 142) end", 0);
    assert_eq!(out, "140 141 142\n");
}
#[test]
fn interp_print_args_141() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(141, 142, 143) end", 0);
    assert_eq!(out, "141 142 143\n");
}
#[test]
fn interp_print_args_142() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(142, 143, 144) end", 0);
    assert_eq!(out, "142 143 144\n");
}
#[test]
fn interp_print_args_143() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(143, 144, 145) end", 0);
    assert_eq!(out, "143 144 145\n");
}
#[test]
fn interp_print_args_144() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(144, 145, 146) end", 0);
    assert_eq!(out, "144 145 146\n");
}
#[test]
fn interp_print_args_145() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(145, 146, 147) end", 0);
    assert_eq!(out, "145 146 147\n");
}
#[test]
fn interp_print_args_146() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(146, 147, 148) end", 0);
    assert_eq!(out, "146 147 148\n");
}
#[test]
fn interp_print_args_147() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(147, 148, 149) end", 0);
    assert_eq!(out, "147 148 149\n");
}
#[test]
fn interp_print_args_148() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(148, 149, 150) end", 0);
    assert_eq!(out, "148 149 150\n");
}
#[test]
fn interp_print_args_149() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(149, 150, 151) end", 0);
    assert_eq!(out, "149 150 151\n");
}
#[test]
fn interp_print_args_150() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(150, 151, 152) end", 0);
    assert_eq!(out, "150 151 152\n");
}
#[test]
fn interp_print_args_151() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(151, 152, 153) end", 0);
    assert_eq!(out, "151 152 153\n");
}
#[test]
fn interp_print_args_152() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(152, 153, 154) end", 0);
    assert_eq!(out, "152 153 154\n");
}
#[test]
fn interp_print_args_153() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(153, 154, 155) end", 0);
    assert_eq!(out, "153 154 155\n");
}
#[test]
fn interp_print_args_154() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(154, 155, 156) end", 0);
    assert_eq!(out, "154 155 156\n");
}
#[test]
fn interp_print_args_155() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(155, 156, 157) end", 0);
    assert_eq!(out, "155 156 157\n");
}
#[test]
fn interp_print_args_156() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(156, 157, 158) end", 0);
    assert_eq!(out, "156 157 158\n");
}
#[test]
fn interp_print_args_157() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(157, 158, 159) end", 0);
    assert_eq!(out, "157 158 159\n");
}
#[test]
fn interp_print_args_158() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(158, 159, 160) end", 0);
    assert_eq!(out, "158 159 160\n");
}
#[test]
fn interp_print_args_159() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(159, 160, 161) end", 0);
    assert_eq!(out, "159 160 161\n");
}
#[test]
fn interp_print_args_160() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(160, 161, 162) end", 0);
    assert_eq!(out, "160 161 162\n");
}
#[test]
fn interp_print_args_161() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(161, 162, 163) end", 0);
    assert_eq!(out, "161 162 163\n");
}
#[test]
fn interp_print_args_162() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(162, 163, 164) end", 0);
    assert_eq!(out, "162 163 164\n");
}
#[test]
fn interp_print_args_163() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(163, 164, 165) end", 0);
    assert_eq!(out, "163 164 165\n");
}
#[test]
fn interp_print_args_164() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(164, 165, 166) end", 0);
    assert_eq!(out, "164 165 166\n");
}
#[test]
fn interp_print_args_165() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(165, 166, 167) end", 0);
    assert_eq!(out, "165 166 167\n");
}
#[test]
fn interp_print_args_166() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(166, 167, 168) end", 0);
    assert_eq!(out, "166 167 168\n");
}
#[test]
fn interp_print_args_167() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(167, 168, 169) end", 0);
    assert_eq!(out, "167 168 169\n");
}
#[test]
fn interp_print_args_168() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(168, 169, 170) end", 0);
    assert_eq!(out, "168 169 170\n");
}
#[test]
fn interp_print_args_169() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(169, 170, 171) end", 0);
    assert_eq!(out, "169 170 171\n");
}
#[test]
fn interp_print_args_170() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(170, 171, 172) end", 0);
    assert_eq!(out, "170 171 172\n");
}
#[test]
fn interp_print_args_171() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(171, 172, 173) end", 0);
    assert_eq!(out, "171 172 173\n");
}
#[test]
fn interp_print_args_172() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(172, 173, 174) end", 0);
    assert_eq!(out, "172 173 174\n");
}
#[test]
fn interp_print_args_173() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(173, 174, 175) end", 0);
    assert_eq!(out, "173 174 175\n");
}
#[test]
fn interp_print_args_174() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(174, 175, 176) end", 0);
    assert_eq!(out, "174 175 176\n");
}
#[test]
fn interp_print_args_175() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(175, 176, 177) end", 0);
    assert_eq!(out, "175 176 177\n");
}
#[test]
fn interp_print_args_176() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(176, 177, 178) end", 0);
    assert_eq!(out, "176 177 178\n");
}
#[test]
fn interp_print_args_177() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(177, 178, 179) end", 0);
    assert_eq!(out, "177 178 179\n");
}
#[test]
fn interp_print_args_178() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(178, 179, 180) end", 0);
    assert_eq!(out, "178 179 180\n");
}
#[test]
fn interp_print_args_179() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(179, 180, 181) end", 0);
    assert_eq!(out, "179 180 181\n");
}
#[test]
fn interp_print_args_180() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(180, 181, 182) end", 0);
    assert_eq!(out, "180 181 182\n");
}
#[test]
fn interp_print_args_181() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(181, 182, 183) end", 0);
    assert_eq!(out, "181 182 183\n");
}
#[test]
fn interp_print_args_182() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(182, 183, 184) end", 0);
    assert_eq!(out, "182 183 184\n");
}
#[test]
fn interp_print_args_183() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(183, 184, 185) end", 0);
    assert_eq!(out, "183 184 185\n");
}
#[test]
fn interp_print_args_184() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(184, 185, 186) end", 0);
    assert_eq!(out, "184 185 186\n");
}
#[test]
fn interp_print_args_185() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(185, 186, 187) end", 0);
    assert_eq!(out, "185 186 187\n");
}
#[test]
fn interp_print_args_186() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(186, 187, 188) end", 0);
    assert_eq!(out, "186 187 188\n");
}
#[test]
fn interp_print_args_187() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(187, 188, 189) end", 0);
    assert_eq!(out, "187 188 189\n");
}
#[test]
fn interp_print_args_188() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(188, 189, 190) end", 0);
    assert_eq!(out, "188 189 190\n");
}
#[test]
fn interp_print_args_189() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(189, 190, 191) end", 0);
    assert_eq!(out, "189 190 191\n");
}
#[test]
fn interp_print_args_190() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(190, 191, 192) end", 0);
    assert_eq!(out, "190 191 192\n");
}
#[test]
fn interp_print_args_191() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(191, 192, 193) end", 0);
    assert_eq!(out, "191 192 193\n");
}
#[test]
fn interp_print_args_192() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(192, 193, 194) end", 0);
    assert_eq!(out, "192 193 194\n");
}
#[test]
fn interp_print_args_193() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(193, 194, 195) end", 0);
    assert_eq!(out, "193 194 195\n");
}
#[test]
fn interp_print_args_194() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(194, 195, 196) end", 0);
    assert_eq!(out, "194 195 196\n");
}
#[test]
fn interp_print_args_195() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(195, 196, 197) end", 0);
    assert_eq!(out, "195 196 197\n");
}
#[test]
fn interp_print_args_196() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(196, 197, 198) end", 0);
    assert_eq!(out, "196 197 198\n");
}
#[test]
fn interp_print_args_197() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(197, 198, 199) end", 0);
    assert_eq!(out, "197 198 199\n");
}
#[test]
fn interp_print_args_198() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(198, 199, 200) end", 0);
    assert_eq!(out, "198 199 200\n");
}
#[test]
fn interp_print_args_199() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(199, 200, 201) end", 0);
    assert_eq!(out, "199 200 201\n");
}
#[test]
fn interp_print_args_200() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(200, 201, 202) end", 0);
    assert_eq!(out, "200 201 202\n");
}
#[test]
fn interp_print_args_201() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(201, 202, 203) end", 0);
    assert_eq!(out, "201 202 203\n");
}
#[test]
fn interp_print_args_202() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(202, 203, 204) end", 0);
    assert_eq!(out, "202 203 204\n");
}
#[test]
fn interp_print_args_203() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(203, 204, 205) end", 0);
    assert_eq!(out, "203 204 205\n");
}
#[test]
fn interp_print_args_204() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(204, 205, 206) end", 0);
    assert_eq!(out, "204 205 206\n");
}
#[test]
fn interp_print_args_205() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(205, 206, 207) end", 0);
    assert_eq!(out, "205 206 207\n");
}
#[test]
fn interp_print_args_206() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(206, 207, 208) end", 0);
    assert_eq!(out, "206 207 208\n");
}
#[test]
fn interp_print_args_207() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(207, 208, 209) end", 0);
    assert_eq!(out, "207 208 209\n");
}
#[test]
fn interp_print_args_208() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(208, 209, 210) end", 0);
    assert_eq!(out, "208 209 210\n");
}
#[test]
fn interp_print_args_209() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(209, 210, 211) end", 0);
    assert_eq!(out, "209 210 211\n");
}
#[test]
fn interp_print_args_210() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(210, 211, 212) end", 0);
    assert_eq!(out, "210 211 212\n");
}
#[test]
fn interp_print_args_211() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(211, 212, 213) end", 0);
    assert_eq!(out, "211 212 213\n");
}
#[test]
fn interp_print_args_212() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(212, 213, 214) end", 0);
    assert_eq!(out, "212 213 214\n");
}
#[test]
fn interp_print_args_213() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(213, 214, 215) end", 0);
    assert_eq!(out, "213 214 215\n");
}
#[test]
fn interp_print_args_214() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(214, 215, 216) end", 0);
    assert_eq!(out, "214 215 216\n");
}
#[test]
fn interp_print_args_215() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(215, 216, 217) end", 0);
    assert_eq!(out, "215 216 217\n");
}
#[test]
fn interp_print_args_216() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(216, 217, 218) end", 0);
    assert_eq!(out, "216 217 218\n");
}
#[test]
fn interp_print_args_217() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(217, 218, 219) end", 0);
    assert_eq!(out, "217 218 219\n");
}
#[test]
fn interp_print_args_218() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(218, 219, 220) end", 0);
    assert_eq!(out, "218 219 220\n");
}
#[test]
fn interp_print_args_219() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(219, 220, 221) end", 0);
    assert_eq!(out, "219 220 221\n");
}
#[test]
fn interp_print_args_220() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(220, 221, 222) end", 0);
    assert_eq!(out, "220 221 222\n");
}
#[test]
fn interp_print_args_221() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(221, 222, 223) end", 0);
    assert_eq!(out, "221 222 223\n");
}
#[test]
fn interp_print_args_222() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(222, 223, 224) end", 0);
    assert_eq!(out, "222 223 224\n");
}
#[test]
fn interp_print_args_223() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(223, 224, 225) end", 0);
    assert_eq!(out, "223 224 225\n");
}
#[test]
fn interp_print_args_224() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(224, 225, 226) end", 0);
    assert_eq!(out, "224 225 226\n");
}
#[test]
fn interp_print_args_225() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(225, 226, 227) end", 0);
    assert_eq!(out, "225 226 227\n");
}
#[test]
fn interp_print_args_226() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(226, 227, 228) end", 0);
    assert_eq!(out, "226 227 228\n");
}
#[test]
fn interp_print_args_227() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(227, 228, 229) end", 0);
    assert_eq!(out, "227 228 229\n");
}
#[test]
fn interp_print_args_228() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(228, 229, 230) end", 0);
    assert_eq!(out, "228 229 230\n");
}
#[test]
fn interp_print_args_229() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(229, 230, 231) end", 0);
    assert_eq!(out, "229 230 231\n");
}
#[test]
fn interp_print_args_230() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(230, 231, 232) end", 0);
    assert_eq!(out, "230 231 232\n");
}
#[test]
fn interp_print_args_231() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(231, 232, 233) end", 0);
    assert_eq!(out, "231 232 233\n");
}
#[test]
fn interp_print_args_232() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(232, 233, 234) end", 0);
    assert_eq!(out, "232 233 234\n");
}
#[test]
fn interp_print_args_233() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(233, 234, 235) end", 0);
    assert_eq!(out, "233 234 235\n");
}
#[test]
fn interp_print_args_234() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(234, 235, 236) end", 0);
    assert_eq!(out, "234 235 236\n");
}
#[test]
fn interp_print_args_235() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(235, 236, 237) end", 0);
    assert_eq!(out, "235 236 237\n");
}
#[test]
fn interp_print_args_236() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(236, 237, 238) end", 0);
    assert_eq!(out, "236 237 238\n");
}
#[test]
fn interp_print_args_237() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(237, 238, 239) end", 0);
    assert_eq!(out, "237 238 239\n");
}
#[test]
fn interp_print_args_238() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(238, 239, 240) end", 0);
    assert_eq!(out, "238 239 240\n");
}
#[test]
fn interp_print_args_239() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(239, 240, 241) end", 0);
    assert_eq!(out, "239 240 241\n");
}
#[test]
fn interp_print_args_240() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(240, 241, 242) end", 0);
    assert_eq!(out, "240 241 242\n");
}
#[test]
fn interp_print_args_241() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(241, 242, 243) end", 0);
    assert_eq!(out, "241 242 243\n");
}
#[test]
fn interp_print_args_242() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(242, 243, 244) end", 0);
    assert_eq!(out, "242 243 244\n");
}
#[test]
fn interp_print_args_243() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(243, 244, 245) end", 0);
    assert_eq!(out, "243 244 245\n");
}
#[test]
fn interp_print_args_244() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(244, 245, 246) end", 0);
    assert_eq!(out, "244 245 246\n");
}
#[test]
fn interp_print_args_245() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(245, 246, 247) end", 0);
    assert_eq!(out, "245 246 247\n");
}
#[test]
fn interp_print_args_246() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(246, 247, 248) end", 0);
    assert_eq!(out, "246 247 248\n");
}
#[test]
fn interp_print_args_247() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(247, 248, 249) end", 0);
    assert_eq!(out, "247 248 249\n");
}
#[test]
fn interp_print_args_248() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(248, 249, 250) end", 0);
    assert_eq!(out, "248 249 250\n");
}
#[test]
fn interp_print_args_249() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(249, 250, 251) end", 0);
    assert_eq!(out, "249 250 251\n");
}
#[test]
fn interp_print_args_250() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(250, 251, 252) end", 0);
    assert_eq!(out, "250 251 252\n");
}
#[test]
fn interp_print_args_251() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(251, 252, 253) end", 0);
    assert_eq!(out, "251 252 253\n");
}
#[test]
fn interp_print_args_252() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(252, 253, 254) end", 0);
    assert_eq!(out, "252 253 254\n");
}
#[test]
fn interp_print_args_253() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(253, 254, 255) end", 0);
    assert_eq!(out, "253 254 255\n");
}
#[test]
fn interp_print_args_254() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(254, 255, 256) end", 0);
    assert_eq!(out, "254 255 256\n");
}
#[test]
fn interp_print_args_255() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(255, 256, 257) end", 0);
    assert_eq!(out, "255 256 257\n");
}
#[test]
fn interp_print_args_256() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(256, 257, 258) end", 0);
    assert_eq!(out, "256 257 258\n");
}
#[test]
fn interp_print_args_257() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(257, 258, 259) end", 0);
    assert_eq!(out, "257 258 259\n");
}
#[test]
fn interp_print_args_258() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(258, 259, 260) end", 0);
    assert_eq!(out, "258 259 260\n");
}
#[test]
fn interp_print_args_259() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(259, 260, 261) end", 0);
    assert_eq!(out, "259 260 261\n");
}
#[test]
fn interp_print_args_260() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(260, 261, 262) end", 0);
    assert_eq!(out, "260 261 262\n");
}
#[test]
fn interp_print_args_261() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(261, 262, 263) end", 0);
    assert_eq!(out, "261 262 263\n");
}
#[test]
fn interp_print_args_262() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(262, 263, 264) end", 0);
    assert_eq!(out, "262 263 264\n");
}
#[test]
fn interp_print_args_263() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(263, 264, 265) end", 0);
    assert_eq!(out, "263 264 265\n");
}
#[test]
fn interp_print_args_264() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(264, 265, 266) end", 0);
    assert_eq!(out, "264 265 266\n");
}
#[test]
fn interp_print_args_265() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(265, 266, 267) end", 0);
    assert_eq!(out, "265 266 267\n");
}
#[test]
fn interp_print_args_266() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(266, 267, 268) end", 0);
    assert_eq!(out, "266 267 268\n");
}
#[test]
fn interp_print_args_267() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(267, 268, 269) end", 0);
    assert_eq!(out, "267 268 269\n");
}
#[test]
fn interp_print_args_268() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(268, 269, 270) end", 0);
    assert_eq!(out, "268 269 270\n");
}
#[test]
fn interp_print_args_269() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(269, 270, 271) end", 0);
    assert_eq!(out, "269 270 271\n");
}
#[test]
fn interp_print_args_270() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(270, 271, 272) end", 0);
    assert_eq!(out, "270 271 272\n");
}
#[test]
fn interp_print_args_271() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(271, 272, 273) end", 0);
    assert_eq!(out, "271 272 273\n");
}
#[test]
fn interp_print_args_272() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(272, 273, 274) end", 0);
    assert_eq!(out, "272 273 274\n");
}
#[test]
fn interp_print_args_273() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(273, 274, 275) end", 0);
    assert_eq!(out, "273 274 275\n");
}
#[test]
fn interp_print_args_274() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(274, 275, 276) end", 0);
    assert_eq!(out, "274 275 276\n");
}
#[test]
fn interp_print_args_275() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(275, 276, 277) end", 0);
    assert_eq!(out, "275 276 277\n");
}
#[test]
fn interp_print_args_276() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(276, 277, 278) end", 0);
    assert_eq!(out, "276 277 278\n");
}
#[test]
fn interp_print_args_277() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(277, 278, 279) end", 0);
    assert_eq!(out, "277 278 279\n");
}
#[test]
fn interp_print_args_278() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(278, 279, 280) end", 0);
    assert_eq!(out, "278 279 280\n");
}
#[test]
fn interp_print_args_279() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(279, 280, 281) end", 0);
    assert_eq!(out, "279 280 281\n");
}
#[test]
fn interp_print_args_280() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(280, 281, 282) end", 0);
    assert_eq!(out, "280 281 282\n");
}
#[test]
fn interp_print_args_281() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(281, 282, 283) end", 0);
    assert_eq!(out, "281 282 283\n");
}
#[test]
fn interp_print_args_282() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(282, 283, 284) end", 0);
    assert_eq!(out, "282 283 284\n");
}
#[test]
fn interp_print_args_283() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(283, 284, 285) end", 0);
    assert_eq!(out, "283 284 285\n");
}
#[test]
fn interp_print_args_284() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(284, 285, 286) end", 0);
    assert_eq!(out, "284 285 286\n");
}
#[test]
fn interp_print_args_285() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(285, 286, 287) end", 0);
    assert_eq!(out, "285 286 287\n");
}
#[test]
fn interp_print_args_286() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(286, 287, 288) end", 0);
    assert_eq!(out, "286 287 288\n");
}
#[test]
fn interp_print_args_287() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(287, 288, 289) end", 0);
    assert_eq!(out, "287 288 289\n");
}
#[test]
fn interp_print_args_288() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(288, 289, 290) end", 0);
    assert_eq!(out, "288 289 290\n");
}
#[test]
fn interp_print_args_289() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(289, 290, 291) end", 0);
    assert_eq!(out, "289 290 291\n");
}
#[test]
fn interp_print_args_290() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(290, 291, 292) end", 0);
    assert_eq!(out, "290 291 292\n");
}
#[test]
fn interp_print_args_291() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(291, 292, 293) end", 0);
    assert_eq!(out, "291 292 293\n");
}
#[test]
fn interp_print_args_292() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(292, 293, 294) end", 0);
    assert_eq!(out, "292 293 294\n");
}
#[test]
fn interp_print_args_293() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(293, 294, 295) end", 0);
    assert_eq!(out, "293 294 295\n");
}
#[test]
fn interp_print_args_294() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(294, 295, 296) end", 0);
    assert_eq!(out, "294 295 296\n");
}
#[test]
fn interp_print_args_295() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(295, 296, 297) end", 0);
    assert_eq!(out, "295 296 297\n");
}
#[test]
fn interp_print_args_296() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(296, 297, 298) end", 0);
    assert_eq!(out, "296 297 298\n");
}
#[test]
fn interp_print_args_297() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(297, 298, 299) end", 0);
    assert_eq!(out, "297 298 299\n");
}
#[test]
fn interp_print_args_298() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(298, 299, 300) end", 0);
    assert_eq!(out, "298 299 300\n");
}
#[test]
fn interp_print_args_299() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(299, 300, 301) end", 0);
    assert_eq!(out, "299 300 301\n");
}
#[test]
fn interp_print_args_300() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(300, 301, 302) end", 0);
    assert_eq!(out, "300 301 302\n");
}
#[test]
fn interp_print_args_301() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(301, 302, 303) end", 0);
    assert_eq!(out, "301 302 303\n");
}
#[test]
fn interp_print_args_302() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(302, 303, 304) end", 0);
    assert_eq!(out, "302 303 304\n");
}
#[test]
fn interp_print_args_303() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(303, 304, 305) end", 0);
    assert_eq!(out, "303 304 305\n");
}
#[test]
fn interp_print_args_304() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(304, 305, 306) end", 0);
    assert_eq!(out, "304 305 306\n");
}
#[test]
fn interp_print_args_305() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(305, 306, 307) end", 0);
    assert_eq!(out, "305 306 307\n");
}
#[test]
fn interp_print_args_306() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(306, 307, 308) end", 0);
    assert_eq!(out, "306 307 308\n");
}
#[test]
fn interp_print_args_307() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(307, 308, 309) end", 0);
    assert_eq!(out, "307 308 309\n");
}
#[test]
fn interp_print_args_308() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(308, 309, 310) end", 0);
    assert_eq!(out, "308 309 310\n");
}
#[test]
fn interp_print_args_309() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(309, 310, 311) end", 0);
    assert_eq!(out, "309 310 311\n");
}
#[test]
fn interp_print_args_310() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(310, 311, 312) end", 0);
    assert_eq!(out, "310 311 312\n");
}
#[test]
fn interp_print_args_311() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(311, 312, 313) end", 0);
    assert_eq!(out, "311 312 313\n");
}
#[test]
fn interp_print_args_312() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(312, 313, 314) end", 0);
    assert_eq!(out, "312 313 314\n");
}
#[test]
fn interp_print_args_313() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(313, 314, 315) end", 0);
    assert_eq!(out, "313 314 315\n");
}
#[test]
fn interp_print_args_314() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(314, 315, 316) end", 0);
    assert_eq!(out, "314 315 316\n");
}
#[test]
fn interp_print_args_315() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(315, 316, 317) end", 0);
    assert_eq!(out, "315 316 317\n");
}
#[test]
fn interp_print_args_316() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(316, 317, 318) end", 0);
    assert_eq!(out, "316 317 318\n");
}
#[test]
fn interp_print_args_317() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(317, 318, 319) end", 0);
    assert_eq!(out, "317 318 319\n");
}
#[test]
fn interp_print_args_318() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(318, 319, 320) end", 0);
    assert_eq!(out, "318 319 320\n");
}
#[test]
fn interp_print_args_319() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(319, 320, 321) end", 0);
    assert_eq!(out, "319 320 321\n");
}
#[test]
fn interp_print_args_320() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(320, 321, 322) end", 0);
    assert_eq!(out, "320 321 322\n");
}
#[test]
fn interp_print_args_321() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(321, 322, 323) end", 0);
    assert_eq!(out, "321 322 323\n");
}
#[test]
fn interp_print_args_322() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(322, 323, 324) end", 0);
    assert_eq!(out, "322 323 324\n");
}
#[test]
fn interp_print_args_323() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(323, 324, 325) end", 0);
    assert_eq!(out, "323 324 325\n");
}
#[test]
fn interp_print_args_324() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(324, 325, 326) end", 0);
    assert_eq!(out, "324 325 326\n");
}
#[test]
fn interp_print_args_325() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(325, 326, 327) end", 0);
    assert_eq!(out, "325 326 327\n");
}
#[test]
fn interp_print_args_326() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(326, 327, 328) end", 0);
    assert_eq!(out, "326 327 328\n");
}
#[test]
fn interp_print_args_327() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(327, 328, 329) end", 0);
    assert_eq!(out, "327 328 329\n");
}
#[test]
fn interp_print_args_328() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(328, 329, 330) end", 0);
    assert_eq!(out, "328 329 330\n");
}
#[test]
fn interp_print_args_329() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(329, 330, 331) end", 0);
    assert_eq!(out, "329 330 331\n");
}
#[test]
fn interp_print_args_330() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(330, 331, 332) end", 0);
    assert_eq!(out, "330 331 332\n");
}
#[test]
fn interp_print_args_331() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(331, 332, 333) end", 0);
    assert_eq!(out, "331 332 333\n");
}
#[test]
fn interp_print_args_332() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(332, 333, 334) end", 0);
    assert_eq!(out, "332 333 334\n");
}
#[test]
fn interp_print_args_333() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(333, 334, 335) end", 0);
    assert_eq!(out, "333 334 335\n");
}
#[test]
fn interp_print_args_334() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(334, 335, 336) end", 0);
    assert_eq!(out, "334 335 336\n");
}
#[test]
fn interp_print_args_335() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(335, 336, 337) end", 0);
    assert_eq!(out, "335 336 337\n");
}
#[test]
fn interp_print_args_336() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(336, 337, 338) end", 0);
    assert_eq!(out, "336 337 338\n");
}
#[test]
fn interp_print_args_337() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(337, 338, 339) end", 0);
    assert_eq!(out, "337 338 339\n");
}
#[test]
fn interp_print_args_338() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(338, 339, 340) end", 0);
    assert_eq!(out, "338 339 340\n");
}
#[test]
fn interp_print_args_339() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(339, 340, 341) end", 0);
    assert_eq!(out, "339 340 341\n");
}
#[test]
fn interp_print_args_340() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(340, 341, 342) end", 0);
    assert_eq!(out, "340 341 342\n");
}
#[test]
fn interp_print_args_341() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(341, 342, 343) end", 0);
    assert_eq!(out, "341 342 343\n");
}
#[test]
fn interp_print_args_342() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(342, 343, 344) end", 0);
    assert_eq!(out, "342 343 344\n");
}
#[test]
fn interp_print_args_343() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(343, 344, 345) end", 0);
    assert_eq!(out, "343 344 345\n");
}
#[test]
fn interp_print_args_344() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(344, 345, 346) end", 0);
    assert_eq!(out, "344 345 346\n");
}
#[test]
fn interp_print_args_345() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(345, 346, 347) end", 0);
    assert_eq!(out, "345 346 347\n");
}
#[test]
fn interp_print_args_346() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(346, 347, 348) end", 0);
    assert_eq!(out, "346 347 348\n");
}
#[test]
fn interp_print_args_347() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(347, 348, 349) end", 0);
    assert_eq!(out, "347 348 349\n");
}
#[test]
fn interp_print_args_348() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(348, 349, 350) end", 0);
    assert_eq!(out, "348 349 350\n");
}
#[test]
fn interp_print_args_349() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(349, 350, 351) end", 0);
    assert_eq!(out, "349 350 351\n");
}
#[test]
fn interp_print_args_350() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(350, 351, 352) end", 0);
    assert_eq!(out, "350 351 352\n");
}
#[test]
fn interp_print_args_351() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(351, 352, 353) end", 0);
    assert_eq!(out, "351 352 353\n");
}
#[test]
fn interp_print_args_352() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(352, 353, 354) end", 0);
    assert_eq!(out, "352 353 354\n");
}
#[test]
fn interp_print_args_353() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(353, 354, 355) end", 0);
    assert_eq!(out, "353 354 355\n");
}
#[test]
fn interp_print_args_354() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(354, 355, 356) end", 0);
    assert_eq!(out, "354 355 356\n");
}
#[test]
fn interp_print_args_355() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(355, 356, 357) end", 0);
    assert_eq!(out, "355 356 357\n");
}
#[test]
fn interp_print_args_356() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(356, 357, 358) end", 0);
    assert_eq!(out, "356 357 358\n");
}
#[test]
fn interp_print_args_357() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(357, 358, 359) end", 0);
    assert_eq!(out, "357 358 359\n");
}
#[test]
fn interp_print_args_358() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(358, 359, 360) end", 0);
    assert_eq!(out, "358 359 360\n");
}
#[test]
fn interp_print_args_359() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(359, 360, 361) end", 0);
    assert_eq!(out, "359 360 361\n");
}
#[test]
fn interp_print_args_360() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(360, 361, 362) end", 0);
    assert_eq!(out, "360 361 362\n");
}
#[test]
fn interp_print_args_361() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(361, 362, 363) end", 0);
    assert_eq!(out, "361 362 363\n");
}
#[test]
fn interp_print_args_362() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(362, 363, 364) end", 0);
    assert_eq!(out, "362 363 364\n");
}
#[test]
fn interp_print_args_363() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(363, 364, 365) end", 0);
    assert_eq!(out, "363 364 365\n");
}
#[test]
fn interp_print_args_364() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(364, 365, 366) end", 0);
    assert_eq!(out, "364 365 366\n");
}
#[test]
fn interp_print_args_365() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(365, 366, 367) end", 0);
    assert_eq!(out, "365 366 367\n");
}
#[test]
fn interp_print_args_366() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(366, 367, 368) end", 0);
    assert_eq!(out, "366 367 368\n");
}
#[test]
fn interp_print_args_367() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(367, 368, 369) end", 0);
    assert_eq!(out, "367 368 369\n");
}
#[test]
fn interp_print_args_368() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(368, 369, 370) end", 0);
    assert_eq!(out, "368 369 370\n");
}
#[test]
fn interp_print_args_369() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(369, 370, 371) end", 0);
    assert_eq!(out, "369 370 371\n");
}
#[test]
fn interp_print_args_370() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(370, 371, 372) end", 0);
    assert_eq!(out, "370 371 372\n");
}
#[test]
fn interp_print_args_371() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(371, 372, 373) end", 0);
    assert_eq!(out, "371 372 373\n");
}
#[test]
fn interp_print_args_372() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(372, 373, 374) end", 0);
    assert_eq!(out, "372 373 374\n");
}
#[test]
fn interp_print_args_373() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(373, 374, 375) end", 0);
    assert_eq!(out, "373 374 375\n");
}
#[test]
fn interp_print_args_374() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(374, 375, 376) end", 0);
    assert_eq!(out, "374 375 376\n");
}
#[test]
fn interp_print_args_375() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(375, 376, 377) end", 0);
    assert_eq!(out, "375 376 377\n");
}
#[test]
fn interp_print_args_376() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(376, 377, 378) end", 0);
    assert_eq!(out, "376 377 378\n");
}
#[test]
fn interp_print_args_377() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(377, 378, 379) end", 0);
    assert_eq!(out, "377 378 379\n");
}
#[test]
fn interp_print_args_378() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(378, 379, 380) end", 0);
    assert_eq!(out, "378 379 380\n");
}
#[test]
fn interp_print_args_379() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(379, 380, 381) end", 0);
    assert_eq!(out, "379 380 381\n");
}
#[test]
fn interp_print_args_380() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(380, 381, 382) end", 0);
    assert_eq!(out, "380 381 382\n");
}
#[test]
fn interp_print_args_381() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(381, 382, 383) end", 0);
    assert_eq!(out, "381 382 383\n");
}
#[test]
fn interp_print_args_382() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(382, 383, 384) end", 0);
    assert_eq!(out, "382 383 384\n");
}
#[test]
fn interp_print_args_383() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(383, 384, 385) end", 0);
    assert_eq!(out, "383 384 385\n");
}
#[test]
fn interp_print_args_384() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(384, 385, 386) end", 0);
    assert_eq!(out, "384 385 386\n");
}
#[test]
fn interp_print_args_385() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(385, 386, 387) end", 0);
    assert_eq!(out, "385 386 387\n");
}
#[test]
fn interp_print_args_386() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(386, 387, 388) end", 0);
    assert_eq!(out, "386 387 388\n");
}
#[test]
fn interp_print_args_387() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(387, 388, 389) end", 0);
    assert_eq!(out, "387 388 389\n");
}
#[test]
fn interp_print_args_388() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(388, 389, 390) end", 0);
    assert_eq!(out, "388 389 390\n");
}
#[test]
fn interp_print_args_389() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(389, 390, 391) end", 0);
    assert_eq!(out, "389 390 391\n");
}
#[test]
fn interp_print_args_390() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(390, 391, 392) end", 0);
    assert_eq!(out, "390 391 392\n");
}
#[test]
fn interp_print_args_391() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(391, 392, 393) end", 0);
    assert_eq!(out, "391 392 393\n");
}
#[test]
fn interp_print_args_392() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(392, 393, 394) end", 0);
    assert_eq!(out, "392 393 394\n");
}
#[test]
fn interp_print_args_393() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(393, 394, 395) end", 0);
    assert_eq!(out, "393 394 395\n");
}
#[test]
fn interp_print_args_394() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(394, 395, 396) end", 0);
    assert_eq!(out, "394 395 396\n");
}
#[test]
fn interp_print_args_395() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(395, 396, 397) end", 0);
    assert_eq!(out, "395 396 397\n");
}
#[test]
fn interp_print_args_396() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(396, 397, 398) end", 0);
    assert_eq!(out, "396 397 398\n");
}
#[test]
fn interp_print_args_397() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(397, 398, 399) end", 0);
    assert_eq!(out, "397 398 399\n");
}
#[test]
fn interp_print_args_398() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(398, 399, 400) end", 0);
    assert_eq!(out, "398 399 400\n");
}
#[test]
fn interp_print_args_399() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(399, 400, 401) end", 0);
    assert_eq!(out, "399 400 401\n");
}
#[test]
fn interp_print_args_400() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(400, 401, 402) end", 0);
    assert_eq!(out, "400 401 402\n");
}
#[test]
fn interp_print_args_401() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(401, 402, 403) end", 0);
    assert_eq!(out, "401 402 403\n");
}
#[test]
fn interp_print_args_402() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(402, 403, 404) end", 0);
    assert_eq!(out, "402 403 404\n");
}
#[test]
fn interp_print_args_403() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(403, 404, 405) end", 0);
    assert_eq!(out, "403 404 405\n");
}
#[test]
fn interp_print_args_404() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(404, 405, 406) end", 0);
    assert_eq!(out, "404 405 406\n");
}
#[test]
fn interp_print_args_405() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(405, 406, 407) end", 0);
    assert_eq!(out, "405 406 407\n");
}
#[test]
fn interp_print_args_406() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(406, 407, 408) end", 0);
    assert_eq!(out, "406 407 408\n");
}
#[test]
fn interp_print_args_407() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(407, 408, 409) end", 0);
    assert_eq!(out, "407 408 409\n");
}
#[test]
fn interp_print_args_408() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(408, 409, 410) end", 0);
    assert_eq!(out, "408 409 410\n");
}
#[test]
fn interp_print_args_409() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(409, 410, 411) end", 0);
    assert_eq!(out, "409 410 411\n");
}
#[test]
fn interp_print_args_410() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(410, 411, 412) end", 0);
    assert_eq!(out, "410 411 412\n");
}
#[test]
fn interp_print_args_411() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(411, 412, 413) end", 0);
    assert_eq!(out, "411 412 413\n");
}
#[test]
fn interp_print_args_412() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(412, 413, 414) end", 0);
    assert_eq!(out, "412 413 414\n");
}
#[test]
fn interp_print_args_413() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(413, 414, 415) end", 0);
    assert_eq!(out, "413 414 415\n");
}
#[test]
fn interp_print_args_414() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(414, 415, 416) end", 0);
    assert_eq!(out, "414 415 416\n");
}
#[test]
fn interp_print_args_415() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(415, 416, 417) end", 0);
    assert_eq!(out, "415 416 417\n");
}
#[test]
fn interp_print_args_416() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(416, 417, 418) end", 0);
    assert_eq!(out, "416 417 418\n");
}
#[test]
fn interp_print_args_417() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(417, 418, 419) end", 0);
    assert_eq!(out, "417 418 419\n");
}
#[test]
fn interp_print_args_418() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(418, 419, 420) end", 0);
    assert_eq!(out, "418 419 420\n");
}
#[test]
fn interp_print_args_419() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(419, 420, 421) end", 0);
    assert_eq!(out, "419 420 421\n");
}
#[test]
fn interp_print_args_420() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(420, 421, 422) end", 0);
    assert_eq!(out, "420 421 422\n");
}
#[test]
fn interp_print_args_421() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(421, 422, 423) end", 0);
    assert_eq!(out, "421 422 423\n");
}
#[test]
fn interp_print_args_422() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(422, 423, 424) end", 0);
    assert_eq!(out, "422 423 424\n");
}
#[test]
fn interp_print_args_423() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(423, 424, 425) end", 0);
    assert_eq!(out, "423 424 425\n");
}
#[test]
fn interp_print_args_424() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(424, 425, 426) end", 0);
    assert_eq!(out, "424 425 426\n");
}
#[test]
fn interp_print_args_425() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(425, 426, 427) end", 0);
    assert_eq!(out, "425 426 427\n");
}
#[test]
fn interp_print_args_426() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(426, 427, 428) end", 0);
    assert_eq!(out, "426 427 428\n");
}
#[test]
fn interp_print_args_427() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(427, 428, 429) end", 0);
    assert_eq!(out, "427 428 429\n");
}
#[test]
fn interp_print_args_428() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(428, 429, 430) end", 0);
    assert_eq!(out, "428 429 430\n");
}
#[test]
fn interp_print_args_429() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(429, 430, 431) end", 0);
    assert_eq!(out, "429 430 431\n");
}
#[test]
fn interp_print_args_430() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(430, 431, 432) end", 0);
    assert_eq!(out, "430 431 432\n");
}
#[test]
fn interp_print_args_431() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(431, 432, 433) end", 0);
    assert_eq!(out, "431 432 433\n");
}
#[test]
fn interp_print_args_432() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(432, 433, 434) end", 0);
    assert_eq!(out, "432 433 434\n");
}
#[test]
fn interp_print_args_433() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(433, 434, 435) end", 0);
    assert_eq!(out, "433 434 435\n");
}
#[test]
fn interp_print_args_434() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(434, 435, 436) end", 0);
    assert_eq!(out, "434 435 436\n");
}
#[test]
fn interp_print_args_435() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(435, 436, 437) end", 0);
    assert_eq!(out, "435 436 437\n");
}
#[test]
fn interp_print_args_436() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(436, 437, 438) end", 0);
    assert_eq!(out, "436 437 438\n");
}
#[test]
fn interp_print_args_437() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(437, 438, 439) end", 0);
    assert_eq!(out, "437 438 439\n");
}
#[test]
fn interp_print_args_438() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(438, 439, 440) end", 0);
    assert_eq!(out, "438 439 440\n");
}
#[test]
fn interp_print_args_439() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(439, 440, 441) end", 0);
    assert_eq!(out, "439 440 441\n");
}
#[test]
fn interp_print_args_440() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(440, 441, 442) end", 0);
    assert_eq!(out, "440 441 442\n");
}
#[test]
fn interp_print_args_441() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(441, 442, 443) end", 0);
    assert_eq!(out, "441 442 443\n");
}
#[test]
fn interp_print_args_442() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(442, 443, 444) end", 0);
    assert_eq!(out, "442 443 444\n");
}
#[test]
fn interp_print_args_443() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(443, 444, 445) end", 0);
    assert_eq!(out, "443 444 445\n");
}
#[test]
fn interp_print_args_444() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(444, 445, 446) end", 0);
    assert_eq!(out, "444 445 446\n");
}
#[test]
fn interp_print_args_445() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(445, 446, 447) end", 0);
    assert_eq!(out, "445 446 447\n");
}
#[test]
fn interp_print_args_446() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(446, 447, 448) end", 0);
    assert_eq!(out, "446 447 448\n");
}
#[test]
fn interp_print_args_447() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(447, 448, 449) end", 0);
    assert_eq!(out, "447 448 449\n");
}
#[test]
fn interp_print_args_448() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(448, 449, 450) end", 0);
    assert_eq!(out, "448 449 450\n");
}
#[test]
fn interp_print_args_449() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(449, 450, 451) end", 0);
    assert_eq!(out, "449 450 451\n");
}
#[test]
fn interp_print_args_450() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(450, 451, 452) end", 0);
    assert_eq!(out, "450 451 452\n");
}
#[test]
fn interp_print_args_451() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(451, 452, 453) end", 0);
    assert_eq!(out, "451 452 453\n");
}
#[test]
fn interp_print_args_452() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(452, 453, 454) end", 0);
    assert_eq!(out, "452 453 454\n");
}
#[test]
fn interp_print_args_453() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(453, 454, 455) end", 0);
    assert_eq!(out, "453 454 455\n");
}
#[test]
fn interp_print_args_454() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(454, 455, 456) end", 0);
    assert_eq!(out, "454 455 456\n");
}
#[test]
fn interp_print_args_455() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(455, 456, 457) end", 0);
    assert_eq!(out, "455 456 457\n");
}
#[test]
fn interp_print_args_456() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(456, 457, 458) end", 0);
    assert_eq!(out, "456 457 458\n");
}
#[test]
fn interp_print_args_457() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(457, 458, 459) end", 0);
    assert_eq!(out, "457 458 459\n");
}
#[test]
fn interp_print_args_458() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(458, 459, 460) end", 0);
    assert_eq!(out, "458 459 460\n");
}
#[test]
fn interp_print_args_459() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(459, 460, 461) end", 0);
    assert_eq!(out, "459 460 461\n");
}
#[test]
fn interp_print_args_460() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(460, 461, 462) end", 0);
    assert_eq!(out, "460 461 462\n");
}
#[test]
fn interp_print_args_461() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(461, 462, 463) end", 0);
    assert_eq!(out, "461 462 463\n");
}
#[test]
fn interp_print_args_462() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(462, 463, 464) end", 0);
    assert_eq!(out, "462 463 464\n");
}
#[test]
fn interp_print_args_463() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(463, 464, 465) end", 0);
    assert_eq!(out, "463 464 465\n");
}
#[test]
fn interp_print_args_464() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(464, 465, 466) end", 0);
    assert_eq!(out, "464 465 466\n");
}
#[test]
fn interp_print_args_465() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(465, 466, 467) end", 0);
    assert_eq!(out, "465 466 467\n");
}
#[test]
fn interp_print_args_466() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(466, 467, 468) end", 0);
    assert_eq!(out, "466 467 468\n");
}
#[test]
fn interp_print_args_467() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(467, 468, 469) end", 0);
    assert_eq!(out, "467 468 469\n");
}
#[test]
fn interp_print_args_468() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(468, 469, 470) end", 0);
    assert_eq!(out, "468 469 470\n");
}
#[test]
fn interp_print_args_469() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(469, 470, 471) end", 0);
    assert_eq!(out, "469 470 471\n");
}
#[test]
fn interp_print_args_470() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(470, 471, 472) end", 0);
    assert_eq!(out, "470 471 472\n");
}
#[test]
fn interp_print_args_471() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(471, 472, 473) end", 0);
    assert_eq!(out, "471 472 473\n");
}
#[test]
fn interp_print_args_472() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(472, 473, 474) end", 0);
    assert_eq!(out, "472 473 474\n");
}
#[test]
fn interp_print_args_473() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(473, 474, 475) end", 0);
    assert_eq!(out, "473 474 475\n");
}
#[test]
fn interp_print_args_474() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(474, 475, 476) end", 0);
    assert_eq!(out, "474 475 476\n");
}
#[test]
fn interp_print_args_475() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(475, 476, 477) end", 0);
    assert_eq!(out, "475 476 477\n");
}
#[test]
fn interp_print_args_476() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(476, 477, 478) end", 0);
    assert_eq!(out, "476 477 478\n");
}
#[test]
fn interp_print_args_477() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(477, 478, 479) end", 0);
    assert_eq!(out, "477 478 479\n");
}
#[test]
fn interp_print_args_478() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(478, 479, 480) end", 0);
    assert_eq!(out, "478 479 480\n");
}
#[test]
fn interp_print_args_479() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(479, 480, 481) end", 0);
    assert_eq!(out, "479 480 481\n");
}
#[test]
fn interp_print_args_480() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(480, 481, 482) end", 0);
    assert_eq!(out, "480 481 482\n");
}
#[test]
fn interp_print_args_481() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(481, 482, 483) end", 0);
    assert_eq!(out, "481 482 483\n");
}
#[test]
fn interp_print_args_482() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(482, 483, 484) end", 0);
    assert_eq!(out, "482 483 484\n");
}
#[test]
fn interp_print_args_483() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(483, 484, 485) end", 0);
    assert_eq!(out, "483 484 485\n");
}
#[test]
fn interp_print_args_484() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(484, 485, 486) end", 0);
    assert_eq!(out, "484 485 486\n");
}
#[test]
fn interp_print_args_485() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(485, 486, 487) end", 0);
    assert_eq!(out, "485 486 487\n");
}
#[test]
fn interp_print_args_486() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(486, 487, 488) end", 0);
    assert_eq!(out, "486 487 488\n");
}
#[test]
fn interp_print_args_487() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(487, 488, 489) end", 0);
    assert_eq!(out, "487 488 489\n");
}
#[test]
fn interp_print_args_488() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(488, 489, 490) end", 0);
    assert_eq!(out, "488 489 490\n");
}
#[test]
fn interp_print_args_489() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(489, 490, 491) end", 0);
    assert_eq!(out, "489 490 491\n");
}
#[test]
fn interp_print_args_490() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(490, 491, 492) end", 0);
    assert_eq!(out, "490 491 492\n");
}
#[test]
fn interp_print_args_491() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(491, 492, 493) end", 0);
    assert_eq!(out, "491 492 493\n");
}
#[test]
fn interp_print_args_492() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(492, 493, 494) end", 0);
    assert_eq!(out, "492 493 494\n");
}
#[test]
fn interp_print_args_493() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(493, 494, 495) end", 0);
    assert_eq!(out, "493 494 495\n");
}
#[test]
fn interp_print_args_494() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(494, 495, 496) end", 0);
    assert_eq!(out, "494 495 496\n");
}
#[test]
fn interp_print_args_495() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(495, 496, 497) end", 0);
    assert_eq!(out, "495 496 497\n");
}
#[test]
fn interp_print_args_496() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(496, 497, 498) end", 0);
    assert_eq!(out, "496 497 498\n");
}
#[test]
fn interp_print_args_497() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(497, 498, 499) end", 0);
    assert_eq!(out, "497 498 499\n");
}
#[test]
fn interp_print_args_498() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(498, 499, 500) end", 0);
    assert_eq!(out, "498 499 500\n");
}
#[test]
fn interp_print_args_499() {
    use axolc_core::interpret;
    let (out, _diags) = interpret("fn main() print(499, 500, 501) end", 0);
    assert_eq!(out, "499 500 501\n");
}
