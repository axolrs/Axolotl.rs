// Owner: PascalElixir / axolrs (GitHub org)
// File: Auto-generated tests for string literal lexer.

use axolc_core::tokenize;

#[test]
fn str_lit_0() {
    let (tokens, _) = tokenize(r#""s0""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_1() {
    let (tokens, _) = tokenize(r#""s1""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_2() {
    let (tokens, _) = tokenize(r#""s2""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_3() {
    let (tokens, _) = tokenize(r#""s3""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_4() {
    let (tokens, _) = tokenize(r#""s4""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_5() {
    let (tokens, _) = tokenize(r#""s5""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_6() {
    let (tokens, _) = tokenize(r#""s6""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_7() {
    let (tokens, _) = tokenize(r#""s7""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_8() {
    let (tokens, _) = tokenize(r#""s8""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_9() {
    let (tokens, _) = tokenize(r#""s9""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_10() {
    let (tokens, _) = tokenize(r#""s10""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_11() {
    let (tokens, _) = tokenize(r#""s11""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_12() {
    let (tokens, _) = tokenize(r#""s12""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_13() {
    let (tokens, _) = tokenize(r#""s13""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_14() {
    let (tokens, _) = tokenize(r#""s14""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_15() {
    let (tokens, _) = tokenize(r#""s15""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_16() {
    let (tokens, _) = tokenize(r#""s16""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_17() {
    let (tokens, _) = tokenize(r#""s17""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_18() {
    let (tokens, _) = tokenize(r#""s18""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_19() {
    let (tokens, _) = tokenize(r#""s19""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_20() {
    let (tokens, _) = tokenize(r#""s20""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_21() {
    let (tokens, _) = tokenize(r#""s21""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_22() {
    let (tokens, _) = tokenize(r#""s22""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_23() {
    let (tokens, _) = tokenize(r#""s23""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_24() {
    let (tokens, _) = tokenize(r#""s24""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_25() {
    let (tokens, _) = tokenize(r#""s25""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_26() {
    let (tokens, _) = tokenize(r#""s26""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_27() {
    let (tokens, _) = tokenize(r#""s27""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_28() {
    let (tokens, _) = tokenize(r#""s28""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_29() {
    let (tokens, _) = tokenize(r#""s29""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_30() {
    let (tokens, _) = tokenize(r#""s30""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_31() {
    let (tokens, _) = tokenize(r#""s31""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_32() {
    let (tokens, _) = tokenize(r#""s32""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_33() {
    let (tokens, _) = tokenize(r#""s33""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_34() {
    let (tokens, _) = tokenize(r#""s34""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_35() {
    let (tokens, _) = tokenize(r#""s35""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_36() {
    let (tokens, _) = tokenize(r#""s36""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_37() {
    let (tokens, _) = tokenize(r#""s37""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_38() {
    let (tokens, _) = tokenize(r#""s38""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_39() {
    let (tokens, _) = tokenize(r#""s39""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_40() {
    let (tokens, _) = tokenize(r#""s40""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_41() {
    let (tokens, _) = tokenize(r#""s41""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_42() {
    let (tokens, _) = tokenize(r#""s42""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_43() {
    let (tokens, _) = tokenize(r#""s43""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_44() {
    let (tokens, _) = tokenize(r#""s44""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_45() {
    let (tokens, _) = tokenize(r#""s45""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_46() {
    let (tokens, _) = tokenize(r#""s46""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_47() {
    let (tokens, _) = tokenize(r#""s47""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_48() {
    let (tokens, _) = tokenize(r#""s48""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn str_lit_49() {
    let (tokens, _) = tokenize(r#""s49""#, 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::StrLit);
}

#[test]
fn raw_str_lit_0() {
    let (tokens, _) = tokenize("`r0`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_1() {
    let (tokens, _) = tokenize("`r1`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_2() {
    let (tokens, _) = tokenize("`r2`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_3() {
    let (tokens, _) = tokenize("`r3`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_4() {
    let (tokens, _) = tokenize("`r4`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_5() {
    let (tokens, _) = tokenize("`r5`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_6() {
    let (tokens, _) = tokenize("`r6`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_7() {
    let (tokens, _) = tokenize("`r7`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_8() {
    let (tokens, _) = tokenize("`r8`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_9() {
    let (tokens, _) = tokenize("`r9`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_10() {
    let (tokens, _) = tokenize("`r10`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_11() {
    let (tokens, _) = tokenize("`r11`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_12() {
    let (tokens, _) = tokenize("`r12`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_13() {
    let (tokens, _) = tokenize("`r13`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_14() {
    let (tokens, _) = tokenize("`r14`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_15() {
    let (tokens, _) = tokenize("`r15`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_16() {
    let (tokens, _) = tokenize("`r16`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_17() {
    let (tokens, _) = tokenize("`r17`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_18() {
    let (tokens, _) = tokenize("`r18`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_19() {
    let (tokens, _) = tokenize("`r19`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_20() {
    let (tokens, _) = tokenize("`r20`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_21() {
    let (tokens, _) = tokenize("`r21`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_22() {
    let (tokens, _) = tokenize("`r22`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_23() {
    let (tokens, _) = tokenize("`r23`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_24() {
    let (tokens, _) = tokenize("`r24`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_25() {
    let (tokens, _) = tokenize("`r25`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_26() {
    let (tokens, _) = tokenize("`r26`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_27() {
    let (tokens, _) = tokenize("`r27`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_28() {
    let (tokens, _) = tokenize("`r28`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_29() {
    let (tokens, _) = tokenize("`r29`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_30() {
    let (tokens, _) = tokenize("`r30`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_31() {
    let (tokens, _) = tokenize("`r31`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_32() {
    let (tokens, _) = tokenize("`r32`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_33() {
    let (tokens, _) = tokenize("`r33`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_34() {
    let (tokens, _) = tokenize("`r34`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_35() {
    let (tokens, _) = tokenize("`r35`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_36() {
    let (tokens, _) = tokenize("`r36`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_37() {
    let (tokens, _) = tokenize("`r37`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_38() {
    let (tokens, _) = tokenize("`r38`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_39() {
    let (tokens, _) = tokenize("`r39`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_40() {
    let (tokens, _) = tokenize("`r40`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_41() {
    let (tokens, _) = tokenize("`r41`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_42() {
    let (tokens, _) = tokenize("`r42`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_43() {
    let (tokens, _) = tokenize("`r43`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_44() {
    let (tokens, _) = tokenize("`r44`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_45() {
    let (tokens, _) = tokenize("`r45`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_46() {
    let (tokens, _) = tokenize("`r46`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_47() {
    let (tokens, _) = tokenize("`r47`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_48() {
    let (tokens, _) = tokenize("`r48`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

#[test]
fn raw_str_lit_49() {
    let (tokens, _) = tokenize("`r49`", 0);
    assert_eq!(tokens[0].kind, axolc_core::lexer::token::TokenKind::RawStrLit);
}

