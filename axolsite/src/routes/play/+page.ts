/*
  Owner: PascalElixir / axolrs (GitHub org)
  Brief: Loader for /play - marks the playground route as prerendered so the static build ships it; the wasm compiler loads client-side at runtime.
*/

export const prerender = true;
