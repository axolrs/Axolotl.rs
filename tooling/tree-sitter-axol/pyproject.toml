[build-system]
build-backend = "setuptools.build_meta"
requires = [ "setuptools>=62.4.0", "wheel" ]

[project]
authors = [ { email = "PascalElixir@proton.me", name = "PascalElixir" } ]
classifiers = [
  "Intended Audience :: Developers",
  "Topic :: Software Development :: Compilers",
  "Topic :: Text Processing :: Linguistic",
  "Typing :: Typed",
]
description = "Treesitter grammer for Axolotl Programming Language"
keywords = [ "incremental", "parsing", "tree-sitter", "axolotlgrammer" ]
license.text = "MIT"
name = "tree-sitter-axolotlgrammer"
readme = "README.md"
requires-python = ">=3.10"
version = "0.1.0-alpha"

[project.urls]
Funding = ""
Homepage = "https://github.com/tree-sitter/tree-sitter-axolotlgrammer"

[project.optional-dependencies]
core = [ "tree-sitter~=0.24" ]

[tool.cibuildwheel]
build = "cp310-*"
build-frontend = "build"
